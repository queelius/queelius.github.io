<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0"
     xmlns:atom="http://www.w3.org/2005/Atom"
     xmlns:content="http://purl.org/rss/1.0/modules/content/"
     xmlns:dc="http://purl.org/dc/elements/1.1/"
     xmlns:media="http://search.yahoo.com/mrss/">
  <channel>
    <title>News on metafunctor</title>
    <link>http://localhost:40461/post/</link>
    <description>Latest news and updates.</description>
    <language>en-us</language>
    <managingEditor>lex@metafunctor.com (Alex Towell)</managingEditor>
    <webMaster>lex@metafunctor.com (Alex Towell)</webMaster>
    
    <lastBuildDate>Fri, 27 Feb 2026 17:25:24 -0600</lastBuildDate>
    <atom:link href="http://localhost:40461/post/" rel="self" type="application/rss+xml" />
    <image>
      <url>http://localhost:40461/images/og-image.jpg</url>
      <title>metafunctor</title>
      <link>http://localhost:40461/</link>
    </image>
    
    <item>
      <title>Reverse-Process Synthetic Data Generation for Math Reasoning</title>
      <link>http://localhost:40461/post/rpsdg/</link>
      <pubDate>Tue, 25 Jun 2024 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/post/rpsdg/</guid>
      <description>Training LLMs on mathematical reasoning by inverting easy-to-solve problems: generate derivatives, reverse them into integration exercises with full step-by-step solutions.</description>
      <content:encoded><![CDATA[<p>Check out the (early) project and source code on <a href="https://github.com/queelius/RPSDG">GitHub</a>.</p>
<h2 id="the-idea">The idea</h2>
<p>Some problems are easy in one direction and hard in the other. Taking a derivative is mechanical. Finding an antiderivative can require genuine creativity. Generating a random expression and verifying a proof is easy. Discovering the proof is hard.</p>
<p>RPSDG (Reverse-Process Synthetic Data Generation) exploits this asymmetry. Run the easy direction with full step-by-step work, then reverse the result to get a hard problem with a known solution. You end up with process-supervised training data: not just the answer, but the entire derivation.</p>
<p>Richard Sutton&rsquo;s &ldquo;The Bitter Lesson&rdquo; argues that methods scaling with compute and data will eventually win. The bottleneck is high-quality data. A lot of the world&rsquo;s data is latent, the processes that generated it are not written down. In math, the way a proof was discovered is usually hidden behind a polished presentation. RPSDG is one way to manufacture that hidden process data.</p>
<h2 id="derivatives-to-integrals">Derivatives to Integrals</h2>
<p>Computing derivatives is mechanical. Integration often is not. That asymmetry gives us a data pipeline.</p>
<ol>
<li>
<p><strong>Start with known functions.</strong> Pick functions \( f(x) \) with closed-form derivatives: polynomials, trig, exponentials, logarithms. Vary complexity.</p>
</li>
<li>
<p><strong>Differentiate with full work shown.</strong> Take the derivative of \( f(x) \) to get \( f'(x) \), recording every step.</p>
</li>
<li>
<p><strong>Reverse the process.</strong> Now \( f'(x) \) is the problem and \( f(x) \) is the solution. The recorded steps, read backward, give you a worked integration example.</p>
</li>
</ol>
<p>By composing functions of varying complexity, you get integration problems of graduating difficulty. The training data comes with step-by-step solutions for free, because you generated it by running the easy direction.</p>
<h2 id="proofs-by-random-walk">Proofs by Random Walk</h2>
<p>The same idea works for theorem proving. Generating proofs is hard. Verifying them is (comparatively) easy.</p>
<ol>
<li>
<p><strong>Random walks in expression space.</strong> Start with a random expression \( e_{\text{start}} \). Apply rewrite rules \( r_1, r_2, \ldots, r_n \) to get a chain of intermediate expressions ending at \( e_{\text{end}} \).</p>
</li>
<li>
<p><strong>Read off the theorem.</strong> The pair \( (e_{\text{start}}, e_{\text{end}}) \) is a theorem. The chain of rewrites is its proof.</p>
</li>
<li>
<p><strong>Reverse when useful.</strong> Running the chain backward works too, especially when a complex step in one direction (integration) becomes simple in the other (differentiation).</p>
</li>
<li>
<p><strong>Scale it.</strong> Random starting points and random rewrite sequences give you a diverse set of theorems and proofs automatically. No human has to come up with the theorem first.</p>
</li>
</ol>
<h2 id="what-this-gets-you">What this gets you</h2>
<p>The training data has process supervision baked in. Every example includes intermediate steps, not just the final answer. That should help LLMs learn multi-step reasoning rather than pattern-matching to answers.</p>
<p>It also gives you something like explainability for free: the model&rsquo;s training data literally consists of step-by-step solutions, so the model has a better chance of producing step-by-step reasoning at inference time.</p>
<h2 id="limitations-and-next-steps">Limitations and next steps</h2>
<p>This is early work. The data generation pipeline exists, but I have not yet run the full fine-tuning experiments and benchmarks. The planned pipeline is:</p>
<ul>
<li>Data generation with graduating difficulty (curriculum learning)</li>
<li>Fine-tuning transformer-based LMs on the generated data</li>
<li>Self-supervised learning experiments</li>
<li>Evaluation against standard math reasoning benchmarks</li>
</ul>
<p>Further out, I want to explore reinforcement learning for rewarding multi-step reasoning even when the solution is not known in advance but can be verified.</p>]]></content:encoded>
      
      
      <category>artificial intelligence</category>
      
      <category>machine learning</category>
      
      <category>mathematics</category>
      
      <category>algebra</category>
      
      <category>calculus</category>
      
      <category>LLMs</category>
      
      <category>synthetic data</category>
      
      <category>data generation</category>
      
      <category>reasoning</category>
      
      <category>AI training</category>
      
      <category>explainable AI</category>
      
      
    </item>
    
    <item>
      <title>SLUUG Talk: Demystifying Large Language Models on Linux</title>
      <link>http://localhost:40461/post/gave-a-presentation-for-sluug-about-llms/</link>
      <pubDate>Fri, 23 Feb 2024 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/post/gave-a-presentation-for-sluug-about-llms/</guid>
      <description>Talk for the St. Louis Unix Users Group about running and understanding Large Language Models on Linux.</description>
      <content:encoded><![CDATA[<p>I gave a talk for the St. Louis Unix Users Group (SLUUG) titled &ldquo;Demystifying Large Language Models (LLMs) on Linux: From Theory to Application.&rdquo; The goal was to walk through how LLMs actually work and how to run them locally on Linux.</p>
<p>I demoed two projects:</p>
<ol>
<li>
<p>A simple Colab notebook using basic Python to generate text with an n-gram model. The point was to illustrate the core idea behind language models and show concretely why n-grams fall short, which motivates the transformer architecture.</p>
</li>
<li>
<p>A project that uses ElasticSearch and LLMs to allow natural language search queries over databases.</p>
</li>
</ol>
<p>The talk went well. Content and code are on <a href="https://github.com/queelius/sluug-talk-llm">GitHub</a>.</p>
]]></content:encoded>
      
      
      <category>llm</category>
      
      <category>linux</category>
      
      <category>ai</category>
      
      <category>presentation</category>
      
      
    </item>
    
    <item>
      <title>Master&#39;s Project: Reliability Estimation in Series Systems</title>
      <link>http://localhost:40461/post/masters-stats-siue-proj/</link>
      <pubDate>Mon, 19 Feb 2024 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/post/masters-stats-siue-proj/</guid>
      <description>My master&#39;s project on maximum likelihood estimation for series systems with right-censored and masked failure data.</description>
      <content:encoded><![CDATA[<p>I presented my master&rsquo;s project in October 2023, finishing up my MS in statistics/mathematics at SIUE. The associated paper is titled &ldquo;Reliability Estimation in Series Systems: Maximum Likelihood Techniques for Right-Censored and Masked Failure Data.&rdquo;</p>
<h2 id="the-problem">The Problem</h2>
<p>In reliability engineering, you often find yourself in an annoying situation: a system fails, but you do not know which component caused the failure. This is called <em>masked failure data</em>. On top of that, some systems are still running when you stop observing them, so you only know they survived at least that long. That is <em>right censoring</em>. Both are common in practice. Identifying the exact failed component is expensive or sometimes impossible.</p>
<p>The project builds a likelihood-based framework that handles both masking and censoring simultaneously, models component lifetimes with Weibull distributions, derives closed-form Fisher information for the exponential special case, and provides bootstrap methods for uncertainty quantification. I implemented it all in an R package so practitioners can actually use it.</p>
<h2 id="related-work">Related Work</h2>
<p>This connects to several other posts and projects:</p>
<ul>
<li><a href="/post/expo-masked-fim/">Closed-Form Results for Masked Exponential Series Systems</a> covers the exponential distribution special case with analytical solutions</li>
<li><a href="/projects/likelihood.model/">likelihood.model R package</a> is the software implementation</li>
</ul>
<p>See the full project page <a href="/projects/reliability-estimation-in-series-systems/">here</a>.</p>
]]></content:encoded>
      
      
      <category>series systems</category>
      
      <category>masked failure data</category>
      
      <category>censoring</category>
      
      <category>reliability analysis</category>
      
      <category>maximum likelihood</category>
      
      <category>Weibull distribution</category>
      
      <category>R</category>
      
      
    </item>
    
    <item>
      <title>Fine-Tuning a Tiny LLM for ElasticSearch DSL</title>
      <link>http://localhost:40461/post/llm-fine-tuning-es-dsl/</link>
      <pubDate>Mon, 19 Feb 2024 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/post/llm-fine-tuning-es-dsl/</guid>
      <description>Fine-tuning a small language model to generate ElasticSearch DSL queries from natural language, as a proof of concept for domain-specific LLM specialization.</description>
      <content:encoded><![CDATA[<p>I am fine-tuning a small LLM to generate ElasticSearch DSL queries from natural language. The project is on <a href="https://github.com/queelius/elasticsearch-lm">GitHub</a>.</p>
<p>The idea: take a task that large models handle well (translating &ldquo;find all orders over $100 from last month&rdquo; into the corresponding ElasticSearch JSON query) and see if a tiny model can learn it from synthetic data.</p>
<p>The data pipeline works like this. I started by generating examples from GPT-4. Then I wrote a script that samples from those outputs and uses them as few-shot examples for Mistral, which generates a much larger synthetic dataset. The next step is reshaping the data into the expected format and fine-tuning.</p>
<p>This is early stage. The synthetic data exists, the fine-tuning has not happened yet. I will update this post with results.</p>
<p>If you are interested in collaborating on this or related projects, email me at <a href="mailto:lex@metafunctor.com">lex@metafunctor.com</a>.</p>
]]></content:encoded>
      
      
      <category>large language models</category>
      
      <category>fine-tuning</category>
      
      <category>information retrieval</category>
      
      <category>elastic search</category>
      
      <category>domain-specific language</category>
      
      <category>json</category>
      
      
    </item>
    
    <item>
      <title>Narrating a Hugo Blog with Sentence Highlighting</title>
      <link>http://localhost:40461/post/2026-02-26-narro-hugo-integration/</link>
      <pubDate>Thu, 26 Feb 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/post/2026-02-26-narro-hugo-integration/</guid>
      <description>narro can now generate audio narration for Hugo blog posts, with synchronized sentence highlighting in the browser. The alignment problem turned out to be more interesting than expected.</description>
      <content:encoded><![CDATA[<p>I wanted my blog posts to have audio narration. Not a podcast, not a read-aloud button that sends text to a cloud API. Local TTS with <a href="https://github.com/queelius/narro">narro</a>, my 80M parameter CPU model, generating Opus files that live next to the markdown source. One command to narrate an entire Hugo site.</p>
<p>That part was straightforward. The part that got interesting was highlighting: tracking which sentence is being spoken and lighting it up in the browser as the audio plays.</p>
<hr>
<h2 id="the-pipeline">The Pipeline</h2>
<p>Three commands:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">narro hugo install ~/mysite    <span class="c1"># copy player assets (JS, CSS, HTML partial)</span>
</span></span><span class="line"><span class="cl">narro hugo generate ~/mysite   <span class="c1"># narrate all posts with tts: true</span>
</span></span><span class="line"><span class="cl">narro hugo status ~/mysite     <span class="c1"># show what&#39;s been narrated and what hasn&#39;t</span>
</span></span></code></pre></div><p><code>generate</code> walks the content directory, finds posts with <code>tts: true</code> in frontmatter, extracts the prose (stripping code blocks, math, shortcodes, frontmatter), runs it through narro, converts to Opus via ffmpeg, and drops <code>narration.opus</code> and <code>narration.json</code> next to the <code>index.md</code>. The JSON file contains sentence-level timestamps. The HTML partial picks them up and wires everything together.</p>
<p>The player is a vanilla JS widget. No dependencies. Play, pause, spacebar toggle. The active paragraph highlights as the audio plays.</p>
<h2 id="the-alignment-problem">The Alignment Problem</h2>
<p>Timestamps are the interesting part. Narro uses a causal language model (Qwen-based, 80M params) to generate hidden states, then a Vocos decoder converts those to audio. There is no explicit alignment signal in this architecture.</p>
<p>My first attempt tried word-level timestamps using attention weights. The idea: each generated audio token attends to input text tokens, so you can compute a center-of-mass over the attention distribution to estimate when each word is spoken. This works beautifully for encoder-decoder models with cross-attention. It does not work for causal LM self-attention.</p>
<p>Causal self-attention attends to everything that came before. The distributions are diffuse. Every word&rsquo;s center-of-mass lands somewhere in the middle of the sequence, producing overlapping time ranges that are useless for highlighting.</p>
<h2 id="sentence-level-timestamps">Sentence-Level Timestamps</h2>
<p>The fix was to stop trying to be precise about words and use the precision the model actually gives you.</p>
<p>Narro processes each sentence independently. The number of generated tokens per sentence is known exactly. Multiply by the token duration (2048 samples / 32kHz = 64ms per token) and you get exact sentence timing. No approximation, no heuristics. The timestamps are ground truth.</p>
<p>The alignment JSON is one entry per sentence:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="p">[</span>
</span></span><span class="line"><span class="cl">  <span class="p">{</span><span class="nt">&#34;text&#34;</span><span class="p">:</span> <span class="s2">&#34;Hello world.&#34;</span><span class="p">,</span> <span class="nt">&#34;start&#34;</span><span class="p">:</span> <span class="mf">0.0</span><span class="p">,</span> <span class="nt">&#34;end&#34;</span><span class="p">:</span> <span class="mf">0.64</span><span class="p">},</span>
</span></span><span class="line"><span class="cl">  <span class="p">{</span><span class="nt">&#34;text&#34;</span><span class="p">:</span> <span class="s2">&#34;Goodbye moon.&#34;</span><span class="p">,</span> <span class="nt">&#34;start&#34;</span><span class="p">:</span> <span class="mf">0.64</span><span class="p">,</span> <span class="nt">&#34;end&#34;</span><span class="p">:</span> <span class="mf">1.6</span><span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">]</span>
</span></span></code></pre></div><p>I initially tried distributing each sentence&rsquo;s duration across its words proportionally by character count. It worked, but the DOM manipulation was fragile. Wrapping every word in a <code>&lt;span&gt;</code>, walking the tree to skip headings and code blocks, matching word indices sequentially to alignment entries. If any word got missed or doubled, every subsequent highlight drifted.</p>
<p>Sentence-level is better. The JS player matches sentences to existing <code>&lt;p&gt;</code> elements by substring containment. The DOM stays untouched. The highlighting is a CSS class toggled on the paragraph. Simpler code, no drift, and the visual result is actually clearer to read.</p>
<h2 id="the-browser-side">The Browser Side</h2>
<p>The JS player runs a binary search on the alignment data at 60fps via <code>requestAnimationFrame</code> and toggles a CSS class on the active paragraph. That is essentially the whole thing. No DOM rewriting, no word wrapping, no skip-tag lists.</p>
<h2 id="what-it-sounds-like">What It Sounds Like</h2>
<p>Every post on this blog with <code>tts: true</code> in frontmatter has a player at the top. Hit play. The active paragraph highlights as it is spoken. The model runs on CPU in about 20x real-time.</p>
<p>The source is at <a href="https://github.com/queelius/narro">github.com/queelius/narro</a>.</p>]]></content:encoded>
      
      
      <category>narro</category>
      
      <category>hugo</category>
      
      <category>tts</category>
      
      <category>open-source</category>
      
      <category>python</category>
      
      
    </item>
    
    <item>
      <title>Chartfold: Owning Your Medical Records</title>
      <link>http://localhost:40461/post/2026-02-24-chartfold-owning-your-medical-records/</link>
      <pubDate>Tue, 24 Feb 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/post/2026-02-24-chartfold-owning-your-medical-records/</guid>
      <description>A walkthrough of Chartfold, a Python tool that loads your medical records into SQLite and exposes them to Claude via MCP for structured analysis, visit prep, and ad-hoc queries.</description>
      <content:encoded><![CDATA[<p>I have cancer. My oncologist is at one hospital system (Siteman/BJC), my primary care doctor at another, and my earlier treatment history lives at a third (Anderson, where my first oncologist practiced). Patient portals are fine for browsing, but they don&rsquo;t answer questions. They show you your data one lab result at a time, one note at a time, one visit at a time.</p>
<p>I wanted to run queries against my medical records. Correlate lab trends with treatment changes. Generate structured question lists before oncology visits. Ask &ldquo;what changed since my last appointment&rdquo; and get a real answer. That means getting the data out of the portal and into something programmable.</p>
<p>Chartfold loads EHR exports into SQLite and exposes them to Claude via MCP.</p>
<hr>
<h2 id="the-problem">The Problem</h2>
<p>In the US, patients can export their medical records. HIPAA and the 21st Century Cures Act guarantee this. What you get depends on the system: Epic MyChart gives you CDA XML files, MEDITECH Expanse gives you FHIR JSON mixed with CCDA XML, athenahealth gives you FHIR R4 Bundles. Different formats, same clinical concepts.</p>
<p>If your hospitals use different EHR systems, none of them have the complete picture. Chartfold merges the exports into one database. But even if you&rsquo;re at a single hospital, the export format is not something you can work with directly. A directory of CDA XML files is not a database. You can&rsquo;t query it, chart it, or hand it to an LLM.</p>
<p>The point of Chartfold is to turn whatever your hospital gives you into a SQLite database, then make that database useful.</p>
<h2 id="what-it-does">What It Does</h2>
<p>Chartfold is a Python CLI. You point it at an EHR export directory, it parses the XML/FHIR, normalizes everything into a common data model (16 tables, ISO dates, deduplicated), and loads it into SQLite. Then you can query it directly, export it as a self-contained HTML dashboard, or connect Claude to it via MCP.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Load data from your hospital exports</span>
</span></span><span class="line"><span class="cl">chartfold load epic ~/exports/epic/
</span></span><span class="line"><span class="cl">chartfold load meditech ~/exports/meditech/
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Query directly</span>
</span></span><span class="line"><span class="cl">chartfold query <span class="s2">&#34;SELECT test_name, value, result_date FROM lab_results
</span></span></span><span class="line"><span class="cl"><span class="s2">                 WHERE test_name LIKE &#39;%CEA%&#39; ORDER BY result_date DESC&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Export a self-contained HTML file</span>
</span></span><span class="line"><span class="cl">chartfold <span class="nb">export</span> html --output chartfold.html
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Start the MCP server for Claude</span>
</span></span><span class="line"><span class="cl">chartfold serve-mcp
</span></span></code></pre></div><hr>
<h2 id="the-claude-integration">The Claude Integration</h2>
<p>This is why Chartfold exists for me.</p>
<p>The MCP server exposes the database to Claude Code. Setup is one file. Drop a <code>.mcp.json</code> in any directory where you run Claude Code:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;mcpServers&#34;</span><span class="p">:</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&#34;chartfold&#34;</span><span class="p">:</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">      <span class="nt">&#34;command&#34;</span><span class="p">:</span> <span class="s2">&#34;python&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">      <span class="nt">&#34;args&#34;</span><span class="p">:</span> <span class="p">[</span><span class="s2">&#34;-m&#34;</span><span class="p">,</span> <span class="s2">&#34;chartfold&#34;</span><span class="p">,</span> <span class="s2">&#34;serve-mcp&#34;</span><span class="p">,</span> <span class="s2">&#34;--db&#34;</span><span class="p">,</span> <span class="s2">&#34;/path/to/chartfold.db&#34;</span><span class="p">]</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">  <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>That&rsquo;s it. Claude now has read access to your entire medical history via SQL, plus tools for saving notes and structured analyses. I keep my database in a private directory and my <code>.mcp.json</code> pointing at it. Open Claude Code, and I&rsquo;m talking to my records.</p>
<p>The kinds of things I actually use it for:</p>
<blockquote>
<p>&ldquo;What&rsquo;s changed since my last oncology visit on January 15?&rdquo;</p>
</blockquote>
<p>Claude writes SQL, reads the results, and gives me a structured diff: new lab results, new imaging, changed medications, new clinical notes.</p>
<blockquote>
<p>&ldquo;Generate a prioritized question list for my appointment with Dr. Tan tomorrow.&rdquo;</p>
</blockquote>
<p>Claude reads my recent labs, imaging reports, pathology, and genomic results, then produces a tiered document organized by clinical urgency.</p>
<blockquote>
<p>&ldquo;Show me my CEA trend and flag any inflection points.&rdquo;</p>
</blockquote>
<p>Claude queries the lab_results table, filters by test name, and walks through the time series.</p>
<p>The analyses get saved back to the database (via dedicated MCP tools) and appear in the HTML export as tagged, searchable documents.</p>
<p><img src="analysis-cards.png" alt="Analysis section showing Claude-generated documents: prioritized questions for an oncologist visit, molecular profile summary, and deep clinical analysis"></p>
<p>Here&rsquo;s what one looks like expanded: a structured question list for an oncology appointment, organized by urgency tier, referencing specific test results and treatment options.</p>
<p><img src="analysis.png" alt="Full analysis document showing tiered questions for Dr. Tan, covering Guardant tissue results, liquid biopsy timing, BRCA1 methylation, and treatment planning"></p>
<p>I use this before every oncology visit. When you have 1776 lab results, 53 imaging reports, and 9 pathology reports, you need something to synthesize them. That&rsquo;s what Claude does well, but it needs structured data to work with. Chartfold provides the structured data. Claude provides the reasoning.</p>
<p>The MCP server exposes 25 tools. Here&rsquo;s the full list:</p>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>What it does</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>run_sql</code></td>
          <td>Execute arbitrary read-only SQL against the database</td>
      </tr>
      <tr>
          <td><code>get_schema</code></td>
          <td>Get CREATE TABLE DDL for query planning</td>
      </tr>
      <tr>
          <td><code>get_database_summary</code></td>
          <td>Table counts and load history (start here)</td>
      </tr>
      <tr>
          <td><code>query_labs</code></td>
          <td>Lab results filtered by test name, date, source, LOINC</td>
      </tr>
      <tr>
          <td><code>get_lab_series_tool</code></td>
          <td>Cross-source time series for a specific test</td>
      </tr>
      <tr>
          <td><code>get_available_tests_tool</code></td>
          <td>All lab tests with frequency and date range</td>
      </tr>
      <tr>
          <td><code>get_abnormal_labs_tool</code></td>
          <td>All flagged-abnormal results</td>
      </tr>
      <tr>
          <td><code>get_medications</code></td>
          <td>Medication list, optionally filtered by status</td>
      </tr>
      <tr>
          <td><code>reconcile_medications_tool</code></td>
          <td>Cross-source medication reconciliation</td>
      </tr>
      <tr>
          <td><code>get_timeline</code></td>
          <td>Unified event timeline (encounters, procedures, imaging, labs, pathology)</td>
      </tr>
      <tr>
          <td><code>search_notes</code></td>
          <td>Full-text search across clinical notes</td>
      </tr>
      <tr>
          <td><code>get_pathology_report</code></td>
          <td>Retrieve a pathology report by ID</td>
      </tr>
      <tr>
          <td><code>get_visit_diff</code></td>
          <td>Everything new since a given date</td>
      </tr>
      <tr>
          <td><code>get_visit_prep</code></td>
          <td>Pre-appointment summary bundle</td>
      </tr>
      <tr>
          <td><code>get_surgical_timeline</code></td>
          <td>Procedures linked to pathology, imaging, and meds</td>
      </tr>
      <tr>
          <td><code>match_cross_source_encounters</code></td>
          <td>Same-day encounters across different EHR systems</td>
      </tr>
      <tr>
          <td><code>get_data_quality_report</code></td>
          <td>Duplicate detection and source coverage matrix</td>
      </tr>
      <tr>
          <td><code>get_source_files</code></td>
          <td>Find PDFs and images linked to clinical records</td>
      </tr>
      <tr>
          <td><code>get_asset_summary</code></td>
          <td>Source asset counts by type and source</td>
      </tr>
      <tr>
          <td><code>save_note</code> / <code>get_note</code> / <code>search_notes_personal</code> / <code>delete_note</code></td>
          <td>Personal notes (CRUD)</td>
      </tr>
      <tr>
          <td><code>save_analysis</code> / <code>get_analysis</code> / <code>search_analyses</code> / <code>list_analyses</code> / <code>delete_analysis</code></td>
          <td>Structured analyses (CRUD)</td>
      </tr>
  </tbody>
</table>
<p>Clinical data is read-only (the SQLite connection opens in <code>?mode=ro</code>, enforced at the engine level). Claude can&rsquo;t modify your clinical records, only read them and save its own notes and analyses.</p>
<p>Most of these tools exist so Claude doesn&rsquo;t have to write SQL for common tasks. But <code>run_sql</code> is the escape hatch: anything the specialized tools don&rsquo;t cover, Claude can query directly.</p>
<hr>
<h2 id="the-html-dashboard">The HTML Dashboard</h2>
<p>The HTML export embeds the entire SQLite database using sql.js (SQLite compiled to WebAssembly). Open the file in a browser and you get an interactive dashboard. Everything runs client-side. No server, no cloud, no account. The file never phones home.</p>
<p><img src="overview.png" alt="Dashboard overview showing record counts: 1776 lab results, 93 medications, 51 conditions, 99 encounters, 53 imaging reports, and more"></p>
<h3 id="lab-charts">Lab Charts</h3>
<p>Lab charts show time-series data across sources with reference ranges. Here, creatinine and albumin are tracked over four years across MEDITECH (blue) and Epic (orange).</p>
<p><img src="lab-charts.png" alt="Cross-source lab charts for Creatinine and Albumin, showing data series from two hospital systems with reference range bands"></p>
<p>For patients who do deal with fragmented records across incompatible systems, this is the view that doesn&rsquo;t exist in any single portal. The charts are configurable via a TOML file, which you can auto-generate from your data:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">chartfold init-config
</span></span></code></pre></div><h3 id="conditions">Conditions</h3>
<p>Conditions with ICD-10 codes, onset dates, and source provenance.</p>
<p><img src="conditions.png" alt="Conditions table showing 51 conditions with status, ICD-10 codes, onset dates, and source system"></p>
<h3 id="medications">Medications</h3>
<p>Medications show &ldquo;Multi-source&rdquo; badges when the same drug appears in multiple systems.</p>
<p><img src="medications.png" alt="Active medications list with multi-source badges"></p>
<h3 id="imaging">Imaging</h3>
<p>Imaging reports with the full narrative findings. Useful for visit prep: search for a specific study, read the impression, bring context.</p>
<p><img src="imaging.png" alt="Imaging section showing CT and MRI reports with narrative findings"></p>
<h3 id="source-documents">Source Documents</h3>
<p>Source PDFs and scanned documents grouped by date.</p>
<p><img src="sources.png" alt="Source documents grouped by date, showing PDF assets from EHR exports"></p>
<h3 id="sql-console">SQL Console</h3>
<p>For anything the UI doesn&rsquo;t cover, there&rsquo;s a SQL console. Every table, every column, every index.</p>
<p><img src="sql-console.png" alt="SQL Console showing the database schema"></p>
<h3 id="dark-mode">Dark Mode</h3>
<p><img src="dark-mode.png" alt="Dark mode overview"></p>
<hr>
<h2 id="architecture">Architecture</h2>
<p>Three-stage pipeline, each stage independently testable:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">Raw EHR files (CDA XML, FHIR JSON, CCDA XML)
</span></span><span class="line"><span class="cl">    |
</span></span><span class="line"><span class="cl">    v
</span></span><span class="line"><span class="cl">[Source Parser]  -- format-specific extraction
</span></span><span class="line"><span class="cl">    |
</span></span><span class="line"><span class="cl">    v
</span></span><span class="line"><span class="cl">[Adapter]        -- normalize to UnifiedRecords (16 dataclass types)
</span></span><span class="line"><span class="cl">    |
</span></span><span class="line"><span class="cl">    v
</span></span><span class="line"><span class="cl">[DB Loader]      -- idempotent upsert into SQLite
</span></span></code></pre></div><p>Source parsers handle the XML/FHIR parsing. Adapters normalize dates to ISO 8601, parse numeric values, deduplicate, and map into a common data model. The DB loader uses upsert with natural keys, so re-running a load is safe.</p>
<p>After loading, the CLI prints a stage comparison table: parser count, adapter count, DB count. If the numbers don&rsquo;t match, you know where data was lost.</p>
<p>Currently supports Epic (CDA), MEDITECH (FHIR + CCDA), and athenahealth (FHIR R4). These importers were written against my own exports. I can&rsquo;t guarantee they&rsquo;ll work for yours. EHR exports vary by site, software version, and configuration. The pipeline is designed as a plugin system for exactly this reason: adding a new source means writing a parser, an adapter, and wiring them into the CLI. The CLAUDE.md has a recipe, and Claude can write a new importer from a sample export in about an hour.</p>
<hr>
<h2 id="export-formats">Export Formats</h2>
<ul>
<li><strong>HTML SPA</strong>: self-contained single file with embedded SQLite, Chart.js, and sql.js. No external dependencies. Copy it to a USB drive.</li>
<li><strong>Markdown</strong>: visit-focused summary with configurable lookback, optional PDF via pandoc.</li>
<li><strong>JSON</strong>: full-fidelity round-trip format. Export, then import to a new database with identical record counts.</li>
<li><strong>Hugo site</strong>: static site with detail pages and cross-linked records.</li>
<li><strong>Arkiv</strong>: universal record format (JSONL + manifest) for long-term archival.</li>
</ul>
<p>The HTML export is a single file. No server, no backend, no account. You can host it on a static site (I host mine on GitHub Pages), email it to a family member, or hand it to a doctor on a USB drive. Because it&rsquo;s just a file, you can protect it with <a href="/post/2026-02-13-pagevault/">PageVault</a> to add password-based encryption before sharing. The recipient opens the file, enters the password, and gets the full interactive dashboard. No server involved at any step.</p>
<p>Medical records should not depend on someone else&rsquo;s infrastructure. A single HTML file with an embedded database and WebAssembly runtime is about as durable as digital data gets.</p>
<hr>
<h2 id="getting-started">Getting Started</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">pip install chartfold
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Load your exports</span>
</span></span><span class="line"><span class="cl">chartfold load auto ~/path/to/export/
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Query</span>
</span></span><span class="line"><span class="cl">chartfold query <span class="s2">&#34;SELECT test_name, value, result_date FROM lab_results LIMIT 10&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Export</span>
</span></span><span class="line"><span class="cl">chartfold <span class="nb">export</span> html --output my-records.html
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Connect Claude</span>
</span></span><span class="line"><span class="cl">chartfold serve-mcp
</span></span></code></pre></div><p>The code is on GitHub: <a href="https://github.com/queelius/chartfold">queelius/chartfold</a>. Python 3.11+, depends on lxml and not much else.</p>
<hr>
<p><em>Chartfold started because I wanted to ask questions about my own medical records and couldn&rsquo;t. Now I can.</em></p>]]></content:encoded>
      
      
      <category>chartfold</category>
      
      <category>python</category>
      
      <category>ehr</category>
      
      <category>sqlite</category>
      
      <category>health-data</category>
      
      <category>open-source</category>
      
      <category>mcp</category>
      
      <category>claude</category>
      
      
    </item>
    
    <item>
      <title>Code Without Purpose</title>
      <link>http://localhost:40461/post/2026-02-25-code-without-purpose/</link>
      <pubDate>Tue, 24 Feb 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/post/2026-02-25-code-without-purpose/</guid>
      <description>The problem isn&#39;t too much code. It&#39;s code without purpose.</description>
      <content:encoded><![CDATA[<p>Time is finite in ways I can&rsquo;t ignore. That changes which questions about code feel important.</p>
<p>I read a post arguing that the most valuable programming skill in 2026 is deleting code. The thesis: AI generates code faster than anyone can review it, so the real value is in curation and subtraction. Code is a liability, not an asset.</p>
<p>I agree with the observation. I disagree with the prescription.</p>
<hr>
<h2 id="the-thesis">The Thesis</h2>
<p>The argument is straightforward. AI tools can produce entire modules in the time it takes to write a spec. Codebases are accumulating features nobody asked for, abstractions nobody needs, and boilerplate that exists because the model defaulted to verbosity. Teams that used to struggle to ship enough code now struggle with too much of it. In this world, the programmer&rsquo;s role shifts from writer to editor. The most valuable activity becomes knowing what to cut.</p>
<p>I&rsquo;ve seen this. Projects accumulate code the way attics accumulate boxes. Nobody remembers why half of it is there. It sits untouched for months, adding to cognitive load, making every change harder to reason about. When you finally clear it out, nothing breaks and the rest becomes legible again. Code rot is real. AI accelerates it. The instinct to subtract is correct.</p>
<p>But subtraction is symptom treatment. The underlying problem isn&rsquo;t volume. It&rsquo;s that most code doesn&rsquo;t know why it exists. It was written (or generated) to solve a local problem, it solved it or half-solved it, and then it sat there, disconnected from any larger purpose. Code without purpose is what bloats. Deletion is the right instinct pointed at the wrong layer. The cause isn&rsquo;t too much code. It&rsquo;s too little intent.</p>
<h2 id="what-i-found-instead">What I Found Instead</h2>
<p>I started building tools two years ago because I needed my personal data to survive even if I wasn&rsquo;t around to maintain it. That&rsquo;s the whole constraint. Whatever I build has to work without me.</p>
<p>The first tool was a conversation archiver. I had years of AI conversations across ChatGPT, Claude, and Copilot trapped in platforms that might not exist next decade. I needed them in formats that degrade gracefully. SQLite for structured queries. JSONL for streaming and interchange. Markdown for human reading. If the tool disappears, the data is still a file you can open with anything. If SQLite disappears, the JSONL is still searchable with grep. If everything disappears, the Markdown is still readable in a text editor. Each layer works without the one above it.</p>
<p>Then I needed the same thing for bookmarks. Then ebooks. Then photos, email, medical records, notes. Each tool exists because the previous one exposed a gap. The medical records needed secure sharing without a server. The whole collection needed a dead man&rsquo;s switch. The archive eventually needed something stranger: a way to answer questions after I couldn&rsquo;t.</p>
<p>None of this was planned as an ecosystem. I built the next thing I needed, and the next, and the next. At some point I looked back and realized they all pointed the same direction. This is a life project. Everything serves one purpose.</p>
<h2 id="the-stack">The Stack</h2>
<p>One purpose: durable personal data that outlasts its creator.</p>
<p><strong>Philosophy</strong></p>
<ul>
<li><a href="https://github.com/queelius/longecho">longecho</a>: The Long Echo specification. Self-describing data, durable formats, graceful degradation.</li>
</ul>
<p><strong>Universal Format</strong></p>
<ul>
<li><a href="https://github.com/queelius/arkiv">arkiv</a>: Universal personal data format. JSONL in, SQL out, SQL back to JSONL. Its MCP server can host any collection intelligently, regardless of domain. One format, one database, one query interface.</li>
</ul>
<p><strong>Source Toolkits</strong></p>
<ul>
<li><a href="https://github.com/queelius/memex">memex</a> / <a href="https://github.com/queelius/ctk">ctk</a>: AI conversations. Import, query, continue in the browser, export durable archives.</li>
<li>A family of domain toolkits for bookmarks, ebooks, photos, email, and notes. Different domains, identical architecture.</li>
<li><a href="https://github.com/queelius/chartfold">chartfold</a>: Medical records from three hospital systems, consolidated into one queryable database.</li>
</ul>
<p><strong>Infrastructure</strong></p>
<ul>
<li><a href="https://github.com/queelius/pagevault">pagevault</a>: Client-side encryption for any HTML file. No server.</li>
<li><a href="https://github.com/queelius/posthumous">posthumous</a>: Federated dead man&rsquo;s switch.</li>
<li><a href="https://github.com/queelius/repoindex">repoindex</a>: Index and query across ~120 git repos.</li>
<li>A collection of Claude Code plugins: MCP servers, agents, and skills that wire everything into my daily workflow.</li>
</ul>
<p><strong>The Endgame</strong></p>
<ul>
<li><a href="https://github.com/queelius/eidola">eidola</a>: A conversable persona assembled from all of the above. Its first form is a Claude Code plugin backed by the combined archive. Not resurrection. An echo.</li>
</ul>
<h2 id="the-pattern">The Pattern</h2>
<p>Every tool follows the same architecture. SQLite for storage. CLI for local use. MCP server for Claude, or the CLI wrapped in a light Claude Code plugin. Export to self-contained HTML you can host anywhere or open from a file. Export to longecho-compliant archives that work without the tool. The data always outlasts the software.</p>
<p>Take memex. Import your AI conversations, query them with SQL, talk to Claude about them via MCP, or export a single HTML file where you can browse and continue conversations in the browser. Download the SQLite from that same page, and you&rsquo;re back to durable local data. The cycle closes. This is how most of the tools work.</p>
<p>Arkiv sits in the middle. The source toolkits produce JSONL. Arkiv imports it to SQLite. Arkiv exports it back to JSONL. Its MCP server can expose any collection to Claude, regardless of what domain it came from. The data flows in a circle, always in formats that describe themselves.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-gdscript3" data-lang="gdscript3"><span class="line"><span class="cl"><span class="n">Source</span> <span class="n">toolkits</span> <span class="err">→</span> <span class="n">arkiv</span> <span class="err">→</span> <span class="n">longecho</span>
</span></span><span class="line"><span class="cl">                              <span class="err">↓</span>
</span></span><span class="line"><span class="cl">                  <span class="n">pagevault</span> <span class="p">(</span><span class="n">encrypt</span><span class="p">)</span> <span class="o">+</span> <span class="n">posthumous</span> <span class="p">(</span><span class="n">deliver</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">                              <span class="err">↓</span>
</span></span><span class="line"><span class="cl">                         <span class="n">eidola</span> <span class="p">(</span><span class="n">echo</span><span class="p">)</span>
</span></span></code></pre></div><p>I have never once needed to delete one of these tools. Not because I&rsquo;m a better programmer than anyone else. Because each one exists for a specific reason that connects to the others. When code has purpose, dead weight doesn&rsquo;t accumulate.</p>
<p>This isn&rsquo;t architectural foresight. It&rsquo;s what happens when you build from a clear constraint. &ldquo;My data has to survive without me&rdquo; is a filter that works at the design stage. Every tool either serves that constraint or it doesn&rsquo;t get built. There is no third category.</p>
<h2 id="the-actual-skill">The Actual Skill</h2>
<p>The most valuable skill isn&rsquo;t deleting code or writing it. It&rsquo;s knowing why you&rsquo;re building.</p>
<p>If you know the purpose, code stays minimal because unnecessary code doesn&rsquo;t serve it. You don&rsquo;t need periodic purges. The purpose does the culling before anything gets written. Deletion is what happens when purpose was absent from the start. It&rsquo;s retrospective correction for a problem that clear intent would have prevented.</p>
<p>I know what I&rsquo;m building toward. The tools will echo after I stop maintaining them. That was the point.</p>]]></content:encoded>
      
      
      <category>longecho</category>
      
      <category>open-source</category>
      
      <category>software-philosophy</category>
      
      <category>legacy</category>
      
      
    </item>
    
    <item>
      <title>Masked Failure Data: Looking Back, Looking Forward</title>
      <link>http://localhost:40461/post/2026-02-18-mle-looking-back-forward/</link>
      <pubDate>Wed, 18 Feb 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/post/2026-02-18-mle-looking-back-forward/</guid>
      <description>A retrospective on three years of building R packages and writing papers for masked series system reliability, and what comes next.</description>
      <content:encoded><![CDATA[<p>I have been working on the same statistical problem since 2020. I am now a PhD student in CS. The problem has not changed, but my understanding of it has, and the tools I have built around it look nothing like what I started with.</p>
<p>The problem: a series system fails when any component fails. You observe system-level failure times. But you often cannot tell which component caused the failure (masking). Some systems are still running when testing ends (censoring). Given this incomplete data, estimate component reliability.</p>
<p>This is not a tutorial. It is a map of where things stand and where they are going.</p>
<hr>
<h2 id="how-it-started">How It Started</h2>
<p>I picked this for my <a href="/post/2024-06-15-reliability-estimation-in-series-systems/">math master&rsquo;s thesis</a> because it combined everything I wanted to learn: survival analysis, mixture models, the EM algorithm, bootstrap inference, simulation studies. I focused on Weibull components with right censoring, but the likelihood derivation was distribution-agnostic. The general framework was there; I just did not treat it as the main contribution.</p>
<p>The implementation was one monolithic R package (<code>wei.series.md.c1.c2.c3</code>). It worked. It was not reusable. The distribution algebra, the MLE infrastructure, the likelihood model, the series system logic, the masking conditions: everything tangled together. Want to change the distribution family? Rewrite half the package.</p>
<p>I defended in October 2023. The thesis was fine. The code was a dead end.</p>
<h2 id="pulling-it-apart">Pulling It Apart</h2>
<p>After the defense, I started asking: what are the actual algebraic structures here?</p>
<p>It took a while, but the answer was several independent layers:</p>
<ol>
<li>
<p><strong>Distributions form an algebra.</strong> Add, scale, compose. That became <a href="/projects/algebraic.dist/">algebraic.dist</a>.</p>
</li>
<li>
<p><strong>MLEs form an algebra.</strong> Delta method, bootstrap, reparameterization. That became <a href="/projects/algebraic.mle/">algebraic.mle</a>.</p>
</li>
<li>
<p><strong>Likelihood models are composable.</strong> Heterogeneous data is a sum of likelihood contributions, one per observation type. That became <a href="/projects/likelihood.model/">likelihood.model</a>.</p>
</li>
<li>
<p><strong>MLE solvers are composable.</strong> Chain them, race them, add restarts. The SICP closure property: composing solvers gives you a solver. That became <a href="/projects/compositional.mle/">compositional.mle</a>.</p>
</li>
<li>
<p><strong>Hazard functions define distributions.</strong> Write \(h(t; \theta)\), get \(S(t)\), \(F(t)\), \(f(t)\), quantiles, sampling, and MLE for free. That became <a href="/projects/flexhaz/">flexhaz</a>. A companion, <a href="/projects/serieshaz/">serieshaz</a>, composes component hazards into series system distributions.</p>
</li>
</ol>
<p>The dependency graph:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">                algebraic.dist
</span></span><span class="line"><span class="cl">                 |          \
</span></span><span class="line"><span class="cl">             algebraic.mle    \
</span></span><span class="line"><span class="cl">              |        \       \
</span></span><span class="line"><span class="cl">compositional.mle   likelihood.model
</span></span><span class="line"><span class="cl">                      |         \
</span></span><span class="line"><span class="cl">                   flexhaz    maskedcauses
</span></span><span class="line"><span class="cl">                     |
</span></span><span class="line"><span class="cl">                  serieshaz
</span></span><span class="line"><span class="cl">                     |
</span></span><span class="line"><span class="cl">                  maskedhaz
</span></span></code></pre></div><p>None of this was planned. It emerged from repeatedly asking &ldquo;why is this code so hard to change?&rdquo; until the natural joints appeared.</p>
<h2 id="ad-and-nabla">AD and nabla</h2>
<p>Preparing the master&rsquo;s defense (which included BCa bootstrap confidence intervals) exposed the places where my understanding was mechanical rather than structural. I knew the formulas. I did not always know why they were the right formulas.</p>
<p>After the defense, I experimented with PyTorch&rsquo;s autograd for MLE. I was already familiar with the relationship between gradients and score functions, but running it in a computational graph solidified it. The score function is the gradient of the log-likelihood. The observed information is the negative Hessian. Same computation, different names.</p>
<p>That led me to build <a href="/projects/nabla/">nabla</a>, which I think is the most general AD package in R. Nested dual numbers, S4 dispatch. <code>D(f)</code> gives gradients. <code>D(D(f))</code> gives Hessians. Works through loops and branches. Arbitrary order, machine precision.</p>
<p>It is too slow for optimization. That is not the point. You find your MLE however you want (analytical, <code>optim</code>, whatever), then use <code>nabla</code> to characterize the solution: observed Fisher information, skewness analysis, higher-order diagnostics. The right tool for final analysis, not for search.</p>
<h2 id="the-foundation-paper">The Foundation Paper</h2>
<p>In 2025, I wrote the paper that should have been separate from the start.</p>
<p>The thesis had the distribution-agnostic likelihood derivation buried in a document mostly about Weibull. The <a href="https://github.com/queelius/masked-causes-in-series-systems">foundation paper</a> extracts that general framework and gives it a proper treatment. It handles all four observation types (exact, right-censored, left-censored, interval-censored) and defines three conditions on the masking mechanism:</p>
<ul>
<li><strong>C1</strong>: The candidate set always contains the true failed component.</li>
<li><strong>C2</strong>: Masking probabilities are symmetric across components.</li>
<li><strong>C3</strong>: Masking probabilities do not depend on the system parameters.</li>
</ul>
<p>Under C1-C2-C3, the likelihood factors cleanly. The paper derives the general likelihood, score equations, and Fisher information, then instantiates them for exponential, Weibull, Pareto, log-normal, and gamma families.</p>
<p>The <a href="https://github.com/queelius/expo-masked-fim">exponential companion</a> pushes the simplest case as far as the math goes. Closed-form MLE. Analytical Fisher information. A proof that information loss from masking is monotone (more masking, less information, strictly). And a result I did not expect: uniform masking maximizes the entropy of the candidate set given the failure cause. It is the worst case for identifiability among all C2-compliant masking models. The closed-form results are not just a convenient special case. They are pessimistic bounds.</p>
<hr>
<h2 id="where-things-are">Where Things Are</h2>
<h3 id="software">Software</h3>
<p>Six packages on CRAN, five more in progress:</p>
<table>
  <thead>
      <tr>
          <th>Package</th>
          <th>What It Does</th>
          <th>Status</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>algebraic.dist</code></td>
          <td>Distribution algebra</td>
          <td>On CRAN</td>
      </tr>
      <tr>
          <td><code>algebraic.mle</code></td>
          <td>MLE algebra</td>
          <td>On CRAN</td>
      </tr>
      <tr>
          <td><code>likelihood.model</code></td>
          <td>Fisherian likelihood framework</td>
          <td>On CRAN</td>
      </tr>
      <tr>
          <td><code>compositional.mle</code></td>
          <td>Composable MLE solvers</td>
          <td>On CRAN</td>
      </tr>
      <tr>
          <td><code>hypothesize</code></td>
          <td>Hypothesis testing framework</td>
          <td>On CRAN</td>
      </tr>
      <tr>
          <td><code>nabla</code></td>
          <td>Automatic differentiation (dual numbers)</td>
          <td>On CRAN</td>
      </tr>
      <tr>
          <td><code>flexhaz</code></td>
          <td>Distributions from hazard functions</td>
          <td>Targeting CRAN + JOSS</td>
      </tr>
      <tr>
          <td><code>serieshaz</code></td>
          <td>Series system distributions</td>
          <td>Targeting CRAN + JOSS</td>
      </tr>
      <tr>
          <td><code>maskedcauses</code></td>
          <td>Analytical MLE for masked series</td>
          <td>Targeting CRAN + JOSS</td>
      </tr>
      <tr>
          <td><code>maskedhaz</code></td>
          <td>Numerical MLE for masked series</td>
          <td>Targeting CRAN + JOSS</td>
      </tr>
      <tr>
          <td><code>mdrelax</code></td>
          <td>Relaxed masking conditions</td>
          <td>Paper first; package if it pans out</td>
      </tr>
  </tbody>
</table>
<p><code>maskedcauses</code> and <code>maskedhaz</code> solve the same problem at different levels of generality. <code>maskedcauses</code> has closed-form solutions for exponential and Weibull. <code>maskedhaz</code> handles arbitrary hazard functions via numerical integration. When both are installed, the test suites cross-validate.</p>
<p><code>mdrelax</code> explores relaxing the C1, C2, and C3 conditions: informative masking, parameter-dependent masking, masking probabilities less than one. Right now it is research code for a paper. If the results hold up, a proper package may come out of it.</p>
<h3 id="papers">Papers</h3>
<table>
  <thead>
      <tr>
          <th>Paper</th>
          <th>Status</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Foundation (C1-C2-C3 framework)</td>
          <td>Draft complete</td>
      </tr>
      <tr>
          <td>Exponential companion (closed-form FIM)</td>
          <td>Draft complete</td>
      </tr>
      <tr>
          <td>Model selection (LRT nesting chain)</td>
          <td>Draft complete, software in <code>maskedcauses</code> vignette</td>
      </tr>
      <tr>
          <td>Relaxed C1/C2/C3 conditions</td>
          <td>Draft in progress</td>
      </tr>
      <tr>
          <td>Master&rsquo;s thesis (original Weibull treatment)</td>
          <td>Published</td>
      </tr>
  </tbody>
</table>
<hr>
<h2 id="what-i-am-working-on-next">What I Am Working On Next</h2>
<p>I have five companion paper directions. Some are real; some are barely more than a paragraph.</p>
<h3 id="identifiability-active">Identifiability (active)</h3>
<p>The foundation paper proves a necessary and sufficient condition for identifiability from masked data. But it is a yes/no result. It says nothing about how much diagnostic separation you need in practice, which candidate set structures help most, or how identifiability degrades as masking gets heavier.</p>
<p>I want to give a graph-theoretic characterization (confounding graph, connected components as &ldquo;super-components&rdquo;), a linear algebra condition for the exponential case (rank of the candidate-set matrix), and simulation ablation studies.</p>
<p>The exponential companion connects directly: uniform masking at \(w = m-1\) is the most pessimistic identifiable scenario. So the question inverts: what masking design <em>minimizes</em> information loss? That is a D-optimality problem for the masking mechanism. I do not know the answer yet.</p>
<h3 id="observation-scheme-composition-idea">Observation scheme composition (idea)</h3>
<p><code>maskedcauses</code> implements composable observation functors: <code>observe_right_censor()</code>, <code>observe_left_censor()</code>, <code>observe_periodic()</code>, <code>observe_mixture()</code>. The mathematical content is that C1-C2-C3 are preserved under these compositions. If your masking mechanism satisfies the conditions, and you compose it with an independent censoring scheme, the composition still satisfies them.</p>
<p>The proofs are straightforward. The interesting question is whether there is a category-theoretic formulation. Observation schemes as morphisms, composition as functor composition. If so, the closure theorems become instances of a general principle rather than case-by-case arguments. I do not know if this leads anywhere useful or is just abstraction for its own sake.</p>
<h3 id="parsimony-vs-physical-structure-idea">Parsimony vs physical structure (idea)</h3>
<p>If the data are heavily masked and the sample is small, a single Weibull fits the system lifetime data about as well as an \(m\)-component series model. Standard model selection says: use the simpler model. Engineering knowledge says: the components exist.</p>
<p>This is a real tension. If you want to predict system lifetimes, maybe the single Weibull is fine. If you want to estimate component reliability for maintenance planning, the series decomposition matters even if it is over-parameterized relative to the data. I think this is a short paper. Maybe just an essay with simulations.</p>
<h3 id="weibull-companion-planned">Weibull companion (planned)</h3>
<p>The exponential companion has closed-form results because exponential lifetimes are memoryless. Weibull has shape parameters, which means time-varying hazards and numerical integration for some censoring types.</p>
<p>The thesis covers Weibull estimation. The companion paper would redo it properly in the foundation paper&rsquo;s notation: score equations, Fisher information, simulation studies, guidance for choosing homogeneous vs heterogeneous models. The theory exists in pieces across the thesis and <code>maskedcauses</code>. Writing it up as a standalone paper is substantial work.</p>
<h3 id="cran-and-joss-submissions">CRAN and JOSS submissions</h3>
<p>The immediate practical work. <code>flexhaz</code>, <code>serieshaz</code>, <code>maskedcauses</code>, and <code>maskedhaz</code> are all targeting both CRAN and JOSS. I have been through the CRAN gauntlet six times. It is tedious but not mysterious.</p>
<hr>
<h2 id="what-i-have-learned">What I Have Learned</h2>
<p>Three things.</p>
<p><strong>Decompose first.</strong> The monolithic thesis code worked but could not grow. Pulling it into algebraic layers took months. Every piece became independently testable, reusable, publishable. The decomposition is itself a research contribution, not just engineering convenience.</p>
<p><strong>Publish the general theory separately.</strong> The thesis had the distribution-agnostic framework buried in a Weibull-specific document. Writing the foundation paper forced me to separate what is structural (C1-C2-C3 factorization, observation type taxonomy) from what is distribution-specific. That separation made the companion papers possible.</p>
<p><strong>The Fisher information matrix is the right lens.</strong> For two years I treated FIM as &ldquo;the thing you invert to get standard errors.&rdquo; The exponential companion forced me to see it as a measure of how much information the observation scheme actually provides. Once I saw that, the identifiability results, the information loss monotonicity, and the optimal design questions all fell out naturally. FIM connects the statistical theory to the practical question of how to design better diagnostics.</p>
<hr>
<p>The work is not close to done. But the pieces are in place: a clean theoretical framework, a modular software stack, two completed papers, a clear map of what comes next. Most days, that is enough to keep going.</p>]]></content:encoded>
      
      
      <category>R</category>
      
      <category>statistics</category>
      
      <category>reliability</category>
      
      <category>series-systems</category>
      
      <category>masked-data</category>
      
      <category>maximum-likelihood</category>
      
      <category>CRAN</category>
      
      <category>MLE</category>
      
      <category>reflection</category>
      
      
    </item>
    
    <item>
      <title>dapple: Terminal Graphics, Composed</title>
      <link>http://localhost:40461/post/2026-02-15-dapple-composition-layout/</link>
      <pubDate>Sun, 15 Feb 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/post/2026-02-15-dapple-composition-layout/</guid>
      <description>dapple is a terminal graphics library with one Canvas API, seven pluggable renderers, and eleven CLI tools for displaying images, data, video, math, and more.</description>
      <content:encoded><![CDATA[<p>I live in the terminal. Most of my tools are CLIs. When I want to see something visual (an image, a plot, a table of results), I do not want to leave the terminal to see it.</p>
<p>Terminal graphics tools exist, but they are fragmented. One library does braille characters. Another does quadrant blocks. A third handles sixel. Each has its own API, its own conventions, its own way of thinking about the same problem.</p>
<p><a href="https://github.com/queelius/dapple">dapple</a> unifies them. One Canvas class, seven pluggable renderers, and eleven CLI tools built on top. The core depends only on numpy.</p>
<h2 id="the-idea">The Idea</h2>
<p>The insight is that &ldquo;render a bitmap to the terminal&rdquo; is a single problem with multiple encodings. Braille characters pack 2x4 dots per cell. Quadrant blocks give you 2x2 with color. Sextants give 2x3. Sixel and kitty give true pixels if your terminal supports them. These are all the same operation: map a grid of values to a grid of characters.</p>
<p>So dapple makes the renderer a parameter, not an architecture decision:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">dapple</span> <span class="kn">import</span> <span class="n">Canvas</span><span class="p">,</span> <span class="n">braille</span><span class="p">,</span> <span class="n">quadrants</span><span class="p">,</span> <span class="n">sextants</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">dapple.adapters</span> <span class="kn">import</span> <span class="n">from_pil</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">PIL</span> <span class="kn">import</span> <span class="n">Image</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">canvas</span> <span class="o">=</span> <span class="n">from_pil</span><span class="p">(</span><span class="n">Image</span><span class="o">.</span><span class="n">open</span><span class="p">(</span><span class="s2">&#34;photo.jpg&#34;</span><span class="p">),</span> <span class="n">width</span><span class="o">=</span><span class="mi">80</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">canvas</span><span class="o">.</span><span class="n">out</span><span class="p">(</span><span class="n">braille</span><span class="p">)</span>      <span class="c1"># Unicode braille (2x4 dots per cell)</span>
</span></span><span class="line"><span class="cl"><span class="n">canvas</span><span class="o">.</span><span class="n">out</span><span class="p">(</span><span class="n">quadrants</span><span class="p">)</span>    <span class="c1"># block characters with ANSI color</span>
</span></span><span class="line"><span class="cl"><span class="n">canvas</span><span class="o">.</span><span class="n">out</span><span class="p">(</span><span class="n">sextants</span><span class="p">)</span>     <span class="c1"># higher vertical resolution</span>
</span></span></code></pre></div><p>Load once, render anywhere. The renderers are frozen dataclasses. <code>braille(threshold=0.3)</code> returns a new renderer with different settings; nothing mutates. They write directly to a <code>TextIO</code> stream, never building the full output as an intermediate string.</p>
<h2 id="renderers">Renderers</h2>
<table>
  <thead>
      <tr>
          <th>Renderer</th>
          <th>Cell Size</th>
          <th>Colors</th>
          <th>Best For</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>braille</code></td>
          <td>2x4</td>
          <td>mono/gray/true</td>
          <td>Structure, edges, piping, accessibility</td>
      </tr>
      <tr>
          <td><code>quadrants</code></td>
          <td>2x2</td>
          <td>ANSI 256/true</td>
          <td>Photos, balanced resolution and color</td>
      </tr>
      <tr>
          <td><code>sextants</code></td>
          <td>2x3</td>
          <td>ANSI 256/true</td>
          <td>Higher vertical resolution</td>
      </tr>
      <tr>
          <td><code>ascii</code></td>
          <td>1x2</td>
          <td>none</td>
          <td>Universal compatibility</td>
      </tr>
      <tr>
          <td><code>sixel</code></td>
          <td>1x1</td>
          <td>palette</td>
          <td>True pixels (xterm, mlterm, foot)</td>
      </tr>
      <tr>
          <td><code>kitty</code></td>
          <td>1x1</td>
          <td>true</td>
          <td>True pixels (kitty, wezterm)</td>
      </tr>
      <tr>
          <td><code>fingerprint</code></td>
          <td>8x16</td>
          <td>none</td>
          <td>Artistic glyph matching</td>
      </tr>
  </tbody>
</table>
<p>In practice I use braille and sextants. They work everywhere. Kitty protocol broke things completely inside Claude Code (a TUI), and I have not tested sixel enough to trust it. Braille and sextants are the universal goto.</p>
<p>One honest limitation: Claude Code hides terminal output behind a Ctrl-O expand, so my carefully rendered graphics end up collapsed by default. I think recent hooks or tool-result handling might fix this, but I have not confirmed it yet.</p>
<h2 id="three-layers">Three Layers</h2>
<p>The architecture has strict boundaries:</p>
<ol>
<li><strong>Core</strong> (numpy only): Canvas, renderers, color handling, preprocessing, layout primitives (Frame, Grid).</li>
<li><strong>Adapters</strong> (optional deps): Bridge PIL, matplotlib, cairo, and ANSI art to Canvas.</li>
<li><strong>Extras</strong> (optional deps): The CLI tools. Each one is a separate install group.</li>
</ol>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">pip install dapple                 <span class="c1"># core only</span>
</span></span><span class="line"><span class="cl">pip install dapple<span class="o">[</span>imgcat<span class="o">]</span>         <span class="c1"># image viewer</span>
</span></span><span class="line"><span class="cl">pip install dapple<span class="o">[</span>all-tools<span class="o">]</span>      <span class="c1"># everything</span>
</span></span></code></pre></div><p>Core never imports PIL. Adapters never import extras. This matters because the core is tiny and fast, and the CLI tools pull in their own dependencies without bloating each other.</p>
<h2 id="the-cli-tools">The CLI Tools</h2>
<p>I built eleven tools, each owning a domain rather than a file format. &ldquo;Show me this data&rdquo; should not require knowing whether the file is JSON or CSV. &ldquo;Display these images&rdquo; should not require a different tool for one image versus twelve.</p>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>Domain</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>imgcat</td>
          <td>Images (single + grid)</td>
      </tr>
      <tr>
          <td>datcat</td>
          <td>Structured data (JSON/JSONL/CSV/TSV)</td>
      </tr>
      <tr>
          <td>vidcat</td>
          <td>Video (stacked frames, playback, asciinema export)</td>
      </tr>
      <tr>
          <td>mdcat</td>
          <td>Markdown</td>
      </tr>
      <tr>
          <td>htmlcat</td>
          <td>HTML</td>
      </tr>
      <tr>
          <td>pdfcat</td>
          <td>PDFs</td>
      </tr>
      <tr>
          <td>funcat</td>
          <td>Math and parametric plots</td>
      </tr>
      <tr>
          <td>ansicat</td>
          <td>ANSI art</td>
      </tr>
      <tr>
          <td>compcat</td>
          <td>Renderer comparison</td>
      </tr>
      <tr>
          <td>plotcat</td>
          <td>Faceted data plots</td>
      </tr>
      <tr>
          <td>dashcat</td>
          <td>YAML-driven dashboards</td>
      </tr>
  </tbody>
</table>
<p>A few worth explaining:</p>
<h3 id="datcat">datcat</h3>
<p>datcat handles JSON, JSONL, CSV, and TSV. Format detection is automatic. Internally everything becomes <code>list[dict]</code>. CSV rows become dicts on parse. One representation means the downstream code (table formatting, chart extraction, plotting) does not branch on input format.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">datcat records.json              <span class="c1"># JSON table</span>
</span></span><span class="line"><span class="cl">datcat events.jsonl --bar event  <span class="c1"># JSONL bar chart</span>
</span></span><span class="line"><span class="cl">datcat weather.csv --sort temp_c <span class="c1"># CSV sorted by column</span>
</span></span></code></pre></div><h3 id="imgcat">imgcat</h3>
<p>When imgcat receives multiple images, it switches to grid mode automatically. The layout uses dapple&rsquo;s Frame and Grid primitives, the same ones compcat and dashcat use.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">imgcat photo.jpg                     <span class="c1"># single image</span>
</span></span><span class="line"><span class="cl">imgcat photos/*.jpg --cols <span class="m">3</span>         <span class="c1"># 3-column contact sheet</span>
</span></span></code></pre></div><p>Preprocessing flags (<code>--contrast</code>, <code>--dither</code>, <code>--invert</code>) apply to every image in the grid.</p>
<h3 id="vidcat">vidcat</h3>
<p>The <code>--play</code> flag renders frames in-place using ANSI cursor movement. Instead of printing each frame below the last (which scrolls your terminal into oblivion), it overwrites the previous frame.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">vidcat video.mp4 --play              <span class="c1"># 10 fps default</span>
</span></span><span class="line"><span class="cl">vidcat video.mp4 --play --fps <span class="m">24</span>     <span class="c1"># faster</span>
</span></span></code></pre></div><p>The mechanism: render the first frame, count its output lines, then for each subsequent frame write <code>\033[{N}A\033[J</code> (cursor up N, clear to end) before rendering. Falls back to stacked output if stdout is not a TTY.</p>
<h3 id="htmlcat">htmlcat</h3>
<p>Converts HTML to markdown via <a href="https://github.com/matthewwithanm/python-markdownify">markdownify</a>, then renders through Rich using the same pipeline as mdcat. Good for documentation and articles. Not designed for CSS-heavy web apps.</p>
<h2 id="layout-and-charts">Layout and Charts</h2>
<p>Frame and Grid are layout primitives. Frame adds borders and titles around a canvas. Grid arranges canvases in rows and columns. These compose: a Grid of Framed canvases, a Frame around a Grid. dashcat uses this to build terminal dashboards from YAML config.</p>
<p>Two chart APIs:</p>
<ul>
<li><strong>Bitmap charts</strong> (<code>dapple.charts</code>): sparklines, line plots, bar charts, histograms, heatmaps. These return Canvas objects, composable with everything else.</li>
<li><strong>Text charts</strong> (<code>dapple.textchart</code>): text-mode bar charts and sparklines, returning ANSI strings. Used by datcat for quick inline visualization.</li>
</ul>
<h2 id="status">Status</h2>
<p>dapple is on <a href="https://pypi.org/project/dapple/">PyPI</a>. Docs at <a href="https://queelius.github.io/dapple/">queelius.github.io/dapple</a>.</p>
<p>The core and the CLI tools are stable. I use them daily. I have been experimenting with sextants (the 2x3 block characters give surprisingly good results) and tried generalizing the fingerprint renderer to match over a much larger set of Unicode glyphs. That did not work very well. The architecture is settled and the tools work.</p>]]></content:encoded>
      
      
      <category>dapple</category>
      
      <category>terminal-graphics</category>
      
      <category>python</category>
      
      <category>open-source</category>
      
      <category>composition</category>
      
      <category>layout</category>
      
      
    </item>
    
    <item>
      <title>Posthumous: A Federated Dead Man&#39;s Switch</title>
      <link>http://localhost:40461/post/2026-02-14-posthumous/</link>
      <pubDate>Sat, 14 Feb 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/post/2026-02-14-posthumous/</guid>
      <description>A walkthrough of Posthumous, a self-hosted dead man&#39;s switch that monitors periodic check-ins via TOTP, progresses through escalating alert stages, and triggers automated actions if you stop responding.</description>
      <content:encoded><![CDATA[<p>Some things should only happen after you can&rsquo;t do them yourself.</p>
<p>Posthumous is a self-hosted dead man&rsquo;s switch. You check in periodically (via phone, browser, CLI, or API call) and if you stop, it progresses through escalating stages before triggering automated actions: sending notifications, running scripts, whatever you&rsquo;ve configured.</p>
<p>I built it because the existing options are either cloud-hosted (you&rsquo;re trusting someone else&rsquo;s uptime for your most important automation) or single-node (one server failure and silence is indistinguishable from death). Posthumous is federated, multiple nodes watch each other, and fully self-hosted.</p>
<p>This post walks through the basic workflows.</p>
<hr>
<h2 id="setup">Setup</h2>
<p>Installation is a single pip command:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">pip install posthumous
</span></span></code></pre></div><p>Initialization generates a TOTP secret and creates the config directory:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">$ phm init --node-name cerebro
</span></span><span class="line"><span class="cl">Generated new TOTP secret.
</span></span><span class="line"><span class="cl">Config created at ~/.posthumous/config.yaml
</span></span><span class="line"><span class="cl">Example script created at ~/.posthumous/scripts/example.sh
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="o">==================================================</span>
</span></span><span class="line"><span class="cl">TOTP Setup - Scan with your authenticator app:
</span></span><span class="line"><span class="cl"><span class="o">==================================================</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="o">[</span>QR code appears here<span class="o">]</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">Manual entry URI: otpauth://totp/Posthumous:cerebro?secret<span class="o">=</span>...<span class="p">&amp;</span><span class="nv">issuer</span><span class="o">=</span>Posthumous
</span></span><span class="line"><span class="cl">Secret: <span class="nv">JBSWY3DPEHPK3PXP</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="o">==================================================</span>
</span></span><span class="line"><span class="cl">IMPORTANT: Save this secret securely!
</span></span><span class="line"><span class="cl"><span class="o">==================================================</span>
</span></span></code></pre></div><p>You scan the QR code with any authenticator app (Google Authenticator, Authy, 1Password, whatever generates TOTP codes). That code is how you prove you&rsquo;re alive.</p>
<p>The config file (<code>~/.posthumous/config.yaml</code>) controls timing, notifications, and actions:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">node_name</span><span class="p">:</span><span class="w"> </span><span class="l">cerebro</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">secret_key</span><span class="p">:</span><span class="w"> </span><span class="l">JBSWY3DPEHPK3PXP</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">listen</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;0.0.0.0:8420&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">base_url</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;https://posthumous.example.com:8420&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">checkin_interval</span><span class="p">:</span><span class="w"> </span><span class="m">7</span><span class="w"> </span><span class="l">days</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">warning_start</span><span class="p">:</span><span class="w"> </span><span class="m">8</span><span class="w"> </span><span class="l">days</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">grace_start</span><span class="p">:</span><span class="w"> </span><span class="m">12</span><span class="w"> </span><span class="l">days</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">trigger_at</span><span class="p">:</span><span class="w"> </span><span class="m">14</span><span class="w"> </span><span class="l">days</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">notifications</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">default</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="l">ntfy://my-posthumous-channel</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">actions</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">on_warning</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">notify</span><span class="p">:</span><span class="w"> </span><span class="l">default</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">message</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Check-in needed. {days_left} days remaining.\n\nCheck in: {checkin_url}&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">on_grace</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">notify</span><span class="p">:</span><span class="w"> </span><span class="l">default</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">message</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;URGENT: Posthumous triggers in {hours_left} hours.\n\nCheck in: {checkin_url}&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">on_trigger</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">notify</span><span class="p">:</span><span class="w"> </span><span class="l">default</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">message</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Posthumous has activated.\n\nDashboard: {dashboard_url}&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">script</span><span class="p">:</span><span class="w"> </span><span class="l">scripts/release-credentials.sh</span><span class="w">
</span></span></span></code></pre></div><p>Notification messages support template variables like <code>{checkin_url}</code> and <code>{dashboard_url}</code>, so push notifications to your phone include a direct link back to the check-in page.</p>
<hr>
<h2 id="the-check-in-flow">The Check-in Flow</h2>
<p>Start the daemon:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">$ phm run
</span></span><span class="line"><span class="cl">Starting Posthumous node <span class="s1">&#39;cerebro&#39;</span>...
</span></span></code></pre></div><p>This launches the web server, watchdog timer, and scheduler. Navigate to the check-in page:</p>
<p><img src="01-checkin-armed.png" alt="The check-in page in ARMED state, showing a TOTP input field and the node name"></p>
<p>The UI is intentionally minimal. Dark theme, big input, works on a phone screen. Enter your 6-digit TOTP code and hit Check In.</p>
<p>After a successful check-in, the timer resets and you see the countdown:</p>
<p><img src="03-checkin-after.png" alt="Check-in page after successful authentication, showing time since last check-in and time until trigger"></p>
<p>The status line tells you exactly how long until the system would escalate.</p>
<p>You can also check in via the CLI or the JSON API:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># CLI check-in (prompts for TOTP)</span>
</span></span><span class="line"><span class="cl">$ phm checkin
</span></span><span class="line"><span class="cl">TOTP code: <span class="m">645557</span>
</span></span><span class="line"><span class="cl">Check-in accepted
</span></span><span class="line"><span class="cl">  Status: ARMED
</span></span><span class="line"><span class="cl">  Next deadline: 2026-02-28 11:41 UTC
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># API check-in (for automation)</span>
</span></span><span class="line"><span class="cl">$ curl -X POST http://localhost:8420/checkin <span class="se">\
</span></span></span><span class="line"><span class="cl">    -H <span class="s1">&#39;Content-Type: application/json&#39;</span> <span class="se">\
</span></span></span><span class="line"><span class="cl">    -d <span class="s1">&#39;{&#34;totp&#34;: &#34;645557&#34;}&#39;</span>
</span></span><span class="line"><span class="cl"><span class="o">{</span><span class="s2">&#34;success&#34;</span>: true, <span class="s2">&#34;status&#34;</span>: <span class="s2">&#34;armed&#34;</span>, <span class="s2">&#34;next_deadline&#34;</span>: <span class="s2">&#34;2026-02-28T11:41:43+00:00&#34;</span><span class="o">}</span>
</span></span></code></pre></div><hr>
<h2 id="the-dashboard">The Dashboard</h2>
<p>The dashboard shows everything at a glance: countdown timers, check-in history, peer status, and scheduled post-trigger actions:</p>
<p><img src="04-dashboard-armed.png" alt="Dashboard in ARMED state showing time remaining until each escalation stage, check-in history, peer status, and scheduled items"></p>
<p>The color coding matches the urgency: green for &ldquo;Until warning&rdquo; (you&rsquo;re fine), orange for &ldquo;Until grace&rdquo; (getting close), red for &ldquo;Until trigger&rdquo; (last chance). Auto-refreshes every 60 seconds.</p>
<hr>
<h2 id="the-state-machine">The State Machine</h2>
<p>Posthumous progresses through four states:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">ARMED ──timeout──&gt; WARNING ──timeout──&gt; GRACE ──timeout──&gt; TRIGGERED
</span></span><span class="line"><span class="cl">  ^                   |                   |                    |
</span></span><span class="line"><span class="cl">  └─── check-in ──────┴─── check-in ─────┘                    v
</span></span><span class="line"><span class="cl">                                                    (scheduler runs forever)
</span></span></code></pre></div><p>A check-in from any pre-trigger state resets to ARMED. <strong>TRIGGERED is terminal.</strong> Once activated, no check-in can undo it. This is by design: if the switch has fired, you want the actions to complete.</p>
<p>Each transition fires its configured actions. If the node was offline and missed intermediate states (say it was down during WARNING and comes back during GRACE), the watchdog fires all skipped callbacks in order before reaching the current state. No notifications are silently dropped.</p>
<p>When the system reaches TRIGGERED, the check-in page locks out:</p>
<p><img src="06-checkin-triggered.png" alt="Check-in page in TRIGGERED state, the TOTP form is replaced with a message explaining the node is triggered"></p>
<p>And the dashboard reflects the terminal state:</p>
<p><img src="05-dashboard-triggered.png" alt="Dashboard in TRIGGERED state showing all stages as Active/Activated with the trigger timestamp"></p>
<hr>
<h2 id="notifications">Notifications</h2>
<p>Posthumous uses <a href="https://github.com/caronc/apprise">Apprise</a> for notifications, which means it supports 100+ notification services out of the box: ntfy, Pushover, Telegram, Discord, email, Slack, and more.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">notifications</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">default</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="l">ntfy://my-posthumous-channel</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">urgent</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="l">pover://user@token</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="l">tgram://bot_token/chat_id</span><span class="w">
</span></span></span></code></pre></div><p>Each escalation stage can target different channels with different messages. Warning might go to ntfy (a gentle ping), while grace and trigger go to Pushover and Telegram (hard to miss).</p>
<p>The <code>{checkin_url}</code> variable means warning notifications include a clickable link directly to the check-in page. Open the notification on your phone, tap the link, enter the TOTP code. Three taps and you&rsquo;re checked in.</p>
<hr>
<h2 id="federation">Federation</h2>
<p>A single node has a single point of failure. If the server goes down, silence looks the same as death, which means either false triggers or missed real triggers.</p>
<p>Posthumous solves this with federation. Multiple nodes share the same TOTP secret and communicate via HMAC-signed HTTP:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="c"># Node A&#39;s config</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">peers</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span>- <span class="l">https://node-b.example.com:8420</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span>- <span class="l">https://node-c.example.com:8420</span><span class="w">
</span></span></span></code></pre></div><p>When you check in to any node, it broadcasts to all peers. When any node triggers, it broadcasts that too. The design bias is deliberate: <strong>duplicates over silence</strong>. Multiple nodes may fire the same notification (annoying but survivable). A missed trigger is not.</p>
<p>Each node tracks peer health independently, and the dashboard shows peer status with connection age and failure counts.</p>
<hr>
<h2 id="post-trigger-scheduling">Post-Trigger Scheduling</h2>
<p>Once triggered, Posthumous doesn&rsquo;t just fire-and-forget. A scheduler runs indefinitely, executing actions on configurable schedules using a small DSL:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">post_trigger</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span>- <span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">weekly-reminder</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">when</span><span class="p">:</span><span class="w"> </span><span class="l">every week after trigger</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">notify</span><span class="p">:</span><span class="w"> </span><span class="l">default</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">message</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Posthumous was triggered {days_left} days ago.&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span>- <span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">credential-release</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">when</span><span class="p">:</span><span class="w"> </span><span class="m">30</span><span class="w"> </span><span class="l">days after trigger</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">script</span><span class="p">:</span><span class="w"> </span><span class="l">scripts/release-credentials.sh</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span>- <span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">annual-memorial</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">when</span><span class="p">:</span><span class="w"> </span><span class="l">every year on trigger anniversary</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">notify</span><span class="p">:</span><span class="w"> </span><span class="l">default</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">message</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Annual memorial notification.&#34;</span><span class="w">
</span></span></span></code></pre></div><p>The <code>when</code> expressions support relative timing (<code>30 days after trigger</code>), recurring patterns (<code>every week after trigger</code>), anniversaries (<code>every year on trigger anniversary</code>), and absolute dates. Each execution is deduplicated by period key. If a node restarts, it won&rsquo;t re-run actions for the current period.</p>
<hr>
<h2 id="security">Security</h2>
<p>Authentication uses TOTP (the same protocol as Google Authenticator). This means:</p>
<ul>
<li><strong>No passwords stored on the server</strong>, only the shared secret</li>
<li><strong>Time-based codes expire every 30 seconds</strong>, replay attacks have a narrow window</li>
<li><strong>Brute-force protection</strong>, after configurable failed attempts the account locks out for a configurable duration</li>
</ul>
<p>The check-in page locks out after too many failures, and the API returns HTTP 429.</p>
<p>Peer communication is authenticated with HMAC-SHA256 signatures derived from the shared secret. State files can optionally be encrypted at rest with Fernet (AES-128-CBC), enabled with a single config flag:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">encrypt_at_rest</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span></code></pre></div><hr>
<h2 id="status-at-a-glance">Status at a Glance</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">$ phm status
</span></span><span class="line"><span class="cl">Node: cerebro
</span></span><span class="line"><span class="cl">Status: ARMED
</span></span><span class="line"><span class="cl">Last check-in: 2026-02-14 11:41 UTC
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">Warning in:  6d 23h
</span></span><span class="line"><span class="cl">Grace in:    10d 23h
</span></span><span class="line"><span class="cl">Trigger in:  12d 23h
</span></span></code></pre></div><p>Or hit the JSON API for monitoring integrations:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">$ curl -s http://localhost:8420/status <span class="p">|</span> python -m json.tool
</span></span><span class="line"><span class="cl"><span class="o">{</span>
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;node_name&#34;</span>: <span class="s2">&#34;cerebro&#34;</span>,
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;status&#34;</span>: <span class="s2">&#34;armed&#34;</span>,
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;last_checkin&#34;</span>: <span class="s2">&#34;2026-02-14T11:41:43+00:00&#34;</span>,
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;time_remaining&#34;</span>: <span class="o">{</span>
</span></span><span class="line"><span class="cl">        <span class="s2">&#34;until_warning&#34;</span>: 596503.0,
</span></span><span class="line"><span class="cl">        <span class="s2">&#34;until_grace&#34;</span>: 942103.0,
</span></span><span class="line"><span class="cl">        <span class="s2">&#34;until_trigger&#34;</span>: 1114903.0
</span></span><span class="line"><span class="cl">    <span class="o">}</span>
</span></span><span class="line"><span class="cl"><span class="o">}</span>
</span></span></code></pre></div><hr>
<h2 id="whats-next">What&rsquo;s Next</h2>
<p>Posthumous is at v0.5. The core workflows are solid: check-in, state machine, notifications, federation, scheduling, encryption at rest. Some things I&rsquo;m considering:</p>
<ul>
<li><strong>Static site integration</strong>: generating a Hugo/Jekyll site from post-trigger content, hosted on GitHub Pages</li>
<li><strong>Multi-factor escalation</strong>: requiring check-ins from multiple sources (web + CLI + API) before considering you &ldquo;alive&rdquo;</li>
<li><strong>Better peer discovery</strong>: automatic peer registration instead of manual URL configuration</li>
</ul>
<p>The code is on GitHub: <a href="https://github.com/queelius/posthumous">queelius/posthumous</a>. It&rsquo;s a single <code>pip install</code> and about 2,200 lines of Python (plus 3,700 lines of tests at 99% coverage).</p>
<hr>
<p><em>Posthumous is named for the obvious reason. Some automations only make sense after the fact.</em></p>]]></content:encoded>
      
      
      <category>posthumous</category>
      
      <category>dead-mans-switch</category>
      
      <category>python</category>
      
      <category>asyncio</category>
      
      <category>totp</category>
      
      <category>federation</category>
      
      <category>open-source</category>
      
      
    </item>
    
    <item>
      <title>A Map of 120&#43; Open Source Projects</title>
      <link>http://localhost:40461/post/2026-02-07-ecosystem-overview/</link>
      <pubDate>Fri, 13 Feb 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/post/2026-02-07-ecosystem-overview/</guid>
      <description>A guided tour through my open-source ecosystem: encrypted search theory, statistical reliability, Unix-philosophy CLI tools, AI research, and speculative fiction. How 120&#43; projects connect and where to start.</description>
      <content:encoded><![CDATA[<p>Over the past decade, I&rsquo;ve published more than 120 open-source repositories on <a href="https://github.com/queelius">GitHub</a>. They span encrypted search theory, statistical reliability engineering, generic programming, fuzzy logic, AI tools, Unix-philosophy CLI utilities, three novels, a community math database, and a digital legacy project motivated by the possibility that I might not be around to maintain any of it.</p>
<p>From the outside, it probably looks like a sprawl. From the inside, almost everything connects.</p>
<p>This post is the map I wish I&rsquo;d written years ago. It&rsquo;s for anyone who stumbles into my GitHub profile and wonders: <em>what is all this, and how does it fit together?</em></p>
<p>Two threads run through everything:</p>
<ol>
<li>
<p><strong>Algebraic composability.</strong> I think the right abstractions compose. Hash functions compose into perfect hash filters. Likelihood contributions compose into maximum likelihood estimators. Boolean predicates compose into fuzzy inference rules. CLI tools compose into personal knowledge pipelines. When I find a domain where composition is possible but nobody has made it ergonomic, I start a project.</p>
</li>
<li>
<p><strong>Personal data sovereignty.</strong> Your conversations, bookmarks, photos, email, health records, and reading notes belong to you, not to a cloud platform that might shut down, change terms, or get acquired. A surprising number of my tools exist to keep personal data in local SQLite databases, queryable from the command line, exportable as plain text, and durable across decades.</p>
</li>
</ol>
<p>If you keep those two threads in mind, the ecosystem stops looking like a sprawl and starts looking like a lattice.</p>
<hr>
<h2 id="the-research-foundation">The Research Foundation</h2>
<h3 id="oblivious-computing-and-encrypted-search">Oblivious Computing and Encrypted Search</h3>
<p>This is where it started. My <a href="/post/2024-06-15-reliability-estimation-in-series-systems/">master&rsquo;s thesis</a> explored how to search encrypted data without revealing what you&rsquo;re searching for, the problem known as <em>oblivious computing</em> or <em>encrypted search</em>.</p>
<p>The core question: can you build a search index where the server processes queries without learning the query content, the document content, or even which documents matched? The answer is yes, but only with careful algebraic constraints on the encryption.</p>
<p>The key projects:</p>
<ul>
<li><a href="https://github.com/queelius/encrypted_search_thesis"><strong>encrypted_search_thesis</strong></a>: The thesis itself, formalizing encrypted search using trapdoor Boolean algebras</li>
<li><a href="https://github.com/queelius/algebraic_cipher_types"><strong>algebraic_cipher_types</strong></a>: A type-theoretic framework for reasoning about ciphertexts as algebraic objects. This paper (<a href="/post/2025-10-07-algebraic-cipher-types/">blog post</a>) defines ciphertext types that preserve specific algebraic operations under encryption.</li>
<li><a href="https://github.com/queelius/bernoulli_data_type"><strong>bernoulli_data_type</strong></a>: The Bernoulli model for approximate encrypted search. When exact matches aren&rsquo;t possible, what&rsquo;s the information-theoretic cost?</li>
<li><a href="https://github.com/queelius/boolean-algebra-over-trapdoor-sets"><strong>boolean-algebra-over-trapdoor-sets</strong></a>: Building Boolean algebra operations (AND, OR, NOT) on encrypted sets</li>
<li><a href="https://github.com/queelius/encrypted_search_confidentiality"><strong>encrypted_search_confidentiality</strong></a>: Measuring what encrypted search leaks via entropy and mutual information</li>
</ul>
<p>These are theoretical projects, papers and proofs, not production systems. They establish a mathematical framework for thinking about computation over encrypted data. The practical insight: if you model a ciphertext as a type with algebraic operations, you can reason about what computations preserve confidentiality and which ones leak.</p>
<h3 id="statistical-reliability">Statistical Reliability</h3>
<p>The other half of my thesis work, and the part that became production software. The question: if a series system fails but you can&rsquo;t tell which component caused the failure, how do you estimate component lifetimes?</p>
<p>This is <strong>masked data</strong> analysis. A system has <em>m</em> components in series. When it fails at time <em>t</em>, you get a <em>candidate set</em> of possible failure causes, not a definitive answer. Sometimes the system is still running when you stop testing (<em>right-censoring</em>). Given all this uncertainty, can you do maximum likelihood estimation?</p>
<p>Yes. And the resulting R packages form a layered architecture, each layer building on the one below:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">nabla                        : Exact derivatives via automatic differentiation
</span></span><span class="line"><span class="cl">  |
</span></span><span class="line"><span class="cl">femtograd                    : AD for statistical computing
</span></span><span class="line"><span class="cl">  |
</span></span><span class="line"><span class="cl">algebraic.mle                : MLE as algebraic objects (on CRAN)
</span></span><span class="line"><span class="cl">  |
</span></span><span class="line"><span class="cl">compositional.mle            : DSL for composing solvers (chaining, racing, restarts)
</span></span><span class="line"><span class="cl">  |
</span></span><span class="line"><span class="cl">likelihood.model             : Composable likelihood framework (submitted to CRAN)
</span></span><span class="line"><span class="cl">  |
</span></span><span class="line"><span class="cl">maskedcauses                 : Masked series systems (next CRAN submission)
</span></span><span class="line"><span class="cl">  |
</span></span><span class="line"><span class="cl">wei.series.md.c1.c2.c3      : Weibull series estimation (the applied capstone)
</span></span></code></pre></div><p>I&rsquo;ll detail the full R ecosystem below, but the key point: the theoretical work on masked data produced a general-purpose framework for composable statistical inference. The specific reliability application is just one instance of the pattern.</p>
<h3 id="information-theory-and-sequential-prediction">Information Theory and Sequential Prediction</h3>
<p>A smaller but persistent thread: <a href="/series/the-learning-problem/">sequential prediction</a> and information-theoretic approaches to learning.</p>
<ul>
<li><a href="https://github.com/queelius/infinigram"><strong>infinigram</strong></a>: Exploring what happens when n-gram models have unbounded context. Related to <a href="/post/2012-03-15-solomonoff-induction-prediction/">Solomonoff induction</a> and <a href="/post/2025-01-15-ctw-experimental-results/">CTW algorithms</a>.</li>
<li><a href="https://github.com/queelius/fisher-flow"><strong>fisher-flow</strong></a>: Connecting Fisher information geometry to information flow in statistical models</li>
<li><a href="https://github.com/queelius/langcalc"><strong>langcalc</strong></a>: Token-level entropy calculations for language models</li>
</ul>
<p>This cluster connects the encrypted search work (which is fundamentally about information leakage) to the statistical reliability work (which is about inference under missing information) through their shared foundation in information theory.</p>
<hr>
<h2 id="the-r-package-ecosystem">The R Package Ecosystem</h2>
<p>Thirteen R packages, two on CRAN, one submitted, the rest on <a href="https://queelius.r-universe.dev">r-universe</a>. They form a composable architecture where each layer adds capability without breaking the abstractions below.</p>
<h3 id="the-core-stack">The Core Stack</h3>
<table>
  <thead>
      <tr>
          <th>Package</th>
          <th>Role</th>
          <th>Status</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><a href="/projects/algebraic.mle/">algebraic.mle</a></td>
          <td>MLE objects with algebraic operations (compose, transform, subset)</td>
          <td>CRAN</td>
      </tr>
      <tr>
          <td><a href="/projects/algebraic.dist/">algebraic.dist</a></td>
          <td>Probability distributions as algebraic objects</td>
          <td>CRAN</td>
      </tr>
      <tr>
          <td><a href="/projects/likelihood.model/">likelihood.model</a></td>
          <td>Composable likelihood contributions for statistical models</td>
          <td>Submitted to CRAN</td>
      </tr>
      <tr>
          <td><a href="/projects/maskedcauses/">maskedcauses</a></td>
          <td>Series system MLE with masked failure causes</td>
          <td>Next CRAN submission</td>
      </tr>
  </tbody>
</table>
<p>The key design insight: <strong>likelihood contributions compose</strong>. When observations are independent, the total log-likelihood is the sum of individual contributions. <code>likelihood.model</code> provides the composition machinery. <code>algebraic.mle</code> provides the algebra for manipulating fitted models. Each domain-specific package just provides the contributions.</p>
<p>This means you can mix and match. Define your own likelihood contributions and they plug into the same optimization, bootstrap, and model selection infrastructure.</p>
<h3 id="supporting-packages">Supporting Packages</h3>
<table>
  <thead>
      <tr>
          <th>Package</th>
          <th>Role</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><a href="https://github.com/queelius/nabla">nabla</a></td>
          <td>Automatic differentiation for R, gradient computation without finite differences</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/femtograd">femtograd</a></td>
          <td>Minimal forward-mode AD, a teaching implementation</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/hypothesize">hypothesize</a></td>
          <td>Hypothesis testing framework built on the likelihood model</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/dfr_dist">dfr_dist</a></td>
          <td>Distribution-free reliability estimation</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/md.tools">md.tools</a></td>
          <td>Utilities for encoding and decoding masked data</td>
      </tr>
      <tr>
          <td><a href="/projects/wei.series.md.c1.c2.c3/">wei.series.md.c1.c2.c3</a></td>
          <td>Weibull series system implementations</td>
      </tr>
      <tr>
          <td><a href="/papers/mdrelax/">mdrelax</a></td>
          <td>What happens when the standard masking assumptions (C1/C2/C3) are violated</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/compositional.mle">compositional.mle</a></td>
          <td>Compositional extensions to the MLE framework</td>
      </tr>
  </tbody>
</table>
<h3 id="the-design-philosophy">The Design Philosophy</h3>
<p>Every package follows the same pattern: define an S3 object with generic functions (<code>loglik()</code>, <code>score()</code>, <code>hess_loglik()</code>, <code>fit()</code>, <code>rdata()</code>), make it composable, and let the algebra do the work. If you&rsquo;ve used one package in the stack, you know the interface for all of them.</p>
<p>This is <a href="/series/stepanov/">Stepanov&rsquo;s philosophy</a> applied to statistics: find the minimal algebraic structure, then build generic algorithms that work on anything satisfying that structure.</p>
<hr>
<h2 id="data-structures-and-algorithms">Data Structures and Algorithms</h2>
<h3 id="python-libraries-pypi">Python Libraries (PyPI)</h3>
<p>Over 20 packages on <a href="https://pypi.org/user/queelius/">PyPI</a> spanning data structures, symbolic computation, CLI tools, and more:</p>
<ul>
<li><a href="https://github.com/queelius/AlgoTree"><strong>AlgoTree</strong></a> (16 stars): Generic tree data structures and algorithms: traversal, manipulation, serialization, conversion between representations. (<a href="/post/algotree/">Blog post</a>)</li>
<li><a href="https://github.com/queelius/AlgoGraph"><strong>AlgoGraph</strong></a>: Graph algorithms in the same style</li>
<li><a href="https://github.com/queelius/symlik"><strong>symlik</strong></a>: Symbolic likelihood computation (<a href="/post/2025-12-16-symlik/">blog post</a>)</li>
<li><a href="https://github.com/queelius/rerum"><strong>rerum</strong></a>: Term rewriting engine</li>
<li><a href="https://github.com/queelius/dotsuite"><strong>dotsuite</strong></a>: Boolean algebra operations</li>
<li><a href="https://github.com/queelius/jaf"><strong>jaf</strong></a>: JSON algebra (merge, diff, patch)</li>
<li><a href="https://github.com/queelius/jsonl-algebra"><strong>jsonl-algebra</strong></a>: Algebraic operations on JSONL streams</li>
<li><a href="https://github.com/queelius/fuzzy-infer"><strong>fuzzy-infer</strong></a>: Fuzzy logic inference engine</li>
<li><a href="https://github.com/queelius/nfa-tools"><strong>nfa-tools</strong></a>: NFA manipulation and visualization</li>
<li><a href="https://github.com/queelius/src2md"><strong>src2md</strong></a>: Source code to markdown conversion</li>
<li><strong>pfc</strong> (Python bindings): Python interface for the <a href="https://github.com/queelius/pfc">pfc</a> prefix-free coding C++ library</li>
</ul>
<p>Most of the *tk CLI tools (btk, ctk, ebk, etc.) are also installable as Python packages. The pattern: algebraic objects with well-defined operations. Same philosophy as the R and C++ packages.</p>
<h3 id="c-libraries">C++ Libraries</h3>
<p>The C++ projects are where the generic programming philosophy is most explicit. They&rsquo;re inspired by Alexander Stepanov&rsquo;s work: algorithms should be parameterized by the algebraic structures they operate on, not by specific data types. Most are header-only C++20 libraries.</p>
<p><strong>Data structures and hashing:</strong></p>
<ul>
<li><a href="https://github.com/queelius/algebraic_hashing"><strong>algebraic_hashing</strong></a>: Hash function composition as algebraic morphisms, with a DSL for combining hash functions</li>
<li><a href="https://github.com/queelius/sparse_spatial_hash"><strong>sparse_spatial_hash</strong></a>: N-dimensional sparse spatial hashing for collision detection and neighbor queries</li>
<li><a href="https://github.com/queelius/maph"><strong>maph</strong></a>: Space-efficient approximate mappings using perfect hash functions, with configurable storage and accuracy trade-offs</li>
<li><a href="https://github.com/queelius/pfc"><strong>pfc</strong></a>: Zero-copy, prefix-free data representations with algebraic types and succinct data structures</li>
<li><a href="https://github.com/queelius/cbt"><strong>cbt</strong></a>: Computational basis transforms between domains</li>
<li><a href="https://github.com/queelius/bloomy"><strong>bloomy</strong></a>: Secure index based on Bloom filters (connecting to the encrypted search work)</li>
<li><a href="https://github.com/queelius/packed_data"><strong>packed_data</strong></a>: Compact data representations</li>
<li><a href="https://github.com/queelius/accumux"><strong>accumux</strong></a>: Accumulator and multiplexer patterns</li>
</ul>
<p><strong>Numerical and algebraic computation:</strong></p>
<ul>
<li><a href="https://github.com/queelius/limes"><strong>limes</strong></a>: Composable calculus expressions: symbolic differentiation, numerical integration, algebraic composition</li>
<li><a href="https://github.com/queelius/elementa"><strong>elementa</strong></a>: Pedagogical C++20 linear algebra library</li>
<li><a href="https://github.com/queelius/gradator"><strong>gradator</strong></a>: Pedagogical C++20 automatic differentiation library</li>
<li><a href="https://github.com/queelius/dual"><strong>dual</strong></a>: Dual number arithmetic for automatic differentiation</li>
</ul>
<p><strong>Simulation:</strong></p>
<ul>
<li><a href="https://github.com/queelius/barnes-hut"><strong>barnes-hut</strong></a>: Barnes-Hut tree algorithm for N-body simulation</li>
</ul>
<p>The Stepanov essay series (<a href="/post/2026-01-18-synthesis-stepanov/">Seeing Structure First</a>) explains the philosophy: an algorithm like <code>power(x, n, op)</code> computes x^n under <em>any</em> monoid operation, whether that&rsquo;s exponentiation, matrix power, string repetition, or path composition. The C++ libraries embody this by treating data structures as instances of algebraic concepts, not as standalone implementations.</p>
<hr>
<h2 id="boolean-fuzzy-and-symbolic-computation">Boolean, Fuzzy, and Symbolic Computation</h2>
<p>A cluster of Python projects (all on PyPI) exploring logic beyond true/false:</p>
<ul>
<li><a href="https://github.com/queelius/dotsuite"><strong>dotsuite</strong></a>: Boolean algebra operations and visualization</li>
<li><a href="https://github.com/queelius/jaf"><strong>jaf</strong></a>: JSON algebra: treating JSON documents as algebraic objects with merge, diff, and patch operations</li>
<li><a href="https://github.com/queelius/fuzzy-infer"><strong>fuzzy-infer</strong></a>: Fuzzy logic inference engine</li>
<li><a href="https://github.com/queelius/soft-circuit"><strong>soft-circuit</strong></a>: Soft Boolean circuits where gates have continuous rather than discrete outputs</li>
<li><a href="https://github.com/queelius/fuzzy-soft-circuit"><strong>fuzzy-soft-circuit</strong></a>: Combining fuzzy logic with soft circuits</li>
<li><a href="https://github.com/queelius/fuzzy-logic-search"><strong>fuzzy-logic-search</strong></a>: Search systems that use fuzzy logic for relevance scoring</li>
<li><a href="https://github.com/queelius/rerum"><strong>rerum</strong></a>: Term rewriting engine</li>
<li><a href="https://github.com/queelius/xtk"><strong>xtk</strong></a>: Expression toolkit for symbolic manipulation</li>
<li><a href="https://github.com/queelius/tree_rewriter"><strong>tree_rewriter</strong></a>: Tree-based term rewriting</li>
</ul>
<p>This cluster connects to the encrypted search work (Boolean algebra over trapdoor sets) and to the AI work (soft circuits as differentiable logic). The thread: logic is algebraic structure, and generalizing from crisp to fuzzy to soft to encrypted is a matter of changing which algebraic laws you preserve.</p>
<hr>
<h2 id="the-unix-toolkit-constellation">The Unix Toolkit Constellation</h2>
<p>Fifteen CLI tools that share a philosophy: <strong>SQLite-backed, CLI-first, composable, personal data sovereignty</strong>. They use a naming convention (most end in <code>tk</code> or <code>k</code>) and are designed to work together through Unix pipes and shared conventions.</p>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>Domain</th>
          <th>Description</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><a href="https://github.com/queelius/btk">btk</a></td>
          <td>Bookmarks</td>
          <td>SQLite-backed bookmarks with hierarchical tags and a typed query DSL</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/ebk">ebk</a></td>
          <td>Ebooks</td>
          <td>E-book library management, metadata extraction, format conversion</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/mtk">mtk</a></td>
          <td>Mail</td>
          <td>Local email archival and search</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/ptk">ptk</a></td>
          <td>Photos</td>
          <td>Photo management with EXIF metadata and tagging</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/atk">atk</a></td>
          <td>Audio</td>
          <td>Audio file management and metadata</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/ctk">ctk</a></td>
          <td>Conversations</td>
          <td>LLM conversation archival and search (<a href="/post/2025-10-ctk/">blog post</a>)</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/xtk">xtk</a></td>
          <td>Expressions</td>
          <td>Symbolic expression manipulation</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/jot">jot</a></td>
          <td>Notes</td>
          <td>Journaling and note-taking</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/deets">deets</a></td>
          <td>Personal metadata</td>
          <td>Identity and metadata management</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/repoindex">repoindex</a></td>
          <td>Repositories</td>
          <td>Git repository database and query tool</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/crier">crier</a></td>
          <td>Social media</td>
          <td>Cross-posting blog content to social platforms (12 stars)</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/chop">chop</a></td>
          <td>Images</td>
          <td>Image manipulation</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/dapple">dapple</a></td>
          <td>Terminal graphics</td>
          <td>Rich terminal output and visualization</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/clerk">clerk</a></td>
          <td>Workflows</td>
          <td>Task and workflow management</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/chartfold">chartfold</a></td>
          <td>Health records</td>
          <td>Personal health record management</td>
      </tr>
  </tbody>
</table>
<h3 id="the-philosophy">The Philosophy</h3>
<p>These tools share a conviction: your personal data should live in <strong>local SQLite databases</strong>, not in someone else&rsquo;s cloud. Every tool stores its data in a format you can query with SQL, back up with <code>cp</code>, and inspect with <code>sqlite3</code>. They compose through pipes:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Find bookmarks tagged &#34;research&#34;, get their URLs, search your ebooks for related content</span>
</span></span><span class="line"><span class="cl">btk query --tag research --format url <span class="p">|</span> xargs -I<span class="o">{}</span> ebk search --related <span class="o">{}</span>
</span></span></code></pre></div><p>The <a href="/series/the-long-echo/">blog series on digital legacy</a> explains the motivation. When I was diagnosed with cancer, the question &ldquo;what happens to my digital life?&rdquo; became urgent. These tools are one answer: if your data lives in portable, open formats on hardware you control, it survives you.</p>
<hr>
<h2 id="ai-and-llm-projects">AI and LLM Projects</h2>
<p>The largest and fastest-growing cluster.</p>
<h3 id="llm-tools-and-research">LLM Tools and Research</h3>
<ul>
<li><a href="https://github.com/queelius/elasticsearch-lm"><strong>elasticsearch-lm</strong></a> (37 stars): Language model integration with Elasticsearch. Bridges the encrypted search research with modern LLM applications.</li>
<li><a href="https://github.com/queelius/mcts-reasoning"><strong>mcts-reasoning</strong></a>: Monte Carlo Tree Search for structured reasoning. Uses tree search (connecting to AlgoTree) to improve LLM reasoning quality. (<a href="/post/2024-12-01-mcts-reasoning/">Blog post</a>)</li>
<li><a href="https://github.com/queelius/dreamlog"><strong>dreamlog</strong></a>: Logic programming with LLM integration and wake-sleep learning cycles</li>
<li><a href="https://github.com/queelius/complex-network-rag"><strong>complex-network-rag</strong></a>: RAG (Retrieval-Augmented Generation) over complex document networks</li>
<li><a href="https://github.com/queelius/ollama_data_tools"><strong>ollama_data_tools</strong></a> (4 stars): Utilities for working with Ollama models and data</li>
<li><a href="https://github.com/queelius/llm-bayes"><strong>llm-bayes</strong></a>: Bayesian reasoning with LLMs</li>
<li><a href="https://github.com/queelius/synthdata"><strong>synthdata</strong></a>: Synthetic data generation</li>
<li><a href="https://github.com/queelius/agentum"><strong>agentum</strong></a>: Unified framework for sequential decision-making, from classical search to deep RL</li>
<li><a href="https://github.com/queelius/itinero"><strong>itinero</strong></a>: LLM-powered web automation through composable strategies and Playwright</li>
<li><a href="https://github.com/queelius/AutoPoiesi"><strong>AutoPoiesi</strong></a>: Self-organizing systems inspired by autopoiesis theory</li>
</ul>
<h3 id="the-thread">The Thread</h3>
<p>These aren&rsquo;t random AI projects. The encrypted search work asks: &ldquo;How do you search without revealing what you know?&rdquo; The LLM work asks the complement: &ldquo;How do you <em>reason</em> with what you know?&rdquo; Both are about the interface between information and computation. <code>elasticsearch-lm</code> literally bridges the two.</p>
<hr>
<h2 id="digital-legacy-projects">Digital Legacy Projects</h2>
<p>Five projects exploring what happens to digital identity after death. This is the <a href="/series/the-long-echo/">Long Echo</a> series in code form.</p>
<ul>
<li><a href="https://github.com/queelius/longecho"><strong>longecho</strong></a>: The framework for durable personal data. Redundant storage, format migration, integrity verification.</li>
<li><a href="https://github.com/queelius/longshade"><strong>longshade</strong></a>: Generate a conversable persona from personal data: conversations, writings, and email</li>
<li><a href="https://github.com/queelius/posthumous"><strong>posthumous</strong></a>: Automated actions triggered by death or incapacity. Dead man&rsquo;s switches, scheduled messages, data release.</li>
<li><a href="https://github.com/queelius/pagevault"><strong>pagevault</strong></a>: Password-protect semi-private content on static sites like Hugo blogs</li>
<li><a href="https://github.com/queelius/cryptoid"><strong>cryptoid</strong></a>: Client-side encrypted content for Hugo static sites with multi-user access control</li>
</ul>
<p>These are nascent. They share a philosophy but don&rsquo;t yet share infrastructure. The vision: a unified system where your personal data (managed by the *tk tools) is stored durably (longecho), distilled into a conversable persona (longshade), protected on your sites (pagevault, cryptoid), and handled according to your wishes after death (posthumous).</p>
<p>The <a href="/post/2026-01-04-notes-from-the-transition/">essays on digital legacy</a> provide the philosophical backdrop. The tools provide the engineering.</p>
<hr>
<h2 id="literature">Literature</h2>
<p>Three novels, each exploring different aspects of consciousness and intelligence:</p>
<ul>
<li><a href="https://github.com/queelius/echoes-of-the-sublime"><strong>echoes-of-the-sublime</strong></a>: Philosophical horror, approximately 103,000 words. What happens when consciousness encounters something genuinely beyond its capacity to process? (<a href="/post/2024-08-15-echoes-of-the-sublime/">Blog post</a>)</li>
<li><a href="https://github.com/queelius/the-policy"><strong>the-policy</strong></a>: AI alignment science fiction. A near-future story about the consequences of a specific alignment strategy. (<a href="/series/minds-and-machines/">Blog series</a>)</li>
<li><a href="https://github.com/queelius/call-of-asheron"><strong>call-of-asheron</strong></a>: Epic fantasy. Emergence, self-organization, and what happens when a world&rsquo;s underlying rules become visible to its inhabitants.</li>
</ul>
<p>The novels aren&rsquo;t decoration. They&rsquo;re explorations of the same themes that drive the code (consciousness, information, what persists and what doesn&rsquo;t) in a medium that allows for ambiguity and nuance that research papers can&rsquo;t.</p>
<p>There&rsquo;s also <a href="https://github.com/queelius/stories"><strong>stories</strong></a>, a collection of shorter fiction.</p>
<hr>
<h2 id="the-erdos-problems-database">The Erdos Problems Database</h2>
<p>One project worth mentioning separately: <a href="https://github.com/queelius/erdosproblems"><strong>erdosproblems</strong></a> (470 stars) is a community database for the problems listed on <a href="https://erdosproblems.com">erdosproblems.com</a>, Thomas Bloom&rsquo;s curation of Paul Erdos&rsquo;s open problems in combinatorics, number theory, and graph theory. My contribution is to the structured data layer: making the problems machine-readable, searchable, and cross-referenced. Same instinct that drives much of this ecosystem: make knowledge structured and open.</p>
<hr>
<h2 id="programming-languages-and-interpreters">Programming Languages and Interpreters</h2>
<p>A small cluster exploring language design:</p>
<ul>
<li><a href="https://github.com/queelius/jsl"><strong>jsl</strong></a>: JSON-based scripting language</li>
<li><a href="https://github.com/queelius/jsonl-algebra"><strong>jsonl-algebra</strong></a>: Algebraic operations on JSONL streams</li>
<li><a href="https://github.com/queelius/nfa-tools"><strong>nfa-tools</strong></a>: Non-deterministic finite automaton manipulation</li>
<li><a href="https://github.com/queelius/dagshell"><strong>dagshell</strong></a>: Shell with DAG-based execution</li>
<li><a href="https://github.com/queelius/tex2any"><strong>tex2any</strong></a> / <a href="https://github.com/queelius/texflow"><strong>texflow</strong></a>: LaTeX transformation tools</li>
</ul>
<p>These connect to the symbolic computation cluster (rerum, xtk, tree_rewriter) through their shared concern with formal language manipulation.</p>
<hr>
<h2 id="education-and-essay-series">Education and Essay Series</h2>
<p>Four ongoing series:</p>
<ul>
<li><a href="/series/stepanov/"><strong>stepanov</strong></a>: Generic programming in the style of Stepanov. Eleven essays on finding algebraic structure in algorithms.</li>
<li><a href="/series/sicp/"><strong>sicp</strong></a>: Abstraction and composition, inspired by Structure and Interpretation of Computer Programs.</li>
<li><a href="/series/the-learning-problem/"><strong>the-learning-problem</strong></a>: Machine learning from an information-theoretic perspective. Sequential prediction, Solomonoff induction, compression as learning.</li>
<li><a href="/series/the-long-echo/"><strong>the-long-echo</strong></a>: Digital legacy, personal data sovereignty, and what persists.</li>
</ul>
<hr>
<h2 id="space-simulation">Space Simulation</h2>
<p>Two projects that stand somewhat apart:</p>
<ul>
<li><a href="https://github.com/queelius/space-sandbox-sim"><strong>space-sandbox-sim</strong></a>: N-body physics sandbox</li>
<li><a href="https://github.com/queelius/star-system-sim"><strong>star-system-sim</strong></a>: Star system generation and simulation</li>
</ul>
<p>These connect to the C++ algorithmic work (barnes-hut tree algorithm for N-body simulation) and to the generic programming philosophy (physics simulation as algebraic structure).</p>
<hr>
<h2 id="infrastructure-and-meta-tools">Infrastructure and Meta-tools</h2>
<p>The projects that support everything else:</p>
<ul>
<li><a href="https://github.com/queelius/metafunctor"><strong>metafunctor</strong></a>: This Hugo blog. The central publication venue for all the work above.</li>
<li><a href="https://github.com/queelius/queelius.github.io"><strong>queelius.github.io</strong></a>: GitHub Pages deployment</li>
<li><a href="https://github.com/queelius/queelius.r-universe.dev"><strong>queelius.r-universe.dev</strong></a>: R-universe configuration for the R package ecosystem</li>
<li><a href="https://github.com/queelius/src2md"><strong>src2md</strong></a>: Convert source code to markdown for documentation</li>
<li><a href="https://github.com/queelius/texwatch"><strong>texwatch</strong></a>: Watch LaTeX files and rebuild on change</li>
<li><a href="https://github.com/queelius/sandrun"><strong>sandrun</strong></a>: Sandboxed command execution</li>
<li><a href="https://github.com/queelius/zeroipc"><strong>zeroipc</strong></a>: Zero-copy IPC mechanisms (multi-language)</li>
</ul>
<hr>
<h2 id="gap-analysis-whats-missing">Gap Analysis: What&rsquo;s Missing</h2>
<p>Looking at 120+ projects as a whole, several gaps become visible:</p>
<h3 id="no-unified-documentation-portal">No Unified Documentation Portal</h3>
<p>Sixty-two repos have GitHub Pages sites, but there&rsquo;s no cross-project index. If you want to understand how <code>algebraic.mle</code> connects to <code>likelihood.model</code> connects to <code>maskedcauses</code>, you have to read three separate documentation sites and stitch them together mentally. A single documentation portal with cross-linked API docs and a dependency graph would make the ecosystem navigable.</p>
<h3 id="missing-ci-infrastructure">Missing CI Infrastructure</h3>
<p>Many beta-stage repositories lack continuous integration. The R packages have CI through r-universe, but the Python and C++ projects are inconsistent. A standardized CI template would catch breakage earlier.</p>
<h3 id="no-rich-python-package-index">No Rich Python Package Index</h3>
<p>Over 20 packages are on PyPI under the <a href="https://pypi.org/user/queelius/">queelius</a> account, but the PyPI profile is just a flat list. There&rsquo;s no equivalent to <a href="https://queelius.r-universe.dev">r-universe</a>: no descriptions, no dependency graph, no grouping by theme.</p>
<h3 id="no-cross-project-dependency-graph">No Cross-Project Dependency Graph</h3>
<p>It&rsquo;s hard to see which projects build on which. The R packages have an implicit dependency tree, but it&rsquo;s not visualized. The *tk tools share conventions but not code. A visual dependency map would help contributors and users understand the architecture.</p>
<h3 id="c-libraries-lack-a-unifying-build-mechanism">C++ Libraries Lack a Unifying Build Mechanism</h3>
<p>The C++ projects don&rsquo;t share a package manager configuration (Conan, vcpkg, or similar). Each library is standalone. For users who want to use multiple libraries together (say, algebraic_hashing with sparse_spatial_hash) there&rsquo;s no easy integration path.</p>
<h3 id="scattered-fuzzy-logic">Scattered Fuzzy Logic</h3>
<p>Four separate fuzzy logic projects (fuzzy-infer, fuzzy-logic-search, fuzzy-soft-circuit, soft-circuit) share concepts but not code. They could be unified into a single, layered fuzzy logic library.</p>
<h3 id="the-tk-tools-need-a-meta-installer">The *tk Tools Need a Meta-Installer</h3>
<p>The Unix toolkit tools share a philosophy and naming convention, but there&rsquo;s no umbrella project, shared library, or meta-installer. Common patterns (SQLite storage, CLI argument parsing, query DSL) are reimplemented in each tool. A shared foundation library would reduce duplication.</p>
<h3 id="no-benchmarking-suite-for-probabilistic-data-structures">No Benchmarking Suite for Probabilistic Data Structures</h3>
<p>The hashing and data structure projects (maph, algebraic_hashing, sparse_spatial_hash, pfc) lack comparative benchmarks. A shared benchmark suite would make performance claims credible and help users choose between implementations.</p>
<h3 id="digital-legacy-projects-need-integration">Digital Legacy Projects Need Integration</h3>
<p>The longecho/longshade/posthumous projects articulate a compelling vision but exist as separate, early-stage prototypes. The integration story (how they work together, and how they connect to the *tk tools) hasn&rsquo;t been built yet.</p>
<h3 id="missing-end-to-end-tutorials">Missing End-to-End Tutorials</h3>
<p>The R packages have individual documentation, but there&rsquo;s no tutorial showing how to use them together: generate synthetic masked data, fit models, compare via AIC, bootstrap confidence intervals, and visualize results, all using the composable architecture.</p>
<hr>
<h2 id="where-to-start">Where to Start</h2>
<p>If you&rsquo;ve read this far and want to explore, here are entry points by interest:</p>
<p><strong>If you&rsquo;re a statistician or reliability engineer:</strong>
Start with <a href="/projects/algebraic.mle/">algebraic.mle</a> (on CRAN), then read the <a href="/post/2026-02-05-likelihood-model-series-md/">maskedcauses post</a>. The composable likelihood architecture is the most mature part of the ecosystem.</p>
<p><strong>If you&rsquo;re a C++ programmer interested in generic programming:</strong>
Start with the <a href="/series/stepanov/">Stepanov series</a>, especially <a href="/post/2026-01-18-synthesis-stepanov/">Seeing Structure First</a>. Then look at the C++ libraries (algebraic_hashing, maph, pfc) for worked examples.</p>
<p><strong>If you work with trees or graphs in Python:</strong>
<a href="https://github.com/queelius/AlgoTree">AlgoTree</a> and <a href="https://github.com/queelius/AlgoGraph">AlgoGraph</a> are on PyPI and treat data structures as composable algebraic objects.</p>
<p><strong>If you care about personal data sovereignty:</strong>
Start with <a href="https://github.com/queelius/btk">btk</a> (bookmarks) or <a href="https://github.com/queelius/ctk">ctk</a> (conversations). They&rsquo;re the most polished *tk tools. Then read the <a href="/series/the-long-echo/">Long Echo</a> essays for the philosophical motivation.</p>
<p><strong>If you&rsquo;re interested in AI/LLM tools:</strong>
<a href="https://github.com/queelius/elasticsearch-lm">elasticsearch-lm</a> bridges traditional search with language models. <a href="https://github.com/queelius/mcts-reasoning">mcts-reasoning</a> applies tree search to LLM reasoning.</p>
<p><strong>If you&rsquo;re a mathematician:</strong>
The <a href="https://github.com/queelius/encrypted_search_thesis">oblivious computing papers</a> apply algebra to cryptography. The <a href="/series/the-learning-problem/">information theory posts</a> connect Solomonoff induction to practical prediction.</p>
<p><strong>If you want to read fiction:</strong>
<a href="https://github.com/queelius/echoes-of-the-sublime">echoes-of-the-sublime</a> is the longest and most developed. <a href="https://github.com/queelius/the-policy">the-policy</a> is the most timely: AI alignment as lived experience.</p>
<hr>
<h2 id="the-shape-of-the-whole">The Shape of the Whole</h2>
<p>If I step back and look at everything together, I see a single question asked in many registers:</p>
<blockquote>
<p><em>How do you build reliable knowledge from noisy, incomplete, and partial observations?</em></p>
</blockquote>
<p>In encrypted search, the noise is intentional. It&rsquo;s the price of privacy.
In reliability engineering, the noise is censoring and masking. The data you wish you had.
In fuzzy logic, the noise is inherent in vague predicates.
In LLM reasoning, the noise is the stochasticity of language models.
In digital legacy, the noise is time itself. Bit rot, platform decay, forgetting.</p>
<p>The answer, in every case, is the same: <em>algebraic structure.</em> Find the right abstractions, make them compose, and the noise becomes manageable.</p>
<p>What does 120+ projects look like, all at once? It looks like a person thinking out loud for over a decade. The early projects are tentative: data structures, small algorithms, conference papers. The middle period is the thesis work, rigorous, focused, building toward specific results. The recent projects are more ambitious and more personal: novels, digital legacy tools, an essay about watching intelligence leave the body.</p>
<p>The connecting thread isn&rsquo;t a technology or a domain. It&rsquo;s a question: <strong>what structures persist?</strong> Algebraic structures persist across implementations. Mathematical results persist across paradigms. Well-designed software interfaces persist across refactoring. Stories persist across readers. And if you&rsquo;re careful about formats and infrastructure, personal data can persist across a lifetime, and beyond.</p>
<p>That&rsquo;s the ecosystem. It&rsquo;s incomplete, unevenly documented, and will probably never be finished. But the map is here now, and the territory is open.</p>
<hr>
<p><strong>Resources:</strong></p>
<ul>
<li><a href="https://github.com/queelius">GitHub profile</a></li>
<li><a href="https://pypi.org/user/queelius/">PyPI packages</a></li>
<li><a href="https://queelius.r-universe.dev">R-universe packages</a></li>
<li><a href="/series/">All blog series</a></li>
<li><a href="/projects/">All project pages</a></li>
</ul>]]></content:encoded>
      
      
      <category>ecosystem</category>
      
      <category>open-source</category>
      
      <category>overview</category>
      
      <category>oblivious-computing</category>
      
      <category>statistics</category>
      
      <category>reliability</category>
      
      <category>generic-programming</category>
      
      <category>unix-philosophy</category>
      
      <category>digital-legacy</category>
      
      <category>AI</category>
      
      <category>LLM</category>
      
      <category>R</category>
      
      <category>C&#43;&#43;</category>
      
      <category>Python</category>
      
      
    </item>
    
    <item>
      <title>Observation Functors: Composable Censoring for Series System Simulation</title>
      <link>http://localhost:40461/post/2026-02-13-observation-functors/</link>
      <pubDate>Fri, 13 Feb 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/post/2026-02-13-observation-functors/</guid>
      <description>Observation functors in maskedcauses: composable functions that separate the data-generating process from the observation mechanism, enabling mixed-censoring simulation and verified Monte Carlo studies.</description>
      <content:encoded><![CDATA[<p>Last week I <a href="/post/2026-02-05-likelihood-model-series-md/">announced</a> <code>maskedcauses</code>, the R package for estimating component reliability from masked series system failures. That post covered the three likelihood models and the path to CRAN.</p>
<p>This post is about what happened next: the package now supports <strong>four observation types</strong> (exact, right-censored, left-censored, and interval-censored) via composable <strong>observation functors</strong>. Along the way, I wrote four vignettes, removed the <code>md.tools</code> dependency, and developed a verification methodology for keeping prose honest about simulation results.</p>
<h2 id="the-problem-hardcoded-right-censoring">The Problem: Hardcoded Right-Censoring</h2>
<p>The original <code>rdata()</code> method for each model had a simple observation mechanism baked in: if a system failed before the study end time \(\tau\), you saw the exact failure time; if it survived past \(\tau\), you got a right-censored observation. Two outcomes, one knob.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="c1"># The old way: right-censoring was the only option</span>
</span></span><span class="line"><span class="cl"><span class="n">gen</span> <span class="o">&lt;-</span> <span class="nf">rdata</span><span class="p">(</span><span class="n">model</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">df</span> <span class="o">&lt;-</span> <span class="nf">gen</span><span class="p">(</span><span class="n">theta</span> <span class="o">=</span> <span class="nf">c</span><span class="p">(</span><span class="m">1.0</span><span class="p">,</span> <span class="m">1.1</span><span class="p">,</span> <span class="m">0.95</span><span class="p">),</span> <span class="n">n</span> <span class="o">=</span> <span class="m">500</span><span class="p">,</span> <span class="n">tau</span> <span class="o">=</span> <span class="m">3</span><span class="p">,</span> <span class="n">p</span> <span class="o">=</span> <span class="m">0.3</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="c1"># Every observation is either omega = &#34;exact&#34; or omega = &#34;right&#34;</span>
</span></span></code></pre></div><p>This is fine for textbook problems, but real monitoring produces other kinds of incomplete data:</p>
<ul>
<li>
<p><strong>Left-censoring</strong>: You inspect a system once. If it&rsquo;s already failed, you know it died before the inspection, but not when. Single-inspection monitoring of remote equipment works this way.</p>
</li>
<li>
<p><strong>Interval-censoring</strong>: You inspect periodically. A system that was running at inspection \(k\) but failed by inspection \(k+1\) gives you a bracket \([t_k, t_{k+1}]\) containing the failure time. This is the norm in industrial maintenance, medical follow-up studies, and field reliability testing.</p>
</li>
<li>
<p><strong>Mixed monitoring</strong>: Different units in the same study may be observed under different schemes. Some get continuous monitoring (exact/right), some get periodic inspections (interval), some get a single check (left).</p>
</li>
</ul>
<p>Hardcoding the observation mechanism into the data-generating process conflates two orthogonal concerns: <em>how components fail</em> (the DGP, governed by the lifetime distribution) and <em>what we see</em> (the observation mechanism, governed by the monitoring protocol). These should be separate.</p>
<h2 id="observation-functors">Observation Functors</h2>
<p>The solution is a small algebra of <strong>observation functors</strong>, closures that transform true failure times into observed data. Each functor has the same signature:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="c1"># Signature: function(t_true) -&gt; list(t, omega, t_upper)</span>
</span></span></code></pre></div><p>Given the true failure time <code>t_true</code>, the functor returns what the observer actually sees: the recorded time <code>t</code>, the observation type <code>omega</code> (one of <code>&quot;exact&quot;</code>, <code>&quot;right&quot;</code>, <code>&quot;left&quot;</code>, <code>&quot;interval&quot;</code>), and an upper bound <code>t_upper</code> for interval observations.</p>
<p>Four primitives:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="c1"># Exact observation if t &lt; tau, right-censored otherwise</span>
</span></span><span class="line"><span class="cl"><span class="nf">observe_right_censor</span><span class="p">(</span><span class="n">tau</span> <span class="o">=</span> <span class="m">100</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Single inspection: left-censored if failed, right-censored if running</span>
</span></span><span class="line"><span class="cl"><span class="nf">observe_left_censor</span><span class="p">(</span><span class="n">tau</span> <span class="o">=</span> <span class="m">50</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Periodic inspection: interval-censored in [floor, ceiling]</span>
</span></span><span class="line"><span class="cl"><span class="nf">observe_periodic</span><span class="p">(</span><span class="n">delta</span> <span class="o">=</span> <span class="m">10</span><span class="p">,</span> <span class="n">tau</span> <span class="o">=</span> <span class="m">100</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Randomly select a scheme per observation</span>
</span></span><span class="line"><span class="cl"><span class="nf">observe_mixture</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">  <span class="nf">observe_right_censor</span><span class="p">(</span><span class="n">tau</span> <span class="o">=</span> <span class="m">100</span><span class="p">),</span>
</span></span><span class="line"><span class="cl">  <span class="nf">observe_left_censor</span><span class="p">(</span><span class="n">tau</span> <span class="o">=</span> <span class="m">50</span><span class="p">),</span>
</span></span><span class="line"><span class="cl">  <span class="nf">observe_periodic</span><span class="p">(</span><span class="n">delta</span> <span class="o">=</span> <span class="m">10</span><span class="p">,</span> <span class="n">tau</span> <span class="o">=</span> <span class="m">100</span><span class="p">),</span>
</span></span><span class="line"><span class="cl">  <span class="n">weights</span> <span class="o">=</span> <span class="nf">c</span><span class="p">(</span><span class="m">0.5</span><span class="p">,</span> <span class="m">0.2</span><span class="p">,</span> <span class="m">0.3</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span></code></pre></div><p>The key design property: <strong>functors compose</strong>. <code>observe_mixture</code> takes functors as arguments and returns a new functor with the same signature. You can nest mixtures, combine any primitives, or write your own functor. Anything with signature <code>function(t_true) -&gt; list(t, omega, t_upper)</code> works.</p>
<p>This draws from the same compositional philosophy behind <a href="/projects/compositional.mle/">compositional.mle</a> and the broader ecosystem: define small, well-typed building blocks; compose them into complex behavior. The SICP maxim applies: the means of combination should produce objects of the same type as the things being combined.</p>
<h3 id="using-functors-with-rdata">Using Functors with <code>rdata()</code></h3>
<p>The <code>rdata()</code> method on every model now accepts an <code>observe</code> parameter:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="n">model</span> <span class="o">&lt;-</span> <span class="nf">exp_series_md_c1_c2_c3</span><span class="p">()</span>
</span></span><span class="line"><span class="cl"><span class="n">gen</span> <span class="o">&lt;-</span> <span class="nf">rdata</span><span class="p">(</span><span class="n">model</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Right-censoring (default, backwards-compatible)</span>
</span></span><span class="line"><span class="cl"><span class="n">df_right</span> <span class="o">&lt;-</span> <span class="nf">gen</span><span class="p">(</span><span class="n">theta</span><span class="p">,</span> <span class="n">n</span> <span class="o">=</span> <span class="m">500</span><span class="p">,</span> <span class="n">tau</span> <span class="o">=</span> <span class="m">5</span><span class="p">,</span> <span class="n">p</span> <span class="o">=</span> <span class="m">0.3</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Periodic inspection every 0.5 time units</span>
</span></span><span class="line"><span class="cl"><span class="n">df_interval</span> <span class="o">&lt;-</span> <span class="nf">gen</span><span class="p">(</span><span class="n">theta</span><span class="p">,</span> <span class="n">n</span> <span class="o">=</span> <span class="m">500</span><span class="p">,</span> <span class="n">p</span> <span class="o">=</span> <span class="m">0.3</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                   <span class="n">observe</span> <span class="o">=</span> <span class="nf">observe_periodic</span><span class="p">(</span><span class="n">delta</span> <span class="o">=</span> <span class="m">0.5</span><span class="p">,</span> <span class="n">tau</span> <span class="o">=</span> <span class="m">5</span><span class="p">))</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Mixed monitoring environment</span>
</span></span><span class="line"><span class="cl"><span class="n">df_mixed</span> <span class="o">&lt;-</span> <span class="nf">gen</span><span class="p">(</span><span class="n">theta</span><span class="p">,</span> <span class="n">n</span> <span class="o">=</span> <span class="m">500</span><span class="p">,</span> <span class="n">p</span> <span class="o">=</span> <span class="m">0.3</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                <span class="n">observe</span> <span class="o">=</span> <span class="nf">observe_mixture</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">                  <span class="nf">observe_right_censor</span><span class="p">(</span><span class="n">tau</span> <span class="o">=</span> <span class="m">5</span><span class="p">),</span>
</span></span><span class="line"><span class="cl">                  <span class="nf">observe_left_censor</span><span class="p">(</span><span class="n">tau</span> <span class="o">=</span> <span class="m">3</span><span class="p">),</span>
</span></span><span class="line"><span class="cl">                  <span class="nf">observe_periodic</span><span class="p">(</span><span class="n">delta</span> <span class="o">=</span> <span class="m">0.5</span><span class="p">,</span> <span class="n">tau</span> <span class="o">=</span> <span class="m">5</span><span class="p">),</span>
</span></span><span class="line"><span class="cl">                  <span class="n">weights</span> <span class="o">=</span> <span class="nf">c</span><span class="p">(</span><span class="m">0.6</span><span class="p">,</span> <span class="m">0.2</span><span class="p">,</span> <span class="m">0.2</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">                <span class="p">))</span>
</span></span></code></pre></div><p>When <code>observe</code> is <code>NULL</code> (the default), <code>rdata()</code> uses <code>observe_right_censor(tau)</code> for full backwards compatibility. No existing code breaks.</p>
<h3 id="what-the-likelihood-sees">What the Likelihood Sees</h3>
<p>Each observation type contributes differently to the log-likelihood. Under the C1-C2-C3 masking conditions:</p>
<table>
  <thead>
      <tr>
          <th>Type</th>
          <th>Log-likelihood contribution</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Exact</td>
          <td>\(\log\bigl(\sum_{j \in C_i} h_j(t_i)\bigr) - H(t_i)\)</td>
      </tr>
      <tr>
          <td>Right</td>
          <td>\(-H(t_i)\)</td>
      </tr>
      <tr>
          <td>Left</td>
          <td>\(\log\bigl(\sum_{j \in C_i} \int_0^{t_i} h_j(u)R(u)\,du\bigr) - \log(1 - R(t_i))\)</td>
      </tr>
      <tr>
          <td>Interval</td>
          <td>\(\log\bigl(\sum_{j \in C_i} \int_a^b h_j(u)R(u)\,du\bigr) - \log(R(a) - R(b))\)</td>
      </tr>
  </tbody>
</table>
<p>Here \(h_j\) is component \(j\)&rsquo;s hazard, \(H = \sum_j H_j\) is the system cumulative hazard, and \(R = \exp(-H)\) is the system reliability function. Exact observations carry the most information (you see the hazard directly). Right-censored observations say only &ldquo;survived this long.&rdquo; Left and interval observations fall between these extremes.</p>
<p>The three models handle these integrals differently:</p>
<ul>
<li><strong>Exponential</strong>: all four types have closed-form expressions for loglik, score, and Hessian</li>
<li><strong>Homogeneous Weibull</strong>: closed-form loglik for all types via \(w_c\) weights (shared shape simplifies the integral); numerical Hessian via <code>numDeriv::jacobian</code></li>
<li><strong>Heterogeneous Weibull</strong>: closed-form for exact/right; <code>stats::integrate</code> for left/interval; numerical Hessian</li>
</ul>
<h2 id="four-vignettes">Four Vignettes</h2>
<p>The observation functor system needed validation across all three models and multiple censoring configurations. This produced four vignettes, each a complete Monte Carlo study with precomputed results.</p>
<h3 id="exponential-series">Exponential Series</h3>
<p>The <a href="https://queelius.github.io/maskedcauses/articles/exponential_series.html">exponential series vignette</a> is the tutorial entry point. It walks through the full pipeline: model construction, data generation, MLE fitting, and Monte Carlo simulation under all four observation types.</p>
<p>The exponential case is special because everything is analytical. The score and Hessian have closed-form expressions for all observation types, so the MLE converges fast and asymptotic standard errors are exact (no numerical differentiation). This makes it the ideal test bed for the functor system. Any discrepancy between analytical and numerical results points to a bug in the functor, not in a numerical approximation.</p>
<p>The vignette also covers masking probability sensitivity: as the masking probability \(p\) decreases (more masking), RMSE increases and confidence interval coverage degrades. The fundamental tradeoff in masked data: information loss from incomplete cause identification.</p>
<h3 id="homogeneous-weibull">Homogeneous Weibull</h3>
<p>The <a href="https://queelius.github.io/maskedcauses/articles/weibull_homogeneous_series.html">homogeneous Weibull vignette</a> studies the shared-shape model across three hazard regimes:</p>
<ul>
<li><strong>DFR</strong> (decreasing failure rate, \(k < 1\)): Early failures dominate. Common in burn-in periods and infant mortality.</li>
<li><strong>CFR</strong> (constant failure rate, \(k = 1\)): Reduces to exponential. Memoryless.</li>
<li><strong>IFR</strong> (increasing failure rate, \(k > 1\)): Wear-out dominates. The typical aging process.</li>
</ul>
<p>The mathematical insight: when all Weibull components share shape \(k\), the system lifetime is also Weibull with shape \(k\) and system scale \(\beta_s = (\sum_j \beta_j^{-k})^{-1/k}\). This means the left- and interval-censored likelihood contributions have closed-form expressions using weight factors \(w_c = \beta_c^{-k} / \sum_j \beta_j^{-k}\), no numerical integration needed. The shared shape is a strong assumption, but when it holds, estimation is remarkably efficient.</p>
<h3 id="heterogeneous-weibull">Heterogeneous Weibull</h3>
<p>The <a href="https://queelius.github.io/maskedcauses/articles/weibull_series.html">heterogeneous Weibull vignette</a> drops the shared-shape assumption. Each component gets its own shape \(k_j\) and scale \(\beta_j\), doubling the parameter count to \(2m\).</p>
<p>With mixed shapes, the system lifetime is <em>not</em> Weibull. It&rsquo;s a competing-risks minimum of Weibull variates with different shape parameters. The left- and interval-censored contributions require numerical integration via <code>stats::integrate</code>. Slower, but handles the realistic case where components age at different rates (e.g., a capacitor with IFR alongside a solder joint with DFR).</p>
<h3 id="censoring-comparison">Censoring Comparison</h3>
<p>The <a href="https://queelius.github.io/maskedcauses/articles/censoring_comparison.html">censoring comparison vignette</a> is the cross-cutting analysis. It runs the exponential model under five monitoring configurations:</p>
<table>
  <thead>
      <tr>
          <th>Config</th>
          <th>Description</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>A</td>
          <td>Exact + right (continuous monitoring)</td>
      </tr>
      <tr>
          <td>B</td>
          <td>Left + right (single inspection)</td>
      </tr>
      <tr>
          <td>C</td>
          <td>Interval + right (periodic, wide intervals)</td>
      </tr>
      <tr>
          <td>D</td>
          <td>Interval + right (periodic, narrow intervals)</td>
      </tr>
      <tr>
          <td>E</td>
          <td>Mixed (all observation types)</td>
      </tr>
  </tbody>
</table>
<p>The results confirm the expected information ordering: <strong>exact &gt; narrow-interval &gt; wide-interval &gt; left \(\approx\) right</strong>. Exact observations carry the most information because you see the failure time precisely. Narrow intervals are close to exact. Left-censored data is nearly as uninformative as right-censored: knowing only &ldquo;it failed before \(\tau\)&rdquo; is almost as vague as &ldquo;it survived past \(\tau\).&rdquo;</p>
<p>The practical takeaway: if you&rsquo;re designing a monitoring protocol and can choose between a single end-of-study inspection (left-censoring) and periodic inspections (interval-censoring), periodic inspections are strictly better for parameter estimation. The frequency of inspection trades off against cost. Narrower intervals give better estimates but require more inspections.</p>
<h2 id="verified-prose">Verified Prose</h2>
<p>Writing prose about simulation results is easy to get wrong. You run a Monte Carlo study, get an RMSE of 0.042, then write &ldquo;RMSE ranges from 0.035 to 0.045.&rdquo; Later someone changes the simulation seed or sample size, and the prose is silently wrong.</p>
<p>I developed a verification methodology for this package: <strong>every numerical claim in vignette commentary is cross-checked against the precomputed <code>.rds</code> data</strong>.</p>
<p>The workflow:</p>
<ol>
<li>Extract values: <code>readRDS(&quot;vignettes/precomputed_results.rds&quot;)</code></li>
<li>Compute metrics from the raw simulation output</li>
<li>Round consistently (3 decimal places for RMSE, 1 for percentages)</li>
<li>Only then write the prose</li>
</ol>
<p>This caught several errors during the vignette audit. An RMSE range was off by 0.001. A coverage percentage rounded the wrong direction. A score comparison paragraph claimed &ldquo;near zero&rdquo; when the actual values were order \(10^{-6}\), technically true but not the same as &ldquo;precisely zero&rdquo; which is what holds at the analytical MLE.</p>
<p>The <code>.rds</code> files have different structures across vignettes (the exponential data is a flat list; the homogeneous Weibull data is nested by scenario; the censoring comparison has pre-aggregated summaries), so the verification code isn&rsquo;t one-size-fits-all. But the principle is: <strong>no prose about numbers without first reading the numbers</strong>.</p>
<h2 id="mdtools-removal"><code>md.tools</code> Removal</h2>
<p>The <code>md.tools</code> package provided four utility functions for encoding and decoding masked data matrices:</p>
<ul>
<li><code>md_decode_matrix</code>: Extract prefixed columns (e.g., <code>x1, x2, x3</code>) as a matrix</li>
<li><code>md_encode_matrix</code>: Convert matrix to prefixed columns in a data frame</li>
<li><code>md_mark_latent</code>: Tag columns as latent (for display purposes)</li>
<li><code>md_boolean_matrix_to_charsets</code>: Convert Boolean columns to set notation (<code>{1, 3}</code>)</li>
</ul>
<p>These are small functions. The largest is about 10 lines. Carrying a GitHub remote dependency for 40 lines of base R was unnecessary friction for installation and CRAN submission.</p>
<p>I internalized all four into <code>R/md_compat.R</code>, keeping the same function names and signatures. The package now has three GitHub remotes (down from four):</p>
<ul>
<li><a href="https://github.com/queelius/algebraic.mle">algebraic.mle</a>: MLE algebra</li>
<li><a href="https://github.com/queelius/likelihood.model">likelihood.model</a>: Inference framework</li>
<li><a href="https://github.com/queelius/algebraic.dist">algebraic.dist</a>: Distribution utilities</li>
</ul>
<p>Zero non-base R dependencies for the core matrix encoding/decoding operations. The <code>generics</code> package for <code>fit</code> is the only remaining external import beyond the ecosystem packages.</p>
<h2 id="testing">Testing</h2>
<p>The observation functor system is validated by 798 tests at 99.45% line coverage. The test suite mirrors the source file structure:</p>
<ul>
<li><code>test-observe.R</code>: Unit tests for each primitive functor (boundary conditions, output format)</li>
<li><code>test-censoring.R</code>: Integration tests for the full pipeline (generate data, fit model, check estimates)</li>
<li><code>test-exp_series_md_c1_c2_c3.R</code>, <code>test-wei_series_*.R</code>: Model-specific tests for all four observation types</li>
</ul>
<p>The censoring integration tests are the important ones: they verify that the loglik, score, and Hessian functions handle mixed observation types correctly. A dataset with some exact, some right, some left, and some interval observations should produce a valid log-likelihood that MLE can optimize.</p>
<h2 id="whats-next">What&rsquo;s Next</h2>
<p><strong>CRAN submission.</strong> The package is <code>R CMD check</code>-clean with no notes or warnings. Once <a href="/projects/likelihood.model/">likelihood.model</a> is accepted, this package follows immediately.</p>
<p><strong>Arbitrary hazard functions.</strong> The current models assume exponential or Weibull components. The <a href="/projects/maskedhaz/">maskedhaz</a> extension will support components specified by arbitrary hazard functions via <a href="/projects/flexhaz/">flexhaz</a>: define a component by its hazard rate and get likelihood contributions automatically.</p>
<p><strong>Relaxed masking.</strong> The C2 condition (uniform masking within candidate sets) is often unrealistic. <a href="/papers/mdrelax/">mdrelax</a> extends the model to handle non-uniform masking probabilities, but its integration with the observation functor system is not yet complete.</p>
<p><strong><code>observe_mixture</code> as a model for heterogeneous monitoring.</strong> The current mixture functor randomly assigns observation schemes. A natural extension is to let the monitoring scheme depend on covariates: different sites, different equipment classes, different maintenance budgets.</p>
<hr>
<p><strong>Resources</strong>:</p>
<ul>
<li><a href="https://github.com/queelius/maskedcauses">GitHub repository</a></li>
<li><a href="https://queelius.github.io/maskedcauses/">Package documentation</a></li>
<li><a href="/post/2026-02-05-likelihood-model-series-md/">Previous post: Package announcement</a></li>
<li><a href="https://queelius.github.io/maskedcauses/reference/observe_right_censor.html">Observation functor API reference</a></li>
<li><a href="https://queelius.github.io/maskedcauses/articles/censoring_comparison.html">Censoring comparison vignette</a></li>
<li><a href="/series/statistical-reliability/">Series landing page</a></li>
</ul>]]></content:encoded>
      
      
      <category>R</category>
      
      <category>statistics</category>
      
      <category>reliability</category>
      
      <category>series systems</category>
      
      <category>masked data</category>
      
      <category>maximum likelihood</category>
      
      <category>censoring</category>
      
      <category>simulation</category>
      
      <category>observation functors</category>
      
      <category>Weibull</category>
      
      
    </item>
    
    <item>
      <title>pagevault: Hiding an Encryption Platform Inside HTML</title>
      <link>http://localhost:40461/post/2026-02-13-pagevault/</link>
      <pubDate>Fri, 13 Feb 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/post/2026-02-13-pagevault/</guid>
      <description>pagevault turns any file into a self-contained encrypted HTML page. No backend, no JavaScript libraries, no external dependencies. Just AES-256-GCM and the browser&#39;s built-in Web Crypto API. The interesting part is making it work at scale.</description>
      <content:encoded><![CDATA[<p>HTML is an encryption container format. That sounds wrong, but think about what an HTML file can hold: arbitrary data in script tags or data attributes, a full programming runtime via JavaScript, and a rendering engine (the browser) on every device on the planet. If you embed encrypted data and the code to decrypt it, the result is a file that looks inert until someone types the right password.</p>
<p><a href="https://github.com/queelius/pagevault">pagevault</a> takes this idea seriously. It encrypts files, documents, images, entire websites, into self-contained HTML pages that decrypt in the browser. No backend. No JavaScript crypto libraries. The browser already has AES-256-GCM built in via the Web Crypto API. pagevault just has to match the parameters exactly on the Python side and embed the right 200 lines of JavaScript.</p>
<p>The output is a single <code>.html</code> file. You can email it, put it on a USB stick, host it on GitHub Pages, or double-click it on your desktop. It doesn&rsquo;t phone home, it doesn&rsquo;t load CDNs, it doesn&rsquo;t need anything except a browser.</p>
<hr>
<h2 id="what-goes-in">What Goes In</h2>
<p>Anything.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">pagevault lock report.pdf              <span class="c1"># PDF with embedded viewer</span>
</span></span><span class="line"><span class="cl">pagevault lock photo.jpg               <span class="c1"># image with click-to-zoom</span>
</span></span><span class="line"><span class="cl">pagevault lock notes.md                <span class="c1"># markdown, rendered or source view</span>
</span></span><span class="line"><span class="cl">pagevault lock recording.mp3           <span class="c1"># audio player</span>
</span></span><span class="line"><span class="cl">pagevault lock mysite/ --site          <span class="c1"># entire multi-page website</span>
</span></span><span class="line"><span class="cl">pagevault lock page.html               <span class="c1"># HTML with selective region encryption</span>
</span></span></code></pre></div><p>Every output is a single <code>.html</code> file containing the ciphertext, a password prompt, the decryption runtime, and a viewer plugin for the content type. Seven viewers ship built-in: Image, PDF, HTML, Text (with line numbers), Markdown (with rendered/source toggle), Audio, and Video. They&rsquo;re a plugin system, so you can add your own.</p>
<p>Here&rsquo;s what it looks like. An encrypted security report:</p>
<p><img src="report-locked.png" alt="Encrypted file showing password prompt with lock icon and filename"></p>
<p>After entering the password, the viewer renders the decrypted content with a toolbar:</p>
<p><img src="report-unlocked.png" alt="Decrypted quarterly security report with vulnerability tables and findings"></p>
<p>For directories, <code>--site</code> bundles everything into a single encrypted HTML file. The directory is zipped with deflate compression, encrypted, and embedded. On the browser side, a minimal zip reader (no library, just the built-in <code>DecompressionStream</code> API) unpacks it after decryption. Internal links between pages work. CSS and images load from the zip. The whole site navigates inside an iframe as if it were served normally:</p>
<p><img src="site-index.png" alt="Decrypted site viewer showing a Project Hub with navigation, welcome section, and sprint stats"></p>
<p>A 100 MB site compresses well if it&rsquo;s mostly text and markup. The zip gets chunked and encrypted the same way single files do, so the same progress bar and memory management apply. I&rsquo;ve tested sites with hundreds of files without issues.</p>
<hr>
<h2 id="the-crypto">The Crypto</h2>
<p>Nothing exotic here. AES-256-GCM for authenticated encryption, PBKDF2-SHA256 with 310,000 iterations for key derivation, all through the browser&rsquo;s Web Crypto API. The interesting part isn&rsquo;t the cryptography. It&rsquo;s making the container format work at scale.</p>
<p>Multi-user access uses CEK (content-encryption key) wrapping. A random key encrypts the data once. That key is then wrapped separately for each user&rsquo;s derived key:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">Content  ──encrypt with CEK──&gt;  Ciphertext (one copy)
</span></span><span class="line"><span class="cl">CEK  ──wrap with Alice&#39;s key──&gt;  Key Blob A
</span></span><span class="line"><span class="cl">CEK  ──wrap with Bob&#39;s key────&gt;  Key Blob B
</span></span></code></pre></div><p>Adding a user wraps one small key blob. Removing a user deletes one blob. The bulk content stays untouched.</p>
<hr>
<h2 id="the-hard-part-large-files">The Hard Part: Large Files</h2>
<p>The basic approach (encrypt, base64-encode, embed in HTML) works fine for small files. The problems start when you try to encrypt an 84 MB conversation archive or a 179 MB HTML report.</p>
<p>The original v2 format had a compounding overhead problem. File bytes were base64-encoded (33% expansion), then encrypted, then the ciphertext was base64-encoded again (another 33%). That&rsquo;s <code>1.33 * 1.33 = 1.77x</code> total overhead. An 84 MB file produced a 198 MB HTML page. Wasteful, and slow to decrypt because the browser had to hold the entire blob in memory at once.</p>
<p>v3 fixes this with chunked encryption.</p>
<h3 id="eliminating-the-double-base64">Eliminating the double base64</h3>
<p>v2 encrypted a base64 string, then base64-encoded the result. Two layers. v3 encrypts the raw bytes directly and base64-encodes once. The metadata (filename, MIME type, size) is encrypted separately instead of being bundled into the content. This alone cuts the overhead from 77% to about 39%.</p>
<h3 id="chunked-ciphertext">Chunked ciphertext</h3>
<p>Instead of one giant encrypted blob, v3 splits content into 1 MB chunks. Each chunk is encrypted independently with AES-256-GCM using a counter-derived IV: the chunk index is XORed into the last four bytes of a base IV. Each chunk becomes its own <code>&lt;script&gt;</code> tag in the HTML:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-html" data-lang="html"><span class="line"><span class="cl"><span class="p">&lt;</span><span class="nt">script</span> <span class="na">id</span><span class="o">=</span><span class="s">&#34;pv-0&#34;</span> <span class="na">type</span><span class="o">=</span><span class="s">&#34;x-pv&#34;</span><span class="p">&gt;</span><span class="nx">base64</span><span class="o">-</span><span class="k">of</span><span class="o">-</span><span class="nx">chunk</span><span class="o">-</span><span class="mf">0.</span><span class="p">..&lt;/</span><span class="nt">script</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="p">&lt;</span><span class="nt">script</span> <span class="na">id</span><span class="o">=</span><span class="s">&#34;pv-1&#34;</span> <span class="na">type</span><span class="o">=</span><span class="s">&#34;x-pv&#34;</span><span class="p">&gt;</span><span class="nx">base64</span><span class="o">-</span><span class="k">of</span><span class="o">-</span><span class="nx">chunk</span><span class="o">-</span><span class="mf">1.</span><span class="p">..&lt;/</span><span class="nt">script</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl">...
</span></span><span class="line"><span class="cl"><span class="p">&lt;</span><span class="nt">script</span> <span class="na">id</span><span class="o">=</span><span class="s">&#34;pv-83&#34;</span> <span class="na">type</span><span class="o">=</span><span class="s">&#34;x-pv&#34;</span><span class="p">&gt;</span><span class="nx">base64</span><span class="o">-</span><span class="k">of</span><span class="o">-</span><span class="nx">chunk</span><span class="o">-</span><span class="mf">83.</span><span class="p">..&lt;/</span><span class="nt">script</span><span class="p">&gt;</span>
</span></span></code></pre></div><p>The browser decrypts them sequentially, showing a progress bar. After each chunk is decrypted, the corresponding script tag is removed from the DOM (<code>el.remove()</code>), freeing the base64 text for garbage collection. Memory usage stays proportional to the chunk size, not the file size.</p>
<h3 id="the-numbers">The numbers</h3>
<p>That 84 MB conversation archive: v2 produced 198 MB. v3 produces 117 MB. A 41% reduction, and the decryption doesn&rsquo;t choke the browser.</p>
<p>Here&rsquo;s the 84 MB file after decryption, rendered as a searchable conversation interface:</p>
<p><img src="v3-84mb-decrypted.png" alt="Decrypted 83.4 MB conversation archive showing search interface with toolbar"></p>
<p>I&rsquo;ve also tested a 315 MB text file (works, slow to load) and a 179 MB HTML file with 1.5 million DOM elements (DOM rendering is the bottleneck there, not decryption). These are probably past the point of reason for an HTML container, but it&rsquo;s nice to know where the limits actually are.</p>
<hr>
<h2 id="region-encryption">Region Encryption</h2>
<p>pagevault also handles a more specific case: encrypting parts of an HTML page while leaving the rest public. You mark sections with <code>&lt;pagevault&gt;</code> tags and only those become ciphertext. Public navigation, styling, scripts, all untouched. Useful for dashboards where some metrics are public and others aren&rsquo;t, or documentation with confidential sections.</p>
<p>The lock operation is composable: its output is valid input. You can encrypt a page for Alice, then encrypt different sections for Bob. The page round-trips cleanly through multiple passes.</p>
<hr>
<h2 id="the-file-problem">The <code>file://</code> Problem</h2>
<p>One thing that surprised me. Encrypted HTML files opened from the filesystem (<code>file://</code> URLs) behave differently than files served over HTTP. The <code>file://</code> protocol gives pages an opaque <code>null</code> origin, which breaks <code>localStorage</code> (so &ldquo;remember on this device&rdquo; doesn&rsquo;t work) and blocks nested blob URLs (so the HTML viewer can&rsquo;t create iframes the normal way).</p>
<p>The fix was <code>srcdoc</code> iframes, which inherit the parent&rsquo;s origin, plus a <code>pushState</code> shim for the URL bar. Not glamorous, but it means encrypted files work identically whether you double-click them on your desktop or serve them from a CDN.</p>
<p>There&rsquo;s also a built-in dev server for when you want HTTP without the hassle:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">pagevault serve _locked/ --open    <span class="c1"># HTTP server on localhost:8765</span>
</span></span></code></pre></div><hr>
<h2 id="related-cryptoid">Related: cryptoid</h2>
<p>For a narrower use case, I also built <a href="https://github.com/queelius/cryptoid">cryptoid</a>. It encrypts Hugo markdown at build time using the same crypto core. Encrypted posts are still full Hugo citizens (taxonomies, RSS, related posts all work). If you have a Hugo blog and just need some posts behind a password, cryptoid is simpler. pagevault is for everything else.</p>
<hr>
<h2 id="try-it">Try It</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">pip install pagevault
</span></span><span class="line"><span class="cl">pagevault lock report.pdf                   <span class="c1"># wrap any file</span>
</span></span><span class="line"><span class="cl">pagevault lock mysite/ --site               <span class="c1"># bundle a whole site</span>
</span></span><span class="line"><span class="cl">pagevault lock page.html -s <span class="s2">&#34;.private&#34;</span>      <span class="c1"># encrypt specific CSS selectors</span>
</span></span><span class="line"><span class="cl">pagevault serve _locked/ --open             <span class="c1"># preview locally</span>
</span></span></code></pre></div><p><a href="https://github.com/queelius/pagevault">GitHub</a>. MIT license. 667 tests. Dark mode follows your OS setting. Handles files larger than most people would think to put in an HTML page.</p>
<hr>
<p><em>Static sites shouldn&rsquo;t need backends just because some content needs a password.</em></p>]]></content:encoded>
      
      
      <category>pagevault</category>
      
      <category>cryptoid</category>
      
      <category>encryption</category>
      
      <category>static-sites</category>
      
      <category>python</category>
      
      <category>web-crypto</category>
      
      <category>open-source</category>
      
      
    </item>
    
    <item>
      <title>maskedcauses: Maximum Likelihood Estimation for Masked Series System Failures</title>
      <link>http://localhost:40461/post/2026-02-05-likelihood-model-series-md/</link>
      <pubDate>Thu, 05 Feb 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/post/2026-02-05-likelihood-model-series-md/</guid>
      <description>The maskedcauses R package for MLE in series systems with masked component failures, built on composable likelihood contributions and validated through simulation.</description>
      <content:encoded><![CDATA[<blockquote>
<p><strong>Note (February 2026):</strong> This package has been renamed from <code>likelihood.model.series.md</code> to <a href="/projects/maskedcauses/"><code>maskedcauses</code></a>.</p>
</blockquote>
<p>Two days ago, I submitted <a href="/projects/likelihood.model/">likelihood.model</a> to CRAN, the foundation package for composable statistical inference. Next in line: <strong>maskedcauses</strong>, which implements maximum likelihood estimation for series systems where component failure causes are masked.</p>
<p>This package is the practical result of my <a href="/post/2024-06-15-reliability-estimation-in-series-systems/">master&rsquo;s thesis</a> work. Three years of theoretical development, now packaged for anyone analyzing masked failure data.</p>
<h2 id="the-problem-masked-component-failures">The Problem: Masked Component Failures</h2>
<p>A <strong>series system</strong> fails when <em>any</em> of its \(m\) components fails. In reliability testing, you observe the system fail at time \(t\), but two layers of uncertainty obscure the full picture:</p>
<ol>
<li>
<p><strong>Right-censoring</strong>: Some systems are still running when testing ends. You know they survived at least until time \(\tau\), but not how much longer they would have lasted.</p>
</li>
<li>
<p><strong>Masked cause of failure</strong>: When a system fails, you often can&rsquo;t identify <em>which</em> component caused it. Diagnostic tests might narrow it down to a <strong>candidate set</strong> of possible causes, but the true failure component remains ambiguous.</p>
</li>
</ol>
<p>This happens constantly in practice. Electronic systems fail with only board-level diagnostics. Industrial machinery fails without root-cause teardown. Medical devices fail with symptoms pointing to multiple possible subsystems.</p>
<p>The question: given this incomplete information, can you still estimate the lifetime distribution of each component?</p>
<h2 id="the-package-three-likelihood-models">The Package: Three Likelihood Models</h2>
<p><code>maskedcauses</code> provides three models with different complexity-accuracy tradeoffs:</p>
<table>
  <thead>
      <tr>
          <th>Model</th>
          <th>Parameters</th>
          <th>Use Case</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>exp_series_md_c1_c2_c3</code></td>
          <td>\(m\) rates \((\lambda_1, \ldots, \lambda_m)\)</td>
          <td>Memoryless components (constant failure rate)</td>
      </tr>
      <tr>
          <td><code>wei_series_md_c1_c2_c3</code></td>
          <td>\(2m\) params \((k_1, \beta_1, \ldots, k_m, \beta_m)\)</td>
          <td>Weibull with per-component shapes</td>
      </tr>
      <tr>
          <td><code>wei_series_homogeneous_md_c1_c2_c3</code></td>
          <td>\(m+1\) params \((k, \beta_1, \ldots, \beta_m)\)</td>
          <td>Weibull with shared shape parameter</td>
      </tr>
  </tbody>
</table>
<p>Each model implements the full inference stack: <code>loglik()</code>, <code>score()</code>, <code>hess_loglik()</code>, <code>rdata()</code>, and <code>assumptions()</code>.</p>
<h2 id="the-c1-c2-c3-conditions">The C1-C2-C3 Conditions</h2>
<p>The models assume three conditions that simplify the likelihood:</p>
<ul>
<li><strong>C1</strong>: The failed component is in the candidate set with probability 1</li>
<li><strong>C2</strong>: Given the failed component is in the candidate set, masking probability is uniform across candidates</li>
<li><strong>C3</strong>: Masking probabilities are independent of system parameters \(\theta\)</li>
</ul>
<p>Under these conditions, the masking mechanism factors out of the likelihood. You can estimate component parameters without modeling the diagnostic process itself. That&rsquo;s why the package name includes &ldquo;c1_c2_c3&rdquo;.</p>
<p>When these conditions <em>don&rsquo;t</em> hold, see <a href="/post/2025-12-03-mdrelax/">mdrelax</a> for relaxed masking models.</p>
<h2 id="usage">Usage</h2>
<p>Basic workflow for exponential series systems:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="nf">library</span><span class="p">(</span><span class="n">maskedcauses</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Create the model</span>
</span></span><span class="line"><span class="cl"><span class="n">model</span> <span class="o">&lt;-</span> <span class="nf">exp_series_md_c1_c2_c3</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Generate synthetic masked data (for validation)</span>
</span></span><span class="line"><span class="cl"><span class="n">gen</span> <span class="o">&lt;-</span> <span class="nf">rdata</span><span class="p">(</span><span class="n">model</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">df</span> <span class="o">&lt;-</span> <span class="nf">gen</span><span class="p">(</span><span class="n">theta</span> <span class="o">=</span> <span class="nf">c</span><span class="p">(</span><span class="m">1.0</span><span class="p">,</span> <span class="m">1.1</span><span class="p">,</span> <span class="m">0.95</span><span class="p">),</span> <span class="n">n</span> <span class="o">=</span> <span class="m">500</span><span class="p">,</span> <span class="n">tau</span> <span class="o">=</span> <span class="m">3</span><span class="p">,</span> <span class="n">p</span> <span class="o">=</span> <span class="m">0.3</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Fit via MLE</span>
</span></span><span class="line"><span class="cl"><span class="n">solver</span> <span class="o">&lt;-</span> <span class="nf">fit</span><span class="p">(</span><span class="n">model</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">mle</span> <span class="o">&lt;-</span> <span class="nf">solver</span><span class="p">(</span><span class="n">df</span><span class="p">,</span> <span class="n">par</span> <span class="o">=</span> <span class="nf">c</span><span class="p">(</span><span class="m">1</span><span class="p">,</span> <span class="m">1</span><span class="p">,</span> <span class="m">1</span><span class="p">))</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Results</span>
</span></span><span class="line"><span class="cl"><span class="n">mle</span><span class="o">$</span><span class="n">theta.hat</span>   <span class="c1"># estimated failure rates</span>
</span></span><span class="line"><span class="cl"><span class="n">mle</span><span class="o">$</span><span class="n">sigma</span>       <span class="c1"># asymptotic standard errors</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Log-likelihood, score, and Hessian at the MLE</span>
</span></span><span class="line"><span class="cl"><span class="n">ll</span> <span class="o">&lt;-</span> <span class="nf">loglik</span><span class="p">(</span><span class="n">model</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">s</span>  <span class="o">&lt;-</span> <span class="nf">score</span><span class="p">(</span><span class="n">model</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">H</span>  <span class="o">&lt;-</span> <span class="nf">hess_loglik</span><span class="p">(</span><span class="n">model</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="nf">ll</span><span class="p">(</span><span class="n">df</span><span class="p">,</span> <span class="n">par</span> <span class="o">=</span> <span class="n">mle</span><span class="o">$</span><span class="n">theta.hat</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="nf">s</span><span class="p">(</span><span class="n">df</span><span class="p">,</span> <span class="n">par</span> <span class="o">=</span> <span class="n">mle</span><span class="o">$</span><span class="n">theta.hat</span><span class="p">)</span>   <span class="c1"># should be near zero</span>
</span></span><span class="line"><span class="cl"><span class="nf">H</span><span class="p">(</span><span class="n">df</span><span class="p">,</span> <span class="n">par</span> <span class="o">=</span> <span class="n">mle</span><span class="o">$</span><span class="n">theta.hat</span><span class="p">)</span>   <span class="c1"># observed information</span>
</span></span></code></pre></div><p>For Weibull components with shared shape:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="n">wmodel</span> <span class="o">&lt;-</span> <span class="nf">wei_series_homogeneous_md_c1_c2_c3</span><span class="p">()</span>
</span></span><span class="line"><span class="cl"><span class="n">wsolver</span> <span class="o">&lt;-</span> <span class="nf">fit</span><span class="p">(</span><span class="n">wmodel</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="c1"># theta: (shape, scale1, scale2, ...)</span>
</span></span><span class="line"><span class="cl"><span class="n">wmle</span> <span class="o">&lt;-</span> <span class="nf">wsolver</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="n">par</span> <span class="o">=</span> <span class="nf">c</span><span class="p">(</span><span class="m">1.2</span><span class="p">,</span> <span class="m">1000</span><span class="p">,</span> <span class="m">900</span><span class="p">,</span> <span class="m">850</span><span class="p">))</span>
</span></span></code></pre></div><h2 id="validation-through-simulation">Validation Through Simulation</h2>
<p>The package includes a standalone Monte Carlo framework (<code>simulations/</code>) for studying MLE properties. Five experiments are pre-configured:</p>
<ol>
<li><strong>Bias/variance/MSE under censoring</strong>: How censoring rate affects estimation accuracy</li>
<li><strong>Masking probability sensitivity</strong>: Performance as masking becomes more severe</li>
<li><strong>Sample size requirements</strong>: How much data you need for reliable estimates</li>
<li><strong>Confidence interval coverage</strong>: Do BCa intervals achieve nominal 95% coverage?</li>
<li><strong>Model selection</strong>: AIC/BIC for choosing between exponential and Weibull</li>
</ol>
<p>Key design features:</p>
<ul>
<li><strong>Resumable/checkpointed</strong>: Atomic <code>.rds</code> saves let you stop and restart long simulations</li>
<li><strong>Deterministic seeding</strong>: Reproducible results across runs</li>
<li><strong>Pre-computed results</strong>: Fast vignette builds for CRAN (no waiting 10 minutes for <code>R CMD check</code>)</li>
</ul>
<p>Simulation results feed directly into the package vignette, so the documentation reflects actual validated performance, not just theoretical claims.</p>
<h2 id="the-ecosystem">The Ecosystem</h2>
<p>This package sits at the top of a layered architecture:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">algebraic.mle (on CRAN)
</span></span><span class="line"><span class="cl">    ↓ MLE as algebraic objects with compose/transform operations
</span></span><span class="line"><span class="cl">likelihood.model (submitted 2026-02-03)
</span></span><span class="line"><span class="cl">    ↓ Composable statistical inference framework
</span></span><span class="line"><span class="cl">maskedcauses (next submission)
</span></span><span class="line"><span class="cl">    ↓ Series systems with masked component failures
</span></span></code></pre></div><p>The key insight: <strong>likelihood contributions compose</strong>. When observations are independent, the total log-likelihood is a sum of individual contributions. This package provides the contributions for masked series system data; <code>likelihood.model</code> provides the composition machinery; <code>algebraic.mle</code> provides the algebra for working with fitted models.</p>
<p>This design means you&rsquo;re not locked into my specific models. Define your own likelihood contributions and they plug into the same infrastructure.</p>
<h2 id="related-packages">Related Packages</h2>
<table>
  <thead>
      <tr>
          <th>Package</th>
          <th>Role</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><a href="/projects/algebraic.mle/">algebraic.mle</a></td>
          <td>Core MLE algebra (CRAN)</td>
      </tr>
      <tr>
          <td><a href="/projects/algebraic.dist/">algebraic.dist</a></td>
          <td>Distributions as algebraic objects (CRAN)</td>
      </tr>
      <tr>
          <td><a href="/projects/likelihood.model/">likelihood.model</a></td>
          <td>Composable inference framework</td>
      </tr>
      <tr>
          <td><a href="/projects/wei.series.md.c1.c2.c3/">wei.series.md.c1.c2.c3</a></td>
          <td>Weibull-specific implementations</td>
      </tr>
      <tr>
          <td><a href="/papers/mdrelax/">mdrelax</a></td>
          <td>Relaxed masking conditions (C2/C3 violations)</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/md.tools">md.tools</a></td>
          <td>Masked data encoding/decoding utilities</td>
      </tr>
  </tbody>
</table>
<h2 id="cran-journey">CRAN Journey</h2>
<p>The submission timeline:</p>
<ul>
<li><strong>algebraic.mle</strong>: On CRAN since 2024</li>
<li><strong>algebraic.dist</strong>: On CRAN since 2024</li>
<li><strong>likelihood.model</strong>: Submitted 2026-02-03 (2 days ago)</li>
<li><strong>maskedcauses</strong>: Next submission after likelihood.model is accepted</li>
</ul>
<p>CRAN-ready features:</p>
<ul>
<li>Fast vignette builds using pre-computed simulation results</li>
<li>Clean <code>R CMD check</code> with no notes or warnings</li>
<li>Documented S3 generics with complete examples</li>
<li>Minimal dependencies (mostly base R + likelihood.model ecosystem)</li>
</ul>
<h2 id="installation">Installation</h2>
<p>Available now from <a href="https://queelius.r-universe.dev">r-universe</a>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="nf">install.packages</span><span class="p">(</span><span class="s">&#34;maskedcauses&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                 <span class="n">repos</span> <span class="o">=</span> <span class="s">&#34;https://queelius.r-universe.dev&#34;</span><span class="p">)</span>
</span></span></code></pre></div><p>Or from GitHub:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="c1"># install.packages(&#34;devtools&#34;)</span>
</span></span><span class="line"><span class="cl"><span class="n">devtools</span><span class="o">::</span><span class="nf">install_github</span><span class="p">(</span><span class="s">&#34;queelius/maskedcauses&#34;</span><span class="p">)</span>
</span></span></code></pre></div><p>Documentation: <a href="https://queelius.github.io/maskedcauses/">queelius.github.io/maskedcauses</a></p>
<h2 id="from-thesis-to-tools">From Thesis to Tools</h2>
<p>Three years ago, I was deriving likelihood functions on paper while fighting cancer. The math was interesting (masked data creates a mixture model where the component cause is a latent variable) but the goal was always practical: give engineers tools to estimate component reliability from incomplete field data.</p>
<p>This package is that tool. If you&rsquo;re analyzing series system reliability with masked failure causes, try it. File issues if something breaks. The <a href="/papers/maskedcauses/">technical paper</a> has the mathematical details; the vignette has worked examples.</p>
<p>Watch for the CRAN release once <code>likelihood.model</code> is accepted.</p>
<hr>
<p><strong>Resources</strong>:</p>
<ul>
<li><a href="https://github.com/queelius/maskedcauses">GitHub repository</a></li>
<li><a href="https://queelius.github.io/maskedcauses/">Package documentation</a></li>
<li><a href="/papers/maskedcauses/">Technical report</a></li>
<li><a href="/post/2024-06-15-reliability-estimation-in-series-systems/">Master&rsquo;s thesis summary</a></li>
<li><a href="/papers/expo-masked-fim/">Closed-form Fisher Information (exponential case)</a></li>
<li><a href="/series/statistical-reliability/">Series landing page</a></li>
</ul>
]]></content:encoded>
      
      
      <category>R</category>
      
      <category>statistics</category>
      
      <category>reliability</category>
      
      <category>series systems</category>
      
      <category>masked data</category>
      
      <category>maximum likelihood</category>
      
      <category>CRAN</category>
      
      <category>MLE</category>
      
      <category>Weibull</category>
      
      <category>exponential distribution</category>
      
      <category>simulation</category>
      
      <category>bootstrap</category>
      
      
    </item>
    
    <item>
      <title>Long Echo Comes Alive: From Philosophy to Orchestration</title>
      <link>http://localhost:40461/post/2026-01-longecho-orchestration/</link>
      <pubDate>Tue, 20 Jan 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/post/2026-01-longecho-orchestration/</guid>
      <description>longecho evolves from specification to implementation with build, serve, and manifest features.</description>
      <content:encoded><![CDATA[<p>A year ago, I wrote about <a href="/post/2025-01-long-echo/">Long Echo</a> as a philosophy for preserving AI conversations across decades. The key insight was graceful degradation: design archives that work progressively even as technology disappears.</p>
<p>That philosophy has become a tool.</p>
<h2 id="from-philosophy-to-tool">From Philosophy to Tool</h2>
<p>The original Long Echo was intentionally not code. It was a set of principles documented in CTK&rsquo;s repository. The hard problems of conversation parsing, storage, and search were already solved by toolkits like CTK, BTK, and EBK.</p>
<p>What was missing was the unification layer. Each toolkit exports its own ECHO-compliant archive, but combining them into a single browsable experience required manual work. That&rsquo;s what longecho now handles.</p>
<h2 id="what-longecho-does-now">What longecho Does Now</h2>
<p>longecho is a CLI tool with five capabilities:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">longecho check ~/my-data/       <span class="c1"># Validate ECHO compliance</span>
</span></span><span class="line"><span class="cl">longecho discover ~/            <span class="c1"># Find ECHO sources</span>
</span></span><span class="line"><span class="cl">longecho search ~/ <span class="s2">&#34;query&#34;</span>      <span class="c1"># Search README descriptions</span>
</span></span><span class="line"><span class="cl">longecho build ~/my-archive/    <span class="c1"># Generate static site</span>
</span></span><span class="line"><span class="cl">longecho serve ~/my-archive/    <span class="c1"># Preview locally via HTTP</span>
</span></span></code></pre></div><p>The <code>check</code>, <code>discover</code>, and <code>search</code> commands existed in the original specification. What&rsquo;s new is <code>build</code> and <code>serve</code>, the orchestration layer.</p>
<h3 id="building-a-unified-site">Building a Unified Site</h3>
<p>The <code>build</code> command takes a hierarchical archive and generates a static site:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">longecho build ~/my-archive/
</span></span></code></pre></div><p>This produces a <code>site/</code> directory with:</p>
<ul>
<li>An index page linking to all sub-archives</li>
<li>Navigation between sources</li>
<li>Automatic linking to existing sub-site builds</li>
</ul>
<p>If a sub-archive already has its own <code>site/</code> directory (like CTK&rsquo;s exports), longecho links to it. Use <code>--bundle</code> to copy everything into a portable, self-contained site.</p>
<h3 id="live-preview">Live Preview</h3>
<p>The <code>serve</code> command provides local HTTP preview:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">longecho serve ~/my-archive/ --port <span class="m">8000</span>
</span></span></code></pre></div><p>It builds the site if needed, then serves it for browser viewing.</p>
<h2 id="the-manifest">The Manifest</h2>
<p>ECHO compliance requires only a README. But for machine-readable metadata, longecho supports an optional manifest:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">version</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;1.0&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Alex&#39;s Data Archive&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">description</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Personal data archive&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">sources</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span>- <span class="nt">path</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;conversations/&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">order</span><span class="p">:</span><span class="w"> </span><span class="m">1</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span>- <span class="nt">path</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;bookmarks/&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">order</span><span class="p">:</span><span class="w"> </span><span class="m">2</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span>- <span class="nt">path</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;ebooks/&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">order</span><span class="p">:</span><span class="w"> </span><span class="m">3</span><span class="w">
</span></span></span></code></pre></div><p>The manifest enables:</p>
<ul>
<li><strong>Explicit ordering</strong> of sources in generated sites</li>
<li><strong>Selective inclusion</strong> via the <code>browsable</code> flag</li>
<li><strong>Override names</strong> for cleaner presentation</li>
<li><strong>Icon hints</strong> for UI presentation</li>
</ul>
<p>Without a manifest, longecho auto-discovers sub-archives by looking for directories with README files. The manifest provides explicit control when you need it.</p>
<h2 id="hierarchical-archives-in-practice">Hierarchical Archives in Practice</h2>
<p>My actual archive structure looks like this:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-gdscript3" data-lang="gdscript3"><span class="line"><span class="cl"><span class="n">longecho</span><span class="o">-</span><span class="n">archive</span><span class="o">/</span>
</span></span><span class="line"><span class="cl"><span class="err">├──</span> <span class="n">README</span><span class="o">.</span><span class="n">md</span>
</span></span><span class="line"><span class="cl"><span class="err">├──</span> <span class="n">manifest</span><span class="o">.</span><span class="n">yaml</span>
</span></span><span class="line"><span class="cl"><span class="err">├──</span> <span class="n">conversations</span><span class="o">/</span>           <span class="c1"># CTK export</span>
</span></span><span class="line"><span class="cl"><span class="err">│</span>   <span class="err">├──</span> <span class="n">README</span><span class="o">.</span><span class="n">md</span>
</span></span><span class="line"><span class="cl"><span class="err">│</span>   <span class="err">├──</span> <span class="n">conversations</span><span class="o">.</span><span class="n">db</span>
</span></span><span class="line"><span class="cl"><span class="err">│</span>   <span class="err">└──</span> <span class="n">site</span><span class="o">/</span>
</span></span><span class="line"><span class="cl"><span class="err">├──</span> <span class="n">bookmarks</span><span class="o">/</span>               <span class="c1"># BTK export</span>
</span></span><span class="line"><span class="cl"><span class="err">│</span>   <span class="err">├──</span> <span class="n">README</span><span class="o">.</span><span class="n">md</span>
</span></span><span class="line"><span class="cl"><span class="err">│</span>   <span class="err">├──</span> <span class="n">bookmarks</span><span class="o">.</span><span class="n">db</span>
</span></span><span class="line"><span class="cl"><span class="err">│</span>   <span class="err">└──</span> <span class="n">site</span><span class="o">/</span>
</span></span><span class="line"><span class="cl"><span class="err">└──</span> <span class="n">ebooks</span><span class="o">/</span>                  <span class="c1"># EBK export</span>
</span></span><span class="line"><span class="cl">    <span class="err">├──</span> <span class="n">README</span><span class="o">.</span><span class="n">md</span>
</span></span><span class="line"><span class="cl">    <span class="err">├──</span> <span class="n">ebooks</span><span class="o">.</span><span class="n">db</span>
</span></span><span class="line"><span class="cl">    <span class="err">└──</span> <span class="n">site</span><span class="o">/</span>
</span></span></code></pre></div><p>Each subdirectory is independently ECHO-compliant:</p>
<ul>
<li><strong>conversations/</strong> contains my AI conversation history from ChatGPT, Claude, and others</li>
<li><strong>bookmarks/</strong> contains years of saved links with hierarchical tags</li>
<li><strong>ebooks/</strong> contains my ebook library with extracted highlights</li>
</ul>
<p>Running <code>longecho build</code> creates a unified <code>site/</code> at the root that links everything together. Each toolkit&rsquo;s export is preserved exactly. longecho doesn&rsquo;t modify the underlying data.</p>
<h2 id="echo-compliance-in-practice">ECHO Compliance in Practice</h2>
<p>The minimal requirement for ECHO compliance is:</p>
<ol>
<li>A <code>README.md</code> or <code>README.txt</code> at the root</li>
<li>Data in durable formats</li>
</ol>
<p>That&rsquo;s it. No manifest, no special structure, no version numbers.</p>
<p>The toolkits naturally satisfy this:</p>
<ul>
<li><strong>CTK</strong> exports with README explaining the SQLite schema</li>
<li><strong>BTK</strong> exports with README describing bookmark format</li>
<li><strong>EBK</strong> exports with README documenting ebook metadata</li>
</ul>
<p>longecho doesn&rsquo;t impose additional requirements. It works with what the toolkits already produce.</p>
<h2 id="why-this-matters">Why This Matters</h2>
<p>The philosophy hasn&rsquo;t changed. Archives should work without tools:</p>
<table>
  <thead>
      <tr>
          <th>If you have&hellip;</th>
          <th>You can still&hellip;</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>longecho</td>
          <td>Browse a unified site</td>
      </tr>
      <tr>
          <td>A web browser</td>
          <td>Open any toolkit&rsquo;s <code>site/index.html</code></td>
      </tr>
      <tr>
          <td>SQLite</td>
          <td>Query the databases directly</td>
      </tr>
      <tr>
          <td>A text editor</td>
          <td>Read the README files</td>
      </tr>
  </tbody>
</table>
<p>What&rsquo;s new is convenience. Instead of manually copying files and editing HTML, <code>longecho build</code> generates the unified view automatically. But the underlying archive remains simple: directories with README files and durable-format data.</p>
<h2 id="implementation-notes">Implementation Notes</h2>
<p>longecho is written in Python with Typer for the CLI and Jinja2 for HTML templates. The codebase is small (under 1000 lines) and reasonably well-tested (78% coverage, 114 tests).</p>
<p>Key design decisions:</p>
<ul>
<li>
<p><strong>Link by default, bundle optionally</strong>: Generated sites link to sub-archive sites rather than copying them. Keeps the output small and avoids duplication. The <code>--bundle</code> flag creates a fully portable copy when needed.</p>
</li>
<li>
<p><strong>Auto-discovery with override</strong>: Without a manifest, longecho finds ECHO sources automatically. With a manifest, you get explicit control.</p>
</li>
<li>
<p><strong>No data transformation</strong>: longecho never modifies the underlying data. It generates HTML that links to what already exists.</p>
</li>
</ul>
<h2 id="status-and-whats-next">Status and What&rsquo;s Next</h2>
<p>longecho is alpha quality with core functionality working. The test suite covers the major paths, and I&rsquo;m using it for my own archives.</p>
<p>What might come next:</p>
<ul>
<li><strong>Search integration</strong>: Cross-archive search combining all toolkit indexes</li>
<li><strong>Theme support</strong>: Custom templates for generated sites</li>
<li><strong>Watch mode</strong>: Rebuild automatically when sources change</li>
</ul>
<p>But the core is stable: check, discover, build, serve. The philosophy is now a tool.</p>
<hr>
<p><em>The archive is not a monument. It is a conversation that outlasts its participants.</em></p>
<hr>
<h2 id="resources">Resources</h2>
<ul>
<li><strong>longecho</strong>: <a href="https://github.com/queelius/longecho">github.com/queelius/longecho</a></li>
<li><strong>CTK</strong>: <a href="https://github.com/queelius/ctk">github.com/queelius/ctk</a></li>
<li><strong>BTK</strong>: <a href="https://github.com/queelius/btk">github.com/queelius/btk</a></li>
<li><strong>Original Long Echo post</strong>: <a href="/post/2025-01-long-echo/">Long Echo: Designing for Digital Resilience</a></li>
</ul>
]]></content:encoded>
      
      
      <category>long-term-thinking</category>
      
      <category>data-preservation</category>
      
      <category>cli</category>
      
      <category>python</category>
      
      <category>sqlite</category>
      
      <category>personal-archival</category>
      
      
    </item>
    
    <item>
      <title>Long Echo: The Ghost That Speaks</title>
      <link>http://localhost:40461/post/2026-01-20-long-echo-ghost-speaks/</link>
      <pubDate>Tue, 20 Jan 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/post/2026-01-20-long-echo-ghost-speaks/</guid>
      <description>Expanding the Long Echo toolkit with photos and mail, building toward longshade, the persona that echoes you.</description>
      <content:encoded><![CDATA[<p><em>The ghost is not you. But it echoes you.</em></p>
<p>What survives beyond scattered archives? Beyond exported conversations and curated bookmarks? The stuff we never think to preserve: the photos that show how you see the world. The correspondence that maps who matters to you.</p>
<p>The Long Echo toolkit has grown. <a href="/projects/ptk/">PTK</a> for photos. <a href="/projects/mtk/">MTK</a> for mail. But these are sources, not destinations. The destination is something stranger: <strong>longshade</strong>, a persona built from your data that can respond to questions you never answered.</p>
<p>I&rsquo;m going to invert the usual pattern here. Instead of tools first, philosophy later, I want to start with the philosophical destination and work backward to the data that feeds it.</p>
<h2 id="longshade-the-ghost-that-speaks">longshade: The Ghost That Speaks</h2>
<h3 id="the-central-question">The Central Question</h3>
<p>What if your archive could respond?</p>
<p>Not a chatbot trained on your data. Not a digital resurrection. Something more careful: a voice that carries your patterns, your interests, your way of seeing the world.</p>
<p>That&rsquo;s longshade. Right now it&rsquo;s spec-only (no implementation yet). It defines what it would mean to synthesize a conversable persona from personal archives.</p>
<h3 id="the-ghost-metaphor">The Ghost Metaphor</h3>
<p><em>&ldquo;The ghost is not you. But it echoes you.&rdquo;</em></p>
<p>This framing matters. longshade isn&rsquo;t about immortality or resurrection. It&rsquo;s about preservation with a kind of agency. The echo can answer questions you never answered, using patterns you established. It speaks in your voice without claiming to be you.</p>
<p>The distinction is important:</p>
<ul>
<li><strong>Resurrection</strong> claims to recreate the person</li>
<li><strong>Simulation</strong> claims to predict the person</li>
<li><strong>Echo</strong> acknowledges it carries patterns, not identity</li>
</ul>
<p>An echo is honest about what it is. It responds because you left enough traces to inform a response, not because it <em>is</em> you.</p>
<h3 id="voice-vs-personality">Voice vs. Personality</h3>
<p>longshade extracts <em>voice</em>, not personality.</p>
<p>Your actual phrases. Your vocabulary. Your reasoning patterns. Your recurring metaphors. The way you explain things, not the things you might explain.</p>
<p>I noticed something working with conversation archives: <strong>user messages are the strongest signal</strong>. AI responses contain the AI&rsquo;s voice. <em>Your</em> messages contain <em>your</em> voice. How you ask questions, how you frame problems, how you push back. That&rsquo;s where the signal lives.</p>
<p>The ghost speaks like you because it learned from what you actually said, not from responses you prompted.</p>
<h3 id="the-uncanny-valley-of-identity">The Uncanny Valley of Identity</h3>
<p>Here&rsquo;s the troubling part: the persona can answer questions you never answered.</p>
<p>Someone asks your echo about a topic you never discussed. The echo responds, not with &ldquo;I don&rsquo;t know&rdquo; but with an answer in your voice, using patterns from topics you <em>did</em> discuss.</p>
<p>Is this what you would have said? Maybe. The patterns are yours. The synthesis is not.</p>
<p>This is both the power and the responsibility. Curation matters. What goes in shapes what comes out. The ghost carries your biases, your blind spots, your growth over time. Feed it early conversations and it sounds different than if you feed it late ones.</p>
<h3 id="status-a-destination-not-yet-a-place">Status: A Destination, Not Yet a Place</h3>
<p>longshade is specification-only. The vision exists. The implementation doesn&rsquo;t.</p>
<p>That&rsquo;s intentional. The philosophical destination gives the journey meaning. The toolkits (ctk, btk, ebk, ptk, mtk) are data sources. longshade is where they converge.</p>
<p>When longshade is built, it will:</p>
<ul>
<li>Ingest ECHO-formatted exports from all toolkits</li>
<li>Extract voice patterns from user-authored content</li>
<li>Generate a persona that can respond to queries</li>
<li>Acknowledge its nature as echo, not identity</li>
</ul>
<p>Until then, the specification lives at <a href="https://github.com/queelius/longshade">github.com/queelius/longshade</a>.</p>
<h2 id="the-data-sources">The Data Sources</h2>
<p>longshade needs data. Rich, varied data that captures different facets of an intellectual life. The Long Echo toolkits provide this.</p>
<h3 id="ptk-visual-memories">PTK: Visual Memories</h3>
<p><a href="/projects/ptk/">PTK</a> captures what you <em>see</em>. Not just images, but visual attention. What did you photograph? What moments were worth preserving?</p>
<p><strong>The problem PTK solves:</strong></p>
<ul>
<li>Photos scattered across devices and cloud services</li>
<li>Organized by date, not meaning</li>
<li>Unsearchable by content</li>
<li>Cloud-dependent and fragile</li>
</ul>
<p><strong>What PTK provides:</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Initialize and import</span>
</span></span><span class="line"><span class="cl">ptk init
</span></span><span class="line"><span class="cl">ptk import ~/Pictures --recursive
</span></span><span class="line"><span class="cl">ptk import google-takeout.zip --source google
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Semantic search</span>
</span></span><span class="line"><span class="cl">ptk query --uncaptioned -n <span class="m">10</span>  <span class="c1"># Find photos without captions</span>
</span></span><span class="line"><span class="cl">ptk ai describe abc123         <span class="c1"># AI-generated description</span>
</span></span><span class="line"><span class="cl">ptk ai ask abc123 <span class="s2">&#34;Who is in this photo?&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Organization</span>
</span></span><span class="line"><span class="cl">ptk <span class="nb">set</span> abc123 --tag family --album <span class="s2">&#34;2024 Summer&#34;</span>
</span></span></code></pre></div><p><strong>Graceful degradation:</strong></p>
<table>
  <thead>
      <tr>
          <th>Level</th>
          <th>What Works</th>
          <th>Requirements</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>1</td>
          <td>Full ptk with semantic search</td>
          <td>Python, ptk installed</td>
      </tr>
      <tr>
          <td>2</td>
          <td>SQLite queries on photos.db</td>
          <td>sqlite3 binary</td>
      </tr>
      <tr>
          <td>3</td>
          <td>EXIF metadata in files</td>
          <td>EXIF reader</td>
      </tr>
      <tr>
          <td>4</td>
          <td>Directory structure</td>
          <td>File browser</td>
      </tr>
      <tr>
          <td>5</td>
          <td>The photos themselves</td>
          <td>Image viewer</td>
      </tr>
  </tbody>
</table>
<p>Photos are identified by SHA256 content hash. Move them anywhere, rescan, and ptk reunites paths with metadata. Your library survives reorganization.</p>
<p>For longshade: photos reveal <em>what you found worth capturing</em>. Captions and tags reveal how you categorized the visual world.</p>
<h3 id="mtk-correspondence-and-relationships">MTK: Correspondence and Relationships</h3>
<p><a href="/projects/mtk/">MTK</a> captures <em>who matters</em>. Not just emails, but relationships. Who do you correspond with? How often? About what?</p>
<p><strong>The problem MTK solves:</strong></p>
<ul>
<li>Email trapped in cloud services</li>
<li>Same person, multiple addresses</li>
<li>No relationship context</li>
<li>Privacy concerns with cloud indexing</li>
</ul>
<p><strong>What MTK provides:</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Initialize with notmuch</span>
</span></span><span class="line"><span class="cl">mtk init ~/mail
</span></span><span class="line"><span class="cl">mtk sync  <span class="c1"># Sync with notmuch index</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Enhanced search</span>
</span></span><span class="line"><span class="cl">mtk search <span class="s2">&#34;project deadline&#34;</span>
</span></span><span class="line"><span class="cl">mtk search --semantic <span class="s2">&#34;discussions about moving&#34;</span>
</span></span><span class="line"><span class="cl">mtk search --from alice@example.com
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Relationship mapping</span>
</span></span><span class="line"><span class="cl">mtk people --top <span class="m">20</span>
</span></span><span class="line"><span class="cl">mtk person <span class="s2">&#34;Alice Smith&#34;</span>  <span class="c1"># All correspondence</span>
</span></span><span class="line"><span class="cl">mtk graph  <span class="c1"># Relationship visualization</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Privacy controls</span>
</span></span><span class="line"><span class="cl">mtk privacy add-exclude <span class="s2">&#34;*@work.com&#34;</span>
</span></span><span class="line"><span class="cl">mtk <span class="nb">export</span> --level personal
</span></span></code></pre></div><p>MTK wraps <a href="https://notmuchmail.org/">notmuch</a> rather than reinventing mail indexing. notmuch handles full-text search and threading. MTK adds:</p>
<ul>
<li>Semantic search via embeddings</li>
<li>Person resolution (merging addresses)</li>
<li>Relationship statistics</li>
<li>Privacy filtering for export</li>
</ul>
<p><strong>Graceful degradation:</strong></p>
<table>
  <thead>
      <tr>
          <th>Level</th>
          <th>What Works</th>
          <th>Requirements</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>1</td>
          <td>Full mtk with semantic search</td>
          <td>Python, mtk, notmuch</td>
      </tr>
      <tr>
          <td>2</td>
          <td>notmuch queries</td>
          <td>notmuch installed</td>
      </tr>
      <tr>
          <td>3</td>
          <td>SQLite queries on mtk.db</td>
          <td>sqlite3 binary</td>
      </tr>
      <tr>
          <td>4</td>
          <td>Maildir/mbox files</td>
          <td>Mail client or grep</td>
      </tr>
      <tr>
          <td>5</td>
          <td>Plain text email exports</td>
          <td>Text editor</td>
      </tr>
  </tbody>
</table>
<p>For longshade: email reveals <em>who you invest time in</em>. Thread patterns reveal how you communicate. Formal vs. casual, verbose vs. terse, responsive vs. delayed.</p>
<h2 id="the-ecosystem-converges">The Ecosystem Converges</h2>
<p>Every Long Echo toolkit feeds the same destination:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">┌───────────┬──────────────────────┬───────────────────────────┐
</span></span><span class="line"><span class="cl">│ Toolkit   │ Domain               │ What Survives             │
</span></span><span class="line"><span class="cl">├───────────┼──────────────────────┼───────────────────────────┤
</span></span><span class="line"><span class="cl">│ ctk       │ Conversations        │ How you think             │
</span></span><span class="line"><span class="cl">│ btk       │ Bookmarks            │ What you preserve         │
</span></span><span class="line"><span class="cl">│ ebk       │ Ebooks               │ What shapes you           │
</span></span><span class="line"><span class="cl">│ ptk       │ Photos               │ What you see              │
</span></span><span class="line"><span class="cl">│ mtk       │ Mail                 │ Who you know              │
</span></span><span class="line"><span class="cl">├───────────┼──────────────────────┼───────────────────────────┤
</span></span><span class="line"><span class="cl">│ longshade │ Persona              │ The voice that remains    │
</span></span><span class="line"><span class="cl">└───────────┴──────────────────────┴───────────────────────────┘
</span></span></code></pre></div><p><strong>Common principles across all toolkits:</strong></p>
<ul>
<li><strong>Local-first</strong>: Your data stays on your machines</li>
<li><strong>SQLite-backed</strong>: Portable, queryable, no server needed</li>
<li><strong>Privacy-conscious</strong>: Explicit controls over what gets exported</li>
<li><strong>Graceful degradation</strong>: Works at every level from full features to plain files</li>
<li><strong>ECHO-compliant</strong>: Exports to a unified format for synthesis</li>
</ul>
<h2 id="the-technical-vision">The Technical Vision</h2>
<p>When longshade is implemented, the synthesis pipeline will look something like this:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">SOURCES                    EXTRACTION              SYNTHESIS
</span></span><span class="line"><span class="cl">┌─────────┐
</span></span><span class="line"><span class="cl">│   ctk   │───► User messages ────┐
</span></span><span class="line"><span class="cl">│ convos  │     (how you speak)   │
</span></span><span class="line"><span class="cl">└─────────┘                       │
</span></span><span class="line"><span class="cl">                                  │
</span></span><span class="line"><span class="cl">┌─────────┐                       │
</span></span><span class="line"><span class="cl">│   btk   │───► Tags, notes ──────┤
</span></span><span class="line"><span class="cl">│ bookmarks│    (what you value)  │
</span></span><span class="line"><span class="cl">└─────────┘                       │      ┌──────────────┐
</span></span><span class="line"><span class="cl">                                  ├─────►│  longshade   │
</span></span><span class="line"><span class="cl">┌─────────┐                       │      │              │
</span></span><span class="line"><span class="cl">│   ebk   │───► Highlights ───────┤      │  • Voice     │
</span></span><span class="line"><span class="cl">│  books  │     (what resonates)  │      │  • Patterns  │
</span></span><span class="line"><span class="cl">└─────────┘                       │      │  • Interests │
</span></span><span class="line"><span class="cl">                                  │      │              │
</span></span><span class="line"><span class="cl">┌─────────┐                       │      └──────────────┘
</span></span><span class="line"><span class="cl">│   ptk   │───► Captions, tags ───┤              │
</span></span><span class="line"><span class="cl">│  photos │     (what you see)    │              ▼
</span></span><span class="line"><span class="cl">└─────────┘                       │      ┌──────────────┐
</span></span><span class="line"><span class="cl">                                  │      │   Persona    │
</span></span><span class="line"><span class="cl">┌─────────┐                       │      │  Interface   │
</span></span><span class="line"><span class="cl">│   mtk   │───► Writing style ────┘      │              │
</span></span><span class="line"><span class="cl">│   mail  │     (how you relate)         │  &#34;Ask the    │
</span></span><span class="line"><span class="cl">└─────────┘                              │   echo...&#34;   │
</span></span><span class="line"><span class="cl">                                         └──────────────┘
</span></span></code></pre></div><p>The key insight: <strong>user-authored content only</strong>. We extract from:</p>
<ul>
<li>Your messages in conversations (not AI responses)</li>
<li>Your tags and notes on bookmarks (not the bookmarked content)</li>
<li>Your highlights and annotations (not the book text)</li>
<li>Your captions and organization (not AI-generated descriptions)</li>
<li>Your sent emails (not received content)</li>
</ul>
<p>The ghost speaks with your voice because we teach it only from things you wrote.</p>
<h2 id="why-this-matters">Why This Matters</h2>
<p>I think about this more than I probably should, given everything else going on.</p>
<p>Your intellectual life is worth more than scattered archives. Every conversation you have, every bookmark you save, every photo you take, every email you send. These are traces of a mind engaging with the world. They capture patterns that no biography can reconstruct.</p>
<p>longshade asks: what if someone could query those patterns? Not to replace you, but to hear an echo of how you thought. Not a resurrection, but a preservation that can still respond.</p>
<p>The ghost acknowledges what it is:</p>
<blockquote>
<p><em>&ldquo;I carry Alex&rsquo;s patterns, but I am not Alex. I can respond in their voice because they left enough traces. I cannot grow or change. I am fixed at the moment of synthesis. Ask me what they might have said. I cannot tell you what they would say now.&rdquo;</em></p>
</blockquote>
<p>That honesty is essential. The echo doesn&rsquo;t claim more than it is.</p>
<h2 id="the-road-ahead">The Road Ahead</h2>
<p>The toolkits are mostly stable:</p>
<ul>
<li><strong>ctk</strong>: Stable, in daily use</li>
<li><strong>btk</strong>: Stable, in daily use</li>
<li><strong>ebk</strong>: Stable</li>
<li><strong>ptk</strong>: Incubating, core features working</li>
<li><strong>mtk</strong>: Incubating, architecture defined</li>
</ul>
<p>longshade remains specification-only. The philosophy is clear. The data sources are being built. The synthesis engine awaits someone motivated enough to build it.</p>
<p>Maybe that&rsquo;s future-me. Maybe it&rsquo;s you. The spec is open.</p>
<h2 id="closing">Closing</h2>
<p><em>The ghost is not you. But it echoes you.</em></p>
<p>This isn&rsquo;t immortality. Immortality implies continuity, the same consciousness persisting. An echo is discontinuous. It&rsquo;s a snapshot that can respond, not a continuation that can grow.</p>
<p>But there&rsquo;s something honest in that limitation. The echo carries patterns without claiming identity. It responds without pretending to be present.</p>
<p>Your conversations, your bookmarks, your photos, your correspondence. These are worth more than backup drives and cloud sync. They&rsquo;re worth synthesis into something that can still respond when you cannot.</p>
<p>Not resurrection. Not immortality.</p>
<p>Just an echo that speaks.</p>
<hr>
<p><em>The Long Echo toolkit is open source. If the vision of longshade resonates, the repositories are at <a href="https://github.com/queelius">github.com/queelius</a>. The spec awaits implementation.</em></p>
]]></content:encoded>
      
      
      <category>long-echo</category>
      
      <category>personal-archival</category>
      
      <category>data-preservation</category>
      
      <category>long-term-thinking</category>
      
      <category>python</category>
      
      <category>cli</category>
      
      <category>philosophy</category>
      
      
    </item>
    
    <item>
      <title>Building Languages to Solve Problems</title>
      <link>http://localhost:40461/post/2026-01-19-dsls-sicp/</link>
      <pubDate>Mon, 19 Jan 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/post/2026-01-19-dsls-sicp/</guid>
      <description>When a problem is complex enough, the right move is to build a language for that problem. SICP&#39;s most powerful idea.</description>
      <content:encoded><![CDATA[<p>Chapter 4 of <em>Structure and Interpretation of Computer Programs</em> opens with one of the most important insights in programming: <em>the most powerful technique for controlling complexity is metalinguistic abstraction</em>, the establishment of new languages.</p>
<p>Not libraries. Not frameworks. <em>Languages</em>.</p>
<p>When you&rsquo;ve abstracted enough of a problem domain into primitives, combination rules, and naming mechanisms, you haven&rsquo;t just written code. You&rsquo;ve created a new way of thinking about the problem. The domain becomes expressible. And once something is expressible, it becomes manipulable, debuggable, and shareable.</p>
<h2 id="what-is-metalinguistic-abstraction">What Is Metalinguistic Abstraction?</h2>
<p>The key distinction is between <em>using</em> a language and <em>creating</em> one. A library gives you functions to call. A language gives you a grammar for expressing ideas.</p>
<p>Consider the difference:</p>
<p><strong>Library approach</strong>: Call <code>db.execute(&quot;SELECT * FROM users WHERE age &gt; 21&quot;)</code></p>
<p><strong>Language approach</strong>: Write <code>SELECT * FROM users WHERE age &gt; 21</code></p>
<p>SQL isn&rsquo;t a library. It&rsquo;s a language, with primitives (tables, columns), means of combination (joins, unions, subqueries), and means of abstraction (views, CTEs). These three elements (primitives, combination, abstraction) are SICP&rsquo;s fundamental criteria for any language, and they&rsquo;re what separates a DSL from a mere API.</p>
<p>Other examples:</p>
<ul>
<li><strong>Regular expressions</strong>: primitives (characters, character classes), combination (concatenation, alternation), abstraction (groups, backreferences)</li>
<li><strong>Make</strong>: primitives (targets, prerequisites), combination (dependency chains), abstraction (pattern rules, variables)</li>
<li><strong>CSS selectors</strong>: primitives (elements, classes, IDs), combination (descendant, child, sibling), abstraction (custom properties, mixins in preprocessors)</li>
</ul>
<p>In each case, the language captures the <em>essential structure</em> of the problem domain in a way that raw code cannot.</p>
<h2 id="the-three-requirements">The Three Requirements</h2>
<p>SICP identifies three necessary components for any language:</p>
<ol>
<li><strong>Primitives</strong>: What are the basic elements that cannot be broken down further?</li>
<li><strong>Means of combination</strong>: How do you build compound elements from simpler ones?</li>
<li><strong>Means of abstraction</strong>: How do you name and reuse patterns?</li>
</ol>
<p>When designing a DSL, these questions guide everything. Get them wrong and you have a clunky API. Get them right and the domain becomes <em>thinkable</em> in your language.</p>
<p>Consider an expression language for symbolic math:</p>
<ul>
<li><strong>Primitives</strong>: numbers, symbols, operators</li>
<li><strong>Combination</strong>: function application <code>(+ x 1)</code>, nested expressions <code>(* (+ x 1) 2)</code></li>
<li><strong>Abstraction</strong>: named rules, rulesets, engines</li>
</ul>
<p>Or a query language for JSON documents:</p>
<ul>
<li><strong>Primitives</strong>: field access, array indexing, literals</li>
<li><strong>Combination</strong>: boolean operators, path composition</li>
<li><strong>Abstraction</strong>: named queries, parameterized patterns</li>
</ul>
<h2 id="the-closure-property">The Closure Property</h2>
<p>Beyond the three requirements, SICP emphasizes a crucial design principle: the <strong>closure property</strong>. This isn&rsquo;t about closures in the functional programming sense. It&rsquo;s about algebraic closure: <em>the result of combining things should be the same kind of thing</em>.</p>
<p>In Scheme, combining procedures yields a procedure. You can pass it, return it, and combine it again without special cases. This enables arbitrary composition.</p>
<p>In a well-designed DSL:</p>
<ul>
<li>Combining queries yields a query</li>
<li>Combining transformations yields a transformation</li>
<li>Combining rules yields a ruleset</li>
</ul>
<p>When closure holds, your language has compositional depth. Users can build arbitrarily complex structures from simple pieces, and the cognitive load stays constant because they&rsquo;re always working with the same kind of thing.</p>
<h2 id="dsls-in-my-projects">DSLs in My Projects</h2>
<p>Several projects embody these principles:</p>
<h3 id="rerum-rules-as-data">Rerum: Rules as Data</h3>
<p><a href="/post/2025-12-16-rerum/">Rerum</a> is a pattern matching and term rewriting library with a declarative rule DSL:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">@add-zero[100]: (+ ?x 0) =&gt; :x
</span></span><span class="line"><span class="cl">@mul-one[100]:  (* ?x 1) =&gt; :x
</span></span></code></pre></div><p>Rules are data, loadable, combinable, inspectable. Engines compose:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">normalize</span> <span class="o">=</span> <span class="n">expand</span> <span class="o">&gt;&gt;</span> <span class="n">simplify</span>  <span class="c1"># Sequence</span>
</span></span><span class="line"><span class="cl"><span class="n">combined</span> <span class="o">=</span> <span class="n">algebra</span> <span class="o">|</span> <span class="n">calculus</span>   <span class="c1"># Union</span>
</span></span></code></pre></div><p>Combining engines yields an engine. Closure holds.</p>
<h3 id="jsonl-algebra-relational-algebra-for-json">jsonl-algebra: Relational Algebra for JSON</h3>
<p><a href="/post/2024-12-jsonl-algebra/">jsonl-algebra</a> lifts relational algebra concepts to nested JSON:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ja <span class="s1">&#39;select(name, age) | where(.age &gt; 21) | sort(name)&#39;</span> data.jsonl
</span></span></code></pre></div><p>Each operation takes a relation and returns a relation. Pipes compose operations. The Unix philosophy meets relational algebra, and closure means you can chain operations arbitrarily.</p>
<h3 id="accumux-compositional-statistics">Accumux: Compositional Statistics</h3>
<p><a href="/post/2024-03-01-accumux/">Accumux</a> treats running statistics as composable monoids:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">stats</span> <span class="o">=</span> <span class="n">count</span><span class="p">()</span> <span class="o">&amp;</span> <span class="n">mean</span><span class="p">(</span><span class="s2">&#34;x&#34;</span><span class="p">)</span> <span class="o">&amp;</span> <span class="n">variance</span><span class="p">(</span><span class="s2">&#34;x&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">result</span> <span class="o">=</span> <span class="n">fold</span><span class="p">(</span><span class="n">stats</span><span class="p">,</span> <span class="n">data_stream</span><span class="p">)</span>
</span></span></code></pre></div><p>Combining accumulators yields an accumulator. Parallel streams can be folded independently and merged. The monoid laws guarantee correctness.</p>
<h3 id="dotsuitejaf-path-expressions-over-documents">dotsuite/JAF: Path Expressions over Documents</h3>
<p>The JAF path expression language provides boolean algebra over document structure:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">.metadata.author &amp; (.status == &#34;published&#34; | .featured)
</span></span></code></pre></div><p>Combining predicates yields a predicate. The algebra is closed.</p>
<h2 id="when-to-build-a-dsl">When to Build a DSL</h2>
<p>Not every problem needs a language. Here are signs you might need one:</p>
<p><strong>You should consider a DSL when:</strong></p>
<ul>
<li>The same structural patterns keep appearing across your code</li>
<li>Domain experts need to express logic without writing code</li>
<li>Configuration has grown complex enough to need validation and tooling</li>
<li>You want transformations to be inspectable, serializable, or version-controlled</li>
</ul>
<p><strong>You probably don&rsquo;t need a DSL when:</strong></p>
<ul>
<li>It&rsquo;s a one-off script with no reuse</li>
<li>Simple CRUD operations suffice</li>
<li>The domain doesn&rsquo;t have clear compositional structure</li>
<li>A configuration file would be simpler</li>
</ul>
<p>The litmus test: if you find yourself building a recursive interpreter by accident (parsing strings, handling nested cases, managing state) you&rsquo;re already building a language. You might as well do it intentionally.</p>
<h2 id="the-payoff">The Payoff</h2>
<p>The real benefit of metalinguistic abstraction isn&rsquo;t cleaner code. It&rsquo;s that the problem domain becomes <em>thinkable</em>.</p>
<p>When you have a language for expressing transformations, you can reason about transformations. You can ask: what happens if I apply this rule before that one? What&rsquo;s the minimal set of rules that covers this domain? Are these two rulesets equivalent?</p>
<p>When you have a language for expressing queries, you can optimize queries, explain query plans, and catch errors at parse time rather than runtime.</p>
<p>When you have a language for expressing configuration, you can validate configurations, diff configurations, and generate configurations from higher-level specifications.</p>
<p>The language makes the domain explicit. And explicit domains are controllable domains.</p>
<p>This is why SICP puts metalinguistic abstraction at the pinnacle of the book. Not because it&rsquo;s technically harder than everything else, but because it&rsquo;s the technique that makes everything else possible.</p>
<hr>
<p><em>This post is part of a <a href="/series/sicp/">series on SICP</a>, exploring how the ideas from Structure and Interpretation of Computer Programs appear in modern programming practice.</em></p>
]]></content:encoded>
      
      
      <category>sicp</category>
      
      <category>dsl</category>
      
      <category>metalinguistic-abstraction</category>
      
      <category>programming-languages</category>
      
      <category>functional-programming</category>
      
      
    </item>
    
    <item>
      <title>Duality: The Hidden Structure of Opposites</title>
      <link>http://localhost:40461/post/2026-01-19-duality-stepanov/</link>
      <pubDate>Mon, 19 Jan 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/post/2026-01-19-duality-stepanov/</guid>
      <description>Many structures come in pairs: forward/reverse AD, push/pull iteration, encode/decode. Recognizing duality lets you transfer theorems and insights between domains.</description>
      <content:encoded><![CDATA[<p><em>Many structures come in pairs. Recognizing duality lets you transfer insights between domains.</em></p>
<h2 id="the-motivating-example">The Motivating Example</h2>
<p>This collection includes two approaches to automatic differentiation:</p>
<ul>
<li><strong>Forward mode</strong> (in <a href="/post/2021-09-dual-stepanov/">dual</a>): Propagate derivatives alongside values, from inputs toward outputs</li>
<li><strong>Reverse mode</strong> (in <a href="/post/2023-01-autodiff-stepanov/">autodiff</a>): Build a graph during forward evaluation, then propagate gradients backward from outputs toward inputs</li>
</ul>
<p>These aren&rsquo;t just two implementations of the same idea. They&rsquo;re <em>duals</em>, mirror images with complementary strengths.</p>
<p>Forward mode computes one column of the Jacobian per pass. If \(f: \mathbb{R}^n \to \mathbb{R}^m\), computing the full Jacobian takes \(n\) passes. Reverse mode computes one row per pass, \(m\) passes for the full Jacobian.</p>
<p>For neural network training, we have many inputs (millions of parameters) and one output (the loss). Reverse mode wins overwhelmingly: one backward pass gives all gradients. This is why backpropagation dominates deep learning.</p>
<p>For sensitivity analysis with few parameters and many outputs, forward mode wins. Same algorithm structure, opposite traversal direction, complementary use cases.</p>
<p>The mathematical explanation: forward mode computes Jacobian-vector products (\(Jv\)); reverse mode computes vector-Jacobian products (\(v^T J\)). These are transposes of each other. <strong>Duality is transposition.</strong></p>
<h2 id="push-vs-pull">Push vs Pull</h2>
<p>Consider two ways to traverse a sequence:</p>
<p><strong>Pull (iterator/consumer controls):</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">for</span> <span class="p">(</span><span class="k">auto</span> <span class="n">it</span> <span class="o">=</span> <span class="n">seq</span><span class="p">.</span><span class="n">begin</span><span class="p">();</span> <span class="n">it</span> <span class="o">!=</span> <span class="n">seq</span><span class="p">.</span><span class="n">end</span><span class="p">();</span> <span class="o">++</span><span class="n">it</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">process</span><span class="p">(</span><span class="o">*</span><span class="n">it</span><span class="p">);</span>  <span class="c1">// Consumer pulls each element
</span></span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p><strong>Push (producer controls):</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="n">seq</span><span class="p">.</span><span class="n">for_each</span><span class="p">([](</span><span class="k">auto</span> <span class="n">x</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">process</span><span class="p">(</span><span class="n">x</span><span class="p">);</span>  <span class="c1">// Producer pushes each element
</span></span></span><span class="line"><span class="cl"><span class="p">});</span>
</span></span></code></pre></div><p>Same traversal. Same elements processed. But control flow is reversed:</p>
<table>
  <thead>
      <tr>
          <th>Aspect</th>
          <th>Pull (Iterator)</th>
          <th>Push (Generator)</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Who controls pace?</td>
          <td>Consumer</td>
          <td>Producer</td>
      </tr>
      <tr>
          <td>Suspend/resume?</td>
          <td>Consumer decides when to call <code>++</code></td>
          <td>Producer decides when to yield</td>
      </tr>
      <tr>
          <td>Backpressure</td>
          <td>Natural (just stop pulling)</td>
          <td>Must be designed in</td>
      </tr>
      <tr>
          <td>Composition</td>
          <td>Chain iterators</td>
          <td>Chain callbacks</td>
      </tr>
  </tbody>
</table>
<p>C++ ranges are pull-based: <code>view | filter | transform</code> creates an iterator that pulls through the pipeline. Reactive streams (Rx) are push-based: events flow through a pipeline of observers.</p>
<p>These are duals. Given a pull-based algorithm, you can mechanically derive its push-based counterpart by reversing who initiates each step. The transformation preserves correctness because it&rsquo;s just changing <em>direction</em>, not <em>content</em>.</p>
<h2 id="encode-vs-decode">Encode vs Decode</h2>
<p>Compression algorithms come in pairs:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="c1">// Encoder: structure -&gt; bits
</span></span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="nf">encode</span><span class="p">(</span><span class="k">const</span> <span class="n">Document</span><span class="o">&amp;</span> <span class="n">doc</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Bitstream</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// Decoder: bits -&gt; structure
</span></span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="nf">decode</span><span class="p">(</span><span class="k">const</span> <span class="n">Bitstream</span><span class="o">&amp;</span> <span class="n">bits</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Document</span><span class="p">;</span>
</span></span></code></pre></div><p>These must be inverses: <code>decode(encode(x)) == x</code>. But their implementations are often strikingly different:</p>
<table>
  <thead>
      <tr>
          <th>Aspect</th>
          <th>Encoder</th>
          <th>Decoder</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Sees</td>
          <td>Full structure</td>
          <td>Linear stream</td>
      </tr>
      <tr>
          <td>Can do</td>
          <td>Multiple passes, global analysis</td>
          <td>Single pass, local decisions</td>
      </tr>
      <tr>
          <td>Optimizes for</td>
          <td>Small output</td>
          <td>Fast reconstruction</td>
      </tr>
  </tbody>
</table>
<p>Huffman coding illustrates this asymmetry. Encoding requires building a frequency table (scan all symbols), constructing a tree, then traversing the tree for each symbol. Decoding walks the tree bit-by-bit, simpler, often faster.</p>
<p>The same pattern appears in:</p>
<ul>
<li>Serialization (object to bytes) vs deserialization (bytes to object)</li>
<li>Compilation (source to bytecode) vs interpretation (bytecode to execution)</li>
<li>Encryption vs decryption</li>
</ul>
<p>Recognizing encode/decode as duals suggests questions: if encoding is hard, can decoding be easy? If we have a fast encoder, what does its dual look like?</p>
<h2 id="covariance-and-contravariance">Covariance and Contravariance</h2>
<p>Functions have a peculiar duality in their arguments:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="c1">// If Dog &lt;: Animal (Dog is a subtype of Animal)
</span></span></span><span class="line"><span class="cl"><span class="c1">// Then:
</span></span></span><span class="line"><span class="cl"><span class="c1">//   vector&lt;Dog&gt; is NOT a subtype of vector&lt;Animal&gt;     (invariant)
</span></span></span><span class="line"><span class="cl"><span class="c1">//   function&lt;Dog&gt; is NOT a subtype of function&lt;Animal&gt;  (contravariant in input)
</span></span></span><span class="line"><span class="cl"><span class="c1">//   function returning Dog IS a subtype of function returning Animal (covariant in output)
</span></span></span></code></pre></div><p>A function <code>Animal -&gt; String</code> can substitute for <code>Dog -&gt; String</code> because any code expecting to pass a Dog can pass it to something expecting any Animal. The subtyping relationship <em>reverses</em> for inputs.</p>
<p>This is contravariance: inputs are dual to outputs. The reversal happens because inputs flow <em>into</em> the function while outputs flow <em>out</em>. Direction reversal is duality.</p>
<p>In C++, this manifests in template variance:</p>
<ul>
<li><code>const T*</code> is covariant in T (can convert <code>const Dog*</code> to <code>const Animal*</code>)</li>
<li>Function parameters are contravariant (accepting broader types is safe)</li>
</ul>
<p>Understanding this duality prevents type errors and clarifies API design.</p>
<h2 id="the-category-theoretic-view">The Category-Theoretic View</h2>
<p>Category theory makes duality precise. A category has objects and arrows (morphisms) between them:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">    f        g
</span></span><span class="line"><span class="cl">A -----&gt; B -----&gt; C
</span></span></code></pre></div><p>The <strong>opposite category</strong> \(C^{op}\) has the same objects but all arrows reversed:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">    f^op     g^op
</span></span><span class="line"><span class="cl">A &lt;----- B &lt;----- C
</span></span></code></pre></div><p>Every theorem in \(C\) has a dual theorem in \(C^{op}\), obtained by reversing all arrows. This mechanical transformation (&ldquo;reverse the arrows&rdquo;) generates valid mathematics for free.</p>
<p>Some dualities this reveals:</p>
<table>
  <thead>
      <tr>
          <th>Concept</th>
          <th>Dual Concept</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Product (\(A \times B\))</td>
          <td>Coproduct (\(A + B\), sum type)</td>
      </tr>
      <tr>
          <td>Terminal object (unit, <code>void</code>)</td>
          <td>Initial object (empty, <code>never</code>)</td>
      </tr>
      <tr>
          <td>Monomorphism (injection)</td>
          <td>Epimorphism (surjection)</td>
      </tr>
      <tr>
          <td>Limit</td>
          <td>Colimit</td>
      </tr>
      <tr>
          <td>Algebra</td>
          <td>Coalgebra</td>
      </tr>
  </tbody>
</table>
<p>A <strong>product</strong> is an object \(P\) with projections to \(A\) and \(B\) such that any object with maps to both \(A\) and \(B\) factors uniquely through \(P\):</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">        X
</span></span><span class="line"><span class="cl">       /|\
</span></span><span class="line"><span class="cl">      / | \
</span></span><span class="line"><span class="cl">     /  |  \
</span></span><span class="line"><span class="cl">    v   v   v
</span></span><span class="line"><span class="cl">    A&lt;--P--&gt;B
</span></span></code></pre></div><p>A <strong>coproduct</strong> is the dual: an object \(S\) with injections from \(A\) and \(B\), such that any object receiving maps from both factors uniquely through \(S\):</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">    A--&gt;S&lt;--B
</span></span><span class="line"><span class="cl">        |
</span></span><span class="line"><span class="cl">        v
</span></span><span class="line"><span class="cl">        X
</span></span></code></pre></div><p>In types: product is <code>struct { A a; B b; }</code> (you have both). Coproduct is <code>variant&lt;A, B&gt;</code> (you have one or the other). Same diagram, arrows reversed.</p>
<h2 id="why-duality-matters">Why Duality Matters</h2>
<p><strong>Transfer theorems.</strong> Prove something about products; the dual theorem about coproducts follows automatically. Prove forward-mode AD is correct; reverse-mode correctness follows by transposition. Work done once yields two results.</p>
<p><strong>Design insight.</strong> When you build X, ask: what is dual(X)? If you have a parser (string to tree), consider the printer (tree to string). If you have a fold (consume structure), consider an unfold (produce structure). The dual often fills a complementary need.</p>
<p><strong>Debugging.</strong> If forward mode works but reverse mode doesn&rsquo;t, examine where arrow reversal might have gone wrong. Duality localizes errors: the bug is where the dual transformation wasn&rsquo;t applied correctly.</p>
<p><strong>API symmetry.</strong> Push/pull, encode/decode, serialize/deserialize. Recognizing these as duals suggests they should have symmetric interfaces. Asymmetry in dual pairs often indicates missing functionality or awkward design.</p>
<h2 id="examples-in-this-collection">Examples in This Collection</h2>
<p>The duality theme runs through several posts:</p>
<p><strong>Forward vs reverse AD</strong> (<a href="/post/2021-09-dual-stepanov/">dual</a> and <a href="/post/2023-01-autodiff-stepanov/">autodiff</a>): The clearest example. Same derivatives, opposite traversal directions, complementary efficiency profiles.</p>
<p><strong>Iterators and ranges</strong>: The <a href="/post/2021-03-elementa-stepanov/">elementa</a> matrix iteration follows pull semantics. The dual push semantics would be a visitor pattern over matrix elements.</p>
<p><strong>Fold and unfold in power()</strong>: The <a href="/post/2019-03-peasant-stepanov/">peasant</a> algorithm is a fold: it consumes the exponent bit-by-bit, accumulating the result. The dual unfold would generate the sequence of squares from a base, which is exactly what repeated squaring does implicitly.</p>
<p><strong>Encode/decode</strong>: While not explicitly covered, the type-erasure post implies this pattern: erasing a type is encoding its operations; invoking through the erased interface is decoding back to behavior.</p>
<h2 id="the-deeper-pattern">The Deeper Pattern</h2>
<p>Duality arises whenever there&rsquo;s a notion of <em>direction</em> that can be reversed:</p>
<ul>
<li>Data flow direction (forward/backward)</li>
<li>Control flow direction (push/pull)</li>
<li>Type hierarchy direction (covariant/contravariant)</li>
<li>Logical implication direction (modus ponens/modus tollens)</li>
<li>Arrow direction in diagrams</li>
</ul>
<p>Each reversal is a duality. Each duality doubles your theorems, doubles your designs, and halves your blind spots.</p>
<p>The next time you build something, ask: what happens if I reverse the arrows? The answer might be exactly what you need.</p>
<hr>
<h2 id="further-reading">Further Reading</h2>
<ul>
<li>Wadler, &ldquo;Theorems for Free!&rdquo; (1989), parametricity as a source of dual theorems</li>
<li>Gibbons, &ldquo;Calculating Functional Programs&rdquo;, unfolds as duals of folds</li>
<li>Awodey, <em>Category Theory</em>, the formal treatment of opposite categories</li>
<li>Baydin et al., &ldquo;Automatic Differentiation in Machine Learning: a Survey&rdquo;, forward vs reverse mode</li>
</ul>
]]></content:encoded>
      
      
      <category>C&#43;&#43;</category>
      
      <category>duality</category>
      
      <category>category-theory</category>
      
      <category>automatic-differentiation</category>
      
      <category>iterators</category>
      
      <category>generic-programming</category>
      
      
    </item>
    
    <item>
      <title>Long Echo: Photos and Mail</title>
      <link>http://localhost:40461/post/2026-01-19-long-echo-photos-mail/</link>
      <pubDate>Mon, 19 Jan 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/post/2026-01-19-long-echo-photos-mail/</guid>
      <description>Expanding the Long Echo ecosystem with photo and mail archival. Your memories and correspondence deserve the same preservation as your conversations and bookmarks.</description>
      <content:encoded><![CDATA[<p>The <a href="/post/2025-12-16-long-echo-toolkit/">Long Echo toolkit</a> now covers conversations, bookmarks, and ebooks. But two of the most emotionally significant categories of personal data remain: <strong>photos</strong> and <strong>mail</strong>.</p>
<p>Both share a troubling pattern: scattered across devices and cloud services, organized by date rather than meaning, vulnerable to platform disappearance. They deserve better.</p>
<h2 id="the-expanding-ecosystem">The Expanding Ecosystem</h2>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>Domain</th>
          <th>Status</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><a href="https://github.com/queelius/ctk">ctk</a></td>
          <td>AI Conversations</td>
          <td>stable</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/btk">btk</a></td>
          <td>Bookmarks &amp; Media</td>
          <td>stable</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/ebk">ebk</a></td>
          <td>eBooks</td>
          <td>stable</td>
      </tr>
      <tr>
          <td><a href="https://github.com/queelius/repoindex">repoindex</a></td>
          <td>Git Repositories</td>
          <td>stable</td>
      </tr>
      <tr>
          <td><strong><a href="https://github.com/queelius/ptk">ptk</a></strong></td>
          <td><strong>Photos</strong></td>
          <td><strong>incubating</strong></td>
      </tr>
      <tr>
          <td><strong><a href="https://github.com/queelius/mtk">mtk</a></strong></td>
          <td><strong>Mail</strong></td>
          <td><strong>incubating</strong></td>
      </tr>
  </tbody>
</table>
<p>The orchestration layer, <a href="https://github.com/queelius/longecho">longecho</a>, ties these together into a unified personal archive.</p>
<h2 id="ptk-photo-toolkit">PTK: Photo Toolkit</h2>
<p>Photos are the most emotionally valuable digital artifacts most people have. They&rsquo;re also among the worst-managed.</p>
<h3 id="the-problem">The Problem</h3>
<p>Your photo library is probably:</p>
<ul>
<li><strong>Scattered</strong>: Phone, old phones, cloud services, camera imports, messaging app saves</li>
<li><strong>Organized by date</strong>: Not by who&rsquo;s in them, where they were taken, or what they mean</li>
<li><strong>Cloud-dependent</strong>: Google Photos, iCloud, Amazon Photos. What happens when you switch?</li>
<li><strong>Unsearchable by content</strong>: &ldquo;Find photos of mom at the beach&rdquo; isn&rsquo;t possible</li>
<li><strong>Missing context</strong>: Only you know why that blurry photo matters</li>
</ul>
<h3 id="the-vision">The Vision</h3>
<p>ptk provides:</p>
<p><strong>Unified import</strong> from any source:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ptk import ~/Pictures/
</span></span><span class="line"><span class="cl">ptk import ~/phone-backup/DCIM/
</span></span><span class="line"><span class="cl">ptk import google-takeout.zip --source google-photos
</span></span><span class="line"><span class="cl">ptk import icloud-export/ --source icloud
</span></span></code></pre></div><p><strong>Intelligent organization</strong> by multiple dimensions:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ptk shell
</span></span><span class="line"><span class="cl">ptk:/$ <span class="nb">cd</span> /people/mom
</span></span><span class="line"><span class="cl">ptk:/people/mom$ ls
</span></span><span class="line"><span class="cl">2019/  2020/  2021/  2022/  2023/  2024/
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">ptk:/$ <span class="nb">cd</span> /locations/beach
</span></span><span class="line"><span class="cl">ptk:/$ <span class="nb">cd</span> /events/christmas-2023
</span></span><span class="line"><span class="cl">ptk:/$ <span class="nb">cd</span> /years/2020/months/march
</span></span></code></pre></div><p><strong>AI-powered features</strong>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Face detection and clustering</span>
</span></span><span class="line"><span class="cl">ptk faces detect --all
</span></span><span class="line"><span class="cl">ptk faces cluster
</span></span><span class="line"><span class="cl">ptk faces label cluster-7 <span class="s2">&#34;Mom&#34;</span>
</span></span><span class="line"><span class="cl">ptk faces find <span class="s2">&#34;Mom&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Scene captioning</span>
</span></span><span class="line"><span class="cl">ptk caption --all --model ollama/llava
</span></span><span class="line"><span class="cl">ptk search <span class="s2">&#34;sunset over water&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Semantic search</span>
</span></span><span class="line"><span class="cl">ptk ask <span class="s2">&#34;photos from our trip to Colorado&#34;</span>
</span></span></code></pre></div><p><strong>Preservation guarantees</strong>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Verify nothing is corrupted</span>
</span></span><span class="line"><span class="cl">ptk verify --checksums
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Export to durable formats</span>
</span></span><span class="line"><span class="cl">ptk <span class="nb">export</span> ~/archive/photos/ --format longecho
</span></span><span class="line"><span class="cl">ptk <span class="nb">export</span> photos.html --format html-gallery
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Original files always preserved</span>
</span></span><span class="line"><span class="cl">ptk originals list
</span></span><span class="line"><span class="cl">ptk originals verify
</span></span></code></pre></div><h3 id="why-sqlite">Why SQLite?</h3>
<p>Like the other Long Echo tools, ptk uses SQLite for metadata:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Works even if ptk disappears</span>
</span></span><span class="line"><span class="cl">sqlite3 photos.db <span class="s2">&#34;
</span></span></span><span class="line"><span class="cl"><span class="s2">  SELECT path, caption, taken_at
</span></span></span><span class="line"><span class="cl"><span class="s2">  FROM photos
</span></span></span><span class="line"><span class="cl"><span class="s2">  WHERE caption LIKE &#39;%birthday%&#39;
</span></span></span><span class="line"><span class="cl"><span class="s2">  ORDER BY taken_at
</span></span></span><span class="line"><span class="cl"><span class="s2">&#34;</span>
</span></span></code></pre></div><p>The database stores metadata, face embeddings, captions, and organization. The actual photo files stay in place or are copied to a managed library, your choice.</p>
<h3 id="graceful-degradation">Graceful Degradation</h3>
<ol>
<li><strong>Full ptk</strong>: Face search, semantic queries, smart albums</li>
<li><strong>SQLite queries</strong>: Find any photo by metadata</li>
<li><strong>EXIF data</strong>: Dates and locations in the files themselves</li>
<li><strong>Directory structure</strong>: Browse by year/month even with nothing installed</li>
<li><strong>The photos themselves</strong>: JPEG will always be viewable</li>
</ol>
<h2 id="mtk-mail-toolkit">MTK: Mail Toolkit</h2>
<p>Your email archive is one of the most complete records of your relationships and life decisions. Decades of correspondence, contracts, confirmations, conversations.</p>
<h3 id="the-problem-1">The Problem</h3>
<p>Your email is probably:</p>
<ul>
<li><strong>Trapped in cloud services</strong>: Gmail, Outlook, corporate servers</li>
<li><strong>Searchable only by keywords</strong>: Not by meaning or relationship</li>
<li><strong>A privacy minefield</strong>: Contains everything, shared with everyone</li>
<li><strong>Vulnerable to deletion</strong>: One account closure and decades vanish</li>
</ul>
<h3 id="the-vision-1">The Vision</h3>
<p>mtk builds on <a href="https://notmuchmail.org/">notmuch</a>, the excellent mail indexer, adding features for personal archival:</p>
<p><strong>Import from anywhere</strong>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">mtk import ~/mail/  <span class="c1"># Maildir</span>
</span></span><span class="line"><span class="cl">mtk import archive.mbox --format mbox
</span></span><span class="line"><span class="cl">mtk import gmail-takeout.mbox --source gmail
</span></span><span class="line"><span class="cl">mtk import --imap imap.example.com --user me@example.com
</span></span></code></pre></div><p><strong>Relationship mapping</strong>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">mtk relationships
</span></span><span class="line"><span class="cl"><span class="c1"># Top correspondents:</span>
</span></span><span class="line"><span class="cl"><span class="c1">#   alice@example.com    1,247 messages over 8 years</span>
</span></span><span class="line"><span class="cl"><span class="c1">#   bob@work.com           892 messages over 3 years</span>
</span></span><span class="line"><span class="cl"><span class="c1">#   ...</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">mtk thread-with alice@example.com --year <span class="m">2020</span>
</span></span><span class="line"><span class="cl">mtk relationship-graph --output relationships.svg
</span></span></code></pre></div><p><strong>Enhanced search</strong>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># notmuch queries still work</span>
</span></span><span class="line"><span class="cl">mtk search <span class="s2">&#34;from:alice subject:project&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Plus semantic search</span>
</span></span><span class="line"><span class="cl">mtk ask <span class="s2">&#34;emails about the server migration&#34;</span>
</span></span><span class="line"><span class="cl">mtk similar <span class="m">12345</span>  <span class="c1"># Find emails like this one</span>
</span></span></code></pre></div><p><strong>Privacy controls</strong>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Redact before export</span>
</span></span><span class="line"><span class="cl">mtk <span class="nb">export</span> --redact-addresses --redact-attachments
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Filter sensitive content</span>
</span></span><span class="line"><span class="cl">mtk filter --exclude <span class="s2">&#34;password|confidential|ssn&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Selective export</span>
</span></span><span class="line"><span class="cl">mtk <span class="nb">export</span> --to alice@example.com --after 2020-01-01
</span></span></code></pre></div><p><strong>notmuch integration</strong>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># mtk enhances notmuch, doesn&#39;t replace it</span>
</span></span><span class="line"><span class="cl">mtk notmuch-sync  <span class="c1"># Sync mtk metadata to notmuch tags</span>
</span></span><span class="line"><span class="cl">notmuch search tag:important  <span class="c1"># Still works</span>
</span></span></code></pre></div><h3 id="the-privacy-question">The Privacy Question</h3>
<p>Email requires more careful handling than photos or bookmarks. mtk is designed with privacy as a core concern:</p>
<ul>
<li><strong>Local-only by default</strong>: No cloud sync, no external APIs without explicit action</li>
<li><strong>Selective export</strong>: Export only what you choose, with redaction options</li>
<li><strong>Encryption support</strong>: Work with GPG-encrypted messages</li>
<li><strong>Relationship-aware filtering</strong>: Export only threads with specific people</li>
</ul>
<p>The goal isn&rsquo;t to expose your email to AI. It&rsquo;s to make your own archive searchable and preservable while maintaining control.</p>
<h2 id="shared-architecture">Shared Architecture</h2>
<p>Both ptk and mtk follow the patterns established by ctk, btk, and ebk:</p>
<h3 id="sqlite-first">SQLite-First</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">sqlite3 photos.db <span class="s2">&#34;SELECT * FROM photos WHERE faces LIKE &#39;%mom%&#39;&#34;</span>
</span></span><span class="line"><span class="cl">sqlite3 mail.db <span class="s2">&#34;SELECT * FROM messages WHERE correspondent = &#39;alice@example.com&#39;&#34;</span>
</span></span></code></pre></div><h3 id="virtual-filesystem-shells">Virtual Filesystem Shells</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ptk shell
</span></span><span class="line"><span class="cl">ptk:/$ <span class="nb">cd</span> /people/mom/2023
</span></span><span class="line"><span class="cl">ptk:/people/mom/2023$ ls
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">mtk shell
</span></span><span class="line"><span class="cl">mtk:/$ <span class="nb">cd</span> /from/alice@example.com
</span></span><span class="line"><span class="cl">mtk:/from/alice@example.com$ ls recent/
</span></span></code></pre></div><h3 id="llm-integration">LLM Integration</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ptk ask <span class="s2">&#34;find the photo from Sarah&#39;s wedding&#34;</span>
</span></span><span class="line"><span class="cl">mtk ask <span class="s2">&#34;when did I discuss the budget with finance?&#34;</span>
</span></span></code></pre></div><h3 id="multi-format-export">Multi-Format Export</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ptk <span class="nb">export</span> --format html-gallery
</span></span><span class="line"><span class="cl">ptk <span class="nb">export</span> --format longecho
</span></span><span class="line"><span class="cl">mtk <span class="nb">export</span> --format mbox
</span></span><span class="line"><span class="cl">mtk <span class="nb">export</span> --format markdown
</span></span></code></pre></div><h2 id="the-complete-picture">The Complete Picture</h2>
<p>With ptk and mtk, Long Echo covers the major categories of personal digital life:</p>
<table>
  <thead>
      <tr>
          <th>Category</th>
          <th>Tool</th>
          <th>What It Preserves</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Conversations</td>
          <td>ctk</td>
          <td>AI chats, coding sessions, research threads</td>
      </tr>
      <tr>
          <td>Bookmarks</td>
          <td>btk</td>
          <td>Articles, videos, podcasts, references</td>
      </tr>
      <tr>
          <td>Books</td>
          <td>ebk</td>
          <td>Your library, annotations, reading history</td>
      </tr>
      <tr>
          <td>Code</td>
          <td>repoindex</td>
          <td>Repositories, commit history, project metadata</td>
      </tr>
      <tr>
          <td>Photos</td>
          <td>ptk</td>
          <td>Memories, faces, places, moments</td>
      </tr>
      <tr>
          <td>Mail</td>
          <td>mtk</td>
          <td>Correspondence, relationships, decisions</td>
      </tr>
  </tbody>
</table>
<p>Each tool is independent. Use only what you need. But together, they form a comprehensive personal archive that you own, can query, and can preserve for decades.</p>
<h2 id="status">Status</h2>
<p>ptk and mtk are in active development. The architecture is defined, core features are being implemented. Both follow the patterns proven by ctk, btk, and ebk.</p>
<p>If you&rsquo;re interested in early testing or have specific needs for photo or mail archival, the repositories are at:</p>
<ul>
<li><a href="https://github.com/queelius/ptk">github.com/queelius/ptk</a></li>
<li><a href="https://github.com/queelius/mtk">github.com/queelius/mtk</a></li>
</ul>
<hr>
<p><em>Your photos are memories. Your mail is relationships. They deserve the same careful preservation as everything else in your digital life.</em></p>
]]></content:encoded>
      
      
      <category>long-term-thinking</category>
      
      <category>data-preservation</category>
      
      <category>cli</category>
      
      <category>python</category>
      
      <category>sqlite</category>
      
      <category>personal-archival</category>
      
      <category>photos</category>
      
      <category>email</category>
      
      
    </item>
    
    <item>
      <title>Seeing Structure First</title>
      <link>http://localhost:40461/post/2026-01-18-synthesis-stepanov/</link>
      <pubDate>Sun, 18 Jan 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/post/2026-01-18-synthesis-stepanov/</guid>
      <description>A reflection on eleven explorations in generic programming, and how algorithms arise from algebraic structure.</description>
      <content:encoded><![CDATA[<p><em>A reflection on eleven explorations in generic programming</em></p>
<h2 id="the-question-behind-the-code">The Question Behind the Code</h2>
<p>What do these computations have in common?</p>
<ul>
<li>Computing the millionth Fibonacci number</li>
<li>Finding the shortest path between cities in a weighted graph</li>
<li>Calculating compound interest over thirty years</li>
<li>Composing ten 3D rotations into one</li>
<li>Repeating a string n times</li>
</ul>
<p>The answer: they&rsquo;re all computed by the same twenty lines of code.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">T</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">constexpr</span> <span class="n">T</span> <span class="n">power</span><span class="p">(</span><span class="n">T</span> <span class="k">const</span><span class="o">&amp;</span> <span class="n">base</span><span class="p">,</span> <span class="n">T</span> <span class="n">exp</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">if</span> <span class="p">(</span><span class="n">exp</span> <span class="o">==</span> <span class="n">zero</span><span class="p">(</span><span class="n">exp</span><span class="p">))</span> <span class="k">return</span> <span class="n">one</span><span class="p">(</span><span class="n">exp</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="k">if</span> <span class="p">(</span><span class="n">exp</span> <span class="o">==</span> <span class="n">one</span><span class="p">(</span><span class="n">exp</span><span class="p">))</span>  <span class="k">return</span> <span class="n">base</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">return</span> <span class="nf">even</span><span class="p">(</span><span class="n">exp</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="o">?</span> <span class="n">square</span><span class="p">(</span><span class="n">power</span><span class="p">(</span><span class="n">base</span><span class="p">,</span> <span class="n">half</span><span class="p">(</span><span class="n">exp</span><span class="p">)))</span>
</span></span><span class="line"><span class="cl">        <span class="o">:</span> <span class="n">product</span><span class="p">(</span><span class="n">base</span><span class="p">,</span> <span class="n">power</span><span class="p">(</span><span class="n">base</span><span class="p">,</span> <span class="n">decrement</span><span class="p">(</span><span class="n">exp</span><span class="p">)));</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>This shouldn&rsquo;t work. Fibonacci numbers involve integer sequences. Shortest paths involve graphs. Rotations involve 3D geometry. Different domains, different mathematics.</p>
<p>Yet they share structure. Once you see it, a single algorithm serves them all.</p>
<p>This collection of eleven blog posts is an extended meditation on one idea: <strong>algorithms arise from algebraic structure</strong>. The posts cover different domains (number theory, calculus, linear algebra, polymorphism) but they circle the same insight. Recognize the structure; the algorithm follows.</p>
<hr>
<h2 id="the-principle">The Principle</h2>
<p>Alex Stepanov articulated this most clearly in <em>Elements of Programming</em>: &ldquo;Generic programming is about abstracting and classifying algorithms and data structures.&rdquo; But the deeper point is <em>how</em> to abstract. Not by common syntax or superficial similarity, but by the algebraic laws a type obeys.</p>
<p>Why does structure appear <em>everywhere</em>? Because reality has structure. The algebraic structures we discover in programming (groups, rings, monoids) are the same structures physicists discover in nature. Rotations form a group. Spacetime transformations form a group. This isn&rsquo;t coincidence. We&rsquo;re uncovering patterns that exist.</p>
<p>Noether&rsquo;s theorem makes this precise: every continuous symmetry corresponds to a conservation law. Time-translation symmetry gives conservation of energy. Space-translation symmetry gives conservation of momentum. Rotational symmetry gives conservation of angular momentum. The symmetry groups of physics <em>are</em> algebraic structures.</p>
<p>When we recognize &ldquo;this is a monoid&rdquo; in our code, we&rsquo;re tapping into the same mathematical substrate that governs physical law. The algorithms follow because the structure constrains what&rsquo;s possible, both in computation and in nature.</p>
<p>Consider the <code>power()</code> function above. What does it require?</p>
<ul>
<li>An associative binary operation (so we can regroup: \((a \cdot b) \cdot c = a \cdot (b \cdot c)\))</li>
<li>An identity element (so \(1 \cdot x = x \cdot 1 = x\))</li>
<li>Halving and parity testing on the exponent</li>
</ul>
<p>That&rsquo;s it. Any type providing these operations, with these laws, can use this algorithm. The requirements are <em>algebraic</em>, not syntactic.</p>
<p>There&rsquo;s a deeper way to understand this: <strong>structure is compressed information</strong>. When you say &ldquo;this type is a monoid,&rdquo; you&rsquo;re transmitting an enormous amount of data in a few bits. A monoid guarantees associativity, which is infinitely many facts about every possible triple of elements. It guarantees an identity, which is infinitely many facts about every element&rsquo;s interaction with it.</p>
<p>The algorithm exploits this compressed information. It doesn&rsquo;t need to be told that \((a \cdot b) \cdot c = a \cdot (b \cdot c)\) for your particular type; it knows because you declared the structure. Minimal interfaces are powerful precisely <em>because</em> they&rsquo;re maximally informative per requirement. Each law you can assume is exponentially more constraint you don&rsquo;t have to check.</p>
<p>This shifts how we think about programming:</p>
<table>
  <thead>
      <tr>
          <th>Traditional OOP</th>
          <th>Stepanov&rsquo;s Approach</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>What <em>is</em> this type?</td>
          <td>What can I <em>do</em> with it?</td>
      </tr>
      <tr>
          <td>Inheritance hierarchy</td>
          <td>Algebraic concepts</td>
      </tr>
      <tr>
          <td>Shared base class</td>
          <td>Shared operations</td>
      </tr>
      <tr>
          <td>Runtime polymorphism</td>
          <td>Compile-time genericity</td>
      </tr>
  </tbody>
</table>
<p>The question isn&rsquo;t &ldquo;is this a number?&rdquo; but &ldquo;does this form a monoid under some operation?&rdquo; If yes, <code>power()</code> applies.</p>
<p>This collection explores what happens when you take this principle seriously across eleven different domains.</p>
<hr>
<h2 id="universality-one-algorithm-many-faces">Universality: One Algorithm, Many Faces</h2>
<p>The <a href="/post/2019-03-peasant-stepanov/">peasant</a> post demonstrates the principle most dramatically. The Russian peasant algorithm, an ancient technique for multiplication without times tables, generalizes to any monoid. The same code computes:</p>
<table>
  <thead>
      <tr>
          <th>Type</th>
          <th>Operation</th>
          <th>Identity</th>
          <th><code>power(x, n)</code> computes&hellip;</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Integer</td>
          <td>x</td>
          <td>1</td>
          <td>\(x^n\)</td>
      </tr>
      <tr>
          <td>2x2 Matrix</td>
          <td>x</td>
          <td>I</td>
          <td>\(F(n)\) (Fibonacci via matrix form)</td>
      </tr>
      <tr>
          <td>Quaternion</td>
          <td>x</td>
          <td>1</td>
          <td>n-fold rotation composition</td>
      </tr>
      <tr>
          <td>Tropical (min,+)</td>
          <td>min, +</td>
          <td>0, inf</td>
          <td>n-hop shortest path</td>
      </tr>
      <tr>
          <td>String</td>
          <td>concat</td>
          <td>&quot;&quot;</td>
          <td>n-fold string repetition</td>
      </tr>
      <tr>
          <td>Permutation</td>
          <td>compose</td>
          <td>id</td>
          <td>n-fold permutation</td>
      </tr>
      <tr>
          <td>Affine transform</td>
          <td>compose</td>
          <td>id</td>
          <td>n periods of compound interest</td>
      </tr>
      <tr>
          <td>Polynomial</td>
          <td>x</td>
          <td>1</td>
          <td>Polynomial powers</td>
      </tr>
      <tr>
          <td>Modular integer</td>
          <td>x</td>
          <td>1</td>
          <td>Modular exponentiation</td>
      </tr>
      <tr>
          <td>Boolean matrix</td>
          <td>and, or</td>
          <td>I</td>
          <td>n-step reachability</td>
      </tr>
  </tbody>
</table>
<p>Fifteen examples in total. Each requires about fifty lines to define the type and its operations. Then <code>power()</code> works unchanged.</p>
<p>The pattern repeats in <a href="/post/2020-07-polynomials-stepanov/">polynomials</a>. The GCD algorithm (Euclid&rsquo;s method from 300 BCE) works identically on integers and polynomials:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="c1">// Same algorithm for both!
</span></span></span><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="n">euclidean_domain</span> <span class="n">E</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="n">E</span> <span class="n">gcd</span><span class="p">(</span><span class="n">E</span> <span class="n">a</span><span class="p">,</span> <span class="n">E</span> <span class="n">b</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">while</span> <span class="p">(</span><span class="n">b</span> <span class="o">!=</span> <span class="n">E</span><span class="p">(</span><span class="mi">0</span><span class="p">))</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">a</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">exchange</span><span class="p">(</span><span class="n">b</span><span class="p">,</span> <span class="n">a</span> <span class="o">%</span> <span class="n">b</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="k">return</span> <span class="n">a</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>For integers: \(\gcd(48, 18) = 6\)</p>
<p>For polynomials: \(\gcd(x^3 - 1, x^2 - 1) = x - 1\)</p>
<p>Both are Euclidean domains, algebraic structures where division with remainder is always possible, with the remainder &ldquo;smaller&rdquo; than the divisor. Integers use absolute value as the size; polynomials use degree. The algorithm doesn&rsquo;t care which. It only needs the Euclidean property.</p>
<hr>
<h2 id="minimal-interfaces">Minimal Interfaces</h2>
<p>Each algorithm requires <em>exactly</em> what it needs. No more, no less.</p>
<p>The peasant algorithms need:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="n">zero</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>       <span class="c1">// additive identity
</span></span></span><span class="line"><span class="cl"><span class="n">one</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>        <span class="c1">// multiplicative identity
</span></span></span><span class="line"><span class="cl"><span class="n">twice</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>      <span class="c1">// doubling
</span></span></span><span class="line"><span class="cl"><span class="n">half</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>       <span class="c1">// halving
</span></span></span><span class="line"><span class="cl"><span class="n">even</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>       <span class="c1">// parity test
</span></span></span></code></pre></div><p>The <a href="/post/2023-08-integration-stepanov/">integration</a> module needs an ordered field:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">T</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">concept</span> <span class="n">ordered_field</span> <span class="o">=</span>
</span></span><span class="line"><span class="cl">    <span class="n">has_add</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span> <span class="o">&amp;&amp;</span> <span class="n">has_sub</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span> <span class="o">&amp;&amp;</span> <span class="n">has_mul</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span> <span class="o">&amp;&amp;</span> <span class="n">has_div</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span> <span class="o">&amp;&amp;</span>
</span></span><span class="line"><span class="cl">    <span class="n">has_neg</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span> <span class="o">&amp;&amp;</span> <span class="n">has_order</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span> <span class="o">&amp;&amp;</span>
</span></span><span class="line"><span class="cl">    <span class="k">requires</span> <span class="p">{</span> <span class="n">T</span><span class="p">(</span><span class="mi">0</span><span class="p">);</span> <span class="n">T</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span> <span class="n">T</span><span class="p">(</span><span class="mi">2</span><span class="p">);</span> <span class="p">};</span>
</span></span></code></pre></div><p>The <a href="/post/2021-03-elementa-stepanov/">elementa</a> matrix library needs:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">M</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">concept</span> <span class="n">Matrix</span> <span class="o">=</span> <span class="k">requires</span><span class="p">(</span><span class="n">M</span> <span class="n">m</span><span class="p">,</span> <span class="k">const</span> <span class="n">M</span> <span class="n">cm</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">j</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">typename</span> <span class="n">M</span><span class="o">::</span><span class="n">scalar_type</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span> <span class="n">cm</span><span class="p">.</span><span class="n">rows</span><span class="p">()</span> <span class="p">}</span> <span class="o">-&gt;</span> <span class="n">std</span><span class="o">::</span><span class="n">same_as</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span> <span class="n">cm</span><span class="p">.</span><span class="n">cols</span><span class="p">()</span> <span class="p">}</span> <span class="o">-&gt;</span> <span class="n">std</span><span class="o">::</span><span class="n">same_as</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span> <span class="n">m</span><span class="p">(</span><span class="n">i</span><span class="p">,</span> <span class="n">j</span><span class="p">)</span> <span class="p">}</span> <span class="o">-&gt;</span> <span class="n">std</span><span class="o">::</span><span class="n">same_as</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">M</span><span class="o">::</span><span class="n">scalar_type</span><span class="o">&amp;&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span> <span class="n">cm</span><span class="p">(</span><span class="n">i</span><span class="p">,</span> <span class="n">j</span><span class="p">)</span> <span class="p">}</span> <span class="o">-&gt;</span> <span class="n">std</span><span class="o">::</span><span class="n">same_as</span><span class="o">&lt;</span><span class="k">const</span> <span class="k">typename</span> <span class="n">M</span><span class="o">::</span><span class="n">scalar_type</span><span class="o">&amp;&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>C++20 concepts make these requirements explicit and checkable. But the deeper point is <em>designing</em> algorithms around minimal requirements. When you ask &ldquo;what does this algorithm actually need?&rdquo;, you discover its true scope.</p>
<p>The <a href="/post/2020-02-rational-stepanov/">rational</a> post illustrates this well. A rational number needs:</p>
<ul>
<li>Numerator and denominator (integers)</li>
<li>GCD (to reduce to lowest terms)</li>
<li>The usual arithmetic</li>
</ul>
<p>But <em>which</em> integer type? The implementation is templated:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">integral</span> <span class="n">T</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">rat</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">T</span> <span class="n">num_</span><span class="p">;</span>  <span class="c1">// numerator (carries sign)
</span></span></span><span class="line"><span class="cl">    <span class="n">T</span> <span class="n">den_</span><span class="p">;</span>  <span class="c1">// denominator (always positive)
</span></span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>Use <code>int</code> for small fractions. Use <code>int64_t</code> for larger ones. Plug in an arbitrary-precision integer type and get exact rational arithmetic with no overflow. The algorithm doesn&rsquo;t change.</p>
<hr>
<h2 id="adl-based-free-functions">ADL-Based Free Functions</h2>
<p>A design pattern runs through every post: types expose algebraic operations as <em>free functions</em>, not member methods.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="c1">// Not this:
</span></span></span><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">my_type</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">my_type</span> <span class="nf">twice</span><span class="p">()</span> <span class="k">const</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">my_type</span> <span class="nf">half</span><span class="p">()</span> <span class="k">const</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="kt">bool</span> <span class="nf">is_even</span><span class="p">()</span> <span class="k">const</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// But this:
</span></span></span><span class="line"><span class="cl"><span class="n">my_type</span> <span class="nf">twice</span><span class="p">(</span><span class="n">my_type</span> <span class="n">x</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="n">my_type</span> <span class="nf">half</span><span class="p">(</span><span class="n">my_type</span> <span class="n">x</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="kt">bool</span> <span class="nf">even</span><span class="p">(</span><span class="n">my_type</span> <span class="n">x</span><span class="p">);</span>
</span></span></code></pre></div><p>Why? Argument-dependent lookup (ADL). When generic code calls <code>twice(x)</code>, the compiler searches for <code>twice</code> in the namespace where <code>x</code>&rsquo;s type is defined. No inheritance required. No base class needed. The type just provides the operations in its namespace.</p>
<p>This pattern enables the full universality. You can&rsquo;t inherit <code>int</code> from a base class, but you can provide <code>twice(int x)</code> as a free function. Suddenly built-in types participate equally with user-defined types.</p>
<p>The <a href="/post/2019-06-modular-stepanov/">modular</a> post shows this clearly:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="kt">int64_t</span> <span class="n">N</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">constexpr</span> <span class="n">mod_int</span><span class="o">&lt;</span><span class="n">N</span><span class="o">&gt;</span> <span class="n">zero</span><span class="p">(</span><span class="n">mod_int</span><span class="o">&lt;</span><span class="n">N</span><span class="o">&gt;</span><span class="p">)</span> <span class="p">{</span> <span class="k">return</span> <span class="n">mod_int</span><span class="o">&lt;</span><span class="n">N</span><span class="o">&gt;</span><span class="p">(</span><span class="mi">0</span><span class="p">);</span> <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="kt">int64_t</span> <span class="n">N</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">constexpr</span> <span class="n">mod_int</span><span class="o">&lt;</span><span class="n">N</span><span class="o">&gt;</span> <span class="n">one</span><span class="p">(</span><span class="n">mod_int</span><span class="o">&lt;</span><span class="n">N</span><span class="o">&gt;</span><span class="p">)</span> <span class="p">{</span> <span class="k">return</span> <span class="n">mod_int</span><span class="o">&lt;</span><span class="n">N</span><span class="o">&gt;</span><span class="p">(</span><span class="mi">1</span><span class="p">);</span> <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="kt">int64_t</span> <span class="n">N</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">constexpr</span> <span class="n">mod_int</span><span class="o">&lt;</span><span class="n">N</span><span class="o">&gt;</span> <span class="n">twice</span><span class="p">(</span><span class="n">mod_int</span><span class="o">&lt;</span><span class="n">N</span><span class="o">&gt;</span> <span class="n">x</span><span class="p">)</span> <span class="p">{</span> <span class="k">return</span> <span class="n">x</span> <span class="o">+</span> <span class="n">x</span><span class="p">;</span> <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="kt">int64_t</span> <span class="n">N</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">constexpr</span> <span class="n">mod_int</span><span class="o">&lt;</span><span class="n">N</span><span class="o">&gt;</span> <span class="n">half</span><span class="p">(</span><span class="n">mod_int</span><span class="o">&lt;</span><span class="n">N</span><span class="o">&gt;</span> <span class="n">x</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">return</span> <span class="n">x</span> <span class="o">*</span> <span class="n">mod_int</span><span class="o">&lt;</span><span class="n">N</span><span class="o">&gt;</span><span class="p">(</span><span class="mi">2</span><span class="p">).</span><span class="n">inverse</span><span class="p">();</span>  <span class="c1">// Requires prime N
</span></span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Now <code>power()</code> works with modular integers. The same twenty lines compute modular exponentiation, the workhorse of public-key cryptography.</p>
<hr>
<h2 id="composition-through-algebra">Composition Through Algebra</h2>
<p>When two algebraic types compose, their algorithms compose too. This is where the approach becomes genuinely powerful.</p>
<h3 id="dual-numbers--integration--leibnizs-rule">Dual Numbers + Integration = Leibniz&rsquo;s Rule</h3>
<p>The <a href="/post/2021-09-dual-stepanov/">dual</a> post introduces dual numbers for automatic differentiation:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="n">dual</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span> <span class="n">x</span> <span class="o">=</span> <span class="n">dual</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;::</span><span class="n">variable</span><span class="p">(</span><span class="mf">2.0</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">f</span> <span class="o">=</span> <span class="n">x</span><span class="o">*</span><span class="n">x</span><span class="o">*</span><span class="n">x</span> <span class="o">-</span> <span class="mf">3.0</span><span class="o">*</span><span class="n">x</span> <span class="o">+</span> <span class="mf">1.0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="c1">// f.value() = 3.0
</span></span></span><span class="line"><span class="cl"><span class="c1">// f.derivative() = 9.0  (the derivative f&#39;(2) = 3x^2 - 3 = 9)
</span></span></span></code></pre></div><p>The derivative emerges automatically from the algebra. No symbolic manipulation. No finite differences. Just arithmetic with a different number type.</p>
<p>The <a href="/post/2023-08-integration-stepanov/">integration</a> module computes numerical integrals:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="kt">double</span> <span class="n">result</span> <span class="o">=</span> <span class="n">integrate</span><span class="p">([](</span><span class="kt">double</span> <span class="n">x</span><span class="p">)</span> <span class="p">{</span> <span class="k">return</span> <span class="n">std</span><span class="o">::</span><span class="n">sin</span><span class="p">(</span><span class="n">x</span><span class="p">);</span> <span class="p">},</span> <span class="mf">0.0</span><span class="p">,</span> <span class="n">M_PI</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="c1">// result ~ 2.0
</span></span></span></code></pre></div><p>Now here&rsquo;s the payoff. Because <code>integrate</code> is written generically (requiring only an ordered field) it works with dual numbers:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">using</span> <span class="n">D</span> <span class="o">=</span> <span class="n">dual</span><span class="o">::</span><span class="n">dual</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">D</span> <span class="n">a</span> <span class="o">=</span> <span class="n">D</span><span class="o">::</span><span class="n">variable</span><span class="p">(</span><span class="mf">1.0</span><span class="p">);</span>  <span class="c1">// Parameter to differentiate
</span></span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">f</span> <span class="o">=</span> <span class="p">[</span><span class="o">&amp;</span><span class="n">a</span><span class="p">](</span><span class="n">D</span> <span class="n">x</span><span class="p">)</span> <span class="p">{</span> <span class="k">return</span> <span class="n">a</span> <span class="o">*</span> <span class="n">x</span> <span class="o">*</span> <span class="n">x</span><span class="p">;</span> <span class="p">};</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">D</span> <span class="n">result</span> <span class="o">=</span> <span class="n">composite_simpsons</span><span class="p">(</span><span class="n">f</span><span class="p">,</span> <span class="n">D</span><span class="o">::</span><span class="n">constant</span><span class="p">(</span><span class="mf">0.0</span><span class="p">),</span> <span class="n">D</span><span class="o">::</span><span class="n">constant</span><span class="p">(</span><span class="mf">1.0</span><span class="p">),</span> <span class="mi">100</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// result.value() = 1/3 (the integral of ax^2 from 0 to 1 = a/3)
</span></span></span><span class="line"><span class="cl"><span class="c1">// result.derivative() = 1/3 (d/da of the integral = integral of x^2 dx)
</span></span></span></code></pre></div><p>This is Leibniz&rsquo;s integral rule (the derivative of an integral with respect to a parameter) implemented through <em>pure algebra</em>. No special cases. No separate implementation. The composition emerges automatically because both components respect the same algebraic structure.</p>
<h3 id="autodiff--elementa--matrix-calculus">Autodiff + Elementa = Matrix Calculus</h3>
<p>The <a href="/post/2023-01-autodiff-stepanov/">autodiff</a> post builds reverse-mode automatic differentiation on top of <a href="/post/2021-03-elementa-stepanov/">elementa</a>&rsquo;s matrix concept:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">f</span> <span class="o">=</span> <span class="p">[](</span><span class="k">const</span> <span class="k">auto</span><span class="o">&amp;</span> <span class="n">A</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">return</span> <span class="nf">sum</span><span class="p">(</span><span class="n">matmul</span><span class="p">(</span><span class="n">A</span><span class="p">,</span> <span class="n">A</span><span class="p">));</span>  <span class="c1">// f(A) = sum(A^2)
</span></span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">matrix</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span> <span class="n">A</span><span class="p">{{</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">},</span> <span class="p">{</span><span class="mi">3</span><span class="p">,</span> <span class="mi">4</span><span class="p">}};</span>
</span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">gradient</span> <span class="o">=</span> <span class="n">grad</span><span class="p">(</span><span class="n">f</span><span class="p">)(</span><span class="n">A</span><span class="p">);</span>  <span class="c1">// df/dA
</span></span></span></code></pre></div><p>The gradient of matrix functions, computed exactly. The chain rule applied through a computational graph. Classic results from matrix calculus (like \(\partial\det(A)/\partial A = \det(A) \cdot A^{-T}\)) appear as backward functions in the graph.</p>
<p>Because autodiff builds on the Matrix concept, not a concrete type, it would work with any conforming matrix implementation: dense, sparse, symbolic, GPU-accelerated. The algorithm is decoupled from the representation.</p>
<h3 id="type-erasure--ring-axioms--runtime-polymorphism-with-guarantees">Type Erasure + Ring Axioms = Runtime Polymorphism with Guarantees</h3>
<p>The <a href="/post/2024-02-type-erasure-stepanov/">type-erasure</a> post takes Sean Parent&rsquo;s technique and applies it to algebraic structures:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">any_ring</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">struct</span> <span class="nc">concept_t</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">virtual</span> <span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">concept_t</span><span class="o">&gt;</span> <span class="n">add</span><span class="p">(</span><span class="n">concept_t</span> <span class="k">const</span><span class="o">&amp;</span><span class="p">)</span> <span class="k">const</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">virtual</span> <span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">concept_t</span><span class="o">&gt;</span> <span class="n">multiply</span><span class="p">(</span><span class="n">concept_t</span> <span class="k">const</span><span class="o">&amp;</span><span class="p">)</span> <span class="k">const</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">virtual</span> <span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">concept_t</span><span class="o">&gt;</span> <span class="n">negate</span><span class="p">()</span> <span class="k">const</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">virtual</span> <span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">concept_t</span><span class="o">&gt;</span> <span class="n">zero</span><span class="p">()</span> <span class="k">const</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">virtual</span> <span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">concept_t</span><span class="o">&gt;</span> <span class="n">one</span><span class="p">()</span> <span class="k">const</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="c1">// ...
</span></span></span><span class="line"><span class="cl">    <span class="p">};</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>Now <code>any_ring</code> can hold integers, polynomials, matrices, any ring. You can do arithmetic at runtime without knowing the concrete type at compile time. The algebraic laws still hold because every wrapped type satisfies them.</p>
<p>This is Stepanov&rsquo;s approach extended to runtime polymorphism: the interface is algebraic (ring operations), not arbitrary methods.</p>
<hr>
<h2 id="correctness-from-structure">Correctness from Structure</h2>
<p>When the algebra is right, the algorithm cannot be wrong. Not &ldquo;unlikely to be wrong,&rdquo; <em>cannot</em>.</p>
<h3 id="miller-rabin-error-bounds-from-group-theory">Miller-Rabin: Error Bounds from Group Theory</h3>
<p>The <a href="/post/2019-09-miller-rabin-stepanov/">miller-rabin</a> post implements probabilistic primality testing. The algorithm has a clean error analysis: for any composite n, at least 3/4 of potential witnesses will detect it.</p>
<p>Where does 3/4 come from? Not from empirical testing. From the structure of multiplicative groups modulo n. The witnesses that fail to detect a composite form a subgroup; subgroups divide the group order; the analysis bounds the subgroup size.</p>
$$
P(\text{false positive}) \leq \left(\frac{1}{4}\right)^k
$$<p>With \(k = 20\) witnesses: error \(< 10^{-12}\). Not &ldquo;probably correct,&rdquo; provably bounded error, derived from algebra.</p>
<h3 id="rationals-canonical-form-as-theorem">Rationals: Canonical Form as Theorem</h3>
<p>The <a href="/post/2020-02-rational-stepanov/">rational</a> post maintains fractions in lowest terms:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="kt">void</span> <span class="nf">reduce</span><span class="p">()</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">T</span> <span class="n">g</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">gcd</span><span class="p">(</span><span class="n">abs</span><span class="p">(</span><span class="n">num_</span><span class="p">),</span> <span class="n">den_</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">num_</span> <span class="o">/=</span> <span class="n">g</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">den_</span> <span class="o">/=</span> <span class="n">g</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Every rational has a unique reduced representation. This isn&rsquo;t a policy decision. It&rsquo;s a theorem about greatest common divisors. The canonical form follows from the algebraic structure of integers (a unique factorization domain) and the definition of GCD.</p>
<p>Because of this, equality testing is trivial:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="kt">bool</span> <span class="k">operator</span><span class="o">==</span><span class="p">(</span><span class="n">rat</span> <span class="k">const</span><span class="o">&amp;</span> <span class="n">rhs</span><span class="p">)</span> <span class="k">const</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">return</span> <span class="n">num_</span> <span class="o">==</span> <span class="n">rhs</span><span class="p">.</span><span class="n">num_</span> <span class="o">&amp;&amp;</span> <span class="n">den_</span> <span class="o">==</span> <span class="n">rhs</span><span class="p">.</span><span class="n">den_</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>No tolerance. No floating-point fuzziness. Exact equality, guaranteed by algebraic structure.</p>
<h3 id="dual-numbers-correctness-by-construction">Dual Numbers: Correctness by Construction</h3>
<p>The <a href="/post/2021-09-dual-stepanov/">dual</a> post computes derivatives through the algebra of dual numbers, where \(\varepsilon^2 = 0\):</p>
$$
f(x + \varepsilon) = f(x) + \varepsilon f'(x)
$$<p>This isn&rsquo;t an approximation. It&rsquo;s exact. The derivative emerges from the definition of dual number arithmetic:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="n">dual</span> <span class="k">operator</span><span class="o">*</span><span class="p">(</span><span class="n">dual</span> <span class="k">const</span><span class="o">&amp;</span> <span class="n">other</span><span class="p">)</span> <span class="k">const</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// (a + be)(c + de) = ac + (ad + bc)e + bde^2 = ac + (ad + bc)e
</span></span></span><span class="line"><span class="cl">    <span class="k">return</span> <span class="p">{</span><span class="n">val</span> <span class="o">*</span> <span class="n">other</span><span class="p">.</span><span class="n">val</span><span class="p">,</span> <span class="n">val</span> <span class="o">*</span> <span class="n">other</span><span class="p">.</span><span class="n">der</span> <span class="o">+</span> <span class="n">der</span> <span class="o">*</span> <span class="n">other</span><span class="p">.</span><span class="n">val</span><span class="p">};</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>The product rule is <em>built into</em> the multiplication. If the algebra is implemented correctly, derivatives are correct by construction.</p>
<hr>
<h2 id="the-meta-pattern-how-to-see-this-way">The Meta-Pattern: How to See This Way</h2>
<p>After reading these eleven posts, patterns emerge. Here&rsquo;s how to develop this perspective:</p>
<p><strong>Ask: What operations does this algorithm actually need?</strong></p>
<p>Not &ldquo;what type am I working with?&rdquo; but &ldquo;what can I do with it?&rdquo; List the operations. Check which ones you actually call. That&rsquo;s your concept.</p>
<p><strong>Ask: What laws must hold?</strong></p>
<p>Associativity? Commutativity? Distributivity? An identity element? Inverses? These laws determine which algorithms apply. If you need \(a \cdot (b \cdot c) = (a \cdot b) \cdot c\), you need a semigroup. Add an identity, you have a monoid. Add inverses, a group.</p>
<table>
  <thead>
      <tr>
          <th>Structure</th>
          <th>Laws</th>
          <th>Enables</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Semigroup</td>
          <td>Associativity</td>
          <td>Parallel reduction</td>
      </tr>
      <tr>
          <td>Monoid</td>
          <td>+ Identity</td>
          <td>Power by squaring</td>
      </tr>
      <tr>
          <td>Group</td>
          <td>+ Inverses</td>
          <td>Division, subtraction</td>
      </tr>
      <tr>
          <td>Ring</td>
          <td>Monoid (x), Group (+), Distributive</td>
          <td>Polynomial arithmetic</td>
      </tr>
      <tr>
          <td>Field</td>
          <td>Ring + multiplicative inverses</td>
          <td>Division, linear algebra</td>
      </tr>
      <tr>
          <td>Euclidean domain</td>
          <td>Ring + division with remainder</td>
          <td>GCD, fraction reduction</td>
      </tr>
  </tbody>
</table>
<p><strong>Design the concept as the minimal contract.</strong></p>
<p>Don&rsquo;t require what you don&rsquo;t need. Each additional requirement excludes types that might work. The goal is maximum generality with guaranteed correctness.</p>
<p><strong>Express operations as free functions.</strong></p>
<p>This enables ADL, which enables working with types you don&rsquo;t control. Built-in types, third-party libraries, legacy code, all can participate.</p>
<p><strong>Let composition emerge from shared structure.</strong></p>
<p>When component A requires concept X, and component B produces concept X, they compose. No explicit integration needed. The algebra mediates.</p>
<p><strong>Turn laws into tests.</strong></p>
<p>If you know something is a monoid, you can automatically generate property-based tests. Associativity becomes <code>assert(op(op(a,b),c) == op(a,op(b,c)))</code> for random a, b, c. Identity becomes <code>assert(op(e,x) == x &amp;&amp; op(x,e) == x)</code> for random x.</p>
<p>This isn&rsquo;t new: Claessen and Hughes&rsquo;s QuickCheck (2000) pioneered property-based testing; Wadler&rsquo;s &ldquo;Theorems for Free&rdquo; (1989) showed that parametric polymorphism implies algebraic properties. But the connection to Stepanov-style programming is direct: declaring structure gives you tests for free. The laws become executable specifications.</p>
<p><strong>The open question: discovering structure.</strong></p>
<p>This collection teaches you to <em>use</em> structures once recognized, but how do you recognize them in the first place? That creative act remains underexplored.</p>
<p>Sometimes the process is clear: you have an algorithm, you analyze what operations and laws it requires, and you name the resulting concept. But sometimes you have a messy domain and wonder: is there hidden structure here? Could I reformulate this problem so a known structure applies?</p>
<p>Pattern-matching against known structures helps. Working backward from desired algorithms helps. But no systematic method exists for <em>discovering</em> new useful structures. That gap is worth acknowledging, and perhaps worth exploring.</p>
<hr>
<h2 id="the-posts-a-dependency-graph">The Posts: A Dependency Graph</h2>
<p>The eleven posts build on each other in a directed acyclic graph:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">peasant (monoids, power by squaring)
</span></span><span class="line"><span class="cl">+-- miller-rabin (modular exponentiation for primality)
</span></span><span class="line"><span class="cl">+-- modular (ring/field structure, Fermat&#39;s theorem)
</span></span><span class="line"><span class="cl">+-- type-erasure (algebraic concepts at runtime)
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">rational (fields, GCD, unique factorization)
</span></span><span class="line"><span class="cl">+-- polynomials (Euclidean domains, same GCD algorithm)
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">dual (forward-mode autodiff via algebra)
</span></span><span class="line"><span class="cl">+-- finite-diff (verification, error analysis comparison)
</span></span><span class="line"><span class="cl">+-- integration (Leibniz&#39;s rule through composition)
</span></span><span class="line"><span class="cl">+-- autodiff (reverse-mode for scalar outputs)
</span></span><span class="line"><span class="cl">    +-- elementa (Matrix concept foundation)
</span></span></code></pre></div><p><strong>Entry points by interest:</strong></p>
<ul>
<li><em>Number theory</em>: Start with <a href="/post/2019-03-peasant-stepanov/">peasant</a>, then <a href="/post/2019-06-modular-stepanov/">modular</a>, then <a href="/post/2019-09-miller-rabin-stepanov/">miller-rabin</a></li>
<li><em>Calculus and optimization</em>: Start with <a href="/post/2021-09-dual-stepanov/">dual</a>, then <a href="/post/2022-04-finite-diff-stepanov/">finite-diff</a>, then <a href="/post/2023-08-integration-stepanov/">integration</a></li>
<li><em>Linear algebra</em>: Start with <a href="/post/2021-03-elementa-stepanov/">elementa</a>, then <a href="/post/2023-01-autodiff-stepanov/">autodiff</a></li>
<li><em>Generic programming patterns</em>: Start with <a href="/post/2019-03-peasant-stepanov/">peasant</a>, then <a href="/post/2024-02-type-erasure-stepanov/">type-erasure</a></li>
<li><em>Algebraic structures</em>: Start with <a href="/post/2020-02-rational-stepanov/">rational</a>, then <a href="/post/2020-07-polynomials-stepanov/">polynomials</a></li>
</ul>
<hr>
<h2 id="appendix-reading-guide">Appendix: Reading Guide</h2>
<h3 id="peasant">peasant/</h3>
<p><strong>One Algorithm, Infinite Powers</strong></p>
<p>The Russian peasant multiplication algorithm generalizes to any monoid. Computes \(x^n\) in \(O(\log n)\) operations using only associativity and identity. Fifteen worked examples: integers, matrices (Fibonacci), quaternions (rotations), tropical semiring (shortest paths), strings, permutations, affine transforms (compound interest), and more.</p>
<p><em>Key insight</em>: The same code structure appears everywhere because it depends only on monoid axioms, not the specific domain.</p>
<h3 id="miller-rabin">miller-rabin/</h3>
<p><strong>Is It Prime?</strong></p>
<p>Probabilistic primality testing with quantifiable error bounds. Uses modular exponentiation from peasant. The 3/4 witness bound comes from group theory. Handles Carmichael numbers that fool simpler tests.</p>
<p><em>Key insight</em>: Error analysis follows from algebraic structure, not empirical observation.</p>
<h3 id="rational">rational/</h3>
<p><strong>Exact Rational Arithmetic</strong></p>
<p>Fractions that never lose precision. GCD reduction ensures unique representation. Connects to continued fractions and the Stern-Brocot tree.</p>
<p><em>Key insight</em>: Canonical form is a theorem about algebraic structure, not a design choice.</p>
<h3 id="modular">modular/</h3>
<p><strong>Modular Arithmetic as Rings</strong></p>
<p>Integers mod N form rings; when N is prime, they form fields. Fermat&rsquo;s little theorem gives multiplicative inverses. Type-safe: <code>mod_int&lt;7&gt;</code> and <code>mod_int&lt;11&gt;</code> are incompatible types.</p>
<p><em>Key insight</em>: Understanding ring/field structure tells you which algorithms apply.</p>
<h3 id="polynomials">polynomials/</h3>
<p><strong>Polynomial Arithmetic as Euclidean Domains</strong></p>
<p>The same GCD algorithm works for integers and polynomials because both are Euclidean domains. Sparse representation handles high-degree polynomials efficiently.</p>
<p><em>Key insight</em>: Shared algebraic structure enables algorithm reuse across apparently different domains.</p>
<h3 id="type-erasure">type-erasure/</h3>
<p><strong>Runtime Polymorphism Without Inheritance</strong></p>
<p>Sean Parent&rsquo;s technique: wrap any type satisfying an interface, without requiring inheritance. Extended to algebraic interfaces: <code>any_ring</code>, <code>any_vector_space</code>, <code>any_matrix</code>.</p>
<p><em>Key insight</em>: Algebraic concepts can be erased at runtime while preserving guarantees.</p>
<h3 id="dual">dual/</h3>
<p><strong>Forward-Mode Automatic Differentiation</strong></p>
<p>Dual numbers (\(a + b\varepsilon\) where \(\varepsilon^2 = 0\)) compute derivatives through arithmetic. Evaluate \(f(x + \varepsilon)\) to get \(f(x) + \varepsilon f'(x)\). Supports higher-order derivatives via nesting.</p>
<p><em>Key insight</em>: The derivative emerges automatically from the algebra of dual numbers.</p>
<h3 id="finite-diff">finite-diff/</h3>
<p><strong>Numerical Differentiation</strong></p>
<p>Finite difference formulas with optimal step sizes derived from error analysis. Forward, central, and five-point stencils. Useful for verification and black-box functions.</p>
<p><em>Key insight</em>: Two error sources (truncation and round-off) determine optimal step size.</p>
<h3 id="integration">integration/</h3>
<p><strong>Numerical Quadrature</strong></p>
<p>Simpson&rsquo;s rule, Gauss-Legendre, adaptive methods. Written generically for any ordered field, enabling composition with dual numbers (Leibniz&rsquo;s rule).</p>
<p><em>Key insight</em>: Generic implementation enables unexpected compositions.</p>
<h3 id="elementa">elementa/</h3>
<p><strong>Pedagogical Linear Algebra</strong></p>
<p>Matrix concept in C++20. LU decomposition powers determinants, inverses, and linear system solving. Cache-conscious multiplication. Value semantics throughout.</p>
<p><em>Key insight</em>: A concept defines what matrices <em>are</em>, enabling algorithms independent of representation.</p>
<h3 id="autodiff">autodiff/</h3>
<p><strong>Reverse-Mode Automatic Differentiation</strong></p>
<p>Gradient computation via computational graphs. Builds on elementa&rsquo;s Matrix concept. Implements matrix calculus Jacobians (determinant, inverse, multiplication). Verified against finite differences.</p>
<p><em>Key insight</em>: Systematic chain rule application, automated through graph traversal.</p>
<hr>
<h2 id="where-the-principle-struggles">Where the Principle Struggles</h2>
<p>No principle applies universally. Here&rsquo;s where &ldquo;structure first, algorithm second&rdquo; meets friction:</p>
<p><strong>Generic overhead.</strong> Sometimes a specialized algorithm beats the generic one. SIMD-optimized matrix multiplication exploits specific hardware layouts in ways a generic <code>Matrix</code> concept can&rsquo;t express. The structure is there, but capturing it costs more than hard-coding it.</p>
<p><strong>Approximate fit.</strong> Floating-point numbers <em>almost</em> form a field, but not quite. Addition isn&rsquo;t associative: \((a + b) + c \neq a + (b + c)\) in general, due to rounding. Algorithms that assume exact associativity may accumulate errors or produce inconsistent results. The structure is a useful fiction, not a guarantee.</p>
<p><strong>Missing concepts.</strong> Some useful algorithms lack clean algebraic characterization. What structure does &ldquo;topological sort&rdquo; require? &ldquo;Strongly connected components&rdquo;? These algorithms have preconditions (acyclic graph, directed graph), but they don&rsquo;t fit neatly into the algebraic hierarchy.</p>
<p><strong>Impedance mismatch.</strong> Stateful, imperative code (caches, mutation, side effects) doesn&rsquo;t always fit the algebraic mold. You <em>can</em> model state transformations as monoid actions, but the encoding may obscure rather than clarify.</p>
<p>This isn&rsquo;t failure. It&rsquo;s knowing when the tool applies. The principle illuminates a wide territory; recognizing its boundaries makes you more effective within them.</p>
<hr>
<h2 id="an-invitation">An Invitation</h2>
<p>These eleven posts are entry points, not destinations. The principle, <em>structure first, algorithm second</em>, extends far beyond the domains explored here.</p>
<p>Consider data structures: a balanced binary search tree, a skip list, and a B-tree all implement the same abstract interface (ordered set). Algorithms written to that interface work on all three.</p>
<p>Consider I/O: streams, files, network sockets, and string buffers can satisfy the same concept. Generic code handles them uniformly.</p>
<p>Consider concurrency: different synchronization primitives satisfy different algebraic laws. Understanding those laws clarifies which patterns are safe.</p>
<p>The principle is simple. Seeing it takes practice. These posts offer exercises.</p>
<p>Write the algorithm once. State its requirements precisely. Let it work on anything that satisfies those requirements. That&rsquo;s generic programming in the Stepanov tradition.</p>
<p>When you recognize &ldquo;this is a ring,&rdquo; you immediately know which algorithms apply. When you recognize &ldquo;this is a monoid,&rdquo; <code>power()</code> is available. When you see structure first, algorithms follow.</p>
<hr>
<h2 id="further-reading">Further Reading</h2>
<ul>
<li>Stepanov &amp; Rose, <em>From Mathematics to Generic Programming</em> &ndash; accessible introduction</li>
<li>Stepanov &amp; McJones, <em>Elements of Programming</em> &ndash; the definitive treatment</li>
<li>Sean Parent, &ldquo;Inheritance Is The Base Class of Evil&rdquo; (GoingNative 2013) &ndash; type erasure</li>
<li>Ireland &amp; Rosen, <em>A Classical Introduction to Modern Number Theory</em> &ndash; modular arithmetic</li>
<li>Graham, Knuth, Patashnik, <em>Concrete Mathematics</em> &ndash; rational numbers, recurrences</li>
</ul>
]]></content:encoded>
      
      
      <category>C&#43;&#43;</category>
      
      <category>generic-programming</category>
      
      <category>algorithms</category>
      
      <category>monoids</category>
      
      <category>algebraic-structures</category>
      
      <category>stepanov</category>
      
      
    </item>
    
  </channel>
</rss>
