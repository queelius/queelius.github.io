<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:media="http://search.yahoo.com/mrss/"><channel><title>Software-Design on metafunctor</title><link>https://metafunctor.com/categories/software-design/</link><description>Research Engineer &amp; Computer Scientist - Machine Learning, Statistical Computing, Open Source Development</description><language>en-us</language><managingEditor>lex@metafunctor.com (Alex Towell)</managingEditor><webMaster>lex@metafunctor.com (Alex Towell)</webMaster><lastBuildDate>Tue, 24 Feb 2026 14:47:39 -0600</lastBuildDate><atom:link href="https://metafunctor.com/categories/software-design/" rel="self" type="application/rss+xml"/><image><url>https://metafunctor.com/images/og-image.jpg</url><title>metafunctor</title><link>https://metafunctor.com/</link></image><item><title>CTK: A Toolkit for Managing AI Conversations Across Platforms</title><link>https://metafunctor.com/post/2025-10-ctk/</link><pubDate>Thu, 09 Oct 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2025-10-ctk/</guid><description>A plugin-based system for importing, storing, searching, and exporting AI conversations from multiple providers in a unified tree format. Part of the Long Echo project.</description><content:encoded><![CDATA[<p><strong><a href="https://github.com/queelius/ctk">CTK</a></strong> is a tool I built to solve a specific annoyance: my AI conversations are scattered across ChatGPT, Claude, Copilot, and various local LLMs. They&rsquo;re unsearchable across platforms, at risk of disappearing if a provider changes their export format, and structurally incompatible with each other.</p>
<p>CTK imports from all of these into a single SQLite database with a unified tree representation, then lets you search, organize, and export them.</p>
<h2 id="the-tree-representation">The tree representation</h2>
<p>The key insight: all conversations are trees. Linear chats are single-path trees. Branching conversations (like ChatGPT&rsquo;s &ldquo;regenerate&rdquo; feature) are multi-path trees:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">User: &#34;Write a poem&#34;
</span></span><span class="line"><span class="cl">  |-- Assistant (v1): &#34;Roses are red...&#34;
</span></span><span class="line"><span class="cl">  +-- Assistant (v2): &#34;In fields of gold...&#34;  [regenerated]
</span></span><span class="line"><span class="cl">      +-- User: &#34;Make it longer&#34;
</span></span><span class="line"><span class="cl">          +-- Assistant: &#34;In fields of gold, where sunshine...&#34;
</span></span></code></pre></div><p>This preserves all branching structure from any provider while giving you a uniform interface for search, export, and analysis.</p>
<h2 id="getting-started">Getting started</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">git clone https://github.com/queelius/ctk.git
</span></span><span class="line"><span class="cl"><span class="nb">cd</span> ctk
</span></span><span class="line"><span class="cl">make setup
</span></span><span class="line"><span class="cl"><span class="nb">source</span> .venv/bin/activate
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Import from multiple providers</span>
</span></span><span class="line"><span class="cl">ctk import chatgpt_export.json --db my_chats.db
</span></span><span class="line"><span class="cl">ctk import claude_export.json --db my_chats.db --format anthropic
</span></span><span class="line"><span class="cl">ctk import ~/.vscode/workspaceStorage --db my_chats.db --format copilot
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Search across everything</span>
</span></span><span class="line"><span class="cl">ctk search <span class="s2">&#34;python async&#34;</span> --db my_chats.db --limit <span class="m">10</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Interactive TUI</span>
</span></span><span class="line"><span class="cl">ctk chat --db my_chats.db
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Export for fine-tuning</span>
</span></span><span class="line"><span class="cl">ctk <span class="nb">export</span> training.jsonl --db my_chats.db --format jsonl
</span></span></code></pre></div><h2 id="plugin-architecture">Plugin architecture</h2>
<p>Adding support for a new provider is straightforward. CTK auto-discovers importers and exporters:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="c1"># File: ctk/integrations/importers/my_format.py</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">ctk.core.plugin</span> <span class="kn">import</span> <span class="n">ImporterPlugin</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">ctk.core.models</span> <span class="kn">import</span> <span class="n">ConversationTree</span><span class="p">,</span> <span class="n">Message</span><span class="p">,</span> <span class="n">MessageContent</span><span class="p">,</span> <span class="n">MessageRole</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">MyFormatImporter</span><span class="p">(</span><span class="n">ImporterPlugin</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">    <span class="n">name</span> <span class="o">=</span> <span class="s2">&#34;my_format&#34;</span>
</span></span><span class="line"><span class="cl">    <span class="n">description</span> <span class="o">=</span> <span class="s2">&#34;Import from My Custom Format&#34;</span>
</span></span><span class="line"><span class="cl">    <span class="n">version</span> <span class="o">=</span> <span class="s2">&#34;1.0.0&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="nf">validate</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">data</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="s2">&#34;&#34;&#34;Check if data is your format&#34;&#34;&#34;</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="s2">&#34;my_format_marker&#34;</span> <span class="ow">in</span> <span class="nb">str</span><span class="p">(</span><span class="n">data</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="nf">import_data</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">data</span><span class="p">,</span> <span class="o">**</span><span class="n">kwargs</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="s2">&#34;&#34;&#34;Convert data to ConversationTree objects&#34;&#34;&#34;</span>
</span></span><span class="line"><span class="cl">        <span class="n">tree</span> <span class="o">=</span> <span class="n">ConversationTree</span><span class="p">(</span><span class="nb">id</span><span class="o">=</span><span class="s2">&#34;conv_1&#34;</span><span class="p">,</span> <span class="n">title</span><span class="o">=</span><span class="s2">&#34;Imported&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="n">msg</span> <span class="o">=</span> <span class="n">Message</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">            <span class="n">role</span><span class="o">=</span><span class="n">MessageRole</span><span class="o">.</span><span class="n">USER</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">            <span class="n">content</span><span class="o">=</span><span class="n">MessageContent</span><span class="p">(</span><span class="n">text</span><span class="o">=</span><span class="s2">&#34;Hello&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="n">tree</span><span class="o">.</span><span class="n">add_message</span><span class="p">(</span><span class="n">msg</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="p">[</span><span class="n">tree</span><span class="p">]</span>
</span></span></code></pre></div><p>Drop the file in the integrations folder and it&rsquo;s discovered at runtime.</p>
<h2 id="supported-providers">Supported providers</h2>
<h3 id="importers">Importers</h3>
<table>
  <thead>
      <tr>
          <th>Provider</th>
          <th>Format</th>
          <th>Branch Support</th>
          <th>Notes</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>OpenAI (ChatGPT)</strong></td>
          <td><code>openai</code></td>
          <td>Full tree</td>
          <td>Preserves all regenerations</td>
      </tr>
      <tr>
          <td><strong>Anthropic (Claude)</strong></td>
          <td><code>anthropic</code></td>
          <td>Full tree</td>
          <td>Supports conversation forking</td>
      </tr>
      <tr>
          <td><strong>GitHub Copilot</strong></td>
          <td><code>copilot</code></td>
          <td>Linear</td>
          <td>Auto-finds VS Code storage</td>
      </tr>
      <tr>
          <td><strong>Google Gemini</strong></td>
          <td><code>gemini</code></td>
          <td>Partial</td>
          <td>Bard conversations</td>
      </tr>
      <tr>
          <td><strong>Generic JSONL</strong></td>
          <td><code>jsonl</code></td>
          <td>Linear</td>
          <td>For local LLMs (Ollama, LM Studio)</td>
      </tr>
      <tr>
          <td><strong>Coding Agents</strong></td>
          <td><code>coding_agent</code></td>
          <td>Linear</td>
          <td>Cursor, Windsurf, etc.</td>
      </tr>
  </tbody>
</table>
<h3 id="exporters">Exporters</h3>
<table>
  <thead>
      <tr>
          <th>Format</th>
          <th>Use Case</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>JSONL</strong></td>
          <td>Fine-tuning datasets</td>
      </tr>
      <tr>
          <td><strong>JSON</strong></td>
          <td>Backup, transfer between databases</td>
      </tr>
      <tr>
          <td><strong>Markdown</strong></td>
          <td>Documentation, sharing</td>
      </tr>
      <tr>
          <td><strong>HTML5</strong></td>
          <td>Interactive browsing with search (works offline)</td>
      </tr>
  </tbody>
</table>
<h2 id="search-and-discovery">Search and discovery</h2>
<p>Full-text search across all providers with SQLite FTS:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ctk search <span class="s2">&#34;machine learning&#34;</span> --db chats.db
</span></span><span class="line"><span class="cl">ctk search <span class="s2">&#34;python&#34;</span> --db chats.db --source ChatGPT --model GPT-4
</span></span><span class="line"><span class="cl">ctk search <span class="s2">&#34;async&#34;</span> --db chats.db --tags <span class="s2">&#34;code,tutorial&#34;</span> --limit <span class="m">20</span>
</span></span><span class="line"><span class="cl">ctk search <span class="s2">&#34;AI&#34;</span> --db chats.db --date-from 2024-01-01 --date-to 2024-12-31
</span></span></code></pre></div><p>There&rsquo;s also a <code>say</code> command that uses LLM tool calling for natural language queries:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ctk say <span class="s2">&#34;show me starred conversations&#34;</span> --db chats.db
</span></span><span class="line"><span class="cl">ctk say <span class="s2">&#34;find discussions about async python&#34;</span> --db chats.db
</span></span><span class="line"><span class="cl">ctk say <span class="s2">&#34;star the last conversation about machine learning&#34;</span> --db chats.db
</span></span></code></pre></div><p>The LLM interprets your command and calls the appropriate CTK functions. This goes beyond queries: you can star, tag, and export through natural language.</p>
<h2 id="the-interactive-tui">The interactive TUI</h2>
<p>The <code>chat</code> command gives you a conversational interface over your entire conversation database:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ctk chat --db chats.db
</span></span></code></pre></div><p>You can ask questions about your history (&ldquo;What did I discuss with Claude about decorators last month?&rdquo;), get summaries, find patterns, and perform operations. The LLM has access to the full database through tool calling.</p>
<p>The TUI also provides visual management: browsing with Rich tables, tree views for branching conversations, path navigation, star/pin/archive operations, live chat with any LLM provider, MCP tool support, forking, and message editing.</p>
<h3 id="tui-commands">TUI commands</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">/browse              <span class="c1"># Browse conversations table</span>
</span></span><span class="line"><span class="cl">/show &lt;id&gt;           <span class="c1"># Show conversation</span>
</span></span><span class="line"><span class="cl">/tree &lt;id&gt;           <span class="c1"># View tree structure</span>
</span></span><span class="line"><span class="cl">/search &lt;query&gt;      <span class="c1"># Full-text search</span>
</span></span><span class="line"><span class="cl">/say &lt;command&gt;       <span class="c1"># Natural language commands</span>
</span></span><span class="line"><span class="cl">/star &lt;id&gt;           <span class="c1"># Star conversation</span>
</span></span><span class="line"><span class="cl">/pin &lt;id&gt;            <span class="c1"># Pin conversation</span>
</span></span><span class="line"><span class="cl">/archive &lt;id&gt;        <span class="c1"># Archive conversation</span>
</span></span><span class="line"><span class="cl">/fork                <span class="c1"># Fork current conversation</span>
</span></span><span class="line"><span class="cl">/regenerate          <span class="c1"># Regenerate last message</span>
</span></span><span class="line"><span class="cl">/export &lt;format&gt;     <span class="c1"># Export current conversation</span>
</span></span><span class="line"><span class="cl">/model &lt;name&gt;        <span class="c1"># Switch LLM model</span>
</span></span></code></pre></div><h2 id="privacy">Privacy</h2>
<p>Everything is local. No telemetry, no cloud dependencies. There&rsquo;s optional secret masking for when you want to share exports:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ctk <span class="nb">export</span> clean_export.jsonl --db chats.db --format jsonl --sanitize
</span></span></code></pre></div><p>This strips API keys, passwords, tokens, SSH keys, database URLs, and similar patterns. You can add custom sanitization rules for company-specific patterns.</p>
<h2 id="path-selection-for-branching-conversations">Path selection for branching conversations</h2>
<p>When exporting, you choose which path to include from branching conversations:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Longest path (most comprehensive)</span>
</span></span><span class="line"><span class="cl">ctk <span class="nb">export</span> out.jsonl --db chats.db --path-selection longest
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># First path (original)</span>
</span></span><span class="line"><span class="cl">ctk <span class="nb">export</span> out.jsonl --db chats.db --path-selection first
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Most recent path (latest regeneration)</span>
</span></span><span class="line"><span class="cl">ctk <span class="nb">export</span> out.jsonl --db chats.db --path-selection last
</span></span></code></pre></div><p>This matters for fine-tuning datasets. ChatGPT conversations often have multiple regenerated responses, and you want to pick the right variant.</p>
<h2 id="database-operations">Database operations</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Merge databases</span>
</span></span><span class="line"><span class="cl">ctk merge source1.db source2.db --output merged.db
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Compare databases</span>
</span></span><span class="line"><span class="cl">ctk diff db1.db db2.db
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Create filtered database</span>
</span></span><span class="line"><span class="cl">ctk filter --db all_chats.db --output work_chats.db --tags <span class="s2">&#34;work&#34;</span>
</span></span><span class="line"><span class="cl">ctk filter --db all_chats.db --output starred.db --starred
</span></span></code></pre></div><h2 id="python-api">Python API</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">ctk</span> <span class="kn">import</span> <span class="n">ConversationDB</span><span class="p">,</span> <span class="n">registry</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">with</span> <span class="n">ConversationDB</span><span class="p">(</span><span class="s2">&#34;chats.db&#34;</span><span class="p">)</span> <span class="k">as</span> <span class="n">db</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">    <span class="n">results</span> <span class="o">=</span> <span class="n">db</span><span class="o">.</span><span class="n">search_conversations</span><span class="p">(</span><span class="s2">&#34;python async&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="n">conv</span> <span class="o">=</span> <span class="n">db</span><span class="o">.</span><span class="n">load_conversation</span><span class="p">(</span><span class="s2">&#34;conv_id_123&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="n">paths</span> <span class="o">=</span> <span class="n">conv</span><span class="o">.</span><span class="n">get_all_paths</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">    <span class="n">longest</span> <span class="o">=</span> <span class="n">conv</span><span class="o">.</span><span class="n">get_longest_path</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kn">from</span> <span class="nn">ctk</span> <span class="kn">import</span> <span class="n">Message</span><span class="p">,</span> <span class="n">MessageContent</span><span class="p">,</span> <span class="n">MessageRole</span>
</span></span><span class="line"><span class="cl">    <span class="n">msg</span> <span class="o">=</span> <span class="n">Message</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">        <span class="n">role</span><span class="o">=</span><span class="n">MessageRole</span><span class="o">.</span><span class="n">USER</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">        <span class="n">content</span><span class="o">=</span><span class="n">MessageContent</span><span class="p">(</span><span class="n">text</span><span class="o">=</span><span class="s2">&#34;New question&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="n">conv</span><span class="o">.</span><span class="n">add_message</span><span class="p">(</span><span class="n">msg</span><span class="p">,</span> <span class="n">parent_id</span><span class="o">=</span><span class="s2">&#34;previous_msg_id&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="n">db</span><span class="o">.</span><span class="n">save_conversation</span><span class="p">(</span><span class="n">conv</span><span class="p">)</span>
</span></span></code></pre></div><h2 id="the-long-echo-connection">The Long Echo connection</h2>
<p>CTK was built for the <a href="/post/2025-01-long-echo/">Long Echo</a> project, which is about preserving digital artifacts for the long term. The export formats are chosen for durability:</p>
<ul>
<li><strong>HTML5</strong>: self-contained, works in any browser offline</li>
<li><strong>Markdown</strong>: plain text with formatting, readable anywhere</li>
<li><strong>JSON</strong>: structured, easy to parse decades later</li>
</ul>
<p>The physical backup strategy includes USB drives, optical media, multiple cloud providers, and local NAS systems. The goal is that these conversations remain accessible regardless of what happens to any particular platform.</p>
<h2 id="mcp-integration">MCP integration</h2>
<p>CTK supports Model Context Protocol for tool calling during live chat:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ctk chat --db chats.db --mcp-config mcp.json
</span></span></code></pre></div><p>The LLM can call external tools (file system operations, web search, database queries, custom functions) during the conversation.</p>
<h2 id="development">Development</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">make <span class="nb">test</span>              <span class="c1"># Run tests</span>
</span></span><span class="line"><span class="cl">make test-unit         <span class="c1"># Unit tests only</span>
</span></span><span class="line"><span class="cl">make test-integration  <span class="c1"># Integration tests</span>
</span></span><span class="line"><span class="cl">make coverage          <span class="c1"># Coverage report</span>
</span></span><span class="line"><span class="cl">make format            <span class="c1"># black + isort</span>
</span></span><span class="line"><span class="cl">make lint              <span class="c1"># flake8 + mypy</span>
</span></span></code></pre></div><h2 id="status">Status</h2>
<p><strong>Done</strong>: TUI, Rich output, natural language commands via <code>say</code>, star/pin/archive, multiple export formats, MCP tool integration, auto-tagging, database merge/diff.</p>
<p><strong>In progress</strong>: embeddings and similarity search, test coverage, performance for large databases.</p>
<p><strong>Planned</strong>: web UI, conversation deduplication, LangChain/LlamaIndex integration, analytics dashboard.</p>
<h2 id="links">Links</h2>
<ul>
<li><strong>Repository</strong>: <a href="https://github.com/queelius/ctk">github.com/queelius/ctk</a></li>
<li><strong>Long Echo</strong>: <a href="/post/2025-01-long-echo/">blog post</a></li>
</ul>
<p>MIT licensed.</p>
<hr>
<p><em>Part of the Long Echo project. Your conversations with AI are knowledge worth preserving.</em></p>
]]></content:encoded><category>Python</category><category>AI</category><category>LLM</category><category>conversation-management</category><category>ChatGPT</category><category>Claude</category><category>Copilot</category><category>TUI</category><category>SQLite</category><category>plugin-architecture</category><category>privacy</category><category>MCP</category></item><item><title>The Dot Ecosystem: From Simple Paths to Data Algebras</title><link>https://metafunctor.com/post/2024-12-dotsuite/</link><pubDate>Sun, 15 Dec 2024 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-12-dotsuite/</guid><description>A composable ecosystem of tools for manipulating nested data structures. From a simple helper function to a full data algebra, guided by purity, pedagogy, and the principle of least power.</description><content:encoded><![CDATA[<p><strong><a href="https://github.com/queelius/dotsuite">dotsuite</a></strong> is a suite of composable tools for working with nested data structures like JSON, YAML, and Python dictionaries. It started as a single helper function and grew into something with actual mathematical structure. That growth is the interesting part.</p>
<h2 id="the-origin">The Origin</h2>
<p>It always starts with a simple problem. You have a nested dictionary and you need a value buried deep inside:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="c1"># Brittle code that crashes on missing keys</span>
</span></span><span class="line"><span class="cl"><span class="n">email</span> <span class="o">=</span> <span class="n">data</span><span class="p">[</span><span class="s1">&#39;user&#39;</span><span class="p">][</span><span class="s1">&#39;contacts&#39;</span><span class="p">][</span><span class="mi">0</span><span class="p">][</span><span class="s1">&#39;email&#39;</span><span class="p">]</span>  <span class="c1"># KeyError? IndexError?</span>
</span></span></code></pre></div><p>The first solution is a helper function:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="c1"># The essence of dotget - simple enough to copy</span>
</span></span><span class="line"><span class="cl"><span class="k">def</span> <span class="nf">get</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="n">path</span><span class="p">,</span> <span class="n">default</span><span class="o">=</span><span class="kc">None</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">    <span class="k">try</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="n">segment</span> <span class="ow">in</span> <span class="n">path</span><span class="o">.</span><span class="n">split</span><span class="p">(</span><span class="s1">&#39;.&#39;</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">            <span class="n">data</span> <span class="o">=</span> <span class="n">data</span><span class="p">[</span><span class="nb">int</span><span class="p">(</span><span class="n">segment</span><span class="p">)]</span> <span class="k">if</span> <span class="n">segment</span><span class="o">.</span><span class="n">isdigit</span><span class="p">()</span> <span class="k">else</span> <span class="n">data</span><span class="p">[</span><span class="n">segment</span><span class="p">]</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">data</span>
</span></span><span class="line"><span class="cl">    <span class="k">except</span> <span class="p">(</span><span class="ne">KeyError</span><span class="p">,</span> <span class="ne">IndexError</span><span class="p">,</span> <span class="ne">TypeError</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">default</span>
</span></span></code></pre></div><p>This is where the story begins. That single function, once you start asking questions about what else you need, leads to a complete ecosystem for data manipulation. The trick is that the questions have a natural structure to them.</p>
<h2 id="the-three-pillars">The Three Pillars</h2>
<p>The ecosystem organizes around three fundamental questions about data:</p>
<h3 id="depth-pillar-where-is-the-data">Depth Pillar: &ldquo;Where is the data?&rdquo;</h3>
<p>Tools for finding and extracting values from within documents.</p>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>Purpose</th>
          <th>Complexity</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>dotget</strong></td>
          <td>Simple exact paths</td>
          <td><code>get(data, &quot;user.name&quot;)</code></td>
      </tr>
      <tr>
          <td><strong>dotstar</strong></td>
          <td>Wildcard patterns</td>
          <td><code>search(data, &quot;users.*.name&quot;)</code></td>
      </tr>
      <tr>
          <td><strong>dotselect</strong></td>
          <td>Advanced selection with predicates</td>
          <td><code>find_first(data, &quot;users[role=admin].name&quot;)</code></td>
      </tr>
      <tr>
          <td><strong>dotpath</strong></td>
          <td>Extensible path engine</td>
          <td>Powers all other tools, Turing-complete</td>
      </tr>
  </tbody>
</table>
<p>The addressing layer forms a free algebra on selectors, with operators being morphisms in the Kleisli category of the powerset monad. In practice this means <code>dotstar</code> composed with <code>dotselect</code> still yields a well-defined set of values. You can compose these things without worrying about edge cases blowing up.</p>
<h3 id="truth-pillar-is-this-assertion-true">Truth Pillar: &ldquo;Is this assertion true?&rdquo;</h3>
<p>Tools for asking boolean questions and validating data.</p>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>Purpose</th>
          <th>Logic</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>dotexists</strong></td>
          <td>Path existence</td>
          <td><code>check(data, &quot;user.email&quot;)</code></td>
      </tr>
      <tr>
          <td><strong>dotany</strong></td>
          <td>Existential quantifier</td>
          <td><code>any_match(data, &quot;users.*.role&quot;, &quot;admin&quot;)</code></td>
      </tr>
      <tr>
          <td><strong>dotall</strong></td>
          <td>Universal quantifier</td>
          <td><code>all_match(data, &quot;users.*.status&quot;, &quot;active&quot;)</code></td>
      </tr>
      <tr>
          <td><strong>dotquery</strong></td>
          <td>Compositional logic engine</td>
          <td><code>Query(&quot;any equals role admin&quot;).check(data)</code></td>
      </tr>
  </tbody>
</table>
<p>Predicates form a Boolean algebra under conjunction, disjunction, and negation that is homomorphic to set algebra on result subsets. This enables short-circuit evaluation and distributive laws. The math isn&rsquo;t decoration; it&rsquo;s what makes the composition reliable.</p>
<h3 id="shape-pillar-how-should-the-data-be-transformed">Shape Pillar: &ldquo;How should the data be transformed?&rdquo;</h3>
<p>Tools for reshaping and modifying data structures.</p>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>Purpose</th>
          <th>Type</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>dotmod</strong></td>
          <td>Surgical modifications</td>
          <td><code>set_(data, &quot;user.status&quot;, &quot;inactive&quot;)</code></td>
      </tr>
      <tr>
          <td><strong>dotbatch</strong></td>
          <td>Atomic transactions</td>
          <td>Apply multiple changes safely</td>
      </tr>
      <tr>
          <td><strong>dotpipe</strong></td>
          <td>Data transformation pipelines</td>
          <td>Reshape documents into new forms</td>
      </tr>
      <tr>
          <td><strong>dotpluck</strong></td>
          <td>Value extraction</td>
          <td>Create new structures from selections</td>
      </tr>
  </tbody>
</table>
<p>Transformations are endofunctors on document spaces with monoid composition. <code>dotmod</code> implements lenses with put-get laws, while <code>dotpipe</code> provides Kleisli composition of pure functions.</p>
<h2 id="the-pedagogical-gradient">The Pedagogical Gradient</h2>
<p>The ecosystem is designed as a learning path. Each level builds on the previous, and no tool becomes obsolete.</p>
<p><strong>1. Hello World</strong> (minimal mental load):</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">depth.dotget.core</span> <span class="kn">import</span> <span class="n">get</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">truth.dotexists.core</span> <span class="kn">import</span> <span class="n">check</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">data</span> <span class="o">=</span> <span class="p">{</span><span class="s2">&#34;users&#34;</span><span class="p">:</span> <span class="p">[{</span><span class="s2">&#34;name&#34;</span><span class="p">:</span> <span class="s2">&#34;Alice&#34;</span><span class="p">}]}</span>
</span></span><span class="line"><span class="cl"><span class="n">name</span> <span class="o">=</span> <span class="n">get</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="s2">&#34;users.0.name&#34;</span><span class="p">)</span>  <span class="c1"># &#34;Alice&#34;</span>
</span></span><span class="line"><span class="cl"><span class="n">exists</span> <span class="o">=</span> <span class="n">check</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="s2">&#34;users.0.email&#34;</span><span class="p">)</span>  <span class="c1"># False</span>
</span></span></code></pre></div><p><strong>2. Pattern Phase</strong> (wildcards and basic changes):</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">depth.dotstar.core</span> <span class="kn">import</span> <span class="n">search</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">shape.dotmod.core</span> <span class="kn">import</span> <span class="n">set_</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">all_names</span> <span class="o">=</span> <span class="n">search</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="s2">&#34;users.*.name&#34;</span><span class="p">)</span>  <span class="c1"># [&#34;Alice&#34;]</span>
</span></span><span class="line"><span class="cl"><span class="n">new_data</span> <span class="o">=</span> <span class="n">set_</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="s2">&#34;users.0.status&#34;</span><span class="p">,</span> <span class="s2">&#34;active&#34;</span><span class="p">)</span>  <span class="c1"># Immutable!</span>
</span></span></code></pre></div><p><strong>3. Power User</strong> (complex operations):</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">truth.dotquery.core</span> <span class="kn">import</span> <span class="n">Query</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Compositional boolean logic</span>
</span></span><span class="line"><span class="cl"><span class="n">query</span> <span class="o">=</span> <span class="n">Query</span><span class="p">(</span><span class="s2">&#34;equals role admin and greater login_count 10&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">admins</span> <span class="o">=</span> <span class="p">[</span><span class="n">user</span> <span class="k">for</span> <span class="n">user</span> <span class="ow">in</span> <span class="n">users</span> <span class="k">if</span> <span class="n">query</span><span class="o">.</span><span class="n">check</span><span class="p">(</span><span class="n">user</span><span class="p">)]</span>
</span></span></code></pre></div><p><strong>4. Expert</strong> (extensible engines):</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">depth.dotpath.core</span> <span class="kn">import</span> <span class="n">PathEngine</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">collections.dotrelate.core</span> <span class="kn">import</span> <span class="n">join</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Custom selector primitives</span>
</span></span><span class="line"><span class="cl"><span class="n">engine</span> <span class="o">=</span> <span class="n">PathEngine</span><span class="p">()</span>
</span></span><span class="line"><span class="cl"><span class="n">engine</span><span class="o">.</span><span class="n">register_selector</span><span class="p">(</span><span class="n">FuzzyKeySelector</span><span class="p">())</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Relational operations</span>
</span></span><span class="line"><span class="cl"><span class="n">enriched</span> <span class="o">=</span> <span class="n">join</span><span class="p">(</span><span class="n">users</span><span class="p">,</span> <span class="n">orders</span><span class="p">,</span> <span class="n">left_on</span><span class="o">=</span><span class="s2">&#34;id&#34;</span><span class="p">,</span> <span class="n">right_on</span><span class="o">=</span><span class="s2">&#34;user_id&#34;</span><span class="p">)</span>
</span></span></code></pre></div><p>A <code>dotget</code> call is still the right choice when you know the exact path. You don&rsquo;t outgrow the simple tools; you add more when you need them.</p>
<h2 id="the-steal-this-code-philosophy">The &ldquo;Steal This Code&rdquo; Philosophy</h2>
<p>Many tools are intentionally simple enough that you can copy their core logic rather than add a dependency:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="c1"># The entire essence of dotget</span>
</span></span><span class="line"><span class="cl"><span class="k">def</span> <span class="nf">get</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="n">path</span><span class="p">,</span> <span class="n">default</span><span class="o">=</span><span class="kc">None</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">    <span class="k">try</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="n">segment</span> <span class="ow">in</span> <span class="n">path</span><span class="o">.</span><span class="n">split</span><span class="p">(</span><span class="s1">&#39;.&#39;</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">            <span class="n">data</span> <span class="o">=</span> <span class="n">data</span><span class="p">[</span><span class="nb">int</span><span class="p">(</span><span class="n">segment</span><span class="p">)]</span> <span class="k">if</span> <span class="n">segment</span><span class="o">.</span><span class="n">isdigit</span><span class="p">()</span> <span class="ow">and</span> <span class="nb">isinstance</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="nb">list</span><span class="p">)</span> <span class="k">else</span> <span class="n">data</span><span class="p">[</span><span class="n">segment</span><span class="p">]</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">data</span>
</span></span><span class="line"><span class="cl">    <span class="k">except</span> <span class="p">(</span><span class="ne">KeyError</span><span class="p">,</span> <span class="ne">IndexError</span><span class="p">,</span> <span class="ne">TypeError</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">default</span>
</span></span></code></pre></div><p>Make each program do one thing so well that its implementation becomes obvious. If you don&rsquo;t need the rest of the suite, just take what you need.</p>
<h2 id="lifting-to-collections">Lifting to Collections</h2>
<p>Once you have operations on single documents, you can lift them to collections. This is where the category theory actually pays off.</p>
<h3 id="boolean-wing-filtering">Boolean Wing (Filtering)</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">collections.dotfilter.core</span> <span class="kn">import</span> <span class="n">filter_collection</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Filter documents matching a predicate</span>
</span></span><span class="line"><span class="cl"><span class="n">admins</span> <span class="o">=</span> <span class="n">filter_collection</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">    <span class="n">users</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">Query</span><span class="p">(</span><span class="s2">&#34;equals role admin&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Boolean algebra on collections</span>
</span></span><span class="line"><span class="cl"><span class="n">active_admins</span> <span class="o">=</span> <span class="n">filter_collection</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">    <span class="n">admins</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">Query</span><span class="p">(</span><span class="s2">&#34;equals status active&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span></code></pre></div><p>Every collection operator is a monoid homomorphism with respect to multiset union, which means you can parallelize and stream without thinking about correctness.</p>
<h3 id="relational-wing-joining-and-transforming">Relational Wing (Joining and Transforming)</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">collections.dotrelate.core</span> <span class="kn">import</span> <span class="n">join</span><span class="p">,</span> <span class="n">project</span><span class="p">,</span> <span class="n">union</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Relational algebra on document collections</span>
</span></span><span class="line"><span class="cl"><span class="n">user_orders</span> <span class="o">=</span> <span class="n">join</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">    <span class="n">users</span><span class="p">,</span> <span class="n">orders</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">left_on</span><span class="o">=</span><span class="s2">&#34;id&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">right_on</span><span class="o">=</span><span class="s2">&#34;user_id&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">how</span><span class="o">=</span><span class="s2">&#34;left&#34;</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Project to specific fields</span>
</span></span><span class="line"><span class="cl"><span class="n">summary</span> <span class="o">=</span> <span class="n">project</span><span class="p">(</span><span class="n">user_orders</span><span class="p">,</span> <span class="p">[</span><span class="s2">&#34;name&#34;</span><span class="p">,</span> <span class="s2">&#34;total_spent&#34;</span><span class="p">])</span>
</span></span></code></pre></div><p>This mirrors database theory: single-document logic is like tuple calculus; <code>dotrelate</code> is like relational algebra.</p>
<h2 id="command-line-first">Command-Line First</h2>
<p>Every tool works from the command line:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Check if any user is an admin</span>
</span></span><span class="line"><span class="cl">cat users.json <span class="p">|</span> dotquery <span class="s2">&#34;any equals role admin&#34;</span> <span class="o">&amp;&amp;</span> <span class="nb">echo</span> <span class="s2">&#34;Admin found&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Extract all email addresses</span>
</span></span><span class="line"><span class="cl">cat contacts.json <span class="p">|</span> dotstar <span class="s2">&#34;contacts.*.email&#34;</span> &gt; emails.txt
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Join users with their orders</span>
</span></span><span class="line"><span class="cl">dotrelate join --on<span class="o">=</span><span class="s2">&#34;user_id&#34;</span> users.jsonl orders.jsonl
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Filter pipeline</span>
</span></span><span class="line"><span class="cl">cat books.json <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> dotquery <span class="s2">&#34;less price 10 or equals author &#39;Tolkien&#39;&#34;</span> <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> dotquery and <span class="s2">&#34;equals in_stock true&#34;</span> <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> dotquery resolve
</span></span></code></pre></div><h2 id="dual-apis-programmatic-and-declarative">Dual APIs: Programmatic and Declarative</h2>
<p>Many tools offer both Python APIs and serializable query formats:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="c1"># Programmatic (Python API)</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">truth.dotquery.primitives</span> <span class="kn">import</span> <span class="n">equals</span><span class="p">,</span> <span class="n">greater</span>
</span></span><span class="line"><span class="cl"><span class="n">query</span> <span class="o">=</span> <span class="n">Query</span><span class="p">(</span><span class="n">equals</span><span class="p">(</span><span class="s1">&#39;role&#39;</span><span class="p">,</span> <span class="s1">&#39;admin&#39;</span><span class="p">)</span> <span class="o">&amp;</span> <span class="n">greater</span><span class="p">(</span><span class="s1">&#39;login_count&#39;</span><span class="p">,</span> <span class="mi">10</span><span class="p">))</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Declarative (string DSL)</span>
</span></span><span class="line"><span class="cl"><span class="n">query</span> <span class="o">=</span> <span class="n">Query</span><span class="p">(</span><span class="s2">&#34;equals role admin and greater login_count 10&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Both produce the same AST</span>
</span></span><span class="line"><span class="cl"><span class="k">assert</span> <span class="n">query</span><span class="o">.</span><span class="n">ast</span> <span class="o">==</span> <span class="n">other_query</span><span class="o">.</span><span class="n">ast</span>
</span></span></code></pre></div><p>This means you can serialize queries to JSON, send them over APIs, version control them in git, and use them from the command line. Same semantics everywhere.</p>
<h2 id="mathematical-foundation">Mathematical Foundation</h2>
<p>For those who care about the formal structure (I do):</p>
<h3 id="type-signatures">Type Signatures</h3>
<p><strong>Depth Pillar</strong> (Addressing):
</p>
\[
\text{dotget}: \mathcal{D} \times \text{Path}_{\text{exact}} \to V \cup \lbrace \emptyset\rbrace
\]<p>
</p>
\[
\text{dotstar}: \mathcal{D} \times \text{Path}_{*} \to V^{*}
\]<p>
</p>
\[
\text{dotpath}: \text{Free algebra on selectors (Turing-complete)}
\]<p><strong>Truth Pillar</strong> (Predicates):
</p>
\[
\text{dotexists}: \mathcal{D} \times P \to \mathbb{B}
\]<p>
</p>
\[
\text{dotquery}: \mathcal{D} \times \mathcal{L}_{\text{BOOL}}(\varphi_i) \to \mathbb{B}
\]<p><strong>Shape Pillar</strong> (Transformations):
</p>
\[
\text{dotmod}: \mathcal{D} \times \delta \to \mathcal{D} \quad \text{(lens with put-get law)}
\]<p>
</p>
\[
\text{dotpipe}: \mathcal{D} \times F^{*} \to \mathcal{D} \quad \text{(Kleisli composition)}
\]<h3 id="functorial-lifting">Functorial Lifting</h3>
<p>Operations lift to collections via functors:</p>
\[
\text{map}: (\mathcal{D} \to X) \to (C \to X^{*})
\]<p>
</p>
\[
\text{filter}: (\mathcal{D} \to \mathbb{B}) \to (C \to C)
\]<p>Where \(C \subseteq \mathcal{D}\) is a finite multiset (collection).</p>
<h3 id="key-properties">Key Properties</h3>
<ol>
<li><strong>Compositionality</strong>: Operators form algebras (Boolean, monoidal, Kleisli) guaranteeing local reasoning</li>
<li><strong>Purity</strong>: Functions are referentially transparent, so concurrency is trivial</li>
<li><strong>Totality</strong>: Missing paths yield the empty set rather than exceptions</li>
<li><strong>Orthogonality</strong>: No operator belongs to multiple pillars</li>
<li><strong>Homomorphism</strong>: Collection operators preserve structure under union</li>
</ol>
<h2 id="real-world-examples">Real-World Examples</h2>
<h3 id="data-pipeline-processing">Data Pipeline Processing</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">depth.dotstar.core</span> <span class="kn">import</span> <span class="n">search</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">truth.dotquery.core</span> <span class="kn">import</span> <span class="n">Query</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">shape.dotpipe.core</span> <span class="kn">import</span> <span class="n">pipe</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Extract -&gt; Filter -&gt; Transform pipeline</span>
</span></span><span class="line"><span class="cl"><span class="n">pipeline</span> <span class="o">=</span> <span class="n">pipe</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">    <span class="k">lambda</span> <span class="n">doc</span><span class="p">:</span> <span class="n">search</span><span class="p">(</span><span class="n">doc</span><span class="p">,</span> <span class="s2">&#34;transactions.*.amount&#34;</span><span class="p">),</span>
</span></span><span class="line"><span class="cl">    <span class="k">lambda</span> <span class="n">amounts</span><span class="p">:</span> <span class="p">[</span><span class="n">a</span> <span class="k">for</span> <span class="n">a</span> <span class="ow">in</span> <span class="n">amounts</span> <span class="k">if</span> <span class="n">a</span> <span class="o">&gt;</span> <span class="mi">100</span><span class="p">],</span>
</span></span><span class="line"><span class="cl">    <span class="k">lambda</span> <span class="n">large</span><span class="p">:</span> <span class="nb">sum</span><span class="p">(</span><span class="n">large</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">total_large_transactions</span> <span class="o">=</span> <span class="n">pipeline</span><span class="p">(</span><span class="n">financial_data</span><span class="p">)</span>
</span></span></code></pre></div><h3 id="etl-with-relational-operations">ETL with Relational Operations</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">collections.dotrelate.core</span> <span class="kn">import</span> <span class="n">join</span><span class="p">,</span> <span class="n">project</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Join users with orders</span>
</span></span><span class="line"><span class="cl"><span class="n">user_orders</span> <span class="o">=</span> <span class="n">join</span><span class="p">(</span><span class="n">users</span><span class="p">,</span> <span class="n">orders</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                   <span class="n">left_on</span><span class="o">=</span><span class="s2">&#34;user_id&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                   <span class="n">right_on</span><span class="o">=</span><span class="s2">&#34;customer_id&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Join with products</span>
</span></span><span class="line"><span class="cl"><span class="n">full_data</span> <span class="o">=</span> <span class="n">join</span><span class="p">(</span><span class="n">user_orders</span><span class="p">,</span> <span class="n">products</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                 <span class="n">left_on</span><span class="o">=</span><span class="s2">&#34;product_id&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                 <span class="n">right_on</span><span class="o">=</span><span class="s2">&#34;id&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Project to summary</span>
</span></span><span class="line"><span class="cl"><span class="n">summary</span> <span class="o">=</span> <span class="n">project</span><span class="p">(</span><span class="n">full_data</span><span class="p">,</span> <span class="p">[</span>
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;user.name&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;order.date&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;product.name&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;order.total&#34;</span>
</span></span><span class="line"><span class="cl"><span class="p">])</span>
</span></span></code></pre></div><h3 id="complex-boolean-logic">Complex Boolean Logic</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">truth.dotquery.core</span> <span class="kn">import</span> <span class="n">Query</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Find books that are:</span>
</span></span><span class="line"><span class="cl"><span class="c1"># - Either cheap (&lt;$10) OR by Tolkien</span>
</span></span><span class="line"><span class="cl"><span class="c1"># - AND in stock</span>
</span></span><span class="line"><span class="cl"><span class="c1"># - AND have good reviews (&gt;4 stars)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">query</span> <span class="o">=</span> <span class="n">Query</span><span class="p">(</span><span class="s2">&#34;&#34;&#34;
</span></span></span><span class="line"><span class="cl"><span class="s2">    (less price 10 or equals author &#39;Tolkien&#39;)
</span></span></span><span class="line"><span class="cl"><span class="s2">    and equals in_stock true
</span></span></span><span class="line"><span class="cl"><span class="s2">    and greater rating 4.0
</span></span></span><span class="line"><span class="cl"><span class="s2">&#34;&#34;&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">matching_books</span> <span class="o">=</span> <span class="p">[</span><span class="n">book</span> <span class="k">for</span> <span class="n">book</span> <span class="ow">in</span> <span class="n">catalog</span> <span class="k">if</span> <span class="n">query</span><span class="o">.</span><span class="n">check</span><span class="p">(</span><span class="n">book</span><span class="p">)]</span>
</span></span></code></pre></div><h2 id="design-principles">Design Principles</h2>
<ul>
<li><strong>Compositionality</strong>: Every tool composes cleanly with others</li>
<li><strong>Immutability</strong>: Original data is never modified</li>
<li><strong>Pedagogical</strong>: Simple tools graduate to powerful ones</li>
<li><strong>Single Purpose</strong>: Each tool has one clear responsibility</li>
<li><strong>Interoperability</strong>: Common patterns work across all tools</li>
<li><strong>Safety</strong>: Graceful handling of missing paths and malformed data</li>
</ul>
<h2 id="category-theory-for-the-curious">Category Theory for the Curious</h2>
<p>For those interested in the theoretical foundations:</p>
<p><strong>Paths as Optics</strong>: The addressing layer implements lenses and prisms from optics theory, providing composable getters and setters.</p>
<p><strong>Predicates as Subobjects</strong>: Boolean queries correspond to subobjects in the topos of sets, with conjunction/disjunction/negation being categorical limits/colimits/exponentials.</p>
<p><strong>Transformations as Endofunctors</strong>: Shape operations are endofunctors on the category of documents, with natural transformations providing the composition structure.</p>
<p><strong>Collections as Functorial Lift</strong>: The map/filter operations on collections are functors from the category of single-document operations to the category of collection operations, preserving algebraic structure.</p>
<p>This isn&rsquo;t mathematical window dressing. These properties guarantee that composition behaves predictably and that parallel evaluation is always safe. When I say &ldquo;this composes,&rdquo; I mean it in the category-theoretic sense, not the marketing sense.</p>
<h2 id="quick-start">Quick Start</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Install from PyPI (once published)</span>
</span></span><span class="line"><span class="cl">pip install dotsuite
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Or install from source</span>
</span></span><span class="line"><span class="cl">git clone https://github.com/queelius/dotsuite.git
</span></span><span class="line"><span class="cl"><span class="nb">cd</span> dotsuite
</span></span><span class="line"><span class="cl">pip install -e .
</span></span></code></pre></div><p>Basic usage:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">depth.dotget.core</span> <span class="kn">import</span> <span class="n">get</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">depth.dotstar.core</span> <span class="kn">import</span> <span class="n">search</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">truth.dotquery.core</span> <span class="kn">import</span> <span class="n">Query</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">shape.dotmod.core</span> <span class="kn">import</span> <span class="n">set_</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Simple addressing</span>
</span></span><span class="line"><span class="cl"><span class="n">data</span> <span class="o">=</span> <span class="p">{</span><span class="s2">&#34;users&#34;</span><span class="p">:</span> <span class="p">[{</span><span class="s2">&#34;name&#34;</span><span class="p">:</span> <span class="s2">&#34;Alice&#34;</span><span class="p">,</span> <span class="s2">&#34;role&#34;</span><span class="p">:</span> <span class="s2">&#34;admin&#34;</span><span class="p">}]}</span>
</span></span><span class="line"><span class="cl"><span class="n">name</span> <span class="o">=</span> <span class="n">get</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="s2">&#34;users.0.name&#34;</span><span class="p">)</span>  <span class="c1"># &#34;Alice&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Pattern matching</span>
</span></span><span class="line"><span class="cl"><span class="n">all_names</span> <span class="o">=</span> <span class="n">search</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="s2">&#34;users.*.name&#34;</span><span class="p">)</span>  <span class="c1"># [&#34;Alice&#34;]</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Boolean queries</span>
</span></span><span class="line"><span class="cl"><span class="n">is_admin</span> <span class="o">=</span> <span class="n">Query</span><span class="p">(</span><span class="s2">&#34;equals role admin&#34;</span><span class="p">)</span><span class="o">.</span><span class="n">check</span><span class="p">(</span><span class="n">data</span><span class="p">[</span><span class="s2">&#34;users&#34;</span><span class="p">][</span><span class="mi">0</span><span class="p">])</span>  <span class="c1"># True</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Immutable modifications</span>
</span></span><span class="line"><span class="cl"><span class="n">new_data</span> <span class="o">=</span> <span class="n">set_</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="s2">&#34;users.0.status&#34;</span><span class="p">,</span> <span class="s2">&#34;active&#34;</span><span class="p">)</span>
</span></span></code></pre></div><h2 id="resources">Resources</h2>
<ul>
<li><strong>Repository</strong>: <a href="https://github.com/queelius/dotsuite">github.com/queelius/dotsuite</a></li>
<li><strong>Philosophy</strong>: <a href="https://github.com/queelius/dotsuite/blob/master/docs/philosophy.md">docs/philosophy.md</a></li>
<li><strong>Formal Spec</strong>: <a href="https://github.com/queelius/dotsuite/blob/master/docs/dot-formalism.md">docs/dot-formalism.md</a></li>
<li><strong>Tool Docs</strong>: Individual README for each tool</li>
<li><strong>JAF Comparison</strong>: <a href="https://github.com/queelius/dotsuite/blob/master/docs/jaf-comparison.md">docs/jaf-comparison.md</a></li>
</ul>
<h2 id="license">License</h2>
<p>MIT</p>
]]></content:encoded><category>Python</category><category>data-structures</category><category>JSON</category><category>functional-programming</category><category>API-design</category><category>category-theory</category><category>compositionality</category><category>Unix-philosophy</category></item></channel></rss>