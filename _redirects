# Netlify Redirects for SEO Canonical URLs

# Redirect www to non-www (canonical domain)
https://www.metafunctor.com/*  https://metafunctor.com/:splat  301!

# Remove trailing slashes for consistency (optional - Hugo handles this, but explicit is good)
# /*/ /:splat 301

# Legacy URL redirects (if you had old URL patterns)
# Add any old URL patterns here as you discover them in analytics

# 404 handling - redirect to custom 404 page
/* /404.html 404
