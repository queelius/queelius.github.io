<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:media="http://search.yahoo.com/mrss/"><channel><title>Category-Theory on metafunctor</title><link>https://metafunctor.com/tags/category-theory/</link><description>Research Engineer &amp; Computer Scientist - Machine Learning, Statistical Computing, Open Source Development</description><language>en-us</language><managingEditor>lex@metafunctor.com (Alex Towell)</managingEditor><webMaster>lex@metafunctor.com (Alex Towell)</webMaster><lastBuildDate>Thu, 09 Jul 2026 17:33:25 -0500</lastBuildDate><atom:link href="https://metafunctor.com/tags/category-theory/" rel="self" type="application/rss+xml"/><image><url>https://metafunctor.com/images/og-image.jpg</url><title>metafunctor</title><link>https://metafunctor.com/</link></image><item><title>Bits Follow Types</title><link>https://metafunctor.com/post/2026-05-codecs-functors-stepanov/</link><pubDate>Thu, 23 Apr 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2026-05-codecs-functors-stepanov/</guid><description>Codecs are not ad-hoc bit formats. They are constructions on the algebraic structure of types.</description><content:encoded><![CDATA[<p><em>Every type decomposes structurally. So does its codec.</em></p>
<h2 id="codecs-as-functors">Codecs as Functors</h2>
<p>You have an <code>optional&lt;vector&lt;pair&lt;int, string&gt;&gt;&gt;</code>. The type decomposes structurally: it is an optional of a free monoid of products of an integer and a string. That decomposition is not an observation about memory layout. It is a statement about the algebraic structure of the type.</p>
<p>Now ask: does the codec decompose the same way?</p>
<p>If the answer is yes, you stop writing one-off encoders. You build a codec for <code>optional&lt;T&gt;</code> from a codec for <code>T</code>. You build a codec for <code>vector&lt;T&gt;</code> from a codec for <code>T</code>. The codec for <code>optional&lt;vector&lt;pair&lt;int, string&gt;&gt;&gt;</code> assembles from its parts with no manual layout decisions, no hand-placed length headers, no ad-hoc format negotiation.</p>
<p>This post argues that the answer is always yes, and shows what the machinery looks like. The thesis: codecs are not ad-hoc bit formats. They are constructions on the algebraic structure of types. The algebraic structure of a type determines its codec, the same way it determines its algorithms.</p>
<p>This extends Stepanov&rsquo;s claim. The <a href="/post/2019-03-peasant-stepanov/">peasant algorithm post</a> showed that algorithms arise from algebraic structure. The <a href="/post/2026-03-homomorphism-stepanov/">homomorphism post</a> showed that structure-preserving maps are the natural morphisms. Here, we show the codec itself is a structure-preserving map, and that it lifts from leaf types to compound types by the same algebraic logic.</p>
<h2 id="bit-io-the-foundation">Bit I/O: The Foundation</h2>
<p>Before combinators, we need concrete bit I/O. The approach taken here follows Stepanov&rsquo;s move in the algorithm posts: state the concept first, then provide a model.</p>
<p>Two concepts govern bit-level I/O:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">T</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">concept</span> <span class="n">BitSink</span> <span class="o">=</span> <span class="k">requires</span><span class="p">(</span><span class="n">T</span><span class="o">&amp;</span> <span class="n">s</span><span class="p">,</span> <span class="kt">bool</span> <span class="n">bit</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span> <span class="n">s</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="n">bit</span><span class="p">)</span> <span class="p">}</span> <span class="o">-&gt;</span> <span class="n">std</span><span class="o">::</span><span class="n">same_as</span><span class="o">&lt;</span><span class="kt">void</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">T</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">concept</span> <span class="n">BitSource</span> <span class="o">=</span> <span class="k">requires</span><span class="p">(</span><span class="n">T</span><span class="o">&amp;</span> <span class="n">s</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span> <span class="n">s</span><span class="p">.</span><span class="n">read</span><span class="p">()</span> <span class="p">}</span> <span class="o">-&gt;</span> <span class="n">std</span><span class="o">::</span><span class="n">same_as</span><span class="o">&lt;</span><span class="kt">bool</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span> <span class="n">s</span><span class="p">.</span><span class="n">peek</span><span class="p">()</span> <span class="p">}</span> <span class="o">-&gt;</span> <span class="n">std</span><span class="o">::</span><span class="n">convertible_to</span><span class="o">&lt;</span><span class="kt">bool</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>A <code>BitSink</code> accepts bits. A <code>BitSource</code> supplies them. A codec is an algorithm parameterized over <code>BitSink</code> and <code>BitSource</code>, not a class hierarchy. This is Stepanov&rsquo;s move at the bit level: require only what the algorithm needs, let anything that satisfies the concept participate.</p>
<p>The standard models are <code>BitWriter</code> and <code>BitReader</code>, which pack bits into byte buffers in LSB-first order:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">BitWriter</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">span</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span><span class="o">&gt;</span> <span class="n">buf_</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">byte_idx_</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span> <span class="n">byte_</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span> <span class="n">bit_pos_</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">public</span><span class="o">:</span>
</span></span><span class="line"><span class="cl">    <span class="k">explicit</span> <span class="n">BitWriter</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">span</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span><span class="o">&gt;</span> <span class="n">buf</span><span class="p">)</span> <span class="k">noexcept</span> <span class="o">:</span> <span class="n">buf_</span><span class="p">(</span><span class="n">buf</span><span class="p">)</span> <span class="p">{}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kt">void</span> <span class="nf">write</span><span class="p">(</span><span class="kt">bool</span> <span class="n">bit</span><span class="p">)</span> <span class="k">noexcept</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">byte_</span> <span class="o">|=</span> <span class="p">(</span><span class="n">bit</span> <span class="o">?</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span><span class="p">{</span><span class="mi">1</span><span class="p">}</span> <span class="o">:</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span><span class="p">{</span><span class="mi">0</span><span class="p">})</span> <span class="o">&lt;&lt;</span> <span class="n">bit_pos_</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="o">++</span><span class="n">bit_pos_</span> <span class="o">==</span> <span class="mi">8</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">buf_</span><span class="p">[</span><span class="n">byte_idx_</span><span class="o">++</span><span class="p">]</span> <span class="o">=</span> <span class="n">byte_</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="n">byte_</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="n">bit_pos_</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kt">void</span> <span class="nf">align</span><span class="p">()</span> <span class="k">noexcept</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">bit_pos_</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">buf_</span><span class="p">[</span><span class="n">byte_idx_</span><span class="o">++</span><span class="p">]</span> <span class="o">=</span> <span class="n">byte_</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="n">byte_</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="n">bit_pos_</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="na">[[nodiscard]]</span> <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">bytes_written</span><span class="p">()</span> <span class="k">const</span> <span class="k">noexcept</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">byte_idx_</span> <span class="o">+</span> <span class="p">(</span><span class="n">bit_pos_</span> <span class="o">&gt;</span> <span class="mi">0</span> <span class="o">?</span> <span class="mi">1</span> <span class="o">:</span> <span class="mi">0</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">BitReader</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">span</span><span class="o">&lt;</span><span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span><span class="o">&gt;</span> <span class="n">buf_</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">byte_idx_</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span> <span class="n">bit_pos_</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">public</span><span class="o">:</span>
</span></span><span class="line"><span class="cl">    <span class="k">explicit</span> <span class="n">BitReader</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">span</span><span class="o">&lt;</span><span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span><span class="o">&gt;</span> <span class="n">buf</span><span class="p">)</span> <span class="k">noexcept</span> <span class="o">:</span> <span class="n">buf_</span><span class="p">(</span><span class="n">buf</span><span class="p">)</span> <span class="p">{}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kt">bool</span> <span class="nf">read</span><span class="p">()</span> <span class="k">noexcept</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="kt">bool</span> <span class="n">bit</span> <span class="o">=</span> <span class="p">((</span><span class="n">buf_</span><span class="p">[</span><span class="n">byte_idx_</span><span class="p">]</span> <span class="o">&gt;&gt;</span> <span class="n">bit_pos_</span><span class="p">)</span> <span class="o">&amp;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="o">++</span><span class="n">bit_pos_</span> <span class="o">==</span> <span class="mi">8</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="o">++</span><span class="n">byte_idx_</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="n">bit_pos_</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">bit</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="na">[[nodiscard]]</span> <span class="kt">bool</span> <span class="n">peek</span><span class="p">()</span> <span class="k">const</span> <span class="k">noexcept</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">byte_idx_</span> <span class="o">&lt;</span> <span class="n">buf_</span><span class="p">.</span><span class="n">size</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>A codec concept rounds out the three-concept core:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">C</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">concept</span> <span class="n">Codec</span> <span class="o">=</span> <span class="k">requires</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">typename</span> <span class="n">C</span><span class="o">::</span><span class="n">value_type</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>The minimal form checks that <code>C</code> names a <code>value_type</code>. The full behavioral requirement, that <code>C</code> provides <code>encode</code> and <code>decode</code> templates matching <code>BitSink</code> and <code>BitSource</code> respectively, is enforced by the template constraints on each codec&rsquo;s methods rather than by the concept itself. This keeps the concept definition lean while the actual requirements are checked at each call site.</p>
<p>All code in this post lives in <code>codecs_functors.hpp</code>. The full suite of round-trip tests is in <code>test_codecs_functors.cpp</code>.</p>
<h2 id="a-leaf-codec-elias-gamma">A Leaf Codec: Elias Gamma</h2>
<p>Before combinators, establish what a leaf codec looks like. Elias gamma encodes positive integers \(n \geq 1\).</p>
<p>The codeword for \(n\) is: \(\lfloor\log_2 n\rfloor\) zero bits, then a one bit, then the bits of \(n\) below its leading 1, written MSB-first. The total length is \(2\lfloor\log_2 n\rfloor + 1\) bits.</p>
<p>Examples: 1 encodes as <code>1</code>, 2 as <code>010</code>, 3 as <code>011</code>, 4 as <code>00100</code>, 7 as <code>00111</code>.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Gamma</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="n">value_type</span> <span class="n">n</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">assert</span><span class="p">(</span><span class="n">n</span> <span class="o">&gt;=</span> <span class="mi">1</span> <span class="o">&amp;&amp;</span> <span class="s">&#34;Gamma is undefined for n = 0&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">bits</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">bit_width</span><span class="p">(</span><span class="n">n</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">bits</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">false</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">true</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="n">bits</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span> <span class="n">i</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">;</span> <span class="o">--</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(((</span><span class="n">n</span> <span class="o">&gt;&gt;</span> <span class="p">(</span><span class="n">i</span> <span class="o">-</span> <span class="mi">1</span><span class="p">))</span> <span class="o">&amp;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">bits</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">while</span> <span class="p">(</span><span class="o">!</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">())</span> <span class="o">++</span><span class="n">bits</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="n">value_type</span> <span class="n">result</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">bits</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">result</span> <span class="o">=</span> <span class="p">(</span><span class="n">result</span> <span class="o">&lt;&lt;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">|</span> <span class="p">(</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">()</span> <span class="o">?</span> <span class="n">value_type</span><span class="p">{</span><span class="mi">1</span><span class="p">}</span> <span class="o">:</span> <span class="n">value_type</span><span class="p">{</span><span class="mi">0</span><span class="p">});</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">result</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>Gamma is asymptotically optimal under a prior proportional to \(1/n^2\). The full story, including the Kraft inequality, optimal code lengths, and the connection to universal codes, belongs to a forthcoming Information Theory by Construction series. For now: gamma is the code you use when you know values are small integers but not their distribution.</p>
<h2 id="combinator-opt">Combinator: Opt</h2>
<p><code>Opt&lt;C&gt;</code> encodes <code>std::optional&lt;T&gt;</code> given a codec <code>C</code> for <code>T</code>. The wire format is one tag bit (1 = present, 0 = absent) followed by <code>C</code>&rsquo;s encoding if present.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">C</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Opt</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">optional</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">C</span><span class="o">::</span><span class="n">value_type</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="k">const</span> <span class="n">value_type</span><span class="o">&amp;</span> <span class="n">v</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">v</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">true</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="n">C</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="o">*</span><span class="n">v</span><span class="p">,</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">false</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">())</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="k">return</span> <span class="n">C</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">std</span><span class="o">::</span><span class="n">nullopt</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>The type-algebra reading: <code>optional&lt;T&gt;</code> is the coproduct of <code>T</code> with the unit type (a single absent value). In type algebra, this is written \(1 + T\). The tag bit selects the branch; the payload is present only in the right branch.</p>
<p>This is the first sign of functoriality. <code>Opt</code> takes a codec and returns a codec. It acts on codecs the same way <code>optional</code> acts on types. The structure of the operation is preserved: the algebraic decomposition of the type is mirrored exactly in the bit encoding.</p>
<h2 id="combinator-either">Combinator: Either</h2>
<p><code>Either&lt;A, B&gt;</code> encodes <code>std::variant&lt;TA, TB&gt;</code> given codecs <code>A</code> for <code>TA</code> and <code>B</code> for <code>TB</code>. One tag bit selects the branch; the payload follows.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">A</span><span class="p">,</span> <span class="k">typename</span> <span class="n">B</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Either</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">variant</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">A</span><span class="o">::</span><span class="n">value_type</span><span class="p">,</span> <span class="k">typename</span> <span class="n">B</span><span class="o">::</span><span class="n">value_type</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="k">const</span> <span class="n">value_type</span><span class="o">&amp;</span> <span class="n">v</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">v</span><span class="p">.</span><span class="n">index</span><span class="p">()</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">false</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="n">A</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">get</span><span class="o">&lt;</span><span class="mi">0</span><span class="o">&gt;</span><span class="p">(</span><span class="n">v</span><span class="p">),</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">true</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="n">B</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">get</span><span class="o">&lt;</span><span class="mi">1</span><span class="o">&gt;</span><span class="p">(</span><span class="n">v</span><span class="p">),</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">())</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="k">return</span> <span class="n">value_type</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">in_place_index</span><span class="o">&lt;</span><span class="mi">1</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">B</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">)};</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">value_type</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">in_place_index</span><span class="o">&lt;</span><span class="mi">0</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">A</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">)};</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>Type algebra: \(A + B\), the binary coproduct. For the worked example in this post, we need a ternary variant, so the implementation also provides <code>Either3</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">A</span><span class="p">,</span> <span class="k">typename</span> <span class="n">B</span><span class="p">,</span> <span class="k">typename</span> <span class="n">C</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Either3</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">variant</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">A</span><span class="o">::</span><span class="n">value_type</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                                    <span class="k">typename</span> <span class="n">B</span><span class="o">::</span><span class="n">value_type</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                                    <span class="k">typename</span> <span class="n">C</span><span class="o">::</span><span class="n">value_type</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="k">const</span> <span class="n">value_type</span><span class="o">&amp;</span> <span class="n">v</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">idx</span> <span class="o">=</span> <span class="n">v</span><span class="p">.</span><span class="n">index</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">        <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">((</span><span class="n">idx</span> <span class="o">&amp;</span> <span class="mh">0x1</span><span class="p">)</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">((</span><span class="n">idx</span> <span class="o">&amp;</span> <span class="mh">0x2</span><span class="p">)</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">idx</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="n">A</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">get</span><span class="o">&lt;</span><span class="mi">0</span><span class="o">&gt;</span><span class="p">(</span><span class="n">v</span><span class="p">),</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">else</span> <span class="nf">if</span> <span class="p">(</span><span class="n">idx</span> <span class="o">==</span> <span class="mi">1</span><span class="p">)</span> <span class="n">B</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">get</span><span class="o">&lt;</span><span class="mi">1</span><span class="o">&gt;</span><span class="p">(</span><span class="n">v</span><span class="p">),</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">else</span> <span class="n">C</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">get</span><span class="o">&lt;</span><span class="mi">2</span><span class="o">&gt;</span><span class="p">(</span><span class="n">v</span><span class="p">),</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">idx</span> <span class="o">=</span> <span class="p">(</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">()</span> <span class="o">?</span> <span class="mi">1u</span> <span class="o">:</span> <span class="mi">0u</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">idx</span> <span class="o">|=</span> <span class="p">(</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">()</span> <span class="o">?</span> <span class="mi">2u</span> <span class="o">:</span> <span class="mi">0u</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">idx</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="k">return</span> <span class="n">value_type</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">in_place_index</span><span class="o">&lt;</span><span class="mi">0</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">A</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">)};</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">idx</span> <span class="o">==</span> <span class="mi">1</span><span class="p">)</span> <span class="k">return</span> <span class="n">value_type</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">in_place_index</span><span class="o">&lt;</span><span class="mi">1</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">B</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">)};</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">value_type</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">in_place_index</span><span class="o">&lt;</span><span class="mi">2</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">C</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">)};</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>The tag for <code>Either3</code> uses two bits, encoding the branch index as a 2-bit integer (LSB first). This costs \(\lceil\log_2 N\rceil\) bits for N branches regardless of how often each branch occurs. When branch probabilities differ significantly, this is wasteful: Huffman-coded tags or arithmetic coding over the branch distribution would do better. That improvement belongs in the forthcoming Information Theory by Construction series. The flat tag scheme is correct and simple; optimality can be layered on separately.</p>
<h2 id="combinator-pair">Combinator: Pair</h2>
<p><code>Pair&lt;A, B&gt;</code> encodes <code>std::pair&lt;TA, TB&gt;</code>: <code>A</code>&rsquo;s encoding followed immediately by <code>B</code>&rsquo;s encoding, with no delimiter.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">A</span><span class="p">,</span> <span class="k">typename</span> <span class="n">B</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Pair</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">pair</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">A</span><span class="o">::</span><span class="n">value_type</span><span class="p">,</span> <span class="k">typename</span> <span class="n">B</span><span class="o">::</span><span class="n">value_type</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="k">const</span> <span class="n">value_type</span><span class="o">&amp;</span> <span class="n">v</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">A</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">v</span><span class="p">.</span><span class="n">first</span><span class="p">,</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">B</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">v</span><span class="p">.</span><span class="n">second</span><span class="p">,</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">auto</span> <span class="n">a</span> <span class="o">=</span> <span class="n">A</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">auto</span> <span class="n">b</span> <span class="o">=</span> <span class="n">B</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">value_type</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="n">a</span><span class="p">),</span> <span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="n">b</span><span class="p">)};</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>Type algebra: \(A \times B\), the binary product. Two bit streams concatenated, recovered in sequence. No length header, no delimiter bit.</p>
<p>The reason this works is prefix-freeness: because <code>A</code>&rsquo;s codec is prefix-free, the decoder always knows exactly when <code>A</code>&rsquo;s payload ends and <code>B</code>&rsquo;s payload begins. The boundary is implicit in the structure of the code. If the codecs were not prefix-free, concatenation would be ambiguous.</p>
<p>See <a href="/post/2026-05-prefix-free-stepanov/">When Lists Become Bits</a> for why prefix-freeness makes concatenation reversible.</p>
<h2 id="combinator-vec">Combinator: Vec</h2>
<p><code>Vec&lt;C&gt;</code> encodes <code>std::vector&lt;T&gt;</code> given a codec <code>C</code> for <code>T</code>. The wire format is a gamma-coded length followed by <code>C</code>&rsquo;s encoding of each element.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">C</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Vec</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">C</span><span class="o">::</span><span class="n">value_type</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="k">const</span> <span class="n">value_type</span><span class="o">&amp;</span> <span class="n">v</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">Gamma</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">v</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">+</span> <span class="mi">1</span><span class="p">,</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="k">const</span> <span class="k">auto</span><span class="o">&amp;</span> <span class="nl">x</span> <span class="p">:</span> <span class="n">v</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">C</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">n</span> <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">Gamma</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">value_type</span> <span class="n">result</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="n">result</span><span class="p">.</span><span class="n">reserve</span><span class="p">(</span><span class="n">n</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">result</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span><span class="n">C</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">));</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">result</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>The <code>+1</code> bias in <code>encode</code> handles the mismatch between Gamma (which requires inputs \(\geq 1\)) and vector sizes (which can be 0). The decoded length subtracts 1 to recover the original size.</p>
<p>Type algebra: <code>vector&lt;T&gt;</code> is <code>List(T)</code>, the free monoid on <code>T</code>. The <a href="/post/2026-03-free-algebra-stepanov/">free-algebra post</a> showed that <code>fold</code> is the unique monoid homomorphism from lists to any monoid: that uniqueness is the universal property of the free monoid. <code>Vec&lt;C&gt;</code> is the lift of that universal property to bit space: it extends a per-element encoding <code>C</code> to the entire list by applying <code>C</code> to each element in sequence.</p>
<p>That lift always exists (it is just fold). The deeper question is whether it is invertible: can the decoder recover the list from the bit string? The answer depends on prefix-freeness, and the analysis belongs in <a href="/post/2026-05-prefix-free-stepanov/">When Lists Become Bits</a>. The short version: <code>Vec&lt;C&gt;</code> works when <code>C</code> is prefix-free, and Gamma is prefix-free.</p>
<h2 id="the-functorial-framing">The Functorial Framing</h2>
<p>Each combinator corresponds to an operation in the algebra of types:</p>
<table>
	<thead>
			<tr>
					<th>Combinator</th>
					<th>Type algebra</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>Opt&lt;C&gt;</code></td>
					<td>\(1 + T\) (coproduct with unit)</td>
			</tr>
			<tr>
					<td><code>Either&lt;A, B&gt;</code></td>
					<td>\(A + B\) (binary coproduct)</td>
			</tr>
			<tr>
					<td><code>Pair&lt;A, B&gt;</code></td>
					<td>\(A \times B\) (binary product)</td>
			</tr>
			<tr>
					<td><code>Vec&lt;C&gt;</code></td>
					<td>\(\text{List}(T)\) (free monoid on T)</td>
			</tr>
	</tbody>
</table>
<p>This table is not a coincidence. Each combinator takes codecs as inputs and returns a codec as output, mirroring the way the corresponding type constructor takes types and returns a type. The mapping from types to codecs respects the type algebra: if you know how to encode <code>A</code> and <code>B</code>, the combinators give you, for free, how to encode <code>A + B</code>, <code>A \times B</code>, <code>1 + A</code>, and <code>\text{List}(A)</code>.</p>
<p>This is the bridge thesis: <strong>the algebraic structure of a type determines its codec, the same way it determines its algorithms.</strong> Stepanov&rsquo;s original claim covered algorithms. These two posts extend it to encodings.</p>
<p>Each combinator is functorial in a precise sense. <code>Vec</code>, for instance, maps a morphism between codec targets (a function <code>f: TA -&gt; TB</code> liftable to <code>f*: vector&lt;TA&gt; -&gt; vector&lt;TB&gt;</code>) to a corresponding transformation between codecs. The formal categorical statement is deferred. For the purposes of this post, the table is the claim: structure-in, structure-out.</p>
<p>Contrast this with the alternative: one-off serialization code written directly for each compound type. That code is not wrong, but it is redundant. The structure of the type already tells you how to encode it. If your serialization code reflects that structure, it is derivable and correct by construction. If it does not, you have extra maintenance burden, a second place to update when the type changes.</p>
<p>The homomorphism post showed that structure-preserving maps are the natural morphisms between algebraic structures. Codecs, under this framing, are structure-preserving maps from the type algebra to the bit-string algebra. The combinators are the proof that these maps exist and compose.</p>
<h2 id="the-composed-example">The Composed Example</h2>
<p>To make this concrete, three auxiliary codecs appear in the worked example. Each is short enough to state plainly.</p>
<p><code>Bool</code> encodes a boolean as a single bit:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Bool</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="kt">bool</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="n">value_type</span> <span class="n">v</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span> <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="n">v</span><span class="p">);</span> <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span> <span class="k">return</span> <span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">();</span> <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p><code>Signed&lt;C&gt;</code> wraps an unsigned codec using zigzag encoding, mapping signed integers to non-negative values and then applying a bias so Gamma (which requires \(\geq 1\)) can handle zero:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">C</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Signed</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="kt">int64_t</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="n">value_type</span> <span class="n">v</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span> <span class="n">z</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">v</span> <span class="o">&lt;</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">z</span> <span class="o">=</span> <span class="p">(</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="o">&gt;</span><span class="p">(</span><span class="o">-</span><span class="p">(</span><span class="n">v</span> <span class="o">+</span> <span class="mi">1</span><span class="p">))</span> <span class="o">&lt;&lt;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">|</span> <span class="mi">1u</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">z</span> <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">v</span><span class="p">)</span> <span class="o">&lt;&lt;</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="n">C</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">z</span> <span class="o">+</span> <span class="mi">1</span><span class="p">,</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span> <span class="n">z</span> <span class="o">=</span> <span class="n">C</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">z</span> <span class="o">&amp;</span> <span class="mi">1u</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="k">return</span> <span class="o">-</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">int64_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">z</span> <span class="o">&gt;&gt;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">int64_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">z</span> <span class="o">&gt;&gt;</span> <span class="mi">1</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p><code>String</code> is gamma-coded length followed by 8 bits per character. It is, conceptually, <code>Vec&lt;Byte&gt;</code> where <code>Byte</code> is an 8-bit leaf codec. The combinator pattern applies recursively: a string is a list of bytes, and its codec is the list combinator applied to a byte codec.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">String</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="k">const</span> <span class="n">value_type</span><span class="o">&amp;</span> <span class="n">s</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">Gamma</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">s</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">+</span> <span class="mi">1</span><span class="p">,</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="kt">char</span> <span class="nl">c</span> <span class="p">:</span> <span class="n">s</span><span class="p">)</span> <span class="n">Byte</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">c</span><span class="p">),</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">n</span> <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">Gamma</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">value_type</span> <span class="n">s</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="n">s</span><span class="p">.</span><span class="n">reserve</span><span class="p">(</span><span class="n">n</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">            <span class="n">s</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="kt">char</span><span class="o">&gt;</span><span class="p">(</span><span class="n">Byte</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">)));</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">s</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>Now the worked example. A configuration record: each entry has a string key and an optional value, where the value can be an integer, a string, or a boolean.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">using</span> <span class="n">Value</span>  <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">variant</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">int64_t</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="p">,</span> <span class="kt">bool</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Entry</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="n">key</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">optional</span><span class="o">&lt;</span><span class="n">Value</span><span class="o">&gt;</span> <span class="n">value</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="kt">bool</span> <span class="k">operator</span><span class="o">==</span><span class="p">(</span><span class="k">const</span> <span class="n">Entry</span><span class="o">&amp;</span><span class="p">)</span> <span class="k">const</span> <span class="o">=</span> <span class="k">default</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">using</span> <span class="n">Config</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">Entry</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">using</span> <span class="n">ValueCodec</span>  <span class="o">=</span> <span class="n">Either3</span><span class="o">&lt;</span><span class="n">Signed</span><span class="o">&lt;</span><span class="n">Gamma</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">String</span><span class="p">,</span> <span class="n">Bool</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">using</span> <span class="n">EntryCodec</span>  <span class="o">=</span> <span class="n">Pair</span><span class="o">&lt;</span><span class="n">String</span><span class="p">,</span> <span class="n">Opt</span><span class="o">&lt;</span><span class="n">ValueCodec</span><span class="o">&gt;&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">using</span> <span class="n">ConfigCodec</span> <span class="o">=</span> <span class="n">Vec</span><span class="o">&lt;</span><span class="n">EntryCodec</span><span class="o">&gt;</span><span class="p">;</span>
</span></span></code></pre></div><p>The round-trip test from <code>test_codecs_functors.cpp</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="n">Config</span> <span class="n">c</span> <span class="o">=</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">Entry</span><span class="p">{</span><span class="s">&#34;port&#34;</span><span class="p">,</span>    <span class="n">Value</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">in_place_index</span><span class="o">&lt;</span><span class="mi">0</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="kt">int64_t</span><span class="p">{</span><span class="mi">8080</span><span class="p">}}},</span>
</span></span><span class="line"><span class="cl">    <span class="n">Entry</span><span class="p">{</span><span class="s">&#34;name&#34;</span><span class="p">,</span>    <span class="n">Value</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">in_place_index</span><span class="o">&lt;</span><span class="mi">1</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="p">{</span><span class="s">&#34;alpha&#34;</span><span class="p">}}},</span>
</span></span><span class="line"><span class="cl">    <span class="n">Entry</span><span class="p">{</span><span class="s">&#34;verbose&#34;</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">nullopt</span><span class="p">},</span>
</span></span><span class="line"><span class="cl">    <span class="n">Entry</span><span class="p">{</span><span class="s">&#34;debug&#34;</span><span class="p">,</span>   <span class="n">Value</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">in_place_index</span><span class="o">&lt;</span><span class="mi">2</span><span class="o">&gt;</span><span class="p">,</span> <span class="nb">true</span><span class="p">}},</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// Convert to the form ConfigCodec expects (vector of pairs).
</span></span></span><span class="line"><span class="cl"><span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">pair</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">optional</span><span class="o">&lt;</span><span class="n">Value</span><span class="o">&gt;&gt;&gt;</span> <span class="n">as_pairs</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">for</span> <span class="p">(</span><span class="k">const</span> <span class="k">auto</span><span class="o">&amp;</span> <span class="nl">e</span> <span class="p">:</span> <span class="n">c</span><span class="p">)</span> <span class="n">as_pairs</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span><span class="n">to_pair</span><span class="p">(</span><span class="n">e</span><span class="p">));</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">std</span><span class="o">::</span><span class="n">array</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span><span class="p">,</span> <span class="mi">1024</span><span class="o">&gt;</span> <span class="n">buf</span><span class="p">{};</span>
</span></span><span class="line"><span class="cl"><span class="n">BitWriter</span> <span class="nf">w</span><span class="p">(</span><span class="n">buf</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="n">ConfigCodec</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">as_pairs</span><span class="p">,</span> <span class="n">w</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="n">w</span><span class="p">.</span><span class="n">align</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">BitReader</span> <span class="nf">r</span><span class="p">({</span><span class="n">buf</span><span class="p">.</span><span class="n">data</span><span class="p">(),</span> <span class="n">w</span><span class="p">.</span><span class="n">bytes_written</span><span class="p">()});</span>
</span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">decoded_pairs</span> <span class="o">=</span> <span class="n">ConfigCodec</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">r</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="c1">// ... reconstruct Entry objects and assert equality
</span></span></span></code></pre></div><p>The codec types mirror the value types exactly. <code>Config</code> is <code>vector&lt;Entry&gt;</code>; <code>ConfigCodec</code> is <code>Vec&lt;EntryCodec&gt;</code>. <code>Entry</code> is <code>pair&lt;string, optional&lt;Value&gt;&gt;</code>; <code>EntryCodec</code> is <code>Pair&lt;String, Opt&lt;ValueCodec&gt;&gt;</code>. <code>Value</code> is <code>variant&lt;int64_t, string, bool&gt;</code>; <code>ValueCodec</code> is <code>Either3&lt;Signed&lt;Gamma&gt;, String, Bool&gt;</code>.</p>
<p>No manual layout. No marshaling. No hand-placed length headers. The encoding emerges from the type structure, because the codecs are constructed from the same algebra as the types.</p>
<h2 id="why-this-matters">Why This Matters</h2>
<p>The zero-copy invariant emerges from this construction. Because each codec&rsquo;s wire format is exactly what the structural recursion produces, the bit pattern in the buffer IS the value, recoverable by the same structural recursion in reverse. Encoding and decoding are not two separate processes that maintain a shared contract; they are a single algebraic object viewed from two directions.</p>
<p>This dissolves marshaling rather than solving it. Traditional serialization has two representations of a value: the in-memory layout and the wire format. These representations must be kept in sync, and every time the type changes, the serialization code must change too. Under the combinator approach, there is one representation: the algebraic structure of the type. The codec derives from that structure. Change the type, and the codec follows automatically.</p>
<p>This is what Stepanov meant when he insisted that algorithms should arise from algebraic structure. Here, it is not algorithms but encodings. The point is the same: when you see the structure clearly, the implementation becomes obvious. The hard work is seeing the structure.</p>
<p>The <a href="/post/2026-01-19-duality-stepanov/">duality post</a> showed that encode and decode are duals. Under this combinator framing, that duality decomposes structurally: the encode-decode duality of a compound type is the product of the encode-decode dualities of its parts.</p>
<h2 id="further-reading">Further Reading</h2>
<p><strong>Next in series:</strong> <a href="/post/2026-05-prefix-free-stepanov/">When Lists Become Bits</a> develops the reason <code>Vec&lt;C&gt;</code> works: prefix-freeness is exactly the condition that makes the free-monoid lift to bit space injective.</p>
<p><strong>Background in this series:</strong></p>
<ul>
<li><a href="/post/2026-03-free-algebra-stepanov/">Why Lists and Polynomials Are Universal</a>: the free-monoid construction that <code>Vec</code> lifts to bit space.</li>
<li><a href="/post/2026-03-homomorphism-stepanov/">The Maps Between Structures</a>: homomorphisms as the natural morphisms between algebraic structures; the combinators are homomorphisms from the type algebra to the codec algebra.</li>
<li><a href="/post/2026-01-19-duality-stepanov/">Structure, Duality, and the Shape of Computation</a>: encode and decode as dual maps; the duality decomposes structurally under the combinators.</li>
</ul>
<p><strong>The production library:</strong> PFC (<a href="https://github.com/queelius/pfc">github.com/queelius/pfc</a>) provides the production version of this design: a richer codec library, full STL integration, and 31k+ test assertions. See <code>include/pfc/algebraic.hpp</code> for the combinators and <code>include/pfc/codecs.hpp</code> for the universal-codes catalog.</p>
]]></content:encoded><media:content url="https://metafunctor.com/post/2026-05-codecs-functors-stepanov/card.jpg" medium="image"><media:title type="plain">Category-Theory</media:title></media:content><category>C++</category><category>generic-programming</category><category>codecs</category><category>information-theory</category><category>category-theory</category><category>functors</category></item><item><title>When Lists Become Bits</title><link>https://metafunctor.com/post/2026-05-prefix-free-stepanov/</link><pubDate>Thu, 23 Apr 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2026-05-prefix-free-stepanov/</guid><description>Prefix-freeness is the property that lifts the free-monoid construction into bit space.</description><content:encoded><![CDATA[<p><em>The free monoid on a type lifts to bit space. It lifts injectively only when the element codec is prefix-free.</em></p>
<h2 id="prefix-free-codes-and-the-free-monoid">Prefix-Free Codes and the Free Monoid</h2>
<p>You have a list of unsigned integers. Encode the list as a single bit string.</p>
<p>Fixed-width encoding wastes space. If you allocate 64 bits per integer, small values like 1 or 7 cost as much as values near \(2^{64}\). Variable-width encoding recovers that space, but immediately raises a harder question: where does one encoded integer end and the next begin?</p>
<p>Two escape routes. First, prefix each encoded item with its length. That works, but the length headers are overhead, and you now need a codec for the lengths as well. Second, choose a code where the structure of the codewords makes boundaries unambiguous without any headers. These are prefix-free codes, and this is the right answer, in a precise categorical sense.</p>
<p>The &ldquo;precise categorical sense&rdquo; is what this post develops. Encoding a list as the concatenation of encoded elements is a monoid homomorphism from the free monoid on \(T\) to the monoid of bit strings under concatenation. The universal property of the free monoid guarantees this homomorphism always exists. The question of whether the decoder can invert it comes down to exactly one property of the element codec: whether it is prefix-free.</p>
<h2 id="the-free-monoid-recalled">The Free Monoid, Recalled</h2>
<p>A monoid is a set with an associative binary operation and an identity element. The free monoid on a set \(S\) is the set of all finite sequences of elements from \(S\), with concatenation as the operation and the empty sequence as the identity.</p>
<p>&ldquo;Free&rdquo; means no equations hold except those forced by the monoid axioms. Nothing is identified with anything else. If you need commutativity or idempotency, you quotient the free monoid by additional equations. But the free monoid itself imposes nothing beyond associativity and identity.</p>
<p>The universal property says: given any monoid \(M\) and any function \(f: S \to M\), there is exactly one monoid homomorphism \(\hat{f}: \text{Free}(S) \to M\) that extends \(f\). That unique extension is fold:</p>
$$\hat{f}([x_1, x_2, \ldots, x_n]) = f(x_1) \cdot f(x_2) \cdot \cdots \cdot f(x_n)$$<p>where \(\cdot\) is the operation in \(M\). The <a href="/post/2026-03-free-algebra-stepanov/">free-algebra post</a> develops this in full. For this post, the one fact that matters is that fold is canonical: it is the unique way to extend a per-element map to a list-consuming function that respects the monoid structure.</p>
<h2 id="the-lifting-question">The Lifting Question</h2>
<p>Fix a type \(T\) and a codec \(C\) for \(T\): a function \(\text{encode}: T \to \text{BitString}\). The monoid we care about here is \((\text{BitString}, +\!\!+, \varepsilon)\), the set of all finite bit strings under concatenation with the empty string as identity.</p>
<p>We want to extend \(\text{encode}\) to a function on lists of \(T\):</p>
$$\text{encode\_list}([x_1, \ldots, x_n]) = \text{encode}(x_1) +\!\!+ \cdots +\!\!+ \text{encode}(x_n)$$<p>By the universal property, this lift always exists. It is fold applied to the encoding function over the free monoid of bit strings. You do not need to do anything clever. The homomorphism exists by construction.</p>
<p>The real question is whether \(\text{encode\_list}\) is injective: given a bit string \(b\), can we recover the unique list \([x_1, \ldots, x_n]\) that encodes to \(b\)?</p>
<p>This is not guaranteed. The lift always exists. It is not always invertible.</p>
<h2 id="when-the-lift-fails-a-counter-example">When the Lift Fails: A Counter-Example</h2>
<p>Consider a code that is not prefix-free. Assign codewords as follows:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">A -&gt; &#34;0&#34;
</span></span><span class="line"><span class="cl">B -&gt; &#34;01&#34;
</span></span><span class="line"><span class="cl">C -&gt; &#34;10&#34;
</span></span><span class="line"><span class="cl">D -&gt; &#34;010&#34;
</span></span></code></pre></div><p>The problem: <code>&quot;0&quot;</code> is a prefix of <code>&quot;01&quot;</code> and <code>&quot;010&quot;</code>. <code>&quot;01&quot;</code> is a prefix of <code>&quot;010&quot;</code>. So when we concatenate codewords to encode a list, the decoder cannot determine where one codeword ends and the next begins.</p>
<p>The bit string <code>&quot;010&quot;</code> has three valid parses:</p>
<ul>
<li><code>[D]</code>, because <code>D</code> maps to <code>&quot;010&quot;</code> directly.</li>
<li><code>[A, C]</code>, because <code>&quot;0&quot; ++ &quot;10&quot; = &quot;010&quot;</code>.</li>
<li><code>[B, A]</code>, because <code>&quot;01&quot; ++ &quot;0&quot; = &quot;010&quot;</code>.</li>
</ul>
<p>This is verified concretely in the test suite:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="n">TEST</span><span class="p">(</span><span class="n">PrefixFreeTest</span><span class="p">,</span> <span class="n">NonPrefixFreeIsAmbiguous</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// A &#34;code&#34; that is NOT prefix-free.
</span></span></span><span class="line"><span class="cl">    <span class="c1">// &#34;0&#34;  is a prefix of &#34;01&#34; and &#34;010&#34;.
</span></span></span><span class="line"><span class="cl">    <span class="c1">// &#34;01&#34; is a prefix of &#34;010&#34;.
</span></span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">map</span><span class="o">&lt;</span><span class="kt">char</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="o">&gt;</span> <span class="n">code</span><span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="p">{</span><span class="sc">&#39;A&#39;</span><span class="p">,</span> <span class="s">&#34;0&#34;</span><span class="p">},</span>
</span></span><span class="line"><span class="cl">        <span class="p">{</span><span class="sc">&#39;B&#39;</span><span class="p">,</span> <span class="s">&#34;01&#34;</span><span class="p">},</span>
</span></span><span class="line"><span class="cl">        <span class="p">{</span><span class="sc">&#39;C&#39;</span><span class="p">,</span> <span class="s">&#34;10&#34;</span><span class="p">},</span>
</span></span><span class="line"><span class="cl">        <span class="p">{</span><span class="sc">&#39;D&#39;</span><span class="p">,</span> <span class="s">&#34;010&#34;</span><span class="p">},</span>
</span></span><span class="line"><span class="cl">    <span class="p">};</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="c1">// [D], [A, C], and [B, A] all encode to the same bit string &#34;010&#34;.
</span></span></span><span class="line"><span class="cl">    <span class="n">EXPECT_EQ</span><span class="p">(</span><span class="n">encode_string</span><span class="p">(</span><span class="n">code</span><span class="p">,</span> <span class="s">&#34;D&#34;</span><span class="p">),</span>  <span class="s">&#34;010&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">EXPECT_EQ</span><span class="p">(</span><span class="n">encode_string</span><span class="p">(</span><span class="n">code</span><span class="p">,</span> <span class="s">&#34;AC&#34;</span><span class="p">),</span> <span class="s">&#34;010&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">EXPECT_EQ</span><span class="p">(</span><span class="n">encode_string</span><span class="p">(</span><span class="n">code</span><span class="p">,</span> <span class="s">&#34;BA&#34;</span><span class="p">),</span> <span class="s">&#34;010&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="c1">// Greedy left-to-right decoding cannot pick a unique answer; the multi-parse
</span></span></span><span class="line"><span class="cl">    <span class="c1">// decoder finds all three valid parses.
</span></span></span><span class="line"><span class="cl">    <span class="k">auto</span> <span class="n">parses</span> <span class="o">=</span> <span class="n">enumerate_parses</span><span class="p">(</span><span class="n">code</span><span class="p">,</span> <span class="s">&#34;010&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">EXPECT_EQ</span><span class="p">(</span><span class="n">parses</span><span class="p">.</span><span class="n">size</span><span class="p">(),</span> <span class="mi">3u</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>The <code>enumerate_parses</code> function is in <code>prefix_free.hpp</code>. It does a recursive scan: at each position in the bit string, it tries every codeword that matches at that position, and recurses on the remainder. For a prefix-free code it would return exactly one parse for any valid encoding. For this code, it returns three.</p>
<p>The decoder genuinely has no principled way to choose. The encoding function is not injective on lists; it maps distinct lists to the same bit string. The free-monoid lift exists but fails to be invertible.</p>
<h2 id="prefix-free-codes">Prefix-Free Codes</h2>
<p>A code is prefix-free if no codeword is a prefix of any other codeword. The definition rules out the failure mode above: if no codeword is a prefix of another, then when the decoder reads bits left-to-right and finds a complete codeword, it knows it has found one. There is no ambiguity about whether more bits belong to the current codeword or start a new one.</p>
<p>The canonical visualization is a trie (a binary tree for binary codes). Each codeword corresponds to a root-to-leaf path: go left for 0, right for 1, and the path terminates when you reach a leaf. Prefix-freeness means no codeword ends at an internal node. Every codeword&rsquo;s path ends at a leaf, and leaves are not ancestors of other leaves.</p>
<p>The two codecs in this post, Unary and Gamma, are both prefix-free.</p>
<p>Unary encodes positive integers: the codeword for \(n\) is \((n-1)\) zero bits followed by a one bit.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Unary</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="n">value_type</span> <span class="n">n</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">assert</span><span class="p">(</span><span class="n">n</span> <span class="o">&gt;=</span> <span class="mi">1</span> <span class="o">&amp;&amp;</span> <span class="s">&#34;Unary is undefined for n = 0&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">value_type</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">false</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">true</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">value_type</span> <span class="n">n</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">while</span> <span class="p">(</span><span class="o">!</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">())</span> <span class="o">++</span><span class="n">n</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">n</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>Prefix-freeness of Unary is immediate: every codeword ends with a <code>1</code>, and no other codeword is a prefix of that. The decoder reads zeros until it sees a one. There is no ambiguity.</p>
<p>Gamma is restated here from post 1. The codeword for \(n \geq 1\) is \(\lfloor\log_2 n\rfloor\) zero bits, then a one bit, then the \(\lfloor\log_2 n\rfloor\) lower bits of \(n\) in MSB-first order.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Gamma</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="n">value_type</span> <span class="n">n</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">assert</span><span class="p">(</span><span class="n">n</span> <span class="o">&gt;=</span> <span class="mi">1</span> <span class="o">&amp;&amp;</span> <span class="s">&#34;Gamma is undefined for n = 0&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">bits</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">bit_width</span><span class="p">(</span><span class="n">n</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">bits</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">false</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">true</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="n">bits</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span> <span class="n">i</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">;</span> <span class="o">--</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(((</span><span class="n">n</span> <span class="o">&gt;&gt;</span> <span class="p">(</span><span class="n">i</span> <span class="o">-</span> <span class="mi">1</span><span class="p">))</span> <span class="o">&amp;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">bits</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">while</span> <span class="p">(</span><span class="o">!</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">())</span> <span class="o">++</span><span class="n">bits</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="n">value_type</span> <span class="n">result</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">bits</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">result</span> <span class="o">=</span> <span class="p">(</span><span class="n">result</span> <span class="o">&lt;&lt;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">|</span> <span class="p">(</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">()</span> <span class="o">?</span> <span class="n">value_type</span><span class="p">{</span><span class="mi">1</span><span class="p">}</span> <span class="o">:</span> <span class="n">value_type</span><span class="p">{</span><span class="mi">0</span><span class="p">});</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">result</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>Gamma is prefix-free: the leading-zero count uniquely determines how many data bits follow. Once the decoder reads the <code>1</code> bit that terminates the unary prefix, it knows exactly how many more bits to consume. The boundary is unambiguous.</p>
<h2 id="the-free-monoid-lift-works-for-prefix-free-codes">The Free-Monoid Lift Works for Prefix-Free Codes</h2>
<p>Now we can state the main result cleanly.</p>
<p>Fix a type \(T\) and a prefix-free codec \(C\) for \(T\). The encoding function specializes the universal property construction: \(S = T\), \(M = (\text{BitString}, +\!\!+, \varepsilon)\), and \(f = \text{encode}_C\). The lift \(\text{encode\_list}\) always exists as fold, as argued above. The new claim is that it is injective when \(C\) is prefix-free.</p>
<p>The argument in the prefix-free direction: given a bit string \(b\) produced by \(\text{encode\_list}\), a greedy left-to-right decoder works as follows. Start at position 0. Find the unique codeword that matches the bits starting at position 0. This codeword is unique because: (a) some codeword must match (otherwise \(b\) was not a valid encoding), and (b) no two codewords share a prefix, so at most one codeword can match at any position. Advance past that codeword. Repeat. The decoder always terminates with a unique parse.</p>
<p>The argument in the other direction: if \(C\) is not prefix-free, then there exist symbols \(a\) and \(b\) such that \(\text{encode}(a)\) is a prefix of \(\text{encode}(b)\). Write \(\text{encode}(b) = \text{encode}(a) +\!\!+ s\) for some non-empty string \(s\). If \(s\) is itself a prefix of some codeword sequence, then \([b]\) and \([a] +\!\!+ \text{decode}(s)\) produce the same bit string. This is exactly the failure mode in the counter-example above.</p>
<p>The bigger claim is that, when \(C\) is prefix-free, the image of \(\text{encode\_list}\) forms a submonoid of \(\text{BitString}\) that is isomorphic to the free monoid on \(T\). The encoding is a monoid isomorphism. &ldquo;Lists of \(T\)&rdquo; and &ldquo;bit strings built from \(C\)&rsquo;s codewords&rdquo; are the same monoid, witnessed by \(\text{encode\_list}\) and its inverse \(\text{decode\_list}\).</p>
<p><code>Vec&lt;C&gt;</code> from <a href="/post/2026-05-codecs-functors-stepanov/">Bits Follow Types</a> is this isomorphism made concrete in C++.</p>
<h2 id="implementation-and-round-trip">Implementation and Round-Trip</h2>
<p>Post 2 has its own minimal <code>Vec&lt;C&gt;</code> implementation, independent of post 1&rsquo;s <code>codecs_functors.hpp</code>. The decision is intentional: each post in this series is self-contained, following the convention that posts can be read in any order. The implementations are identical in spirit.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">C</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Vec</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">C</span><span class="o">::</span><span class="n">value_type</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="k">const</span> <span class="n">value_type</span><span class="o">&amp;</span> <span class="n">v</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">Gamma</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">v</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">+</span> <span class="mi">1</span><span class="p">,</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="k">const</span> <span class="k">auto</span><span class="o">&amp;</span> <span class="nl">x</span> <span class="p">:</span> <span class="n">v</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">C</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">n</span> <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">Gamma</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">value_type</span> <span class="n">result</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="n">result</span><span class="p">.</span><span class="n">reserve</span><span class="p">(</span><span class="n">n</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">result</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span><span class="n">C</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">));</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">result</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>The length is gamma-coded as <code>size + 1</code> because Gamma requires inputs \(\geq 1\) while vectors can be empty. The decoder subtracts 1 to recover the actual size.</p>
<p>The round-trip test:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="n">TEST</span><span class="p">(</span><span class="n">PrefixFreeTest</span><span class="p">,</span> <span class="n">VecOfGammaRoundTrip</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">Codec</span> <span class="o">=</span> <span class="n">Vec</span><span class="o">&lt;</span><span class="n">Gamma</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">Codec</span><span class="o">::</span><span class="n">value_type</span> <span class="n">v</span><span class="p">{</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">8</span><span class="p">,</span> <span class="mi">13</span><span class="p">,</span> <span class="mi">21</span><span class="p">};</span>
</span></span><span class="line"><span class="cl">    <span class="n">EXPECT_EQ</span><span class="p">(</span><span class="n">round_trip</span><span class="o">&lt;</span><span class="n">Codec</span><span class="o">&gt;</span><span class="p">(</span><span class="n">v</span><span class="p">),</span> <span class="n">v</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">TEST</span><span class="p">(</span><span class="n">PrefixFreeTest</span><span class="p">,</span> <span class="n">VecEmptyRoundTrip</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">Codec</span> <span class="o">=</span> <span class="n">Vec</span><span class="o">&lt;</span><span class="n">Gamma</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">Codec</span><span class="o">::</span><span class="n">value_type</span> <span class="n">v</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">EXPECT_EQ</span><span class="p">(</span><span class="n">round_trip</span><span class="o">&lt;</span><span class="n">Codec</span><span class="o">&gt;</span><span class="p">(</span><span class="n">v</span><span class="p">),</span> <span class="n">v</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Both pass. The Fibonacci sequence encodes and decodes cleanly; the empty list handles the edge case. Gamma&rsquo;s prefix-freeness is what makes this possible: each element&rsquo;s boundary is determined by the code structure, not by a length header.</p>
<h2 id="krafts-inequality-lightly">Kraft&rsquo;s Inequality, Lightly</h2>
<p>There is a necessary condition on codeword lengths for a prefix-free code to exist. Kraft&rsquo;s inequality states: for a binary prefix-free code with codeword lengths \(l_1, l_2, \ldots\), we have</p>
$$\sum_i 2^{-l_i} \leq 1$$<p>The sum counts how much of the &ldquo;leaf capacity&rdquo; of an infinite binary tree each codeword consumes. Prefix-freeness requires each codeword to occupy its own leaf without overlap; the total capacity of the tree is 1, so the sum cannot exceed it.</p>
<p>Both Unary and Gamma satisfy Kraft&rsquo;s inequality, verified computationally:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="n">TEST</span><span class="p">(</span><span class="n">PrefixFreeTest</span><span class="p">,</span> <span class="n">KraftInequalityHoldsForUnary</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// Unary code: codeword for n has length n bits.
</span></span></span><span class="line"><span class="cl">    <span class="c1">// Sum of 2^-l for n = 1..K is 1 - 2^-K, which is &lt; 1.
</span></span></span><span class="line"><span class="cl">    <span class="kt">double</span> <span class="n">sum</span> <span class="o">=</span> <span class="mf">0.0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="k">constexpr</span> <span class="kt">int</span> <span class="n">K</span> <span class="o">=</span> <span class="mi">30</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">n</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span> <span class="n">n</span> <span class="o">&lt;=</span> <span class="n">K</span><span class="p">;</span> <span class="o">++</span><span class="n">n</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">sum</span> <span class="o">+=</span> <span class="n">std</span><span class="o">::</span><span class="n">ldexp</span><span class="p">(</span><span class="mf">1.0</span><span class="p">,</span> <span class="o">-</span><span class="n">n</span><span class="p">);</span>  <span class="c1">// 2^-n
</span></span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="n">EXPECT_LE</span><span class="p">(</span><span class="n">sum</span><span class="p">,</span> <span class="mf">1.0</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// Convergence check: sum approaches 1 as K grows.
</span></span></span><span class="line"><span class="cl">    <span class="n">EXPECT_GT</span><span class="p">(</span><span class="n">sum</span><span class="p">,</span> <span class="mf">1.0</span> <span class="o">-</span> <span class="n">std</span><span class="o">::</span><span class="n">ldexp</span><span class="p">(</span><span class="mf">1.0</span><span class="p">,</span> <span class="o">-</span><span class="p">(</span><span class="n">K</span> <span class="o">-</span> <span class="mi">1</span><span class="p">)));</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">TEST</span><span class="p">(</span><span class="n">PrefixFreeTest</span><span class="p">,</span> <span class="n">KraftInequalityHoldsForGamma</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// Gamma code: codeword for n has length 2 * floor(log2(n)) + 1.
</span></span></span><span class="line"><span class="cl">    <span class="kt">double</span> <span class="n">sum</span> <span class="o">=</span> <span class="mf">0.0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="k">constexpr</span> <span class="kt">int</span> <span class="n">N</span> <span class="o">=</span> <span class="mi">1</span> <span class="o">&lt;&lt;</span> <span class="mi">14</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">n</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span> <span class="n">n</span> <span class="o">&lt;=</span> <span class="n">N</span><span class="p">;</span> <span class="o">++</span><span class="n">n</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="kt">int</span> <span class="n">len</span> <span class="o">=</span> <span class="mi">2</span> <span class="o">*</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">bit_width</span><span class="p">(</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="kt">unsigned</span><span class="o">&gt;</span><span class="p">(</span><span class="n">n</span><span class="p">)))</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="n">sum</span> <span class="o">+=</span> <span class="n">std</span><span class="o">::</span><span class="n">ldexp</span><span class="p">(</span><span class="mf">1.0</span><span class="p">,</span> <span class="o">-</span><span class="n">len</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="n">EXPECT_LE</span><span class="p">(</span><span class="n">sum</span><span class="p">,</span> <span class="mf">1.0</span> <span class="o">+</span> <span class="mf">1e-9</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Unary sums to \(1 - 2^{-K}\) for the first \(K\) integers, converging to 1 from below. Gamma sums to exactly 1 in the limit: each &ldquo;block&rdquo; of integers sharing the same codeword length contributes \(2^{-(k+1)}\), and \(\sum_{k=0}^{\infty} 2^{-(k+1)} = 1\). Both satisfy \(\leq 1\).</p>
<p>The converse of Kraft&rsquo;s inequality is also true: any sequence of lengths satisfying the inequality is realized by some prefix-free code. This characterizes exactly which length vectors are achievable. The forthcoming Information Theory by Construction series develops the proof and connects it to optimal code design.</p>
<h2 id="the-bridge-claim">The Bridge Claim</h2>
<p>Pull back to the structure.</p>
<p>Prefix-freeness is the property that lifts the free-monoid construction into bit space injectively. Without it, the lift exists as a function but cannot be inverted. With it, the lift is an isomorphism of monoids.</p>
<p>The same pattern applies to the other combinators from <a href="/post/2026-05-codecs-functors-stepanov/">Bits Follow Types</a>:</p>
<table>
	<thead>
			<tr>
					<th>Combinator</th>
					<th>Universal property being lifted</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>Opt&lt;C&gt;</code></td>
					<td>Coproduct with unit \(1 + T\)</td>
			</tr>
			<tr>
					<td><code>Either&lt;A, B&gt;</code></td>
					<td>Binary coproduct \(A + B\)</td>
			</tr>
			<tr>
					<td><code>Pair&lt;A, B&gt;</code></td>
					<td>Binary product \(A \times B\) (given both factors prefix-free)</td>
			</tr>
			<tr>
					<td><code>Vec&lt;C&gt;</code></td>
					<td>Free monoid on \(T\) (given \(C\) prefix-free)</td>
			</tr>
	</tbody>
</table>
<p>Each combinator lifts a universal property from the type algebra to the bit-string algebra. The tag-bit in <code>Opt</code> and <code>Either</code> is what makes their coproduct construction injective: the tag selects the branch, and the branch codec handles the payload. The ordering in <code>Pair</code> is recoverable because the first codec is prefix-free, so the decoder knows where the first field ends. And <code>Vec</code> is recoverable because the element codec is prefix-free, as just argued.</p>
<p>The unifying frame: codecs are structure-preserving embeddings from the type algebra into the algebra of bit strings. When the type algebra involves a free construction (the free monoid for lists, or the free algebra for more general term structures), the lift is canonical by the universal property. When the codec is also prefix-free, the lift is reversible: encode and decode form an isomorphism.</p>
<p>This is the sense in which prefix-freeness is &ldquo;the categorically right answer&rdquo; to the boundary problem. It is not just convenient. It is exactly what is needed for the algebraic structure of the encoding to match the algebraic structure of the type.</p>
<p>The <a href="/post/2026-01-19-duality-stepanov/">duality post</a> noted that encode and decode are duals. Under the combinator framing, that duality decomposes structurally: the encode-decode duality of a compound type is the product of the encode-decode dualities of its parts. Prefix-freeness is the condition that makes the decode half of that duality well-defined.</p>
<h2 id="further-reading">Further Reading</h2>
<p><strong>Background in this series:</strong></p>
<ul>
<li><a href="/post/2026-05-codecs-functors-stepanov/">Bits Follow Types</a>: <code>Vec&lt;C&gt;</code> and the other combinators. This post explains why they work.</li>
<li><a href="/post/2026-03-free-algebra-stepanov/">Why Lists and Polynomials Are Universal</a>: the free-monoid construction being lifted to bit space here.</li>
<li><a href="/post/2026-03-homomorphism-stepanov/">The Maps Between Structures</a>: the lift is a monoid homomorphism; this post covers what that means and why it matters.</li>
<li><a href="/post/2026-01-19-duality-stepanov/">Structure, Duality, and the Shape of Computation</a>: prefix-freeness is what makes the decode half of the encode-decode duality well-defined.</li>
</ul>
<p><strong>Where this leads:</strong> The forthcoming Information Theory by Construction series develops Kraft&rsquo;s inequality proof, the converse (every Kraft-satisfying length vector is realizable by a prefix-free code), and the full universal-codes treatment connecting prefix-freeness to entropy, Huffman coding, and arithmetic coding.</p>
<p><strong>The production library:</strong> PFC (<a href="https://github.com/queelius/pfc">github.com/queelius/pfc</a>) provides the production version with a richer codec library, full STL integration, and 31k+ test assertions. See <code>include/pfc/algebraic.hpp</code> for the combinators and <code>include/pfc/codecs.hpp</code> for the full catalog of universal codes.</p>
]]></content:encoded><media:content url="https://metafunctor.com/post/2026-05-prefix-free-stepanov/card.jpg" medium="image"><media:title type="plain">Category-Theory</media:title></media:content><category>C++</category><category>generic-programming</category><category>codecs</category><category>information-theory</category><category>category-theory</category><category>monoids</category></item><item><title>Free Algebras: Why Lists and Polynomials Are Universal</title><link>https://metafunctor.com/post/2026-03-free-algebra-stepanov/</link><pubDate>Fri, 13 Mar 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2026-03-free-algebra-stepanov/</guid><description>The free monoid on a set is the type of lists over that set. The universal property says fold is the unique homomorphism from lists to any monoid. This explains why lists, multisets, and polynomials appear everywhere.</description><content:encoded><![CDATA[<p><em>Lists are everywhere in programming. Not because they are convenient. Because they are algebraically universal.</em></p>
<h2 id="why-lists">Why Lists?</h2>
<p>Arrays are more cache-friendly. Hash maps have better lookup. Yet lists (sequences, vectors, streams) remain the default container in nearly every language. The standard explanation is convention, or ease of construction. The real explanation is algebraic.</p>
<p>A list is the <strong>free monoid</strong>. It is the most general monoid you can build from a set of generators. And the universal property of free monoids says that fold, the operation that processes a list element by element, is not a design pattern. It is a theorem.</p>
<h2 id="the-free-monoid">The Free Monoid</h2>
<p>Start with a set \(S\) of generators. The free monoid on \(S\) is the set of all finite sequences of elements from \(S\), with concatenation as the operation and the empty sequence as the identity.</p>
<p>&ldquo;Free&rdquo; means: no equations hold except those forced by the monoid axioms (associativity and identity). In particular:</p>
<ul>
<li>\([a, b] \neq [b, a]\). Commutativity is not imposed.</li>
<li>\([a, a] \neq [a]\). Idempotency is not imposed.</li>
<li>\([a, b, c] = [a] \cdot [b] \cdot [c]\). Every sequence is a product of singletons.</li>
</ul>
<p>In C++:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">T</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">free_monoid</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span> <span class="n">elements_</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">public</span><span class="o">:</span>
</span></span><span class="line"><span class="cl">    <span class="n">free_monoid</span><span class="p">()</span> <span class="o">=</span> <span class="k">default</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="k">explicit</span> <span class="nf">free_monoid</span><span class="p">(</span><span class="n">T</span> <span class="n">x</span><span class="p">)</span> <span class="o">:</span> <span class="n">elements_</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="n">x</span><span class="p">)}</span> <span class="p">{}</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// ...
</span></span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// Monoid operations via ADL
</span></span></span><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">T</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="n">free_monoid</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span> <span class="n">op</span><span class="p">(</span><span class="k">const</span> <span class="n">free_monoid</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;&amp;</span> <span class="n">a</span><span class="p">,</span> <span class="k">const</span> <span class="n">free_monoid</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;&amp;</span> <span class="n">b</span><span class="p">);</span>  <span class="c1">// concatenation
</span></span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">T</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="n">free_monoid</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span> <span class="n">identity</span><span class="p">(</span><span class="k">const</span> <span class="n">free_monoid</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;&amp;</span><span class="p">);</span>  <span class="c1">// empty sequence
</span></span></span></code></pre></div><p>The <code>free_monoid&lt;int&gt;</code> is the type of finite sequences of integers. Its operation is concatenation. It satisfies the <code>Monoid</code> concept. And it is the most general monoid on <code>int</code>: no structure beyond associativity and identity.</p>
<h2 id="the-universal-property">The Universal Property</h2>
<p>Here is the key fact. Given any function \(f: S \to M\) where \(M\) is a monoid, there exists a <strong>unique</strong> monoid homomorphism \(\overline{f}: \text{Free}(S) \to M\) extending \(f\). This homomorphism is defined by:</p>
$$\overline{f}([a_1, a_2, \ldots, a_n]) = f(a_1) \cdot f(a_2) \cdot \ldots \cdot f(a_n)$$<p>In code:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="n">Monoid</span> <span class="n">M</span><span class="p">,</span> <span class="k">typename</span> <span class="n">T</span><span class="p">,</span> <span class="k">typename</span> <span class="n">F</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="n">M</span> <span class="n">extend</span><span class="p">(</span><span class="n">F</span> <span class="n">f</span><span class="p">,</span> <span class="k">const</span> <span class="n">free_monoid</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;&amp;</span> <span class="n">xs</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">M</span> <span class="n">result</span> <span class="o">=</span> <span class="n">identity</span><span class="p">(</span><span class="n">M</span><span class="p">{});</span>
</span></span><span class="line"><span class="cl">    <span class="k">for</span> <span class="p">(</span><span class="k">const</span> <span class="k">auto</span><span class="o">&amp;</span> <span class="nl">x</span> <span class="p">:</span> <span class="n">xs</span><span class="p">.</span><span class="n">elements</span><span class="p">())</span>
</span></span><span class="line"><span class="cl">        <span class="n">result</span> <span class="o">=</span> <span class="n">op</span><span class="p">(</span><span class="n">result</span><span class="p">,</span> <span class="n">f</span><span class="p">(</span><span class="n">x</span><span class="p">));</span>
</span></span><span class="line"><span class="cl">    <span class="k">return</span> <span class="n">result</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>This is fold. The universal property says fold is the <strong>only</strong> structure-preserving way to interpret a list in a monoid. Any function that respects the monoid structure must agree with fold.</p>
<h2 id="fold-is-a-theorem">Fold Is a Theorem</h2>
<p>When you write <code>std::accumulate</code> or <code>std::reduce</code>, you are invoking the universal property. The homomorphism condition:</p>
$$\overline{f}(\text{op}(xs, ys)) = \text{op}(\overline{f}(xs), \overline{f}(ys))$$<p>is not something you need to verify. It follows from the construction. Fold preserves concatenation because it is the unique homomorphism.</p>
<p>This also explains why fold over an empty list returns the identity element. Not by convention, but by the homomorphism property: \(\overline{f}([]) = e_M\), because homomorphisms preserve identity.</p>
<h2 id="concrete-examples">Concrete Examples</h2>
<p>Every standard reduction is an instance of <code>extend</code>:</p>
<table>
	<thead>
			<tr>
					<th>Target monoid</th>
					<th>Operation</th>
					<th>Identity</th>
					<th><code>extend</code> computes</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>additive&lt;int&gt;</code></td>
					<td>+</td>
					<td>0</td>
					<td>sum</td>
			</tr>
			<tr>
					<td><code>multiplicative&lt;int&gt;</code></td>
					<td>*</td>
					<td>1</td>
					<td>product</td>
			</tr>
			<tr>
					<td><code>max_monoid&lt;int&gt;</code></td>
					<td>max</td>
					<td>lowest</td>
					<td>maximum</td>
			</tr>
			<tr>
					<td><code>string_concat</code></td>
					<td>+</td>
					<td>&quot;&quot;</td>
					<td>concatenation</td>
			</tr>
	</tbody>
</table>
<p>Length is also an instance: map every element to 1 in <code>additive&lt;int&gt;</code>, then fold. The count of elements in a list is a monoid homomorphism from the free monoid to \((\mathbb{Z}, +, 0)\).</p>
<h2 id="free-commutative-monoids-multisets">Free Commutative Monoids: Multisets</h2>
<p>The free monoid imposes no commutativity. What if we do?</p>
<p>The free <strong>commutative</strong> monoid on \(S\) is the set of multisets (bags) over \(S\). Order is forgotten, but multiplicity is preserved. \({a, b} = {b, a}\), but \({a, a} \neq {a}\).</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">T</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">free_commutative_monoid</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">map</span><span class="o">&lt;</span><span class="n">T</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">size_t</span><span class="o">&gt;</span> <span class="n">counts_</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">public</span><span class="o">:</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// Operation: add counts. Identity: empty multiset.
</span></span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>Adding more axioms yields more specialized structures:</p>
<table>
	<thead>
			<tr>
					<th>Free algebra</th>
					<th>Axioms imposed</th>
					<th>Elements are</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Free monoid</td>
					<td>associativity, identity</td>
					<td>sequences (lists)</td>
			</tr>
			<tr>
					<td>Free commutative monoid</td>
					<td>+ commutativity</td>
					<td>multisets (bags)</td>
			</tr>
			<tr>
					<td>Free abelian group</td>
					<td>+ inverses</td>
					<td>signed multisets</td>
			</tr>
			<tr>
					<td>Free commutative ring</td>
					<td>ring axioms</td>
					<td>polynomials</td>
			</tr>
	</tbody>
</table>
<p>Each row adds equations. Each addition collapses more elements into equivalence. The free monoid is the most general; polynomials are the most constrained in this table.</p>
<h2 id="the-polynomial-connection">The Polynomial Connection</h2>
<p>The <a href="https://metafunctor.com/post/2020-07-polynomials-stepanov/">polynomial post</a> built polynomial arithmetic as a Euclidean domain. But a polynomial in one variable over a ring \(R\) is an element of the free commutative \(R\)-algebra on one generator. Polynomial evaluation at a point \(a\):</p>
$$p(x) = c_0 + c_1 x + c_2 x^2 + \cdots \mapsto c_0 + c_1 a + c_2 a^2 + \cdots$$<p>is the universal homomorphism from the free algebra to \(R\). The polynomial post was working with a free algebra all along. Evaluation is <code>extend</code>, with the generator \(x\) mapped to the value \(a\).</p>
<h2 id="why-this-matters">Why This Matters</h2>
<p>Free algebras explain why certain data structures recur across programming languages and mathematical disciplines. Lists appear everywhere because the free monoid is universal: any computation that respects associativity and identity factors through it. Multisets appear in combinatorics because the free commutative monoid is universal for commutative computations. Polynomials appear in algebra because the free ring is universal for ring computations.</p>
<p>These structures are not design choices. They are forced by the axioms. The free algebra on a set of axioms is the inevitable starting point, the structure you get before imposing any problem-specific equations.</p>
<p>Algorithms arise from algebraic structure. Free algebras explain which structures are the starting points.</p>
<h2 id="further-reading">Further Reading</h2>
<ul>
<li>Mac Lane, <em>Categories for the Working Mathematician</em>, ch. IV (free algebras)</li>
<li>Stepanov &amp; Rose, <em>From Mathematics to Generic Programming</em></li>
<li>Awodey, <em>Category Theory</em>, ch. 2 (free/forgetful adjunction)</li>
</ul>
<h2 id="watch-the-episode">Watch the Episode</h2>
<div class="youtube-episode" style="position:relative;width:100%;padding-bottom:56.25%;height:0;overflow:hidden;margin:1.5rem 0;">
  <iframe
    src="https://www.youtube-nocookie.com/embed/Y76txQRQssY"
    title="Free Algebras: Why Lists and Polynomials Are Universal"
    style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;"
    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
    allowfullscreen
    loading="lazy"
    referrerpolicy="strict-origin-when-cross-origin"></iframe>
</div>
<p class="youtube-episode-links"><em>This post is also a video episode: <a href="https://youtu.be/Y76txQRQssY" rel="noopener" target="_blank">Free Algebras: Why Lists and Polynomials Are Universal</a>, part of <a href="https://www.youtube.com/playlist?list=PLDqx3XgZpNtc" rel="noopener" target="_blank">the series playlist</a>.</em></p>
]]></content:encoded><media:content url="https://metafunctor.com/post/2026-03-free-algebra-stepanov/card.jpg" medium="image"><media:title type="plain">Category-Theory</media:title></media:content><category>C++</category><category>generic-programming</category><category>algorithms</category><category>monoids</category><category>category-theory</category></item><item><title>Duality: The Hidden Structure of Opposites</title><link>https://metafunctor.com/post/2026-01-19-duality-stepanov/</link><pubDate>Mon, 19 Jan 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2026-01-19-duality-stepanov/</guid><description>Many structures come in pairs: forward/reverse AD, push/pull iteration, encode/decode. Recognizing duality lets you transfer theorems and insights between domains.</description><content:encoded><![CDATA[<p><em>Many structures come in pairs. Recognizing duality lets you transfer insights between domains.</em></p>
<h2 id="the-motivating-example">The Motivating Example</h2>
<p>This collection includes two approaches to automatic differentiation:</p>
<ul>
<li><strong>Forward mode</strong> (in <a href="/post/2021-09-dual-stepanov/">dual</a>): Propagate derivatives alongside values, from inputs toward outputs</li>
<li><strong>Reverse mode</strong> (in <a href="/post/2023-01-autodiff-stepanov/">autodiff</a>): Build a graph during forward evaluation, then propagate gradients backward from outputs toward inputs</li>
</ul>
<p>These aren&rsquo;t just two implementations of the same idea. They&rsquo;re <em>duals</em>, mirror images with complementary strengths.</p>
<p>Forward mode computes one column of the Jacobian per pass. If \(f: \mathbb{R}^n \to \mathbb{R}^m\), computing the full Jacobian takes \(n\) passes. Reverse mode computes one row per pass, \(m\) passes for the full Jacobian.</p>
<p>For neural network training, we have many inputs (millions of parameters) and one output (the loss). Reverse mode wins overwhelmingly: one backward pass gives all gradients. This is why backpropagation dominates deep learning.</p>
<p>For sensitivity analysis with few parameters and many outputs, forward mode wins. Same algorithm structure, opposite traversal direction, complementary use cases.</p>
<p>The mathematical explanation: forward mode computes Jacobian-vector products (\(Jv\)); reverse mode computes vector-Jacobian products (\(v^T J\)). These are transposes of each other. <strong>Duality is transposition.</strong></p>
<h2 id="push-vs-pull">Push vs Pull</h2>
<p>Consider two ways to traverse a sequence:</p>
<p><strong>Pull (iterator/consumer controls):</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">for</span> <span class="p">(</span><span class="k">auto</span> <span class="n">it</span> <span class="o">=</span> <span class="n">seq</span><span class="p">.</span><span class="n">begin</span><span class="p">();</span> <span class="n">it</span> <span class="o">!=</span> <span class="n">seq</span><span class="p">.</span><span class="n">end</span><span class="p">();</span> <span class="o">++</span><span class="n">it</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">process</span><span class="p">(</span><span class="o">*</span><span class="n">it</span><span class="p">);</span>  <span class="c1">// Consumer pulls each element
</span></span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p><strong>Push (producer controls):</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="n">seq</span><span class="p">.</span><span class="n">for_each</span><span class="p">([](</span><span class="k">auto</span> <span class="n">x</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">process</span><span class="p">(</span><span class="n">x</span><span class="p">);</span>  <span class="c1">// Producer pushes each element
</span></span></span><span class="line"><span class="cl"><span class="p">});</span>
</span></span></code></pre></div><p>Same traversal. Same elements processed. But control flow is reversed:</p>
<table>
	<thead>
			<tr>
					<th>Aspect</th>
					<th>Pull (Iterator)</th>
					<th>Push (Generator)</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Who controls pace?</td>
					<td>Consumer</td>
					<td>Producer</td>
			</tr>
			<tr>
					<td>Suspend/resume?</td>
					<td>Consumer decides when to call <code>++</code></td>
					<td>Producer decides when to yield</td>
			</tr>
			<tr>
					<td>Backpressure</td>
					<td>Natural (just stop pulling)</td>
					<td>Must be designed in</td>
			</tr>
			<tr>
					<td>Composition</td>
					<td>Chain iterators</td>
					<td>Chain callbacks</td>
			</tr>
	</tbody>
</table>
<p>C++ ranges are pull-based: <code>view | filter | transform</code> creates an iterator that pulls through the pipeline. Reactive streams (Rx) are push-based: events flow through a pipeline of observers.</p>
<p>These are duals. Given a pull-based algorithm, you can mechanically derive its push-based counterpart by reversing who initiates each step. The transformation preserves correctness because it&rsquo;s just changing <em>direction</em>, not <em>content</em>.</p>
<h2 id="encode-vs-decode">Encode vs Decode</h2>
<p>Compression algorithms come in pairs:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="c1">// Encoder: structure -&gt; bits
</span></span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="nf">encode</span><span class="p">(</span><span class="k">const</span> <span class="n">Document</span><span class="o">&amp;</span> <span class="n">doc</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Bitstream</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// Decoder: bits -&gt; structure
</span></span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="nf">decode</span><span class="p">(</span><span class="k">const</span> <span class="n">Bitstream</span><span class="o">&amp;</span> <span class="n">bits</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">Document</span><span class="p">;</span>
</span></span></code></pre></div><p>These must be inverses: <code>decode(encode(x)) == x</code>. But their implementations are often strikingly different:</p>
<table>
	<thead>
			<tr>
					<th>Aspect</th>
					<th>Encoder</th>
					<th>Decoder</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Sees</td>
					<td>Full structure</td>
					<td>Linear stream</td>
			</tr>
			<tr>
					<td>Can do</td>
					<td>Multiple passes, global analysis</td>
					<td>Single pass, local decisions</td>
			</tr>
			<tr>
					<td>Optimizes for</td>
					<td>Small output</td>
					<td>Fast reconstruction</td>
			</tr>
	</tbody>
</table>
<p>Huffman coding illustrates this asymmetry. Encoding requires building a frequency table (scan all symbols), constructing a tree, then traversing the tree for each symbol. Decoding walks the tree bit-by-bit, simpler, often faster.</p>
<p>The same pattern appears in:</p>
<ul>
<li>Serialization (object to bytes) vs deserialization (bytes to object)</li>
<li>Compilation (source to bytecode) vs interpretation (bytecode to execution)</li>
<li>Encryption vs decryption</li>
</ul>
<p>Recognizing encode/decode as duals suggests questions: if encoding is hard, can decoding be easy? If we have a fast encoder, what does its dual look like?</p>
<h2 id="covariance-and-contravariance">Covariance and Contravariance</h2>
<p>Functions have a peculiar duality in their arguments:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="c1">// If Dog &lt;: Animal (Dog is a subtype of Animal)
</span></span></span><span class="line"><span class="cl"><span class="c1">// Then:
</span></span></span><span class="line"><span class="cl"><span class="c1">//   vector&lt;Dog&gt; is NOT a subtype of vector&lt;Animal&gt;     (invariant)
</span></span></span><span class="line"><span class="cl"><span class="c1">//   function&lt;Dog&gt; is NOT a subtype of function&lt;Animal&gt;  (contravariant in input)
</span></span></span><span class="line"><span class="cl"><span class="c1">//   function returning Dog IS a subtype of function returning Animal (covariant in output)
</span></span></span></code></pre></div><p>A function <code>Animal -&gt; String</code> can substitute for <code>Dog -&gt; String</code> because any code expecting to pass a Dog can pass it to something expecting any Animal. The subtyping relationship <em>reverses</em> for inputs.</p>
<p>This is contravariance: inputs are dual to outputs. The reversal happens because inputs flow <em>into</em> the function while outputs flow <em>out</em>. Direction reversal is duality.</p>
<p>In C++, this manifests in template variance:</p>
<ul>
<li><code>const T*</code> is covariant in T (can convert <code>const Dog*</code> to <code>const Animal*</code>)</li>
<li>Function parameters are contravariant (accepting broader types is safe)</li>
</ul>
<p>Understanding this duality prevents type errors and clarifies API design.</p>
<h2 id="the-category-theoretic-view">The Category-Theoretic View</h2>
<p>Category theory makes duality precise. A category has objects and arrows (morphisms) between them:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">    f        g
</span></span><span class="line"><span class="cl">A -----&gt; B -----&gt; C
</span></span></code></pre></div><p>The <strong>opposite category</strong> \(C^{op}\) has the same objects but all arrows reversed:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">    f^op     g^op
</span></span><span class="line"><span class="cl">A &lt;----- B &lt;----- C
</span></span></code></pre></div><p>Every theorem in \(C\) has a dual theorem in \(C^{op}\), obtained by reversing all arrows. This mechanical transformation (&ldquo;reverse the arrows&rdquo;) generates valid mathematics for free.</p>
<p>Some dualities this reveals:</p>
<table>
	<thead>
			<tr>
					<th>Concept</th>
					<th>Dual Concept</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Product (\(A \times B\))</td>
					<td>Coproduct (\(A + B\), sum type)</td>
			</tr>
			<tr>
					<td>Terminal object (unit, <code>void</code>)</td>
					<td>Initial object (empty, <code>never</code>)</td>
			</tr>
			<tr>
					<td>Monomorphism (injection)</td>
					<td>Epimorphism (surjection)</td>
			</tr>
			<tr>
					<td>Limit</td>
					<td>Colimit</td>
			</tr>
			<tr>
					<td>Algebra</td>
					<td>Coalgebra</td>
			</tr>
	</tbody>
</table>
<p>A <strong>product</strong> is an object \(P\) with projections to \(A\) and \(B\) such that any object with maps to both \(A\) and \(B\) factors uniquely through \(P\):</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">        X
</span></span><span class="line"><span class="cl">       /|\
</span></span><span class="line"><span class="cl">      / | \
</span></span><span class="line"><span class="cl">     /  |  \
</span></span><span class="line"><span class="cl">    v   v   v
</span></span><span class="line"><span class="cl">    A&lt;--P--&gt;B
</span></span></code></pre></div><p>A <strong>coproduct</strong> is the dual: an object \(S\) with injections from \(A\) and \(B\), such that any object receiving maps from both factors uniquely through \(S\):</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">    A--&gt;S&lt;--B
</span></span><span class="line"><span class="cl">        |
</span></span><span class="line"><span class="cl">        v
</span></span><span class="line"><span class="cl">        X
</span></span></code></pre></div><p>In types: product is <code>struct { A a; B b; }</code> (you have both). Coproduct is <code>variant&lt;A, B&gt;</code> (you have one or the other). Same diagram, arrows reversed.</p>
<h2 id="why-duality-matters">Why Duality Matters</h2>
<p><strong>Transfer theorems.</strong> Prove something about products; the dual theorem about coproducts follows automatically. Prove forward-mode AD is correct; reverse-mode correctness follows by transposition. Work done once yields two results.</p>
<p><strong>Design insight.</strong> When you build X, ask: what is dual(X)? If you have a parser (string to tree), consider the printer (tree to string). If you have a fold (consume structure), consider an unfold (produce structure). The dual often fills a complementary need.</p>
<p><strong>Debugging.</strong> If forward mode works but reverse mode doesn&rsquo;t, examine where arrow reversal might have gone wrong. Duality localizes errors: the bug is where the dual transformation wasn&rsquo;t applied correctly.</p>
<p><strong>API symmetry.</strong> Push/pull, encode/decode, serialize/deserialize. Recognizing these as duals suggests they should have symmetric interfaces. Asymmetry in dual pairs often indicates missing functionality or awkward design.</p>
<h2 id="examples-in-this-collection">Examples in This Collection</h2>
<p>The duality theme runs through several posts:</p>
<p><strong>Forward vs reverse AD</strong> (<a href="/post/2021-09-dual-stepanov/">dual</a> and <a href="/post/2023-01-autodiff-stepanov/">autodiff</a>): The clearest example. Same derivatives, opposite traversal directions, complementary efficiency profiles.</p>
<p><strong>Iterators and ranges</strong>: The <a href="/post/2021-03-elementa-stepanov/">elementa</a> matrix iteration follows pull semantics. The dual push semantics would be a visitor pattern over matrix elements.</p>
<p><strong>Fold and unfold in power()</strong>: The <a href="/post/2019-03-peasant-stepanov/">peasant</a> algorithm is a fold: it consumes the exponent bit-by-bit, accumulating the result. The dual unfold would generate the sequence of squares from a base, which is exactly what repeated squaring does implicitly.</p>
<p><strong>Encode/decode</strong>: While not explicitly covered, the type-erasure post implies this pattern: erasing a type is encoding its operations; invoking through the erased interface is decoding back to behavior.</p>
<h2 id="the-deeper-pattern">The Deeper Pattern</h2>
<p>Duality arises whenever there&rsquo;s a notion of <em>direction</em> that can be reversed:</p>
<ul>
<li>Data flow direction (forward/backward)</li>
<li>Control flow direction (push/pull)</li>
<li>Type hierarchy direction (covariant/contravariant)</li>
<li>Logical implication direction (modus ponens/modus tollens)</li>
<li>Arrow direction in diagrams</li>
</ul>
<p>Each reversal is a duality. Each duality doubles your theorems, doubles your designs, and halves your blind spots.</p>
<p>The next time you build something, ask: what happens if I reverse the arrows? The answer might be exactly what you need.</p>
<hr>
<h2 id="further-reading">Further Reading</h2>
<ul>
<li>Wadler, &ldquo;Theorems for Free!&rdquo; (1989), parametricity as a source of dual theorems</li>
<li>Gibbons, &ldquo;Calculating Functional Programs&rdquo;, unfolds as duals of folds</li>
<li>Awodey, <em>Category Theory</em>, the formal treatment of opposite categories</li>
<li>Baydin et al., &ldquo;Automatic Differentiation in Machine Learning: a Survey&rdquo;, forward vs reverse mode</li>
</ul>
]]></content:encoded><media:content url="https://metafunctor.com/post/2026-01-19-duality-stepanov/card.jpg" medium="image"><media:title type="plain">Category-Theory</media:title></media:content><category>C++</category><category>duality</category><category>category-theory</category><category>automatic-differentiation</category><category>iterators</category><category>generic-programming</category></item><item><title>Category Theory for Programmers</title><link>https://metafunctor.com/media/category-theory-for-programmers/</link><pubDate>Wed, 17 Dec 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/media/category-theory-for-programmers/</guid><description/><content:encoded></content:encoded><category>category-theory</category><category>functional-programming</category><category>haskell</category></item><item><title>Cipher Maps: A Unified Framework for Oblivious Function Approximation Through Algebraic Structures and Bernoulli Models</title><link>https://metafunctor.com/papers/cipher-maps/</link><pubDate>Tue, 14 Oct 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/papers/cipher-maps/</guid><description> Download PDF</description><content:encoded><![CDATA[<style>
.pdf-container {
  width: 100%;
  margin: 0;
}
.pdf-container embed {
  width: 100%;
  min-height: 90vh;
  border: 1px solid var(--color-border-default, #ddd);
  border-radius: 0.5rem;
}
.action-bar {
  margin: 0 0 1rem 0;
  display: flex;
  gap: .75rem;
  flex-wrap: wrap;
}
.action-bar a {
  text-decoration: none;
  padding: .5rem .75rem;
  border: 1px solid var(--color-border-default, #ddd);
  border-radius: .4rem;
  display: inline-block;
  color: var(--color-accent-primary, #007acc);
}
.action-bar a:hover {
  background: var(--color-bg-secondary, #f5f5f5);
}
</style>
<div class="action-bar">
  <a href="/latex/cipher-maps/cipher_maps.pdf" target="_blank" rel="noopener">Download PDF</a>
</div>
<div class="pdf-container">
  <embed src="/latex/cipher-maps/cipher_maps.pdf" type="application/pdf">
</div>]]></content:encoded><category>oblivious computing</category><category>cipher maps</category><category>algebraic cipher types</category><category>Bernoulli models</category><category>function approximation</category><category>oblivious data types</category><category>cryptography</category><category>encrypted search</category><category>probabilistic data structures</category><category>category theory</category></item><item><title>Algebraic Cipher Types: A Functorial Framework for Secure Computation</title><link>https://metafunctor.com/papers/algebraic-cipher-types/</link><pubDate>Tue, 07 Oct 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/papers/algebraic-cipher-types/</guid><description> Download PDF</description><content:encoded><![CDATA[<style>
.pdf-container {
  width: 100%;
  margin: 0;
}
.pdf-container embed {
  width: 100%;
  min-height: 90vh;
  border: 1px solid var(--color-border-default, #ddd);
  border-radius: 0.5rem;
}
.action-bar {
  margin: 0 0 1rem 0;
  display: flex;
  gap: .75rem;
  flex-wrap: wrap;
}
.action-bar a {
  text-decoration: none;
  padding: .5rem .75rem;
  border: 1px solid var(--color-border-default, #ddd);
  border-radius: .4rem;
  display: inline-block;
  color: var(--color-accent-primary, #007acc);
}
.action-bar a:hover {
  background: var(--color-bg-secondary, #f5f5f5);
}
</style>
<div class="action-bar">
  <a href="/latex/algebraic-cipher-types/main.pdf" target="_blank" rel="noopener">Download PDF</a>
</div>
<div class="pdf-container">
  <embed src="/latex/algebraic-cipher-types/main.pdf" type="application/pdf">
</div>]]></content:encoded><category>oblivious computing</category><category>algebraic cipher types</category><category>category theory</category><category>functors</category><category>cryptography</category><category>encrypted search</category><category>homomorphic operations</category><category>secure computation</category><category>oblivious data types</category><category>type theory</category></item><item><title>The Dot Ecosystem: From Simple Paths to Data Algebras</title><link>https://metafunctor.com/post/2024-12-dotsuite/</link><pubDate>Sun, 15 Dec 2024 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-12-dotsuite/</guid><description>A composable ecosystem of tools for manipulating nested data structures. From a simple helper function to a full data algebra, guided by purity, pedagogy, and the principle of least power.</description><content:encoded><![CDATA[<p><strong><a href="https://github.com/queelius/dotsuite">dotsuite</a></strong> is a suite of composable tools for working with nested data structures like JSON, YAML, and Python dictionaries. It started as a single helper function and grew into something with actual mathematical structure. That growth is the interesting part.</p>
<h2 id="the-origin">The Origin</h2>
<p>It always starts with a simple problem. You have a nested dictionary and you need a value buried deep inside:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="c1"># Brittle code that crashes on missing keys</span>
</span></span><span class="line"><span class="cl"><span class="n">email</span> <span class="o">=</span> <span class="n">data</span><span class="p">[</span><span class="s1">&#39;user&#39;</span><span class="p">][</span><span class="s1">&#39;contacts&#39;</span><span class="p">][</span><span class="mi">0</span><span class="p">][</span><span class="s1">&#39;email&#39;</span><span class="p">]</span>  <span class="c1"># KeyError? IndexError?</span>
</span></span></code></pre></div><p>The first solution is a helper function:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="c1"># The essence of dotget - simple enough to copy</span>
</span></span><span class="line"><span class="cl"><span class="k">def</span> <span class="nf">get</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="n">path</span><span class="p">,</span> <span class="n">default</span><span class="o">=</span><span class="kc">None</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">    <span class="k">try</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="n">segment</span> <span class="ow">in</span> <span class="n">path</span><span class="o">.</span><span class="n">split</span><span class="p">(</span><span class="s1">&#39;.&#39;</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">            <span class="n">data</span> <span class="o">=</span> <span class="n">data</span><span class="p">[</span><span class="nb">int</span><span class="p">(</span><span class="n">segment</span><span class="p">)]</span> <span class="k">if</span> <span class="n">segment</span><span class="o">.</span><span class="n">isdigit</span><span class="p">()</span> <span class="k">else</span> <span class="n">data</span><span class="p">[</span><span class="n">segment</span><span class="p">]</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">data</span>
</span></span><span class="line"><span class="cl">    <span class="k">except</span> <span class="p">(</span><span class="ne">KeyError</span><span class="p">,</span> <span class="ne">IndexError</span><span class="p">,</span> <span class="ne">TypeError</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">default</span>
</span></span></code></pre></div><p>This is where the story begins. That single function, once you start asking questions about what else you need, leads to a complete ecosystem for data manipulation. The trick is that the questions have a natural structure to them.</p>
<h2 id="the-three-pillars">The Three Pillars</h2>
<p>The ecosystem organizes around three fundamental questions about data:</p>
<h3 id="depth-pillar-where-is-the-data">Depth Pillar: &ldquo;Where is the data?&rdquo;</h3>
<p>Tools for finding and extracting values from within documents.</p>
<table>
	<thead>
			<tr>
					<th>Tool</th>
					<th>Purpose</th>
					<th>Complexity</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><strong>dotget</strong></td>
					<td>Simple exact paths</td>
					<td><code>get(data, &quot;user.name&quot;)</code></td>
			</tr>
			<tr>
					<td><strong>dotstar</strong></td>
					<td>Wildcard patterns</td>
					<td><code>search(data, &quot;users.*.name&quot;)</code></td>
			</tr>
			<tr>
					<td><strong>dotselect</strong></td>
					<td>Advanced selection with predicates</td>
					<td><code>find_first(data, &quot;users[role=admin].name&quot;)</code></td>
			</tr>
			<tr>
					<td><strong>dotpath</strong></td>
					<td>Extensible path engine</td>
					<td>Powers all other tools, Turing-complete</td>
			</tr>
	</tbody>
</table>
<p>The addressing layer forms a free algebra on selectors, with operators being morphisms in the Kleisli category of the powerset monad. In practice this means <code>dotstar</code> composed with <code>dotselect</code> still yields a well-defined set of values. You can compose these things without worrying about edge cases blowing up.</p>
<h3 id="truth-pillar-is-this-assertion-true">Truth Pillar: &ldquo;Is this assertion true?&rdquo;</h3>
<p>Tools for asking boolean questions and validating data.</p>
<table>
	<thead>
			<tr>
					<th>Tool</th>
					<th>Purpose</th>
					<th>Logic</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><strong>dotexists</strong></td>
					<td>Path existence</td>
					<td><code>check(data, &quot;user.email&quot;)</code></td>
			</tr>
			<tr>
					<td><strong>dotany</strong></td>
					<td>Existential quantifier</td>
					<td><code>any_match(data, &quot;users.*.role&quot;, &quot;admin&quot;)</code></td>
			</tr>
			<tr>
					<td><strong>dotall</strong></td>
					<td>Universal quantifier</td>
					<td><code>all_match(data, &quot;users.*.status&quot;, &quot;active&quot;)</code></td>
			</tr>
			<tr>
					<td><strong>dotquery</strong></td>
					<td>Compositional logic engine</td>
					<td><code>Query(&quot;any equals role admin&quot;).check(data)</code></td>
			</tr>
	</tbody>
</table>
<p>Predicates form a Boolean algebra under conjunction, disjunction, and negation that is homomorphic to set algebra on result subsets. This enables short-circuit evaluation and distributive laws. The math isn&rsquo;t decoration; it&rsquo;s what makes the composition reliable.</p>
<h3 id="shape-pillar-how-should-the-data-be-transformed">Shape Pillar: &ldquo;How should the data be transformed?&rdquo;</h3>
<p>Tools for reshaping and modifying data structures.</p>
<table>
	<thead>
			<tr>
					<th>Tool</th>
					<th>Purpose</th>
					<th>Type</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><strong>dotmod</strong></td>
					<td>Surgical modifications</td>
					<td><code>set_(data, &quot;user.status&quot;, &quot;inactive&quot;)</code></td>
			</tr>
			<tr>
					<td><strong>dotbatch</strong></td>
					<td>Atomic transactions</td>
					<td>Apply multiple changes safely</td>
			</tr>
			<tr>
					<td><strong>dotpipe</strong></td>
					<td>Data transformation pipelines</td>
					<td>Reshape documents into new forms</td>
			</tr>
			<tr>
					<td><strong>dotpluck</strong></td>
					<td>Value extraction</td>
					<td>Create new structures from selections</td>
			</tr>
	</tbody>
</table>
<p>Transformations are endofunctors on document spaces with monoid composition. <code>dotmod</code> implements lenses with put-get laws, while <code>dotpipe</code> provides Kleisli composition of pure functions.</p>
<h2 id="the-pedagogical-gradient">The Pedagogical Gradient</h2>
<p>The ecosystem is designed as a learning path. Each level builds on the previous, and no tool becomes obsolete.</p>
<p><strong>1. Hello World</strong> (minimal mental load):</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">depth.dotget.core</span> <span class="kn">import</span> <span class="n">get</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">truth.dotexists.core</span> <span class="kn">import</span> <span class="n">check</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">data</span> <span class="o">=</span> <span class="p">{</span><span class="s2">&#34;users&#34;</span><span class="p">:</span> <span class="p">[{</span><span class="s2">&#34;name&#34;</span><span class="p">:</span> <span class="s2">&#34;Alice&#34;</span><span class="p">}]}</span>
</span></span><span class="line"><span class="cl"><span class="n">name</span> <span class="o">=</span> <span class="n">get</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="s2">&#34;users.0.name&#34;</span><span class="p">)</span>  <span class="c1"># &#34;Alice&#34;</span>
</span></span><span class="line"><span class="cl"><span class="n">exists</span> <span class="o">=</span> <span class="n">check</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="s2">&#34;users.0.email&#34;</span><span class="p">)</span>  <span class="c1"># False</span>
</span></span></code></pre></div><p><strong>2. Pattern Phase</strong> (wildcards and basic changes):</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">depth.dotstar.core</span> <span class="kn">import</span> <span class="n">search</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">shape.dotmod.core</span> <span class="kn">import</span> <span class="n">set_</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">all_names</span> <span class="o">=</span> <span class="n">search</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="s2">&#34;users.*.name&#34;</span><span class="p">)</span>  <span class="c1"># [&#34;Alice&#34;]</span>
</span></span><span class="line"><span class="cl"><span class="n">new_data</span> <span class="o">=</span> <span class="n">set_</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="s2">&#34;users.0.status&#34;</span><span class="p">,</span> <span class="s2">&#34;active&#34;</span><span class="p">)</span>  <span class="c1"># Immutable!</span>
</span></span></code></pre></div><p><strong>3. Power User</strong> (complex operations):</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">truth.dotquery.core</span> <span class="kn">import</span> <span class="n">Query</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Compositional boolean logic</span>
</span></span><span class="line"><span class="cl"><span class="n">query</span> <span class="o">=</span> <span class="n">Query</span><span class="p">(</span><span class="s2">&#34;equals role admin and greater login_count 10&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">admins</span> <span class="o">=</span> <span class="p">[</span><span class="n">user</span> <span class="k">for</span> <span class="n">user</span> <span class="ow">in</span> <span class="n">users</span> <span class="k">if</span> <span class="n">query</span><span class="o">.</span><span class="n">check</span><span class="p">(</span><span class="n">user</span><span class="p">)]</span>
</span></span></code></pre></div><p><strong>4. Expert</strong> (extensible engines):</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">depth.dotpath.core</span> <span class="kn">import</span> <span class="n">PathEngine</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">collections.dotrelate.core</span> <span class="kn">import</span> <span class="n">join</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Custom selector primitives</span>
</span></span><span class="line"><span class="cl"><span class="n">engine</span> <span class="o">=</span> <span class="n">PathEngine</span><span class="p">()</span>
</span></span><span class="line"><span class="cl"><span class="n">engine</span><span class="o">.</span><span class="n">register_selector</span><span class="p">(</span><span class="n">FuzzyKeySelector</span><span class="p">())</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Relational operations</span>
</span></span><span class="line"><span class="cl"><span class="n">enriched</span> <span class="o">=</span> <span class="n">join</span><span class="p">(</span><span class="n">users</span><span class="p">,</span> <span class="n">orders</span><span class="p">,</span> <span class="n">left_on</span><span class="o">=</span><span class="s2">&#34;id&#34;</span><span class="p">,</span> <span class="n">right_on</span><span class="o">=</span><span class="s2">&#34;user_id&#34;</span><span class="p">)</span>
</span></span></code></pre></div><p>A <code>dotget</code> call is still the right choice when you know the exact path. You don&rsquo;t outgrow the simple tools; you add more when you need them.</p>
<h2 id="the-steal-this-code-philosophy">The &ldquo;Steal This Code&rdquo; Philosophy</h2>
<p>Many tools are intentionally simple enough that you can copy their core logic rather than add a dependency:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="c1"># The entire essence of dotget</span>
</span></span><span class="line"><span class="cl"><span class="k">def</span> <span class="nf">get</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="n">path</span><span class="p">,</span> <span class="n">default</span><span class="o">=</span><span class="kc">None</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">    <span class="k">try</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="n">segment</span> <span class="ow">in</span> <span class="n">path</span><span class="o">.</span><span class="n">split</span><span class="p">(</span><span class="s1">&#39;.&#39;</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">            <span class="n">data</span> <span class="o">=</span> <span class="n">data</span><span class="p">[</span><span class="nb">int</span><span class="p">(</span><span class="n">segment</span><span class="p">)]</span> <span class="k">if</span> <span class="n">segment</span><span class="o">.</span><span class="n">isdigit</span><span class="p">()</span> <span class="ow">and</span> <span class="nb">isinstance</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="nb">list</span><span class="p">)</span> <span class="k">else</span> <span class="n">data</span><span class="p">[</span><span class="n">segment</span><span class="p">]</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">data</span>
</span></span><span class="line"><span class="cl">    <span class="k">except</span> <span class="p">(</span><span class="ne">KeyError</span><span class="p">,</span> <span class="ne">IndexError</span><span class="p">,</span> <span class="ne">TypeError</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">default</span>
</span></span></code></pre></div><p>Make each program do one thing so well that its implementation becomes obvious. If you don&rsquo;t need the rest of the suite, just take what you need.</p>
<h2 id="lifting-to-collections">Lifting to Collections</h2>
<p>Once you have operations on single documents, you can lift them to collections. This is where the category theory actually pays off.</p>
<h3 id="boolean-wing-filtering">Boolean Wing (Filtering)</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">collections.dotfilter.core</span> <span class="kn">import</span> <span class="n">filter_collection</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Filter documents matching a predicate</span>
</span></span><span class="line"><span class="cl"><span class="n">admins</span> <span class="o">=</span> <span class="n">filter_collection</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">    <span class="n">users</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">Query</span><span class="p">(</span><span class="s2">&#34;equals role admin&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Boolean algebra on collections</span>
</span></span><span class="line"><span class="cl"><span class="n">active_admins</span> <span class="o">=</span> <span class="n">filter_collection</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">    <span class="n">admins</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">Query</span><span class="p">(</span><span class="s2">&#34;equals status active&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span></code></pre></div><p>Every collection operator is a monoid homomorphism with respect to multiset union, which means you can parallelize and stream without thinking about correctness.</p>
<h3 id="relational-wing-joining-and-transforming">Relational Wing (Joining and Transforming)</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">collections.dotrelate.core</span> <span class="kn">import</span> <span class="n">join</span><span class="p">,</span> <span class="n">project</span><span class="p">,</span> <span class="n">union</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Relational algebra on document collections</span>
</span></span><span class="line"><span class="cl"><span class="n">user_orders</span> <span class="o">=</span> <span class="n">join</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">    <span class="n">users</span><span class="p">,</span> <span class="n">orders</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">left_on</span><span class="o">=</span><span class="s2">&#34;id&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">right_on</span><span class="o">=</span><span class="s2">&#34;user_id&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">how</span><span class="o">=</span><span class="s2">&#34;left&#34;</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Project to specific fields</span>
</span></span><span class="line"><span class="cl"><span class="n">summary</span> <span class="o">=</span> <span class="n">project</span><span class="p">(</span><span class="n">user_orders</span><span class="p">,</span> <span class="p">[</span><span class="s2">&#34;name&#34;</span><span class="p">,</span> <span class="s2">&#34;total_spent&#34;</span><span class="p">])</span>
</span></span></code></pre></div><p>This mirrors database theory: single-document logic is like tuple calculus; <code>dotrelate</code> is like relational algebra.</p>
<h2 id="command-line-first">Command-Line First</h2>
<p>Every tool works from the command line:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Check if any user is an admin</span>
</span></span><span class="line"><span class="cl">cat users.json <span class="p">|</span> dotquery <span class="s2">&#34;any equals role admin&#34;</span> <span class="o">&amp;&amp;</span> <span class="nb">echo</span> <span class="s2">&#34;Admin found&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Extract all email addresses</span>
</span></span><span class="line"><span class="cl">cat contacts.json <span class="p">|</span> dotstar <span class="s2">&#34;contacts.*.email&#34;</span> &gt; emails.txt
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Join users with their orders</span>
</span></span><span class="line"><span class="cl">dotrelate join --on<span class="o">=</span><span class="s2">&#34;user_id&#34;</span> users.jsonl orders.jsonl
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Filter pipeline</span>
</span></span><span class="line"><span class="cl">cat books.json <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> dotquery <span class="s2">&#34;less price 10 or equals author &#39;Tolkien&#39;&#34;</span> <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> dotquery and <span class="s2">&#34;equals in_stock true&#34;</span> <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> dotquery resolve
</span></span></code></pre></div><h2 id="dual-apis-programmatic-and-declarative">Dual APIs: Programmatic and Declarative</h2>
<p>Many tools offer both Python APIs and serializable query formats:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="c1"># Programmatic (Python API)</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">truth.dotquery.primitives</span> <span class="kn">import</span> <span class="n">equals</span><span class="p">,</span> <span class="n">greater</span>
</span></span><span class="line"><span class="cl"><span class="n">query</span> <span class="o">=</span> <span class="n">Query</span><span class="p">(</span><span class="n">equals</span><span class="p">(</span><span class="s1">&#39;role&#39;</span><span class="p">,</span> <span class="s1">&#39;admin&#39;</span><span class="p">)</span> <span class="o">&amp;</span> <span class="n">greater</span><span class="p">(</span><span class="s1">&#39;login_count&#39;</span><span class="p">,</span> <span class="mi">10</span><span class="p">))</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Declarative (string DSL)</span>
</span></span><span class="line"><span class="cl"><span class="n">query</span> <span class="o">=</span> <span class="n">Query</span><span class="p">(</span><span class="s2">&#34;equals role admin and greater login_count 10&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Both produce the same AST</span>
</span></span><span class="line"><span class="cl"><span class="k">assert</span> <span class="n">query</span><span class="o">.</span><span class="n">ast</span> <span class="o">==</span> <span class="n">other_query</span><span class="o">.</span><span class="n">ast</span>
</span></span></code></pre></div><p>This means you can serialize queries to JSON, send them over APIs, version control them in git, and use them from the command line. Same semantics everywhere.</p>
<h2 id="mathematical-foundation">Mathematical Foundation</h2>
<p>For those who care about the formal structure (I do):</p>
<h3 id="type-signatures">Type Signatures</h3>
<p><strong>Depth Pillar</strong> (Addressing):
</p>
\[
\text{dotget}: \mathcal{D} \times \text{Path}_{\text{exact}} \to V \cup \lbrace \emptyset\rbrace
\]<p>
</p>
\[
\text{dotstar}: \mathcal{D} \times \text{Path}_{*} \to V^{*}
\]<p>
</p>
\[
\text{dotpath}: \text{Free algebra on selectors (Turing-complete)}
\]<p><strong>Truth Pillar</strong> (Predicates):
</p>
\[
\text{dotexists}: \mathcal{D} \times P \to \mathbb{B}
\]<p>
</p>
\[
\text{dotquery}: \mathcal{D} \times \mathcal{L}_{\text{BOOL}}(\varphi_i) \to \mathbb{B}
\]<p><strong>Shape Pillar</strong> (Transformations):
</p>
\[
\text{dotmod}: \mathcal{D} \times \delta \to \mathcal{D} \quad \text{(lens with put-get law)}
\]<p>
</p>
\[
\text{dotpipe}: \mathcal{D} \times F^{*} \to \mathcal{D} \quad \text{(Kleisli composition)}
\]<h3 id="functorial-lifting">Functorial Lifting</h3>
<p>Operations lift to collections via functors:</p>
\[
\text{map}: (\mathcal{D} \to X) \to (C \to X^{*})
\]<p>
</p>
\[
\text{filter}: (\mathcal{D} \to \mathbb{B}) \to (C \to C)
\]<p>Where \(C \subseteq \mathcal{D}\) is a finite multiset (collection).</p>
<h3 id="key-properties">Key Properties</h3>
<ol>
<li><strong>Compositionality</strong>: Operators form algebras (Boolean, monoidal, Kleisli) guaranteeing local reasoning</li>
<li><strong>Purity</strong>: Functions are referentially transparent, so concurrency is trivial</li>
<li><strong>Totality</strong>: Missing paths yield the empty set rather than exceptions</li>
<li><strong>Orthogonality</strong>: No operator belongs to multiple pillars</li>
<li><strong>Homomorphism</strong>: Collection operators preserve structure under union</li>
</ol>
<h2 id="real-world-examples">Real-World Examples</h2>
<h3 id="data-pipeline-processing">Data Pipeline Processing</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">depth.dotstar.core</span> <span class="kn">import</span> <span class="n">search</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">truth.dotquery.core</span> <span class="kn">import</span> <span class="n">Query</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">shape.dotpipe.core</span> <span class="kn">import</span> <span class="n">pipe</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Extract -&gt; Filter -&gt; Transform pipeline</span>
</span></span><span class="line"><span class="cl"><span class="n">pipeline</span> <span class="o">=</span> <span class="n">pipe</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">    <span class="k">lambda</span> <span class="n">doc</span><span class="p">:</span> <span class="n">search</span><span class="p">(</span><span class="n">doc</span><span class="p">,</span> <span class="s2">&#34;transactions.*.amount&#34;</span><span class="p">),</span>
</span></span><span class="line"><span class="cl">    <span class="k">lambda</span> <span class="n">amounts</span><span class="p">:</span> <span class="p">[</span><span class="n">a</span> <span class="k">for</span> <span class="n">a</span> <span class="ow">in</span> <span class="n">amounts</span> <span class="k">if</span> <span class="n">a</span> <span class="o">&gt;</span> <span class="mi">100</span><span class="p">],</span>
</span></span><span class="line"><span class="cl">    <span class="k">lambda</span> <span class="n">large</span><span class="p">:</span> <span class="nb">sum</span><span class="p">(</span><span class="n">large</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">total_large_transactions</span> <span class="o">=</span> <span class="n">pipeline</span><span class="p">(</span><span class="n">financial_data</span><span class="p">)</span>
</span></span></code></pre></div><h3 id="etl-with-relational-operations">ETL with Relational Operations</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">collections.dotrelate.core</span> <span class="kn">import</span> <span class="n">join</span><span class="p">,</span> <span class="n">project</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Join users with orders</span>
</span></span><span class="line"><span class="cl"><span class="n">user_orders</span> <span class="o">=</span> <span class="n">join</span><span class="p">(</span><span class="n">users</span><span class="p">,</span> <span class="n">orders</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                   <span class="n">left_on</span><span class="o">=</span><span class="s2">&#34;user_id&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                   <span class="n">right_on</span><span class="o">=</span><span class="s2">&#34;customer_id&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Join with products</span>
</span></span><span class="line"><span class="cl"><span class="n">full_data</span> <span class="o">=</span> <span class="n">join</span><span class="p">(</span><span class="n">user_orders</span><span class="p">,</span> <span class="n">products</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                 <span class="n">left_on</span><span class="o">=</span><span class="s2">&#34;product_id&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                 <span class="n">right_on</span><span class="o">=</span><span class="s2">&#34;id&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Project to summary</span>
</span></span><span class="line"><span class="cl"><span class="n">summary</span> <span class="o">=</span> <span class="n">project</span><span class="p">(</span><span class="n">full_data</span><span class="p">,</span> <span class="p">[</span>
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;user.name&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;order.date&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;product.name&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;order.total&#34;</span>
</span></span><span class="line"><span class="cl"><span class="p">])</span>
</span></span></code></pre></div><h3 id="complex-boolean-logic">Complex Boolean Logic</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">truth.dotquery.core</span> <span class="kn">import</span> <span class="n">Query</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Find books that are:</span>
</span></span><span class="line"><span class="cl"><span class="c1"># - Either cheap (&lt;$10) OR by Tolkien</span>
</span></span><span class="line"><span class="cl"><span class="c1"># - AND in stock</span>
</span></span><span class="line"><span class="cl"><span class="c1"># - AND have good reviews (&gt;4 stars)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">query</span> <span class="o">=</span> <span class="n">Query</span><span class="p">(</span><span class="s2">&#34;&#34;&#34;
</span></span></span><span class="line"><span class="cl"><span class="s2">    (less price 10 or equals author &#39;Tolkien&#39;)
</span></span></span><span class="line"><span class="cl"><span class="s2">    and equals in_stock true
</span></span></span><span class="line"><span class="cl"><span class="s2">    and greater rating 4.0
</span></span></span><span class="line"><span class="cl"><span class="s2">&#34;&#34;&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">matching_books</span> <span class="o">=</span> <span class="p">[</span><span class="n">book</span> <span class="k">for</span> <span class="n">book</span> <span class="ow">in</span> <span class="n">catalog</span> <span class="k">if</span> <span class="n">query</span><span class="o">.</span><span class="n">check</span><span class="p">(</span><span class="n">book</span><span class="p">)]</span>
</span></span></code></pre></div><h2 id="design-principles">Design Principles</h2>
<ul>
<li><strong>Compositionality</strong>: Every tool composes cleanly with others</li>
<li><strong>Immutability</strong>: Original data is never modified</li>
<li><strong>Pedagogical</strong>: Simple tools graduate to powerful ones</li>
<li><strong>Single Purpose</strong>: Each tool has one clear responsibility</li>
<li><strong>Interoperability</strong>: Common patterns work across all tools</li>
<li><strong>Safety</strong>: Graceful handling of missing paths and malformed data</li>
</ul>
<h2 id="category-theory-for-the-curious">Category Theory for the Curious</h2>
<p>For those interested in the theoretical foundations:</p>
<p><strong>Paths as Optics</strong>: The addressing layer implements lenses and prisms from optics theory, providing composable getters and setters.</p>
<p><strong>Predicates as Subobjects</strong>: Boolean queries correspond to subobjects in the topos of sets, with conjunction/disjunction/negation being categorical limits/colimits/exponentials.</p>
<p><strong>Transformations as Endofunctors</strong>: Shape operations are endofunctors on the category of documents, with natural transformations providing the composition structure.</p>
<p><strong>Collections as Functorial Lift</strong>: The map/filter operations on collections are functors from the category of single-document operations to the category of collection operations, preserving algebraic structure.</p>
<p>This isn&rsquo;t mathematical window dressing. These properties guarantee that composition behaves predictably and that parallel evaluation is always safe. When I say &ldquo;this composes,&rdquo; I mean it in the category-theoretic sense, not the marketing sense.</p>
<h2 id="quick-start">Quick Start</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Install from PyPI (once published)</span>
</span></span><span class="line"><span class="cl">pip install dotsuite
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Or install from source</span>
</span></span><span class="line"><span class="cl">git clone https://github.com/queelius/dotsuite.git
</span></span><span class="line"><span class="cl"><span class="nb">cd</span> dotsuite
</span></span><span class="line"><span class="cl">pip install -e .
</span></span></code></pre></div><p>Basic usage:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">depth.dotget.core</span> <span class="kn">import</span> <span class="n">get</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">depth.dotstar.core</span> <span class="kn">import</span> <span class="n">search</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">truth.dotquery.core</span> <span class="kn">import</span> <span class="n">Query</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">shape.dotmod.core</span> <span class="kn">import</span> <span class="n">set_</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Simple addressing</span>
</span></span><span class="line"><span class="cl"><span class="n">data</span> <span class="o">=</span> <span class="p">{</span><span class="s2">&#34;users&#34;</span><span class="p">:</span> <span class="p">[{</span><span class="s2">&#34;name&#34;</span><span class="p">:</span> <span class="s2">&#34;Alice&#34;</span><span class="p">,</span> <span class="s2">&#34;role&#34;</span><span class="p">:</span> <span class="s2">&#34;admin&#34;</span><span class="p">}]}</span>
</span></span><span class="line"><span class="cl"><span class="n">name</span> <span class="o">=</span> <span class="n">get</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="s2">&#34;users.0.name&#34;</span><span class="p">)</span>  <span class="c1"># &#34;Alice&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Pattern matching</span>
</span></span><span class="line"><span class="cl"><span class="n">all_names</span> <span class="o">=</span> <span class="n">search</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="s2">&#34;users.*.name&#34;</span><span class="p">)</span>  <span class="c1"># [&#34;Alice&#34;]</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Boolean queries</span>
</span></span><span class="line"><span class="cl"><span class="n">is_admin</span> <span class="o">=</span> <span class="n">Query</span><span class="p">(</span><span class="s2">&#34;equals role admin&#34;</span><span class="p">)</span><span class="o">.</span><span class="n">check</span><span class="p">(</span><span class="n">data</span><span class="p">[</span><span class="s2">&#34;users&#34;</span><span class="p">][</span><span class="mi">0</span><span class="p">])</span>  <span class="c1"># True</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Immutable modifications</span>
</span></span><span class="line"><span class="cl"><span class="n">new_data</span> <span class="o">=</span> <span class="n">set_</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="s2">&#34;users.0.status&#34;</span><span class="p">,</span> <span class="s2">&#34;active&#34;</span><span class="p">)</span>
</span></span></code></pre></div><h2 id="resources">Resources</h2>
<ul>
<li><strong>Repository</strong>: <a href="https://github.com/queelius/dotsuite">github.com/queelius/dotsuite</a></li>
<li><strong>Philosophy</strong>: <a href="https://github.com/queelius/dotsuite/blob/master/docs/philosophy.md">docs/philosophy.md</a></li>
<li><strong>Formal Spec</strong>: <a href="https://github.com/queelius/dotsuite/blob/master/docs/dot-formalism.md">docs/dot-formalism.md</a></li>
<li><strong>Tool Docs</strong>: Individual README for each tool</li>
<li><strong>JAF Comparison</strong>: <a href="https://github.com/queelius/dotsuite/blob/master/docs/jaf-comparison.md">docs/jaf-comparison.md</a></li>
</ul>
<h2 id="license">License</h2>
<p>MIT</p>
]]></content:encoded><media:content url="https://metafunctor.com/post/2024-12-dotsuite/card.jpg" medium="image"><media:title type="plain">Category-Theory</media:title></media:content><category>Python</category><category>data-structures</category><category>JSON</category><category>functional-programming</category><category>API-design</category><category>category-theory</category><category>compositionality</category><category>Unix-philosophy</category></item></channel></rss>