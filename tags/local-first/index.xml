<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0"
     xmlns:atom="http://www.w3.org/2005/Atom"
     xmlns:content="http://purl.org/rss/1.0/modules/content/"
     xmlns:dc="http://purl.org/dc/elements/1.1/"
     xmlns:media="http://search.yahoo.com/mrss/">
  <channel>
    <title>Local-First on metafunctor</title>
    <link>https://metafunctor.com/tags/local-first/</link>
    <description>Research Engineer &amp; Computer Scientist - Machine Learning, Statistical Computing, Open Source Development</description>
    <language>en-us</language>
    <managingEditor>lex@metafunctor.com (Alex Towell)</managingEditor>
    <webMaster>lex@metafunctor.com (Alex Towell)</webMaster>
    
    <lastBuildDate>Tue, 05 May 2026 08:26:30 -0500</lastBuildDate>
    <atom:link href="https://metafunctor.com/tags/local-first/" rel="self" type="application/rss+xml" />
    <image>
      <url>https://metafunctor.com/images/og-image.jpg</url>
      <title>metafunctor</title>
      <link>https://metafunctor.com/</link>
    </image>
    
    <item>
      <title>I Tried to Voice-Clone Myself in 90 MB</title>
      <link>https://metafunctor.com/post/2026-04-26-behavioral-fine-tuning-without-the-tuning/</link>
      <pubDate>Sun, 26 Apr 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">https://metafunctor.com/post/2026-04-26-behavioral-fine-tuning-without-the-tuning/</guid>
      <description>A tiny LLM in the browser, mixed at sample time with a token-level n-gram trained on every word I have published. Result is mediocre. Architecture is interesting. Notes on what worked, what didn&#39;t, and what would make it work.</description>
      <content:encoded><![CDATA[<p>The experiment: a tiny SmolLM2 running in your browser. A token-level n-gram trained on every word I have published. Mix the two distributions in probability space at every generation step. Sample from the mix.</p>
<p>You can try it at <a href="/ask/">/ask</a>. There is a slider for how strongly the n-gram bleeds in.</p>
<p>I expected a small chatbot that sounds like me. I got a 135M parameter model that uses my words to produce paranoid lorem-ipsum, and a 1.7B model that mostly behaves like a competent chatbot decorated with my function words. Sometimes a phrase comes out that I might actually write. More often the output is grammatically OK but conceptually empty.</p>
<p>The result is weak. The architecture is interesting anyway, and I want to write up why.</p>
<h2 id="the-pieces">The Pieces</h2>
<p>Three things, all running locally:</p>
<ol>
<li>
<p>SmolLM2-Instruct in 135M, 360M, or 1.7B sizes. Q4_K_M GGUF, served from HuggingFace, run via <a href="https://github.com/ngxson/wllama">Wllama</a>. 90 MB to 1 GB on disk. CPU only.</p>
</li>
<li>
<p>A token-level n-gram over my blog corpus: every post tokenized with SmolLM2&rsquo;s BPE, indexed with a <a href="https://github.com/queelius/infinigram">suffix array</a>. 1.6 MB of source text, 470,000 tokens, 1.9 MB suffix array.</p>
</li>
<li>
<p>A token-by-token sampling loop that mixes the LLM&rsquo;s output distribution with the n-gram&rsquo;s, in probability space.</p>
</li>
</ol>
<p>The third piece is the part worth thinking about.</p>
<h2 id="the-math">The Math</h2>
<p>At each generation step the LLM produces <code>p_llm(t)</code> over its 49,152-token vocabulary, exposed by Wllama&rsquo;s <code>getLogits(-1)</code>. The n-gram, given the longest suffix of the current context that occurs in the corpus, produces a sparse <code>p_ngram(t)</code>: nonzero on tokens it has seen following that context, zero elsewhere.</p>
<p>Linear combination:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">p_mix(t) = α · p_ngram(t) + (1 − α) · p_llm(t)
</span></span></code></pre></div><p>That is the whole algorithm. The inner loop is small enough to fit on screen:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-js" data-lang="js"><span class="line"><span class="cl"><span class="k">for</span> <span class="p">(</span><span class="kd">let</span> <span class="nx">step</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="nx">step</span> <span class="o">&lt;</span> <span class="nx">N</span><span class="p">;</span> <span class="nx">step</span><span class="o">++</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="kr">const</span> <span class="nx">llm</span> <span class="o">=</span> <span class="kr">await</span> <span class="nx">wllama</span><span class="p">.</span><span class="nx">getLogits</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">  <span class="kr">const</span> <span class="nx">m</span> <span class="o">=</span> <span class="nx">ig</span><span class="p">.</span><span class="nx">longestSuffixMatch</span><span class="p">(</span><span class="nx">context</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">  <span class="kr">const</span> <span class="nx">ngram</span> <span class="o">=</span> <span class="nx">m</span><span class="p">.</span><span class="nx">suffixLen</span> <span class="o">&gt;</span> <span class="mi">0</span>
</span></span><span class="line"><span class="cl">    <span class="o">?</span> <span class="k">new</span> <span class="nx">Map</span><span class="p">(</span><span class="nx">ig</span><span class="p">.</span><span class="nx">continuations</span><span class="p">(</span><span class="nx">m</span><span class="p">.</span><span class="nx">matchedTokens</span><span class="p">).</span><span class="nx">map</span><span class="p">(</span><span class="nx">c</span> <span class="p">=&gt;</span> <span class="p">[</span><span class="nx">c</span><span class="p">.</span><span class="nx">token</span><span class="p">,</span> <span class="nx">c</span><span class="p">.</span><span class="nx">prob</span><span class="p">]))</span>
</span></span><span class="line"><span class="cl">    <span class="o">:</span> <span class="kc">null</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">  <span class="kr">const</span> <span class="nx">mix</span> <span class="o">=</span> <span class="k">new</span> <span class="nx">Map</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">  <span class="k">for</span> <span class="p">(</span><span class="kr">const</span> <span class="p">{</span> <span class="nx">token</span><span class="p">,</span> <span class="nx">p</span> <span class="p">}</span> <span class="k">of</span> <span class="nx">llm</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kr">const</span> <span class="nx">pn</span> <span class="o">=</span> <span class="nx">ngram</span><span class="o">?</span><span class="p">.</span><span class="nx">get</span><span class="p">(</span><span class="nx">token</span><span class="p">)</span> <span class="o">??</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="nx">mix</span><span class="p">.</span><span class="nx">set</span><span class="p">(</span><span class="nx">token</span><span class="p">,</span> <span class="nx">alpha</span> <span class="o">*</span> <span class="nx">pn</span> <span class="o">+</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="nx">alpha</span><span class="p">)</span> <span class="o">*</span> <span class="nx">p</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">  <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">  <span class="kr">const</span> <span class="nx">next</span> <span class="o">=</span> <span class="nx">sample</span><span class="p">(</span><span class="nx">mix</span><span class="p">,</span> <span class="nx">temperature</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">  <span class="k">if</span> <span class="p">(</span><span class="kr">await</span> <span class="nx">wllama</span><span class="p">.</span><span class="nx">isTokenEOG</span><span class="p">(</span><span class="nx">next</span><span class="p">))</span> <span class="k">break</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">  <span class="nx">context</span><span class="p">.</span><span class="nx">push</span><span class="p">(</span><span class="nx">next</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">  <span class="kr">await</span> <span class="nx">wllama</span><span class="p">.</span><span class="nx">decode</span><span class="p">([</span><span class="nx">next</span><span class="p">]);</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Tokens unseen by the n-gram have <code>p_ngram = 0</code> and retain <code>(1 − α) · p_llm</code> in the mixture. They are not zeroed out, just unboosted.</p>
<p>α = 0 is the LLM. α = 1 is the n-gram, which loops as soon as the generated context drifts off-corpus. In between is a model running on LLM grammar with n-gram register.</p>
<h2 id="what-it-actually-produces">What It Actually Produces</h2>
<p>The 135M, prompt &ldquo;Hello&rdquo;, α = 0.1:</p>
<blockquote>
<p>Hi! I&rsquo;m working on a project with you! I&rsquo;m trying to hide some of your work. I&rsquo;ll have to wait for your response, but I&rsquo;ll give you a brief summary to let you know what I&rsquo;m working on. Hope you&rsquo;re doing well!</p>
</blockquote>
<p>The vocabulary is mine. I write about hiding data, encrypted search, comparing approaches. The 135M has no idea what those concepts mean. It glues my words into paragraphs that read like a paranoid academic argument with the visitor.</p>
<p>The 1.7B is better but the failure mode is more subtle. With α = 0.1 it produces grammatical paragraphs that drop in occasional Alex-shaped phrases (&ldquo;compositional depth&rdquo;, &ldquo;rules as data&rdquo;, project names from my corpus). At α = 0 it sounds like a generic chatbot. At α = 1 it sounds like me, in fragments, looping.</p>
<p>What mixing-at-sample-time actually does, I think, is pull register without pulling competence. The n-gram makes the surface me-shaped. It does not give the model any of the conceptual machinery that lets me mean something when I use those words. The output is texture, not thought.</p>
<h2 id="why-the-architecture-is-interesting-anyway">Why The Architecture Is Interesting Anyway</h2>
<p>The result is weak, but the structural properties are real:</p>
<p><strong>The corpus stays uncompressed.</strong> Conventional fine-tuning is a lossy gradient encoding of your training distribution. Here, the corpus ships in full. The &ldquo;fine-tune&rdquo; is a static asset you can read.</p>
<p><strong>Adding a document is a 30 ms rebuild.</strong> Not a training run.</p>
<p><strong>Removing a document is the same operation reversed.</strong> No question of whether the model &ldquo;forgot&rdquo; it. There is no gradient that could remember.</p>
<p><strong>Auditing memorization is a binary search.</strong> O(log n) to find every occurrence of a span in the corpus. With gradient-descent fine-tuning, you cannot easily answer this.</p>
<p><strong>Tokenizer parity makes model swaps free.</strong> All three SmolLM2 sizes share the BPE, so the same suffix array works against any of them.</p>
<p>These properties are why I think the idea has legs even though the demo doesn&rsquo;t.</p>
<h2 id="whats-wrong-with-it">What&rsquo;s Wrong With It</h2>
<p>The failure modes I observed, mostly so I know what to fix:</p>
<p><strong>Linear mixing is a soft prior, not a guide.</strong> It lifts tokens. It does not constrain meaning. The model still hallucinates; the prior just makes it hallucinate in my register.</p>
<p><strong>Token-level matching is too local.</strong> The n-gram looks at the longest in-corpus suffix, which is usually a handful of tokens. Enough for register, not enough for thematic coherence. Sequential sentences can pull from completely different parts of the corpus.</p>
<p><strong>The corpus is too small.</strong> 1.6 MB has only so many patterns. With α near 1 the model loops within seconds.</p>
<p><strong>Tiny models cannot be steered into competence.</strong> The 135M under any prior is a syntax engine. With the prior it is a syntax engine with my vocabulary. Useful for a curio, not for a tool.</p>
<p><strong>Temperature interacts unpredictably.</strong> Applying temperature to the mixed distribution is mathematically clean, but small changes to T produce large changes in output character that I cannot tune intuitively.</p>
<h2 id="what-would-make-it-work">What Would Make It Work</h2>
<p>Roughly in order of how much I think each would help:</p>
<p><strong>Passage-level retrieval, also mixed in.</strong> A second prior at the level of full passages, retrieved via embedding search, with passage-level distributions mixed alongside the token-level n-gram. Token-level for register, passage-level for theme.</p>
<p><strong>Logit-space mixing instead of probability-space.</strong> <code>log p_mix = log p_llm + λ · log(p_ngram + ε)</code> actively penalizes tokens with low corpus support, rather than only adding lift to high-support ones. Sharper steering.</p>
<p><strong>A different LLM family.</strong> SmolLM2 caps at 1.7B. Qwen 2.5 1.5B or Llama 3.2 3B are similar size and stronger on instruction-following. Requires retokenizing the corpus; the JS doesn&rsquo;t change.</p>
<p><strong>Context-dependent α.</strong> Right now α is fixed. When the longest match is short (the LLM is off-corpus), trust the LLM more. When the match is long (the LLM is producing in-corpus text), trust the prior more. Probably the change with the most upside of these.</p>
<h2 id="a-note-on-local-first">A Note On Local-First</h2>
<p>The reason the whole pipeline runs in the browser, even though the result is weak, is that this is the use case fine-tuning has historically been worst at: people who want a personal model from their own writing, without running a GPU cluster, without uploading their writing anywhere, and without ending up with an opaque-weights artifact they cannot inspect.</p>
<p>Sample-time mixing with a public LLM is one possible answer. The model is a public utility, the corpus is a static asset, the mixing is in user space. Anyone curious about what is being applied to the model can read the code.</p>
<p>This particular implementation is mediocre. The shape of the answer is right.</p>
<h2 id="what-this-is">What This Is</h2>
<p>Not a fine-tune. Not a chatbot that sounds like me. A research artifact. A thing that shows LLM and n-gram can be composed at sample time, with all the structural properties that follow, and that the architecture is worth more work even though this particular composition does not yet land.</p>
<p>The toy is at <a href="/ask/">/ask</a> and <a href="/infinigram/">/infinigram</a>. Have at it.</p>
]]></content:encoded>
      
      
      <category>llm</category>
      
      <category>fine-tuning</category>
      
      <category>infinigram</category>
      
      <category>n-grams</category>
      
      <category>local-first</category>
      
      <category>voice</category>
      
      <category>sample-time</category>
      
      
    </item>
    
    <item>
      <title>Your Blog Will Outlive Your Database (It Doesn&#39;t Have To)</title>
      <link>https://metafunctor.com/post/2026-04-24-your-blog-will-outlive-your-database/</link>
      <pubDate>Fri, 24 Apr 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">https://metafunctor.com/post/2026-04-24-your-blog-will-outlive-your-database/</guid>
      <description>Your reader&#39;s words evaporate while your writer&#39;s words endure. Markdown won the durability war by being boring. Every time we add interactivity, we drag back the database, and durability dies. There&#39;s a missing primitive: writes that are as durable as the reads. Git is that primitive.</description>
      <content:encoded><![CDATA[<p><em>(it doesn&rsquo;t have to)</em></p>
<h2 id="0-the-puzzle">0. The puzzle</h2>
<p>Scroll to the bottom of this page. There&rsquo;s a jigsaw puzzle there, and 47
people have placed pieces in it. Some of them placed a piece an hour
ago. One placed a piece while you were reading this sentence. You can see the
picture assembling itself, tile by tile, toward something that isn&rsquo;t quite
resolved yet.</p>
<p>Every piece placement is a git commit. When you drag a tile into position and it clicks into place, a commit
lands in a public repository. It carries your GitHub username, a timestamp,
and a structured record of which piece went where. The whole puzzle&rsquo;s solving
history is a git log. That log is not stored in a database that I control. It is not behind a
paywall or an API rate limit. It is not going to disappear if I stop paying
for a server. Every person who has ever touched this puzzle is in that log,
and the log will exist as long as one copy of the repository exists somewhere.
Someone can fork it tonight. Someone can clone it in 2035. The history does
not belong to me; it belongs to the commits, which means it belongs to
everyone who made one.</p>
<p>The standard way to build a
puzzle like this would be a database table: <code>piece_id</code>, <code>slot_x</code>, <code>slot_y</code>,
<code>user_id</code>, <code>timestamp</code>. The table lives on a server. The server is somebody&rsquo;s
responsibility. When that person gets tired or goes broke or moves on, the
table disappears, and the history disappears with it. Every comment thread,
every leaderboard, every &ldquo;who solved it first&rdquo; record: gone.</p>
<p>Your forum threads from 2008 are gone.</p>
<h2 id="1-the-asymmetry">1. The asymmetry</h2>
<p>But your 2008 blog post still renders.</p>
<p>If someone published it as static HTML or Markdown on a personal domain and kept
paying eight dollars a year for hosting, the URL still works. The page still
loads. The words are still there. You can read it right now on a browser that
didn&rsquo;t exist when it was written, running on hardware the author never imagined,
fetched over a protocol version that postdates the post itself. None of that
matters. The file is a file.</p>
<p>Markdown won the durability war by being boring. There is no schema to migrate.
No application server to restart. No vendor to outlive. A <code>.md</code> file is a text
file; a text file from 2008 is as readable today as it was then. The boring-ness
is the point. When a format makes no demands on its environment, the environment
can change freely around it. People who chose flat files in 2006 were not
visionaries; they were lazy in the exact right way.</p>
<p>The forum did not have that option. A phpBB community lives only while someone tends the server. The moment the
hosting bill bounces or the moderator takes a new job, the state goes with them. Not just the
posts, but the replies, the edits, the votes, the relationships between pieces of content. When the operator left, the database closed.</p>
<p>And this is not just about old forums. It is the same story for every layer of
interactivity we add to a page today. Want comments? You need mutable state. Want
reactions, edits, collaborative cursors, presence indicators? Each one needs a
write path, and every write path needs a server, and every server needs someone
responsible for it. The content stays up. The interactions disappear.</p>
<p><em>Reads are durable; writes are not.</em></p>
<p>The question is why we ever stopped building things that work that way.</p>
<h2 id="2-why-this-happened">2. Why this happened</h2>
<p>The read path won, and it won completely. Over the 2000s and 2010s, the publishing layer settled into defaults that are genuinely excellent: write a flat file, run it through a static-site generator, push it to a CDN. Jekyll in 2008, Hugo a few years later, GitHub Pages as a free host for anyone with a repository. The result was durable content that anyone could serve from anywhere. That part of the story went right.</p>
<p>The write path never got the memo. While the read path was reinventing itself around flat files and CDN delivery, the write path stayed in the 1995 mold: a server with mutable state, owned by an operator, with someone responsible for paying the bill. Comments, accounts, sessions, edit history, anything that required a user to change something: all of it still went through a database somewhere, administered by someone, dependent on that someone staying interested.</p>
<p>And every product that came along to fix WordPress reproduced the same architectural mistake. Ghost replaced WordPress with a cleaner editor; Substack replaced Ghost with built-in audiences. The hosting changed, the operator-dependency did not. The shape underneath stays exactly the same: a privileged server holding mutable state that the user does not own. Different paint, same chassis.</p>
<p>The Jamstack movement looked like it might break this pattern. Static frontends, decoupled backends, the whole read path served from a CDN. But &ldquo;static frontend plus a SaaS backend&rdquo; is not an architectural improvement; it is the same failure mode with extra steps. The HTML is still durable. The dynamic layer, the comments, the reaction counts, the personalization, still lives in a database somewhere. When that SaaS raises prices or shuts down, the participation layer dies exactly the way the phpBB forum died. The vocabulary changed. The structure did not.</p>
<h2 id="3-the-missing-primitive">3. The missing primitive</h2>
<p>The structure needs one new thing: a write substrate as durable as the read substrate.</p>
<p>It has been sitting in <code>.git/</code> the whole time. Git is append-only: commits are never modified, only accumulated. It is content-addressed: every object in the store is named by a cryptographic hash of its contents, so the history cannot be silently altered. It is signed: commits can carry GPG signatures that bind authorship to a public key. It is fully replicated by every clone: no single server holds the authoritative copy. It is forkable without permission. These properties are the reason version control works at all. They also happen to be exactly the properties that the write path for the web has never had.</p>
<p>The unit of change in this substrate is a git commit, not a SQL row. Call this <strong>commit-as-write</strong>: a structured, signed, append-only record of a reader&rsquo;s action, living in the same repository as the content it touches, replicated everywhere the content is replicated.</p>
<p>Build with commit-as-write as the default and you get <strong>git-native publishing</strong>: the static web with a write path that matches the read path&rsquo;s durability.</p>
<p>The Ink &amp; Switch local-first essay (2019) is the philosophical predecessor to this argument. It named the problem clearly: users should own their data, software should work offline, and nothing should disappear because a vendor stopped paying its server bill. Git-native publishing is local-first applied specifically to the public web&rsquo;s read-write substrate, using git&rsquo;s existing infrastructure. The &ldquo;git-based CMS&rdquo; industry (Decap, TinaCMS, CloudCannon) is the closest existing term, but it describes the wrong layer: those tools put git behind the editorial workflow for site <em>operators</em>, not behind the participation layer for <em>readers</em>. Utterances and Giscus proved that reader writes can live in a GitHub-hosted repository without a separate database, but they write to Issues and Discussions, not to the commit log, and neither project claims to generalize the pattern.</p>
<p>This substrate has a vocabulary REST does not.</p>
<h2 id="4-gits-vocabulary-is-strictly-richer-than-rests">4. Git&rsquo;s vocabulary is strictly richer than REST&rsquo;s</h2>
<p>REST&rsquo;s vocabulary is five verbs applied to named resources: GET, POST, PUT, PATCH, DELETE. That model has been the operating assumption of the writable web for roughly 25 years. It is not wrong; it maps cleanly onto databases, fits HTTP semantics, and scales to most application needs. But it is a model for mutating state, not for accumulating history.</p>
<p>Git&rsquo;s vocabulary includes all five of those operations and adds six that REST has no native equivalent for: <code>branch</code>, <code>tag</code>, <code>merge</code>, <code>fork</code>, signed commit, <code>submodule</code>. They are the core operations that make distributed version control work, and each one carries semantics REST cannot express.</p>
<table>
  <thead>
      <tr>
          <th>REST/DB verb</th>
          <th>Git operation</th>
          <th>What git adds that REST/SQL can&rsquo;t</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>POST</code> (create)</td>
          <td><code>commit</code> (new file)</td>
          <td>signed, time-stamped, cryptographically attributed</td>
      </tr>
      <tr>
          <td><code>PUT</code> (replace)</td>
          <td><code>commit</code> (overwrite)</td>
          <td>prior versions preserved automatically</td>
      </tr>
      <tr>
          <td><code>PATCH</code> (partial)</td>
          <td><code>commit</code> (line-level diff)</td>
          <td>the diff <em>is</em> the structured patch, no separate schema</td>
      </tr>
      <tr>
          <td><code>DELETE</code></td>
          <td><code>commit</code> (remove) or <code>revert</code></td>
          <td>reversible; deletion is a record, not an erasure</td>
      </tr>
      <tr>
          <td><code>GET</code></td>
          <td>read working tree</td>
          <td>or read any historical state, by hash</td>
      </tr>
      <tr>
          <td><code>(none)</code></td>
          <td><code>branch</code></td>
          <td>parallel / private / proposed state, native</td>
      </tr>
      <tr>
          <td><code>(none)</code></td>
          <td><code>tag</code></td>
          <td>named / canonical / published version</td>
      </tr>
      <tr>
          <td><code>(none)</code></td>
          <td><code>merge</code></td>
          <td>consensus and reconciliation as first-class ops</td>
      </tr>
      <tr>
          <td><code>(none)</code></td>
          <td><code>fork</code></td>
          <td>take all your data and leave, lossless</td>
      </tr>
      <tr>
          <td><code>(none)</code></td>
          <td>signed commit</td>
          <td>authentication baked into the data layer</td>
      </tr>
      <tr>
          <td><code>(none)</code></td>
          <td><code>submodule</code></td>
          <td>composable embedded references across repos</td>
      </tr>
  </tbody>
</table>
<p>Two entries in that table carry the most rhetorical weight. A signed commit binds authorship to a public key at the data layer, not at the application layer. You do not need an accounts table or a session store; identity travels with the record itself. A fork means a user can take the entire history and leave: not an export, not a backup request, but a full lossless copy with its own future. A <code>DELETE /users/me</code> removes your account; it does not give you your history.</p>
<p>The deeper point is that git&rsquo;s log is already an event store in the sense Greg Young articulated with event sourcing and CQRS: each commit is a domain event, and the working tree is a projection derived from replaying those events. The same log can feed many different applications via different read projections: a comment widget, a reaction aggregator, a moderation log. None of them require a schema migration when a new projection is added; they just read the same log through a different lens. The commit-as-write primitive that §3 named is, in event-sourcing terms, an append to an immutable event log.</p>
<p>A commit message can carry a typed payload, making the log a free event store with no schema layer:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">op: react
</span></span><span class="line"><span class="cl">target: posts/your-blog-will-outlive-your-database
</span></span><span class="line"><span class="cl">value: 🔥
</span></span><span class="line"><span class="cl">actor: queelius
</span></span><span class="line"><span class="cl">ts: 2026-04-24T22:45:00Z
</span></span></code></pre></div><p>Any reader action that can be expressed as a typed operation and a target fits this shape: a comment, a reaction, a vote. The next section uses a jigsaw puzzle for exactly this reason: discrete pieces, unambiguous positions, multiple actors, shared state.</p>
<h2 id="5-the-jigsaw-a-worked-example">5. The jigsaw, a worked example</h2>
<p>Go back to the puzzle at the bottom of the page. Those 47 people who have placed pieces in this puzzle: every move they made is a commit in a public repository, signed by their GitHub identity, timestamped, carrying a structured payload. When you place a piece, your client pushes something that looks like this:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">op: place
</span></span><span class="line"><span class="cl">piece: 042
</span></span><span class="line"><span class="cl">slot: [3, 7]
</span></span><span class="line"><span class="cl">actor: queelius
</span></span><span class="line"><span class="cl">ts: 2026-04-24T22:47:13Z
</span></span></code></pre></div><p>The shape should look familiar. No schema negotiation. No database column to add. The commit message is the record.</p>
<p>Before that commit lands, a pre-commit hook runs a verifier. Piece 042 either fits in slot [3, 7] or it does not; the source image is the ground truth. Most multi-author write systems cannot validate writes before accepting them, because there is no ground truth to check against: a comment is whatever the user typed, and the server has no way to reject it on correctness grounds. The jigsaw has ground truth. The pre-commit hook can reject a bad placement the way a compiler rejects a type error, not by policy but by reference to something real.</p>
<p>If two readers grab piece 042 at the same moment, one commit lands first. The second client gets a conflict, fetches fresh state, and retries with a piece that is actually available. Git&rsquo;s model is optimistic concurrency: no locking, no coordination, just commit and retry if you collide. No CRDT machinery required; no special conflict-resolution protocol.</p>
<p>Two people placing different pieces never conflict at all. The moves commute: order does not matter for the final picture.</p>
<p>Participation is visceral in a way comments are not. People want to place a piece; they want to see the picture advance. And the picture forming in front of you is the demo itself: you can watch the assembly happen across all contributors in real time.</p>
<p>Each week&rsquo;s puzzle uses a freshly-generated image. Reverse image search returns nothing; the picture is genuinely new to everyone who shows up.</p>
<p>When the puzzle is complete, the solving history is the git log. Anyone can clone it tonight. Anyone can study it in 2035. Every contributor is in that log, signed by the identity they used, in the order they placed their pieces. That record does not belong to me: it belongs to the commits. The log will exist as long as one copy of the repository exists anywhere.</p>
<p>That is the shape of git-native publishing in practice. But there is a class of interactions the commit log cannot hold, and pretending otherwise would be the same mistake this essay is trying to name.</p>
<h2 id="6-honest-limits">6. Honest limits</h2>
<p>This is not a substrate for high-frequency writes. Placing 500 jigsaw pieces per week is fine; 500 tweets per second is not. Git was designed for human-paced collaboration, not real-time mutable state at scale. Twitter, Instagram feeds, live chat: wrong problem. Comments, reactions, puzzle moves, forum threads, slow social: those fit.</p>
<p>The MVP uses GitHub OAuth and the GitHub commit API. That dependency is real but contingent. The architecture binds to git, not to Microsoft. Any git host works. GitHub is where most readers already have accounts; it is the simplest starting point.</p>
<p>Moderation is post-hoc, not pre-hoc. A revert or rebase can remove a bad commit, but the commit has to land first. WordPress&rsquo;s approval queue blocks spam before anyone sees it; this model cannot do that. For open-internet participation with anonymous actors, this is a genuine cost. For contexts where all participants are identified before they can commit, the gap narrows. For open-internet participation, it does not.</p>
<p>The right-to-be-forgotten cuts against append-only history. Git&rsquo;s content-addressing means each commit hash depends on every commit before it. True deletion requires rebasing, and that rebase must be accepted by every clone holder. Cooperation cannot be guaranteed. This is a structural cost, not an engineering problem waiting for a solution: anything that enters the commit payload may sit there forever.</p>
<p>The argument here is not that this beats everything.</p>
<h2 id="7-the-claim-and-the-invitation">7. The claim, and the invitation</h2>
<p>This is git-native publishing. The unit of change is commit-as-write. These are not new tools; they are a new name for a category that has existed without one, and a label for a primitive that has been available since git became ubiquitous.</p>
<p>I am not selling anything. There is no product here. The argument is narrower: the durable write substrate has been absent from the web&rsquo;s read-write architecture since the beginning, and git already provides it for the class of interactions where that absence hurts most. It either holds up or it doesn&rsquo;t.</p>
<p>If you have built something in this shape, I want to know. Not because it validates the category, but because I want to study what you learned. Reach me at <a href="mailto:lex@metafunctor.com">lex@metafunctor.com</a> or as @queelius.</p>
<p>The puzzle is at <code>/arcade/jigsaw</code>. Place a piece. It takes thirty seconds. When you do, your name goes into the commit log, signed by your GitHub identity, alongside everyone else who has touched it.</p>
<p>This essay&rsquo;s own source is markdown in a git repository at <code>github.com/queelius/git-native-publishing</code>. The library that powers the jigsaw is at <code>github.com/queelius/git-native</code>. When you read this essay, you are reading commits in the substrate it is naming. The argument is demonstrated by the thing you are holding.</p>
]]></content:encoded>
      
      
      <category>git-native-publishing</category>
      
      <category>commit-as-write</category>
      
      <category>local-first</category>
      
      <category>static-sites</category>
      
      <category>durability</category>
      
      <category>jamstack</category>
      
      
    </item>
    
  </channel>
</rss>
