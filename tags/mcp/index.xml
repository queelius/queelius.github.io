<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:media="http://search.yahoo.com/mrss/"><channel><title>MCP on metafunctor</title><link>https://metafunctor.com/tags/mcp/</link><description>Research Engineer &amp; Computer Scientist - Machine Learning, Statistical Computing, Open Source Development</description><language>en-us</language><managingEditor>lex@metafunctor.com (Alex Towell)</managingEditor><webMaster>lex@metafunctor.com (Alex Towell)</webMaster><lastBuildDate>Mon, 16 Mar 2026 10:20:10 -0500</lastBuildDate><atom:link href="https://metafunctor.com/tags/mcp/" rel="self" type="application/rss+xml"/><image><url>https://metafunctor.com/images/og-image.jpg</url><title>metafunctor</title><link>https://metafunctor.com/</link></image><item><title>Chartfold: Owning Your Medical Records</title><link>https://metafunctor.com/post/2026-02-24-chartfold-owning-your-medical-records/</link><pubDate>Tue, 24 Feb 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2026-02-24-chartfold-owning-your-medical-records/</guid><description>A walkthrough of Chartfold, a Python tool that loads your medical records into SQLite and exposes them to Claude via MCP for structured analysis, visit prep, and ad-hoc queries.</description><content:encoded><![CDATA[<p>I have cancer. My oncologist is at one hospital system (Siteman/BJC), my primary care doctor at another, and my earlier treatment history lives at a third (Anderson, where my first oncologist practiced). Patient portals are fine for browsing, but they don&rsquo;t answer questions. They show you your data one lab result at a time, one note at a time, one visit at a time.</p>
<p>I wanted to run queries against my medical records. Correlate lab trends with treatment changes. Generate structured question lists before oncology visits. Ask &ldquo;what changed since my last appointment&rdquo; and get a real answer. That means getting the data out of the portal and into something programmable.</p>
<p>Chartfold loads EHR exports into SQLite and exposes them to Claude via MCP.</p>
<hr>
<h2 id="the-problem">The Problem</h2>
<p>In the US, patients can export their medical records. HIPAA and the 21st Century Cures Act guarantee this. What you get depends on the system: Epic MyChart gives you CDA XML files, MEDITECH Expanse gives you FHIR JSON mixed with CCDA XML, athenahealth gives you FHIR R4 Bundles. Different formats, same clinical concepts.</p>
<p>If your hospitals use different EHR systems, none of them have the complete picture. Chartfold merges the exports into one database. But even if you&rsquo;re at a single hospital, the export format is not something you can work with directly. A directory of CDA XML files is not a database. You can&rsquo;t query it, chart it, or hand it to an LLM.</p>
<p>The point of Chartfold is to turn whatever your hospital gives you into a SQLite database, then make that database useful.</p>
<h2 id="what-it-does">What It Does</h2>
<p>Chartfold is a Python CLI. You point it at an EHR export directory, it parses the XML/FHIR, normalizes everything into a common data model (16 tables, ISO dates, deduplicated), and loads it into SQLite. Then you can query it directly, export it as a self-contained HTML dashboard, or connect Claude to it via MCP.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Load data from your hospital exports</span>
</span></span><span class="line"><span class="cl">chartfold load epic ~/exports/epic/
</span></span><span class="line"><span class="cl">chartfold load meditech ~/exports/meditech/
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Query directly</span>
</span></span><span class="line"><span class="cl">chartfold query <span class="s2">&#34;SELECT test_name, value, result_date FROM lab_results
</span></span></span><span class="line"><span class="cl"><span class="s2">                 WHERE test_name LIKE &#39;%CEA%&#39; ORDER BY result_date DESC&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Export a self-contained HTML file</span>
</span></span><span class="line"><span class="cl">chartfold <span class="nb">export</span> html --output chartfold.html
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Start the MCP server for Claude</span>
</span></span><span class="line"><span class="cl">chartfold serve-mcp
</span></span></code></pre></div><hr>
<h2 id="the-claude-integration">The Claude Integration</h2>
<p>This is why Chartfold exists for me.</p>
<p>The MCP server exposes the database to Claude Code. Setup is one file. Drop a <code>.mcp.json</code> in any directory where you run Claude Code:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;mcpServers&#34;</span><span class="p">:</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&#34;chartfold&#34;</span><span class="p">:</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">      <span class="nt">&#34;command&#34;</span><span class="p">:</span> <span class="s2">&#34;python&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">      <span class="nt">&#34;args&#34;</span><span class="p">:</span> <span class="p">[</span><span class="s2">&#34;-m&#34;</span><span class="p">,</span> <span class="s2">&#34;chartfold&#34;</span><span class="p">,</span> <span class="s2">&#34;serve-mcp&#34;</span><span class="p">,</span> <span class="s2">&#34;--db&#34;</span><span class="p">,</span> <span class="s2">&#34;/path/to/chartfold.db&#34;</span><span class="p">]</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">  <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>That&rsquo;s it. Claude now has read access to your entire medical history via SQL, plus tools for saving notes and structured analyses. I keep my database in a private directory and my <code>.mcp.json</code> pointing at it. Open Claude Code, and I&rsquo;m talking to my records.</p>
<p>The kinds of things I actually use it for:</p>
<blockquote>
<p>&ldquo;What&rsquo;s changed since my last oncology visit on January 15?&rdquo;</p>
</blockquote>
<p>Claude writes SQL, reads the results, and gives me a structured diff: new lab results, new imaging, changed medications, new clinical notes.</p>
<blockquote>
<p>&ldquo;Generate a prioritized question list for my appointment with Dr. Tan tomorrow.&rdquo;</p>
</blockquote>
<p>Claude reads my recent labs, imaging reports, pathology, and genomic results, then produces a tiered document organized by clinical urgency.</p>
<blockquote>
<p>&ldquo;Show me my CEA trend and flag any inflection points.&rdquo;</p>
</blockquote>
<p>Claude queries the lab_results table, filters by test name, and walks through the time series.</p>
<p>The analyses get saved back to the database (via dedicated MCP tools) and appear in the HTML export as tagged, searchable documents.</p>
<p><img src="analysis-cards.png" alt="Analysis section showing Claude-generated documents: prioritized questions for an oncologist visit, molecular profile summary, and deep clinical analysis"></p>
<p>Here&rsquo;s what one looks like expanded: a structured question list for an oncology appointment, organized by urgency tier, referencing specific test results and treatment options.</p>
<p><img src="analysis.png" alt="Full analysis document showing tiered questions for Dr. Tan, covering Guardant tissue results, liquid biopsy timing, BRCA1 methylation, and treatment planning"></p>
<p>I use this before every oncology visit. When you have 1776 lab results, 53 imaging reports, and 9 pathology reports, you need something to synthesize them. That&rsquo;s what Claude does well, but it needs structured data to work with. Chartfold provides the structured data. Claude provides the reasoning.</p>
<p>The MCP server exposes 25 tools. Here&rsquo;s the full list:</p>
<table>
  <thead>
      <tr>
          <th>Tool</th>
          <th>What it does</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>run_sql</code></td>
          <td>Execute arbitrary read-only SQL against the database</td>
      </tr>
      <tr>
          <td><code>get_schema</code></td>
          <td>Get CREATE TABLE DDL for query planning</td>
      </tr>
      <tr>
          <td><code>get_database_summary</code></td>
          <td>Table counts and load history (start here)</td>
      </tr>
      <tr>
          <td><code>query_labs</code></td>
          <td>Lab results filtered by test name, date, source, LOINC</td>
      </tr>
      <tr>
          <td><code>get_lab_series_tool</code></td>
          <td>Cross-source time series for a specific test</td>
      </tr>
      <tr>
          <td><code>get_available_tests_tool</code></td>
          <td>All lab tests with frequency and date range</td>
      </tr>
      <tr>
          <td><code>get_abnormal_labs_tool</code></td>
          <td>All flagged-abnormal results</td>
      </tr>
      <tr>
          <td><code>get_medications</code></td>
          <td>Medication list, optionally filtered by status</td>
      </tr>
      <tr>
          <td><code>reconcile_medications_tool</code></td>
          <td>Cross-source medication reconciliation</td>
      </tr>
      <tr>
          <td><code>get_timeline</code></td>
          <td>Unified event timeline (encounters, procedures, imaging, labs, pathology)</td>
      </tr>
      <tr>
          <td><code>search_notes</code></td>
          <td>Full-text search across clinical notes</td>
      </tr>
      <tr>
          <td><code>get_pathology_report</code></td>
          <td>Retrieve a pathology report by ID</td>
      </tr>
      <tr>
          <td><code>get_visit_diff</code></td>
          <td>Everything new since a given date</td>
      </tr>
      <tr>
          <td><code>get_visit_prep</code></td>
          <td>Pre-appointment summary bundle</td>
      </tr>
      <tr>
          <td><code>get_surgical_timeline</code></td>
          <td>Procedures linked to pathology, imaging, and meds</td>
      </tr>
      <tr>
          <td><code>match_cross_source_encounters</code></td>
          <td>Same-day encounters across different EHR systems</td>
      </tr>
      <tr>
          <td><code>get_data_quality_report</code></td>
          <td>Duplicate detection and source coverage matrix</td>
      </tr>
      <tr>
          <td><code>get_source_files</code></td>
          <td>Find PDFs and images linked to clinical records</td>
      </tr>
      <tr>
          <td><code>get_asset_summary</code></td>
          <td>Source asset counts by type and source</td>
      </tr>
      <tr>
          <td><code>save_note</code> / <code>get_note</code> / <code>search_notes_personal</code> / <code>delete_note</code></td>
          <td>Personal notes (CRUD)</td>
      </tr>
      <tr>
          <td><code>save_analysis</code> / <code>get_analysis</code> / <code>search_analyses</code> / <code>list_analyses</code> / <code>delete_analysis</code></td>
          <td>Structured analyses (CRUD)</td>
      </tr>
  </tbody>
</table>
<p>Clinical data is read-only (the SQLite connection opens in <code>?mode=ro</code>, enforced at the engine level). Claude can&rsquo;t modify your clinical records, only read them and save its own notes and analyses.</p>
<p>Most of these tools exist so Claude doesn&rsquo;t have to write SQL for common tasks. But <code>run_sql</code> is the escape hatch: anything the specialized tools don&rsquo;t cover, Claude can query directly.</p>
<hr>
<h2 id="the-html-dashboard">The HTML Dashboard</h2>
<p>The HTML export embeds the entire SQLite database using sql.js (SQLite compiled to WebAssembly). Open the file in a browser and you get an interactive dashboard. Everything runs client-side. No server, no cloud, no account. The file never phones home.</p>
<p><img src="overview.png" alt="Dashboard overview showing record counts: 1776 lab results, 93 medications, 51 conditions, 99 encounters, 53 imaging reports, and more"></p>
<h3 id="lab-charts">Lab Charts</h3>
<p>Lab charts show time-series data across sources with reference ranges. Here, creatinine and albumin are tracked over four years across MEDITECH (blue) and Epic (orange).</p>
<p><img src="lab-charts.png" alt="Cross-source lab charts for Creatinine and Albumin, showing data series from two hospital systems with reference range bands"></p>
<p>For patients who do deal with fragmented records across incompatible systems, this is the view that doesn&rsquo;t exist in any single portal. The charts are configurable via a TOML file, which you can auto-generate from your data:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">chartfold init-config
</span></span></code></pre></div><h3 id="conditions">Conditions</h3>
<p>Conditions with ICD-10 codes, onset dates, and source provenance.</p>
<p><img src="conditions.png" alt="Conditions table showing 51 conditions with status, ICD-10 codes, onset dates, and source system"></p>
<h3 id="medications">Medications</h3>
<p>Medications show &ldquo;Multi-source&rdquo; badges when the same drug appears in multiple systems.</p>
<p><img src="medications.png" alt="Active medications list with multi-source badges"></p>
<h3 id="imaging">Imaging</h3>
<p>Imaging reports with the full narrative findings. Useful for visit prep: search for a specific study, read the impression, bring context.</p>
<p><img src="imaging.png" alt="Imaging section showing CT and MRI reports with narrative findings"></p>
<h3 id="source-documents">Source Documents</h3>
<p>Source PDFs and scanned documents grouped by date.</p>
<p><img src="sources.png" alt="Source documents grouped by date, showing PDF assets from EHR exports"></p>
<h3 id="sql-console">SQL Console</h3>
<p>For anything the UI doesn&rsquo;t cover, there&rsquo;s a SQL console. Every table, every column, every index.</p>
<p><img src="sql-console.png" alt="SQL Console showing the database schema"></p>
<h3 id="dark-mode">Dark Mode</h3>
<p><img src="dark-mode.png" alt="Dark mode overview"></p>
<hr>
<h2 id="architecture">Architecture</h2>
<p>Three-stage pipeline, each stage independently testable:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">Raw EHR files (CDA XML, FHIR JSON, CCDA XML)
</span></span><span class="line"><span class="cl">    |
</span></span><span class="line"><span class="cl">    v
</span></span><span class="line"><span class="cl">[Source Parser]  -- format-specific extraction
</span></span><span class="line"><span class="cl">    |
</span></span><span class="line"><span class="cl">    v
</span></span><span class="line"><span class="cl">[Adapter]        -- normalize to UnifiedRecords (16 dataclass types)
</span></span><span class="line"><span class="cl">    |
</span></span><span class="line"><span class="cl">    v
</span></span><span class="line"><span class="cl">[DB Loader]      -- idempotent upsert into SQLite
</span></span></code></pre></div><p>Source parsers handle the XML/FHIR parsing. Adapters normalize dates to ISO 8601, parse numeric values, deduplicate, and map into a common data model. The DB loader uses upsert with natural keys, so re-running a load is safe.</p>
<p>After loading, the CLI prints a stage comparison table: parser count, adapter count, DB count. If the numbers don&rsquo;t match, you know where data was lost.</p>
<p>Currently supports Epic (CDA), MEDITECH (FHIR + CCDA), and athenahealth (FHIR R4). These importers were written against my own exports. I can&rsquo;t guarantee they&rsquo;ll work for yours. EHR exports vary by site, software version, and configuration. The pipeline is designed as a plugin system for exactly this reason: adding a new source means writing a parser, an adapter, and wiring them into the CLI. The CLAUDE.md has a recipe, and Claude can write a new importer from a sample export in about an hour.</p>
<hr>
<h2 id="export-formats">Export Formats</h2>
<ul>
<li><strong>HTML SPA</strong>: self-contained single file with embedded SQLite, Chart.js, and sql.js. No external dependencies. Copy it to a USB drive.</li>
<li><strong>Markdown</strong>: visit-focused summary with configurable lookback, optional PDF via pandoc.</li>
<li><strong>JSON</strong>: full-fidelity round-trip format. Export, then import to a new database with identical record counts.</li>
<li><strong>Hugo site</strong>: static site with detail pages and cross-linked records.</li>
<li><strong>Arkiv</strong>: universal record format (JSONL + manifest) for long-term archival.</li>
</ul>
<p>The HTML export is a single file. No server, no backend, no account. You can host it on a static site (I host mine on GitHub Pages), email it to a family member, or hand it to a doctor on a USB drive. Because it&rsquo;s just a file, you can protect it with <a href="/post/2026-02-13-pagevault/">PageVault</a> to add password-based encryption before sharing. The recipient opens the file, enters the password, and gets the full interactive dashboard. No server involved at any step.</p>
<p>Medical records should not depend on someone else&rsquo;s infrastructure. A single HTML file with an embedded database and WebAssembly runtime is about as durable as digital data gets.</p>
<hr>
<h2 id="getting-started">Getting Started</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">pip install chartfold
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Load your exports</span>
</span></span><span class="line"><span class="cl">chartfold load auto ~/path/to/export/
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Query</span>
</span></span><span class="line"><span class="cl">chartfold query <span class="s2">&#34;SELECT test_name, value, result_date FROM lab_results LIMIT 10&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Export</span>
</span></span><span class="line"><span class="cl">chartfold <span class="nb">export</span> html --output my-records.html
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Connect Claude</span>
</span></span><span class="line"><span class="cl">chartfold serve-mcp
</span></span></code></pre></div><p>The code is on GitHub: <a href="https://github.com/queelius/chartfold">queelius/chartfold</a>. Python 3.11+, depends on lxml and not much else.</p>
<hr>
<p><em>Chartfold started because I wanted to ask questions about my own medical records and couldn&rsquo;t. Now I can.</em></p>]]></content:encoded><category>chartfold</category><category>python</category><category>ehr</category><category>sqlite</category><category>health-data</category><category>open-source</category><category>mcp</category><category>claude</category></item><item><title>repoindex: Collection Awareness for Your Git Repos</title><link>https://metafunctor.com/post/2025-12-16-repoindex/</link><pubDate>Tue, 16 Dec 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2025-12-16-repoindex/</guid><description>A metadata index that gives LLM tools like Claude Code awareness of your entire repository collection.</description><content:encoded><![CDATA[<p>I have around 100 git repos. When I&rsquo;m working with Claude Code on one of them, the AI has deep knowledge of <em>that</em> repo but zero awareness of the rest. Questions like &ldquo;which of my repos already has a fuzzy search implementation?&rdquo; or &ldquo;what other projects use this pattern?&rdquo; require me to go dig around manually.</p>
<p><a href="/projects/repoindex/">repoindex</a> fixes that.</p>
<h2 id="the-idea">The Idea</h2>
<p>Separation of concerns:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">Claude Code (deep work on ONE repo)
</span></span><span class="line"><span class="cl">         |
</span></span><span class="line"><span class="cl">         |  &#34;What else do I have?&#34;
</span></span><span class="line"><span class="cl">         |  &#34;Which repos need X?&#34;
</span></span><span class="line"><span class="cl">         v
</span></span><span class="line"><span class="cl">    repoindex (collection awareness)
</span></span><span class="line"><span class="cl">         |
</span></span><span class="line"><span class="cl">         +-- repo://...     -&gt; what exists
</span></span><span class="line"><span class="cl">         +-- tags://...     -&gt; organization
</span></span><span class="line"><span class="cl">         +-- stats://...    -&gt; aggregations
</span></span><span class="line"><span class="cl">         +-- events://...   -&gt; what happened
</span></span></code></pre></div><p>Claude Code works <em>inside</em> repositories. repoindex knows <em>about</em> repositories: metadata, tags, status, relationships. Together they give you full portfolio awareness.</p>
<h2 id="mcp-server-integration">MCP Server Integration</h2>
<p>The most useful part is the MCP (Model Context Protocol) server. Add it to your Claude Code configuration and the AI can query your collection directly:</p>
<ul>
<li>&ldquo;Which of my Python repos don&rsquo;t have a LICENSE file?&rdquo;</li>
<li>&ldquo;What repos have I updated in the last week?&rdquo;</li>
<li>&ldquo;Show me all projects tagged with <code>ml</code>&rdquo;</li>
</ul>
<p>The server exposes resources like <code>repo://</code>, <code>tags://</code>, <code>stats://</code>, and <code>events://</code> that Claude Code reads to understand your portfolio.</p>
<h2 id="core-features">Core Features</h2>
<p><strong>Tag-Based Organization.</strong> Hierarchical tags for categorizing repos. Tags can be explicit (<code>repoindex tag add myproject topic:ml</code>) or implicit (derived automatically from language, directory, features).</p>
<p><strong>Query Language.</strong> Filter repos with expressions:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">repoindex query <span class="s2">&#34;language == &#39;Python&#39; and &#39;ml&#39; in tags&#34;</span>
</span></span><span class="line"><span class="cl">repoindex query <span class="s2">&#34;stars &gt; 10 and has:docs&#34;</span>
</span></span></code></pre></div><p><strong>Event Tracking.</strong> What happened across your collection:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">repoindex events --since 7d --pretty
</span></span></code></pre></div><p>New releases, tags, PyPI publishes, all in one view.</p>
<p><strong>JSONL Output.</strong> Every command outputs newline-delimited JSON by default, so it plays well with Unix pipelines:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">repoindex status <span class="p">|</span> jq <span class="s1">&#39;select(.status.clean == false)&#39;</span>
</span></span></code></pre></div><h2 id="installation">Installation</h2>
<p>Available on <a href="https://pypi.org/project/repoindex/">PyPI</a>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">pip install repoindex
</span></span></code></pre></div><p>Configure your repository directories and start indexing:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">repoindex config generate
</span></span><span class="line"><span class="cl">repoindex list --pretty
</span></span></code></pre></div><h2 id="why-the-rename">Why the Rename?</h2>
<p>This was previously called <code>ghops</code>. The new name is more honest about what it does: it indexes repositories. The old name implied GitHub-specific operations, but the tool works with any git repo.</p>
<p>See the <a href="/projects/repoindex/">project page</a> for full documentation.</p>
]]></content:encoded><category>developer-tools</category><category>python</category><category>mcp</category><category>claude-code</category><category>git</category><category>devops</category></item><item><title>EBK: Ebook Toolkit</title><link>https://metafunctor.com/post/2025-10-ebk/</link><pubDate>Mon, 13 Oct 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2025-10-ebk/</guid><description>EBK is a comprehensive eBook metadata management tool with AI-powered enrichment, semantic search, and knowledge graphs. Part of the Long Echo toolkit.</description><content:encoded><![CDATA[<p>Your books represent decades of accumulated knowledge. Technical references, formative texts, research that shaped your thinking. They deserve better than scattered files on a hard drive with inconsistent metadata and no way to search across them.</p>
<p><a href="https://github.com/queelius/ebk">EBK</a> treats your ebook library as a queryable, searchable knowledge base. It&rsquo;s part of the <a href="/post/2025-01-long-echo/">Long Echo</a> toolkit: tools for preserving your digital intellectual life in formats you control.</p>
<h2 id="the-core-abstraction">The Core Abstraction</h2>
<p>At its heart, EBK is a SQLAlchemy + SQLite database with a normalized schema. Everything else (CLI, AI features, exports) is layered on top. This means your library metadata is always queryable with standard tools, even if EBK itself disappears.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Works even without EBK installed</span>
</span></span><span class="line"><span class="cl">sqlite3 library.db <span class="s2">&#34;SELECT title, author FROM books WHERE favorite = 1&#34;</span>
</span></span></code></pre></div><h2 id="what-it-does">What It Does</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Initialize and import</span>
</span></span><span class="line"><span class="cl">ebk db-init ~/my-library
</span></span><span class="line"><span class="cl">ebk db-import ~/Documents/book.pdf ~/my-library
</span></span><span class="line"><span class="cl">ebk db-import-calibre ~/Calibre/Library ~/my-library
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Search with FTS5 full-text search</span>
</span></span><span class="line"><span class="cl">ebk db-search <span class="s2">&#34;quantum computing&#34;</span> ~/my-library
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Field-specific queries</span>
</span></span><span class="line"><span class="cl">ebk db-search <span class="s2">&#34;title:Python author:Knuth tag:programming&#34;</span> ~/my-library
</span></span></code></pre></div><p>Behind a simple import, EBK automatically extracts text from PDFs (PyMuPDF with pypdf fallback) and EPUBs, generates text chunks for semantic search, computes SHA256 hashes for deduplication, extracts covers, and indexes everything in FTS5.</p>
<h3 id="deduplication">Deduplication</h3>
<p>Same file (same hash) gets skipped. Same book in a different format gets added as an additional format. Different book gets imported as new. Books are stored in hash-prefixed directories for scalability.</p>
<h3 id="ai-enrichment">AI Enrichment</h3>
<p>EBK can use LLMs to auto-generate tags, categories, and descriptions for books with sparse metadata:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ebk enrich <span class="m">42</span>  <span class="c1"># Enhance metadata with LLM</span>
</span></span></code></pre></div><p>Semantic search finds books by meaning, not just keywords:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">results</span> <span class="o">=</span> <span class="n">lib</span><span class="o">.</span><span class="n">semantic_search</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;explaining complex mathematical concepts simply&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">threshold</span><span class="o">=</span><span class="mf">0.7</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span></code></pre></div><p>Uses vector embeddings when available, TF-IDF fallback for offline use.</p>
<h3 id="knowledge-graphs">Knowledge Graphs</h3>
<p>Using NetworkX, EBK can extract concept relationships across your library:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">graph</span> <span class="o">=</span> <span class="n">lib</span><span class="o">.</span><span class="n">build_knowledge_graph</span><span class="p">(</span><span class="n">extract_entities</span><span class="o">=</span><span class="kc">True</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">graph</span><span class="o">.</span><span class="n">visualize</span><span class="p">(</span><span class="n">output</span><span class="o">=</span><span class="s2">&#34;library_knowledge.html&#34;</span><span class="p">)</span>
</span></span></code></pre></div><p>This reveals connections you didn&rsquo;t know existed. &ldquo;These books about functional programming also discuss category theory.&rdquo;</p>
<h3 id="fluent-python-api">Fluent Python API</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">ebk</span> <span class="kn">import</span> <span class="n">Library</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">lib</span> <span class="o">=</span> <span class="n">Library</span><span class="o">.</span><span class="n">open</span><span class="p">(</span><span class="s2">&#34;~/ebooks&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">results</span> <span class="o">=</span> <span class="p">(</span><span class="n">lib</span><span class="o">.</span><span class="n">query</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">    <span class="o">.</span><span class="n">where</span><span class="p">(</span><span class="s2">&#34;language&#34;</span><span class="p">,</span> <span class="s2">&#34;en&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="o">.</span><span class="n">where</span><span class="p">(</span><span class="s2">&#34;date&#34;</span><span class="p">,</span> <span class="s2">&#34;2020&#34;</span><span class="p">,</span> <span class="s2">&#34;&gt;=&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="o">.</span><span class="n">where</span><span class="p">(</span><span class="s2">&#34;subjects&#34;</span><span class="p">,</span> <span class="s2">&#34;Python&#34;</span><span class="p">,</span> <span class="s2">&#34;contains&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="o">.</span><span class="n">order_by</span><span class="p">(</span><span class="s2">&#34;title&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="o">.</span><span class="n">take</span><span class="p">(</span><span class="mi">10</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="o">.</span><span class="n">execute</span><span class="p">())</span>
</span></span></code></pre></div><h3 id="export">Export</h3>
<p>Multiple formats for different needs:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ebk <span class="nb">export</span> hugo ~/library ~/hugo-site --organize-by subject --include-covers
</span></span><span class="line"><span class="cl">ebk export-dag ~/library ~/output  <span class="c1"># Navigable symlink directory structure</span>
</span></span></code></pre></div><p>The Hugo export creates a browsable website. The DAG export creates a tag-based directory structure where books appear via symlinks under multiple categories. Both work without EBK installed.</p>
<h3 id="set-theoretic-operations">Set-Theoretic Operations</h3>
<p>Merge libraries with mathematical precision:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ebk merge union ~/merged ~/lib1 ~/lib2    <span class="c1"># All unique books</span>
</span></span><span class="line"><span class="cl">ebk merge intersect ~/common ~/lib1 ~/lib2 <span class="c1"># Books in ALL libraries</span>
</span></span><span class="line"><span class="cl">ebk merge diff ~/unique ~/lib1 ~/lib2      <span class="c1"># Books in lib1 NOT in lib2</span>
</span></span></code></pre></div><h3 id="mcp-server">MCP Server</h3>
<p>EBK includes an MCP server that lets AI assistants query your library directly during conversations. &ldquo;What books do I have about transformer architectures?&rdquo; gets an answer from your actual collection.</p>
<h2 id="architecture">Architecture</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">Integrations (Streamlit, MCP, Viz)
</span></span><span class="line"><span class="cl">CLI (Typer commands)
</span></span><span class="line"><span class="cl">Core Library (Fluent API)
</span></span><span class="line"><span class="cl">Import/Export (Format handlers)
</span></span><span class="line"><span class="cl">Database (SQLAlchemy + SQLite)
</span></span></code></pre></div><p>Each layer is independently testable. Extensions are optional (<code>pip install ebk[streamlit]</code>, <code>pip install ebk[mcp]</code>).</p>
<h2 id="graceful-degradation">Graceful Degradation</h2>
<ol>
<li><strong>Full EBK</strong>: Rich CLI, semantic search, AI enrichment, knowledge graphs</li>
<li><strong>SQLite queries</strong>: Direct database access with standard tools</li>
<li><strong>HTML export</strong>: Browse catalog in any browser</li>
<li><strong>File system</strong>: The books themselves, always readable</li>
</ol>
<p>The database stores metadata and search indexes. Your actual files stay wherever you put them.</p>
<h2 id="resources">Resources</h2>
<ul>
<li><strong>Repository</strong>: <a href="https://github.com/queelius/ebk">github.com/queelius/ebk</a></li>
<li><strong>Long Echo Philosophy</strong>: <a href="/post/2025-01-long-echo/">Designing for Digital Resilience</a></li>
</ul>
<hr>
<p><em>Your books represent significant intellectual investment. They deserve better than a pile of files in a directory.</em></p>
]]></content:encoded><category>Python</category><category>ebook-manager</category><category>SQLite</category><category>SQLAlchemy</category><category>full-text-search</category><category>knowledge-graphs</category><category>semantic-search</category><category>MCP</category><category>AI</category><category>cli</category><category>data-management</category></item><item><title>CTK: Conversation Toolkit</title><link>https://metafunctor.com/post/2025-10-ctk/</link><pubDate>Thu, 09 Oct 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2025-10-ctk/</guid><description>A plugin-based toolkit for managing AI conversations from multiple providers. Import, store, search, and export conversations in a unified tree format. Built for the Long Echo project.</description><content:encoded><![CDATA[<p><a href="https://github.com/queelius/ctk">CTK</a> manages AI conversations across platforms. Import from ChatGPT, Claude, Copilot, Gemini. Store locally in SQLite. Search, tag, export. Keep everything.</p>
<h2 id="the-problem">The Problem</h2>
<p>If you use multiple AI assistants, your conversations are scattered across incompatible platforms, unsearchable, and dependent on companies that may not exist in 20 years. ChatGPT lives in OpenAI&rsquo;s web app. Claude is siloed in Anthropic&rsquo;s interface. Copilot chat history is buried in VS Code storage.</p>
<p>You can&rsquo;t search across them. You can&rsquo;t back them up in a unified format. You can&rsquo;t own them.</p>
<h2 id="the-key-insight-conversations-are-trees">The Key Insight: Conversations Are Trees</h2>
<p>Most tools treat conversations as linear sequences. They&rsquo;re not. ChatGPT&rsquo;s &ldquo;regenerate&rdquo; feature creates branches. Claude supports conversation forking. Even a simple &ldquo;let me try that again&rdquo; is a tree operation.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">User: &#34;Write a poem&#34;
</span></span><span class="line"><span class="cl">  ├── Assistant (v1): &#34;Roses are red...&#34;
</span></span><span class="line"><span class="cl">  └── Assistant (v2): &#34;In fields of gold...&#34;  [regenerated]
</span></span><span class="line"><span class="cl">      └── User: &#34;Make it longer&#34;
</span></span><span class="line"><span class="cl">          └── Assistant: &#34;In fields of gold, where sunshine...&#34;
</span></span></code></pre></div><p>CTK stores all conversations as trees. Linear chats are single-path trees. Branching conversations preserve every path. This means you never lose a regeneration, and you can export any path you want.</p>
<h2 id="what-it-does">What It Does</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Import from any platform</span>
</span></span><span class="line"><span class="cl">ctk import chatgpt_export.json --db my_chats.db
</span></span><span class="line"><span class="cl">ctk import claude_export.json --db my_chats.db --format anthropic
</span></span><span class="line"><span class="cl">ctk import ~/.vscode/workspaceStorage --db my_chats.db --format copilot
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Search across everything</span>
</span></span><span class="line"><span class="cl">ctk search <span class="s2">&#34;python async&#34;</span> --db my_chats.db
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Natural language queries via LLM tool calling</span>
</span></span><span class="line"><span class="cl">ctk say <span class="s2">&#34;find conversations about distributed systems&#34;</span> --db my_chats.db
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Interactive TUI for browsing and chatting</span>
</span></span><span class="line"><span class="cl">ctk chat --db my_chats.db
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Export for fine-tuning, archival, or publishing</span>
</span></span><span class="line"><span class="cl">ctk <span class="nb">export</span> training.jsonl --db my_chats.db --format jsonl
</span></span><span class="line"><span class="cl">ctk <span class="nb">export</span> archive.html --db my_chats.db --format html5
</span></span><span class="line"><span class="cl">ctk <span class="nb">export</span> archive/ --db my_chats.db --format markdown
</span></span></code></pre></div><h3 id="plugin-architecture">Plugin Architecture</h3>
<p>Adding a new provider is one file. Implement <code>ImporterPlugin</code>, drop it in the integrations folder, done. Auto-discovered at runtime. No registry, no config.</p>
<p>Currently supported: OpenAI/ChatGPT (full tree), Anthropic/Claude (full tree), GitHub Copilot, Google Gemini, generic JSONL, coding agents (Cursor, Windsurf).</p>
<h3 id="privacy">Privacy</h3>
<p>100% local. No telemetry. Optional sanitization strips API keys, passwords, and personal identifiers before export.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ctk <span class="nb">export</span> clean_export.jsonl --db chats.db --format jsonl --sanitize
</span></span></code></pre></div><h3 id="html5-export">HTML5 Export</h3>
<p>The HTML5 exporter produces a self-contained file with embedded search, tree visualization, and dark mode. No server, no internet, no dependencies. The file works offline in any browser, including continuing conversations with a local LLM directly in the exported HTML.</p>
<p>This is the Long Echo principle made concrete: if CTK disappears tomorrow, the HTML file still works.</p>
<h3 id="organization">Organization</h3>
<p>Star, pin, archive conversations. Auto-tag with LLM analysis. Create filtered databases for specific purposes.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ctk star abc123 --db chats.db
</span></span><span class="line"><span class="cl">ctk filter --db all_chats.db --output work.db --tags <span class="s2">&#34;work&#34;</span>
</span></span><span class="line"><span class="cl">ctk merge source1.db source2.db --output merged.db
</span></span></code></pre></div><h3 id="mcp-integration">MCP Integration</h3>
<p>CTK supports Model Context Protocol for tool calling during live chat. Connect file systems, databases, or custom functions. The LLM can use these tools while you&rsquo;re having a conversation.</p>
<h2 id="the-long-echo-connection">The Long Echo Connection</h2>
<p>CTK was the first tool I built for Long Echo, and for a while I thought it <em>was</em> Long Echo. The hard problems of conversation parsing, unified representation, search, and storage were all solved here.</p>
<p>What Long Echo added was the philosophical framework: graceful degradation, multi-format export, the USB-drive-in-2074 thought experiment. CTK implements that framework for conversations.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">Level 1: Full CTK       Semantic search, TUI, RAG
</span></span><span class="line"><span class="cl">Level 2: SQLite         Direct queries (CTK gone, database remains)
</span></span><span class="line"><span class="cl">Level 3: JSONL          grep through files
</span></span><span class="line"><span class="cl">Level 4: HTML           Open in any browser
</span></span><span class="line"><span class="cl">Level 5: Markdown       Read in any text editor
</span></span></code></pre></div><p>Each level works even when all higher levels fail.</p>
<h2 id="resources">Resources</h2>
<ul>
<li><strong>Repository</strong>: <a href="https://github.com/queelius/ctk">github.com/queelius/ctk</a></li>
<li><strong>Long Echo Philosophy</strong>: <a href="/post/2025-01-long-echo/">Designing for Digital Resilience</a></li>
</ul>
<hr>
<p><em>Your conversations with AI are valuable knowledge. They deserve better than &ldquo;hope the company doesn&rsquo;t shut down.&rdquo;</em></p>
]]></content:encoded><category>Python</category><category>AI</category><category>LLM</category><category>conversation-management</category><category>ChatGPT</category><category>Claude</category><category>Copilot</category><category>TUI</category><category>SQLite</category><category>plugin-architecture</category><category>privacy</category><category>MCP</category></item></channel></rss>