<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:media="http://search.yahoo.com/mrss/"><channel><title>Fisher Information on metafunctor</title><link>https://metafunctor.com/tags/fisher-information/</link><description>Research Engineer &amp; Computer Scientist - Machine Learning, Statistical Computing, Open Source Development</description><language>en-us</language><managingEditor>lex@metafunctor.com (Alex Towell)</managingEditor><webMaster>lex@metafunctor.com (Alex Towell)</webMaster><lastBuildDate>Fri, 05 Jun 2026 00:59:30 -0500</lastBuildDate><atom:link href="https://metafunctor.com/tags/fisher-information/" rel="self" type="application/rss+xml"/><image><url>https://metafunctor.com/images/og-image.jpg</url><title>metafunctor</title><link>https://metafunctor.com/</link></image><item><title>Closed-Form Results for Masked Exponential Series Systems</title><link>https://metafunctor.com/post/2024-04-15-expo-masked-fim/</link><pubDate>Tue, 02 Dec 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-04-15-expo-masked-fim/</guid><description>Closed-form MLEs and Fisher information for exponential series systems with masked failure data. No numerical optimization required.</description><content:encoded><![CDATA[<p>In a series system, the system fails when any component fails. You observe the system failure time \(t\) and a candidate set \(C \subseteq \lbrace 1,2,\ldots,m\rbrace\) of components that might have caused the failure. But you do not know which component in \(C\) actually failed. This is masked failure data.</p>
<p>The standard approach is numerical optimization of the likelihood. This paper shows that for exponential component lifetimes, everything has a closed form.</p>
<h2 id="closed-form-fisher-information">Closed-Form Fisher Information</h2>
<p>For exponential masked data with arbitrary masking patterns:</p>
$$I_{ij}(\boldsymbol{\lambda}) = n \cdot \sum_{A \ni i,j} \frac{\hat{\omega}_A}{(\sum_{k \in A} \lambda_k)^2}$$<p>where \(\hat{\omega}_A\) is the observed frequency of candidate set \(A\). You can compute asymptotic variances directly, check identifiability before running any estimation, and analyze optimization stability. All without fitting a model first.</p>
<h2 id="sufficient-statistics">Sufficient Statistics</h2>
<p>The mean system lifetime and the candidate set frequency vector are sufficient statistics. That reduces an entire dataset to \(1 + \binom{m}{w}\) numbers, where \(w\) is the masking width.</p>
<p>This is a real simplification. All the statistical information in your data is captured by two things: how often each candidate set appears, and what the average failure time is. Nothing else matters for inference.</p>
<h2 id="closed-form-mle-for-three-components">Closed-Form MLE for Three Components</h2>
<p>For \(m=3\) components with pairwise masking (\(w=2\)), the MLE has an explicit closed-form solution:</p>
$$\hat{\lambda}_j = \frac{\sum_{A \ni j} \hat{\omega}_A}{\bar{t} \cdot n}$$<p>No numerical optimization. No iterative algorithms. Just plug in your sufficient statistics.</p>
<p>The \(w=2\) case is the interesting one. \(w=1\) means no masking (you know exactly which component failed). \(w=m\) means complete masking (the candidate set is always everything, so you have no diagnostic information). \(w=2\) is the simplest case where masking actually matters, and it is the one where closed-form solutions exist.</p>
<h2 id="asymptotic-theory">Asymptotic Theory</h2>
<p>The MLE follows:</p>
$$\sqrt{n}(\hat{\boldsymbol{\lambda}}_n - \boldsymbol{\lambda}^\star) \xrightarrow{d} \mathcal{N}(\mathbf{0}, \mathcal{I}^{-1}(\boldsymbol{\lambda}^\star))$$<p>with explicit Wald-type confidence intervals using the closed-form Fisher information. So you get point estimates and uncertainty quantification, all analytically.</p>
<h2 id="why-exponential">Why Exponential?</h2>
<p>The exponential assumption is not just for tractability, though it helps. Constant hazard rate models systems subject to random external shocks. The memoryless property simplifies the likelihood structure. And exponential is the foundation for generalization to Weibull and other distributions.</p>
<p>More practically, exponential is the one case where closed-form solutions exist. The insights from the exponential case guide numerical methods for more complex lifetime distributions.</p>
<h2 id="connections">Connections</h2>
<p>This paper is the analytical foundation for the generalized Weibull treatment in my <a href="/publications/math-proj/">master&rsquo;s thesis</a>, which handles shape and scale parameters, more realistic masking models, and model selection between candidate failure mechanisms.</p>
<p>The R packages <a href="/projects/likelihood.model/">likelihood.model</a> and <a href="/projects/maskedcauses/">maskedcauses</a> implement these methods.</p>
<h2 id="links">Links</h2>
<ul>
<li><strong>Paper</strong>: <a href="/papers/expo-masked-fim/">Statistical Inference for Series Systems from Masked Failure Time Data: The Exponential Case</a></li>
<li><strong>Repository</strong>: <a href="https://github.com/queelius/expo-masked-fim">github.com/queelius/expo-masked-fim</a></li>
<li><strong>Key result</strong>: Complete analytical treatment including MLE, Fisher information, sufficient statistics, and asymptotic theory</li>
</ul>
]]></content:encoded><category>statistics</category><category>fisher information</category><category>masked data</category><category>series systems</category><category>reliability</category><category>exponential distribution</category><category>maximum likelihood</category><category>sufficient statistics</category></item><item><title>Your Optimizer Is (Approximately) Propagating Fisher Information</title><link>https://metafunctor.com/post/2023-09-12-your-optimizer-is-approximately-doing-fisher-flow/</link><pubDate>Tue, 12 Sep 2023 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2023-09-12-your-optimizer-is-approximately-doing-fisher-flow/</guid><description>Adam, K-FAC, EWC, and natural gradient are all approximating the same thing at different fidelity levels. The math and the caveats.</description><content:encoded><![CDATA[<p>Adam&rsquo;s second moment estimate is a diagonal approximation to the empirical Fisher Information Matrix. K-FAC uses a Kronecker-factored approximation. EWC uses the Fisher as a regularization penalty. Natural gradient uses the full thing. Same underlying structure, different computational budgets.</p>
<p>None of this is new. Amari formalized natural gradient in 1998. Martens (2020) wrote a thorough 76-page survey connecting it to second-order optimization. Khan and Rue (2023) unified these algorithms under a single Bayesian learning rule. Kunstner, Hennig, and Balles (2019) showed where the empirical Fisher approximation breaks. I spent a while working through the math, and the pattern is clean once you see it, so I want to write it down.</p>
<h2 id="fisher-information">Fisher Information</h2>
<p>Parametric model $p(x|\theta)$. Here $x$ is observed data and $\theta \in \mathbb{R}^d$ is the parameter vector. The <strong>score function</strong> is the gradient of the log-likelihood with respect to the parameters:</p>
$$s(\theta; x) = \nabla_\theta \log p(x|\theta)$$<p>Under regularity conditions, the score has zero mean: $\mathbb{E}_{x \sim p(x|\theta)}[s(\theta; x)] = 0$. The <strong>Fisher Information Matrix</strong> $\mathcal{I}(\theta) \in \mathbb{R}^{d \times d}$ is its covariance:</p>
$$\mathcal{I}(\theta) = \text{Cov}[s(\theta; x)] = \mathbb{E}_{x \sim p(x|\theta)}\!\left[s(\theta; x)\, s(\theta; x)^\top\right]$$<p>Under the same regularity conditions (differentiability, integrability, interchange of differentiation and integration), this equals $-\mathbb{E}[\nabla^2_\theta \log p(x|\theta)]$, the expected negative Hessian. This is Bartlett&rsquo;s second identity.</p>
<p>What does the Fisher measure? Not how sharply the likelihood peaks around the data. It measures how well the data <em>distinguishes</em> nearby parameter values. If $\mathcal{I}(\theta)$ is large in some direction, then a small change in $\theta$ along that direction produces a large change in the distribution $p(x|\theta)$, which means the data is informative about that component. If $\mathcal{I}(\theta)$ is small, nearby parameter values produce nearly indistinguishable distributions, and the data can&rsquo;t tell them apart. Formally, the Fisher is the Hessian of the KL divergence $D_{\text{KL}}(p_\theta | p_{\theta + \delta\theta})$ at $\delta\theta = 0$.</p>
<p>Fisher information from independent observations adds:</p>
$$\mathcal{I}_{1:n}(\theta) = \sum_{i=1}^n \mathcal{I}_{x_i}(\theta)$$<p>where $\mathcal{I}<em>{x_i}(\theta) = s(\theta; x_i) s(\theta; x_i)^\top$ is the per-observation Fisher contribution. (For i.i.d. observations, $\mathcal{I}</em>{1:n}(\theta) = n \cdot \mathcal{I}(\theta)$, but the additivity holds for non-identical observations too, which is what makes sequential and distributed estimation clean.)</p>
<p>This gives you a natural update rule. Maintain a running estimate $\hat\theta_t \in \mathbb{R}^d$ and accumulated Fisher information $\mathcal{I}<em>t \in \mathbb{R}^{d \times d}$. When new data arrives with score $s_t$ and Fisher contribution $\mathcal{I}</em>{\text{new}}$:</p>
$$\mathcal{I}_t = \mathcal{I}_{t-1} + \mathcal{I}_{\text{new}}$$<p>
</p>
$$\hat\theta_t = \hat\theta_{t-1} + \eta\, \mathcal{I}_t^{-1} s_t$$<p>where $\eta &gt; 0$ is a learning rate. Since the score $s_t$ points in the direction of increasing log-likelihood, this is gradient <em>ascent</em> on the likelihood, preconditioned by the inverse Fisher. The preconditioner $\mathcal{I}_t^{-1}$ makes this a <em>natural</em> gradient step: it rescales the update to respect the Fisher-Rao metric on the statistical manifold, so that equal-sized steps correspond to equal changes in the distribution, not equal changes in the parameter vector. If you&rsquo;re minimizing a loss $L = -\log p$, the gradient is $-s_t$ and the update has the usual descent sign.</p>
<h2 id="the-approximation-hierarchy">The Approximation Hierarchy</h2>
<p>Every common optimizer can be viewed as using some approximation to the Fisher as its preconditioner. The framing is most natural for natural gradient, less natural for SGD (where it&rsquo;s more of an interpretation than a design choice):</p>
<table>
	<thead>
			<tr>
					<th>Method</th>
					<th>Preconditioner structure</th>
					<th>Per-step cost</th>
					<th>What&rsquo;s discarded</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>SGD</td>
					<td>$\eta I_d$ (learning rate $\times$ identity)</td>
					<td>$O(d)$</td>
					<td>All curvature information</td>
			</tr>
			<tr>
					<td>Adam</td>
					<td>$\text{diag}(\hat{v}_t)^{-1/2}$, $\hat{v}_t \in \mathbb{R}^d$</td>
					<td>$O(d)$</td>
					<td>Off-diagonal structure, uses square root</td>
			</tr>
			<tr>
					<td>K-FAC</td>
					<td>$(A \otimes B)^{-1}$, $A \in \mathbb{R}^{n \times n}$, $B \in \mathbb{R}^{m \times m}$</td>
					<td>$O(m^3 + n^3)$*</td>
					<td>Cross-layer coupling</td>
			</tr>
			<tr>
					<td>Natural gradient</td>
					<td>$\mathcal{I}(\theta)^{-1} \in \mathbb{R}^{d \times d}$</td>
					<td>$O(d^3)$</td>
					<td>Nothing</td>
			</tr>
	</tbody>
</table>
<p>$d$ is the total parameter count. $I_d$ is the $d \times d$ identity. $\otimes$ is the Kronecker product. For K-FAC, $A = \mathbb{E}[a_{l-1} a_{l-1}^\top]$ is the input activation covariance and $B = \mathbb{E}[\delta_l \delta_l^\top]$ is the output gradient covariance for a layer with weight matrix $W_l \in \mathbb{R}^{m \times n}$, input activations $a_{l-1} \in \mathbb{R}^n$, and backpropagated gradients $\delta_l \in \mathbb{R}^m$ (Martens and Grosse, 2015).</p>
<p>*K-FAC&rsquo;s $O(m^3 + n^3)$ cost is for the periodic inversion of the Kronecker factors. K-FAC does not invert every step; it amortizes the cost by inverting every $T_{\text{inv}}$ steps and using stale inverses in between. The per-step factor update (accumulating $a a^\top$ and $\delta \delta^\top$) costs $O(n^2 + m^2)$.</p>
<p>Calling SGD a &ldquo;scalar Fisher&rdquo; approximation is the Fisher perspective&rsquo;s interpretation, not SGD&rsquo;s self-understanding. SGD uses a fixed learning rate because it works, not because someone decided to approximate the Fisher with a scalar. But the interpretation is structurally correct: SGD treats all parameter directions as equally curved, which is what happens when you precondition with a multiple of the identity.</p>
<h2 id="adam-and-the-diagonal-fisher">Adam and the Diagonal Fisher</h2>
<p>Adam&rsquo;s second moment update is $\hat{v}<em>t = \beta_2 \hat{v}</em>{t-1} + (1-\beta_2) g_t^2$. Here $g_t \in \mathbb{R}^d$ is the mini-batch gradient at step $t$, $\beta_2 \in [0,1)$ is the exponential decay rate, and the square is element-wise. This is an exponentially-weighted moving average of squared gradients, which tracks the diagonal of what you might call the &ldquo;empirical Fisher.&rdquo;</p>
<p>I say &ldquo;might call&rdquo; because there are two things people mean by &ldquo;empirical Fisher,&rdquo; and they&rsquo;re not the same:</p>
<ul>
<li><strong>Per-sample empirical Fisher</strong>: $\hat{\mathcal{I}} = \frac{1}{n} \sum_{i=1}^n s(\theta; x_i) s(\theta; x_i)^\top$, where each $s(\theta; x_i)$ is the per-sample gradient. The diagonal of this is $\frac{1}{n} \sum_i [s(\theta; x_i)]^2$, the mean of per-sample squared gradients.</li>
<li><strong>Mini-batch gradient outer product</strong>: $g_t g_t^\top$, where $g_t = \frac{1}{|B|}\sum_{i \in B} s(\theta; x_i)$ is the batch-averaged gradient. The diagonal is $g_t^2$.</li>
</ul>
<p>Adam tracks the second one, not the first. These differ because $(\frac{1}{n}\sum_i s_i)^2 \neq \frac{1}{n}\sum_i s_i^2$ in general. The distinction is discussed by Kunstner et al. (2019), who show that neither version generally approximates the true Fisher well away from critical points.</p>
<p>The connection between Adam and natural gradient is real but requires four simplifications to make exact:</p>
<ol>
<li><strong>Drop bias correction.</strong> Remove the $1/(1 - \beta_2^t)$ warmup term.</li>
<li><strong>Drop epsilon.</strong> Set the numerical stability constant to zero.</li>
<li><strong>Drop the square root.</strong> This is the big one. Adam divides by $\sqrt{\hat{v}_t} + \epsilon$. The natural gradient preconditioner divides by $\hat{v}_t$. Adam is using a square-root preconditioner: $\text{diag}(\hat{v}_t)^{-1/2}$ rather than $\text{diag}(\hat{v}_t)^{-1}$. These are different objects with different scaling properties.</li>
<li><strong>Accept the empirical Fisher.</strong> The true Fisher is $\mathbb{E}<em>{x \sim p</em>\theta}[s(\theta; x) s(\theta; x)^\top]$, an expectation under the <em>model&rsquo;s</em> distribution. The empirical Fisher uses the <em>training data</em> distribution. They agree when the gradient is zero (i.e., at critical points of the expected log-likelihood, where the model&rsquo;s distribution matches the data distribution in the relevant sense). In practice you are almost never at such a point.</li>
</ol>
<p>With all four simplifications, simplified Adam and diagonal natural gradient with exponential forgetting $\beta_2$ produce identical parameter trajectories. Without them, they don&rsquo;t. The square root is the most consequential difference: it changes the effective preconditioning from $1/v_i$ to $1/\sqrt{v_i}$, compressing the dynamic range of the per-parameter learning rates.</p>
<p>This tells you what Adam is actually doing: per-parameter learning rate adaptation based on recent gradient magnitudes. It is not principled curvature estimation in most of parameter space. The Fisher interpretation holds approximately near convergence, where the empirical and true Fisher converge and the square root is a monotonic distortion that preserves the ranking of directions. Away from convergence, the interpretation is loose.</p>
<h2 id="ewc-and-the-fisher-as-memory">EWC and the Fisher as Memory</h2>
<p>Elastic Weight Consolidation (Kirkpatrick et al., 2017) is the simplest case because there is no approximation gap to analyze. EWC defines its loss as:</p>
$$\mathcal{L}\_{\text{EWC}}(\theta) = \mathcal{L}\_{\text{new}}(\theta) + \frac{\lambda}{2} \sum\_{i=1}^{d} F_i (\theta_i - \theta_i^\*)^2$$<p>$\mathcal{L}_{\text{new}}(\theta)$ is the loss on the current task. $\lambda &gt; 0$ controls regularization strength. $F_i$ is the $i$-th diagonal entry of the (empirical) Fisher Information Matrix computed at the end of training on the previous task. $\theta^* \in \mathbb{R}^d$ are the parameters saved after that training.</p>
<p>The Fisher acts as a memory of which parameters mattered. High $F_i$ means the likelihood was sensitive to parameter $\theta_i$ during the old task (equivalently, the old task&rsquo;s data was informative about $\theta_i$). The quadratic penalty is large, so $\theta_i$ resists moving. Low $F_i$ means the old task didn&rsquo;t depend on that parameter, so it&rsquo;s free to change for the new task.</p>
<p>This <em>is</em> Fisher regularization. Kirkpatrick et al. designed it this way, citing the Fisher explicitly. There is no hidden approximation in the connection. (There is an approximation in the <em>method</em>: EWC uses a diagonal Fisher and a quadratic penalty around $\theta^<em>$, which is a Laplace approximation to the log-posterior. Whether that&rsquo;s a good approximation depends on the geometry of the loss surface near $\theta^</em>$.)</p>
<h2 id="two-regimes">Two Regimes</h2>
<p>All of the above assumes something that usually isn&rsquo;t true in practice: convergence.</p>
<p><strong>Classical regime.</strong> Train to convergence on a well-specified model. At the MLE $\hat\theta_n$, the observed Fisher (negative Hessian of the log-likelihood) converges to the expected Fisher. Asymptotic theory gives you consistency, normality, and Cramer-Rao efficiency: the MLE achieves the lowest possible variance among unbiased estimators, and that variance is $\mathcal{I}(\theta_0)^{-1}/n$ where $\theta_0$ is the true parameter. Uncertainty quantification via the inverse Fisher is principled.</p>
<p><strong>Modern ML regime.</strong> Stop before convergence, which is what actually happens. Computational budgets, early stopping for generalization, training instability. The Fisher information accumulated along the optimization trajectory is not the Fisher at any stationary point. It reflects the geometry of the path through parameter space, not curvature at the destination.</p>
<p>The distinction matters because most results that make Fisher information elegant require the classical regime. Additivity is always true (it&rsquo;s a property of independent data, not of convergence). But the Cramer-Rao bound, the interpretation of $\mathcal{I}^{-1}$ as a covariance, the equivalence of expected and observed Fisher: these require convergence to the true parameter (or at least to a neighborhood of it).</p>
<p>When you&rsquo;re not converged, the accumulated Fisher still does useful work, but as a different kind of object. Barrett and Dherin (2021) showed that SGD with finite learning rate implicitly penalizes the trace of the empirical Fisher, connecting the optimizer&rsquo;s discretization error to a Fisher-based regularizer. Jastrzebski et al. (2021) showed that the Fisher trace in the early phase of training has a lasting effect on generalization. The information-geometric view still applies in this regime. The classical statistical guarantees don&rsquo;t.</p>
<h2 id="references">References</h2>
<ul>
<li>Amari, S. (1998). Natural gradient works efficiently in learning. <em>Neural Computation</em>, 10(2), 251-276.</li>
<li>Barrett, D.G.T. and Dherin, B. (2021). Implicit Gradient Regularization. <em>ICLR</em>.</li>
<li>Jastrzebski, S. et al. (2021). Catastrophic Fisher Explosion: Early Phase Fisher Matrix Impacts Generalization. <em>ICML</em>.</li>
<li>Khan, M.E. and Rue, H. (2023). The Bayesian Learning Rule. <em>JMLR</em>, 24(281), 1-76.</li>
<li>Kirkpatrick, J. et al. (2017). Overcoming catastrophic forgetting in neural networks. <em>PNAS</em>, 114(13), 3521-3526.</li>
<li>Kunstner, F., Hennig, P., and Balles, L. (2019). Limitations of the Empirical Fisher Approximation for Natural Gradient Descent. <em>NeurIPS</em>.</li>
<li>Martens, J. (2020). New Insights and Perspectives on the Natural Gradient Method. <em>JMLR</em>, 21(146), 1-76.</li>
<li>Martens, J. and Grosse, R. (2015). Optimizing Neural Networks with Kronecker-Factored Approximate Curvature. <em>ICML</em>.</li>
</ul>
]]></content:encoded><category>fisher-information</category><category>natural-gradient</category><category>optimization</category><category>information-geometry</category></item><item><title>algebraic.mle: MLEs as Algebraic Objects</title><link>https://metafunctor.com/post/2024-05-15-algebraic.mle/</link><pubDate>Sat, 15 May 2021 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-05-15-algebraic.mle/</guid><description>An R package that treats MLEs as algebraic objects. They carry Fisher information, compose through independent likelihoods, and propagate uncertainty correctly.</description><content:encoded><![CDATA[<p>Maximum likelihood estimators have rich mathematical structure. They are consistent, asymptotically normal, efficient. <a href="https://github.com/queelius/algebraic.mle">algebraic.mle</a> exposes this structure through an algebra where MLEs are objects you compose, transform, and query.</p>
<h2 id="the-abstraction">The Abstraction</h2>
<p>An MLE is not just a vector of parameter estimates. It is a statistical object that carries point estimates \(\hat{\theta}\), the Fisher information matrix \(I(\hat{\theta})\), the variance-covariance matrix \(I^{-1}(\hat{\theta})\), Wald-type confidence intervals from asymptotic normality, the log-likelihood value, and convergence diagnostics.</p>
<p>The package wraps all of this in a consistent interface:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="nf">library</span><span class="p">(</span><span class="n">algebraic.mle</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">fit</span> <span class="o">&lt;-</span> <span class="nf">mle</span><span class="p">(</span><span class="n">likelihood_model</span><span class="p">,</span> <span class="n">data</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="nf">coef</span><span class="p">(</span><span class="n">fit</span><span class="p">)</span>           <span class="c1"># Parameter estimates</span>
</span></span><span class="line"><span class="cl"><span class="nf">vcov</span><span class="p">(</span><span class="n">fit</span><span class="p">)</span>           <span class="c1"># Variance-covariance matrix</span>
</span></span><span class="line"><span class="cl"><span class="nf">confint</span><span class="p">(</span><span class="n">fit</span><span class="p">)</span>        <span class="c1"># Confidence intervals</span>
</span></span><span class="line"><span class="cl"><span class="nf">logLik</span><span class="p">(</span><span class="n">fit</span><span class="p">)</span>         <span class="c1"># Log-likelihood</span>
</span></span><span class="line"><span class="cl"><span class="nf">aic</span><span class="p">(</span><span class="n">fit</span><span class="p">)</span>            <span class="c1"># Model selection</span>
</span></span></code></pre></div><h2 id="composition">Composition</h2>
<p>The real point is that MLEs compose. Independent models combine:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="n">fit1</span> <span class="o">&lt;-</span> <span class="nf">mle</span><span class="p">(</span><span class="n">model1</span><span class="p">,</span> <span class="n">data1</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">fit2</span> <span class="o">&lt;-</span> <span class="nf">mle</span><span class="p">(</span><span class="n">model2</span><span class="p">,</span> <span class="n">data2</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">combined</span> <span class="o">&lt;-</span> <span class="n">fit1</span> <span class="o">+</span> <span class="n">fit2</span>  <span class="c1"># Joint likelihood</span>
</span></span></code></pre></div><p>The package handles the algebra. Joint log-likelihood, block-diagonal Fisher information, everything propagates correctly. This works because likelihoods from independent data sources multiply, and multiplication of likelihoods is addition of log-likelihoods. That is a monoid. The package enforces it.</p>
<h2 id="the-ecosystem">The Ecosystem</h2>
<p><code>algebraic.mle</code> is the foundation for a family of packages:</p>
<table>
	<thead>
			<tr>
					<th>Package</th>
					<th>Purpose</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><a href="/projects/likelihood.model/">likelihood.model</a></td>
					<td>Compositional likelihood specification</td>
			</tr>
			<tr>
					<td><a href="/projects/maskedcauses/">maskedcauses</a></td>
					<td>Masked failure data in series systems</td>
			</tr>
			<tr>
					<td><a href="/projects/mdrelax/">mdrelax</a></td>
					<td>Relaxed masking conditions</td>
			</tr>
			<tr>
					<td><a href="/projects/algebraic.dist/">algebraic.dist</a></td>
					<td>Distributions as algebraic objects</td>
			</tr>
			<tr>
					<td><a href="/projects/flexhaz/">flexhaz</a></td>
					<td>Dynamic failure rate distributions</td>
			</tr>
			<tr>
					<td><a href="/projects/hypothesize/">hypothesize</a></td>
					<td>Likelihood ratio tests on MLEs</td>
			</tr>
			<tr>
					<td><a href="/projects/numerical.mle/">numerical.mle</a></td>
					<td>Numerical optimization backends</td>
			</tr>
	</tbody>
</table>
<p>The typical workflow:</p>
<ol>
<li>Define distributions with <code>algebraic.dist</code></li>
<li>Specify likelihood contributions with <code>likelihood.model</code></li>
<li>Fit the model and get an <code>mle</code> object from <code>algebraic.mle</code></li>
<li>Query statistical properties: confidence intervals, hypothesis tests, model selection</li>
</ol>
<p>For series systems with masked data:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="nf">library</span><span class="p">(</span><span class="n">maskedcauses</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="nf">library</span><span class="p">(</span><span class="n">algebraic.mle</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Specify masking model (C1-C2-C3 conditions)</span>
</span></span><span class="line"><span class="cl"><span class="n">model</span> <span class="o">&lt;-</span> <span class="nf">md_likelihood_model</span><span class="p">(</span><span class="n">components</span> <span class="o">=</span> <span class="m">3</span><span class="p">,</span> <span class="n">masking</span> <span class="o">=</span> <span class="s">&#34;bernoulli&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Fit -&gt; returns algebraic.mle object</span>
</span></span><span class="line"><span class="cl"><span class="n">fit</span> <span class="o">&lt;-</span> <span class="nf">md_mle_exp_series_C1_C2_C3</span><span class="p">(</span><span class="n">masked_data</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># All the standard MLE methods work</span>
</span></span><span class="line"><span class="cl"><span class="nf">confint</span><span class="p">(</span><span class="n">fit</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="nf">vcov</span><span class="p">(</span><span class="n">fit</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="nf">aic</span><span class="p">(</span><span class="n">fit</span><span class="p">)</span>
</span></span></code></pre></div><h2 id="theory">Theory</h2>
<p>The asymptotic properties that <code>algebraic.mle</code> exploits come from classical MLE theory:</p>
$$\sqrt{n}(\hat{\theta}_n - \theta^{\ast}) \xrightarrow{d} \mathcal{N}(0, I^{-1}(\theta^{\ast}))$$<p>The <a href="/papers/expo-masked-fim/">expo-masked-fim</a> paper derives closed-form Fisher information for exponential series systems. That is exactly what <code>algebraic.mle</code> uses internally for variance estimation in that case.</p>
<p>For more complex models (Weibull, relaxed masking conditions), we compute Fisher information numerically via observed information:</p>
$$\hat{I}(\hat{\theta}) = -\frac{\partial^2 \ell}{\partial \theta \partial \theta^T}\bigg|_{\theta=\hat{\theta}}$$<h2 id="design-principles">Design Principles</h2>
<p><strong>Separation of concerns.</strong> The likelihood specification (<code>likelihood.model</code>) is independent of the fitting algorithm (<code>numerical.mle</code>) and the result type (<code>algebraic.mle</code>). You can swap optimizers without changing downstream code.</p>
<p><strong>Correctness by construction.</strong> Standard errors, confidence intervals, and hypothesis tests are computed from the Fisher information, not ad-hoc formulas. If your likelihood is correct, statistical inference follows automatically.</p>
<p><strong>Composability.</strong> Build complex models from simpler ones. The algebra ensures properties propagate correctly.</p>
<p>This package directly supports the work in my <a href="/publications/math-proj/">master&rsquo;s thesis</a> on reliability estimation in series systems. The bootstrap confidence intervals, likelihood ratio tests, and model selection all use <code>algebraic.mle</code> objects.</p>
<h2 id="resources">Resources</h2>
<ul>
<li><strong>GitHub</strong>: <a href="https://github.com/queelius/algebraic.mle">github.com/queelius/algebraic.mle</a></li>
<li><strong>Documentation</strong>: <a href="https://queelius.github.io/algebraic.mle/">queelius.github.io/algebraic.mle</a></li>
<li><strong>Related paper</strong>: <a href="/papers/expo-masked-fim/">Closed-Form Fisher Information</a></li>
<li><strong>Companion package</strong>: <a href="/projects/algebraic.dist/">algebraic.dist</a> (distributions as algebraic objects)</li>
</ul>
]]></content:encoded><category>R</category><category>statistics</category><category>maximum likelihood</category><category>algebra</category><category>Fisher information</category></item></channel></rss>