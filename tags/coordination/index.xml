<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:media="http://search.yahoo.com/mrss/"><channel><title>Coordination on metafunctor</title><link>https://metafunctor.com/tags/coordination/</link><description>Research Engineer &amp; Computer Scientist - Machine Learning, Statistical Computing, Open Source Development</description><language>en-us</language><managingEditor>lex@metafunctor.com (Alex Towell)</managingEditor><webMaster>lex@metafunctor.com (Alex Towell)</webMaster><lastBuildDate>Sat, 14 Mar 2026 15:37:57 -0500</lastBuildDate><atom:link href="https://metafunctor.com/tags/coordination/" rel="self" type="application/rss+xml"/><image><url>https://metafunctor.com/images/og-image.jpg</url><title>metafunctor</title><link>https://metafunctor.com/</link></image><item><title>Apertures: Coordinated Partial Evaluation for Distributed Computation</title><link>https://metafunctor.com/papers/aperture/</link><pubDate>Wed, 01 Oct 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/papers/aperture/</guid><description> Open full page Download PDF</description><content:encoded><![CDATA[<style>
.iframe-wrap {
  width: 100%;
  max-width: 100%;
  margin: 0;
  padding: 0;
}
.iframe-wrap iframe {
  display: block;
  width: 100%;
  border: 0;
  min-height: 120vh;
}
.action-bar {
  margin: 0 0 1rem 0;
  padding: 0 1rem;
  display: flex;
  gap: .75rem;
  flex-wrap: wrap;
}
.action-bar a {
  text-decoration: none;
  padding: .5rem .75rem;
  border: 1px solid var(--color-border-default, #ddd);
  border-radius: .4rem;
  display: inline-block;
  color: var(--color-accent-primary, #007acc);
}
.action-bar a:hover {
  background: var(--color-bg-secondary, #f5f5f5);
}
</style>
<div class="action-bar">
  <a href="/latex/aperture/index.html" target="_blank" rel="noopener">Open full page</a>
  <a href="/latex/aperture/apertures-workshop.pdf" target="_blank" rel="noopener">Download PDF</a>
</div>
<div class="iframe-wrap">
  <iframe id="ltx" src="/latex/aperture/index.html" loading="lazy" scrolling="no"></iframe>
</div>
<script>
(function () {
  const iframe = document.getElementById('ltx');

  if (location.hash) {
    const u = new URL(iframe.src, location.origin);
    u.hash = location.hash;
    iframe.src = u.toString();
  }

  let lastScrollHeight = 0;

  function setHeight(h) {
    const newH = Math.ceil(h);
    if (newH > lastScrollHeight + 5) {
      iframe.style.height = newH + 'px';
      lastScrollHeight = newH;
    }
  }

  function measure(doc) {
    const h = Math.max(
      doc.body?.scrollHeight || 0,
      doc.documentElement?.scrollHeight || 0
    );
    if (h > 0) setHeight(h);
  }

  iframe.addEventListener('load', () => {
    const doc = iframe.contentDocument || iframe.contentWindow.document;
    if (!doc) return;

    setTimeout(() => measure(doc), 100);
    setTimeout(() => measure(doc), 500);

    doc.fonts?.ready?.then(() => {
      setTimeout(() => measure(doc), 50);
    }).catch(() => {});

    const images = Array.from(doc.images || []);
    if (images.length > 0) {
      let loadedCount = 0;
      images.forEach(img => {
        if (img.complete) {
          loadedCount++;
        } else {
          img.addEventListener('load', () => {
            loadedCount++;
            if (loadedCount === images.length) {
              setTimeout(() => measure(doc), 50);
            }
          }, { once: true });
        }
      });
    }

    let resizeTimeout;
    window.addEventListener('resize', () => {
      clearTimeout(resizeTimeout);
      resizeTimeout = setTimeout(() => {
        lastScrollHeight = 0;
        measure(doc);
      }, 300);
    });
  });
})();
</script>
]]></content:encoded><category>oblivious computing</category><category>encrypted search</category><category>distributed computation</category><category>partial evaluation</category><category>coordination</category><category>cryptography</category><category>secure multi-party computation</category><category>oblivious query processing</category></item><item><title>Apertures: Partial Evaluation with Holes for Distributed Computation</title><link>https://metafunctor.com/post/2024-04-01-aperture/</link><pubDate>Mon, 01 Apr 2024 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-04-01-aperture/</guid><description>Apertures are a coordination mechanism for distributed computation. Programs with explicit holes can be partially evaluated, optimized, and resumed when the missing pieces arrive. No cryptographic guarantees. Honest about what leaks.</description><content:encoded><![CDATA[<p>The idea behind apertures is simple. You have a computation with holes in it (denoted <code>?variable</code>). You can partially evaluate the parts you know, optimize the result, and fill in the holes later when the data arrives.</p>
<p>This is not a cryptographic protocol. Apertures make no security claims. They leak information through program structure, evaluation patterns, and algebraic relationships. But many real-world coordination problems do not need cryptographic privacy. They need a way for multiple parties to share computation structure while controlling when and where data enters.</p>
<h2 id="the-core-mechanism">The Core Mechanism</h2>
<p>An aperture is an unknown expression, a hole in a computation. Programs with apertures can be partially evaluated by parties who do not know the aperture values, optimized and transformed without seeing the data, and resumed when apertures are eventually filled.</p>
<p>The most useful pattern is local computation:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-lisp" data-lang="lisp"><span class="line"><span class="cl"><span class="c1">;; Server optimizes without seeing data or results</span>
</span></span><span class="line"><span class="cl"><span class="p">(</span><span class="k">let</span> <span class="p">((</span><span class="nv">plan</span> <span class="p">(</span><span class="nv">server-optimize</span> <span class="nv">?query</span><span class="p">)))</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nv">client-execute</span> <span class="nv">plan</span> <span class="nv">?local-data</span><span class="p">))</span>
</span></span><span class="line"><span class="cl"><span class="c1">;; Results stay local</span>
</span></span></code></pre></div><p>The server provides optimization expertise without seeing actual query parameters, the local data being processed, or query results. But the server does learn the query structure and patterns. That is the tradeoff.</p>
<h2 id="language-and-evaluation">Language and Evaluation</h2>
<p>Apertures extend a Lisp-like language with holes:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">e ::= v | x | ?h | (e e*) | (λ (x*) e)
</span></span><span class="line"><span class="cl">    | (let ((x e)*) e) | (if e e e)
</span></span></code></pre></div><p>Evaluation proceeds until it gets blocked by an aperture:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">E[(+ n1 n2)] → n1 + n2           (arithmetic)
</span></span><span class="line"><span class="cl">E[(+ ?h n)]  →p (+ ?h n)         (partial - blocked)
</span></span><span class="line"><span class="cl">E[?h]        →p E[?h]            (aperture blocks)
</span></span></code></pre></div><p>Algebraic simplification happens during partial evaluation:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">(* 0 ?x) →p 0
</span></span><span class="line"><span class="cl">(+ 0 ?x) →p ?x
</span></span><span class="line"><span class="cl">(* 1 ?x) →p ?x
</span></span></code></pre></div><p>These simplifications improve performance. They also leak information. If <code>(* 0 ?x)</code> reduces to <code>0</code>, an observer learns the multiplier was zero without seeing <code>?x</code>.</p>
<h2 id="coordination-patterns">Coordination Patterns</h2>
<p><strong>Progressive refinement.</strong> Multiple parties fill apertures incrementally:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-lisp" data-lang="lisp"><span class="line"><span class="cl"><span class="c1">;; Initial query with holes</span>
</span></span><span class="line"><span class="cl"><span class="p">(</span><span class="nv">select</span> <span class="nv">?table</span>
</span></span><span class="line"><span class="cl">  <span class="p">(</span><span class="nv">where</span> <span class="p">(</span><span class="nb">and</span> <span class="p">(</span><span class="nf">&gt;</span> <span class="nv">?col1</span> <span class="nv">?val1</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">              <span class="p">(</span><span class="nf">&lt;</span> <span class="nv">?col2</span> <span class="nv">?val2</span><span class="p">))))</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">;; Party A knows table structure</span>
</span></span><span class="line"><span class="cl"><span class="p">(</span><span class="nf">fill</span> <span class="nv">query</span> <span class="nv">{table:</span> <span class="s">&#34;users&#34;</span><span class="o">,</span> <span class="nv">col1:</span> <span class="s">&#34;age&#34;</span><span class="o">,</span> <span class="nv">col2:</span> <span class="s">&#34;salary&#34;</span><span class="nv">}</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">;; Party B knows constraints</span>
</span></span><span class="line"><span class="cl"><span class="p">(</span><span class="nf">fill</span> <span class="nv">query</span> <span class="nv">{val1:</span> <span class="mi">18</span><span class="o">,</span> <span class="nv">val2:</span> <span class="nv">100000}</span><span class="p">)</span>
</span></span></code></pre></div><p><strong>Speculative compilation.</strong> The server compiles multiple code paths without knowing which will be used:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-lisp" data-lang="lisp"><span class="line"><span class="cl"><span class="p">(</span><span class="nb">case</span> <span class="nv">?config</span>
</span></span><span class="line"><span class="cl">  <span class="nv">[</span><span class="p">(</span><span class="nv">fast</span><span class="p">)</span> <span class="p">(</span><span class="nv">quick-process</span> <span class="nv">data</span><span class="p">)</span><span class="nv">]</span>
</span></span><span class="line"><span class="cl">  <span class="nv">[</span><span class="p">(</span><span class="nv">accurate</span><span class="p">)</span> <span class="p">(</span><span class="nv">slow-process</span> <span class="nv">data</span><span class="p">)</span><span class="nv">]</span>
</span></span><span class="line"><span class="cl">  <span class="nv">[</span><span class="p">(</span><span class="nv">balanced</span><span class="p">)</span> <span class="p">(</span><span class="nv">hybrid-process</span> <span class="nv">data</span><span class="p">)</span><span class="nv">]</span><span class="p">)</span>
</span></span></code></pre></div><h2 id="performance">Performance</h2>
<p>Overhead compared to direct evaluation is 2-5%:</p>
<table>
  <thead>
      <tr>
          <th>Operation</th>
          <th>Direct (us)</th>
          <th>With Apertures (us)</th>
          <th>Overhead</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Arithmetic</td>
          <td>127</td>
          <td>131</td>
          <td>3.1%</td>
      </tr>
      <tr>
          <td>List processing</td>
          <td>89</td>
          <td>93</td>
          <td>4.5%</td>
      </tr>
      <tr>
          <td>Lambda application</td>
          <td>156</td>
          <td>164</td>
          <td>5.1%</td>
      </tr>
  </tbody>
</table>
<p>The overhead comes from hole checking and maintaining partial evaluation state. It is small because the mechanism is simple.</p>
<h2 id="when-not-to-use-apertures">When Not to Use Apertures</h2>
<p>I want to be clear about the limitations.</p>
<p><strong>Adversarial settings</strong>: an adversary can infer values from structure and exploit evaluation patterns. <strong>High-sensitivity data</strong>: medical records, financial data, personal information. Apertures leak too much. <strong>Regulatory compliance</strong>: GDPR, HIPAA, financial regulations require stronger guarantees.</p>
<p>Here is a concrete example of leakage:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-lisp" data-lang="lisp"><span class="line"><span class="cl"><span class="p">(</span><span class="k">if</span> <span class="p">(</span><span class="nf">&gt;</span> <span class="nv">?age</span> <span class="mi">65</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="p">(</span><span class="nv">discount</span> <span class="mf">0.2</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="p">(</span><span class="k">if</span> <span class="p">(</span><span class="nf">&lt;</span> <span class="nv">?age</span> <span class="mi">18</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="p">(</span><span class="nv">discount</span> <span class="mf">0.1</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="p">(</span><span class="nv">no-discount</span><span class="p">)))</span>
</span></span></code></pre></div><p>Even without seeing <code>?age</code>, an observer learns that age-based discount tiers exist, the exact discount amounts, and the full business logic structure. If that matters, do not use apertures.</p>
<h2 id="when-apertures-fit">When Apertures Fit</h2>
<p>They work when the computation structure is not sensitive, parties are honest-but-curious rather than adversarial, and performance matters more than perfect privacy. Scientific computing with public algorithms, optimization of non-sensitive business logic, collaborative debugging, educational systems.</p>
<p>The C++ implementation provides an S-expression parser with aperture syntax, a partial evaluator with algebraic simplification, hole tracking and filling, and stack-based evaluation with continuation frames.</p>
<h2 id="the-point">The Point</h2>
<p>Not every distributed computation needs cryptographic security. But all of them need honest accounting of their tradeoffs. Apertures fill the gap between &ldquo;no coordination&rdquo; and &ldquo;full cryptographic protocol&rdquo; with minimal overhead and no pretense about what they protect.</p>
<p><a href="/latex/aperture/apertures-workshop.pdf">View the full workshop paper (PDF)</a></p>
<h2 id="related-work">Related Work</h2>
<ul>
<li><strong>Partial Evaluation</strong>: Apertures extend partial evaluation with explicit holes for multi-party coordination</li>
<li><strong>Homomorphic Encryption</strong>: Provides strong privacy with 1000-10000x overhead</li>
<li><strong>Secure Multi-Party Computation</strong>: Cryptographic guarantees for adversarial settings</li>
<li><strong>Staged Computation</strong>: Multi-stage programming for performance optimization</li>
</ul>
]]></content:encoded><category>distributed-systems</category><category>partial-evaluation</category><category>coordination</category><category>programming-languages</category></item></channel></rss>