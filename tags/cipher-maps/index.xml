<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0"
     xmlns:atom="http://www.w3.org/2005/Atom"
     xmlns:content="http://purl.org/rss/1.0/modules/content/"
     xmlns:dc="http://purl.org/dc/elements/1.1/"
     xmlns:media="http://search.yahoo.com/mrss/">
  <channel>
    <title>Cipher Maps on metafunctor</title>
    <link>http://localhost:40461/tags/cipher-maps/</link>
    <description>Research Engineer &amp; Computer Scientist - Machine Learning, Statistical Computing, Open Source Development</description>
    <language>en-us</language>
    <managingEditor>lex@metafunctor.com (Alex Towell)</managingEditor>
    <webMaster>lex@metafunctor.com (Alex Towell)</webMaster>
    
    <lastBuildDate>Fri, 27 Feb 2026 17:25:24 -0600</lastBuildDate>
    <atom:link href="http://localhost:40461/tags/cipher-maps/" rel="self" type="application/rss+xml" />
    <image>
      <url>http://localhost:40461/images/og-image.jpg</url>
      <title>metafunctor</title>
      <link>http://localhost:40461/</link>
    </image>
    
    <item>
      <title>Cipher Maps: A Unified Framework for Oblivious Function Approximation Through Algebraic Structures and Bernoulli Models</title>
      <link>http://localhost:40461/papers/cipher-maps/</link>
      <pubDate>Tue, 14 Oct 2025 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/papers/cipher-maps/</guid>
      <description> Open full page Download PDF </description>
      <content:encoded><![CDATA[<style>
.iframe-wrap {
  width: 100%;
  max-width: 100%;
  margin: 0;
  padding: 0;
}
.iframe-wrap iframe {
  display: block;
  width: 100%;
  border: 0;
  min-height: 120vh;
}
.action-bar {
  margin: 0 0 1rem 0;
  padding: 0 1rem;
  display: flex;
  gap: .75rem;
  flex-wrap: wrap;
}
.action-bar a {
  text-decoration: none;
  padding: .5rem .75rem;
  border: 1px solid var(--color-border-default, #ddd);
  border-radius: .4rem;
  display: inline-block;
  color: var(--color-accent-primary, #007acc);
}
.action-bar a:hover {
  background: var(--color-bg-secondary, #f5f5f5);
}
</style>
<div class="action-bar">
  <a href="/latex/cipher-maps/index.html" target="_blank" rel="noopener">Open full page</a>
  <a href="/latex/cipher-maps/cipher_maps.pdf" target="_blank" rel="noopener">Download PDF</a>
</div>
<div class="iframe-wrap">
  <iframe id="ltx" src="/latex/cipher-maps/index.html" loading="lazy" scrolling="no"></iframe>
</div>
<script>
(function () {
  const iframe = document.getElementById('ltx');

  if (location.hash) {
    const u = new URL(iframe.src, location.origin);
    u.hash = location.hash;
    iframe.src = u.toString();
  }

  let lastScrollHeight = 0;

  function setHeight(h) {
    const newH = Math.ceil(h);
    if (newH > lastScrollHeight + 5) {
      iframe.style.height = newH + 'px';
      lastScrollHeight = newH;
    }
  }

  function measure(doc) {
    const h = Math.max(
      doc.body?.scrollHeight || 0,
      doc.documentElement?.scrollHeight || 0
    );
    if (h > 0) setHeight(h);
  }

  iframe.addEventListener('load', () => {
    const doc = iframe.contentDocument || iframe.contentWindow.document;
    if (!doc) return;

    setTimeout(() => measure(doc), 100);
    setTimeout(() => measure(doc), 500);

    doc.fonts?.ready?.then(() => {
      setTimeout(() => measure(doc), 50);
    }).catch(() => {});

    const images = Array.from(doc.images || []);
    if (images.length > 0) {
      let loadedCount = 0;
      images.forEach(img => {
        if (img.complete) {
          loadedCount++;
        } else {
          img.addEventListener('load', () => {
            loadedCount++;
            if (loadedCount === images.length) {
              setTimeout(() => measure(doc), 50);
            }
          }, { once: true });
        }
      });
    }

    let resizeTimeout;
    window.addEventListener('resize', () => {
      clearTimeout(resizeTimeout);
      resizeTimeout = setTimeout(() => {
        lastScrollHeight = 0;
        measure(doc);
      }, 300);
    });
  });
})();
</script>
]]></content:encoded>
      
      
      <category>oblivious computing</category>
      
      <category>cipher maps</category>
      
      <category>algebraic cipher types</category>
      
      <category>Bernoulli models</category>
      
      <category>function approximation</category>
      
      <category>oblivious data types</category>
      
      <category>cryptography</category>
      
      <category>encrypted search</category>
      
      <category>probabilistic data structures</category>
      
      <category>category theory</category>
      
      
    </item>
    
    <item>
      <title>Cipher Maps: Category Theory Meets Oblivious Computing</title>
      <link>http://localhost:40461/post/2025-10-cipher-maps-algebraic-computing/</link>
      <pubDate>Tue, 14 Oct 2025 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">http://localhost:40461/post/2025-10-cipher-maps-algebraic-computing/</guid>
      <description>Formalizing oblivious computing through cipher maps and algebraic cipher types, using category theory for functorial composition of privacy-preserving transformations.</description>
      <content:encoded><![CDATA[<p>What if you could compute on encrypted data while preserving algebraic structure? Not through expensive homomorphic encryption, but through a mathematical framework that unifies oblivious computing, Bernoulli types, and categorical abstractions.</p>
<p>That&rsquo;s what cipher maps and algebraic cipher types are about.</p>
<h2 id="computing-without-seeing">Computing without seeing</h2>
<p>Start with a monoid <code>(S, *, e)</code> with some operation <code>*</code> and identity <code>e</code>. Now compute on encrypted versions of elements from <code>S</code> while preserving the monoidal structure, hiding actual values, getting approximate but privacy-preserving results, and maintaining composability.</p>
<p>Cipher functors make this possible.</p>
<h2 id="two-papers">Two papers</h2>
<h3 id="1-algebraic-cipher-types-paper">1. Algebraic cipher types (<a href="/papers/algebraic-cipher-types/">paper</a>)</h3>
<p>A cipher functor <code>cA</code> lifts a monoid <code>(S, *, e)</code> into a cipher monoid <code>(cA(S), +, cA(e))</code>.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">Latent space:  (S, *, e)      -- the &#34;real&#34; monoid
</span></span><span class="line"><span class="cl">                | cA (cipher functor)
</span></span><span class="line"><span class="cl">                v
</span></span><span class="line"><span class="cl">Cipher space:  (cA(S), +, e&#39;)  -- encrypted monoid
</span></span></code></pre></div><p>Key properties:</p>
<ul>
<li><code>cA(x * y) ~ cA(x) + cA(y)</code> (structure preservation, up to approximation)</li>
<li>Operations on ciphers don&rsquo;t reveal underlying values</li>
<li>Composition of cipher operations mirrors composition in the base monoid</li>
</ul>
<p>This gives you a type system for oblivious computing. Instead of ad-hoc encryption schemes, you get principled constructions that preserve algebraic structure.</p>
<p>Concrete examples:</p>
<ul>
<li><strong>Encrypted counters</strong>: <code>(N, +, 0)</code> lifts to oblivious count structures</li>
<li><strong>Encrypted sets</strong>: <code>(P(U), union, empty)</code> lifts to hash-based oblivious sets (Bloom filters)</li>
<li><strong>Encrypted multisets</strong>: lifts to Count-Min sketches</li>
</ul>
<h3 id="2-cipher-maps-paper">2. Cipher maps (<a href="/papers/cipher-maps/">paper</a>)</h3>
<p>This paper unifies three threads:</p>
<ol>
<li>Algebraic cipher types (categorical foundations)</li>
<li>Bernoulli types (statistical approximation theory)</li>
<li>Oblivious function approximation (practical algorithms)</li>
</ol>
<p>Cipher maps are morphisms in a category where objects are cipher types, morphisms preserve both algebraic structure and statistical properties, and composition gives you principled ways to build complex oblivious computations.</p>
<p>The key contribution is oblivious Bernoulli maps: functions <code>f : cA(S) -&gt; cA(T)</code> that operate on encrypted inputs, produce encrypted outputs, have provable error bounds from Bernoulli type theory, and preserve categorical structure from cipher functors.</p>
<p>In practice, you can:</p>
<ol>
<li>Start with a regular function <code>f : S -&gt; T</code></li>
<li>Lift it to a cipher map <code>cA(f) : cA(S) -&gt; cA(T)</code></li>
<li>Get automatic privacy guarantees</li>
<li>Get formal error bounds</li>
<li>Compose multiple cipher maps while tracking error propagation</li>
</ol>
<h2 id="why-category-theory">Why category theory</h2>
<p>The natural question: why all the abstract machinery?</p>
<p>Category theory gives you composition for free. Without it, every new oblivious algorithm is a one-off construction with custom security proofs. With cipher functors:</p>
<ul>
<li><strong>Functoriality</strong>: <code>cA(f . g) = cA(f) . cA(g)</code>, composition of secure operations is secure</li>
<li><strong>Natural transformations</strong>: generic ways to convert between different oblivious representations</li>
<li><strong>Limits/colimits</strong>: systematic construction of compound oblivious types</li>
<li><strong>Monoidal structure</strong>: parallel composition of oblivious computations</li>
</ul>
<p>This isn&rsquo;t just mathematical aesthetics. It&rsquo;s practical abstraction that lets you build complex systems from verified components.</p>
<h2 id="hash-based-oblivious-sets">Hash-based oblivious sets</h2>
<p>The <a href="/papers/boolean-algebra-trapdoor-sets/">boolean-algebra-trapdoor-sets paper</a> shows a concrete instantiation.</p>
<p>Hash-Based Oblivious Sets (HBOS) = cipher functor applied to <code>(P(U), union, empty)</code>.</p>
<p>The construction:</p>
<ol>
<li>Start with the powerset monoid (sets with union)</li>
<li>Apply the cipher functor with hash-based Bernoulli maps</li>
<li>Get Bloom filters with provable privacy guarantees</li>
<li>The categorical structure gives you set operations (union, intersection) for free</li>
</ol>
<p>Unlike traditional Bloom filters (just data structures), HBOS are oblivious types with formal privacy semantics, compositional error bounds, type-safe operations, and integration with cipher maps for complex queries.</p>
<h2 id="applications">Applications</h2>
<p>This framework enables several things I&rsquo;m working on:</p>
<p><strong>Privacy-preserving databases</strong>: cipher maps for database operators (join, project, select) that work on oblivious data while hiding access patterns.</p>
<p><strong>Encrypted analytics</strong>: aggregation queries (SUM, COUNT, AVG) lift naturally to cipher maps over appropriate monoids.</p>
<p><strong>Federated learning</strong>: gradient aggregation with oblivious types ensures differential privacy without trusted aggregators.</p>
<p><strong>Secure streaming</strong>: process streams with cipher maps over streaming monoids, constant memory, provable privacy.</p>
<h2 id="implementation">Implementation</h2>
<p>A cipher functor isn&rsquo;t just abstraction. It&rsquo;s a design pattern for building oblivious systems:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="c1">// Pseudocode sketch
</span></span></span><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">T</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">CipherFunctor</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">Cipher</span> <span class="o">=</span> <span class="n">ObliviousBernoulli</span><span class="o">&lt;</span><span class="n">T</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="n">Cipher</span> <span class="nf">lift</span><span class="p">(</span><span class="n">T</span> <span class="n">value</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">T</span> <span class="nf">approximate_inverse</span><span class="p">(</span><span class="n">Cipher</span> <span class="n">cipher</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="c1">// Monoidal operation on ciphers
</span></span></span><span class="line"><span class="cl">    <span class="n">Cipher</span> <span class="nf">combine</span><span class="p">(</span><span class="n">Cipher</span> <span class="n">a</span><span class="p">,</span> <span class="n">Cipher</span> <span class="n">b</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>I&rsquo;m working on C++ implementations using modern template metaprogramming to make cipher types zero-cost abstractions.</p>
<h2 id="whats-next">What&rsquo;s next</h2>
<p>These papers open several research directions:</p>
<ol>
<li><strong>Higher categories</strong>: what about functors between cipher categories?</li>
<li><strong>Dependent types</strong>: can we encode privacy/accuracy trade-offs in the type system?</li>
<li><strong>Effect systems</strong>: track information flow using cipher maps</li>
<li><strong>Compiler integration</strong>: automatically lift regular code to oblivious versions</li>
</ol>
<h2 id="why-now">Why now</h2>
<p>Privacy regulations demand oblivious computing. Scale requires approximate algorithms. Complexity demands compositional abstractions. Cipher maps provide the mathematical foundation to build these systems correctly.</p>
<p>Instead of ad-hoc crypto plus hope, custom privacy proofs for each algorithm, and non-compositional security guarantees, you get principled constructions from category theory, automatic privacy from functoriality, and compositional security from categorical structure.</p>
<p><strong>Papers:</strong></p>
<ul>
<li><a href="/papers/algebraic-cipher-types/">Algebraic Cipher Types</a></li>
<li><a href="/papers/cipher-maps/">Cipher Maps: Unified Framework</a></li>
<li><a href="/papers/boolean-algebra-trapdoor-sets/">Hash-Based Oblivious Sets</a></li>
<li><a href="/post/2025-10-bernoulli-types-foundations/">Bernoulli Types Series</a></li>
<li><a href="/post/2025-10-encrypted-search-oblivious-types/">Encrypted Search with Oblivious Types</a></li>
</ul>
<hr>
<p><em>This work synthesizes my research in oblivious computing, type theory, and cryptography. The mathematics is involved, but the payoff is practical: systematic construction of privacy-preserving systems with formal guarantees.</em></p>
]]></content:encoded>
      
      
      <category>oblivious computing</category>
      
      <category>category theory</category>
      
      <category>algebraic cipher types</category>
      
      <category>cipher maps</category>
      
      <category>functors</category>
      
      <category>type theory</category>
      
      <category>cryptography</category>
      
      <category>Bernoulli models</category>
      
      <category>research</category>
      
      
    </item>
    
  </channel>
</rss>
