<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0"
     xmlns:atom="http://www.w3.org/2005/Atom"
     xmlns:content="http://purl.org/rss/1.0/modules/content/"
     xmlns:dc="http://purl.org/dc/elements/1.1/"
     xmlns:media="http://search.yahoo.com/mrss/">
  <channel>
    <title>Information on metafunctor</title>
    <link>https://metafunctor.com/tags/information/</link>
    <description>Research Engineer &amp; Computer Scientist - Machine Learning, Statistical Computing, Open Source Development</description>
    <language>en-us</language>
    <managingEditor>lex@metafunctor.com (Alex Towell)</managingEditor>
    <webMaster>lex@metafunctor.com (Alex Towell)</webMaster>
    
    <lastBuildDate>Sat, 04 Jul 2026 21:09:25 -0500</lastBuildDate>
    <atom:link href="https://metafunctor.com/tags/information/" rel="self" type="application/rss+xml" />
    <image>
      <url>https://metafunctor.com/images/og-image.jpg</url>
      <title>metafunctor</title>
      <link>https://metafunctor.com/</link>
    </image>
    
    <item>
      <title>Classical Information First: The Coin Under the Cup</title>
      <link>https://metafunctor.com/post/00-classical-information/</link>
      <pubDate>Mon, 08 Jan 2024 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">https://metafunctor.com/post/00-classical-information/</guid>
      <description>Before the qubit, the ordinary object it edits: classical probability as vectors and stochastic matrices, and the one rule (weights never go negative) that quantum information breaks.</description>
      <content:encoded><![CDATA[<p>This series builds a quantum computer simulator from nothing, and the next post
starts it properly with the qubit. This one comes first because a qubit is not a
strange new object dropped in from nowhere. It is an ordinary object, the
classical bit, with exactly one rule changed. If I build the ordinary object
carefully, in code, the quantum one turns out to be almost a one-line edit, and
the rest of the series is the story of what that edit buys. So there are no
qubits here until the last few lines. There are coins, NumPy arrays, and the
probability theory you already half-know, written down until it is explicit.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">import</span> <span class="nn">numpy</span> <span class="k">as</span> <span class="nn">np</span>
</span></span></code></pre></div><h2 id="the-coin-under-the-cup">The coin under the cup</h2>
<p>Take a coin out of your pocket, spin it, catch it, and slap it down on the back
of your hand. Before you look, cover it with a cup.</p>
<p>Is it heads or tails?</p>
<p>You do not know. But you do not doubt for a moment that it is one or the other.
Under the cup, in the dark, the coin is in some perfectly definite state. It has
been since your hand came down. The not-knowing is yours. The coin already made
up its mind.</p>
<p>Now bring in a friend. While you look away, they lift the edge of the cup,
glance under it, and set it back down without a word. Ask each of you the odds.
For you, still fifty-fifty. For your friend, no odds at all: it is heads,
plainly, a settled fact. Same coin, same cup, same instant, two different
numbers. Nothing about the coin changed when your friend looked. No force
reached under the cup. The only thing that changed in the whole room was what one
of you knew.</p>
<p>So the fifty-fifty was never a property of the coin. It was a property of you, a
fair summary of a mind holding two live guesses with no way yet to choose between
them. The probability lived in the gap between the coin and your knowledge of it,
and your friend, by looking, closed the gap. What your friend gained has a plain
name: information. The coin holds a single piece of it, one yes-or-no, the piece
engineers call a bit, and your uncertainty was nothing but the absence of that
one piece.</p>
<p>That is the whole starting point, and it is worth holding onto because the
quantum world is going to break it: the thing is definite, the odds are about
the knower, and information is closing the gap.</p>
<h2 id="a-state-is-a-vector-of-what-you-know">A state is a vector of what you know</h2>
<p>Everything that follows is just that idea written as arithmetic. Call the two
outcomes heads and tails, and give each a slot in a length-two array. Your state
of knowledge is a pair of non-negative numbers that sum to one: how much of your
belief sits on each outcome.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">state</span> <span class="o">=</span> <span class="n">np</span><span class="o">.</span><span class="n">array</span><span class="p">([</span><span class="mf">0.5</span><span class="p">,</span> <span class="mf">0.5</span><span class="p">])</span>   <span class="c1"># fifty-fifty: I know it is a coin, nothing more</span>
</span></span><span class="line"><span class="cl"><span class="n">state</span>
</span></span></code></pre></div><pre><code>array([0.5, 0.5])
</code></pre>
<p>When you are certain, the array is <em>one-hot</em>: all the weight on one outcome, none
on the other. The friend who peeked is holding one of these.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">heads</span> <span class="o">=</span> <span class="n">np</span><span class="o">.</span><span class="n">array</span><span class="p">([</span><span class="mf">1.0</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">])</span>
</span></span><span class="line"><span class="cl"><span class="n">tails</span> <span class="o">=</span> <span class="n">np</span><span class="o">.</span><span class="n">array</span><span class="p">([</span><span class="mf">0.0</span><span class="p">,</span> <span class="mf">1.0</span><span class="p">])</span>
</span></span><span class="line"><span class="cl"><span class="n">heads</span>
</span></span></code></pre></div><pre><code>array([1., 0.])
</code></pre>
<p>There is nothing behind the array. The distribution is not a blurry picture of a
hidden number; it <em>is</em> the state of your knowledge, in full. The coin is heads or
tails underneath, but what you carry, and the only thing any operation below acts
on, is this vector.</p>
<h2 id="operations-are-stochastic-matrices">Operations are stochastic matrices</h2>
<p>If a state is a vector, an operation on it is a matrix, and it acts by matrix
multiplication. The simplest is a deterministic flip, the classical NOT, which
swaps heads and tails. As a matrix it has a single one in each column.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">NOT</span> <span class="o">=</span> <span class="n">np</span><span class="o">.</span><span class="n">array</span><span class="p">([[</span><span class="mf">0.0</span><span class="p">,</span> <span class="mf">1.0</span><span class="p">],</span>
</span></span><span class="line"><span class="cl">                <span class="p">[</span><span class="mf">1.0</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">]])</span>
</span></span><span class="line"><span class="cl"><span class="n">NOT</span> <span class="o">@</span> <span class="n">heads</span>
</span></span></code></pre></div><pre><code>array([0., 1.])
</code></pre>
<p>An operation can also be random. A fresh fair toss throws away whatever you knew
and returns fifty-fifty no matter what you fed in. A worn, biased process might
leave the coin mostly as it was and flip it now and then. Both are matrices whose
columns are themselves probability vectors: non-negative, each summing to one.
These are the <em>stochastic</em> matrices, and they are exactly the operations that
send a distribution to a distribution.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">fair</span>  <span class="o">=</span> <span class="n">np</span><span class="o">.</span><span class="n">array</span><span class="p">([[</span><span class="mf">0.5</span><span class="p">,</span> <span class="mf">0.5</span><span class="p">],</span>
</span></span><span class="line"><span class="cl">                  <span class="p">[</span><span class="mf">0.5</span><span class="p">,</span> <span class="mf">0.5</span><span class="p">]])</span>   <span class="c1"># re-randomize: forget the input</span>
</span></span><span class="line"><span class="cl"><span class="n">drift</span> <span class="o">=</span> <span class="n">np</span><span class="o">.</span><span class="n">array</span><span class="p">([[</span><span class="mf">0.9</span><span class="p">,</span> <span class="mf">0.1</span><span class="p">],</span>
</span></span><span class="line"><span class="cl">                  <span class="p">[</span><span class="mf">0.1</span><span class="p">,</span> <span class="mf">0.9</span><span class="p">]])</span>   <span class="c1"># mostly keep it, flip 1 in 10</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">drift</span> <span class="o">@</span> <span class="n">heads</span>                    <span class="c1"># start certain-heads, come out 90/10</span>
</span></span></code></pre></div><pre><code>array([0.9, 0.1])
</code></pre>
<p>The payoff is composition: running one operation after another is multiplying
their matrices. That gives you a single matrix for the whole pipeline, and it is
still stochastic. Toss the fair coin, then toss it again, and multiplying the
matrices confirms what you already expect, that a second fair toss changes
nothing.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">fair</span> <span class="o">@</span> <span class="n">fair</span>          <span class="c1"># two fair tosses back to back</span>
</span></span></code></pre></div><pre><code>array([[0.5, 0.5],
       [0.5, 0.5]])
</code></pre>
<p>That is the entire mechanics of classical information: states are vectors,
certain or not; operations are stochastic matrices; and to chain them you
multiply. Hold that in place, because the quantum world keeps every word of it
and changes only what the numbers are allowed to be.</p>
<h2 id="the-one-rule-weights-never-go-negative">The one rule: weights never go negative</h2>
<p>There is one constraint underneath all of this that is so obvious it is easy to
walk past, and it is the constraint the whole series turns on. The weights in
these vectors and matrices are probabilities, so they are never negative. They
only ever add.</p>
<p>Watch what that forces. Suppose an outcome can be reached by more than one route,
through some intermediate step. The total probability of arriving is the sum of
what each route contributes, and every contribution is a non-negative number.
Here is a two-stage process, and I will pull out the separate routes to one
outcome.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">s0</span>     <span class="o">=</span> <span class="n">np</span><span class="o">.</span><span class="n">array</span><span class="p">([</span><span class="mf">1.0</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">])</span>   <span class="c1"># start certain</span>
</span></span><span class="line"><span class="cl"><span class="n">A</span>      <span class="o">=</span> <span class="n">np</span><span class="o">.</span><span class="n">array</span><span class="p">([[</span><span class="mf">0.5</span><span class="p">,</span> <span class="mf">0.5</span><span class="p">],</span>
</span></span><span class="line"><span class="cl">                   <span class="p">[</span><span class="mf">0.5</span><span class="p">,</span> <span class="mf">0.5</span><span class="p">]])</span>  <span class="c1"># stage one: branch</span>
</span></span><span class="line"><span class="cl"><span class="n">B</span>      <span class="o">=</span> <span class="n">np</span><span class="o">.</span><span class="n">array</span><span class="p">([[</span><span class="mf">0.5</span><span class="p">,</span> <span class="mf">0.5</span><span class="p">],</span>
</span></span><span class="line"><span class="cl">                   <span class="p">[</span><span class="mf">0.5</span><span class="p">,</span> <span class="mf">0.5</span><span class="p">]])</span>  <span class="c1"># stage two: recombine</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">mid</span>    <span class="o">=</span> <span class="n">A</span> <span class="o">@</span> <span class="n">s0</span>                 <span class="c1"># distribution after stage one</span>
</span></span><span class="line"><span class="cl"><span class="n">routes</span> <span class="o">=</span> <span class="n">B</span><span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="p">:]</span> <span class="o">*</span> <span class="n">mid</span>          <span class="c1"># each route&#39;s contribution to outcome 0</span>
</span></span><span class="line"><span class="cl"><span class="n">routes</span>
</span></span></code></pre></div><pre><code>array([0.25, 0.25])
</code></pre>
<p>Each route hands outcome 0 a piece of probability, and the total is just their
sum.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="nb">float</span><span class="p">(</span><span class="n">routes</span><span class="o">.</span><span class="n">sum</span><span class="p">())</span>
</span></span></code></pre></div><pre><code>0.5
</code></pre>
<p>Now the part that has no escape hatch. Because every contribution is at least
zero, opening a second route to an outcome can only <em>raise</em> its total, never
lower it. More ways to arrive means more chance of arriving.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">one_route</span>  <span class="o">=</span> <span class="nb">float</span><span class="p">(</span><span class="n">routes</span><span class="p">[</span><span class="mi">0</span><span class="p">])</span>               <span class="c1"># only the first route open</span>
</span></span><span class="line"><span class="cl"><span class="n">two_routes</span> <span class="o">=</span> <span class="nb">float</span><span class="p">(</span><span class="n">routes</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">+</span> <span class="n">routes</span><span class="p">[</span><span class="mi">1</span><span class="p">])</span>   <span class="c1"># both routes open</span>
</span></span><span class="line"><span class="cl"><span class="n">one_route</span><span class="p">,</span> <span class="n">two_routes</span>
</span></span></code></pre></div><pre><code>(0.25, 0.5)
</code></pre>
<p>There is no way to arrange a classical coin so that opening a second path to
heads makes heads <em>less</em> likely. To make routes work against each other you would
need one of them to contribute a negative weight, and a negative weight is not a
probability. It is forbidden by the one rule. Probability piles up. It never digs
a hole.</p>
<h2 id="the-one-change-that-makes-it-quantum">The one change that makes it quantum</h2>
<p>Here is the whole crossing into the quantum world, and it really is this small.
Keep the vectors. Keep the matrices. Keep multiplying to compose. Change only the
one rule: let the weights be <em>amplitudes</em>, numbers that may carry a minus sign,
and recover a probability only by squaring one at the very end.</p>
<p>That single permission is the entire difference, because now two routes to the
same outcome can carry opposite signs, and opposite signs cancel.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="mf">0.5</span> <span class="o">+</span> <span class="mf">0.5</span>            <span class="c1"># classical weights: two routes pile up</span>
</span></span></code></pre></div><pre><code>1.0
</code></pre>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="p">(</span><span class="mf">0.5</span> <span class="o">+</span> <span class="o">-</span><span class="mf">0.5</span><span class="p">)</span> <span class="o">**</span> <span class="mi">2</span>    <span class="c1"># amplitudes: opposite signs, squared at the end, cancel to nothing</span>
</span></span></code></pre></div><pre><code>0.0
</code></pre>
<p>Two routes to a place, and the chance of arriving drops to zero. That is the
impossible-sounding thing a classical coin can never do, and it is what an
amplitude does as a matter of course. The cancellation has a name, interference,
and everything in this series that sounds strange (superposition, the algorithms,
the whole apparent magic of the thing) is this one move and nothing else.</p>
<p>The object that carries the change is <a href="/post/00-what-is-a-qubit/">the qubit</a>, and
building it is the next post. It keeps the length-two array. It keeps the matrix
operations. It relaxes exactly the rule we just spent this whole post making
explicit, so that you can see precisely what is being given up and what is bought
in return. Bring the coin. Bring your certainty about it.</p>
<h2 id="a-companion-note">A companion note</h2>
<p>This post follows the on-ramp of my book <em>Multitudes: The Indifference of
Measure</em>, whose Part I builds the same physics from this same coin under the same
cup, in pictures rather than code. If the prose here lands for you, the book is
the long version:
<a href="https://www.amazon.com/dp/B0H64TXL2B">Amazon</a> and
<a href="https://metafunctor.com/writing/multitudes/">metafunctor.com/writing/multitudes</a>.</p>
<p>The ladder this post climbs, classical states as vectors, then operations as
matrices, then the single quantum change, follows John Watrous&rsquo;s lovely treatment
in <em>Basics of Quantum Information</em>. The punchline is his: as small as the change
is, it is the whole of the difference.</p>
<h2 id="watch-the-episode">Watch the Episode</h2>
<div class="youtube-episode" style="position:relative;width:100%;padding-bottom:56.25%;height:0;overflow:hidden;margin:1.5rem 0;">
  <iframe
    src="https://www.youtube-nocookie.com/embed/kSbtARgBRw8"
    title="Classical Information First: The Coin Under the Cup"
    style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;"
    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
    allowfullscreen
    loading="lazy"
    referrerpolicy="strict-origin-when-cross-origin"></iframe>
</div>
<p class="youtube-episode-links"><em>This post is also a video episode: <a href="https://youtu.be/kSbtARgBRw8" rel="noopener" target="_blank">Classical Information First: The Coin Under the Cup</a>, part of <a href="https://www.youtube.com/playlist?list=PLdnVusLuULrk" rel="noopener" target="_blank">the series playlist</a>.</em></p>
]]></content:encoded>
      
      
      <category>quantum-computing</category>
      
      <category>information</category>
      
      <category>python</category>
      
      <category>from-scratch</category>
      
      
    </item>
    
  </channel>
</rss>
