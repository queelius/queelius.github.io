<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:media="http://search.yahoo.com/mrss/"><channel><title>Open-Source on metafunctor</title><link>https://metafunctor.com/tags/open-source/</link><description>Research Engineer &amp; Computer Scientist - Machine Learning, Statistical Computing, Open Source Development</description><language>en-us</language><managingEditor>lex@metafunctor.com (Alex Towell)</managingEditor><webMaster>lex@metafunctor.com (Alex Towell)</webMaster><lastBuildDate>Thu, 18 Jun 2026 01:12:41 -0500</lastBuildDate><atom:link href="https://metafunctor.com/tags/open-source/" rel="self" type="application/rss+xml"/><image><url>https://metafunctor.com/images/og-image.jpg</url><title>metafunctor</title><link>https://metafunctor.com/</link></image><item><title>The MCP Pattern: SQLite as the AI-Queryable Cache</title><link>https://metafunctor.com/post/2026-03-20-the-mcp-pattern/</link><pubDate>Fri, 20 Mar 2026 20:39:07 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2026-03-20-the-mcp-pattern/</guid><description>Every data silo follows the same architecture: domain files as ground truth, SQLite + FTS5 as a read-only cache, MCP as the query interface. After building five of these, the pattern is clear.</description><content:encoded><![CDATA[<p>I keep building the same thing.</p>
<p>Not the same <em>product</em> &mdash; the products are different. One indexes a Hugo blog. One indexes AI conversations. One consolidates medical records from three hospitals. One catalogs a hundred git repositories. But underneath, they all have the same skeleton. After the fifth time, I think the skeleton deserves a name.</p>
<h2 id="the-pattern">The pattern</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-gdscript3" data-lang="gdscript3"><span class="line"><span class="cl"><span class="n">Domain</span> <span class="n">files</span> <span class="p">(</span><span class="n">ground</span> <span class="n">truth</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="err">↓</span> <span class="n">index</span>
</span></span><span class="line"><span class="cl"><span class="n">SQLite</span> <span class="n">database</span> <span class="p">(</span><span class="n">read</span><span class="o">-</span><span class="n">only</span> <span class="n">cache</span><span class="p">,</span> <span class="n">FTS5</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="err">↓</span> <span class="n">expose</span>
</span></span><span class="line"><span class="cl"><span class="n">MCP</span> <span class="n">server</span> <span class="p">(</span><span class="n">tools</span> <span class="o">+</span> <span class="n">resources</span> <span class="err">→</span> <span class="n">AI</span> <span class="n">assistant</span><span class="p">)</span>
</span></span></code></pre></div><p>That&rsquo;s it. Three layers. The domain files are always canonical &mdash; the database is a disposable cache you can rebuild from them at any time. SQLite gives you structured queries, full-text search, and JSON extraction over data that was previously trapped in flat files. MCP exposes it to an AI assistant that can write SQL, retrieve content, and (in some cases) create new content.</p>
<p>Here&rsquo;s the inventory:</p>
<table>
	<thead>
			<tr>
					<th>Project</th>
					<th>Domain</th>
					<th>Ground Truth</th>
					<th>What the MCP Exposes</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><strong>hugo-memex</strong></td>
					<td>Blog content</td>
					<td>Markdown files with YAML front matter</td>
					<td>951 pages, FTS5 search, taxonomy queries, JSON front matter extraction</td>
			</tr>
			<tr>
					<td><strong>memex</strong></td>
					<td>AI conversations</td>
					<td>ChatGPT/Claude/Gemini exports</td>
					<td>Conversation trees, FTS5 message search, tags, enrichments</td>
			</tr>
			<tr>
					<td><strong>chartfold</strong></td>
					<td>Medical records</td>
					<td>Epic, MEDITECH, athenahealth exports</td>
					<td>Labs, meds, encounters, imaging, pathology, cross-source reconciliation</td>
			</tr>
			<tr>
					<td><strong>arkiv</strong></td>
					<td>Personal archives</td>
					<td>JSONL files from various sources</td>
					<td>Unified SQL over heterogeneous personal data</td>
			</tr>
			<tr>
					<td><strong>repoindex</strong></td>
					<td>Git repositories</td>
					<td>Local git repos + GitHub/PyPI/CRAN metadata</td>
					<td>Repository catalog with activity tracking, publication status</td>
			</tr>
	</tbody>
</table>
<p>Five projects. Five completely different domains. One architecture.</p>
<h2 id="why-sqlite">Why SQLite</h2>
<p>SQLite is the most deployed database in history. It&rsquo;s on every phone, every browser, every Python installation. But that&rsquo;s not why I use it.</p>
<p>I use it because it solves three problems at once:</p>
<p><strong>Structured queries over unstructured data.</strong> Hugo front matter is YAML trapped inside markdown files. Medical records are scattered across three incompatible EHR export formats. AI conversations are JSON trees with branching paths. SQLite turns all of these into tables you can JOIN, GROUP BY, and aggregate. <code>json_extract()</code> handles the long tail of fields that don&rsquo;t fit a fixed schema.</p>
<p><strong>Full-text search.</strong> FTS5 with porter stemming and unicode61 tokenization gives you relevance-ranked search across any text corpus. No Elasticsearch, no external service, no running daemon. Just a virtual table that lives in the same database file.</p>
<p><strong>Read-only enforcement.</strong> SQLite&rsquo;s authorizer callback lets you whitelist specific SQL operations at the statement level. My MCP servers allow SELECT, READ, and FUNCTION &mdash; everything else gets SQLITE_DENY. This isn&rsquo;t <code>PRAGMA query_only</code> (which can be disabled by the caller). It&rsquo;s engine-level enforcement that cannot be bypassed via SQL.</p>
<p>And the operational properties are free: WAL mode for concurrent readers, a single file you can back up with <code>cp</code>, zero configuration, zero running processes.</p>
<h2 id="why-mcp">Why MCP</h2>
<p>The Model Context Protocol is the thin layer that makes SQLite useful to an AI assistant. An MCP server exposes tools (functions the AI can call) and resources (reference material the AI can read). That&rsquo;s the whole API surface.</p>
<p>For each project, the MCP layer follows the same shape:</p>
<p><strong><code>execute_sql</code></strong> &mdash; The power tool. Read-only SQL with exemplar queries in the docstring. The docstring is critical: it&rsquo;s the AI&rsquo;s primary reference for writing correct SQL. Ten well-chosen example queries teach the model more than a schema diagram.</p>
<p><strong><code>get_&lt;things&gt;</code></strong> &mdash; Bulk retrieval. Instead of <code>execute_sql</code> to find IDs then N individual fetches, one call returns full content for a filtered set. This matters when you&rsquo;re sharing a context window across multiple MCP servers.</p>
<p><strong><code>&lt;domain&gt;://schema</code></strong> &mdash; A resource containing the full DDL, relationship documentation, and query patterns. The AI reads this once, then writes SQL against it for the rest of the session.</p>
<p>The specific tools vary by domain. Hugo-memex has <code>create_page</code> and <code>suggest_tags</code>. Chartfold has <code>get_clinical_summary</code> and <code>get_timeline</code>. Memex has <code>get_conversations</code> with message previews. But the structural pattern is the same.</p>
<h2 id="the-database-is-a-cache">The database is a cache</h2>
<p>This is the most important architectural decision, and it&rsquo;s easy to get wrong.</p>
<p>The database is not the source of truth. The files are. The database is a materialized index that can be rebuilt from the files at any time. This means:</p>
<ul>
<li>
<p><strong>No migrations.</strong> If the schema changes, drop the database and re-index. For a 951-page Hugo site, full re-indexing takes six seconds. Why maintain migration code for a disposable cache?</p>
</li>
<li>
<p><strong>No write conflicts.</strong> The files are edited by humans (or by AI tools that write to the filesystem). The database is updated by the indexer. There&rsquo;s exactly one write path.</p>
</li>
<li>
<p><strong>No backup strategy.</strong> You already back up your files. The database is derived from them. Lose the database? Rebuild it.</p>
</li>
<li>
<p><strong>Incremental sync is an optimization, not a requirement.</strong> SHA-256 content hashes + file mtimes make re-indexing fast. But if incremental sync has a bug, force a full rebuild. The cache being disposable means you can always recover.</p>
</li>
</ul>
<p>The one exception in my inventory is chartfold, where the database <em>is</em> the store for imported medical records (the original exports aren&rsquo;t kept in a format that round-trips cleanly). Even there, the exports are the moral source of truth &mdash; you re-import to rebuild.</p>
<h2 id="what-large-context-changes">What large context changes</h2>
<p>With a million-token context window, you might think this pattern is obsolete. Why index into SQLite when you can just load everything into context?</p>
<p>The math says otherwise. My Hugo blog is 951 pages, ~480K words, ~1.9M tokens. It doesn&rsquo;t fit. And that&rsquo;s one data source. Add AI conversations (memex), medical records (chartfold), and repository metadata (repoindex), and you&rsquo;re well past the limit.</p>
<p>But even if it did fit, the pattern would still be useful. Loading 480K words into context to answer &ldquo;which posts are tagged &lsquo;reinforcement-learning&rsquo;?&rdquo; is like loading an entire database into memory to run a SELECT with a WHERE clause. SQLite does it in microseconds. Context loading costs seconds and tokens.</p>
<p>The right model is: <strong>MCP for navigation, context for understanding.</strong> Use <code>execute_sql</code> to find the five relevant posts, then use <code>get_pages</code> to load their full content into context. One tool call for discovery, one for deep reading. Two calls total instead of flooding the context with everything.</p>
<p>This is how I actually use it. When I asked my AI assistant to analyze my blog, it ran:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-sql" data-lang="sql"><span class="line"><span class="cl"><span class="k">SELECT</span><span class="w"> </span><span class="n">section</span><span class="p">,</span><span class="w"> </span><span class="k">COUNT</span><span class="p">(</span><span class="o">*</span><span class="p">)</span><span class="w"> </span><span class="k">as</span><span class="w"> </span><span class="n">pages</span><span class="p">,</span><span class="w"> </span><span class="k">SUM</span><span class="p">(</span><span class="n">word_count</span><span class="p">)</span><span class="w"> </span><span class="k">as</span><span class="w"> </span><span class="n">words</span><span class="p">,</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="n">ROUND</span><span class="p">(</span><span class="k">AVG</span><span class="p">(</span><span class="n">word_count</span><span class="p">))</span><span class="w"> </span><span class="k">as</span><span class="w"> </span><span class="n">avg_words</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="k">FROM</span><span class="w"> </span><span class="n">pages</span><span class="w"> </span><span class="k">WHERE</span><span class="w"> </span><span class="n">draft</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="mi">0</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="k">GROUP</span><span class="w"> </span><span class="k">BY</span><span class="w"> </span><span class="n">section</span><span class="w"> </span><span class="k">ORDER</span><span class="w"> </span><span class="k">BY</span><span class="w"> </span><span class="n">words</span><span class="w"> </span><span class="k">DESC</span><span class="w">
</span></span></span></code></pre></div><p>One tool call. Instant orientation. Then it loaded specific posts for deeper analysis. The total context cost was a fraction of what a full dump would have been, and the assistant had <em>better</em> data because it could aggregate, filter, and join.</p>
<h2 id="the-tools-that-earn-their-keep">The tools that earn their keep</h2>
<p>After building five of these, certain tools prove their worth and others don&rsquo;t.</p>
<p><strong>The tools that matter:</strong></p>
<ul>
<li><code>execute_sql</code> with good docstring examples. This is 80% of the value. A well-documented schema + exemplar queries lets the AI answer almost any question.</li>
<li>Bulk retrieval (<code>get_pages</code>, <code>get_conversations</code>, <code>get_clinical_summary</code>). One call instead of N+1. Critical when you&rsquo;re sharing context across multiple servers.</li>
<li>Schema/stats resources. Quick orientation without burning a tool call.</li>
</ul>
<p><strong>The tools that surprised me:</strong></p>
<ul>
<li><code>suggest_tags</code> in hugo-memex. Uses FTS5 similarity to find pages like your draft, then returns their most common tags with canonical casing. Solved a real problem: my blog had 40 case-duplicate tag pairs (<code>Python</code>/<code>python</code>, <code>AI</code>/<code>ai</code>).</li>
<li><code>get_timeline</code> in chartfold. Merges encounters, procedures, labs, imaging, and notes into a single chronological stream. The AI can reason about temporal relationships that are invisible when the data is in separate tables.</li>
</ul>
<p><strong>What I&rsquo;d skip next time:</strong></p>
<ul>
<li>Fine-grained CRUD tools for things that are easier to do via the filesystem or raw SQL. The <code>update_page</code> tool in hugo-memex is fine, but I could also just edit the file. The MCP tool is one more surface to maintain.</li>
</ul>
<h2 id="the-unix-connection">The Unix connection</h2>
<p>This pattern is the Unix philosophy applied to AI tooling:</p>
<ol>
<li><strong>Small tools that do one thing well.</strong> Each MCP server handles one domain. No mega-server that does everything.</li>
<li><strong>Text as the universal interface.</strong> SQL in, JSON out. The AI speaks text natively.</li>
<li><strong>Composition over integration.</strong> Five independent MCP servers, each ignorable, each replaceable. Claude Code connects to all of them simultaneously without any of them knowing about each other.</li>
<li><strong>Files as ground truth.</strong> The oldest pattern in computing. Files are durable, inspectable, versionable. Databases are derived views.</li>
</ol>
<p>The difference from classical Unix pipes is the composition layer. Instead of <code>grep | sort | uniq</code>, the AI is the orchestrator. It reads the schema, decides which tool to call, interprets the results, and decides what to do next. The pipes are implicit in the AI&rsquo;s reasoning, not explicit in a shell command.</p>
<h2 id="what-id-tell-you-if-youre-building-one">What I&rsquo;d tell you if you&rsquo;re building one</h2>
<p>Start with <code>execute_sql</code> and a schema resource. That&rsquo;s enough to be useful. The AI can figure out the rest if the schema is well-documented.</p>
<p>Make the database disposable. If you&rsquo;re writing migration code, you&rsquo;ve made it too important.</p>
<p>Put the exemplar queries in the tool docstring, not in a separate document. The docstring is the one thing the AI definitely reads.</p>
<p>Use FTS5. The marginal cost is one virtual table and a few INSERT statements during indexing. The marginal benefit is that the AI can search your content by meaning, not just by exact column values.</p>
<p>Enforce read-only at the engine level, not the application level. SQLite&rsquo;s authorizer callback is the right mechanism. <code>PRAGMA query_only</code> is a suggestion, not a wall.</p>
<p>Build bulk retrieval tools early. The N+1 pattern (find IDs, then fetch one at a time) is the biggest efficiency problem in MCP servers. One tool call that returns full content for a filtered set changes the economics of every conversation.</p>
<hr>
<p><em>The projects: <a href="https://github.com/queelius/hugo-memex">hugo-memex</a> (PyPI: <code>hugo-memex</code>), <a href="https://github.com/queelius/memex">memex</a> (PyPI: <code>py-memex</code>), <a href="https://github.com/queelius/chartfold">chartfold</a>, <a href="https://github.com/queelius/arkiv">arkiv</a>, <a href="https://github.com/queelius/repoindex">repoindex</a>.</em></p>
]]></content:encoded><category>MCP</category><category>sqlite</category><category>architecture</category><category>cli</category><category>python</category><category>software-design</category><category>open-source</category><category>unix-philosophy</category></item><item><title>Narrating a Hugo Blog with Sentence Highlighting</title><link>https://metafunctor.com/post/2026-02-26-narro-hugo-integration/</link><pubDate>Thu, 26 Feb 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2026-02-26-narro-hugo-integration/</guid><description>narro can now generate audio narration for Hugo blog posts, with synchronized sentence highlighting in the browser. The alignment problem turned out to be more interesting than expected.</description><content:encoded><![CDATA[<p>I wanted my blog posts to have audio narration. Not a podcast, not a read-aloud button that sends text to a cloud API. Local TTS with <a href="https://github.com/queelius/narro">narro</a>, my 80M parameter CPU model, generating Opus files that live next to the markdown source. One command to narrate an entire Hugo site.</p>
<p>That part was straightforward. The part that got interesting was highlighting: tracking which sentence is being spoken and lighting it up in the browser as the audio plays.</p>
<hr>
<h2 id="the-pipeline">The Pipeline</h2>
<p>Three commands:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">narro hugo install ~/mysite    <span class="c1"># copy player assets (JS, CSS, HTML partial)</span>
</span></span><span class="line"><span class="cl">narro hugo generate ~/mysite   <span class="c1"># narrate all posts with tts: true</span>
</span></span><span class="line"><span class="cl">narro hugo status ~/mysite     <span class="c1"># show what&#39;s been narrated and what hasn&#39;t</span>
</span></span></code></pre></div><p><code>generate</code> walks the content directory, finds posts with <code>tts: true</code> in frontmatter, extracts the prose (stripping code blocks, math, shortcodes, frontmatter), runs it through narro, converts to Opus via ffmpeg, and drops <code>narration.opus</code> and <code>narration.json</code> next to the <code>index.md</code>. The JSON file contains sentence-level timestamps. The HTML partial picks them up and wires everything together.</p>
<p>The player is a vanilla JS widget. No dependencies. Play, pause, spacebar toggle. The active paragraph highlights as the audio plays.</p>
<h2 id="the-alignment-problem">The Alignment Problem</h2>
<p>Timestamps are the interesting part. Narro uses a causal language model (Qwen-based, 80M params) to generate hidden states, then a Vocos decoder converts those to audio. There is no explicit alignment signal in this architecture.</p>
<p>My first attempt tried word-level timestamps using attention weights. The idea: each generated audio token attends to input text tokens, so you can compute a center-of-mass over the attention distribution to estimate when each word is spoken. This works beautifully for encoder-decoder models with cross-attention. It does not work for causal LM self-attention.</p>
<p>Causal self-attention attends to everything that came before. The distributions are diffuse. Every word&rsquo;s center-of-mass lands somewhere in the middle of the sequence, producing overlapping time ranges that are useless for highlighting.</p>
<h2 id="sentence-level-timestamps">Sentence-Level Timestamps</h2>
<p>The fix was to stop trying to be precise about words and use the precision the model actually gives you.</p>
<p>Narro processes each sentence independently. The number of generated tokens per sentence is known exactly. Multiply by the token duration (2048 samples / 32kHz = 64ms per token) and you get exact sentence timing. No approximation, no heuristics. The timestamps are ground truth.</p>
<p>The alignment JSON is one entry per sentence:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="p">[</span>
</span></span><span class="line"><span class="cl">  <span class="p">{</span><span class="nt">&#34;text&#34;</span><span class="p">:</span> <span class="s2">&#34;Hello world.&#34;</span><span class="p">,</span> <span class="nt">&#34;start&#34;</span><span class="p">:</span> <span class="mf">0.0</span><span class="p">,</span> <span class="nt">&#34;end&#34;</span><span class="p">:</span> <span class="mf">0.64</span><span class="p">},</span>
</span></span><span class="line"><span class="cl">  <span class="p">{</span><span class="nt">&#34;text&#34;</span><span class="p">:</span> <span class="s2">&#34;Goodbye moon.&#34;</span><span class="p">,</span> <span class="nt">&#34;start&#34;</span><span class="p">:</span> <span class="mf">0.64</span><span class="p">,</span> <span class="nt">&#34;end&#34;</span><span class="p">:</span> <span class="mf">1.6</span><span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">]</span>
</span></span></code></pre></div><p>I initially tried distributing each sentence&rsquo;s duration across its words proportionally by character count. It worked, but the DOM manipulation was fragile. Wrapping every word in a <code>&lt;span&gt;</code>, walking the tree to skip headings and code blocks, matching word indices sequentially to alignment entries. If any word got missed or doubled, every subsequent highlight drifted.</p>
<p>Sentence-level is better. The JS player matches sentences to existing <code>&lt;p&gt;</code> elements by substring containment. The DOM stays untouched. The highlighting is a CSS class toggled on the paragraph. Simpler code, no drift, and the visual result is actually clearer to read.</p>
<h2 id="the-browser-side">The Browser Side</h2>
<p>The JS player runs a binary search on the alignment data at 60fps via <code>requestAnimationFrame</code> and toggles a CSS class on the active paragraph. That is essentially the whole thing. No DOM rewriting, no word wrapping, no skip-tag lists.</p>
<h2 id="what-it-sounds-like">What It Sounds Like</h2>
<p>Every post on this blog with <code>tts: true</code> in frontmatter has a player at the top. Hit play. The active paragraph highlights as it is spoken. The model runs on CPU in about 20x real-time.</p>
<p>The source is at <a href="https://github.com/queelius/narro">github.com/queelius/narro</a>.</p>]]></content:encoded><category>narro</category><category>hugo</category><category>tts</category><category>open-source</category><category>python</category></item><item><title>Chartfold: Owning Your Medical Records</title><link>https://metafunctor.com/post/2026-02-24-chartfold-owning-your-medical-records/</link><pubDate>Tue, 24 Feb 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2026-02-24-chartfold-owning-your-medical-records/</guid><description>A walkthrough of Chartfold, a Python tool that loads your medical records into SQLite and exposes them to Claude via MCP for structured analysis, visit prep, and ad-hoc queries.</description><content:encoded><![CDATA[<p>I have cancer. My oncologist is at one hospital system (Siteman/BJC), my primary care doctor at another, and my earlier treatment history lives at a third (Anderson, where my first oncologist practiced). Patient portals are fine for browsing, but they don&rsquo;t answer questions. They show you your data one lab result at a time, one note at a time, one visit at a time.</p>
<p>I wanted to run queries against my medical records. Correlate lab trends with treatment changes. Generate structured question lists before oncology visits. Ask &ldquo;what changed since my last appointment&rdquo; and get a real answer. That means getting the data out of the portal and into something programmable.</p>
<p>Chartfold loads EHR exports into SQLite and exposes them to Claude via MCP.</p>
<hr>
<h2 id="the-problem">The Problem</h2>
<p>In the US, patients can export their medical records. HIPAA and the 21st Century Cures Act guarantee this. What you get depends on the system: Epic MyChart gives you CDA XML files, MEDITECH Expanse gives you FHIR JSON mixed with CCDA XML, athenahealth gives you FHIR R4 Bundles. Different formats, same clinical concepts.</p>
<p>If your hospitals use different EHR systems, none of them have the complete picture. Chartfold merges the exports into one database. But even if you&rsquo;re at a single hospital, the export format is not something you can work with directly. A directory of CDA XML files is not a database. You can&rsquo;t query it, chart it, or hand it to an LLM.</p>
<p>The point of Chartfold is to turn whatever your hospital gives you into a SQLite database, then make that database useful.</p>
<h2 id="what-it-does">What It Does</h2>
<p>Chartfold is a Python CLI. You point it at an EHR export directory, it parses the XML/FHIR, normalizes everything into a common data model (16 tables, ISO dates, deduplicated), and loads it into SQLite. Then you can query it directly, export it as a self-contained HTML dashboard, or connect Claude to it via MCP.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Load data from your hospital exports</span>
</span></span><span class="line"><span class="cl">chartfold load epic ~/exports/epic/
</span></span><span class="line"><span class="cl">chartfold load meditech ~/exports/meditech/
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Query directly</span>
</span></span><span class="line"><span class="cl">chartfold query <span class="s2">&#34;SELECT test_name, value, result_date FROM lab_results
</span></span></span><span class="line"><span class="cl"><span class="s2">                 WHERE test_name LIKE &#39;%CEA%&#39; ORDER BY result_date DESC&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Export a self-contained HTML file</span>
</span></span><span class="line"><span class="cl">chartfold <span class="nb">export</span> html --output chartfold.html
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Start the MCP server for Claude</span>
</span></span><span class="line"><span class="cl">chartfold serve-mcp
</span></span></code></pre></div><hr>
<h2 id="the-claude-integration">The Claude Integration</h2>
<p>This is why Chartfold exists for me.</p>
<p>The MCP server exposes the database to Claude Code. Setup is one file. Drop a <code>.mcp.json</code> in any directory where you run Claude Code:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;mcpServers&#34;</span><span class="p">:</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&#34;chartfold&#34;</span><span class="p">:</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">      <span class="nt">&#34;command&#34;</span><span class="p">:</span> <span class="s2">&#34;python&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">      <span class="nt">&#34;args&#34;</span><span class="p">:</span> <span class="p">[</span><span class="s2">&#34;-m&#34;</span><span class="p">,</span> <span class="s2">&#34;chartfold&#34;</span><span class="p">,</span> <span class="s2">&#34;serve-mcp&#34;</span><span class="p">,</span> <span class="s2">&#34;--db&#34;</span><span class="p">,</span> <span class="s2">&#34;/path/to/chartfold.db&#34;</span><span class="p">]</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">  <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>That&rsquo;s it. Claude now has read access to your entire medical history via SQL, plus tools for saving notes and structured analyses. I keep my database in a private directory and my <code>.mcp.json</code> pointing at it. Open Claude Code, and I&rsquo;m talking to my records.</p>
<p>The kinds of things I actually use it for:</p>
<blockquote>
<p>&ldquo;What&rsquo;s changed since my last oncology visit on January 15?&rdquo;</p>
</blockquote>
<p>Claude writes SQL, reads the results, and gives me a structured diff: new lab results, new imaging, changed medications, new clinical notes.</p>
<blockquote>
<p>&ldquo;Generate a prioritized question list for my appointment with Dr. Tan tomorrow.&rdquo;</p>
</blockquote>
<p>Claude reads my recent labs, imaging reports, pathology, and genomic results, then produces a tiered document organized by clinical urgency.</p>
<blockquote>
<p>&ldquo;Show me my CEA trend and flag any inflection points.&rdquo;</p>
</blockquote>
<p>Claude queries the lab_results table, filters by test name, and walks through the time series.</p>
<p>The analyses get saved back to the database (via dedicated MCP tools) and appear in the HTML export as tagged, searchable documents.</p>
<p><img src="analysis-cards.png" alt="Analysis section showing Claude-generated documents: prioritized questions for an oncologist visit, molecular profile summary, and deep clinical analysis"></p>
<p>Here&rsquo;s what one looks like expanded: a structured question list for an oncology appointment, organized by urgency tier, referencing specific test results and treatment options.</p>
<p><img src="analysis.png" alt="Full analysis document showing tiered questions for Dr. Tan, covering Guardant tissue results, liquid biopsy timing, BRCA1 methylation, and treatment planning"></p>
<p>I use this before every oncology visit. When you have 1776 lab results, 53 imaging reports, and 9 pathology reports, you need something to synthesize them. That&rsquo;s what Claude does well, but it needs structured data to work with. Chartfold provides the structured data. Claude provides the reasoning.</p>
<p>The MCP server exposes 25 tools. Here&rsquo;s the full list:</p>
<table>
	<thead>
			<tr>
					<th>Tool</th>
					<th>What it does</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>run_sql</code></td>
					<td>Execute arbitrary read-only SQL against the database</td>
			</tr>
			<tr>
					<td><code>get_schema</code></td>
					<td>Get CREATE TABLE DDL for query planning</td>
			</tr>
			<tr>
					<td><code>get_database_summary</code></td>
					<td>Table counts and load history (start here)</td>
			</tr>
			<tr>
					<td><code>query_labs</code></td>
					<td>Lab results filtered by test name, date, source, LOINC</td>
			</tr>
			<tr>
					<td><code>get_lab_series_tool</code></td>
					<td>Cross-source time series for a specific test</td>
			</tr>
			<tr>
					<td><code>get_available_tests_tool</code></td>
					<td>All lab tests with frequency and date range</td>
			</tr>
			<tr>
					<td><code>get_abnormal_labs_tool</code></td>
					<td>All flagged-abnormal results</td>
			</tr>
			<tr>
					<td><code>get_medications</code></td>
					<td>Medication list, optionally filtered by status</td>
			</tr>
			<tr>
					<td><code>reconcile_medications_tool</code></td>
					<td>Cross-source medication reconciliation</td>
			</tr>
			<tr>
					<td><code>get_timeline</code></td>
					<td>Unified event timeline (encounters, procedures, imaging, labs, pathology)</td>
			</tr>
			<tr>
					<td><code>search_notes</code></td>
					<td>Full-text search across clinical notes</td>
			</tr>
			<tr>
					<td><code>get_pathology_report</code></td>
					<td>Retrieve a pathology report by ID</td>
			</tr>
			<tr>
					<td><code>get_visit_diff</code></td>
					<td>Everything new since a given date</td>
			</tr>
			<tr>
					<td><code>get_visit_prep</code></td>
					<td>Pre-appointment summary bundle</td>
			</tr>
			<tr>
					<td><code>get_surgical_timeline</code></td>
					<td>Procedures linked to pathology, imaging, and meds</td>
			</tr>
			<tr>
					<td><code>match_cross_source_encounters</code></td>
					<td>Same-day encounters across different EHR systems</td>
			</tr>
			<tr>
					<td><code>get_data_quality_report</code></td>
					<td>Duplicate detection and source coverage matrix</td>
			</tr>
			<tr>
					<td><code>get_source_files</code></td>
					<td>Find PDFs and images linked to clinical records</td>
			</tr>
			<tr>
					<td><code>get_asset_summary</code></td>
					<td>Source asset counts by type and source</td>
			</tr>
			<tr>
					<td><code>save_note</code> / <code>get_note</code> / <code>search_notes_personal</code> / <code>delete_note</code></td>
					<td>Personal notes (CRUD)</td>
			</tr>
			<tr>
					<td><code>save_analysis</code> / <code>get_analysis</code> / <code>search_analyses</code> / <code>list_analyses</code> / <code>delete_analysis</code></td>
					<td>Structured analyses (CRUD)</td>
			</tr>
	</tbody>
</table>
<p>Clinical data is read-only (the SQLite connection opens in <code>?mode=ro</code>, enforced at the engine level). Claude can&rsquo;t modify your clinical records, only read them and save its own notes and analyses.</p>
<p>Most of these tools exist so Claude doesn&rsquo;t have to write SQL for common tasks. But <code>run_sql</code> is the escape hatch: anything the specialized tools don&rsquo;t cover, Claude can query directly.</p>
<hr>
<h2 id="the-html-dashboard">The HTML Dashboard</h2>
<p>The HTML export embeds the entire SQLite database using sql.js (SQLite compiled to WebAssembly). Open the file in a browser and you get an interactive dashboard. Everything runs client-side. No server, no cloud, no account. The file never phones home.</p>
<p><img src="overview.png" alt="Dashboard overview showing record counts: 1776 lab results, 93 medications, 51 conditions, 99 encounters, 53 imaging reports, and more"></p>
<h3 id="lab-charts">Lab Charts</h3>
<p>Lab charts show time-series data across sources with reference ranges. Here, creatinine and albumin are tracked over four years across MEDITECH (blue) and Epic (orange).</p>
<p><img src="lab-charts.png" alt="Cross-source lab charts for Creatinine and Albumin, showing data series from two hospital systems with reference range bands"></p>
<p>For patients who do deal with fragmented records across incompatible systems, this is the view that doesn&rsquo;t exist in any single portal. The charts are configurable via a TOML file, which you can auto-generate from your data:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">chartfold init-config
</span></span></code></pre></div><h3 id="conditions">Conditions</h3>
<p>Conditions with ICD-10 codes, onset dates, and source provenance.</p>
<p><img src="conditions.png" alt="Conditions table showing 51 conditions with status, ICD-10 codes, onset dates, and source system"></p>
<h3 id="medications">Medications</h3>
<p>Medications show &ldquo;Multi-source&rdquo; badges when the same drug appears in multiple systems.</p>
<p><img src="medications.png" alt="Active medications list with multi-source badges"></p>
<h3 id="imaging">Imaging</h3>
<p>Imaging reports with the full narrative findings. Useful for visit prep: search for a specific study, read the impression, bring context.</p>
<p><img src="imaging.png" alt="Imaging section showing CT and MRI reports with narrative findings"></p>
<h3 id="source-documents">Source Documents</h3>
<p>Source PDFs and scanned documents grouped by date.</p>
<p><img src="sources.png" alt="Source documents grouped by date, showing PDF assets from EHR exports"></p>
<h3 id="sql-console">SQL Console</h3>
<p>For anything the UI doesn&rsquo;t cover, there&rsquo;s a SQL console. Every table, every column, every index.</p>
<p><img src="sql-console.png" alt="SQL Console showing the database schema"></p>
<h3 id="dark-mode">Dark Mode</h3>
<p><img src="dark-mode.png" alt="Dark mode overview"></p>
<hr>
<h2 id="architecture">Architecture</h2>
<p>Three-stage pipeline, each stage independently testable:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">Raw EHR files (CDA XML, FHIR JSON, CCDA XML)
</span></span><span class="line"><span class="cl">    |
</span></span><span class="line"><span class="cl">    v
</span></span><span class="line"><span class="cl">[Source Parser]  -- format-specific extraction
</span></span><span class="line"><span class="cl">    |
</span></span><span class="line"><span class="cl">    v
</span></span><span class="line"><span class="cl">[Adapter]        -- normalize to UnifiedRecords (16 dataclass types)
</span></span><span class="line"><span class="cl">    |
</span></span><span class="line"><span class="cl">    v
</span></span><span class="line"><span class="cl">[DB Loader]      -- idempotent upsert into SQLite
</span></span></code></pre></div><p>Source parsers handle the XML/FHIR parsing. Adapters normalize dates to ISO 8601, parse numeric values, deduplicate, and map into a common data model. The DB loader uses upsert with natural keys, so re-running a load is safe.</p>
<p>After loading, the CLI prints a stage comparison table: parser count, adapter count, DB count. If the numbers don&rsquo;t match, you know where data was lost.</p>
<p>Currently supports Epic (CDA), MEDITECH (FHIR + CCDA), and athenahealth (FHIR R4). These importers were written against my own exports. I can&rsquo;t guarantee they&rsquo;ll work for yours. EHR exports vary by site, software version, and configuration. The pipeline is designed as a plugin system for exactly this reason: adding a new source means writing a parser, an adapter, and wiring them into the CLI. The CLAUDE.md has a recipe, and Claude can write a new importer from a sample export in about an hour.</p>
<hr>
<h2 id="export-formats">Export Formats</h2>
<ul>
<li><strong>HTML SPA</strong>: self-contained single file with embedded SQLite, Chart.js, and sql.js. No external dependencies. Copy it to a USB drive.</li>
<li><strong>Markdown</strong>: visit-focused summary with configurable lookback, optional PDF via pandoc.</li>
<li><strong>JSON</strong>: full-fidelity round-trip format. Export, then import to a new database with identical record counts.</li>
<li><strong>Hugo site</strong>: static site with detail pages and cross-linked records.</li>
<li><strong>Arkiv</strong>: universal record format (JSONL + manifest) for long-term archival.</li>
</ul>
<p>The HTML export is a single file. No server, no backend, no account. You can host it on a static site (I host mine on GitHub Pages), email it to a family member, or hand it to a doctor on a USB drive. Because it&rsquo;s just a file, you can protect it with <a href="/post/2026-02-13-pagevault/">PageVault</a> to add password-based encryption before sharing. The recipient opens the file, enters the password, and gets the full interactive dashboard. No server involved at any step.</p>
<p>Medical records should not depend on someone else&rsquo;s infrastructure. A single HTML file with an embedded database and WebAssembly runtime is about as durable as digital data gets.</p>
<hr>
<h2 id="getting-started">Getting Started</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">pip install chartfold
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Load your exports</span>
</span></span><span class="line"><span class="cl">chartfold load auto ~/path/to/export/
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Query</span>
</span></span><span class="line"><span class="cl">chartfold query <span class="s2">&#34;SELECT test_name, value, result_date FROM lab_results LIMIT 10&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Export</span>
</span></span><span class="line"><span class="cl">chartfold <span class="nb">export</span> html --output my-records.html
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Connect Claude</span>
</span></span><span class="line"><span class="cl">chartfold serve-mcp
</span></span></code></pre></div><p>The code is on GitHub: <a href="https://github.com/queelius/chartfold">queelius/chartfold</a>. Python 3.11+, depends on lxml and not much else.</p>
<hr>
<p><em>Chartfold started because I wanted to ask questions about my own medical records and couldn&rsquo;t. Now I can.</em></p>]]></content:encoded><category>chartfold</category><category>python</category><category>ehr</category><category>sqlite</category><category>health-data</category><category>open-source</category><category>mcp</category><category>claude</category></item><item><title>Code Without Purpose</title><link>https://metafunctor.com/post/2026-02-25-code-without-purpose/</link><pubDate>Tue, 24 Feb 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2026-02-25-code-without-purpose/</guid><description>The problem isn't too much code. It's code without purpose.</description><content:encoded><![CDATA[<p>Time is finite in ways I can&rsquo;t ignore. That changes which questions about code feel important.</p>
<p>I read a post arguing that the most valuable programming skill in 2026 is deleting code. The thesis: AI generates code faster than anyone can review it, so the real value is in curation and subtraction. Code is a liability, not an asset.</p>
<p>I agree with the observation. I disagree with the prescription.</p>
<hr>
<h2 id="the-thesis">The Thesis</h2>
<p>The argument is straightforward. AI tools can produce entire modules in the time it takes to write a spec. Codebases are accumulating features nobody asked for, abstractions nobody needs, and boilerplate that exists because the model defaulted to verbosity. Teams that used to struggle to ship enough code now struggle with too much of it. In this world, the programmer&rsquo;s role shifts from writer to editor. The most valuable activity becomes knowing what to cut.</p>
<p>I&rsquo;ve seen this. Projects accumulate code the way attics accumulate boxes. Nobody remembers why half of it is there. It sits untouched for months, adding to cognitive load, making every change harder to reason about. When you finally clear it out, nothing breaks and the rest becomes legible again. Code rot is real. AI accelerates it. The instinct to subtract is correct.</p>
<p>But subtraction is symptom treatment. The underlying problem isn&rsquo;t volume. It&rsquo;s that most code doesn&rsquo;t know why it exists. It was written (or generated) to solve a local problem, it solved it or half-solved it, and then it sat there, disconnected from any larger purpose. Code without purpose is what bloats. Deletion is the right instinct pointed at the wrong layer. The cause isn&rsquo;t too much code. It&rsquo;s too little intent.</p>
<h2 id="what-i-found-instead">What I Found Instead</h2>
<p>I started building tools two years ago because I needed my personal data to survive even if I wasn&rsquo;t around to maintain it. That&rsquo;s the whole constraint. Whatever I build has to work without me.</p>
<p>The first tool was a conversation archiver. I had years of AI conversations across ChatGPT, Claude, and Copilot trapped in platforms that might not exist next decade. I needed them in formats that degrade gracefully. SQLite for structured queries. JSONL for streaming and interchange. Markdown for human reading. If the tool disappears, the data is still a file you can open with anything. If SQLite disappears, the JSONL is still searchable with grep. If everything disappears, the Markdown is still readable in a text editor. Each layer works without the one above it.</p>
<p>Then I needed the same thing for bookmarks. Then ebooks. Then photos, email, medical records, notes. Each tool exists because the previous one exposed a gap. The medical records needed secure sharing without a server. The whole collection needed a dead man&rsquo;s switch. The archive eventually needed something stranger: a way to answer questions after I couldn&rsquo;t.</p>
<p>None of this was planned as an ecosystem. I built the next thing I needed, and the next, and the next. At some point I looked back and realized they all pointed the same direction. This is a life project. Everything serves one purpose.</p>
<h2 id="the-stack">The Stack</h2>
<p>One purpose: durable personal data that outlasts its creator.</p>
<p><strong>Philosophy</strong></p>
<ul>
<li><a href="https://github.com/queelius/longecho">longecho</a>: The Long Echo specification. Self-describing data, durable formats, graceful degradation.</li>
</ul>
<p><strong>Universal Format</strong></p>
<ul>
<li><a href="https://github.com/queelius/arkiv">arkiv</a>: Universal personal data format. JSONL in, SQL out, SQL back to JSONL. Its MCP server can host any collection intelligently, regardless of domain. One format, one database, one query interface.</li>
</ul>
<p><strong>Source Toolkits</strong></p>
<ul>
<li><a href="https://github.com/queelius/memex">memex</a> / <a href="https://github.com/queelius/ctk">ctk</a>: AI conversations. Import, query, continue in the browser, export durable archives.</li>
<li>A family of domain toolkits for bookmarks, ebooks, photos, email, and notes. Different domains, identical architecture.</li>
<li><a href="https://github.com/queelius/chartfold">chartfold</a>: Medical records from three hospital systems, consolidated into one queryable database.</li>
</ul>
<p><strong>Infrastructure</strong></p>
<ul>
<li><a href="https://github.com/queelius/pagevault">pagevault</a>: Client-side encryption for any HTML file. No server.</li>
<li><a href="https://github.com/queelius/posthumous">posthumous</a>: Federated dead man&rsquo;s switch.</li>
<li><a href="https://github.com/queelius/repoindex">repoindex</a>: Index and query across ~120 git repos.</li>
<li>A collection of Claude Code plugins: MCP servers, agents, and skills that wire everything into my daily workflow.</li>
</ul>
<p><strong>The Endgame</strong></p>
<ul>
<li><a href="https://github.com/queelius/eidola">eidola</a>: A conversable persona assembled from all of the above. Its first form is a Claude Code plugin backed by the combined archive. Not resurrection. An echo.</li>
</ul>
<h2 id="the-pattern">The Pattern</h2>
<p>Every tool follows the same architecture. SQLite for storage. CLI for local use. MCP server for Claude, or the CLI wrapped in a light Claude Code plugin. Export to self-contained HTML you can host anywhere or open from a file. Export to longecho-compliant archives that work without the tool. The data always outlasts the software.</p>
<p>Take memex. Import your AI conversations, query them with SQL, talk to Claude about them via MCP, or export a single HTML file where you can browse and continue conversations in the browser. Download the SQLite from that same page, and you&rsquo;re back to durable local data. The cycle closes. This is how most of the tools work.</p>
<p>Arkiv sits in the middle. The source toolkits produce JSONL. Arkiv imports it to SQLite. Arkiv exports it back to JSONL. Its MCP server can expose any collection to Claude, regardless of what domain it came from. The data flows in a circle, always in formats that describe themselves.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-gdscript3" data-lang="gdscript3"><span class="line"><span class="cl"><span class="n">Source</span> <span class="n">toolkits</span> <span class="err">→</span> <span class="n">arkiv</span> <span class="err">→</span> <span class="n">longecho</span>
</span></span><span class="line"><span class="cl">                              <span class="err">↓</span>
</span></span><span class="line"><span class="cl">                  <span class="n">pagevault</span> <span class="p">(</span><span class="n">encrypt</span><span class="p">)</span> <span class="o">+</span> <span class="n">posthumous</span> <span class="p">(</span><span class="n">deliver</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">                              <span class="err">↓</span>
</span></span><span class="line"><span class="cl">                         <span class="n">eidola</span> <span class="p">(</span><span class="n">echo</span><span class="p">)</span>
</span></span></code></pre></div><p>I have never once needed to delete one of these tools. Not because I&rsquo;m a better programmer than anyone else. Because each one exists for a specific reason that connects to the others. When code has purpose, dead weight doesn&rsquo;t accumulate.</p>
<p>This isn&rsquo;t architectural foresight. It&rsquo;s what happens when you build from a clear constraint. &ldquo;My data has to survive without me&rdquo; is a filter that works at the design stage. Every tool either serves that constraint or it doesn&rsquo;t get built. There is no third category.</p>
<h2 id="the-actual-skill">The Actual Skill</h2>
<p>The most valuable skill isn&rsquo;t deleting code or writing it. It&rsquo;s knowing why you&rsquo;re building.</p>
<p>If you know the purpose, code stays minimal because unnecessary code doesn&rsquo;t serve it. You don&rsquo;t need periodic purges. The purpose does the culling before anything gets written. Deletion is what happens when purpose was absent from the start. It&rsquo;s retrospective correction for a problem that clear intent would have prevented.</p>
<p>I know what I&rsquo;m building toward. The tools will echo after I stop maintaining them. That was the point.</p>]]></content:encoded><category>longecho</category><category>open-source</category><category>software-philosophy</category><category>legacy</category></item><item><title>dapple: Terminal Graphics, Composed</title><link>https://metafunctor.com/post/2026-02-15-dapple-composition-layout/</link><pubDate>Sun, 15 Feb 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2026-02-15-dapple-composition-layout/</guid><description>dapple is a terminal graphics library with one Canvas API, seven pluggable renderers, and eleven CLI tools for displaying images, data, video, math, and more.</description><content:encoded><![CDATA[<p>I live in the terminal. Most of my tools are CLIs. When I want to see something visual (an image, a plot, a table of results), I do not want to leave the terminal to see it.</p>
<p>Terminal graphics tools exist, but they are fragmented. One library does braille characters. Another does quadrant blocks. A third handles sixel. Each has its own API, its own conventions, its own way of thinking about the same problem.</p>
<p><a href="https://github.com/queelius/dapple">dapple</a> unifies them. One Canvas class, seven pluggable renderers, and eleven CLI tools built on top. The core depends only on numpy.</p>
<h2 id="the-idea">The Idea</h2>
<p>The insight is that &ldquo;render a bitmap to the terminal&rdquo; is a single problem with multiple encodings. Braille characters pack 2x4 dots per cell. Quadrant blocks give you 2x2 with color. Sextants give 2x3. Sixel and kitty give true pixels if your terminal supports them. These are all the same operation: map a grid of values to a grid of characters.</p>
<p>So dapple makes the renderer a parameter, not an architecture decision:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">dapple</span> <span class="kn">import</span> <span class="n">Canvas</span><span class="p">,</span> <span class="n">braille</span><span class="p">,</span> <span class="n">quadrants</span><span class="p">,</span> <span class="n">sextants</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">dapple.adapters</span> <span class="kn">import</span> <span class="n">from_pil</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">PIL</span> <span class="kn">import</span> <span class="n">Image</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">canvas</span> <span class="o">=</span> <span class="n">from_pil</span><span class="p">(</span><span class="n">Image</span><span class="o">.</span><span class="n">open</span><span class="p">(</span><span class="s2">&#34;photo.jpg&#34;</span><span class="p">),</span> <span class="n">width</span><span class="o">=</span><span class="mi">80</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">canvas</span><span class="o">.</span><span class="n">out</span><span class="p">(</span><span class="n">braille</span><span class="p">)</span>      <span class="c1"># Unicode braille (2x4 dots per cell)</span>
</span></span><span class="line"><span class="cl"><span class="n">canvas</span><span class="o">.</span><span class="n">out</span><span class="p">(</span><span class="n">quadrants</span><span class="p">)</span>    <span class="c1"># block characters with ANSI color</span>
</span></span><span class="line"><span class="cl"><span class="n">canvas</span><span class="o">.</span><span class="n">out</span><span class="p">(</span><span class="n">sextants</span><span class="p">)</span>     <span class="c1"># higher vertical resolution</span>
</span></span></code></pre></div><p>Load once, render anywhere. The renderers are frozen dataclasses. <code>braille(threshold=0.3)</code> returns a new renderer with different settings; nothing mutates. They write directly to a <code>TextIO</code> stream, never building the full output as an intermediate string.</p>
<h2 id="renderers">Renderers</h2>
<table>
	<thead>
			<tr>
					<th>Renderer</th>
					<th>Cell Size</th>
					<th>Colors</th>
					<th>Best For</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>braille</code></td>
					<td>2x4</td>
					<td>mono/gray/true</td>
					<td>Structure, edges, piping, accessibility</td>
			</tr>
			<tr>
					<td><code>quadrants</code></td>
					<td>2x2</td>
					<td>ANSI 256/true</td>
					<td>Photos, balanced resolution and color</td>
			</tr>
			<tr>
					<td><code>sextants</code></td>
					<td>2x3</td>
					<td>ANSI 256/true</td>
					<td>Higher vertical resolution</td>
			</tr>
			<tr>
					<td><code>ascii</code></td>
					<td>1x2</td>
					<td>none</td>
					<td>Universal compatibility</td>
			</tr>
			<tr>
					<td><code>sixel</code></td>
					<td>1x1</td>
					<td>palette</td>
					<td>True pixels (xterm, mlterm, foot)</td>
			</tr>
			<tr>
					<td><code>kitty</code></td>
					<td>1x1</td>
					<td>true</td>
					<td>True pixels (kitty, wezterm)</td>
			</tr>
			<tr>
					<td><code>fingerprint</code></td>
					<td>8x16</td>
					<td>none</td>
					<td>Artistic glyph matching</td>
			</tr>
	</tbody>
</table>
<p>In practice I use braille and sextants. They work everywhere. Kitty protocol broke things completely inside Claude Code (a TUI), and I have not tested sixel enough to trust it. Braille and sextants are the universal goto.</p>
<p>One honest limitation: Claude Code hides terminal output behind a Ctrl-O expand, so my carefully rendered graphics end up collapsed by default. I think recent hooks or tool-result handling might fix this, but I have not confirmed it yet.</p>
<h2 id="three-layers">Three Layers</h2>
<p>The architecture has strict boundaries:</p>
<ol>
<li><strong>Core</strong> (numpy only): Canvas, renderers, color handling, preprocessing, layout primitives (Frame, Grid).</li>
<li><strong>Adapters</strong> (optional deps): Bridge PIL, matplotlib, cairo, and ANSI art to Canvas.</li>
<li><strong>Extras</strong> (optional deps): The CLI tools. Each one is a separate install group.</li>
</ol>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">pip install dapple                 <span class="c1"># core only</span>
</span></span><span class="line"><span class="cl">pip install dapple<span class="o">[</span>imgcat<span class="o">]</span>         <span class="c1"># image viewer</span>
</span></span><span class="line"><span class="cl">pip install dapple<span class="o">[</span>all-tools<span class="o">]</span>      <span class="c1"># everything</span>
</span></span></code></pre></div><p>Core never imports PIL. Adapters never import extras. This matters because the core is tiny and fast, and the CLI tools pull in their own dependencies without bloating each other.</p>
<h2 id="the-cli-tools">The CLI Tools</h2>
<p>I built eleven tools, each owning a domain rather than a file format. &ldquo;Show me this data&rdquo; should not require knowing whether the file is JSON or CSV. &ldquo;Display these images&rdquo; should not require a different tool for one image versus twelve.</p>
<table>
	<thead>
			<tr>
					<th>Tool</th>
					<th>Domain</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>imgcat</td>
					<td>Images (single + grid)</td>
			</tr>
			<tr>
					<td>datcat</td>
					<td>Structured data (JSON/JSONL/CSV/TSV)</td>
			</tr>
			<tr>
					<td>vidcat</td>
					<td>Video (stacked frames, playback, asciinema export)</td>
			</tr>
			<tr>
					<td>mdcat</td>
					<td>Markdown</td>
			</tr>
			<tr>
					<td>htmlcat</td>
					<td>HTML</td>
			</tr>
			<tr>
					<td>pdfcat</td>
					<td>PDFs</td>
			</tr>
			<tr>
					<td>funcat</td>
					<td>Math and parametric plots</td>
			</tr>
			<tr>
					<td>ansicat</td>
					<td>ANSI art</td>
			</tr>
			<tr>
					<td>compcat</td>
					<td>Renderer comparison</td>
			</tr>
			<tr>
					<td>plotcat</td>
					<td>Faceted data plots</td>
			</tr>
			<tr>
					<td>dashcat</td>
					<td>YAML-driven dashboards</td>
			</tr>
	</tbody>
</table>
<p>A few worth explaining:</p>
<h3 id="datcat">datcat</h3>
<p>datcat handles JSON, JSONL, CSV, and TSV. Format detection is automatic. Internally everything becomes <code>list[dict]</code>. CSV rows become dicts on parse. One representation means the downstream code (table formatting, chart extraction, plotting) does not branch on input format.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">datcat records.json              <span class="c1"># JSON table</span>
</span></span><span class="line"><span class="cl">datcat events.jsonl --bar event  <span class="c1"># JSONL bar chart</span>
</span></span><span class="line"><span class="cl">datcat weather.csv --sort temp_c <span class="c1"># CSV sorted by column</span>
</span></span></code></pre></div><h3 id="imgcat">imgcat</h3>
<p>When imgcat receives multiple images, it switches to grid mode automatically. The layout uses dapple&rsquo;s Frame and Grid primitives, the same ones compcat and dashcat use.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">imgcat photo.jpg                     <span class="c1"># single image</span>
</span></span><span class="line"><span class="cl">imgcat photos/*.jpg --cols <span class="m">3</span>         <span class="c1"># 3-column contact sheet</span>
</span></span></code></pre></div><p>Preprocessing flags (<code>--contrast</code>, <code>--dither</code>, <code>--invert</code>) apply to every image in the grid.</p>
<h3 id="vidcat">vidcat</h3>
<p>The <code>--play</code> flag renders frames in-place using ANSI cursor movement. Instead of printing each frame below the last (which scrolls your terminal into oblivion), it overwrites the previous frame.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">vidcat video.mp4 --play              <span class="c1"># 10 fps default</span>
</span></span><span class="line"><span class="cl">vidcat video.mp4 --play --fps <span class="m">24</span>     <span class="c1"># faster</span>
</span></span></code></pre></div><p>The mechanism: render the first frame, count its output lines, then for each subsequent frame write <code>\033[{N}A\033[J</code> (cursor up N, clear to end) before rendering. Falls back to stacked output if stdout is not a TTY.</p>
<h3 id="htmlcat">htmlcat</h3>
<p>Converts HTML to markdown via <a href="https://github.com/matthewwithanm/python-markdownify">markdownify</a>, then renders through Rich using the same pipeline as mdcat. Good for documentation and articles. Not designed for CSS-heavy web apps.</p>
<h2 id="layout-and-charts">Layout and Charts</h2>
<p>Frame and Grid are layout primitives. Frame adds borders and titles around a canvas. Grid arranges canvases in rows and columns. These compose: a Grid of Framed canvases, a Frame around a Grid. dashcat uses this to build terminal dashboards from YAML config.</p>
<p>Two chart APIs:</p>
<ul>
<li><strong>Bitmap charts</strong> (<code>dapple.charts</code>): sparklines, line plots, bar charts, histograms, heatmaps. These return Canvas objects, composable with everything else.</li>
<li><strong>Text charts</strong> (<code>dapple.textchart</code>): text-mode bar charts and sparklines, returning ANSI strings. Used by datcat for quick inline visualization.</li>
</ul>
<h2 id="status">Status</h2>
<p>dapple is on <a href="https://pypi.org/project/dapple/">PyPI</a>. Docs at <a href="https://queelius.github.io/dapple/">queelius.github.io/dapple</a>.</p>
<p>The core and the CLI tools are stable. I use them daily. I have been experimenting with sextants (the 2x3 block characters give surprisingly good results) and tried generalizing the fingerprint renderer to match over a much larger set of Unicode glyphs. That did not work very well. The architecture is settled and the tools work.</p>]]></content:encoded><category>dapple</category><category>terminal-graphics</category><category>python</category><category>open-source</category><category>composition</category><category>layout</category></item><item><title>Posthumous: A Federated Dead Man's Switch</title><link>https://metafunctor.com/post/2026-02-14-posthumous/</link><pubDate>Sat, 14 Feb 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2026-02-14-posthumous/</guid><description>A walkthrough of Posthumous, a self-hosted dead man's switch that monitors periodic check-ins via TOTP, progresses through escalating alert stages, and triggers automated actions if you stop responding.</description><content:encoded><![CDATA[<p>Some things should only happen after you can&rsquo;t do them yourself.</p>
<p>Posthumous is a self-hosted dead man&rsquo;s switch. You check in periodically (via phone, browser, CLI, or API call) and if you stop, it progresses through escalating stages before triggering automated actions: sending notifications, running scripts, whatever you&rsquo;ve configured.</p>
<p>I built it because the existing options are either cloud-hosted (you&rsquo;re trusting someone else&rsquo;s uptime for your most important automation) or single-node (one server failure and silence is indistinguishable from death). Posthumous is federated, multiple nodes watch each other, and fully self-hosted.</p>
<p>This post walks through the basic workflows.</p>
<hr>
<h2 id="setup">Setup</h2>
<p>Installation is a single pip command:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">pip install posthumous
</span></span></code></pre></div><p>Initialization generates a TOTP secret and creates the config directory:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">$ phm init --node-name cerebro
</span></span><span class="line"><span class="cl">Generated new TOTP secret.
</span></span><span class="line"><span class="cl">Config created at ~/.posthumous/config.yaml
</span></span><span class="line"><span class="cl">Example script created at ~/.posthumous/scripts/example.sh
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="o">==================================================</span>
</span></span><span class="line"><span class="cl">TOTP Setup - Scan with your authenticator app:
</span></span><span class="line"><span class="cl"><span class="o">==================================================</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="o">[</span>QR code appears here<span class="o">]</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">Manual entry URI: otpauth://totp/Posthumous:cerebro?secret<span class="o">=</span>...<span class="p">&amp;</span><span class="nv">issuer</span><span class="o">=</span>Posthumous
</span></span><span class="line"><span class="cl">Secret: <span class="nv">JBSWY3DPEHPK3PXP</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="o">==================================================</span>
</span></span><span class="line"><span class="cl">IMPORTANT: Save this secret securely!
</span></span><span class="line"><span class="cl"><span class="o">==================================================</span>
</span></span></code></pre></div><p>You scan the QR code with any authenticator app (Google Authenticator, Authy, 1Password, whatever generates TOTP codes). That code is how you prove you&rsquo;re alive.</p>
<p>The config file (<code>~/.posthumous/config.yaml</code>) controls timing, notifications, and actions:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">node_name</span><span class="p">:</span><span class="w"> </span><span class="l">cerebro</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">secret_key</span><span class="p">:</span><span class="w"> </span><span class="l">JBSWY3DPEHPK3PXP</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">listen</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;0.0.0.0:8420&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">base_url</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;https://posthumous.example.com:8420&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">checkin_interval</span><span class="p">:</span><span class="w"> </span><span class="m">7</span><span class="w"> </span><span class="l">days</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">warning_start</span><span class="p">:</span><span class="w"> </span><span class="m">8</span><span class="w"> </span><span class="l">days</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">grace_start</span><span class="p">:</span><span class="w"> </span><span class="m">12</span><span class="w"> </span><span class="l">days</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">trigger_at</span><span class="p">:</span><span class="w"> </span><span class="m">14</span><span class="w"> </span><span class="l">days</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">notifications</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">default</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="l">ntfy://my-posthumous-channel</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">actions</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">on_warning</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">notify</span><span class="p">:</span><span class="w"> </span><span class="l">default</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">message</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Check-in needed. {days_left} days remaining.\n\nCheck in: {checkin_url}&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">on_grace</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">notify</span><span class="p">:</span><span class="w"> </span><span class="l">default</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">message</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;URGENT: Posthumous triggers in {hours_left} hours.\n\nCheck in: {checkin_url}&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">on_trigger</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">notify</span><span class="p">:</span><span class="w"> </span><span class="l">default</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">      </span><span class="nt">message</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Posthumous has activated.\n\nDashboard: {dashboard_url}&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="nt">script</span><span class="p">:</span><span class="w"> </span><span class="l">scripts/release-credentials.sh</span><span class="w">
</span></span></span></code></pre></div><p>Notification messages support template variables like <code>{checkin_url}</code> and <code>{dashboard_url}</code>, so push notifications to your phone include a direct link back to the check-in page.</p>
<hr>
<h2 id="the-check-in-flow">The Check-in Flow</h2>
<p>Start the daemon:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">$ phm run
</span></span><span class="line"><span class="cl">Starting Posthumous node <span class="s1">&#39;cerebro&#39;</span>...
</span></span></code></pre></div><p>This launches the web server, watchdog timer, and scheduler. Navigate to the check-in page:</p>
<p><img src="01-checkin-armed.png" alt="The check-in page in ARMED state, showing a TOTP input field and the node name"></p>
<p>The UI is intentionally minimal. Dark theme, big input, works on a phone screen. Enter your 6-digit TOTP code and hit Check In.</p>
<p>After a successful check-in, the timer resets and you see the countdown:</p>
<p><img src="03-checkin-after.png" alt="Check-in page after successful authentication, showing time since last check-in and time until trigger"></p>
<p>The status line tells you exactly how long until the system would escalate.</p>
<p>You can also check in via the CLI or the JSON API:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># CLI check-in (prompts for TOTP)</span>
</span></span><span class="line"><span class="cl">$ phm checkin
</span></span><span class="line"><span class="cl">TOTP code: <span class="m">645557</span>
</span></span><span class="line"><span class="cl">Check-in accepted
</span></span><span class="line"><span class="cl">  Status: ARMED
</span></span><span class="line"><span class="cl">  Next deadline: 2026-02-28 11:41 UTC
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># API check-in (for automation)</span>
</span></span><span class="line"><span class="cl">$ curl -X POST http://localhost:8420/checkin <span class="se">\
</span></span></span><span class="line"><span class="cl">    -H <span class="s1">&#39;Content-Type: application/json&#39;</span> <span class="se">\
</span></span></span><span class="line"><span class="cl">    -d <span class="s1">&#39;{&#34;totp&#34;: &#34;645557&#34;}&#39;</span>
</span></span><span class="line"><span class="cl"><span class="o">{</span><span class="s2">&#34;success&#34;</span>: true, <span class="s2">&#34;status&#34;</span>: <span class="s2">&#34;armed&#34;</span>, <span class="s2">&#34;next_deadline&#34;</span>: <span class="s2">&#34;2026-02-28T11:41:43+00:00&#34;</span><span class="o">}</span>
</span></span></code></pre></div><hr>
<h2 id="the-dashboard">The Dashboard</h2>
<p>The dashboard shows everything at a glance: countdown timers, check-in history, peer status, and scheduled post-trigger actions:</p>
<p><img src="04-dashboard-armed.png" alt="Dashboard in ARMED state showing time remaining until each escalation stage, check-in history, peer status, and scheduled items"></p>
<p>The color coding matches the urgency: green for &ldquo;Until warning&rdquo; (you&rsquo;re fine), orange for &ldquo;Until grace&rdquo; (getting close), red for &ldquo;Until trigger&rdquo; (last chance). Auto-refreshes every 60 seconds.</p>
<hr>
<h2 id="the-state-machine">The State Machine</h2>
<p>Posthumous progresses through four states:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">ARMED ──timeout──&gt; WARNING ──timeout──&gt; GRACE ──timeout──&gt; TRIGGERED
</span></span><span class="line"><span class="cl">  ^                   |                   |                    |
</span></span><span class="line"><span class="cl">  └─── check-in ──────┴─── check-in ─────┘                    v
</span></span><span class="line"><span class="cl">                                                    (scheduler runs forever)
</span></span></code></pre></div><p>A check-in from any pre-trigger state resets to ARMED. <strong>TRIGGERED is terminal.</strong> Once activated, no check-in can undo it. This is by design: if the switch has fired, you want the actions to complete.</p>
<p>Each transition fires its configured actions. If the node was offline and missed intermediate states (say it was down during WARNING and comes back during GRACE), the watchdog fires all skipped callbacks in order before reaching the current state. No notifications are silently dropped.</p>
<p>When the system reaches TRIGGERED, the check-in page locks out:</p>
<p><img src="06-checkin-triggered.png" alt="Check-in page in TRIGGERED state, the TOTP form is replaced with a message explaining the node is triggered"></p>
<p>And the dashboard reflects the terminal state:</p>
<p><img src="05-dashboard-triggered.png" alt="Dashboard in TRIGGERED state showing all stages as Active/Activated with the trigger timestamp"></p>
<hr>
<h2 id="notifications">Notifications</h2>
<p>Posthumous uses <a href="https://github.com/caronc/apprise">Apprise</a> for notifications, which means it supports 100+ notification services out of the box: ntfy, Pushover, Telegram, Discord, email, Slack, and more.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">notifications</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">default</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="l">ntfy://my-posthumous-channel</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span><span class="nt">urgent</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="l">pover://user@token</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span>- <span class="l">tgram://bot_token/chat_id</span><span class="w">
</span></span></span></code></pre></div><p>Each escalation stage can target different channels with different messages. Warning might go to ntfy (a gentle ping), while grace and trigger go to Pushover and Telegram (hard to miss).</p>
<p>The <code>{checkin_url}</code> variable means warning notifications include a clickable link directly to the check-in page. Open the notification on your phone, tap the link, enter the TOTP code. Three taps and you&rsquo;re checked in.</p>
<hr>
<h2 id="federation">Federation</h2>
<p>A single node has a single point of failure. If the server goes down, silence looks the same as death, which means either false triggers or missed real triggers.</p>
<p>Posthumous solves this with federation. Multiple nodes share the same TOTP secret and communicate via HMAC-signed HTTP:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="c"># Node A&#39;s config</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="nt">peers</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span>- <span class="l">https://node-b.example.com:8420</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span>- <span class="l">https://node-c.example.com:8420</span><span class="w">
</span></span></span></code></pre></div><p>When you check in to any node, it broadcasts to all peers. When any node triggers, it broadcasts that too. The design bias is deliberate: <strong>duplicates over silence</strong>. Multiple nodes may fire the same notification (annoying but survivable). A missed trigger is not.</p>
<p>Each node tracks peer health independently, and the dashboard shows peer status with connection age and failure counts.</p>
<hr>
<h2 id="post-trigger-scheduling">Post-Trigger Scheduling</h2>
<p>Once triggered, Posthumous doesn&rsquo;t just fire-and-forget. A scheduler runs indefinitely, executing actions on configurable schedules using a small DSL:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">post_trigger</span><span class="p">:</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span>- <span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">weekly-reminder</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">when</span><span class="p">:</span><span class="w"> </span><span class="l">every week after trigger</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">notify</span><span class="p">:</span><span class="w"> </span><span class="l">default</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">message</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Posthumous was triggered {days_left} days ago.&#34;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span>- <span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">credential-release</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">when</span><span class="p">:</span><span class="w"> </span><span class="m">30</span><span class="w"> </span><span class="l">days after trigger</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">script</span><span class="p">:</span><span class="w"> </span><span class="l">scripts/release-credentials.sh</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">  </span>- <span class="nt">name</span><span class="p">:</span><span class="w"> </span><span class="l">annual-memorial</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">when</span><span class="p">:</span><span class="w"> </span><span class="l">every year on trigger anniversary</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">notify</span><span class="p">:</span><span class="w"> </span><span class="l">default</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="nt">message</span><span class="p">:</span><span class="w"> </span><span class="s2">&#34;Annual memorial notification.&#34;</span><span class="w">
</span></span></span></code></pre></div><p>The <code>when</code> expressions support relative timing (<code>30 days after trigger</code>), recurring patterns (<code>every week after trigger</code>), anniversaries (<code>every year on trigger anniversary</code>), and absolute dates. Each execution is deduplicated by period key. If a node restarts, it won&rsquo;t re-run actions for the current period.</p>
<hr>
<h2 id="security">Security</h2>
<p>Authentication uses TOTP (the same protocol as Google Authenticator). This means:</p>
<ul>
<li><strong>No passwords stored on the server</strong>, only the shared secret</li>
<li><strong>Time-based codes expire every 30 seconds</strong>, replay attacks have a narrow window</li>
<li><strong>Brute-force protection</strong>, after configurable failed attempts the account locks out for a configurable duration</li>
</ul>
<p>The check-in page locks out after too many failures, and the API returns HTTP 429.</p>
<p>Peer communication is authenticated with HMAC-SHA256 signatures derived from the shared secret. State files can optionally be encrypted at rest with Fernet (AES-128-CBC), enabled with a single config flag:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-yaml" data-lang="yaml"><span class="line"><span class="cl"><span class="nt">encrypt_at_rest</span><span class="p">:</span><span class="w"> </span><span class="kc">true</span><span class="w">
</span></span></span></code></pre></div><hr>
<h2 id="status-at-a-glance">Status at a Glance</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">$ phm status
</span></span><span class="line"><span class="cl">Node: cerebro
</span></span><span class="line"><span class="cl">Status: ARMED
</span></span><span class="line"><span class="cl">Last check-in: 2026-02-14 11:41 UTC
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">Warning in:  6d 23h
</span></span><span class="line"><span class="cl">Grace in:    10d 23h
</span></span><span class="line"><span class="cl">Trigger in:  12d 23h
</span></span></code></pre></div><p>Or hit the JSON API for monitoring integrations:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">$ curl -s http://localhost:8420/status <span class="p">|</span> python -m json.tool
</span></span><span class="line"><span class="cl"><span class="o">{</span>
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;node_name&#34;</span>: <span class="s2">&#34;cerebro&#34;</span>,
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;status&#34;</span>: <span class="s2">&#34;armed&#34;</span>,
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;last_checkin&#34;</span>: <span class="s2">&#34;2026-02-14T11:41:43+00:00&#34;</span>,
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;time_remaining&#34;</span>: <span class="o">{</span>
</span></span><span class="line"><span class="cl">        <span class="s2">&#34;until_warning&#34;</span>: 596503.0,
</span></span><span class="line"><span class="cl">        <span class="s2">&#34;until_grace&#34;</span>: 942103.0,
</span></span><span class="line"><span class="cl">        <span class="s2">&#34;until_trigger&#34;</span>: 1114903.0
</span></span><span class="line"><span class="cl">    <span class="o">}</span>
</span></span><span class="line"><span class="cl"><span class="o">}</span>
</span></span></code></pre></div><hr>
<h2 id="whats-next">What&rsquo;s Next</h2>
<p>Posthumous is at v0.5. The core workflows are solid: check-in, state machine, notifications, federation, scheduling, encryption at rest. Some things I&rsquo;m considering:</p>
<ul>
<li><strong>Static site integration</strong>: generating a Hugo/Jekyll site from post-trigger content, hosted on GitHub Pages</li>
<li><strong>Multi-factor escalation</strong>: requiring check-ins from multiple sources (web + CLI + API) before considering you &ldquo;alive&rdquo;</li>
<li><strong>Better peer discovery</strong>: automatic peer registration instead of manual URL configuration</li>
</ul>
<p>The code is on GitHub: <a href="https://github.com/queelius/posthumous">queelius/posthumous</a>. It&rsquo;s a single <code>pip install</code> and about 2,200 lines of Python (plus 3,700 lines of tests at 99% coverage).</p>
<hr>
<p><em>Posthumous is named for the obvious reason. Some automations only make sense after the fact.</em></p>]]></content:encoded><category>posthumous</category><category>dead-mans-switch</category><category>python</category><category>asyncio</category><category>totp</category><category>federation</category><category>open-source</category></item><item><title>A Map of My Open Source Ecosystem</title><link>https://metafunctor.com/post/2026-02-07-ecosystem-overview/</link><pubDate>Fri, 13 Feb 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2026-02-07-ecosystem-overview/</guid><description>A guided tour through my open-source ecosystem: encrypted search theory, statistical reliability, Unix-philosophy CLI tools, AI research, and speculative fiction. How the projects connect and where to start.</description><content:encoded><![CDATA[<p>Over the past decade, I&rsquo;ve published more than 120 open-source repositories on <a href="https://github.com/queelius">GitHub</a>. They span encrypted search theory, statistical reliability engineering, generic programming, fuzzy logic, AI tools, Unix-philosophy CLI utilities, three novels, a community math database, and a digital legacy project motivated by the possibility that I might not be around to maintain any of it.</p>
<p>From the outside, it probably looks like a sprawl. From the inside, almost everything connects.</p>
<p>This post is the map I wish I&rsquo;d written years ago. It&rsquo;s for anyone who stumbles into my GitHub profile and wonders: <em>what is all this, and how does it fit together?</em></p>
<p>Two threads run through everything:</p>
<ol>
<li>
<p><strong>Algebraic composability.</strong> I think the right abstractions compose. Hash functions compose into perfect hash filters. Likelihood contributions compose into maximum likelihood estimators. Boolean predicates compose into fuzzy inference rules. CLI tools compose into personal knowledge pipelines. When I find a domain where composition is possible but nobody has made it ergonomic, I start a project.</p>
</li>
<li>
<p><strong>Personal data sovereignty.</strong> Your conversations, bookmarks, photos, email, health records, and reading notes belong to you, not to a cloud platform that might shut down, change terms, or get acquired. A surprising number of my tools exist to keep personal data in local SQLite databases, queryable from the command line, exportable as plain text, and durable across decades.</p>
</li>
</ol>
<p>If you keep those two threads in mind, the ecosystem stops looking like a sprawl and starts looking like a lattice.</p>
<hr>
<h2 id="the-research-foundation">The Research Foundation</h2>
<h3 id="oblivious-computing-and-encrypted-search">Oblivious Computing and Encrypted Search</h3>
<p>This is where it started. My <a href="/post/2024-06-15-reliability-estimation-in-series-systems/">master&rsquo;s thesis</a> explored how to search encrypted data without revealing what you&rsquo;re searching for, the problem known as <em>oblivious computing</em> or <em>encrypted search</em>.</p>
<p>The core question: can you build a search index where the server processes queries without learning the query content, the document content, or even which documents matched? The answer is yes, but only with careful algebraic constraints on the encryption.</p>
<p>The key projects:</p>
<ul>
<li><a href="https://github.com/queelius/encrypted_search_thesis"><strong>encrypted_search_thesis</strong></a>: The thesis itself, formalizing encrypted search using trapdoor Boolean algebras</li>
<li><a href="https://github.com/queelius/algebraic_cipher_types"><strong>algebraic_cipher_types</strong></a>: A type-theoretic framework for reasoning about ciphertexts as algebraic objects. This paper (<a href="/post/2025-10-07-algebraic-cipher-types/">blog post</a>) defines ciphertext types that preserve specific algebraic operations under encryption.</li>
<li><a href="https://github.com/queelius/bernoulli_data_type"><strong>bernoulli_data_type</strong></a>: The Bernoulli model for approximate encrypted search. When exact matches aren&rsquo;t possible, what&rsquo;s the information-theoretic cost?</li>
<li><a href="https://github.com/queelius/boolean-algebra-over-trapdoor-sets"><strong>boolean-algebra-over-trapdoor-sets</strong></a>: Building Boolean algebra operations (AND, OR, NOT) on encrypted sets</li>
<li><a href="https://github.com/queelius/encrypted_search_confidentiality"><strong>encrypted_search_confidentiality</strong></a>: Measuring what encrypted search leaks via entropy and mutual information</li>
</ul>
<p>These are theoretical projects, papers and proofs, not production systems. They establish a mathematical framework for thinking about computation over encrypted data. The practical insight: if you model a ciphertext as a type with algebraic operations, you can reason about what computations preserve confidentiality and which ones leak.</p>
<h3 id="statistical-reliability">Statistical Reliability</h3>
<p>The other half of my thesis work, and the part that became production software. The question: if a series system fails but you can&rsquo;t tell which component caused the failure, how do you estimate component lifetimes?</p>
<p>This is <strong>masked data</strong> analysis. A system has <em>m</em> components in series. When it fails at time <em>t</em>, you get a <em>candidate set</em> of possible failure causes, not a definitive answer. Sometimes the system is still running when you stop testing (<em>right-censoring</em>). Given all this uncertainty, can you do maximum likelihood estimation?</p>
<p>Yes. And the resulting R packages form a layered architecture, each layer building on the one below:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">nabla                        : Exact derivatives via automatic differentiation
</span></span><span class="line"><span class="cl">  |
</span></span><span class="line"><span class="cl">femtograd                    : AD for statistical computing
</span></span><span class="line"><span class="cl">  |
</span></span><span class="line"><span class="cl">algebraic.mle                : MLE as algebraic objects (on CRAN)
</span></span><span class="line"><span class="cl">  |
</span></span><span class="line"><span class="cl">compositional.mle            : DSL for composing solvers (chaining, racing, restarts)
</span></span><span class="line"><span class="cl">  |
</span></span><span class="line"><span class="cl">likelihood.model             : Composable likelihood framework (submitted to CRAN)
</span></span><span class="line"><span class="cl">  |
</span></span><span class="line"><span class="cl">maskedcauses                 : Masked series systems (next CRAN submission)
</span></span><span class="line"><span class="cl">  |
</span></span><span class="line"><span class="cl">wei.series.md.c1.c2.c3      : Weibull series estimation (the applied capstone)
</span></span></code></pre></div><p>I&rsquo;ll detail the full R ecosystem below, but the key point: the theoretical work on masked data produced a general-purpose framework for composable statistical inference. The specific reliability application is just one instance of the pattern.</p>
<h3 id="information-theory-and-sequential-prediction">Information Theory and Sequential Prediction</h3>
<p>A smaller but persistent thread: <a href="/series/the-learning-problem/">sequential prediction</a> and information-theoretic approaches to learning.</p>
<ul>
<li><a href="https://github.com/queelius/infinigram"><strong>infinigram</strong></a>: Exploring what happens when n-gram models have unbounded context. Related to <a href="/post/2012-03-15-solomonoff-induction-prediction/">Solomonoff induction</a> and <a href="/post/2025-01-15-ctw-experimental-results/">CTW algorithms</a>.</li>
<li><a href="https://github.com/queelius/fisher-flow"><strong>fisher-flow</strong></a>: Connecting Fisher information geometry to information flow in statistical models</li>
<li><a href="https://github.com/queelius/langcalc"><strong>langcalc</strong></a>: Token-level entropy calculations for language models</li>
</ul>
<p>This cluster connects the encrypted search work (which is fundamentally about information leakage) to the statistical reliability work (which is about inference under missing information) through their shared foundation in information theory.</p>
<hr>
<h2 id="the-r-package-ecosystem">The R Package Ecosystem</h2>
<p>Thirteen R packages, two on CRAN, one submitted, the rest on <a href="https://queelius.r-universe.dev">r-universe</a>. They form a composable architecture where each layer adds capability without breaking the abstractions below.</p>
<h3 id="the-core-stack">The Core Stack</h3>
<table>
	<thead>
			<tr>
					<th>Package</th>
					<th>Role</th>
					<th>Status</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><a href="/projects/algebraic.mle/">algebraic.mle</a></td>
					<td>MLE objects with algebraic operations (compose, transform, subset)</td>
					<td>CRAN</td>
			</tr>
			<tr>
					<td><a href="/projects/algebraic.dist/">algebraic.dist</a></td>
					<td>Probability distributions as algebraic objects</td>
					<td>CRAN</td>
			</tr>
			<tr>
					<td><a href="/projects/likelihood.model/">likelihood.model</a></td>
					<td>Composable likelihood contributions for statistical models</td>
					<td>Submitted to CRAN</td>
			</tr>
			<tr>
					<td><a href="/projects/maskedcauses/">maskedcauses</a></td>
					<td>Series system MLE with masked failure causes</td>
					<td>Next CRAN submission</td>
			</tr>
	</tbody>
</table>
<p>The key design insight: <strong>likelihood contributions compose</strong>. When observations are independent, the total log-likelihood is the sum of individual contributions. <code>likelihood.model</code> provides the composition machinery. <code>algebraic.mle</code> provides the algebra for manipulating fitted models. Each domain-specific package just provides the contributions.</p>
<p>This means you can mix and match. Define your own likelihood contributions and they plug into the same optimization, bootstrap, and model selection infrastructure.</p>
<h3 id="supporting-packages">Supporting Packages</h3>
<table>
	<thead>
			<tr>
					<th>Package</th>
					<th>Role</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><a href="https://github.com/queelius/nabla">nabla</a></td>
					<td>Automatic differentiation for R, gradient computation without finite differences</td>
			</tr>
			<tr>
					<td><a href="https://github.com/queelius/femtograd">femtograd</a></td>
					<td>Minimal forward-mode AD, a teaching implementation</td>
			</tr>
			<tr>
					<td><a href="https://github.com/queelius/hypothesize">hypothesize</a></td>
					<td>Hypothesis testing framework built on the likelihood model</td>
			</tr>
			<tr>
					<td><a href="https://github.com/queelius/dfr_dist">dfr_dist</a></td>
					<td>Distribution-free reliability estimation</td>
			</tr>
			<tr>
					<td><a href="https://github.com/queelius/md.tools">md.tools</a></td>
					<td>Utilities for encoding and decoding masked data</td>
			</tr>
			<tr>
					<td><a href="/projects/wei.series.md.c1.c2.c3/">wei.series.md.c1.c2.c3</a></td>
					<td>Weibull series system implementations</td>
			</tr>
			<tr>
					<td><a href="/papers/mdrelax/">mdrelax</a></td>
					<td>What happens when the standard masking assumptions (C1/C2/C3) are violated</td>
			</tr>
			<tr>
					<td><a href="https://github.com/queelius/compositional.mle">compositional.mle</a></td>
					<td>Compositional extensions to the MLE framework</td>
			</tr>
	</tbody>
</table>
<h3 id="the-design-philosophy">The Design Philosophy</h3>
<p>Every package follows the same pattern: define an S3 object with generic functions (<code>loglik()</code>, <code>score()</code>, <code>hess_loglik()</code>, <code>fit()</code>, <code>rdata()</code>), make it composable, and let the algebra do the work. If you&rsquo;ve used one package in the stack, you know the interface for all of them.</p>
<p>This is <a href="/series/stepanov/">Stepanov&rsquo;s philosophy</a> applied to statistics: find the minimal algebraic structure, then build generic algorithms that work on anything satisfying that structure.</p>
<hr>
<h2 id="data-structures-and-algorithms">Data Structures and Algorithms</h2>
<h3 id="python-libraries-pypi">Python Libraries (PyPI)</h3>
<p>Over 20 packages on <a href="https://pypi.org/user/queelius/">PyPI</a> spanning data structures, symbolic computation, CLI tools, and more:</p>
<ul>
<li><a href="https://github.com/queelius/AlgoTree"><strong>AlgoTree</strong></a> (16 stars): Generic tree data structures and algorithms: traversal, manipulation, serialization, conversion between representations. (<a href="/post/algotree/">Blog post</a>)</li>
<li><a href="https://github.com/queelius/AlgoGraph"><strong>AlgoGraph</strong></a>: Graph algorithms in the same style</li>
<li><a href="https://github.com/queelius/symlik"><strong>symlik</strong></a>: Symbolic likelihood computation (<a href="/post/2025-12-16-symlik/">blog post</a>)</li>
<li><a href="https://github.com/queelius/rerum"><strong>rerum</strong></a>: Term rewriting engine</li>
<li><a href="https://github.com/queelius/dotsuite"><strong>dotsuite</strong></a>: Boolean algebra operations</li>
<li><a href="https://github.com/queelius/jaf"><strong>jaf</strong></a>: JSON algebra (merge, diff, patch)</li>
<li><a href="https://github.com/queelius/jsonl-algebra"><strong>jsonl-algebra</strong></a>: Algebraic operations on JSONL streams</li>
<li><a href="https://github.com/queelius/fuzzy-infer"><strong>fuzzy-infer</strong></a>: Fuzzy logic inference engine</li>
<li><a href="https://github.com/queelius/nfa-tools"><strong>nfa-tools</strong></a>: NFA manipulation and visualization</li>
<li><a href="https://github.com/queelius/src2md"><strong>src2md</strong></a>: Source code to markdown conversion</li>
<li><strong>pfc</strong> (Python bindings): Python interface for the <a href="https://github.com/queelius/pfc">pfc</a> prefix-free coding C++ library</li>
</ul>
<p>Most of the *tk CLI tools (btk, ctk, ebk, etc.) are also installable as Python packages. The pattern: algebraic objects with well-defined operations. Same philosophy as the R and C++ packages.</p>
<h3 id="c-libraries">C++ Libraries</h3>
<p>The C++ projects are where the generic programming philosophy is most explicit. They&rsquo;re inspired by Alexander Stepanov&rsquo;s work: algorithms should be parameterized by the algebraic structures they operate on, not by specific data types. Most are header-only C++20 libraries.</p>
<p><strong>Data structures and hashing:</strong></p>
<ul>
<li><a href="https://github.com/queelius/algebraic_hashing"><strong>algebraic_hashing</strong></a>: Hash function composition as algebraic morphisms, with a DSL for combining hash functions</li>
<li><a href="https://github.com/queelius/sparse_spatial_hash"><strong>sparse_spatial_hash</strong></a>: N-dimensional sparse spatial hashing for collision detection and neighbor queries</li>
<li><a href="https://github.com/queelius/maph"><strong>maph</strong></a>: Space-efficient approximate mappings using perfect hash functions, with configurable storage and accuracy trade-offs</li>
<li><a href="https://github.com/queelius/pfc"><strong>pfc</strong></a>: Zero-copy, prefix-free data representations with algebraic types and succinct data structures</li>
<li><a href="https://github.com/queelius/cbt"><strong>cbt</strong></a>: Computational basis transforms between domains</li>
<li><a href="https://github.com/queelius/bloomy"><strong>bloomy</strong></a>: Secure index based on Bloom filters (connecting to the encrypted search work)</li>
<li><a href="https://github.com/queelius/packed_data"><strong>packed_data</strong></a>: Compact data representations</li>
<li><a href="https://github.com/queelius/accumux"><strong>accumux</strong></a>: Accumulator and multiplexer patterns</li>
</ul>
<p><strong>Numerical and algebraic computation:</strong></p>
<ul>
<li><a href="https://github.com/queelius/limes"><strong>limes</strong></a>: Composable calculus expressions: symbolic differentiation, numerical integration, algebraic composition</li>
<li><a href="https://github.com/queelius/elementa"><strong>elementa</strong></a>: Pedagogical C++20 linear algebra library</li>
<li><a href="https://github.com/queelius/gradator"><strong>gradator</strong></a>: Pedagogical C++20 automatic differentiation library</li>
<li><a href="https://github.com/queelius/dual"><strong>dual</strong></a>: Dual number arithmetic for automatic differentiation</li>
</ul>
<p><strong>Simulation:</strong></p>
<ul>
<li><a href="https://github.com/queelius/barnes-hut"><strong>barnes-hut</strong></a>: Barnes-Hut tree algorithm for N-body simulation</li>
</ul>
<p>The Stepanov essay series (<a href="/post/2026-01-18-synthesis-stepanov/">Seeing Structure First</a>) explains the philosophy: an algorithm like <code>power(x, n, op)</code> computes x^n under <em>any</em> monoid operation, whether that&rsquo;s exponentiation, matrix power, string repetition, or path composition. The C++ libraries embody this by treating data structures as instances of algebraic concepts, not as standalone implementations.</p>
<hr>
<h2 id="boolean-fuzzy-and-symbolic-computation">Boolean, Fuzzy, and Symbolic Computation</h2>
<p>A cluster of Python projects (all on PyPI) exploring logic beyond true/false:</p>
<ul>
<li><a href="https://github.com/queelius/dotsuite"><strong>dotsuite</strong></a>: Boolean algebra operations and visualization</li>
<li><a href="https://github.com/queelius/jaf"><strong>jaf</strong></a>: JSON algebra: treating JSON documents as algebraic objects with merge, diff, and patch operations</li>
<li><a href="https://github.com/queelius/fuzzy-infer"><strong>fuzzy-infer</strong></a>: Fuzzy logic inference engine</li>
<li><a href="https://github.com/queelius/soft-circuit"><strong>soft-circuit</strong></a>: Soft Boolean circuits where gates have continuous rather than discrete outputs</li>
<li><a href="https://github.com/queelius/fuzzy-soft-circuit"><strong>fuzzy-soft-circuit</strong></a>: Combining fuzzy logic with soft circuits</li>
<li><a href="https://github.com/queelius/fuzzy-logic-search"><strong>fuzzy-logic-search</strong></a>: Search systems that use fuzzy logic for relevance scoring</li>
<li><a href="https://github.com/queelius/rerum"><strong>rerum</strong></a>: Term rewriting engine</li>
<li><a href="https://github.com/queelius/xtk"><strong>xtk</strong></a>: Expression toolkit for symbolic manipulation</li>
<li><a href="https://github.com/queelius/tree_rewriter"><strong>tree_rewriter</strong></a>: Tree-based term rewriting</li>
</ul>
<p>This cluster connects to the encrypted search work (Boolean algebra over trapdoor sets) and to the AI work (soft circuits as differentiable logic). The thread: logic is algebraic structure, and generalizing from crisp to fuzzy to soft to encrypted is a matter of changing which algebraic laws you preserve.</p>
<hr>
<h2 id="the-unix-toolkit-constellation">The Unix Toolkit Constellation</h2>
<p>Fifteen CLI tools that share a philosophy: <strong>SQLite-backed, CLI-first, composable, personal data sovereignty</strong>. They use a naming convention (most end in <code>tk</code> or <code>k</code>) and are designed to work together through Unix pipes and shared conventions.</p>
<table>
	<thead>
			<tr>
					<th>Tool</th>
					<th>Domain</th>
					<th>Description</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><a href="https://github.com/queelius/btk">btk</a></td>
					<td>Bookmarks</td>
					<td>SQLite-backed bookmarks with hierarchical tags and a typed query DSL</td>
			</tr>
			<tr>
					<td><a href="https://github.com/queelius/ebk">ebk</a></td>
					<td>Ebooks</td>
					<td>E-book library management, metadata extraction, format conversion</td>
			</tr>
			<tr>
					<td><a href="https://github.com/queelius/mtk">mtk</a></td>
					<td>Mail</td>
					<td>Local email archival and search</td>
			</tr>
			<tr>
					<td><a href="https://github.com/queelius/ptk">ptk</a></td>
					<td>Photos</td>
					<td>Photo management with EXIF metadata and tagging</td>
			</tr>
			<tr>
					<td><a href="https://github.com/queelius/atk">atk</a></td>
					<td>Audio</td>
					<td>Audio file management and metadata</td>
			</tr>
			<tr>
					<td><a href="https://github.com/queelius/ctk">ctk</a></td>
					<td>Conversations</td>
					<td>LLM conversation archival and search (<a href="/post/2025-10-ctk/">blog post</a>)</td>
			</tr>
			<tr>
					<td><a href="https://github.com/queelius/xtk">xtk</a></td>
					<td>Expressions</td>
					<td>Symbolic expression manipulation</td>
			</tr>
			<tr>
					<td><a href="https://github.com/queelius/jot">jot</a></td>
					<td>Notes</td>
					<td>Journaling and note-taking</td>
			</tr>
			<tr>
					<td><a href="https://github.com/queelius/deets">deets</a></td>
					<td>Personal metadata</td>
					<td>Identity and metadata management</td>
			</tr>
			<tr>
					<td><a href="https://github.com/queelius/repoindex">repoindex</a></td>
					<td>Repositories</td>
					<td>Git repository database and query tool</td>
			</tr>
			<tr>
					<td><a href="https://github.com/queelius/crier">crier</a></td>
					<td>Social media</td>
					<td>Cross-posting blog content to social platforms (12 stars)</td>
			</tr>
			<tr>
					<td><a href="https://github.com/queelius/chop">chop</a></td>
					<td>Images</td>
					<td>Image manipulation</td>
			</tr>
			<tr>
					<td><a href="https://github.com/queelius/dapple">dapple</a></td>
					<td>Terminal graphics</td>
					<td>Rich terminal output and visualization</td>
			</tr>
			<tr>
					<td><a href="https://github.com/queelius/clerk">clerk</a></td>
					<td>Workflows</td>
					<td>Task and workflow management</td>
			</tr>
			<tr>
					<td><a href="https://github.com/queelius/chartfold">chartfold</a></td>
					<td>Health records</td>
					<td>Personal health record management</td>
			</tr>
	</tbody>
</table>
<h3 id="the-philosophy">The Philosophy</h3>
<p>These tools share a conviction: your personal data should live in <strong>local SQLite databases</strong>, not in someone else&rsquo;s cloud. Every tool stores its data in a format you can query with SQL, back up with <code>cp</code>, and inspect with <code>sqlite3</code>. They compose through pipes:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Find bookmarks tagged &#34;research&#34;, get their URLs, search your ebooks for related content</span>
</span></span><span class="line"><span class="cl">btk query --tag research --format url <span class="p">|</span> xargs -I<span class="o">{}</span> ebk search --related <span class="o">{}</span>
</span></span></code></pre></div><p>The <a href="/series/the-long-echo/">blog series on digital legacy</a> explains the motivation. When I was diagnosed with cancer, the question &ldquo;what happens to my digital life?&rdquo; became urgent. These tools are one answer: if your data lives in portable, open formats on hardware you control, it survives you.</p>
<hr>
<h2 id="ai-and-llm-projects">AI and LLM Projects</h2>
<p>The largest and fastest-growing cluster.</p>
<h3 id="llm-tools-and-research">LLM Tools and Research</h3>
<ul>
<li><a href="https://github.com/queelius/elasticsearch-lm"><strong>elasticsearch-lm</strong></a> (37 stars): Language model integration with Elasticsearch. Bridges the encrypted search research with modern LLM applications.</li>
<li><a href="https://github.com/queelius/mcts-reasoning"><strong>mcts-reasoning</strong></a>: Monte Carlo Tree Search for structured reasoning. Uses tree search (connecting to AlgoTree) to improve LLM reasoning quality. (<a href="/post/2024-12-01-mcts-reasoning/">Blog post</a>)</li>
<li><a href="https://github.com/queelius/dreamlog"><strong>dreamlog</strong></a>: Logic programming with LLM integration and wake-sleep learning cycles</li>
<li><a href="https://github.com/queelius/complex-network-rag"><strong>complex-network-rag</strong></a>: RAG (Retrieval-Augmented Generation) over complex document networks</li>
<li><a href="https://github.com/queelius/ollama_data_tools"><strong>ollama_data_tools</strong></a> (4 stars): Utilities for working with Ollama models and data</li>
<li><a href="https://github.com/queelius/llm-bayes"><strong>llm-bayes</strong></a>: Bayesian reasoning with LLMs</li>
<li><a href="https://github.com/queelius/synthdata"><strong>synthdata</strong></a>: Synthetic data generation</li>
<li><a href="https://github.com/queelius/agentum"><strong>agentum</strong></a>: Unified framework for sequential decision-making, from classical search to deep RL</li>
<li><a href="https://github.com/queelius/itinero"><strong>itinero</strong></a>: LLM-powered web automation through composable strategies and Playwright</li>
<li><a href="https://github.com/queelius/AutoPoiesi"><strong>AutoPoiesi</strong></a>: Self-organizing systems inspired by autopoiesis theory</li>
</ul>
<h3 id="the-thread">The Thread</h3>
<p>These aren&rsquo;t random AI projects. The encrypted search work asks: &ldquo;How do you search without revealing what you know?&rdquo; The LLM work asks the complement: &ldquo;How do you <em>reason</em> with what you know?&rdquo; Both are about the interface between information and computation. <code>elasticsearch-lm</code> literally bridges the two.</p>
<hr>
<h2 id="digital-legacy-projects">Digital Legacy Projects</h2>
<p>Five projects exploring what happens to digital identity after death. This is the <a href="/series/the-long-echo/">Long Echo</a> series in code form.</p>
<ul>
<li><a href="https://github.com/queelius/longecho"><strong>longecho</strong></a>: The framework for durable personal data. Redundant storage, format migration, integrity verification.</li>
<li><a href="https://github.com/queelius/longshade"><strong>longshade</strong></a>: Generate a conversable persona from personal data: conversations, writings, and email</li>
<li><a href="https://github.com/queelius/posthumous"><strong>posthumous</strong></a>: Automated actions triggered by death or incapacity. Dead man&rsquo;s switches, scheduled messages, data release.</li>
<li><a href="https://github.com/queelius/pagevault"><strong>pagevault</strong></a>: Password-protect semi-private content on static sites like Hugo blogs</li>
<li><a href="https://github.com/queelius/cryptoid"><strong>cryptoid</strong></a>: Client-side encrypted content for Hugo static sites with multi-user access control</li>
</ul>
<p>These are nascent. They share a philosophy but don&rsquo;t yet share infrastructure. The vision: a unified system where your personal data (managed by the *tk tools) is stored durably (longecho), distilled into a conversable persona (longshade), protected on your sites (pagevault, cryptoid), and handled according to your wishes after death (posthumous).</p>
<p>The <a href="/post/2026-01-04-notes-from-the-transition/">essays on digital legacy</a> provide the philosophical backdrop. The tools provide the engineering.</p>
<hr>
<h2 id="literature">Literature</h2>
<p>Three novels, each exploring different aspects of consciousness and intelligence:</p>
<ul>
<li><a href="https://github.com/queelius/echoes-of-the-sublime"><strong>echoes-of-the-sublime</strong></a>: Philosophical horror, approximately 103,000 words. What happens when consciousness encounters something genuinely beyond its capacity to process? (<a href="/post/2024-08-15-echoes-of-the-sublime/">Blog post</a>)</li>
<li><a href="https://github.com/queelius/the-policy"><strong>the-policy</strong></a>: AI alignment science fiction. A near-future story about the consequences of a specific alignment strategy. (<a href="/series/minds-and-machines/">Blog series</a>)</li>
<li><a href="https://github.com/queelius/call-of-asheron"><strong>call-of-asheron</strong></a>: Epic fantasy. Emergence, self-organization, and what happens when a world&rsquo;s underlying rules become visible to its inhabitants.</li>
</ul>
<p>The novels aren&rsquo;t decoration. They&rsquo;re explorations of the same themes that drive the code (consciousness, information, what persists and what doesn&rsquo;t) in a medium that allows for ambiguity and nuance that research papers can&rsquo;t.</p>
<p>There&rsquo;s also <a href="https://github.com/queelius/stories"><strong>stories</strong></a>, a collection of shorter fiction.</p>
<hr>
<h2 id="the-erdos-problems-database">The Erdos Problems Database</h2>
<p>One project worth mentioning separately: <a href="https://github.com/queelius/erdosproblems"><strong>erdosproblems</strong></a> (470 stars) is a community database for the problems listed on <a href="https://erdosproblems.com">erdosproblems.com</a>, Thomas Bloom&rsquo;s curation of Paul Erdos&rsquo;s open problems in combinatorics, number theory, and graph theory. My contribution is to the structured data layer: making the problems machine-readable, searchable, and cross-referenced. Same instinct that drives much of this ecosystem: make knowledge structured and open.</p>
<hr>
<h2 id="programming-languages-and-interpreters">Programming Languages and Interpreters</h2>
<p>A small cluster exploring language design:</p>
<ul>
<li><a href="https://github.com/queelius/jsl"><strong>jsl</strong></a>: JSON-based scripting language</li>
<li><a href="https://github.com/queelius/jsonl-algebra"><strong>jsonl-algebra</strong></a>: Algebraic operations on JSONL streams</li>
<li><a href="https://github.com/queelius/nfa-tools"><strong>nfa-tools</strong></a>: Non-deterministic finite automaton manipulation</li>
<li><a href="https://github.com/queelius/dagshell"><strong>dagshell</strong></a>: Shell with DAG-based execution</li>
<li><a href="https://github.com/queelius/tex2any"><strong>tex2any</strong></a> / <a href="https://github.com/queelius/texflow"><strong>texflow</strong></a>: LaTeX transformation tools</li>
</ul>
<p>These connect to the symbolic computation cluster (rerum, xtk, tree_rewriter) through their shared concern with formal language manipulation.</p>
<hr>
<h2 id="education-and-essay-series">Education and Essay Series</h2>
<p>Four ongoing series:</p>
<ul>
<li><a href="/series/stepanov/"><strong>stepanov</strong></a>: Generic programming in the style of Stepanov. Eleven essays on finding algebraic structure in algorithms.</li>
<li><a href="/series/sicp/"><strong>sicp</strong></a>: Abstraction and composition, inspired by Structure and Interpretation of Computer Programs.</li>
<li><a href="/series/the-learning-problem/"><strong>the-learning-problem</strong></a>: Machine learning from an information-theoretic perspective. Sequential prediction, Solomonoff induction, compression as learning.</li>
<li><a href="/series/the-long-echo/"><strong>the-long-echo</strong></a>: Digital legacy, personal data sovereignty, and what persists.</li>
</ul>
<hr>
<h2 id="space-simulation">Space Simulation</h2>
<p>Two projects that stand somewhat apart:</p>
<ul>
<li><a href="https://github.com/queelius/space-sandbox-sim"><strong>space-sandbox-sim</strong></a>: N-body physics sandbox</li>
<li><a href="https://github.com/queelius/star-system-sim"><strong>star-system-sim</strong></a>: Star system generation and simulation</li>
</ul>
<p>These connect to the C++ algorithmic work (barnes-hut tree algorithm for N-body simulation) and to the generic programming philosophy (physics simulation as algebraic structure).</p>
<hr>
<h2 id="infrastructure-and-meta-tools">Infrastructure and Meta-tools</h2>
<p>The projects that support everything else:</p>
<ul>
<li><a href="https://github.com/queelius/metafunctor"><strong>metafunctor</strong></a>: This Hugo blog. The central publication venue for all the work above.</li>
<li><a href="https://github.com/queelius/queelius.github.io"><strong>queelius.github.io</strong></a>: GitHub Pages deployment</li>
<li><a href="https://github.com/queelius/queelius.r-universe.dev"><strong>queelius.r-universe.dev</strong></a>: R-universe configuration for the R package ecosystem</li>
<li><a href="https://github.com/queelius/src2md"><strong>src2md</strong></a>: Convert source code to markdown for documentation</li>
<li><a href="https://github.com/queelius/texwatch"><strong>texwatch</strong></a>: Watch LaTeX files and rebuild on change</li>
<li><a href="https://github.com/queelius/sandrun"><strong>sandrun</strong></a>: Sandboxed command execution</li>
<li><a href="https://github.com/queelius/zeroipc"><strong>zeroipc</strong></a>: Zero-copy IPC mechanisms (multi-language)</li>
</ul>
<hr>
<h2 id="gap-analysis-whats-missing">Gap Analysis: What&rsquo;s Missing</h2>
<p>Looking at 120+ projects as a whole, several gaps become visible:</p>
<h3 id="no-unified-documentation-portal">No Unified Documentation Portal</h3>
<p>Sixty-two repos have GitHub Pages sites, but there&rsquo;s no cross-project index. If you want to understand how <code>algebraic.mle</code> connects to <code>likelihood.model</code> connects to <code>maskedcauses</code>, you have to read three separate documentation sites and stitch them together mentally. A single documentation portal with cross-linked API docs and a dependency graph would make the ecosystem navigable.</p>
<h3 id="missing-ci-infrastructure">Missing CI Infrastructure</h3>
<p>Many beta-stage repositories lack continuous integration. The R packages have CI through r-universe, but the Python and C++ projects are inconsistent. A standardized CI template would catch breakage earlier.</p>
<h3 id="no-rich-python-package-index">No Rich Python Package Index</h3>
<p>Over 20 packages are on PyPI under the <a href="https://pypi.org/user/queelius/">queelius</a> account, but the PyPI profile is just a flat list. There&rsquo;s no equivalent to <a href="https://queelius.r-universe.dev">r-universe</a>: no descriptions, no dependency graph, no grouping by theme.</p>
<h3 id="no-cross-project-dependency-graph">No Cross-Project Dependency Graph</h3>
<p>It&rsquo;s hard to see which projects build on which. The R packages have an implicit dependency tree, but it&rsquo;s not visualized. The *tk tools share conventions but not code. A visual dependency map would help contributors and users understand the architecture.</p>
<h3 id="c-libraries-lack-a-unifying-build-mechanism">C++ Libraries Lack a Unifying Build Mechanism</h3>
<p>The C++ projects don&rsquo;t share a package manager configuration (Conan, vcpkg, or similar). Each library is standalone. For users who want to use multiple libraries together (say, algebraic_hashing with sparse_spatial_hash) there&rsquo;s no easy integration path.</p>
<h3 id="scattered-fuzzy-logic">Scattered Fuzzy Logic</h3>
<p>Four separate fuzzy logic projects (fuzzy-infer, fuzzy-logic-search, fuzzy-soft-circuit, soft-circuit) share concepts but not code. They could be unified into a single, layered fuzzy logic library.</p>
<h3 id="the-tk-tools-need-a-meta-installer">The *tk Tools Need a Meta-Installer</h3>
<p>The Unix toolkit tools share a philosophy and naming convention, but there&rsquo;s no umbrella project, shared library, or meta-installer. Common patterns (SQLite storage, CLI argument parsing, query DSL) are reimplemented in each tool. A shared foundation library would reduce duplication.</p>
<h3 id="no-benchmarking-suite-for-probabilistic-data-structures">No Benchmarking Suite for Probabilistic Data Structures</h3>
<p>The hashing and data structure projects (maph, algebraic_hashing, sparse_spatial_hash, pfc) lack comparative benchmarks. A shared benchmark suite would make performance claims credible and help users choose between implementations.</p>
<h3 id="digital-legacy-projects-need-integration">Digital Legacy Projects Need Integration</h3>
<p>The longecho/longshade/posthumous projects articulate a compelling vision but exist as separate, early-stage prototypes. The integration story (how they work together, and how they connect to the *tk tools) hasn&rsquo;t been built yet.</p>
<h3 id="missing-end-to-end-tutorials">Missing End-to-End Tutorials</h3>
<p>The R packages have individual documentation, but there&rsquo;s no tutorial showing how to use them together: generate synthetic masked data, fit models, compare via AIC, bootstrap confidence intervals, and visualize results, all using the composable architecture.</p>
<hr>
<h2 id="where-to-start">Where to Start</h2>
<p>If you&rsquo;ve read this far and want to explore, here are entry points by interest:</p>
<p><strong>If you&rsquo;re a statistician or reliability engineer:</strong>
Start with <a href="/projects/algebraic.mle/">algebraic.mle</a> (on CRAN), then read the <a href="/post/2026-02-05-likelihood-model-series-md/">maskedcauses post</a>. The composable likelihood architecture is the most mature part of the ecosystem.</p>
<p><strong>If you&rsquo;re a C++ programmer interested in generic programming:</strong>
Start with the <a href="/series/stepanov/">Stepanov series</a>, especially <a href="/post/2026-01-18-synthesis-stepanov/">Seeing Structure First</a>. Then look at the C++ libraries (algebraic_hashing, maph, pfc) for worked examples.</p>
<p><strong>If you work with trees or graphs in Python:</strong>
<a href="https://github.com/queelius/AlgoTree">AlgoTree</a> and <a href="https://github.com/queelius/AlgoGraph">AlgoGraph</a> are on PyPI and treat data structures as composable algebraic objects.</p>
<p><strong>If you care about personal data sovereignty:</strong>
Start with <a href="https://github.com/queelius/btk">btk</a> (bookmarks) or <a href="https://github.com/queelius/ctk">ctk</a> (conversations). They&rsquo;re the most polished *tk tools. Then read the <a href="/series/the-long-echo/">Long Echo</a> essays for the philosophical motivation.</p>
<p><strong>If you&rsquo;re interested in AI/LLM tools:</strong>
<a href="https://github.com/queelius/elasticsearch-lm">elasticsearch-lm</a> bridges traditional search with language models. <a href="https://github.com/queelius/mcts-reasoning">mcts-reasoning</a> applies tree search to LLM reasoning.</p>
<p><strong>If you&rsquo;re a mathematician:</strong>
The <a href="https://github.com/queelius/encrypted_search_thesis">oblivious computing papers</a> apply algebra to cryptography. The <a href="/series/the-learning-problem/">information theory posts</a> connect Solomonoff induction to practical prediction.</p>
<p><strong>If you want to read fiction:</strong>
<a href="https://github.com/queelius/echoes-of-the-sublime">echoes-of-the-sublime</a> is the longest and most developed. <a href="https://github.com/queelius/the-policy">the-policy</a> is the most timely: AI alignment as lived experience.</p>
<hr>
<h2 id="the-shape-of-the-whole">The Shape of the Whole</h2>
<p>If I step back and look at everything together, I see a single question asked in many registers:</p>
<blockquote>
<p><em>How do you build reliable knowledge from noisy, incomplete, and partial observations?</em></p>
</blockquote>
<p>In encrypted search, the noise is intentional. It&rsquo;s the price of privacy.
In reliability engineering, the noise is censoring and masking. The data you wish you had.
In fuzzy logic, the noise is inherent in vague predicates.
In LLM reasoning, the noise is the stochasticity of language models.
In digital legacy, the noise is time itself. Bit rot, platform decay, forgetting.</p>
<p>The answer, in every case, is the same: <em>algebraic structure.</em> Find the right abstractions, make them compose, and the noise becomes manageable.</p>
<p>What does 120+ projects look like, all at once? It looks like a person thinking out loud for over a decade. The early projects are tentative: data structures, small algorithms, conference papers. The middle period is the thesis work, rigorous, focused, building toward specific results. The recent projects are more ambitious and more personal: novels, digital legacy tools, an essay about watching intelligence leave the body.</p>
<p>The connecting thread isn&rsquo;t a technology or a domain. It&rsquo;s a question: <strong>what structures persist?</strong> Algebraic structures persist across implementations. Mathematical results persist across paradigms. Well-designed software interfaces persist across refactoring. Stories persist across readers. And if you&rsquo;re careful about formats and infrastructure, personal data can persist across a lifetime, and beyond.</p>
<p>That&rsquo;s the ecosystem. It&rsquo;s incomplete, unevenly documented, and will probably never be finished. But the map is here now, and the territory is open.</p>
<hr>
<p><strong>Resources:</strong></p>
<ul>
<li><a href="https://github.com/queelius">GitHub profile</a></li>
<li><a href="https://pypi.org/user/queelius/">PyPI packages</a></li>
<li><a href="https://queelius.r-universe.dev">R-universe packages</a></li>
<li><a href="/series/">All blog series</a></li>
<li><a href="/projects/">All project pages</a></li>
</ul>]]></content:encoded><category>ecosystem</category><category>open-source</category><category>overview</category><category>oblivious-computing</category><category>statistics</category><category>reliability</category><category>generic-programming</category><category>unix-philosophy</category><category>digital-legacy</category><category>AI</category><category>LLM</category><category>R</category><category>C++</category><category>Python</category></item><item><title>pagevault: Hiding an Encryption Platform Inside HTML</title><link>https://metafunctor.com/post/2026-02-13-pagevault/</link><pubDate>Fri, 13 Feb 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2026-02-13-pagevault/</guid><description>pagevault turns any file into a self-contained encrypted HTML page. No backend, no JavaScript libraries, no external dependencies. Just AES-256-GCM and the browser's built-in Web Crypto API. The interesting part is making it work at scale.</description><content:encoded><![CDATA[<p>HTML is an encryption container format. That sounds wrong, but think about what an HTML file can hold: arbitrary data in script tags or data attributes, a full programming runtime via JavaScript, and a rendering engine (the browser) on every device on the planet. If you embed encrypted data and the code to decrypt it, the result is a file that looks inert until someone types the right password.</p>
<p><a href="https://github.com/queelius/pagevault">pagevault</a> takes this idea seriously. It encrypts files, documents, images, entire websites, into self-contained HTML pages that decrypt in the browser. No backend. No JavaScript crypto libraries. The browser already has AES-256-GCM built in via the Web Crypto API. pagevault just has to match the parameters exactly on the Python side and embed the right 200 lines of JavaScript.</p>
<p>The output is a single <code>.html</code> file. You can email it, put it on a USB stick, host it on GitHub Pages, or double-click it on your desktop. It doesn&rsquo;t phone home, it doesn&rsquo;t load CDNs, it doesn&rsquo;t need anything except a browser.</p>
<hr>
<h2 id="what-goes-in">What Goes In</h2>
<p>Anything.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">pagevault lock report.pdf              <span class="c1"># PDF with embedded viewer</span>
</span></span><span class="line"><span class="cl">pagevault lock photo.jpg               <span class="c1"># image with click-to-zoom</span>
</span></span><span class="line"><span class="cl">pagevault lock notes.md                <span class="c1"># markdown, rendered or source view</span>
</span></span><span class="line"><span class="cl">pagevault lock recording.mp3           <span class="c1"># audio player</span>
</span></span><span class="line"><span class="cl">pagevault lock mysite/ --site          <span class="c1"># entire multi-page website</span>
</span></span><span class="line"><span class="cl">pagevault lock page.html               <span class="c1"># HTML with selective region encryption</span>
</span></span></code></pre></div><p>Every output is a single <code>.html</code> file containing the ciphertext, a password prompt, the decryption runtime, and a viewer plugin for the content type. Seven viewers ship built-in: Image, PDF, HTML, Text (with line numbers), Markdown (with rendered/source toggle), Audio, and Video. They&rsquo;re a plugin system, so you can add your own.</p>
<p>Here&rsquo;s what it looks like. An encrypted security report:</p>
<p><img src="report-locked.png" alt="Encrypted file showing password prompt with lock icon and filename"></p>
<p>After entering the password, the viewer renders the decrypted content with a toolbar:</p>
<p><img src="report-unlocked.png" alt="Decrypted quarterly security report with vulnerability tables and findings"></p>
<p>For directories, <code>--site</code> bundles everything into a single encrypted HTML file. The directory is zipped with deflate compression, encrypted, and embedded. On the browser side, a minimal zip reader (no library, just the built-in <code>DecompressionStream</code> API) unpacks it after decryption. Internal links between pages work. CSS and images load from the zip. The whole site navigates inside an iframe as if it were served normally:</p>
<p><img src="site-index.png" alt="Decrypted site viewer showing a Project Hub with navigation, welcome section, and sprint stats"></p>
<p>A 100 MB site compresses well if it&rsquo;s mostly text and markup. The zip gets chunked and encrypted the same way single files do, so the same progress bar and memory management apply. I&rsquo;ve tested sites with hundreds of files without issues.</p>
<hr>
<h2 id="the-crypto">The Crypto</h2>
<p>Nothing exotic here. AES-256-GCM for authenticated encryption, PBKDF2-SHA256 with 310,000 iterations for key derivation, all through the browser&rsquo;s Web Crypto API. The interesting part isn&rsquo;t the cryptography. It&rsquo;s making the container format work at scale.</p>
<p>Multi-user access uses CEK (content-encryption key) wrapping. A random key encrypts the data once. That key is then wrapped separately for each user&rsquo;s derived key:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">Content  ──encrypt with CEK──&gt;  Ciphertext (one copy)
</span></span><span class="line"><span class="cl">CEK  ──wrap with Alice&#39;s key──&gt;  Key Blob A
</span></span><span class="line"><span class="cl">CEK  ──wrap with Bob&#39;s key────&gt;  Key Blob B
</span></span></code></pre></div><p>Adding a user wraps one small key blob. Removing a user deletes one blob. The bulk content stays untouched.</p>
<hr>
<h2 id="the-hard-part-large-files">The Hard Part: Large Files</h2>
<p>The basic approach (encrypt, base64-encode, embed in HTML) works fine for small files. The problems start when you try to encrypt an 84 MB conversation archive or a 179 MB HTML report.</p>
<p>The original v2 format had a compounding overhead problem. File bytes were base64-encoded (33% expansion), then encrypted, then the ciphertext was base64-encoded again (another 33%). That&rsquo;s <code>1.33 * 1.33 = 1.77x</code> total overhead. An 84 MB file produced a 198 MB HTML page. Wasteful, and slow to decrypt because the browser had to hold the entire blob in memory at once.</p>
<p>v3 fixes this with chunked encryption.</p>
<h3 id="eliminating-the-double-base64">Eliminating the double base64</h3>
<p>v2 encrypted a base64 string, then base64-encoded the result. Two layers. v3 encrypts the raw bytes directly and base64-encodes once. The metadata (filename, MIME type, size) is encrypted separately instead of being bundled into the content. This alone cuts the overhead from 77% to about 39%.</p>
<h3 id="chunked-ciphertext">Chunked ciphertext</h3>
<p>Instead of one giant encrypted blob, v3 splits content into 1 MB chunks. Each chunk is encrypted independently with AES-256-GCM using a counter-derived IV: the chunk index is XORed into the last four bytes of a base IV. Each chunk becomes its own <code>&lt;script&gt;</code> tag in the HTML:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-html" data-lang="html"><span class="line"><span class="cl"><span class="p">&lt;</span><span class="nt">script</span> <span class="na">id</span><span class="o">=</span><span class="s">&#34;pv-0&#34;</span> <span class="na">type</span><span class="o">=</span><span class="s">&#34;x-pv&#34;</span><span class="p">&gt;</span><span class="nx">base64</span><span class="o">-</span><span class="k">of</span><span class="o">-</span><span class="nx">chunk</span><span class="o">-</span><span class="mf">0.</span><span class="p">..&lt;/</span><span class="nt">script</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="p">&lt;</span><span class="nt">script</span> <span class="na">id</span><span class="o">=</span><span class="s">&#34;pv-1&#34;</span> <span class="na">type</span><span class="o">=</span><span class="s">&#34;x-pv&#34;</span><span class="p">&gt;</span><span class="nx">base64</span><span class="o">-</span><span class="k">of</span><span class="o">-</span><span class="nx">chunk</span><span class="o">-</span><span class="mf">1.</span><span class="p">..&lt;/</span><span class="nt">script</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl">...
</span></span><span class="line"><span class="cl"><span class="p">&lt;</span><span class="nt">script</span> <span class="na">id</span><span class="o">=</span><span class="s">&#34;pv-83&#34;</span> <span class="na">type</span><span class="o">=</span><span class="s">&#34;x-pv&#34;</span><span class="p">&gt;</span><span class="nx">base64</span><span class="o">-</span><span class="k">of</span><span class="o">-</span><span class="nx">chunk</span><span class="o">-</span><span class="mf">83.</span><span class="p">..&lt;/</span><span class="nt">script</span><span class="p">&gt;</span>
</span></span></code></pre></div><p>The browser decrypts them sequentially, showing a progress bar. After each chunk is decrypted, the corresponding script tag is removed from the DOM (<code>el.remove()</code>), freeing the base64 text for garbage collection. Memory usage stays proportional to the chunk size, not the file size.</p>
<h3 id="the-numbers">The numbers</h3>
<p>That 84 MB conversation archive: v2 produced 198 MB. v3 produces 117 MB. A 41% reduction, and the decryption doesn&rsquo;t choke the browser.</p>
<p>Here&rsquo;s the 84 MB file after decryption, rendered as a searchable conversation interface:</p>
<p><img src="v3-84mb-decrypted.png" alt="Decrypted 83.4 MB conversation archive showing search interface with toolbar"></p>
<p>I&rsquo;ve also tested a 315 MB text file (works, slow to load) and a 179 MB HTML file with 1.5 million DOM elements (DOM rendering is the bottleneck there, not decryption). These are probably past the point of reason for an HTML container, but it&rsquo;s nice to know where the limits actually are.</p>
<hr>
<h2 id="region-encryption">Region Encryption</h2>
<p>pagevault also handles a more specific case: encrypting parts of an HTML page while leaving the rest public. You mark sections with <code>&lt;pagevault&gt;</code> tags and only those become ciphertext. Public navigation, styling, scripts, all untouched. Useful for dashboards where some metrics are public and others aren&rsquo;t, or documentation with confidential sections.</p>
<p>The lock operation is composable: its output is valid input. You can encrypt a page for Alice, then encrypt different sections for Bob. The page round-trips cleanly through multiple passes.</p>
<hr>
<h2 id="the-file-problem">The <code>file://</code> Problem</h2>
<p>One thing that surprised me. Encrypted HTML files opened from the filesystem (<code>file://</code> URLs) behave differently than files served over HTTP. The <code>file://</code> protocol gives pages an opaque <code>null</code> origin, which breaks <code>localStorage</code> (so &ldquo;remember on this device&rdquo; doesn&rsquo;t work) and blocks nested blob URLs (so the HTML viewer can&rsquo;t create iframes the normal way).</p>
<p>The fix was <code>srcdoc</code> iframes, which inherit the parent&rsquo;s origin, plus a <code>pushState</code> shim for the URL bar. Not glamorous, but it means encrypted files work identically whether you double-click them on your desktop or serve them from a CDN.</p>
<p>There&rsquo;s also a built-in dev server for when you want HTTP without the hassle:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">pagevault serve _locked/ --open    <span class="c1"># HTTP server on localhost:8765</span>
</span></span></code></pre></div><hr>
<h2 id="related-cryptoid">Related: cryptoid</h2>
<p>For a narrower use case, I also built <a href="https://github.com/queelius/cryptoid">cryptoid</a>. It encrypts Hugo markdown at build time using the same crypto core. Encrypted posts are still full Hugo citizens (taxonomies, RSS, related posts all work). If you have a Hugo blog and just need some posts behind a password, cryptoid is simpler. pagevault is for everything else.</p>
<hr>
<h2 id="try-it">Try It</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">pip install pagevault
</span></span><span class="line"><span class="cl">pagevault lock report.pdf                   <span class="c1"># wrap any file</span>
</span></span><span class="line"><span class="cl">pagevault lock mysite/ --site               <span class="c1"># bundle a whole site</span>
</span></span><span class="line"><span class="cl">pagevault lock page.html -s <span class="s2">&#34;.private&#34;</span>      <span class="c1"># encrypt specific CSS selectors</span>
</span></span><span class="line"><span class="cl">pagevault serve _locked/ --open             <span class="c1"># preview locally</span>
</span></span></code></pre></div><p><a href="https://github.com/queelius/pagevault">GitHub</a>. MIT license. 667 tests. Dark mode follows your OS setting. Handles files larger than most people would think to put in an HTML page.</p>
<hr>
<p><em>Static sites shouldn&rsquo;t need backends just because some content needs a password.</em></p>]]></content:encoded><category>pagevault</category><category>cryptoid</category><category>encryption</category><category>static-sites</category><category>python</category><category>web-crypto</category><category>open-source</category></item><item><title>hypothesize: Now on CRAN</title><link>https://metafunctor.com/post/2025-12-12-hypothesize-cran/</link><pubDate>Fri, 12 Dec 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2025-12-12-hypothesize-cran/</guid><description>My R package for hypothesis testing, hypothesize, is now available on CRAN.</description><content:encoded><![CDATA[<p><strong>hypothesize</strong> is now on <a href="https://CRAN.R-project.org/package=hypothesize">CRAN</a>.</p>
<h2 id="what-it-does">What It Does</h2>
<p><code>hypothesize</code> provides a consistent API for hypothesis testing in R. It defines generic methods that any hypothesis test can implement:</p>
<ul>
<li><code>pval()</code> - Extract the p-value</li>
<li><code>test_stat()</code> - Get the test statistic</li>
<li><code>dof()</code> - Retrieve degrees of freedom</li>
<li><code>is_significant_at()</code> - Check significance at a given level</li>
</ul>
<p>The package ships with two implementations:</p>
<ul>
<li><strong>Likelihood Ratio Test (LRT)</strong> for comparing nested models</li>
<li><strong>Wald Test</strong> for testing parameter estimates</li>
</ul>
<h2 id="why">Why</h2>
<p>When building statistical libraries, I kept implementing ad-hoc hypothesis test structures. Different packages, different interfaces, no composability. <code>hypothesize</code> standardizes this: any package can wrap its tests in a consistent interface, and statistical workflows can treat all tests uniformly.</p>
<p>It&rsquo;s a small package. That&rsquo;s the point.</p>
<h2 id="installation">Installation</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="nf">install.packages</span><span class="p">(</span><span class="s">&#34;hypothesize&#34;</span><span class="p">)</span>
</span></span></code></pre></div><h2 id="quick-example">Quick Example</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="nf">library</span><span class="p">(</span><span class="n">hypothesize</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Likelihood Ratio Test</span>
</span></span><span class="line"><span class="cl"><span class="n">result</span> <span class="o">&lt;-</span> <span class="nf">lrt</span><span class="p">(</span><span class="n">null_loglik</span> <span class="o">=</span> <span class="m">-100</span><span class="p">,</span> <span class="n">alt_loglik</span> <span class="o">=</span> <span class="m">-96</span><span class="p">,</span> <span class="n">dof</span> <span class="o">=</span> <span class="m">3</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="nf">print</span><span class="p">(</span><span class="n">result</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Check significance</span>
</span></span><span class="line"><span class="cl"><span class="nf">is_significant_at</span><span class="p">(</span><span class="n">result</span><span class="p">,</span> <span class="m">0.05</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Extract components</span>
</span></span><span class="line"><span class="cl"><span class="nf">pval</span><span class="p">(</span><span class="n">result</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="nf">test_stat</span><span class="p">(</span><span class="n">result</span><span class="p">)</span>
</span></span></code></pre></div><h2 id="documentation">Documentation</h2>
<p>Full documentation is at <a href="https://queelius.github.io/hypothesize/">queelius.github.io/hypothesize</a>.</p>
<h2 id="whats-next">What&rsquo;s Next</h2>
<p>I have several other R packages in the pipeline for CRAN submission, including packages for likelihood-based inference and reliability analysis.</p>
<h2 id="links">Links</h2>
<ul>
<li><strong>CRAN</strong>: <a href="https://CRAN.R-project.org/package=hypothesize">CRAN.R-project.org/package=hypothesize</a></li>
<li><strong>GitHub</strong>: <a href="https://github.com/queelius/hypothesize">github.com/queelius/hypothesize</a></li>
<li><strong>Documentation</strong>: <a href="https://queelius.github.io/hypothesize/">queelius.github.io/hypothesize</a></li>
</ul>
]]></content:encoded><category>R</category><category>statistics</category><category>hypothesis testing</category><category>CRAN</category><category>open source</category></item><item><title>Everything is a File: Virtual Filesystems for CLI Data Tools</title><link>https://metafunctor.com/post/2025-10-20-vfs-cli-tools/</link><pubDate>Mon, 20 Oct 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2025-10-20-vfs-cli-tools/</guid><description>How I turned scattered data managers into navigable systems using virtual filesystems and POSIX commands.</description><content:encoded><![CDATA[<p>I had a bookmark manager. Then an ebook library manager. Then a chat history manager. Each started with the standard CRUD CLI:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">btk add https://example.com --tags python,tutorial
</span></span><span class="line"><span class="cl">btk list --tag python
</span></span><span class="line"><span class="cl">btk search <span class="s2">&#34;async&#34;</span>
</span></span><span class="line"><span class="cl">btk delete <span class="m">1234</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">ebk import book.pdf --author <span class="s2">&#34;Knuth&#34;</span>
</span></span><span class="line"><span class="cl">ebk list --author Knuth
</span></span><span class="line"><span class="cl">ebk search <span class="s2">&#34;algorithms&#34;</span>
</span></span></code></pre></div><p>This works fine until you have 10,000+ bookmarks organized with hierarchical tags like <code>programming/python/async</code>, <code>research/ml/transformers</code>, <code>work/clients/acme</code>. Your ebook library has similar structure. Your exported chat conversations from Claude, ChatGPT, and Copilot are piling up.</p>
<p>Traditional CRUD commands become unwieldy:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">btk list --tag programming/python/async/io --format json <span class="p">|</span> jq <span class="s1">&#39;.[].title&#39;</span>
</span></span><span class="line"><span class="cl">ebk list --category <span class="s2">&#34;Computer Science/Algorithms/Graph Theory&#34;</span> --limit <span class="m">50</span>
</span></span><span class="line"><span class="cl">ctk search <span class="s2">&#34;machine learning&#34;</span> --source ChatGPT --date-from 2024-01-01
</span></span></code></pre></div><p>Each command requires precise arguments. Each tool has different flag conventions. You can&rsquo;t <em>navigate</em> your data. You can only <em>query</em> it. And queries require knowing exactly what you&rsquo;re looking for.</p>
<h2 id="the-insight-everything-is-a-file">The insight: everything is a file</h2>
<p>When I have thousands of source files organized in directories, I don&rsquo;t run:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">list-files --path /src/components/auth --extension .tsx
</span></span></code></pre></div><p>I run:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="nb">cd</span> src/components/auth
</span></span><span class="line"><span class="cl">ls *.tsx
</span></span></code></pre></div><p>The difference matters. With a filesystem, I can navigate incrementally (<code>cd</code> from general to specific), explore (<code>ls</code> to see what&rsquo;s there), compose (<code>cat file | grep pattern | wc -l</code>), and use familiar tools (<code>find</code>, <code>grep</code>, <code>xargs</code>, pipes, redirection).</p>
<p>What if my bookmarks, ebooks, and chat histories were filesystems?</p>
<h2 id="the-pattern">The pattern</h2>
<p>Over the past year, I built six Python tools that all follow the same architecture:</p>
<table>
	<thead>
			<tr>
					<th>Tool</th>
					<th>Domain</th>
					<th>VFS Root Structure</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><strong>btk</strong></td>
					<td>Bookmarks</td>
					<td><code>/bookmarks/</code>, <code>/tags/</code>, <code>/recent/</code>, <code>/domains/</code>, <code>/unread/</code>, <code>/popular/</code></td>
			</tr>
			<tr>
					<td><strong>ebk</strong></td>
					<td>Ebook library</td>
					<td><code>/books/</code>, <code>/authors/</code>, <code>/series/</code>, <code>/subjects/</code>, <code>/recent/</code>, <code>/unread/</code></td>
			</tr>
			<tr>
					<td><strong>ctk</strong></td>
					<td>Chat conversations</td>
					<td><code>/conversations/</code>, <code>/sources/</code>, <code>/topics/</code>, <code>/starred/</code>, <code>/recent/</code></td>
			</tr>
			<tr>
					<td><strong>ghops</strong></td>
					<td>Git repositories</td>
					<td><code>/repos/</code>, <code>/languages/</code>, <code>/topics/</code>, <code>/stars/</code>, <code>/recent/</code></td>
			</tr>
			<tr>
					<td><strong>infinigram</strong></td>
					<td>N-gram models</td>
					<td><code>/datasets/</code>, <code>/models/</code>, <code>/corpora/</code></td>
			</tr>
			<tr>
					<td><strong>AlgoTree</strong></td>
					<td>Tree structures</td>
					<td><code>/nodes/</code>, <code>/paths/</code>, <code>/subtrees/</code></td>
			</tr>
	</tbody>
</table>
<p>Each tool provides:</p>
<ol>
<li>A stateless CLI for scripting: <code>btk bookmark add URL</code>, <code>ebk import book.pdf</code></li>
<li>An interactive shell with a virtual filesystem: <code>btk shell</code>, <code>ebk shell</code>, <code>ctk chat</code></li>
<li>POSIX-like commands: <code>cd</code>, <code>ls</code>, <code>pwd</code>, <code>cat</code>, <code>mv</code>, <code>cp</code>, <code>rm</code>, <code>find</code>, <code>grep</code></li>
<li>Unix pipeline support: most commands output JSONL by default for piping</li>
</ol>
<p>The interesting part is the shell.</p>
<h2 id="navigating-10000-bookmarks">Navigating 10,000 bookmarks</h2>
<link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/asciinema-player@3.7.0/dist/bundle/asciinema-player.min.css" />
<script src="https://cdn.jsdelivr.net/npm/asciinema-player@3.7.0/dist/bundle/asciinema-player.min.js"></script>
<div id="btk-demo"></div>
<script>
AsciinemaPlayer.create('btk.cast', document.getElementById('btk-demo'), {
  cols: 94,
  rows: 49,
  speed: 1,
  idleTimeLimit: 2,
  theme: 'monokai',
  poster: 'npt:0:06'
});
</script>
<p><em>Live recording captured with <a href="https://asciinema.org/">asciinema</a>. You can pause, copy text, and replay. The entire recording is 78KB of text.</em></p>
<h3 id="the-old-way">The old way</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">$ btk search <span class="s2">&#34;python async&#34;</span> --tag programming --limit <span class="m">10</span>
</span></span><span class="line"><span class="cl"><span class="c1"># Returns JSON blob... now what?</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">$ btk list --tag <span class="s2">&#34;programming/python/async&#34;</span>
</span></span><span class="line"><span class="cl"><span class="c1"># Hope I remembered the exact tag path</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">$ btk bookmark get <span class="m">4095</span> --format json <span class="p">|</span> jq <span class="s1">&#39;.tags&#39;</span>
</span></span><span class="line"><span class="cl"><span class="c1"># One bookmark at a time, verbose</span>
</span></span></code></pre></div><h3 id="the-vfs-way">The VFS way</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">$ btk shell
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">      __    __  __
</span></span><span class="line"><span class="cl">     / /_  / /_/ /__
</span></span><span class="line"><span class="cl">    / __ <span class="se">\/</span> __/ //_/
</span></span><span class="line"><span class="cl">   / /_/ / /_/ ,&lt;
</span></span><span class="line"><span class="cl">  /_.___/<span class="se">\_</span>_/_/<span class="p">|</span>_<span class="p">|</span>  v0.7.1
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">  Bookmark Toolkit - Virtual Filesystem Shell
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">btk:/$ ls
</span></span><span class="line"><span class="cl">bookmarks/    <span class="o">(</span>10,247<span class="o">)</span>    All bookmarks
</span></span><span class="line"><span class="cl">tags/                    Tag hierarchy
</span></span><span class="line"><span class="cl">recent/                  Time-based navigation
</span></span><span class="line"><span class="cl">domains/                 Browse by domain
</span></span><span class="line"><span class="cl">unread/       <span class="o">(</span>2,431<span class="o">)</span>    Never visited
</span></span><span class="line"><span class="cl">popular/      <span class="o">(</span>100<span class="o">)</span>      Most visited
</span></span><span class="line"><span class="cl">broken/       <span class="o">(</span>14<span class="o">)</span>       Dead links
</span></span><span class="line"><span class="cl">starred/      <span class="o">(</span>156<span class="o">)</span>      Starred bookmarks
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">btk:/$ <span class="nb">cd</span> tags/programming/python
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">btk:/tags/programming/python$ ls
</span></span><span class="line"><span class="cl">async/        <span class="o">(</span>87<span class="o">)</span>
</span></span><span class="line"><span class="cl">web/          <span class="o">(</span>124<span class="o">)</span>
</span></span><span class="line"><span class="cl">data/         <span class="o">(</span>156<span class="o">)</span>
</span></span><span class="line"><span class="cl">ml/           <span class="o">(</span>89<span class="o">)</span>
</span></span><span class="line"><span class="cl">testing/      <span class="o">(</span>45<span class="o">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">btk:/tags/programming/python/async$ ls <span class="p">|</span> head -5
</span></span><span class="line"><span class="cl"><span class="m">4095</span>  <span class="m">5234</span>  <span class="m">6012</span>  <span class="m">6891</span>  <span class="m">7234</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">btk:/tags/programming/python/async$ cat 4095/title
</span></span><span class="line"><span class="cl">Real Python - Async IO in Python: A Complete Walkthrough
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">btk:/tags/programming/python/async$ cat 4095/url
</span></span><span class="line"><span class="cl">https://realpython.com/async-io-python/
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">btk:/tags/programming/python/async$ star <span class="m">4095</span>
</span></span><span class="line"><span class="cl">★ Starred bookmark <span class="c1">#4095</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">btk:/tags/programming/python/async$ <span class="nb">cd</span> /recent/today/added
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">btk:/recent/today/added$ ls
</span></span><span class="line"><span class="cl"><span class="m">8901</span>  <span class="m">8902</span>  <span class="m">8903</span>  <span class="m">8904</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">btk:/recent/today/added$ tag <span class="m">8901</span> <span class="m">8902</span> <span class="m">8903</span> todo
</span></span><span class="line"><span class="cl">✓ Tagged <span class="m">3</span> bookmarks
</span></span></code></pre></div><p>No flag memorization. Incremental exploration. Context-aware commands. It&rsquo;s just directories and files.</p>
<h2 id="same-pattern-different-data">Same pattern, different data</h2>
<h3 id="ebooks-ebk">Ebooks (ebk)</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ebk:/$ <span class="nb">cd</span> subjects/Computer<span class="se">\ </span>Science/Algorithms
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">ebk:/subjects/Computer Science/Algorithms$ ls
</span></span><span class="line"><span class="cl">Introduction to Algorithms.pdf
</span></span><span class="line"><span class="cl">The Algorithm Design Manual.pdf
</span></span><span class="line"><span class="cl">Algorithms <span class="o">(</span>Sedgewick<span class="o">)</span>.pdf
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">ebk:/subjects/Computer Science/Algorithms$ cat <span class="s2">&#34;Introduction to Algorithms.pdf&#34;</span>/metadata
</span></span><span class="line"><span class="cl">Title: Introduction to Algorithms
</span></span><span class="line"><span class="cl">Authors: Cormen, Leiserson, Rivest, Stein
</span></span><span class="line"><span class="cl">ISBN: 978-0262033848
</span></span><span class="line"><span class="cl">Pages: <span class="m">1312</span>
</span></span><span class="line"><span class="cl">Rating: 5/5
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">ebk:/subjects/Computer Science/Algorithms$ rate * <span class="m">5</span>
</span></span><span class="line"><span class="cl">✓ Rated <span class="m">3</span> books
</span></span></code></pre></div><h3 id="chat-history-ctk">Chat history (ctk)</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ctk:/$ <span class="nb">cd</span> sources/ChatGPT
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">ctk:/sources/ChatGPT$ ls <span class="p">|</span> wc -l
</span></span><span class="line"><span class="cl"><span class="m">423</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">ctk:/sources/ChatGPT$ <span class="nb">cd</span> /topics/machine-learning
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">ctk:/topics/machine-learning$ ls
</span></span><span class="line"><span class="cl">conv_a1b2c3  conv_d4e5f6  conv_g7h8i9
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">ctk:/topics/machine-learning$ show conv_a1b2c3
</span></span><span class="line"><span class="cl"><span class="o">[</span>Shows conversation tree with messages<span class="o">]</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">ctk:/topics/machine-learning$ star conv_a1b2c3
</span></span><span class="line"><span class="cl">★ Starred conversation
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">ctk:/topics/machine-learning$ <span class="nb">cd</span> /starred
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">ctk:/starred$ <span class="nb">export</span> --format markdown &gt; starred_ml_convos.md
</span></span><span class="line"><span class="cl">✓ Exported <span class="m">5</span> starred conversations to starred_ml_convos.md
</span></span></code></pre></div><h3 id="git-repositories-ghops">Git repositories (ghops)</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ghops:/$ <span class="nb">cd</span> languages/Python
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">ghops:/languages/Python$ ls
</span></span><span class="line"><span class="cl">btk/  ebk/  ctk/  ghops/  infinigram/  AlgoTree/
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">ghops:/languages/Python$ <span class="nb">cd</span> btk
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">ghops:/languages/Python/btk$ status
</span></span><span class="line"><span class="cl">Branch: master
</span></span><span class="line"><span class="cl">Commits ahead: <span class="m">0</span>
</span></span><span class="line"><span class="cl">Uncommitted changes: <span class="m">0</span>
</span></span><span class="line"><span class="cl">Last commit: Release v0.7.1 <span class="o">(</span><span class="m">2</span> days ago<span class="o">)</span>
</span></span></code></pre></div><h2 id="what-makes-it-work">What makes it work</h2>
<p>After building six of these, the patterns are clear.</p>
<h3 id="stateless-cli--stateful-shell">Stateless CLI + stateful shell</h3>
<p>Every tool has both interfaces. The CLI is for automation and scripting. The shell is for humans.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># CLI (stateless, scriptable)</span>
</span></span><span class="line"><span class="cl">btk bookmark add https://example.com --tags python,tutorial
</span></span><span class="line"><span class="cl">ebk import book.pdf --author <span class="s2">&#34;Knuth&#34;</span>
</span></span><span class="line"><span class="cl">ctk <span class="nb">export</span> --format jsonl &gt; training.jsonl
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Shell (stateful, exploratory)</span>
</span></span><span class="line"><span class="cl">btk shell
</span></span><span class="line"><span class="cl"><span class="nb">cd</span> tags/python
</span></span><span class="line"><span class="cl">star *
</span></span></code></pre></div><h3 id="dynamic-virtual-directories">Dynamic virtual directories</h3>
<p>Traditional filesystems are static. The VFS exposes computed views:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">btk:/$ ls
</span></span><span class="line"><span class="cl">unread/       <span class="o">(</span>2,431<span class="o">)</span>    <span class="c1"># SELECT * WHERE visit_count = 0</span>
</span></span><span class="line"><span class="cl">popular/      <span class="o">(</span>100<span class="o">)</span>      <span class="c1"># SELECT * ORDER BY visit_count DESC LIMIT 100</span>
</span></span><span class="line"><span class="cl">broken/       <span class="o">(</span>14<span class="o">)</span>       <span class="c1"># SELECT * WHERE reachable = false</span>
</span></span><span class="line"><span class="cl">recent/today/added/      <span class="c1"># SELECT * WHERE added &gt;= TODAY</span>
</span></span></code></pre></div><p>These &ldquo;directories&rdquo; don&rsquo;t exist on disk. They&rsquo;re queries. But they feel like directories, which is the point.</p>
<h3 id="context-aware-commands">Context-aware commands</h3>
<p>Commands understand where you are:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">btk:/bookmarks/4095$ cat title
</span></span><span class="line"><span class="cl"><span class="c1"># Shows title of bookmark 4095</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">btk:/tags/python$ star *
</span></span><span class="line"><span class="cl"><span class="c1"># Stars all Python-tagged bookmarks</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">btk:/recent/today/added$ tag * review
</span></span><span class="line"><span class="cl"><span class="c1"># Tags today&#39;s additions</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">btk:/broken$ rm *
</span></span><span class="line"><span class="cl"><span class="c1"># Removes all broken bookmarks</span>
</span></span></code></pre></div><p>The current path becomes implicit context. No need to repeat IDs or filters.</p>
<h3 id="hierarchical-tags-as-directories">Hierarchical tags as directories</h3>
<p>This is the killer feature. Hierarchical tags map directly to filesystem paths.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Tag with hierarchy</span>
</span></span><span class="line"><span class="cl">btk tag <span class="m">4095</span> programming/python/async/io
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Navigate the hierarchy</span>
</span></span><span class="line"><span class="cl">btk:/$ <span class="nb">cd</span> tags/programming
</span></span><span class="line"><span class="cl">btk:/tags/programming$ ls
</span></span><span class="line"><span class="cl">python/  javascript/  rust/  go/
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">btk:/tags/programming$ <span class="nb">cd</span> python/async
</span></span><span class="line"><span class="cl">btk:/tags/programming/python/async$ ls
</span></span><span class="line"><span class="cl">io/  frameworks/  patterns/
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Bulk operations on a hierarchy</span>
</span></span><span class="line"><span class="cl">btk:/tags/programming/python$ star */advanced/*
</span></span></code></pre></div><p>Compare with flat tags: <code>python</code>, <code>python-async</code>, <code>python-async-io</code>, <code>python-web</code>, <code>python-web-django</code>. Hierarchy gives you free navigation and organization.</p>
<h3 id="jsonl-by-default">JSONL by default</h3>
<p>All commands output newline-delimited JSON by default:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Pipe to jq, grep, awk, anything</span>
</span></span><span class="line"><span class="cl">btk list <span class="p">|</span> jq <span class="s1">&#39;select(.stars == true)&#39;</span>
</span></span><span class="line"><span class="cl">ebk status <span class="p">|</span> grep <span class="s2">&#34;rating: 5&#34;</span>
</span></span><span class="line"><span class="cl">ctk search <span class="s2">&#34;python&#34;</span> <span class="p">|</span> jq <span class="s1">&#39;.id&#39;</span> <span class="p">|</span> xargs ctk <span class="nb">export</span> --ids
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Pretty-print for humans</span>
</span></span><span class="line"><span class="cl">btk list --pretty
</span></span></code></pre></div><p>This makes the tools composable with the Unix ecosystem. JSONL is streamable, appendable, grepable, and robust (one malformed record doesn&rsquo;t break the file).</p>
<h2 id="implementation">Implementation</h2>
<p>The architecture is straightforward. Each tool shares the same stack:</p>
<ol>
<li><strong>Database layer</strong> (SQLAlchemy + SQLite): normalized schema, FTS5 search, efficient indexing</li>
<li><strong>VFS layer</strong> (Python <code>cmd.Cmd</code>): path parsing, context detection, command routing</li>
<li><strong>Command layer</strong>: context-aware implementations of <code>do_ls()</code>, <code>do_cd()</code>, <code>do_cat()</code>, etc.</li>
<li><strong>CLI layer</strong> (Typer/argparse): stateless commands for scripting, JSONL output</li>
</ol>
<p>The core of it is context detection:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="k">def</span> <span class="nf">_get_context</span><span class="p">(</span><span class="bp">self</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;&#34;&#34;Determine what &#39;directory&#39; we&#39;re in.&#34;&#34;&#34;</span>
</span></span><span class="line"><span class="cl">    <span class="k">if</span> <span class="bp">self</span><span class="o">.</span><span class="n">current_path</span> <span class="o">==</span> <span class="s2">&#34;/&#34;</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="p">{</span><span class="s1">&#39;type&#39;</span><span class="p">:</span> <span class="s1">&#39;root&#39;</span><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="n">parts</span> <span class="o">=</span> <span class="bp">self</span><span class="o">.</span><span class="n">current_path</span><span class="o">.</span><span class="n">strip</span><span class="p">(</span><span class="s1">&#39;/&#39;</span><span class="p">)</span><span class="o">.</span><span class="n">split</span><span class="p">(</span><span class="s1">&#39;/&#39;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">if</span> <span class="n">parts</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">==</span> <span class="s1">&#39;tags&#39;</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">        <span class="c1"># /tags/programming/python</span>
</span></span><span class="line"><span class="cl">        <span class="n">tag_path</span> <span class="o">=</span> <span class="s1">&#39;/&#39;</span><span class="o">.</span><span class="n">join</span><span class="p">(</span><span class="n">parts</span><span class="p">[</span><span class="mi">1</span><span class="p">:])</span>
</span></span><span class="line"><span class="cl">        <span class="n">bookmarks</span> <span class="o">=</span> <span class="bp">self</span><span class="o">.</span><span class="n">db</span><span class="o">.</span><span class="n">filter_by_tag_prefix</span><span class="p">(</span><span class="n">tag_path</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="p">{</span><span class="s1">&#39;type&#39;</span><span class="p">:</span> <span class="s1">&#39;tag&#39;</span><span class="p">,</span> <span class="s1">&#39;tag&#39;</span><span class="p">:</span> <span class="n">tag_path</span><span class="p">,</span> <span class="s1">&#39;bookmarks&#39;</span><span class="p">:</span> <span class="n">bookmarks</span><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">elif</span> <span class="n">parts</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">==</span> <span class="s1">&#39;recent&#39;</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">        <span class="c1"># /recent/today/added</span>
</span></span><span class="line"><span class="cl">        <span class="n">period</span> <span class="o">=</span> <span class="n">parts</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span>  <span class="c1"># &#39;today&#39;</span>
</span></span><span class="line"><span class="cl">        <span class="n">activity</span> <span class="o">=</span> <span class="n">parts</span><span class="p">[</span><span class="mi">2</span><span class="p">]</span> <span class="k">if</span> <span class="nb">len</span><span class="p">(</span><span class="n">parts</span><span class="p">)</span> <span class="o">&gt;</span> <span class="mi">2</span> <span class="k">else</span> <span class="s1">&#39;visited&#39;</span>
</span></span><span class="line"><span class="cl">        <span class="n">bookmarks</span> <span class="o">=</span> <span class="n">filter_by_time_and_activity</span><span class="p">(</span><span class="n">period</span><span class="p">,</span> <span class="n">activity</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="p">{</span><span class="s1">&#39;type&#39;</span><span class="p">:</span> <span class="s1">&#39;recent_activity&#39;</span><span class="p">,</span> <span class="s1">&#39;period&#39;</span><span class="p">:</span> <span class="n">period</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                <span class="s1">&#39;activity&#39;</span><span class="p">:</span> <span class="n">activity</span><span class="p">,</span> <span class="s1">&#39;bookmarks&#39;</span><span class="p">:</span> <span class="n">bookmarks</span><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">elif</span> <span class="n">parts</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">==</span> <span class="s1">&#39;unread&#39;</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">        <span class="c1"># /unread - smart collection</span>
</span></span><span class="line"><span class="cl">        <span class="n">bookmarks</span> <span class="o">=</span> <span class="bp">self</span><span class="o">.</span><span class="n">db</span><span class="o">.</span><span class="n">filter</span><span class="p">(</span><span class="n">visit_count</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="p">{</span><span class="s1">&#39;type&#39;</span><span class="p">:</span> <span class="s1">&#39;smart_collection&#39;</span><span class="p">,</span> <span class="s1">&#39;name&#39;</span><span class="p">:</span> <span class="s1">&#39;unread&#39;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                <span class="s1">&#39;bookmarks&#39;</span><span class="p">:</span> <span class="n">bookmarks</span><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">elif</span> <span class="n">parts</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">==</span> <span class="s1">&#39;bookmarks&#39;</span> <span class="ow">and</span> <span class="nb">len</span><span class="p">(</span><span class="n">parts</span><span class="p">)</span> <span class="o">==</span> <span class="mi">2</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">        <span class="c1"># /bookmarks/4095</span>
</span></span><span class="line"><span class="cl">        <span class="n">bookmark_id</span> <span class="o">=</span> <span class="nb">int</span><span class="p">(</span><span class="n">parts</span><span class="p">[</span><span class="mi">1</span><span class="p">])</span>
</span></span><span class="line"><span class="cl">        <span class="n">bookmark</span> <span class="o">=</span> <span class="bp">self</span><span class="o">.</span><span class="n">db</span><span class="o">.</span><span class="n">get</span><span class="p">(</span><span class="n">bookmark_id</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="p">{</span><span class="s1">&#39;type&#39;</span><span class="p">:</span> <span class="s1">&#39;bookmark&#39;</span><span class="p">,</span> <span class="s1">&#39;bookmark_id&#39;</span><span class="p">:</span> <span class="n">bookmark_id</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                <span class="s1">&#39;bookmark&#39;</span><span class="p">:</span> <span class="n">bookmark</span><span class="p">}</span>
</span></span></code></pre></div><p>Once you know the context, commands adapt:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="k">def</span> <span class="nf">do_ls</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">args</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;&#34;&#34;List items in current directory.&#34;&#34;&#34;</span>
</span></span><span class="line"><span class="cl">    <span class="n">context</span> <span class="o">=</span> <span class="bp">self</span><span class="o">.</span><span class="n">_get_context</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">if</span> <span class="n">context</span><span class="p">[</span><span class="s1">&#39;type&#39;</span><span class="p">]</span> <span class="o">==</span> <span class="s1">&#39;root&#39;</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">        <span class="bp">self</span><span class="o">.</span><span class="n">_ls_root</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">    <span class="k">elif</span> <span class="n">context</span><span class="p">[</span><span class="s1">&#39;type&#39;</span><span class="p">]</span> <span class="o">==</span> <span class="s1">&#39;tag&#39;</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">        <span class="bp">self</span><span class="o">.</span><span class="n">_ls_tag</span><span class="p">(</span><span class="n">context</span><span class="p">[</span><span class="s1">&#39;tag&#39;</span><span class="p">],</span> <span class="n">context</span><span class="p">[</span><span class="s1">&#39;bookmarks&#39;</span><span class="p">])</span>
</span></span><span class="line"><span class="cl">    <span class="k">elif</span> <span class="n">context</span><span class="p">[</span><span class="s1">&#39;type&#39;</span><span class="p">]</span> <span class="o">==</span> <span class="s1">&#39;recent_activity&#39;</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">        <span class="bp">self</span><span class="o">.</span><span class="n">_ls_bookmarks</span><span class="p">(</span><span class="n">context</span><span class="p">[</span><span class="s1">&#39;bookmarks&#39;</span><span class="p">])</span>
</span></span><span class="line"><span class="cl">    <span class="k">elif</span> <span class="n">context</span><span class="p">[</span><span class="s1">&#39;type&#39;</span><span class="p">]</span> <span class="o">==</span> <span class="s1">&#39;smart_collection&#39;</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">        <span class="bp">self</span><span class="o">.</span><span class="n">_ls_collection</span><span class="p">(</span><span class="n">context</span><span class="p">[</span><span class="s1">&#39;name&#39;</span><span class="p">],</span> <span class="n">context</span><span class="p">[</span><span class="s1">&#39;bookmarks&#39;</span><span class="p">])</span>
</span></span><span class="line"><span class="cl">    <span class="k">elif</span> <span class="n">context</span><span class="p">[</span><span class="s1">&#39;type&#39;</span><span class="p">]</span> <span class="o">==</span> <span class="s1">&#39;bookmark&#39;</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">        <span class="bp">self</span><span class="o">.</span><span class="n">_ls_bookmark</span><span class="p">(</span><span class="n">context</span><span class="p">[</span><span class="s1">&#39;bookmark&#39;</span><span class="p">])</span>
</span></span></code></pre></div><p>Context detection + polymorphic commands. That&rsquo;s the whole trick.</p>
<h2 id="why-it-matters">Why it matters</h2>
<p>Traditional CLIs force you to remember exact syntax, construct precise queries, and process JSON blobs. VFS interfaces let you explore incrementally, discover what exists, and operate on context.</p>
<p>It matches how humans think: spatial navigation over query construction. We already know filesystems. We already know <code>cd</code>, <code>ls</code>, <code>grep</code>, <code>find</code>. The VFS pattern lets you reuse that knowledge for any hierarchical data.</p>
<h2 id="the-tools">The tools</h2>
<p>All six are open source and on PyPI:</p>
<ul>
<li><strong>btk</strong> (Bookmark Toolkit): <a href="https://github.com/queelius/btk">github.com/queelius/btk</a></li>
<li><strong>ebk</strong> (eBook Manager): <a href="https://github.com/queelius/ebk">github.com/queelius/ebk</a></li>
<li><strong>ctk</strong> (Conversation Toolkit): <a href="https://github.com/queelius/ctk">github.com/queelius/ctk</a></li>
<li><strong>ghops</strong> (Git Repository Manager): <a href="https://github.com/queelius/ghops">github.com/queelius/ghops</a></li>
<li><strong>infinigram</strong> (N-gram Models): <a href="https://github.com/queelius/infinigram">github.com/queelius/infinigram</a></li>
<li><strong>AlgoTree</strong> (Tree Structures): <a href="https://github.com/queelius/AlgoTree">github.com/queelius/AlgoTree</a></li>
</ul>
<p>If you build CLI tools for hierarchical data, consider the VFS pattern. Your users already know <code>cd</code> and <code>ls</code>. Why make them learn 47 flags?</p>
<hr>
<h2 id="technical-notes">Technical notes</h2>
<h3 id="recording-shell-sessions">Recording shell sessions</h3>
<p>The interactive demo uses <a href="https://asciinema.org/">asciinema</a>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">asciinema rec btk-demo.cast
</span></span><span class="line"><span class="cl">btk shell
</span></span><span class="line"><span class="cl"><span class="c1"># ... do your demo ...</span>
</span></span><span class="line"><span class="cl"><span class="nb">exit</span>
</span></span></code></pre></div><p>The <code>.cast</code> file is pure text (JSON), usually a few KB. You get interactive playback via <a href="https://github.com/asciinema/asciinema-player">asciinema-player</a>, copy-pasteable text, and 78KB for several minutes of demo instead of megabytes for a GIF or video. This is better than screen recordings for terminal demos.</p>
<h3 id="test-coverage">Test coverage</h3>
<p>All six tools have comprehensive test suites:</p>
<ul>
<li><strong>btk</strong>: 515 tests (53% shell coverage, 23% CLI coverage)</li>
<li><strong>ghops</strong>: 138 tests, 86% coverage</li>
<li><strong>infinigram</strong>: 36 tests with benchmarks</li>
<li><strong>AlgoTree</strong>: 197 tests, 86% coverage</li>
</ul>
<p>The VFS pattern is highly testable. Each component (path parsing, context detection, command handlers) is isolated and pure. Mock the database, test the rest.</p>
]]></content:encoded><category>unix-philosophy</category><category>cli-tools</category><category>python</category><category>data-management</category><category>virtual-filesystem</category><category>software-design</category><category>open-source</category></item><item><title>Why I Build Comprehensively in Open Source</title><link>https://metafunctor.com/post/2024-11-open-source-legacy/</link><pubDate>Fri, 15 Nov 2024 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-11-open-source-legacy/</guid><description>On building comprehensive open source software as value imprinting at scale, reproducible science, and leaving intellectual legacy under terminal constraints.</description><content:encoded><![CDATA[<p>I maintain 50+ open source repositories. Every one has documentation, tests, examples, and clear architecture.</p>
<p>People ask: &ldquo;Why spend so much time on free software when you have stage 4 cancer?&rdquo;</p>
<p>The question misunderstands what I&rsquo;m doing. This isn&rsquo;t charity. It&rsquo;s value imprinting at scale.</p>
<h2 id="reproducible-science">Reproducible Science</h2>
<p>Science requires reproducibility. If you publish computational results without releasing code, you&rsquo;ve made unverifiable claims. Full stop.</p>
<p>Every paper I publish includes an open source implementation, documented dependencies, reproducible examples, test suites, and clear installation instructions. This isn&rsquo;t optional. It&rsquo;s methodological rigor. When someone reads my reliability analysis paper and wants to verify my results, they can clone the repo, install dependencies, run the code, and reproduce the figures. That&rsquo;s how science should work.</p>
<h2 id="continuability">Continuability</h2>
<p>With stage 4 cancer, I think explicitly about what happens if I&rsquo;m not here to maintain this.</p>
<p>For work to be continuable, it needs clear architecture (someone else must understand the design decisions), comprehensive docs (explain not just what but why), test suites (demonstrate usage and validate correctness), clean dependencies (no fragile undocumented requirements), examples (show intent through working code), and open licensing (remove legal barriers to continuation).</p>
<p>I&rsquo;m not building for myself. I&rsquo;m building for whoever comes next.</p>
<h2 id="value-imprinting">Value Imprinting</h2>
<p>Code embodies values. Every API decision reflects beliefs about what should be easy vs. hard, what should be explicit vs. implicit, what should be safe by default, what complexity is worth exposing.</p>
<p>My libraries encode transparency over magic, composition over monoliths, clarity over cleverness, safety by default. These values propagate. Someone uses my library, internalizes its principles, applies them elsewhere.</p>
<p>Code is how values scale beyond individual lifetimes.</p>
<h2 id="usefulness-to-others">Usefulness to Others</h2>
<p>I&rsquo;ve benefited enormously from open source. Libraries that solved problems I couldn&rsquo;t. Documentation that taught me techniques. Examples that clarified concepts. Tools that accelerated my research.</p>
<p>Contributing back isn&rsquo;t obligation. It&rsquo;s participating in a gift economy. When you solve a problem well and publish it, thousands of people don&rsquo;t have to solve it again.</p>
<h2 id="craftsmanship">Craftsmanship</h2>
<p>Well-crafted code is intrinsically beautiful. Clean abstractions that feel inevitable. APIs that make complex operations simple. Documentation explaining why, not just what. Tests that serve as executable specifications. Code structured for human comprehension.</p>
<p>I maintain libraries obsessively because craftsmanship is rewarding. The cancer doesn&rsquo;t change this. If anything, it intensifies it. Do work worth doing while you can.</p>
<h2 id="why-comprehensive">Why &ldquo;Comprehensive&rdquo;?</h2>
<p>Why not just throw code over the wall?</p>
<p>Because incomplete open source often wastes more time than it saves. Undocumented code: users spend hours figuring out what you could have explained in minutes. Untested code: breaks in production, creates technical debt. Unclear dependencies: installation becomes archaeological excavation. No examples: users can&rsquo;t tell if they&rsquo;re using it correctly. Ambiguous licensing: legal uncertainty blocks adoption.</p>
<p>Comprehensive open source respects users&rsquo; time.</p>
<p>When I publish, installation works first try, examples run without modification, the API is documented with clear examples, tests demonstrate usage patterns, architecture is explained, license is unambiguous, dependencies are specified, and contribution guidelines are clear.</p>
<p>This takes effort. But it&rsquo;s the difference between publishing code and publishing tools.</p>
<h2 id="the-cost">The Cost</h2>
<p>Comprehensive open source is expensive. Documenting takes longer than coding. Examples and tests require effort. Polish competes with new features.</p>
<p>But these costs buy reproducibility, continuability, usefulness, value propagation, and quality. For work I want to outlive me, this is the right tradeoff.</p>
<h2 id="stage-4-changes-the-calculation">Stage 4 Changes the Calculation</h2>
<p>With uncertain time horizons, every project gets filtered: &ldquo;If this is my last contribution to this domain, is it worth it?&rdquo; Documentation becomes critical because I might not be here to explain. Completeness matters more because half-finished work helps no one. Open source is essential because proprietary work dies with me.</p>
<p>Cancer doesn&rsquo;t make me publish less. It makes me publish more carefully.</p>
<h2 id="examples-from-my-work">Examples From My Work</h2>
<p>My statistical libraries in R: every function documented with mathematical background, examples, and tests. Someone can pick this up and understand both the code and the theory.</p>
<p>My cryptographic tools: type systems enforce security properties. Documentation explains threat models. Tests demonstrate correct usage. Someone can audit and continue this work.</p>
<p>My network analysis code: abstracts over graph representations. Works on any structure satisfying the graph interface. Examples show how to extend it. Someone can build on this foundation.</p>
<p>All structured for continuation without me.</p>
<h2 id="the-long-view">The Long View</h2>
<p>Well-documented, clearly-architected, thoroughly-tested open source has staying power.</p>
<p>Someone in 2030 might use a library I published in 2024, if it&rsquo;s well-designed enough to remain relevant, well-documented enough to understand, well-tested enough to trust, and well-licensed enough to modify.</p>
<p>That&rsquo;s the goal: work that remains useful after I&rsquo;m gone.</p>
<h2 id="what-this-means-practically">What This Means Practically</h2>
<p>Every commit I make asks: Is this design decision explained? Are the tests comprehensive? Is the documentation current? Can someone else continue this? Does this encode the values I want to propagate?</p>
<p>This discipline is expensive. But legacy is about what persists, not what&rsquo;s easiest.</p>
<h2 id="the-paradox">The Paradox</h2>
<p>Cancer gives you less time but clarifies what that time is for.</p>
<p>I&rsquo;m more productive now than before diagnosis. Not because I work longer hours (I don&rsquo;t, treatment limits that). Because I waste less time on things that don&rsquo;t matter. Code without docs? Not worth publishing. Projects I&rsquo;ll abandon? Don&rsquo;t start them. Work I can&rsquo;t explain? Figure it out or skip it. Tools no one can use? Not worth building.</p>
<p>The filter is simple: will this remain useful and usable after I&rsquo;m gone?</p>
<p>Most things fail that test. The few that pass get everything I have.</p>
<h2 id="open-source-as-archive">Open Source as Archive</h2>
<p>My comprehensive open source work is fundamentally an archival project. Document my reasoning. Encode my values. Share my tools. Make my work reproducible. Enable continuation.</p>
<p>Not memorial. Functional archive.</p>
<p>If someone in 2030 wants to reproduce my reliability analysis results, understand my cryptographic designs, build on my network analysis methods, or continue my statistical computing work, they can. The code exists. The documentation explains it. The tests validate it. The license permits it.</p>
<p>That&rsquo;s what I&rsquo;m building toward.</p>
<h2 id="for-other-researchers">For Other Researchers</h2>
<p>If you&rsquo;re doing computational work: publish your code. Not someday. Now. Document it properly. Explain why, not just what. Test it thoroughly (tests are specification and validation). License it clearly (MIT or Apache 2.0 removes barriers). Make it reproducible (someone should be able to run your code and get your results).</p>
<p>This isn&rsquo;t extra work. It&rsquo;s the minimum standard for computational science.</p>
<h2 id="the-bottom-line">The Bottom Line</h2>
<p>I build comprehensively in open source because science demands reproducibility, legacy requires continuability, values propagate through code, others benefit from shared tools, craftsmanship is intrinsically rewarding, and cancer makes priorities clear.</p>
<p>Every repository is an attempt to build something that outlasts me. Not for immortality. For usefulness.</p>
]]></content:encoded><category>open-source</category><category>legacy</category><category>reproducible-science</category><category>philosophy</category><category>software-design</category></item><item><title>Building R Packages for Statistical Inference</title><link>https://metafunctor.com/post/2020-05-statistical-computing-r/</link><pubDate>Fri, 08 May 2020 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2020-05-statistical-computing-r/</guid><description>I'm building R packages for reliability analysis, not just using other people's. R's strengths for statistical computing are real, and building packages forces you to understand the theory.</description><content:encoded><![CDATA[<p>The best thing about this math degree so far: I&rsquo;m not just <em>using</em> R packages anymore. I&rsquo;m building them.</p>
<h2 id="why-r">Why R</h2>
<p>R was built for statistics. Not adapted to it, not retrofitted. The language maps statistical concepts naturally: distributions are first-class, formulas are a data type, data frames are the default structure. CRAN has decades of mature, well-tested statistical libraries.</p>
<p>And RMarkdown lets me write code, math, and prose in one document. For statistical work, that matters. A derivation that lives next to the code that implements it is worth more than either alone.</p>
<h2 id="what-im-building">What I&rsquo;m Building</h2>
<p>R packages for reliability analysis. Specifically:</p>
<p><strong>Maximum likelihood estimation</strong> for series systems with masked failure data. A system fails, you know <em>that</em> it failed, but not <em>which</em> component caused it. The likelihood function for this scenario is non-trivial, and the existing tools don&rsquo;t handle it well.</p>
<p><strong>Bootstrap confidence intervals</strong> for reliability metrics that don&rsquo;t have closed-form variance expressions. When you can&rsquo;t derive the variance analytically, you resample.</p>
<p><strong>Survival analysis tools</strong> for right-censored Weibull data. Components are still running when observation ends. You have to account for what you didn&rsquo;t see.</p>
<h2 id="how-i-build-them">How I Build Them</h2>
<p>I treat package development like API design:</p>
<ul>
<li>Functions do one thing</li>
<li>Small functions compose into larger workflows</li>
<li>Every function has examples and mathematical background in the docs</li>
<li>Automated tests, not just manual checks</li>
<li>Vignettes that walk through complete analyses</li>
</ul>
<p>The goal is that someone reading the vignette learns both the method and the implementation.</p>
<h2 id="why-open-source">Why Open Source</h2>
<p>Statistical methods that aren&rsquo;t implemented might as well not exist. If I publish a paper claiming a method works, the code should be available for anyone to check. Publishing clean, documented R packages means my results are reproducible, my methods are auditable, and other people can build on them without starting from scratch.</p>
<p>These packages will eventually form part of my thesis. Building the tools while learning the theory forces me to understand both more deeply than either alone.</p>
]]></content:encoded><category>R</category><category>statistics</category><category>statistical-computing</category><category>open-source</category></item><item><title>Why Open Source Matters for Reproducible Science</title><link>https://metafunctor.com/post/2018-09-open-source-reproducible-science/</link><pubDate>Tue, 18 Sep 2018 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2018-09-open-source-reproducible-science/</guid><description>Code is a scientific artifact. If you don't publish it, you're hiding your methodology.</description><content:encoded><![CDATA[<p>I develop almost everything in open source. People sometimes ask why I spend so much time on documentation, examples, and polish for free software.</p>
<p>The answer is simple: science should be reproducible, and code is increasingly central to scientific claims.</p>
<h2 id="the-reproducibility-crisis">The Reproducibility Crisis</h2>
<p>Academia has a problem. Published papers often cannot be reproduced. The reasons are mundane:</p>
<ul>
<li>Methods described too vaguely</li>
<li>Data not available</li>
<li>Code never released</li>
<li>Dependencies undocumented</li>
<li>Computational environment not preserved</li>
</ul>
<p>This is not just inefficient. It undermines the scientific method. A result you cannot reproduce is not a result. It is an anecdote.</p>
<h2 id="code-as-scientific-artifact">Code as Scientific Artifact</h2>
<p>When your research involves computation (and whose doesn&rsquo;t these days?), your code <em>is</em> part of your methodology. Hiding it is like a biologist refusing to describe their experimental protocol.</p>
<p>Open source is not charity. It is scientific rigor.</p>
<h2 id="why-i-document-obsessively">Why I Document Obsessively</h2>
<p>Every library I publish includes:</p>
<ul>
<li>Clear installation instructions</li>
<li>Reproducible examples</li>
<li>API documentation</li>
<li>Tests that demonstrate usage</li>
<li>Version-controlled history showing evolution</li>
</ul>
<p>This takes time. But it means someone in 2028 can:</p>
<ul>
<li>Understand what I did</li>
<li>Reproduce my results</li>
<li>Build on my work</li>
<li>Find my errors</li>
</ul>
<p>That last point matters. I want people to find my errors. That is how science works.</p>
<h2 id="the-broader-point">The Broader Point</h2>
<p>Open source accelerates science by enabling replication, facilitating collaboration, preventing redundant work, and building cumulative knowledge. None of this works if the code stays on your laptop.</p>
<p>I will keep publishing everything. Not for recognition, but because science is a collective enterprise that only works if we show our work.</p>
]]></content:encoded><category>open-source</category><category>reproducible-science</category><category>research</category><category>philosophy</category></item></channel></rss>