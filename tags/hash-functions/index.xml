<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:media="http://search.yahoo.com/mrss/"><channel><title>Hash-Functions on metafunctor</title><link>https://metafunctor.com/tags/hash-functions/</link><description>Research Engineer &amp; Computer Scientist - Machine Learning, Statistical Computing, Open Source Development</description><language>en-us</language><managingEditor>lex@metafunctor.com (Alex Towell)</managingEditor><webMaster>lex@metafunctor.com (Alex Towell)</webMaster><lastBuildDate>Thu, 09 Jul 2026 08:45:06 -0500</lastBuildDate><atom:link href="https://metafunctor.com/tags/hash-functions/" rel="self" type="application/rss+xml"/><image><url>https://metafunctor.com/images/og-image.jpg</url><title>metafunctor</title><link>https://metafunctor.com/</link></image><item><title>Bernoulli Types: Theory and Construction</title><link>https://metafunctor.com/papers/bernoulli-theory/</link><pubDate>Sat, 24 Jan 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/papers/bernoulli-theory/</guid><description> Download PDF</description><content:encoded><![CDATA[<style>
.pdf-container {
  width: 100%;
  margin: 0;
}
.pdf-container embed {
  width: 100%;
  min-height: 90vh;
  border: 1px solid var(--color-border-default, #ddd);
  border-radius: 0.5rem;
}
.action-bar {
  margin: 0 0 1rem 0;
  display: flex;
  gap: .75rem;
  flex-wrap: wrap;
}
.action-bar a {
  text-decoration: none;
  padding: .5rem .75rem;
  border: 1px solid var(--color-border-default, #ddd);
  border-radius: .4rem;
  display: inline-block;
  color: var(--color-accent-primary, #007acc);
}
.action-bar a:hover {
  background: var(--color-bg-secondary, #f5f5f5);
}
</style>
<div class="action-bar">
  <a href="/latex/bernoulli-theory/paper.pdf" target="_blank" rel="noopener">Download PDF</a>
</div>
<div class="pdf-container">
  <embed src="/latex/bernoulli-theory/paper.pdf" type="application/pdf">
</div>
]]></content:encoded><category>probabilistic data structures</category><category>type theory</category><category>Bloom filters</category><category>approximate computing</category><category>error propagation</category><category>hash functions</category><category>oblivious computing</category></item><item><title>Algebraic Hashing A Modern C++20 Library for Composable Hash Functions Version 2.0</title><link>https://metafunctor.com/papers/algebraic_hashing/</link><pubDate>Wed, 01 Oct 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/papers/algebraic_hashing/</guid><description> Open full page Download PDF</description><content:encoded><![CDATA[<style>
.iframe-wrap {
  width: 100%;
  max-width: 100%;
  margin: 0;
  padding: 0;
}
.iframe-wrap iframe {
  display: block;
  width: 100%;
  border: 0;
  min-height: 120vh;
}
.action-bar {
  margin: 0 0 1rem 0;
  padding: 0 1rem;
  display: flex;
  gap: .75rem;
  flex-wrap: wrap;
}
.action-bar a {
  text-decoration: none;
  padding: .5rem .75rem;
  border: 1px solid var(--color-border-default, #ddd);
  border-radius: .4rem;
  display: inline-block;
  color: var(--color-accent-primary, #007acc);
}
.action-bar a:hover {
  background: var(--color-bg-secondary, #f5f5f5);
}
</style>
<div class="action-bar">
  <a href="/latex/algebraic_hashing/index.html" target="_blank" rel="noopener">Open full page</a>
  <a href="/latex/algebraic_hashing/algebraic_hashing_whitepaper.pdf" target="_blank" rel="noopener">Download PDF</a>
</div>
<div class="iframe-wrap">
  <iframe id="ltx" src="/latex/algebraic_hashing/index.html" loading="lazy" scrolling="no"></iframe>
</div>
<script>
(function () {
  const iframe = document.getElementById('ltx');

  if (location.hash) {
    const u = new URL(iframe.src, location.origin);
    u.hash = location.hash;
    iframe.src = u.toString();
  }

  let lastScrollHeight = 0;

  function setHeight(h) {
    const newH = Math.ceil(h);
    if (newH > lastScrollHeight + 5) {
      iframe.style.height = newH + 'px';
      lastScrollHeight = newH;
    }
  }

  function measure(doc) {
    const h = Math.max(
      doc.body?.scrollHeight || 0,
      doc.documentElement?.scrollHeight || 0
    );
    if (h > 0) setHeight(h);
  }

  iframe.addEventListener('load', () => {
    const doc = iframe.contentDocument || iframe.contentWindow.document;
    if (!doc) return;

    setTimeout(() => measure(doc), 100);
    setTimeout(() => measure(doc), 500);

    doc.fonts?.ready?.then(() => {
      setTimeout(() => measure(doc), 50);
    }).catch(() => {});

    const images = Array.from(doc.images || []);
    if (images.length > 0) {
      let loadedCount = 0;
      images.forEach(img => {
        if (img.complete) {
          loadedCount++;
        } else {
          img.addEventListener('load', () => {
            loadedCount++;
            if (loadedCount === images.length) {
              setTimeout(() => measure(doc), 50);
            }
          }, { once: true });
        }
      });
    }

    let resizeTimeout;
    window.addEventListener('resize', () => {
      clearTimeout(resizeTimeout);
      resizeTimeout = setTimeout(() => {
        lastScrollHeight = 0;
        measure(doc);
      }, 300);
    });
  });
})();
</script>
]]></content:encoded><category>cryptography</category><category>hash functions</category><category>algebra</category><category>c++</category><category>library</category></item><item><title>Cryptographic perfect hash functions: A theoretical analysis on space efficiency, time complexity, and entropy</title><link>https://metafunctor.com/papers/crypto-perf-hash/</link><pubDate>Wed, 01 Oct 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/papers/crypto-perf-hash/</guid><description> Open full page Download PDF</description><content:encoded><![CDATA[<style>
.iframe-wrap {
  width: 100%;
  max-width: 100%;
  margin: 0;
  padding: 0;
}
.iframe-wrap iframe {
  display: block;
  width: 100%;
  border: 0;
  min-height: 120vh;
}
.action-bar {
  margin: 0 0 1rem 0;
  padding: 0 1rem;
  display: flex;
  gap: .75rem;
  flex-wrap: wrap;
}
.action-bar a {
  text-decoration: none;
  padding: .5rem .75rem;
  border: 1px solid var(--color-border-default, #ddd);
  border-radius: .4rem;
  display: inline-block;
  color: var(--color-accent-primary, #007acc);
}
.action-bar a:hover {
  background: var(--color-bg-secondary, #f5f5f5);
}
</style>
<div class="action-bar">
  <a href="/latex/crypto-perf-hash/index.html" target="_blank" rel="noopener">Open full page</a>
  <a href="/latex/crypto-perf-hash/paper.pdf" target="_blank" rel="noopener">Download PDF</a>
</div>
<div class="iframe-wrap">
  <iframe id="ltx" src="/latex/crypto-perf-hash/index.html" loading="lazy" scrolling="no"></iframe>
</div>
<script>
(function () {
  const iframe = document.getElementById('ltx');

  if (location.hash) {
    const u = new URL(iframe.src, location.origin);
    u.hash = location.hash;
    iframe.src = u.toString();
  }

  let lastScrollHeight = 0;

  function setHeight(h) {
    const newH = Math.ceil(h);
    if (newH > lastScrollHeight + 5) {
      iframe.style.height = newH + 'px';
      lastScrollHeight = newH;
    }
  }

  function measure(doc) {
    const h = Math.max(
      doc.body?.scrollHeight || 0,
      doc.documentElement?.scrollHeight || 0
    );
    if (h > 0) setHeight(h);
  }

  iframe.addEventListener('load', () => {
    const doc = iframe.contentDocument || iframe.contentWindow.document;
    if (!doc) return;

    setTimeout(() => measure(doc), 100);
    setTimeout(() => measure(doc), 500);

    doc.fonts?.ready?.then(() => {
      setTimeout(() => measure(doc), 50);
    }).catch(() => {});

    const images = Array.from(doc.images || []);
    if (images.length > 0) {
      let loadedCount = 0;
      images.forEach(img => {
        if (img.complete) {
          loadedCount++;
        } else {
          img.addEventListener('load', () => {
            loadedCount++;
            if (loadedCount === images.length) {
              setTimeout(() => measure(doc), 50);
            }
          }, { once: true });
        }
      });
    }

    let resizeTimeout;
    window.addEventListener('resize', () => {
      clearTimeout(resizeTimeout);
      resizeTimeout = setTimeout(() => {
        lastScrollHeight = 0;
        measure(doc);
      }, 300);
    });
  });
})();
</script>
]]></content:encoded><category>cryptography</category><category>performance</category><category>hash functions</category><category>data structures</category><category>c++</category><category>library</category></item><item><title>maph: Maps Based on Perfect Hashing for Sub-Microsecond Key-Value Storage</title><link>https://metafunctor.com/papers/maph/</link><pubDate>Wed, 01 Oct 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/papers/maph/</guid><description> Open full page Download PDF</description><content:encoded><![CDATA[<style>
.iframe-wrap {
  width: 100%;
  max-width: 100%;
  margin: 0;
  padding: 0;
}
.iframe-wrap iframe {
  display: block;
  width: 100%;
  border: 0;
  min-height: 120vh;
}
.action-bar {
  margin: 0 0 1rem 0;
  padding: 0 1rem;
  display: flex;
  gap: .75rem;
  flex-wrap: wrap;
}
.action-bar a {
  text-decoration: none;
  padding: .5rem .75rem;
  border: 1px solid var(--color-border-default, #ddd);
  border-radius: .4rem;
  display: inline-block;
  color: var(--color-accent-primary, #007acc);
}
.action-bar a:hover {
  background: var(--color-bg-secondary, #f5f5f5);
}
</style>
<div class="action-bar">
  <a href="/latex/maph/index.html" target="_blank" rel="noopener">Open full page</a>
  <a href="/latex/maph/maph_technical_report.pdf" target="_blank" rel="noopener">Download PDF</a>
</div>
<div class="iframe-wrap">
  <iframe id="ltx" src="/latex/maph/index.html" loading="lazy" scrolling="no"></iframe>
</div>
<script>
(function () {
  const iframe = document.getElementById('ltx');

  if (location.hash) {
    const u = new URL(iframe.src, location.origin);
    u.hash = location.hash;
    iframe.src = u.toString();
  }

  let lastScrollHeight = 0;

  function setHeight(h) {
    const newH = Math.ceil(h);
    if (newH > lastScrollHeight + 5) {
      iframe.style.height = newH + 'px';
      lastScrollHeight = newH;
    }
  }

  function measure(doc) {
    const h = Math.max(
      doc.body?.scrollHeight || 0,
      doc.documentElement?.scrollHeight || 0
    );
    if (h > 0) setHeight(h);
  }

  iframe.addEventListener('load', () => {
    const doc = iframe.contentDocument || iframe.contentWindow.document;
    if (!doc) return;

    setTimeout(() => measure(doc), 100);
    setTimeout(() => measure(doc), 500);

    doc.fonts?.ready?.then(() => {
      setTimeout(() => measure(doc), 50);
    }).catch(() => {});

    const images = Array.from(doc.images || []);
    if (images.length > 0) {
      let loadedCount = 0;
      images.forEach(img => {
        if (img.complete) {
          loadedCount++;
        } else {
          img.addEventListener('load', () => {
            loadedCount++;
            if (loadedCount === images.length) {
              setTimeout(() => measure(doc), 50);
            }
          }, { once: true });
        }
      });
    }

    let resizeTimeout;
    window.addEventListener('resize', () => {
      clearTimeout(resizeTimeout);
      resizeTimeout = setTimeout(() => {
        lastScrollHeight = 0;
        measure(doc);
      }, 300);
    });
  });
})();
</script>
]]></content:encoded><category>hash functions</category><category>data structures</category><category>c++</category><category>library</category><category>perfect hashing</category><category>key-value storage</category></item><item><title>The Beautiful Deception: How 256 Bits Pretend to be Infinity</title><link>https://metafunctor.com/post/2024-07-01-beautiful-deception/</link><pubDate>Mon, 01 Jul 2024 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-07-01-beautiful-deception/</guid><description>Cryptographic theory assumes random oracles with infinite output. We have 256 bits. This paper explores how we bridge that gap, and what it means that we can.</description><content:encoded><![CDATA[<p>How do you store infinity in 256 bits? You don&rsquo;t. But you can fake it well enough that no bounded observer can tell the difference. This paper is about that deception, why it works, and what it tells us about randomness.</p>
<h2 id="the-impossible-oracle">The impossible oracle</h2>
<p>A random oracle maps any input to an infinite sequence of perfectly random bits. Try to implement one and you fail immediately:</p>
<ol>
<li><strong>Memory unboundedness</strong>: each new query exhausts memory</li>
<li><strong>Non-serializability</strong>: can&rsquo;t save/restore state</li>
<li><strong>Non-reproducibility</strong>: each instance generates different values</li>
<li><strong>Non-distributability</strong>: can&rsquo;t share across systems</li>
</ol>
<p>This isn&rsquo;t a limitation of current hardware. It&rsquo;s a constructive proof that true random oracles can&rsquo;t exist in our computational universe.</p>
<h2 id="the-lie-that-works">The lie that works</h2>
<p>From this impossibility comes something useful:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">LazyDigest</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="fm">__init__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">seed</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="bp">self</span><span class="o">.</span><span class="n">seed</span> <span class="o">=</span> <span class="n">seed</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="fm">__getitem__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">index</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="nb">hash</span><span class="p">(</span><span class="n">seed</span> <span class="o">||</span> <span class="n">index</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>
</span></span></code></pre></div><p>256 bits of entropy generating what appears to be an infinite random sequence.</p>
<p>The deception:</p>
<ul>
<li><strong>Appears</strong>: infinite random sequence</li>
<li><strong>Actually</strong>: deterministic function with 256 bits of state</li>
<li><strong>Information content</strong>: K(LazyDigest) = 256 bits + constant</li>
<li><strong>Apparent information</strong>: infinite</li>
</ul>
<p>We&rsquo;re achieving a compression ratio of infinity, representing unbounded data with bounded information.</p>
<h2 id="why-it-works">Why it works</h2>
<p>Computational indistinguishability. If <code>h</code> is a secure PRF, no polynomial-time algorithm can tell LazyDigest apart from truly random output. We&rsquo;re not random. We&rsquo;re computationally hard to distinguish from random. This weaker guarantee is sufficient for all of cryptography.</p>
<p>Since LazyDigest has finite state, it must eventually cycle. After at most 2^256 queries, it repeats. But the expected cycle length is 2^128, roughly 10^38. At a billion queries per second, cycling takes about 10^21 years. The universe is roughly 10^10 years old.</p>
<h2 id="advanced-constructions">Advanced constructions</h2>
<p><strong>Hierarchical seeding</strong> extends the effective period:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">epoch_seed</span> <span class="o">=</span> <span class="n">h</span><span class="p">(</span><span class="n">master_seed</span> <span class="o">||</span> <span class="s2">&#34;epoch&#34;</span> <span class="o">||</span> <span class="n">epoch</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">chunk_seed</span> <span class="o">=</span> <span class="n">h</span><span class="p">(</span><span class="n">epoch_seed</span> <span class="o">||</span> <span class="s2">&#34;chunk&#34;</span> <span class="o">||</span> <span class="n">chunk</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">value</span> <span class="o">=</span> <span class="n">h</span><span class="p">(</span><span class="n">chunk_seed</span> <span class="o">||</span> <span class="n">position</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>
</span></span></code></pre></div><p><strong>XOR multi-hash</strong> hedges against individual algorithm failures:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">result</span> <span class="o">=</span> <span class="n">sha256</span><span class="p">(</span><span class="n">seed</span><span class="o">||</span><span class="n">index</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span> <span class="o">^</span> <span class="n">sha512</span><span class="p">(</span><span class="n">seed</span><span class="o">||</span><span class="n">index</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span> <span class="o">^</span>
</span></span><span class="line"><span class="cl">         <span class="n">sha3_256</span><span class="p">(</span><span class="n">seed</span><span class="o">||</span><span class="n">index</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span> <span class="o">^</span> <span class="n">blake2b</span><span class="p">(</span><span class="n">seed</span><span class="o">||</span><span class="n">index</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>
</span></span></code></pre></div><p>The system stays secure if at least one hash function holds. This hedges against future cryptanalysis, quantum vulnerabilities, and implementation bugs.</p>
<p><strong>Sponge construction</strong> reserves capacity bits that never leave the system, providing tunable security with 2^(capacity/2) collision resistance.</p>
<h2 id="random-oracles-and-uncomputable-reals">Random oracles and uncomputable reals</h2>
<p>Most real numbers are uncomputable. They require infinite information to specify. A true random oracle is the cryptographic analog:</p>
<ul>
<li><strong>Computable reals</strong> (like pi, e): measure zero, finite programs</li>
<li><strong>Uncomputable reals</strong>: measure one, infinite information</li>
<li><strong>LazyDigest</strong>: computable, appears random</li>
<li><strong>Random oracle</strong>: uncomputable, truly random</li>
</ul>
<p>We&rsquo;re using computable functions to approximate uncomputable ones.</p>
<p>The parallel with pi is instructive. We can compute any digit of pi using a finite algorithm (Bailey-Borwein-Plouffe formula). Finite description, infinite output, computed on demand. LazyDigest does the same thing. The difference is semantic: pi encodes geometric relationships, LazyDigest encodes computational hardness. Both achieve the same compression: finite program to infinite sequence.</p>
<h2 id="two-kinds-of-randomness">Two kinds of randomness</h2>
<p>There are two incompatible definitions of randomness:</p>
<ol>
<li><strong>Information-theoretic</strong>: high Kolmogorov complexity</li>
<li><strong>Computational</strong>: indistinguishable from random by efficient algorithms</li>
</ol>
<p>LazyDigest fails (1) catastrophically. It has tiny Kolmogorov complexity, just 256 bits plus the hash function definition.</p>
<p>LazyDigest passes (2) perfectly, assuming secure hash functions.</p>
<p>The insight: &ldquo;randomness&rdquo; in cryptography is actually computational hardness in disguise. We don&rsquo;t generate randomness. We generate a computationally hard problem and bet that nobody can solve it.</p>
<h2 id="entropy-and-computational-bounds">Entropy and computational bounds</h2>
<p>Consider Laplace&rsquo;s demon, a being with unlimited computational power. To it, LazyDigest is trivially non-random, just compute h(seed||i). No entropy exists, only deterministic evolution.</p>
<p>But we are computationally bounded, and that boundedness creates our entire experience of randomness. The past is fixed (we remember it). The future is uncertain (we can&rsquo;t compute fast enough to predict it). Entropy increases (we can&rsquo;t invert dynamics). Randomness exists (we can&rsquo;t distinguish it from true randomness).</p>
<p>A hash function is a one-way time machine. Forward: h(x) is easy. Backward: finding x given h(x) is computationally infeasible.</p>
<h2 id="the-p--np-bet">The P != NP bet</h2>
<p>Every use of LazyDigest implicitly bets that P != NP. If P = NP, we could invert the hash function in polynomial time and distinguish LazyDigest from random output. We&rsquo;re not generating randomness. We&rsquo;re generating a hard problem and hoping nobody solves it.</p>
<h2 id="the-constructivist-angle">The constructivist angle</h2>
<p>I think the most interesting part is what this says about mathematics. Classical mathematics posits incoherent infinities and uncomputable reals as real objects. LazyDigest represents a constructivist alternative: a finite, computable object that serves all practical purposes of an infinite random sequence.</p>
<p>The deception is honest:</p>
<ul>
<li>We don&rsquo;t claim infinite objects. We have finite programs generating unbounded output.</li>
<li>We don&rsquo;t pretend uncomputable things exist. We work with computable approximations.</li>
<li>We don&rsquo;t hide behind axioms. We build finite machines and study their behavior.</li>
</ul>
<p>Cryptography forces you to be a constructivist. The boundary of cryptographic possibility is precisely the boundary of computational constructibility.</p>
<p>In a universe that may itself be computational, LazyDigest isn&rsquo;t approximating some &ldquo;true&rdquo; random oracle in a Platonic realm. It may be as real as randomness gets.</p>
<p><a href="/latex/beautiful-deception/beautiful_deception.pdf">View the full paper (PDF)</a></p>
<h2 id="key-takeaways">Key takeaways</h2>
<ol>
<li>True random oracles are impossible to implement (proven constructively)</li>
<li>LazyDigest uses 256 bits to simulate infinity through computational hardness</li>
<li>&ldquo;Randomness&rdquo; in cryptography means computationally indistinguishable from random</li>
<li>The deception works because we&rsquo;re all computationally bounded observers</li>
<li>This is a constructivist approach to mathematics and computation</li>
</ol>
]]></content:encoded><media:content url="https://metafunctor.com/post/2024-07-01-beautiful-deception/card.jpg" medium="image"><media:title type="plain">Hash-Functions</media:title></media:content><category>cryptography</category><category>hash-functions</category><category>randomness</category><category>information-theory</category><category>kolmogorov-complexity</category></item><item><title>Perfect Hashing: Space Bounds, Entropy, and Cryptographic Security</title><link>https://metafunctor.com/post/2024-02-01-crypto-perf-hash/</link><pubDate>Thu, 01 Feb 2024 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-02-01-crypto-perf-hash/</guid><description>Space bounds, entropy requirements, and cryptographic security properties of perfect hash functions.</description><content:encoded><![CDATA[<p>Can a perfect hash function be cryptographically secure, space-optimal, and maximum-entropy encoded all at once? This paper proves such a construction exists and analyzes exactly what you sacrifice to get all three.</p>
<h2 id="the-impossible-triangle">The Impossible Triangle</h2>
<p>Perfect hash functions typically face tradeoffs. Space-optimal constructions (CHD, BDZ) sacrifice randomness. Cryptographic hash functions waste space on collision resistance. Maximum-entropy encodings require extra bits.</p>
<p>The question: can you have all three?</p>
<h2 id="the-construction">The Construction</h2>
<p>The data type is <em><em>PH = {0,1}</em> x N</em>* with a constructor <code>ph(X, r) = (n', N)</code> where:</p>
<ul>
<li><strong>N = ceil(m/r)</strong>: Hash table size (where m = |X|)</li>
<li><strong>beta(x,n) = trunc(hash(x&rsquo; # n&rsquo;), k)&rsquo; mod N</strong>: Hash function parameterized by seed n</li>
<li><strong>n = min{j in N | beta is collision-free on X}</strong>: Search for smallest collision-free seed</li>
<li><strong>n&rsquo;</strong>: Geometric code encoding n (variable-length prefix-free)</li>
</ul>
<p>The algorithm: try seeds n = 0, 1, 2, &hellip; until beta(.,n) has no collisions on X. Each trial is geometrically distributed with success probability p(m,r), so the expected space for encoding n&rsquo; achieves the information-theoretic lower bound of roughly 1.44 bits per element.</p>
<p>Under the random oracle assumption, hash: {0,1}* -&gt; {0,1}^infinity outputs uniform random bits, making the final encoding (n&rsquo;, N) indistinguishable from a random bit string.</p>
<h2 id="rate-distortion-tradeoff">Rate-Distortion Tradeoff</h2>
<p>The &ldquo;rate-distortion&rdquo; framing comes from information theory: what&rsquo;s the rate (bits per key) for a given distortion (lookup time)?</p>
<p>Zero distortion (O(1) lookup): roughly n log n bits. Constant distortion (small tables): practical two-level schemes approach roughly 1.44n bits. Variable distortion: trade bits for lookup time continuously.</p>
<h2 id="why-cryptographic-matters">Why Cryptographic Matters</h2>
<p>Non-cryptographic perfect hashes are deterministic, so adversaries can engineer collision-inducing inputs. A cryptographic perfect hash (random oracle) prevents adversarial key selection (can&rsquo;t craft keys that break the hash), side-channel attacks (encoding reveals no information about keys), and fingerprinting (maximum entropy makes encodings look random).</p>
<h2 id="the-algebra-of-composition">The Algebra of Composition</h2>
<p>Section 5 proves that composing perfect hash functions preserves injectivity. If h1: S -&gt; T and h2: T -&gt; U are injective, then h2 composed with h1: S -&gt; U is injective.</p>
<p>This connects to my <code>algebraic_hashing</code> library, where composition of cryptographic hashes via XOR preserves both security and structure.</p>
<h2 id="connection-to-oblivious-computing">Connection to Oblivious Computing</h2>
<p>This paper is fundamentally about minimizing information leakage while maintaining functionality, which is the core of the oblivious computing philosophy. The maximum-entropy encoding hides which permutation you&rsquo;re using, even though the function is deterministic.</p>
<p><strong>Research paper</strong> • 4★ • <a href="/latex/crypto-perf-hash/">View PDF</a> • <a href="https://github.com/queelius/crypto-perf-hash">GitHub</a></p>
]]></content:encoded><media:content url="https://metafunctor.com/post/2024-02-01-crypto-perf-hash/card.jpg" medium="image"><media:title type="plain">Hash-Functions</media:title></media:content><category>cryptography</category><category>hash functions</category><category>information theory</category><category>space complexity</category><category>entropy</category><category>perfect hashing</category></item><item><title>Algebraic Hashing: Composable Hash Functions Through XOR</title><link>https://metafunctor.com/post/2024-06-01-algebraic_hashing/</link><pubDate>Tue, 01 Nov 2022 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-06-01-algebraic_hashing/</guid><description>A C++ library for composable hash functions using algebraic structure over XOR, with template metaprogramming.</description><content:encoded><![CDATA[<p>Most hash libraries treat hash functions as opaque blobs. You put data in, you get bits out, and that&rsquo;s the end of the story. Algebraic Hashing takes a different approach: it exposes the mathematical structure underneath, so you can compose hash functions like algebraic expressions. And because this is C++20 with concepts and templates, the composition resolves entirely at compile time. Zero runtime overhead.</p>
<h2 id="the-observation">The observation</h2>
<p>Hash functions form an abelian group under XOR:</p>
<ul>
<li><strong>Closure</strong>: <code>h1 XOR h2</code> is still a valid hash function</li>
<li><strong>Associativity</strong>: <code>(h1 XOR h2) XOR h3 = h1 XOR (h2 XOR h3)</code></li>
<li><strong>Identity</strong>: XOR with zero</li>
<li><strong>Inverses</strong>: each hash is its own inverse under XOR</li>
</ul>
<p>This is a clean algebraic structure, and it&rsquo;s the foundation for everything that follows.</p>
<h2 id="what-you-can-do-with-it">What you can do with it</h2>
<p><strong>Compile-time composition.</strong> Using C++20 concepts and template metaprogramming:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">composed</span> <span class="o">=</span> <span class="n">fnv1a</span><span class="o">&lt;&gt;</span> <span class="o">^</span> <span class="n">sha256</span><span class="o">&lt;&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">hash</span> <span class="o">=</span> <span class="n">composed</span><span class="p">(</span><span class="s">&#34;data&#34;</span><span class="p">);</span>  <span class="c1">// Zero runtime overhead
</span></span></span></code></pre></div><p>All composition resolves at compile time. No virtual dispatch, no function pointers.</p>
<p><strong>Provable properties.</strong> XOR composition preserves uniform distribution (under independence), avalanche effect, and collision resistance for cryptographic hashes. These aren&rsquo;t just empirical observations. They follow from the group structure.</p>
<p><strong>Universal interface.</strong> Works with any hash function: non-cryptographic (FNV-1a), perfect (FKS), or cryptographic (SHA-256). The algebra doesn&rsquo;t care about the implementation details.</p>
<h2 id="practical-uses">Practical uses</h2>
<ul>
<li><strong>Domain separation</strong>: <code>hash_user ^ hash_timestamp</code> prevents collision attacks across domains</li>
<li><strong>Perfect hashing</strong>: FKS two-level scheme with pluggable base hash functions</li>
<li><strong>Composite keys</strong>: hash multiple fields independently, then XOR</li>
<li><strong>Type-based hashing</strong>: different hash functions for different types, composed generically</li>
</ul>
<h2 id="connection-to-oblivious-computing">Connection to oblivious computing</h2>
<p>This work shares DNA with my oblivious computing research. The common thread is making mathematical structure explicit in the type system. Just as Bernoulli types enforce privacy invariants algebraically, algebraic hashing enforces compositional invariants through group theory. Same philosophy, different domain.</p>
<p>The library is header-only C++20 with zero-cost abstractions via concepts.</p>
<p><a href="/latex/algebraic_hashing/">View the whitepaper</a> | <a href="https://github.com/queelius/algebraic_hashing">GitHub</a></p>
]]></content:encoded><media:content url="https://metafunctor.com/post/2024-06-01-algebraic_hashing/card.jpg" medium="image"><media:title type="plain">Hash-Functions</media:title></media:content><category>cryptography</category><category>hash functions</category><category>algebra</category><category>C++</category><category>template metaprogramming</category></item><item><title>Random Oracles in Python</title><link>https://metafunctor.com/post/2016-06-random-oracles-python/</link><pubDate>Sun, 12 Jun 2016 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2016-06-random-oracles-python/</guid><description>Three Python approximations of a random oracle, each showing a different tradeoff between true randomness, determinism, and composability.</description><content:encoded><![CDATA[<p>A <strong>random oracle</strong> is a function $\mathcal{O}: {0,1}^* \to {0,1}^\infty$ where each output bit is independently and uniformly random, but the function is consistent: same input, same output, every time (<a href="https://cseweb.ucsd.edu/~mihir/papers/ro.pdf">Bellare &amp; Rogaway, 1993</a>).</p>
<p>You cannot build one. But you can build three approximations, each showing a different tradeoff.</p>
<h2 id="approximation-1-true-randomness-cached">Approximation 1: True Randomness, Cached</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">OracleDigest</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;&#34;&#34;Infinite digest backed by true randomness and a cache.&#34;&#34;&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="fm">__init__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">entropy</span><span class="o">=</span><span class="kc">None</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="bp">self</span><span class="o">.</span><span class="n">_entropy</span> <span class="o">=</span> <span class="n">entropy</span> <span class="ow">or</span> <span class="p">(</span><span class="k">lambda</span><span class="p">:</span> <span class="n">hashlib</span><span class="o">.</span><span class="n">sha256</span><span class="p">(</span><span class="n">os</span><span class="o">.</span><span class="n">urandom</span><span class="p">(</span><span class="mi">32</span><span class="p">))</span><span class="o">.</span><span class="n">digest</span><span class="p">())</span>
</span></span><span class="line"><span class="cl">        <span class="bp">self</span><span class="o">.</span><span class="n">_cache</span> <span class="o">=</span> <span class="p">{}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="fm">__getitem__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">index</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="n">index</span> <span class="ow">not</span> <span class="ow">in</span> <span class="bp">self</span><span class="o">.</span><span class="n">_cache</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">            <span class="bp">self</span><span class="o">.</span><span class="n">_cache</span><span class="p">[</span><span class="n">index</span><span class="p">]</span> <span class="o">=</span> <span class="bp">self</span><span class="o">.</span><span class="n">_entropy</span><span class="p">()[</span><span class="mi">0</span><span class="p">]</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="bp">self</span><span class="o">.</span><span class="n">_cache</span><span class="p">[</span><span class="n">index</span><span class="p">]</span>
</span></span></code></pre></div><p>Each new index draws a fresh random byte from the OS entropy pool and caches it. Genuinely random, but the cache grows without bound, cannot be serialized, and dies with the process. Each instance is a <em>different</em> random oracle from the family of all possible oracles.</p>
<h2 id="approximation-2-deterministic-infinite">Approximation 2: Deterministic, Infinite</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">LazyDigest</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;&#34;&#34;Infinite deterministic digest via counter-mode PRF: h(seed || i).&#34;&#34;&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="fm">__init__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">seed</span><span class="p">,</span> <span class="n">hash_fn</span><span class="o">=</span><span class="n">hashlib</span><span class="o">.</span><span class="n">sha256</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="bp">self</span><span class="o">.</span><span class="n">_seed</span> <span class="o">=</span> <span class="n">seed</span>
</span></span><span class="line"><span class="cl">        <span class="bp">self</span><span class="o">.</span><span class="n">_hash_fn</span> <span class="o">=</span> <span class="n">hash_fn</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="fm">__getitem__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">index</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="n">h</span> <span class="o">=</span> <span class="bp">self</span><span class="o">.</span><span class="n">_hash_fn</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">        <span class="n">h</span><span class="o">.</span><span class="n">update</span><span class="p">(</span><span class="bp">self</span><span class="o">.</span><span class="n">_seed</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="n">h</span><span class="o">.</span><span class="n">update</span><span class="p">(</span><span class="n">index</span><span class="o">.</span><span class="n">to_bytes</span><span class="p">(</span><span class="mi">8</span><span class="p">,</span> <span class="s2">&#34;big&#34;</span><span class="p">))</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">h</span><span class="o">.</span><span class="n">digest</span><span class="p">()[</span><span class="mi">0</span><span class="p">]</span>
</span></span></code></pre></div><p><a href="https://csrc.nist.gov/pubs/sp/800/108/r1/upd1/final">Counter-mode PRF</a>: $\text{byte}_i = H(\text{seed} | i)[0]$. Same seed gives the same infinite sequence, on any machine, forever. $O(1)$ space, random access to any position. Only as &ldquo;random&rdquo; as the seed and hash function.</p>
<p>Note the fixed-width index encoding (<code>to_bytes(8, 'big')</code>). If you used <code>str(index).encode()</code> instead, index <code>10</code> and the concatenation of index <code>1</code> + index <code>0</code> would collide. Real KDFs like <a href="https://www.rfc-editor.org/rfc/rfc5869">HKDF</a> use fixed-width encodings for the same reason.</p>
<h2 id="approximation-3-full-oracle">Approximation 3: Full Oracle</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">OracleHash</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;&#34;&#34;Approximates {0,1}* -&gt; {0,1}^inf via hash + lazy expansion.&#34;&#34;&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="fm">__init__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">hash_fn</span><span class="o">=</span><span class="n">hashlib</span><span class="o">.</span><span class="n">sha256</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="bp">self</span><span class="o">.</span><span class="n">_hash_fn</span> <span class="o">=</span> <span class="n">hash_fn</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="fm">__call__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">LazyDigest</span><span class="p">(</span><span class="bp">self</span><span class="o">.</span><span class="n">_hash_fn</span><span class="p">(</span><span class="n">x</span><span class="p">)</span><span class="o">.</span><span class="n">digest</span><span class="p">(),</span> <span class="bp">self</span><span class="o">.</span><span class="n">_hash_fn</span><span class="p">)</span>
</span></span></code></pre></div><p>This composes the two: hash the input to get a seed, expand with counter-mode PRF. Type: ${0,1}^* \to {0,1}^\infty$.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">oracle</span> <span class="o">=</span> <span class="n">OracleHash</span><span class="p">()</span>
</span></span><span class="line"><span class="cl"><span class="n">d</span> <span class="o">=</span> <span class="n">oracle</span><span class="p">(</span><span class="sa">b</span><span class="s2">&#34;hello&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="nb">print</span><span class="p">(</span><span class="nb">bytes</span><span class="p">(</span><span class="n">d</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="mi">16</span><span class="p">))</span><span class="o">.</span><span class="n">hex</span><span class="p">())</span>  <span class="c1"># 4c9460cf7b89806e9ebc7b94c815d8c0</span>
</span></span></code></pre></div><h2 id="the-tradeoff">The Tradeoff</h2>
<table>
	<thead>
			<tr>
					<th></th>
					<th><code>OracleDigest</code></th>
					<th><code>LazyDigest</code></th>
					<th><code>OracleHash</code></th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Randomness</td>
					<td>True (OS entropy)</td>
					<td>Pseudo (PRF)</td>
					<td>Pseudo (PRF)</td>
			</tr>
			<tr>
					<td>Space</td>
					<td>$O(k)$ queries</td>
					<td>$O(1)$</td>
					<td>$O(1)$</td>
			</tr>
			<tr>
					<td>Serializable</td>
					<td>No</td>
					<td>Yes (seed)</td>
					<td>Yes (hash fn)</td>
			</tr>
			<tr>
					<td>Lifetime</td>
					<td>Process-scoped</td>
					<td>Permanent</td>
					<td>Permanent</td>
			</tr>
	</tbody>
</table>
<h2 id="why-this-matters">Why This Matters</h2>
<p>Random oracles are the backbone of the <a href="https://en.wikipedia.org/wiki/Random_oracle_model">random oracle model</a> (ROM), used throughout cryptography to prove security. The <a href="https://en.wikipedia.org/wiki/Fiat%E2%80%93Shamir_heuristic">Fiat-Shamir transform</a>, which turns interactive proofs into non-interactive signatures, is the most important example: replace the verifier&rsquo;s random challenge with $H(\text{transcript})$.</p>
<p>The gap between a random oracle and a real hash function is real. <a href="https://doi.org/10.1145/1008731.1008734">Canetti, Goldreich &amp; Halevi (2004)</a> showed there exist schemes provably secure in the ROM that break when instantiated with <em>any</em> hash function. In practice, it usually works. Understanding why starts with understanding what you are approximating.</p>]]></content:encoded><media:content url="https://metafunctor.com/post/2016-06-random-oracles-python/card.jpg" medium="image"><media:title type="plain">Hash-Functions</media:title></media:content><category>cryptography</category><category>random-oracles</category><category>hash-functions</category><category>python</category></item></channel></rss>