<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:media="http://search.yahoo.com/mrss/"><channel><title>Hash Functions on metafunctor</title><link>https://metafunctor.com/tags/hash-functions/</link><description>Research Engineer &amp; Computer Scientist - Machine Learning, Statistical Computing, Open Source Development</description><language>en-us</language><managingEditor>lex@metafunctor.com (Alex Towell)</managingEditor><webMaster>lex@metafunctor.com (Alex Towell)</webMaster><lastBuildDate>Wed, 18 Feb 2026 19:12:50 -0600</lastBuildDate><atom:link href="https://metafunctor.com/tags/hash-functions/" rel="self" type="application/rss+xml"/><image><url>https://metafunctor.com/images/og-image.jpg</url><title>metafunctor</title><link>https://metafunctor.com/</link></image><item><title>Bernoulli Types: Theory and Construction</title><link>https://metafunctor.com/papers/bernoulli-theory/</link><pubDate>Sat, 24 Jan 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/papers/bernoulli-theory/</guid><description> Download PDF</description><content:encoded><![CDATA[<style>
.pdf-container {
  width: 100%;
  margin: 0;
}
.pdf-container embed {
  width: 100%;
  min-height: 90vh;
  border: 1px solid var(--color-border-default, #ddd);
  border-radius: 0.5rem;
}
.action-bar {
  margin: 0 0 1rem 0;
  display: flex;
  gap: .75rem;
  flex-wrap: wrap;
}
.action-bar a {
  text-decoration: none;
  padding: .5rem .75rem;
  border: 1px solid var(--color-border-default, #ddd);
  border-radius: .4rem;
  display: inline-block;
  color: var(--color-accent-primary, #007acc);
}
.action-bar a:hover {
  background: var(--color-bg-secondary, #f5f5f5);
}
</style>
<div class="action-bar">
  <a href="/latex/bernoulli-theory/paper.pdf" target="_blank" rel="noopener">Download PDF</a>
</div>
<div class="pdf-container">
  <embed src="/latex/bernoulli-theory/paper.pdf" type="application/pdf">
</div>]]></content:encoded><category>probabilistic data structures</category><category>type theory</category><category>Bloom filters</category><category>approximate computing</category><category>error propagation</category><category>hash functions</category><category>oblivious computing</category></item><item><title>Entropy Maps: When Hashing Meets Information Theory</title><link>https://metafunctor.com/post/2026-01-entropy-maps-intro/</link><pubDate>Thu, 15 Jan 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2026-01-entropy-maps-intro/</guid><description>A conceptual introduction to entropy maps—implementing functions with hash functions and prefix-free codes.</description><content:encoded><![CDATA[<p>What if you could implement any function using nothing but a hash function and some clever encoding? No lookup tables. No stored keys. Just hashing.</p>
<p>This is the core idea behind <strong>entropy maps</strong>—and the mathematics tells us exactly how efficient they can be.</p>
<h2 id="the-insight">The Insight</h2>
<p>An entropy map implements a function $f: X \to Y$ by:</p>
<ol>
<li>Hashing each input $x$ to get a bit string</li>
<li>Checking if a prefix of that hash matches a code for some output $y$</li>
<li>Returning the decoded output</li>
</ol>
<p>The trick: we assign multiple prefix-free codes to each output value. High-probability outputs get more codes. The result? <strong>Space usage equals the Shannon entropy of the output distribution.</strong></p>
<p>This isn&rsquo;t just elegant—it&rsquo;s <em>optimal</em>.</p>
<h2 id="why-this-matters">Why This Matters</h2>
<p>Bloom filters are entropy maps in disguise. When you test membership in a Bloom filter, you&rsquo;re really asking: &ldquo;does this hash prefix decode to <em>member</em> or <em>non-member</em>?&rdquo;</p>
<p>The entropy map lens reveals what&rsquo;s really happening:</p>
<ul>
<li>The &ldquo;member&rdquo; code has probability ε (the false positive rate)</li>
<li>The &ldquo;non-member&rdquo; code has probability 1-ε</li>
<li>Total space: the entropy of this distribution</li>
</ul>
<p>This framework unifies Bloom filters, Count-Min sketches, and other probabilistic structures under one information-theoretic roof.</p>
<h2 id="the-broader-pattern">The Broader Pattern</h2>
<p>Entropy maps taught me something fundamental: <strong>information theory tells us the limits of what&rsquo;s possible, and hash functions let us approach those limits.</strong></p>
<p>When you understand that space efficiency is really about entropy, you gain a new design principle. You&rsquo;re not just building data structures—you&rsquo;re doing lossy compression of functions.</p>
<p>This connects directly to <a href="/post/2025-10-bernoulli-types-foundations/">Bernoulli types</a>—my broader framework for approximate computing. The entropy map is the construction that makes the theory practical.</p>
<hr>
<p><em>For the full mathematical treatment with confusion matrices and algorithms, see the <a href="/post/entropy-map/">detailed entropy maps post</a>.</em></p>
]]></content:encoded><category>entropy</category><category>information-theory</category><category>probabilistic-data-structures</category><category>hash-functions</category></item><item><title>Algebraic Hashing A Modern C++20 Library for Composable Hash Functions Version 2.0</title><link>https://metafunctor.com/papers/algebraic_hashing/</link><pubDate>Wed, 01 Oct 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/papers/algebraic_hashing/</guid><description> Open full page Download PDF</description><content:encoded><![CDATA[<style>
.iframe-wrap {
  width: 100%;
  max-width: 100%;
  margin: 0;
  padding: 0;
}
.iframe-wrap iframe {
  display: block;
  width: 100%;
  border: 0;
  min-height: 120vh;
}
.action-bar {
  margin: 0 0 1rem 0;
  padding: 0 1rem;
  display: flex;
  gap: .75rem;
  flex-wrap: wrap;
}
.action-bar a {
  text-decoration: none;
  padding: .5rem .75rem;
  border: 1px solid var(--color-border-default, #ddd);
  border-radius: .4rem;
  display: inline-block;
  color: var(--color-accent-primary, #007acc);
}
.action-bar a:hover {
  background: var(--color-bg-secondary, #f5f5f5);
}
</style>
<div class="action-bar">
  <a href="/latex/algebraic_hashing/index.html" target="_blank" rel="noopener">Open full page</a>
  <a href="/latex/algebraic_hashing/algebraic_hashing_whitepaper.pdf" target="_blank" rel="noopener">Download PDF</a>
</div>
<div class="iframe-wrap">
  <iframe id="ltx" src="/latex/algebraic_hashing/index.html" loading="lazy" scrolling="no"></iframe>
</div>
<script>
(function () {
  const iframe = document.getElementById('ltx');

  if (location.hash) {
    const u = new URL(iframe.src, location.origin);
    u.hash = location.hash;
    iframe.src = u.toString();
  }

  let lastScrollHeight = 0;

  function setHeight(h) {
    const newH = Math.ceil(h);
    if (newH > lastScrollHeight + 5) {
      iframe.style.height = newH + 'px';
      lastScrollHeight = newH;
    }
  }

  function measure(doc) {
    const h = Math.max(
      doc.body?.scrollHeight || 0,
      doc.documentElement?.scrollHeight || 0
    );
    if (h > 0) setHeight(h);
  }

  iframe.addEventListener('load', () => {
    const doc = iframe.contentDocument || iframe.contentWindow.document;
    if (!doc) return;

    setTimeout(() => measure(doc), 100);
    setTimeout(() => measure(doc), 500);

    doc.fonts?.ready?.then(() => {
      setTimeout(() => measure(doc), 50);
    }).catch(() => {});

    const images = Array.from(doc.images || []);
    if (images.length > 0) {
      let loadedCount = 0;
      images.forEach(img => {
        if (img.complete) {
          loadedCount++;
        } else {
          img.addEventListener('load', () => {
            loadedCount++;
            if (loadedCount === images.length) {
              setTimeout(() => measure(doc), 50);
            }
          }, { once: true });
        }
      });
    }

    let resizeTimeout;
    window.addEventListener('resize', () => {
      clearTimeout(resizeTimeout);
      resizeTimeout = setTimeout(() => {
        lastScrollHeight = 0;
        measure(doc);
      }, 300);
    });
  });
})();
</script>
]]></content:encoded><category>cryptography</category><category>hash functions</category><category>algebra</category><category>c++</category><category>library</category></item><item><title>Cryptographic perfect hash functions: A theoretical analysis on space efficiency, time complexity, and entropy</title><link>https://metafunctor.com/papers/crypto-perf-hash/</link><pubDate>Wed, 01 Oct 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/papers/crypto-perf-hash/</guid><description> Open full page Download PDF</description><content:encoded><![CDATA[<style>
.iframe-wrap {
  width: 100%;
  max-width: 100%;
  margin: 0;
  padding: 0;
}
.iframe-wrap iframe {
  display: block;
  width: 100%;
  border: 0;
  min-height: 120vh;
}
.action-bar {
  margin: 0 0 1rem 0;
  padding: 0 1rem;
  display: flex;
  gap: .75rem;
  flex-wrap: wrap;
}
.action-bar a {
  text-decoration: none;
  padding: .5rem .75rem;
  border: 1px solid var(--color-border-default, #ddd);
  border-radius: .4rem;
  display: inline-block;
  color: var(--color-accent-primary, #007acc);
}
.action-bar a:hover {
  background: var(--color-bg-secondary, #f5f5f5);
}
</style>
<div class="action-bar">
  <a href="/latex/crypto-perf-hash/index.html" target="_blank" rel="noopener">Open full page</a>
  <a href="/latex/crypto-perf-hash/paper.pdf" target="_blank" rel="noopener">Download PDF</a>
</div>
<div class="iframe-wrap">
  <iframe id="ltx" src="/latex/crypto-perf-hash/index.html" loading="lazy" scrolling="no"></iframe>
</div>
<script>
(function () {
  const iframe = document.getElementById('ltx');

  if (location.hash) {
    const u = new URL(iframe.src, location.origin);
    u.hash = location.hash;
    iframe.src = u.toString();
  }

  let lastScrollHeight = 0;

  function setHeight(h) {
    const newH = Math.ceil(h);
    if (newH > lastScrollHeight + 5) {
      iframe.style.height = newH + 'px';
      lastScrollHeight = newH;
    }
  }

  function measure(doc) {
    const h = Math.max(
      doc.body?.scrollHeight || 0,
      doc.documentElement?.scrollHeight || 0
    );
    if (h > 0) setHeight(h);
  }

  iframe.addEventListener('load', () => {
    const doc = iframe.contentDocument || iframe.contentWindow.document;
    if (!doc) return;

    setTimeout(() => measure(doc), 100);
    setTimeout(() => measure(doc), 500);

    doc.fonts?.ready?.then(() => {
      setTimeout(() => measure(doc), 50);
    }).catch(() => {});

    const images = Array.from(doc.images || []);
    if (images.length > 0) {
      let loadedCount = 0;
      images.forEach(img => {
        if (img.complete) {
          loadedCount++;
        } else {
          img.addEventListener('load', () => {
            loadedCount++;
            if (loadedCount === images.length) {
              setTimeout(() => measure(doc), 50);
            }
          }, { once: true });
        }
      });
    }

    let resizeTimeout;
    window.addEventListener('resize', () => {
      clearTimeout(resizeTimeout);
      resizeTimeout = setTimeout(() => {
        lastScrollHeight = 0;
        measure(doc);
      }, 300);
    });
  });
})();
</script>
]]></content:encoded><category>cryptography</category><category>performance</category><category>hash functions</category><category>data structures</category><category>c++</category><category>library</category></item><item><title>maph: Maps Based on Perfect Hashing for Sub-Microsecond Key-Value Storage</title><link>https://metafunctor.com/papers/maph/</link><pubDate>Wed, 01 Oct 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/papers/maph/</guid><description> Open full page Download PDF</description><content:encoded><![CDATA[<style>
.iframe-wrap {
  width: 100%;
  max-width: 100%;
  margin: 0;
  padding: 0;
}
.iframe-wrap iframe {
  display: block;
  width: 100%;
  border: 0;
  min-height: 120vh;
}
.action-bar {
  margin: 0 0 1rem 0;
  padding: 0 1rem;
  display: flex;
  gap: .75rem;
  flex-wrap: wrap;
}
.action-bar a {
  text-decoration: none;
  padding: .5rem .75rem;
  border: 1px solid var(--color-border-default, #ddd);
  border-radius: .4rem;
  display: inline-block;
  color: var(--color-accent-primary, #007acc);
}
.action-bar a:hover {
  background: var(--color-bg-secondary, #f5f5f5);
}
</style>
<div class="action-bar">
  <a href="/latex/maph/index.html" target="_blank" rel="noopener">Open full page</a>
  <a href="/latex/maph/maph_technical_report.pdf" target="_blank" rel="noopener">Download PDF</a>
</div>
<div class="iframe-wrap">
  <iframe id="ltx" src="/latex/maph/index.html" loading="lazy" scrolling="no"></iframe>
</div>
<script>
(function () {
  const iframe = document.getElementById('ltx');

  if (location.hash) {
    const u = new URL(iframe.src, location.origin);
    u.hash = location.hash;
    iframe.src = u.toString();
  }

  let lastScrollHeight = 0;

  function setHeight(h) {
    const newH = Math.ceil(h);
    if (newH > lastScrollHeight + 5) {
      iframe.style.height = newH + 'px';
      lastScrollHeight = newH;
    }
  }

  function measure(doc) {
    const h = Math.max(
      doc.body?.scrollHeight || 0,
      doc.documentElement?.scrollHeight || 0
    );
    if (h > 0) setHeight(h);
  }

  iframe.addEventListener('load', () => {
    const doc = iframe.contentDocument || iframe.contentWindow.document;
    if (!doc) return;

    setTimeout(() => measure(doc), 100);
    setTimeout(() => measure(doc), 500);

    doc.fonts?.ready?.then(() => {
      setTimeout(() => measure(doc), 50);
    }).catch(() => {});

    const images = Array.from(doc.images || []);
    if (images.length > 0) {
      let loadedCount = 0;
      images.forEach(img => {
        if (img.complete) {
          loadedCount++;
        } else {
          img.addEventListener('load', () => {
            loadedCount++;
            if (loadedCount === images.length) {
              setTimeout(() => measure(doc), 50);
            }
          }, { once: true });
        }
      });
    }

    let resizeTimeout;
    window.addEventListener('resize', () => {
      clearTimeout(resizeTimeout);
      resizeTimeout = setTimeout(() => {
        lastScrollHeight = 0;
        measure(doc);
      }, 300);
    });
  });
})();
</script>
]]></content:encoded><category>hash functions</category><category>data structures</category><category>c++</category><category>library</category><category>perfect hashing</category><category>key-value storage</category></item><item><title>The Beautiful Deception: How 256 Bits Pretend to be Infinity</title><link>https://metafunctor.com/post/2024-07-01-beautiful-deception/</link><pubDate>Mon, 01 Jul 2024 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-07-01-beautiful-deception/</guid><description>How do you store infinity in 256 bits? An exploration of the fundamental deception at the heart of cryptography: using finite information to simulate infinite randomness.</description><content:encoded><![CDATA[<p><strong>How do you store infinity in 256 bits?</strong></p>
<p>This paper explores a fundamental paradox in cryptography: cryptographic theory assumes random oracles that produce infinite sequences of random bits, yet we only have computers with finite memory. The answer? We can&rsquo;t implement true random oracles. Instead, we create an elaborate and beautiful deception.</p>
<h2 id="the-impossible-oracle">The Impossible Oracle</h2>
<p>Random oracles are functions that map any input to an infinite sequence of perfectly random bits. But trying to implement one (<code>OracleDigest</code>) fails catastrophically:</p>
<ol>
<li><strong>Memory Unboundedness</strong>: Each new query exhausts memory</li>
<li><strong>Non-Serializability</strong>: Cannot save/restore state</li>
<li><strong>Non-Reproducibility</strong>: Each instance generates different values</li>
<li><strong>Non-Distributability</strong>: Cannot share across systems</li>
</ol>
<p>This isn&rsquo;t a bug—it&rsquo;s a <strong>constructive proof</strong> that true random oracles cannot exist in our computational universe.</p>
<h2 id="the-beautiful-lie-lazydigest">The Beautiful Lie: LazyDigest</h2>
<p>From this impossibility emerges something remarkable:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">LazyDigest</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="fm">__init__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">seed</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="bp">self</span><span class="o">.</span><span class="n">seed</span> <span class="o">=</span> <span class="n">seed</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="fm">__getitem__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">index</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="nb">hash</span><span class="p">(</span><span class="n">seed</span> <span class="o">||</span> <span class="n">index</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>
</span></span></code></pre></div><p>Just <strong>256 bits of entropy</strong> generating what appears to be an <strong>infinite random sequence</strong>.</p>
<h3 id="the-core-deception">The Core Deception</h3>
<ul>
<li><strong>Appears</strong>: Infinite random sequence</li>
<li><strong>Actually</strong>: Deterministic function with 256 bits of state</li>
<li><strong>Information content</strong>: K(LazyDigest) = 256 bits + constant</li>
<li><strong>Apparent information</strong>: Infinite</li>
</ul>
<p>We&rsquo;re achieving a compression ratio of <strong>infinity</strong>—representing unbounded data with bounded information.</p>
<h2 id="why-the-deception-works">Why the Deception Works</h2>
<p><strong>Computational indistinguishability</strong>: If <code>h</code> is a secure PRF, no polynomial-time distinguisher can tell LazyDigest apart from truly random output.</p>
<p>We&rsquo;re not random—we&rsquo;re <strong>computationally hard to distinguish from random</strong>. This weaker guarantee is sufficient for cryptography.</p>
<h3 id="the-inevitable-cycle">The Inevitable Cycle</h3>
<p>Since LazyDigest has finite state, it must eventually repeat. After at most 2^256 queries, it will cycle.</p>
<p><strong>But</strong> 2^128 (expected cycle length) ≈ 10^38. At one billion queries per second, it would take 10^21 years—<strong>far longer than the age of the universe</strong>.</p>
<h2 id="advanced-constructions">Advanced Constructions</h2>
<h3 id="hierarchical-seeding">Hierarchical Seeding</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">epoch_seed</span> <span class="o">=</span> <span class="n">h</span><span class="p">(</span><span class="n">master_seed</span> <span class="o">||</span> <span class="s2">&#34;epoch&#34;</span> <span class="o">||</span> <span class="n">epoch</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">chunk_seed</span> <span class="o">=</span> <span class="n">h</span><span class="p">(</span><span class="n">epoch_seed</span> <span class="o">||</span> <span class="s2">&#34;chunk&#34;</span> <span class="o">||</span> <span class="n">chunk</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">value</span> <span class="o">=</span> <span class="n">h</span><span class="p">(</span><span class="n">chunk_seed</span> <span class="o">||</span> <span class="n">position</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>
</span></span></code></pre></div><p>Extends the effective period by structuring state hierarchically.</p>
<h3 id="xor-multi-hash-defense-in-depth">XOR Multi-Hash (Defense in Depth)</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">result</span> <span class="o">=</span> <span class="n">sha256</span><span class="p">(</span><span class="n">seed</span><span class="o">||</span><span class="n">index</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span> <span class="err">⊕</span> <span class="n">sha512</span><span class="p">(</span><span class="n">seed</span><span class="o">||</span><span class="n">index</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span> <span class="err">⊕</span>
</span></span><span class="line"><span class="cl">         <span class="n">sha3_256</span><span class="p">(</span><span class="n">seed</span><span class="o">||</span><span class="n">index</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span> <span class="err">⊕</span> <span class="n">blake2b</span><span class="p">(</span><span class="n">seed</span><span class="o">||</span><span class="n">index</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>
</span></span></code></pre></div><p>System remains secure if <strong>at least one</strong> hash function is secure. Hedges against:</p>
<ul>
<li>Future cryptanalysis breaking individual algorithms</li>
<li>Quantum computing vulnerabilities</li>
<li>Implementation bugs</li>
</ul>
<h3 id="sponge-construction">Sponge Construction</h3>
<p>Reserves capacity bits that never leave the system, providing tunable security with 2^(capacity/2) collision resistance.</p>
<h2 id="deep-connections">Deep Connections</h2>
<h3 id="random-oracles-as-uncomputable-reals">Random Oracles as Uncomputable Reals</h3>
<p>Most real numbers are <strong>uncomputable</strong>—they require infinite information to specify. A true random oracle is the cryptographic analog of an uncomputable real:</p>
<ul>
<li><strong>Computable reals</strong> (like π, e): Measure zero, finite programs</li>
<li><strong>Uncomputable reals</strong>: Measure one, infinite information</li>
<li><strong>LazyDigest</strong>: Computable, appears random</li>
<li><strong>Random oracle</strong>: Uncomputable, truly random</li>
</ul>
<p>We&rsquo;re using computable functions to approximate uncomputable ones.</p>
<h3 id="lazy-evaluation-and-mathematical-constants">Lazy Evaluation and Mathematical Constants</h3>
<p>Consider π: we can compute any digit using a finite algorithm (Bailey-Borwein-Plouffe formula). This is lazy evaluation in its purest form:</p>
<ul>
<li><strong>Finite description</strong>: Algorithm is hundreds of bytes</li>
<li><strong>Infinite output</strong>: Arbitrarily many digits</li>
<li><strong>On-demand computation</strong>: Only compute what&rsquo;s observed</li>
</ul>
<p>LazyDigest parallels this exactly. The difference is semantic:</p>
<ul>
<li><strong>π</strong> encodes geometric relationships</li>
<li><strong>LazyDigest</strong> encodes computational hardness</li>
</ul>
<p>Both achieve the same compression: <strong>finite program → infinite sequence</strong>.</p>
<h2 id="randomness-as-computational-hardness">Randomness as Computational Hardness</h2>
<p>We have two incompatible definitions of randomness:</p>
<ol>
<li><strong>Information-theoretic</strong>: High Kolmogorov complexity</li>
<li><strong>Computational</strong>: Indistinguishable from random by efficient algorithms</li>
</ol>
<p>LazyDigest <strong>fails</strong> (1) catastrophically—it has tiny Kolmogorov complexity.
LazyDigest <strong>passes</strong> (2) perfectly—assuming secure hash functions.</p>
<p><strong>The profound insight</strong>: &ldquo;Randomness&rdquo; in cryptography is actually <strong>computational hardness</strong> in disguise.</p>
<h2 id="entropy-times-arrow-and-laplaces-demon">Entropy, Time&rsquo;s Arrow, and Laplace&rsquo;s Demon</h2>
<p>Consider a being with unlimited computational power (Laplace&rsquo;s demon):</p>
<ul>
<li>LazyDigest is trivially non-random—just compute <code>h(seed||i)</code></li>
<li>No entropy exists—only deterministic evolution</li>
<li>Past and future are equally accessible</li>
</ul>
<p>But <strong>we are computationally bounded</strong>, which creates our entire experience:</p>
<ul>
<li>Past is fixed (we remember it)</li>
<li>Future is uncertain (cannot compute fast enough)</li>
<li>Entropy increases (cannot invert dynamics)</li>
<li><strong>Randomness exists</strong> (cannot distinguish from true random)</li>
</ul>
<p>A hash function is a &ldquo;fast-forward only&rdquo; time machine:</p>
<ul>
<li><strong>Forward</strong>: <code>h(x)</code> is easy</li>
<li><strong>Backward</strong>: Finding <code>x</code> given <code>h(x)</code> is computationally infeasible</li>
</ul>
<h2 id="the-p--np-bet">The P ≠ NP Bet</h2>
<p>Every use of LazyDigest implicitly bets that <strong>P ≠ NP</strong>. If P = NP, we could invert the hash function in polynomial time, distinguishing LazyDigest from random.</p>
<p>We&rsquo;re not generating randomness—we&rsquo;re <strong>generating a computationally hard problem and hoping nobody can solve it</strong>.</p>
<h2 id="applications">Applications</h2>
<p><strong>Deterministic Test Data</strong>: Reproducible infinite test streams
<strong>Memory-Hard KDF</strong>: Force memory access patterns for password hashing
<strong>Blockchain PoW</strong>: Model mining as searching the output space</p>
<h2 id="the-beautiful-paradox">The Beautiful Paradox</h2>
<p>LazyDigest embodies a beautiful paradox:</p>
<ul>
<li><strong>Simple</strong>: Just hash seed concatenated with index</li>
<li><strong>Complex</strong>: Output appears completely random</li>
<li><strong>Finite</strong>: Only 256 bits of true information</li>
<li><strong>Infinite</strong>: Can generate unbounded output</li>
<li><strong>Deterministic</strong>: Same seed always gives same sequence</li>
<li><strong>Unpredictable</strong>: Cannot predict next value without seed</li>
</ul>
<p>This paradox is <strong>the heart of modern cryptography</strong>: simple deterministic processes that appear complex and random to bounded observers.</p>
<h2 id="a-constructivist-victory">A Constructivist Victory</h2>
<p>Perhaps most profoundly, LazyDigest represents a victory for constructivism in mathematics. While classical mathematics struggles with incoherent infinities and uncomputable reals, we&rsquo;ve built something better: <strong>a finite, computable object that serves all practical purposes of an infinite random sequence</strong>.</p>
<p>The &ldquo;deception&rdquo; is beautiful because it&rsquo;s <strong>honest</strong>:</p>
<ul>
<li>We don&rsquo;t claim infinite objects—we have finite programs generating unbounded output</li>
<li>We don&rsquo;t pretend uncomputable things exist—we work with computable approximations</li>
<li>We don&rsquo;t hide behind axioms—we build finite machines and study their behavior</li>
</ul>
<p>Cryptography forces us to be constructivists. The boundary of cryptographic possibility is precisely the boundary of computational constructibility.</p>
<p>In a universe that may itself be computational, LazyDigest isn&rsquo;t approximating some &ldquo;true&rdquo; random oracle in a Platonic realm. <strong>It may be as real as randomness gets</strong>.</p>
<p><a href="/latex/beautiful-deception/beautiful_deception.pdf">View the full paper (PDF)</a></p>
<h2 id="key-takeaways">Key Takeaways</h2>
<ol>
<li>True random oracles are <strong>impossible to implement</strong> (proven constructively)</li>
<li>LazyDigest uses <strong>256 bits to simulate infinity</strong> through computational hardness</li>
<li>&ldquo;Randomness&rdquo; in cryptography means <strong>computationally indistinguishable from random</strong></li>
<li>The deception works because we&rsquo;re all <strong>computationally bounded observers</strong></li>
<li>This represents a <strong>constructivist approach</strong> to mathematics and computation</li>
</ol>
]]></content:encoded><category>cryptography</category><category>hash-functions</category><category>randomness</category><category>information-theory</category><category>kolmogorov-complexity</category></item><item><title>Perfect Hashing: Space Bounds, Entropy, and Cryptographic Security</title><link>https://metafunctor.com/post/2024-02-01-crypto-perf-hash/</link><pubDate>Thu, 01 Feb 2024 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-02-01-crypto-perf-hash/</guid><description>Analyzing the space bounds, entropy requirements, and cryptographic security properties of perfect hash functions.</description><content:encoded><![CDATA[<p>What if a perfect hash function could simultaneously be: (1) cryptographically secure, (2) space-optimal, and (3) maximum-entropy encoded? This paper proves such a construction exists—and analyzes exactly what you sacrifice to get all three.</p>
<h2 id="the-impossible-triangle">The Impossible Triangle</h2>
<p>Perfect hash functions typically face tradeoffs:</p>
<ul>
<li><strong>Space-optimal</strong> constructions (CHD, BDZ) sacrifice randomness</li>
<li><strong>Cryptographic</strong> hash functions waste space on collision resistance</li>
<li><strong>Maximum-entropy</strong> encodings require extra bits</li>
</ul>
<p>We ask: <strong>can you have all three?</strong></p>
<h2 id="the-construction">The Construction</h2>
<p>The data type is <em><em>PH = {0,1}</em> × ℕ</em>* with a constructor <code>ph(X, r) = (n', N)</code> where:</p>
<ul>
<li><strong>N = ⌈m/r⌉</strong>: Hash table size (where m = |X|)</li>
<li><strong>β(x,n) = trunc(hash(x&rsquo; # n&rsquo;), k)&rsquo; mod N</strong>: Hash function parameterized by seed n</li>
<li><strong>n = min{j ∈ ℕ | β is collision-free on X}</strong>: Search for smallest collision-free seed</li>
<li><strong>n&rsquo;</strong>: Geometric code encoding n (variable-length prefix-free)</li>
</ul>
<p><strong>The algorithm</strong>: Try seeds n = 0, 1, 2, &hellip; until β(·,n) has no collisions on X. Each trial is geometrically distributed with success probability p(m,r), so the expected space for encoding n&rsquo; achieves the <strong>information-theoretic lower bound of ~1.44 bits per element</strong>.</p>
<p><strong>Random oracle assumption</strong>: hash: {0,1}* → {0,1}^∞ outputs uniform random bits, making the final encoding (n&rsquo;, N) indistinguishable from a random bit string.</p>
<h2 id="rate-distortion-tradeoff">Rate-Distortion Tradeoff</h2>
<p>The title&rsquo;s &ldquo;rate-distortion&rdquo; comes from information theory: what&rsquo;s the <strong>rate</strong> (bits per key) for a given <strong>distortion</strong> (lookup time)?</p>
<p><strong>Zero distortion</strong> (O(1) lookup): <code>~n log n</code> bits
<strong>Constant distortion</strong> (small tables): Practical two-level schemes approach <code>~1.44n</code> bits
<strong>Variable distortion</strong>: Trade bits for lookup time continuously</p>
<h2 id="why-cryptographic-matters">Why Cryptographic Matters</h2>
<p>Non-cryptographic perfect hashes are <strong>deterministic</strong>—adversaries can engineer collision-inducing inputs. A cryptographic perfect hash (random oracle) prevents:</p>
<ul>
<li><strong>Adversarial key selection</strong>: Can&rsquo;t craft keys that break the hash</li>
<li><strong>Side-channel attacks</strong>: Encoding reveals no information about keys</li>
<li><strong>Fingerprinting</strong>: Maximum entropy makes encodings look random</li>
</ul>
<h2 id="the-algebra-of-composition">The Algebra of Composition</h2>
<p>Section 5 proves: <strong>composing perfect hash functions preserves injectivity</strong>. If <code>h₁: S → T</code> and <code>h₂: T → U</code> are injective, then <code>h₂ ∘ h₁: S → U</code> is injective.</p>
<p>This connects to my <code>algebraic_hashing</code> library—composition of cryptographic hashes (via XOR) preserves both security AND structure.</p>
<h2 id="connection-to-oblivious-computing">Connection to Oblivious Computing</h2>
<p>This paper is about <strong>minimizing information leakage</strong> while maintaining functionality—exactly the oblivious computing philosophy. The maximum-entropy encoding hides which permutation you&rsquo;re using, even though the function is deterministic.</p>
<p><strong>Research paper</strong> • 4★ • <a href="/latex/crypto-perf-hash/">View PDF</a> • <a href="https://github.com/queelius/crypto-perf-hash">GitHub</a></p>
]]></content:encoded><category>cryptography</category><category>hash functions</category><category>information theory</category><category>space complexity</category><category>entropy</category><category>perfect hashing</category></item><item><title>The Beautiful Deception: How 256 Bits Pretend to be Infinity</title><link>https://metafunctor.com/publications/preprints/beautiful-deception/</link><pubDate>Wed, 10 Jan 2024 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/publications/preprints/beautiful-deception/</guid><description/><content:encoded></content:encoded><category>cryptography</category><category>random oracles</category><category>hash functions</category><category>lazy evaluation</category><category>philosophy</category></item><item><title>Algebraic Hashing: Composable Hash Functions Through XOR</title><link>https://metafunctor.com/post/2024-06-01-algebraic_hashing/</link><pubDate>Tue, 01 Nov 2022 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-06-01-algebraic_hashing/</guid><description>A C++ library for composable hash functions using algebraic structure over XOR, with template metaprogramming.</description><content:encoded><![CDATA[<p>Most hash libraries treat hash functions as black boxes. <strong>Algebraic Hashing</strong> exposes their mathematical structure, letting you compose hash functions like algebraic expressions—with zero runtime overhead.</p>
<h2 id="the-core-insight">The Core Insight</h2>
<p>Hash functions form an <strong>abelian group</strong> under XOR:</p>
<ul>
<li><strong>Closure</strong>: <code>h₁ ⊕ h₂</code> is still a valid hash function</li>
<li><strong>Associativity</strong>: <code>(h₁ ⊕ h₂) ⊕ h₃ = h₁ ⊕ (h₂ ⊕ h₃)</code></li>
<li><strong>Identity</strong>: XOR with zero</li>
<li><strong>Inverses</strong>: Each hash is its own inverse under XOR</li>
</ul>
<p>This algebraic structure is the foundation for <strong>compositional hashing</strong>.</p>
<h2 id="why-this-matters">Why This Matters</h2>
<p><strong>1. Compile-Time Composition</strong></p>
<p>Using C++20 concepts and template metaprogramming:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">composed</span> <span class="o">=</span> <span class="n">fnv1a</span><span class="o">&lt;&gt;</span> <span class="o">^</span> <span class="n">sha256</span><span class="o">&lt;&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">hash</span> <span class="o">=</span> <span class="n">composed</span><span class="p">(</span><span class="s">&#34;data&#34;</span><span class="p">);</span>  <span class="c1">// Zero runtime overhead
</span></span></span></code></pre></div><p>All composition resolves at compile time—no virtual dispatch, no function pointers.</p>
<p><strong>2. Provable Properties</strong></p>
<p>We prove that XOR composition <strong>preserves</strong>:</p>
<ul>
<li>Uniform distribution (under independence)</li>
<li>Avalanche effect (bit changes cascade)</li>
<li>Collision resistance (for cryptographic hashes)</li>
</ul>
<p><strong>3. Universal Interface</strong></p>
<p>Works with <strong>any</strong> hash function—non-cryptographic (FNV-1a), perfect (FKS), or cryptographic (SHA-256). The algebra doesn&rsquo;t care about the implementation.</p>
<h2 id="practical-use-cases">Practical Use Cases</h2>
<ul>
<li><strong>Domain separation</strong>: <code>hash_user ^ hash_timestamp</code> prevents collision attacks across domains</li>
<li><strong>Perfect hashing</strong>: FKS two-level scheme with pluggable base hash functions</li>
<li><strong>Composite keys</strong>: Hash multiple fields independently, then XOR</li>
<li><strong>Type-based hashing</strong>: Different hash functions for different types, composed generically</li>
</ul>
<h2 id="connection-to-oblivious-computing">Connection to Oblivious Computing</h2>
<p>This work shares DNA with my oblivious computing research: <strong>make mathematical structure explicit in the type system</strong>. Just as Bernoulli types enforce privacy invariants algebraically, algebraic hashing enforces compositional invariants through group theory.</p>
<p><strong>Technical</strong>: Modern C++20 header-only library • Zero-cost abstractions via concepts • Production-ready implementations</p>
<p><a href="/latex/algebraic_hashing/">View the whitepaper</a> • <a href="https://github.com/queelius/algebraic_hashing">GitHub</a></p>
]]></content:encoded><category>cryptography</category><category>hash functions</category><category>algebra</category><category>C++</category><category>template metaprogramming</category></item><item><title>Random Oracles in Python</title><link>https://metafunctor.com/post/2016-06-random-oracles-python/</link><pubDate>Sun, 12 Jun 2016 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2016-06-random-oracles-python/</guid><description>Three Python approximations of a random oracle, each illuminating a different tradeoff between true randomness, determinism, and composability.</description><content:encoded><![CDATA[<p>A <strong>random oracle</strong> is a function $\mathcal{O}: {0,1}^* \to {0,1}^\infty$ where each output bit is independently and uniformly random, but the function is consistent &ndash; same input, same output, every time (<a href="https://cseweb.ucsd.edu/~mihir/papers/ro.pdf">Bellare &amp; Rogaway, 1993</a>).</p>
<p>You can&rsquo;t build one. But you can build three approximations, each illuminating a different tradeoff.</p>
<h2 id="approximation-1-true-randomness-cached">Approximation 1: True Randomness, Cached</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">OracleDigest</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;&#34;&#34;Infinite digest backed by true randomness and a cache.&#34;&#34;&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="fm">__init__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">entropy</span><span class="o">=</span><span class="kc">None</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="bp">self</span><span class="o">.</span><span class="n">_entropy</span> <span class="o">=</span> <span class="n">entropy</span> <span class="ow">or</span> <span class="p">(</span><span class="k">lambda</span><span class="p">:</span> <span class="n">hashlib</span><span class="o">.</span><span class="n">sha256</span><span class="p">(</span><span class="n">os</span><span class="o">.</span><span class="n">urandom</span><span class="p">(</span><span class="mi">32</span><span class="p">))</span><span class="o">.</span><span class="n">digest</span><span class="p">())</span>
</span></span><span class="line"><span class="cl">        <span class="bp">self</span><span class="o">.</span><span class="n">_cache</span> <span class="o">=</span> <span class="p">{}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="fm">__getitem__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">index</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="n">index</span> <span class="ow">not</span> <span class="ow">in</span> <span class="bp">self</span><span class="o">.</span><span class="n">_cache</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">            <span class="bp">self</span><span class="o">.</span><span class="n">_cache</span><span class="p">[</span><span class="n">index</span><span class="p">]</span> <span class="o">=</span> <span class="bp">self</span><span class="o">.</span><span class="n">_entropy</span><span class="p">()[</span><span class="mi">0</span><span class="p">]</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="bp">self</span><span class="o">.</span><span class="n">_cache</span><span class="p">[</span><span class="n">index</span><span class="p">]</span>
</span></span></code></pre></div><p>Each new index draws a fresh random byte from the OS entropy pool and caches it. Genuinely random, but the cache grows without bound, can&rsquo;t be serialized, and dies with the process. Each instance is a <em>different</em> random oracle from the family of all possible oracles.</p>
<h2 id="approximation-2-deterministic-infinite">Approximation 2: Deterministic, Infinite</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">LazyDigest</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;&#34;&#34;Infinite deterministic digest via counter-mode PRF: h(seed || i).&#34;&#34;&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="fm">__init__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">seed</span><span class="p">,</span> <span class="n">hash_fn</span><span class="o">=</span><span class="n">hashlib</span><span class="o">.</span><span class="n">sha256</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="bp">self</span><span class="o">.</span><span class="n">_seed</span> <span class="o">=</span> <span class="n">seed</span>
</span></span><span class="line"><span class="cl">        <span class="bp">self</span><span class="o">.</span><span class="n">_hash_fn</span> <span class="o">=</span> <span class="n">hash_fn</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="fm">__getitem__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">index</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="n">h</span> <span class="o">=</span> <span class="bp">self</span><span class="o">.</span><span class="n">_hash_fn</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">        <span class="n">h</span><span class="o">.</span><span class="n">update</span><span class="p">(</span><span class="bp">self</span><span class="o">.</span><span class="n">_seed</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="n">h</span><span class="o">.</span><span class="n">update</span><span class="p">(</span><span class="n">index</span><span class="o">.</span><span class="n">to_bytes</span><span class="p">(</span><span class="mi">8</span><span class="p">,</span> <span class="s2">&#34;big&#34;</span><span class="p">))</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">h</span><span class="o">.</span><span class="n">digest</span><span class="p">()[</span><span class="mi">0</span><span class="p">]</span>
</span></span></code></pre></div><p><a href="https://csrc.nist.gov/pubs/sp/800/108/r1/upd1/final">Counter-mode PRF</a>: $\text{byte}_i = H(\text{seed} | i)[0]$. Same seed = same infinite sequence, on any machine, forever. $O(1)$ space, random access to any position. Only as &ldquo;random&rdquo; as the seed and hash function.</p>
<p>Note the fixed-width index encoding (<code>to_bytes(8, 'big')</code>). If you used <code>str(index).encode()</code> instead, index <code>10</code> and the concatenation of index <code>1</code> + index <code>0</code> would collide. Real KDFs like <a href="https://www.rfc-editor.org/rfc/rfc5869">HKDF</a> use fixed-width encodings for the same reason.</p>
<h2 id="approximation-3-full-oracle">Approximation 3: Full Oracle</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">OracleHash</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">    <span class="s2">&#34;&#34;&#34;Approximates {0,1}* -&gt; {0,1}^inf via hash + lazy expansion.&#34;&#34;&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="fm">__init__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">hash_fn</span><span class="o">=</span><span class="n">hashlib</span><span class="o">.</span><span class="n">sha256</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="bp">self</span><span class="o">.</span><span class="n">_hash_fn</span> <span class="o">=</span> <span class="n">hash_fn</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="fm">__call__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">LazyDigest</span><span class="p">(</span><span class="bp">self</span><span class="o">.</span><span class="n">_hash_fn</span><span class="p">(</span><span class="n">x</span><span class="p">)</span><span class="o">.</span><span class="n">digest</span><span class="p">(),</span> <span class="bp">self</span><span class="o">.</span><span class="n">_hash_fn</span><span class="p">)</span>
</span></span></code></pre></div><p>Composes the two: hash the input to get a seed, expand with counter-mode PRF. Type: ${0,1}^* \to {0,1}^\infty$.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">oracle</span> <span class="o">=</span> <span class="n">OracleHash</span><span class="p">()</span>
</span></span><span class="line"><span class="cl"><span class="n">d</span> <span class="o">=</span> <span class="n">oracle</span><span class="p">(</span><span class="sa">b</span><span class="s2">&#34;hello&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="nb">print</span><span class="p">(</span><span class="nb">bytes</span><span class="p">(</span><span class="n">d</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nb">range</span><span class="p">(</span><span class="mi">16</span><span class="p">))</span><span class="o">.</span><span class="n">hex</span><span class="p">())</span>  <span class="c1"># 4c9460cf7b89806e9ebc7b94c815d8c0</span>
</span></span></code></pre></div><h2 id="the-tradeoff">The Tradeoff</h2>
<table>
  <thead>
      <tr>
          <th></th>
          <th><code>OracleDigest</code></th>
          <th><code>LazyDigest</code></th>
          <th><code>OracleHash</code></th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Randomness</td>
          <td>True (OS entropy)</td>
          <td>Pseudo (PRF)</td>
          <td>Pseudo (PRF)</td>
      </tr>
      <tr>
          <td>Space</td>
          <td>$O(k)$ queries</td>
          <td>$O(1)$</td>
          <td>$O(1)$</td>
      </tr>
      <tr>
          <td>Serializable</td>
          <td>No</td>
          <td>Yes (seed)</td>
          <td>Yes (hash fn)</td>
      </tr>
      <tr>
          <td>Lifetime</td>
          <td>Process-scoped</td>
          <td>Permanent</td>
          <td>Permanent</td>
      </tr>
  </tbody>
</table>
<h2 id="why-this-matters">Why This Matters</h2>
<p>Random oracles are the backbone of the <a href="https://en.wikipedia.org/wiki/Random_oracle_model">random oracle model</a> (ROM), used throughout cryptography to prove security. The <a href="https://en.wikipedia.org/wiki/Fiat%E2%80%93Shamir_heuristic">Fiat-Shamir transform</a> &ndash; which turns interactive proofs into non-interactive signatures &ndash; is the most important example: replace the verifier&rsquo;s random challenge with $H(\text{transcript})$.</p>
<p>The gap between a random oracle and a real hash function is real. <a href="https://doi.org/10.1145/1008731.1008734">Canetti, Goldreich &amp; Halevi (2004)</a> showed there exist schemes provably secure in the ROM that break when instantiated with <em>any</em> hash function. In practice, it usually works. Understanding why starts with understanding what you&rsquo;re approximating.</p>]]></content:encoded><category>cryptography</category><category>random-oracles</category><category>hash-functions</category><category>python</category></item></channel></rss>