<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:media="http://search.yahoo.com/mrss/"><channel><title>Image-Processing on metafunctor</title><link>https://metafunctor.com/tags/image-processing/</link><description>Research Engineer &amp; Computer Scientist - Machine Learning, Statistical Computing, Open Source Development</description><language>en-us</language><managingEditor>lex@metafunctor.com (Alex Towell)</managingEditor><webMaster>lex@metafunctor.com (Alex Towell)</webMaster><lastBuildDate>Thu, 09 Apr 2026 05:32:43 -0500</lastBuildDate><atom:link href="https://metafunctor.com/tags/image-processing/" rel="self" type="application/rss+xml"/><image><url>https://metafunctor.com/images/og-image.jpg</url><title>metafunctor</title><link>https://metafunctor.com/</link></image><item><title>chop: When Every Command Returns the Same Kind of Thing</title><link>https://metafunctor.com/post/2025-07-15-chop/</link><pubDate>Tue, 15 Jul 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2025-07-15-chop/</guid><description>27 image commands, one constraint: read JSON, write JSON. The closure property as a generative design principle.</description><content:encoded><![CDATA[<p>Section 2.2 of <em>Structure and Interpretation of Computer Programs</em> introduces the <strong>closure property</strong>: the result of combining things should be the same kind of thing you started with. <code>cons</code> two values and you get a pair, which you can <code>cons</code> again. This is what makes recursive data structures possible. Without it, you get flat records. With it, you get trees, lists, nested structure of arbitrary depth.</p>
<p>Abelson and Sussman are careful to distinguish this from <em>lexical</em> closures (functions that capture their environment). Algebraic closure is about the <em>type signature</em> of combination: if the output type matches the input type, composition is unlimited.</p>
<p>Most discussions of closure treat it as a property to <em>verify</em>. You check whether your algebra is closed and move on. But closure is more powerful than that. It&rsquo;s a <strong>design method</strong>: choose a type, force every operation to consume and produce that type, and see what emerges. The constraint does the creative work.</p>
<p><a href="https://github.com/queelius/chop"><strong>chop</strong></a> is an image-manipulation CLI built on exactly this principle. 27 commands. One rule: read JSON from stdin, write JSON to stdout.</p>
<h2 id="the-problem-with-image-pipelines">The Problem with Image Pipelines</h2>
<p>Traditional image CLIs violate closure. ImageMagick consumes a file and produces a file. Each invocation is terminal. The output is pixels, not something you can pipe into further processing without going back to disk. Composition happens through flag accumulation inside a single command, not through the shell&rsquo;s native composition mechanism.</p>
<p>You can&rsquo;t <code>tee</code> a midpoint. You can&rsquo;t save a half-finished pipeline as a recipe and apply it later. You can&rsquo;t branch.</p>
<p>The SICP parallel is direct: if <code>cons</code> produced an atom instead of a pair, you could build flat structures but not recursive ones. If an image command produces pixels instead of a composable description, you can build single transformations but not pipelines.</p>
<h2 id="one-constraint-json-in-json-out">One Constraint: JSON In, JSON Out</h2>
<p>Every <code>chop</code> command reads a <code>PipelineState</code> JSON object from stdin, appends one operation, and writes the updated JSON to stdout. The wire format carries no image data, only a recipe:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;version&#34;</span><span class="p">:</span> <span class="mi">3</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;ops&#34;</span><span class="p">:</span> <span class="p">[</span>
</span></span><span class="line"><span class="cl">    <span class="p">[</span><span class="s2">&#34;load&#34;</span><span class="p">,</span> <span class="p">[</span><span class="s2">&#34;photo.jpg&#34;</span><span class="p">],</span> <span class="p">{}],</span>
</span></span><span class="line"><span class="cl">    <span class="p">[</span><span class="s2">&#34;resize&#34;</span><span class="p">,</span> <span class="p">[</span><span class="s2">&#34;50%&#34;</span><span class="p">],</span> <span class="p">{}],</span>
</span></span><span class="line"><span class="cl">    <span class="p">[</span><span class="s2">&#34;pad&#34;</span><span class="p">,</span> <span class="p">[</span><span class="mi">10</span><span class="p">],</span> <span class="p">{</span><span class="nt">&#34;color&#34;</span><span class="p">:</span> <span class="s2">&#34;white&#34;</span><span class="p">}]</span>
</span></span><span class="line"><span class="cl">  <span class="p">],</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;metadata&#34;</span><span class="p">:</span> <span class="p">{}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Each operation is a <code>[name, args, kwargs]</code> triple. The pipeline accumulates operations as data. Here&rsquo;s what this looks like in practice:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">chop load photo.jpg <span class="p">|</span> chop resize 50% <span class="p">|</span> chop save out.png
</span></span></code></pre></div><p>And here&rsquo;s what flows between the pipes, the actual stdout of <code>chop resize 50%</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="p">{</span><span class="nt">&#34;version&#34;</span><span class="p">:</span><span class="mi">3</span><span class="p">,</span><span class="nt">&#34;ops&#34;</span><span class="p">:[[</span><span class="s2">&#34;load&#34;</span><span class="p">,[</span><span class="s2">&#34;photo.jpg&#34;</span><span class="p">],{}],[</span><span class="s2">&#34;resize&#34;</span><span class="p">,[</span><span class="s2">&#34;50%&#34;</span><span class="p">],{}]],</span><span class="nt">&#34;metadata&#34;</span><span class="p">:{}}</span>
</span></span></code></pre></div><p>The type signature is <code>PipelineState -&gt; PipelineState</code>. Every command, without exception, consumes and produces the same kind of thing. Closure holds.</p>
<h2 id="what-emerges">What Emerges</h2>
<p>The interesting part isn&rsquo;t the constraint itself. It&rsquo;s what the constraint produces. Four capabilities fall out of the design that were never planned individually.</p>
<h3 id="non-terminal-save">Non-Terminal Save</h3>
<p>In most CLIs, <code>save</code> is a terminus. In <code>chop</code>, <code>save</code> materializes the pipeline (touches pixels for the first time), writes the file as a side effect to stderr, and then <em>outputs its JSON state like every other command</em>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">chop load photo.jpg <span class="p">|</span> chop save full.png <span class="p">|</span> chop resize 50% <span class="p">|</span> chop save thumb.png
</span></span></code></pre></div><p>Two output files from a single pipeline. This works because <code>save</code> isn&rsquo;t special. It follows the same contract as <code>resize</code> or <code>blur</code>. The uniform type signature means there&rsquo;s no distinction between &ldquo;intermediate&rdquo; and &ldquo;terminal&rdquo; commands. Every command is both.</p>
<h3 id="recipes-as-data">Recipes as Data</h3>
<p>What happens if you build a pipeline without <code>load</code>? You get an unbound program, a sequence of operations with no input image:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">chop resize 50% <span class="p">|</span> chop pad <span class="m">10</span> <span class="p">|</span> chop grayscale &gt; recipe.json
</span></span></code></pre></div><p>The resulting JSON is a first-class value:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="p">{</span><span class="nt">&#34;version&#34;</span><span class="p">:</span><span class="mi">3</span><span class="p">,</span><span class="nt">&#34;ops&#34;</span><span class="p">:[[</span><span class="s2">&#34;resize&#34;</span><span class="p">,[</span><span class="s2">&#34;50%&#34;</span><span class="p">],{}],[</span><span class="s2">&#34;pad&#34;</span><span class="p">,[</span><span class="mi">10</span><span class="p">],{}],[</span><span class="s2">&#34;grayscale&#34;</span><span class="p">,[],{}]],</span><span class="nt">&#34;metadata&#34;</span><span class="p">:{}}</span>
</span></span></code></pre></div><p>Apply it later with <code>apply</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">chop load photo.jpg <span class="p">|</span> chop apply recipe.json <span class="p">|</span> chop save out.png
</span></span></code></pre></div><p>This is SICP Chapter 4&rsquo;s programs-as-data principle. A pipeline without <code>load</code> is a function waiting for an argument. The <code>apply</code> command is function application. And because recipes are JSON, they&rsquo;re inspectable, version-controllable, and shareable. You can commit a <code>thumbnail-recipe.json</code> alongside your build scripts.</p>
<h3 id="multi-image-composition">Multi-Image Composition</h3>
<p>The JSON format naturally extends to labeled images. Load with <code>--as</code> to name images; target specific images with <code>--on</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">chop load --as <span class="nb">bg</span> photo.jpg <span class="p">|</span> chop load --as <span class="nb">fg</span> logo.png <span class="se">\
</span></span></span><span class="line"><span class="cl">    <span class="p">|</span> chop resize 50% --on <span class="nb">fg</span> <span class="se">\
</span></span></span><span class="line"><span class="cl">    <span class="p">|</span> chop overlay <span class="nb">bg</span> <span class="nb">fg</span> <span class="se">\
</span></span></span><span class="line"><span class="cl">    <span class="p">|</span> chop save watermarked.png
</span></span></code></pre></div><p>Under the hood, <code>materialize()</code> maintains a labeled context: a dictionary mapping names to images with a cursor tracking the &ldquo;current&rdquo; image. Composition operations like <code>overlay</code>, <code>hstack</code>, <code>vstack</code>, and <code>grid</code> take label arguments and combine multiple images into one. The result goes back into the context, and the JSON contract never changes.</p>
<p>This wasn&rsquo;t designed as a separate feature. It&rsquo;s the same <code>PipelineState -&gt; PipelineState</code> contract accommodating richer operation types. The closure property scales.</p>
<h3 id="inspectable-intermediates">Inspectable Intermediates</h3>
<p>Because stdout is always valid JSON, standard Unix tools work at any point in the pipeline:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">chop load photo.jpg <span class="p">|</span> chop resize 50% <span class="p">|</span> tee step.json <span class="p">|</span> chop pad <span class="m">10</span> <span class="p">|</span> chop save out.png
</span></span></code></pre></div><p><code>tee</code> captures the intermediate state. <code>jq</code> can query it. <code>diff</code> can compare two pipeline states. <code>wc -c</code> tells you the recipe size. None of this required any design effort. It&rsquo;s a consequence of the output being structured text rather than binary pixels.</p>
<h2 id="lazy-evaluation">Lazy Evaluation</h2>
<p>Nothing in the pipe is an image. The JSON carries <code>[name, args, kwargs]</code> triples, a <em>description</em> of computation, not the computation itself. No pixels are loaded, no transforms applied, until a command explicitly materializes the pipeline.</p>
<p>Only two commands trigger materialization: <code>save</code> (to write a file) and <code>info</code> (to report dimensions and metadata). Everything else is pure recording.</p>
<p>This is SICP Chapter 3.5&rsquo;s insight about streams: separate the <em>description</em> of a computation from its <em>execution</em>. A pipeline of ten operations doesn&rsquo;t create ten intermediate images. It creates one JSON object with ten entries, then executes them all in a single pass at the end.</p>
<p>The <code>materialize()</code> method in <code>pipeline.py</code> walks the ops list, building a labeled image context as it goes (loading files, applying transforms, executing compositions) and returns the image at the cursor. It&rsquo;s the only eager path in the system.</p>
<h2 id="the-architecture">The Architecture</h2>
<p>The codebase is small and sharply factored:</p>
<ul>
<li>
<p><strong><code>output.py</code></strong> (19 lines): One function. Writes pipeline state as JSON to stdout. No conditions, no flags. This is where the closure constraint lives, and it&rsquo;s 19 lines because enforcing uniformity is trivial.</p>
</li>
<li>
<p><strong><code>operations.py</code></strong> (696 lines): Pure <code>Image -&gt; Image</code> functions. Sixteen single-image transforms (resize, crop, rotate, brightness, blur, etc.) and four multi-image compositions (hstack, vstack, overlay, grid). No I/O, no JSON, no state. Just pixel manipulation.</p>
</li>
<li>
<p><strong><code>pipeline.py</code></strong> (311 lines): The <code>PipelineState</code> dataclass. JSON serialization, deserialization, and <code>materialize()</code>, the single method that turns a recipe into pixels.</p>
</li>
<li>
<p><strong><code>cli.py</code></strong> (645 lines): 27 commands, each roughly 10 lines. Read state, append one op, return state. The uniformity of the contract means every command handler has nearly identical structure.</p>
</li>
</ul>
<p>The total is under 1,700 lines. The interesting thing is the ratio: 696 lines of image operations, 19 lines enforcing the design constraint. The constraint is cheap to implement and expensive to violate, which is exactly what you want from an architectural decision.</p>
<hr>
<p>One design decision (every command reads JSON, writes JSON) and four capabilities emerge: non-terminal saves, reusable recipes, multi-image composition, and inspectable intermediates. Plus lazy evaluation for free, because the uniform representation turns out to be a recipe rather than a result.</p>
<p>SICP presents the closure property as something to notice about well-designed systems. But the deeper lesson is that closure is something to <em>impose</em>. Pick a type. Force everything through it. The constraint doesn&rsquo;t limit what you can build. It generates capabilities you wouldn&rsquo;t have thought to design.</p>
<p>The best design decisions are the ones you only have to make once.</p>
<hr>
<p><em>This post is part of a <a href="/series/sicp/">series on SICP</a>, exploring how the ideas from Structure and Interpretation of Computer Programs appear in modern programming practice.</em></p>
]]></content:encoded><category>sicp</category><category>dsl</category><category>unix-philosophy</category><category>python</category><category>image-processing</category><category>closure-property</category><category>lazy-evaluation</category></item><item><title>Pipes All the Way Down</title><link>https://metafunctor.com/post/2024-06-15-chop-pipelines/</link><pubDate>Sat, 15 Jun 2024 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-06-15-chop-pipelines/</guid><description>An interactive exploration of the Unix pipeline philosophy through image manipulation</description><content:encoded><![CDATA[<p>In 1964, Doug McIlroy wrote a memo that changed how we think about programs. He wanted tools that connected cleanly, like a garden hose: screw two ends together and water flows. His idea took shape as the Unix pipe.</p>
<p>Fifty years later you still use it every day. <code>ls | grep .ts | wc -l</code>. Three programs, two pipes, one number. Each program has no idea the others exist. They all just read from stdin and write to stdout, and the shell handles the plumbing.</p>
<p>This post explores that idea through image processing. Instead of text streams, we have pixel buffers. Instead of <code>grep</code> and <code>sed</code>, we have <code>blur</code>, <code>rotate</code>, and <code>grayscale</code>. The plumbing is the same.</p>
<div id="chop-pipelines">
<h2 id="one-thing-well">One Thing Well</h2>
<p>McIlroy&rsquo;s first rule: write programs that do one thing and do it well.</p>
<p>A <code>grayscale</code> command converts pixels to luminance values. That is all it does. It does not resize, rotate, or compress. It does not ask what you plan to do next. It takes pixels in and puts pixels out, and it does that one thing correctly.</p>
<p>This constraint sounds limiting but it is the source of power. A program that does one thing can be composed with any other program that does one thing. A program that does many things can only be composed with programs that expect that exact combination.</p>
<p>The widget below shows individual operations in isolation. Pick an operation, adjust its parameter, and watch the result. Notice how each one has a clear, bounded effect.</p>
<div id="chop-one-thing"></div>
<h2 id="the-pipe">The Pipe</h2>
<p>When you write <code>cat image | grayscale | blur 5</code>, something interesting happens. The shell connects the output of each program directly to the input of the next. The intermediate images never need a name. They exist only as data flowing from one process to the next.</p>
<p>This is what makes pipelines powerful: order matters, not identity. You can blur before grayscaling or after, and you get different results. The pipeline is a recipe, and the order of steps is the recipe&rsquo;s logic.</p>
<p>Pipelines also fail gracefully. If one step errors, the downstream steps never run. You do not get silently corrupted output. You get a stopped pipeline and a clear failure point.</p>
<p>Click the operation buttons to build a pipeline. Click a node in the visualization to remove that step. The JSON below the pipeline shows the serialized form of what you built.</p>
<div id="chop-the-pipe"></div>
<h2 id="more-than-one">More Than One</h2>
<p>A single stream is useful. But sometimes you have two images and you want to work on them in parallel before combining them. This is where the <code>--as</code> and <code>--on</code> flags come in.</p>
<p><code>chop load a.png --as left | blur 3 --on left | load b.png --as right | rotate 90 --on right | hstack</code></p>
<p>The context is now a dictionary: a named slot for each image. <code>--as</code> loads an image into a named slot. <code>--on</code> targets an operation at a specific slot. <code>hstack</code> reads all slots and stacks them side by side.</p>
<p>This is still a pipeline. Each step takes a context and returns a context. The context just happens to hold multiple images instead of one. The composition at the end (hstack, vstack, overlay) collapses the dictionary back into a single image.</p>
<p>The widget below lets you build multi-image pipelines. Notice how the visualization splits into parallel tracks, one per named image, converging at the composition step.</p>
<div id="chop-more-than-one"></div>
<h2 id="programs-as-data">Programs as Data</h2>
<p>Here is the part most people miss: a pipeline is just a list of instructions. You can write it down, save it to a file, send it to a friend, and run it on a different image.</p>
<p>The JSON you saw in the previous sections is not a side effect. It is the canonical representation of the pipeline. The visualization is derived from it. The execution is driven by it. The JSON is the program.</p>
<p>This is what McIlroy meant by composability at a deeper level. Not just that programs can be connected at runtime, but that the connection itself can be treated as data. You can write programs that generate pipelines. You can write programs that transform pipelines. You can store pipelines in a database and run them later.</p>
<p>Lazy evaluation is the mechanism. The pipeline does not run when you add a step. It runs when you ask for the output. This means you can build up a recipe incrementally, inspect it, modify it, and only then commit to the computation.</p>
<p>Build a recipe here by adding operations. Watch the JSON update in real time. Apply the same recipe to different images and see how they diverge.</p>
<div id="chop-programs-data"></div>
<h2 id="putting-it-together">Putting It Together</h2>
<p>The sandbox below has everything: multiple image slots, all available operations, composition steps, and a download button. There are no constraints on what you can build.</p>
<p>A few things worth trying: load two images and overlay one on the other with reduced opacity. Build a pipeline with five or six steps and watch the JSON grow. Apply an aggressive blur, then an edge-detect, and see what comes out.</p>
<p>The interesting thing about an unconstrained sandbox is what you reach for first. That usually tells you something about which operations feel natural to compose and which feel like dead ends.</p>
<div id="chop-sandbox"></div>
</div>
<style>
/* =========================================================
   chop-pipelines styles — scoped under #chop-pipelines
   ========================================================= */

#chop-pipelines *, #chop-pipelines *::before, #chop-pipelines *::after {
  box-sizing: border-box;
}

#chop-pipelines {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  color: #e0e0e0;
}

/* ---------------------------------------------------------
   Pipeline visualization
   --------------------------------------------------------- */

#chop-pipelines .chop-pipeline-container {
  margin: 12px 0;
  min-height: 100px;
}

#chop-pipelines .chop-pipeline-scroll {
  overflow-x: auto;
  -webkit-mask-image: linear-gradient(to right, transparent 0, black 16px, black calc(100% - 16px), transparent 100%);
  mask-image: linear-gradient(to right, transparent 0, black 16px, black calc(100% - 16px), transparent 100%);
}

#chop-pipelines .chop-pipeline-track {
  display: flex;
  align-items: center;
  gap: 0;
  padding: 16px 8px;
}

#chop-pipelines .chop-pipeline-node,
#chop-pipelines .chop-node {
  width: 120px;
  min-width: 120px;
  background: #2a2a3e;
  border: 2px solid #3a3a5e;
  border-radius: 8px;
  padding: 10px;
  text-align: center;
  position: relative;
  cursor: pointer;
  transition: border-color 0.2s, background 0.2s;
}

#chop-pipelines .chop-pipeline-node:hover,
#chop-pipelines .chop-node:hover {
  background: #333355;
}

#chop-pipelines .chop-node-active {
  border-color: #4a9eff;
}

#chop-pipelines .chop-node-error {
  border-color: #e94560;
}

#chop-pipelines .chop-node-label {
  font-size: 11px;
  color: #4a9eff;
  text-transform: uppercase;
  letter-spacing: 1px;
  margin-bottom: 6px;
}

#chop-pipelines .chop-node-thumb {
  max-width: 80px;
  max-height: 60px;
  display: block;
  margin: 0 auto;
  border-radius: 4px;
}

#chop-pipelines .chop-node-info {
  font-size: 10px;
  color: #888;
  margin-top: 4px;
  word-break: break-word;
}

#chop-pipelines .chop-pipe-symbol {
  color: #e94560;
  font-size: 20px;
  font-weight: bold;
  padding: 0 4px;
  flex-shrink: 0;
}

/* ---------------------------------------------------------
   Node popover
   --------------------------------------------------------- */

#chop-pipelines .chop-node-popover {
  position: absolute;
  bottom: 100%;
  left: 50%;
  transform: translateX(-50%);
  background: #0d0d1a;
  border: 1px solid #4a9eff;
  border-radius: 6px;
  padding: 8px 12px;
  z-index: 10;
  max-width: 300px;
  font-size: 11px;
  white-space: pre;
  pointer-events: none;
  margin-bottom: 8px;
}

/* ---------------------------------------------------------
   Multi-track
   --------------------------------------------------------- */

#chop-pipelines .chop-pipeline-track--multi {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

#chop-pipelines .chop-track-label {
  background: #4a9eff;
  color: #0d0d1a;
  font-size: 10px;
  padding: 2px 6px;
  border-radius: 3px;
  font-weight: bold;
  margin-right: 8px;
}

#chop-pipelines .chop-composition-span {
  opacity: 0.8;
}

/* ---------------------------------------------------------
   Controls
   --------------------------------------------------------- */

#chop-pipelines .chop-controls {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
  align-items: center;
  margin-bottom: 12px;
}

#chop-pipelines .chop-palette {
  display: flex;
  gap: 4px;
  flex-wrap: wrap;
  margin-bottom: 8px;
}

#chop-pipelines .chop-op-btn {
  background: #2a2a3e;
  color: #e0e0e0;
  border: 1px solid #3a3a5e;
  padding: 6px 12px;
  border-radius: 4px;
  cursor: pointer;
  font-size: 12px;
  transition: background 0.2s;
}

#chop-pipelines .chop-op-btn:hover {
  background: #3a3a5e;
}

#chop-pipelines .chop-op-btn:active {
  background: #4a9eff;
  color: #0d0d1a;
}

/* ---------------------------------------------------------
   Slider
   --------------------------------------------------------- */

#chop-pipelines .chop-param-slider {
  display: flex;
  align-items: center;
  gap: 8px;
}

#chop-pipelines .chop-slider {
  accent-color: #4a9eff;
  cursor: pointer;
}

#chop-pipelines .chop-slider-label {
  font-size: 12px;
  color: #888;
  min-width: 50px;
}

#chop-pipelines .chop-slider-value {
  font-size: 12px;
  color: #4a9eff;
  min-width: 30px;
  text-align: right;
}

/* ---------------------------------------------------------
   Select dropdowns
   --------------------------------------------------------- */

#chop-pipelines .chop-op-select,
#chop-pipelines .chop-target-select {
  background: #2a2a3e;
  color: #e0e0e0;
  border: 1px solid #3a3a5e;
  padding: 6px 12px;
  border-radius: 4px;
  font-size: 12px;
}

/* ---------------------------------------------------------
   Image slots
   --------------------------------------------------------- */

#chop-pipelines .chop-slots-row {
  display: flex;
  gap: 16px;
  margin-bottom: 12px;
  flex-wrap: wrap;
}

#chop-pipelines .chop-image-slot {
  background: #2a2a3e;
  border: 1px solid #3a3a5e;
  border-radius: 8px;
  padding: 12px;
  text-align: center;
}

#chop-pipelines .chop-slot-label {
  font-size: 11px;
  color: #888;
  margin-bottom: 6px;
  text-transform: uppercase;
  display: block;
}

#chop-pipelines .chop-slot-preview {
  max-width: 120px;
  max-height: 90px;
  border-radius: 4px;
  margin-bottom: 8px;
  display: block;
  margin-left: auto;
  margin-right: auto;
}

#chop-pipelines .chop-slot-upload {
  font-size: 11px;
  max-width: 120px;
}

#chop-pipelines .chop-label-input {
  background: #1a1a2e;
  border: 1px solid #3a3a5e;
  color: #4a9eff;
  padding: 2px 6px;
  border-radius: 3px;
  font-size: 11px;
  width: 60px;
  text-align: center;
  margin-top: 4px;
}

/* ---------------------------------------------------------
   JSON display
   --------------------------------------------------------- */

#chop-pipelines .chop-json-display {
  background: #0d0d1a;
  border: 1px solid #3a3a5e;
  border-radius: 6px;
  padding: 12px;
  margin: 12px 0;
  overflow-x: auto;
}

#chop-pipelines .chop-json-pre {
  margin: 0;
  font-family: 'Courier New', monospace;
  font-size: 12px;
  color: #e0e0e0;
  white-space: pre-wrap;
}

#chop-pipelines .chop-json-toggle {
  background: #2a2a3e;
  color: #e0e0e0;
  border: 1px solid #4a9eff;
  padding: 6px 12px;
  border-radius: 4px;
  cursor: pointer;
  font-size: 12px;
  transition: background 0.2s;
}

#chop-pipelines .chop-json-toggle:hover {
  background: #3a3a5e;
}

/* ---------------------------------------------------------
   Apply area (Section 4)
   --------------------------------------------------------- */

#chop-pipelines .chop-apply-area {
  display: flex;
  gap: 16px;
  flex-wrap: wrap;
  margin-top: 12px;
}

#chop-pipelines .chop-apply-card {
  background: #2a2a3e;
  border: 1px solid #3a3a5e;
  border-radius: 8px;
  padding: 12px;
  flex: 1;
  min-width: 200px;
}

#chop-pipelines .chop-apply-label,
#chop-pipelines .chop-apply-card-label {
  font-size: 12px;
  color: #4a9eff;
  margin-bottom: 8px;
  text-transform: uppercase;
}

#chop-pipelines .chop-apply-pipeline,
#chop-pipelines .chop-mini-pipeline {
  min-height: 80px;
}

/* ---------------------------------------------------------
   Download button
   --------------------------------------------------------- */

#chop-pipelines .chop-download-btn {
  background: #4a9eff;
  color: #0d0d1a;
  border: none;
  padding: 8px 16px;
  border-radius: 4px;
  cursor: pointer;
  font-weight: bold;
  font-size: 13px;
  margin-top: 8px;
}

#chop-pipelines .chop-download-btn:hover {
  background: #6ab0ff;
}

/* ---------------------------------------------------------
   Reset button
   --------------------------------------------------------- */

#chop-pipelines .chop-reset-btn {
  background: #2a2a3e;
  color: #e94560;
  border: 1px solid #e94560;
  padding: 6px 12px;
  border-radius: 4px;
  cursor: pointer;
  font-size: 12px;
  transition: background 0.2s;
  margin-bottom: 8px;
}

#chop-pipelines .chop-reset-btn:hover {
  background: #3a1a24;
}

/* ---------------------------------------------------------
   Add image / misc buttons
   --------------------------------------------------------- */

#chop-pipelines .chop-add-image-btn {
  background: #2a2a3e;
  color: #e0e0e0;
  border: 1px solid #3a3a5e;
  padding: 6px 12px;
  border-radius: 4px;
  cursor: pointer;
  font-size: 12px;
  margin-top: 8px;
  transition: background 0.2s;
}

#chop-pipelines .chop-add-image-btn:hover {
  background: #3a3a5e;
}

#chop-pipelines .chop-add-image-btn:disabled {
  opacity: 0.4;
  cursor: default;
}

/* ---------------------------------------------------------
   Pipeline area
   --------------------------------------------------------- */

#chop-pipelines .chop-pipeline-area {
  margin: 8px 0;
}

/* ---------------------------------------------------------
   Responsive
   --------------------------------------------------------- */

@media (max-width: 640px) {
  #chop-pipelines .chop-controls { flex-direction: column; }
  #chop-pipelines .chop-slots-row { flex-direction: column; }
  #chop-pipelines .chop-apply-area { flex-direction: column; }
}

</style>
<script>
"use strict";(()=>{function T(e,o,n=0,t=0,a=0,r=255){let l=new Uint8ClampedArray(e*o*4);for(let i=0;i<e*o;i++)l[i*4+0]=n,l[i*4+1]=t,l[i*4+2]=a,l[i*4+3]=r;return{data:l,width:e,height:o}}function H(e,o,n){let t=(n*e.width+o)*4;return[e.data[t+0],e.data[t+1],e.data[t+2],e.data[t+3]]}function j(e){return{data:new Uint8ClampedArray(e.data),width:e.width,height:e.height}}function ue(e){let o=j(e);for(let n=0;n<o.width*o.height;n++){let t=n*4,a=o.data[t+0],r=o.data[t+1],l=o.data[t+2],i=Math.round(.299*a+.587*r+.114*l);o.data[t+0]=i,o.data[t+1]=i,o.data[t+2]=i}return o}function fe(e,o){let n=j(e);for(let t=0;t<n.width*n.height;t++){let a=t*4;n.data[a+0]=Math.round(n.data[a+0]*o),n.data[a+1]=Math.round(n.data[a+1]*o),n.data[a+2]=Math.round(n.data[a+2]*o)}return n}function he(e,o){let n=j(e);for(let t=0;t<n.width*n.height;t++){let a=t*4;n.data[a+0]=Math.round((n.data[a+0]-128)*o+128),n.data[a+1]=Math.round((n.data[a+1]-128)*o+128),n.data[a+2]=Math.round((n.data[a+2]-128)*o+128)}return n}function ge(e,o){let n=j(e);for(let t=0;t<n.width*n.height;t++){let a=t*4;n.data[a+3]=Math.round(n.data[a+3]*o)}return n}function K(e,o,n){let t=T(o,n);for(let a=0;a<n;a++)for(let r=0;r<o;r++){let l=Math.floor(r*e.width/o),i=Math.floor(a*e.height/n),[s,c,p,m]=H(e,l,i),d=(a*o+r)*4;t.data[d+0]=s,t.data[d+1]=c,t.data[d+2]=p,t.data[d+3]=m}return t}function xe(e,o,n,t,a){let r=T(t,a);for(let l=0;l<a;l++)for(let i=0;i<t;i++){let[s,c,p,m]=H(e,o+i,n+l),d=(l*t+i)*4;r.data[d+0]=s,r.data[d+1]=c,r.data[d+2]=p,r.data[d+3]=m}return r}function Q(e,o,n,t,a,r){let l=e.width+o*2,i=e.height+o*2,s=T(l,i,n,t,a,r);for(let c=0;c<e.height;c++)for(let p=0;p<e.width;p++){let[m,d,u,f]=H(e,p,c),h=((c+o)*l+(p+o))*4;s.data[h+0]=m,s.data[h+1]=d,s.data[h+2]=u,s.data[h+3]=f}return s}function be(e,o,n,t,a,r){return Q(e,o,n,t,a,r)}function ve(e,o){let n=T(e.width,e.height);for(let t=0;t<e.height;t++)for(let a=0;a<e.width;a++){let r=0,l=0,i=0,s=0,c=0;for(let m=-o;m<=o;m++)for(let d=-o;d<=o;d++){let u=Math.max(0,Math.min(e.width-1,a+d)),f=Math.max(0,Math.min(e.height-1,t+m)),[h,g,M,I]=H(e,u,f);r+=h,l+=g,i+=M,s+=I,c++}let p=(t*e.width+a)*4;n.data[p+0]=Math.round(r/c),n.data[p+1]=Math.round(l/c),n.data[p+2]=Math.round(i/c),n.data[p+3]=Math.round(s/c)}return n}function Z(e,o){let n=e.width+o.width,t=Math.max(e.height,o.height),a={data:new Uint8ClampedArray(n*t*4),width:n,height:t},r=(l,i)=>{for(let s=0;s<l.height;s++)for(let c=0;c<l.width;c++){let[p,m,d,u]=H(l,c,s),f=(s*n+i+c)*4;a.data[f+0]=p,a.data[f+1]=m,a.data[f+2]=d,a.data[f+3]=u}};return r(e,0),r(o,e.width),a}function ee(e,o){let n=Math.max(e.width,o.width),t=e.height+o.height,a={data:new Uint8ClampedArray(n*t*4),width:n,height:t},r=(l,i)=>{for(let s=0;s<l.height;s++)for(let c=0;c<l.width;c++){let[p,m,d,u]=H(l,c,s),f=((i+s)*n+c)*4;a.data[f+0]=p,a.data[f+1]=m,a.data[f+2]=d,a.data[f+3]=u}};return r(e,0),r(o,e.height),a}function te(e,o,n,t,a){let r=j(e),l=a/255;for(let i=0;i<o.height;i++)for(let s=0;s<o.width;s++){let c=n+s,p=t+i;if(c<0||c>=e.width||p<0||p>=e.height)continue;let[m,d,u,f]=H(o,s,i),[h,g,M,I]=H(r,c,p),w=f/255*l,D=I/255,B=w+D*(1-w),P=(p*r.width+c)*4;B===0?(r.data[P+0]=0,r.data[P+1]=0,r.data[P+2]=0,r.data[P+3]=0):(r.data[P+0]=Math.round((m*w+h*D*(1-w))/B),r.data[P+1]=Math.round((d*w+g*D*(1-w))/B),r.data[P+2]=Math.round((u*w+M*D*(1-w))/B),r.data[P+3]=Math.round(B*255))}return r}var Ce=new Map([["grayscale",e=>ue(e)],["brightness",(e,o)=>fe(e,o[0])],["contrast",(e,o)=>he(e,o[0])],["opacity",(e,o)=>ge(e,o[0])],["resize",(e,o)=>{if(o.length===1){let n=o[0]/100;return K(e,Math.max(1,Math.round(e.width*n)),Math.max(1,Math.round(e.height*n)))}return K(e,o[0],o[1])}],["crop",(e,o)=>xe(e,o[0],o[1],o[2],o[3])],["pad",(e,o)=>Q(e,o[0],o[1],o[2],o[3],o[4])],["border",(e,o)=>be(e,o[0],o[1],o[2],o[3],o[4])],["blur",(e,o)=>ve(e,o[0])],["mask",(e,o)=>ye(e,o[0],o[1])],["hstack",e=>e],["vstack",e=>e],["overlay",e=>e]]),k=[{name:"grayscale",label:"Grayscale",category:"transform",param:null},{name:"brightness",label:"Brightness",category:"transform",param:{label:"Factor",min:0,max:4,step:.05,defaultVal:3}},{name:"contrast",label:"Contrast",category:"transform",param:{label:"Factor",min:0,max:4,step:.05,defaultVal:3}},{name:"opacity",label:"Opacity",category:"transform",param:{label:"Factor",min:0,max:1,step:.01,defaultVal:.2}},{name:"resize",label:"Resize",category:"transform",param:{label:"Scale %",min:10,max:200,step:5,defaultVal:50}},{name:"crop",label:"Crop",category:"transform",param:{label:"Width",min:1,max:1024,step:1,defaultVal:32}},{name:"pad",label:"Pad",category:"transform",param:{label:"Amount",min:0,max:64,step:1,defaultVal:16}},{name:"border",label:"Border",category:"transform",param:{label:"Width",min:0,max:64,step:1,defaultVal:8}},{name:"blur",label:"Blur",category:"transform",param:{label:"Radius",min:0,max:20,step:1,defaultVal:5}},{name:"mask",label:"Mask",category:"transform",param:null},{name:"hstack",label:"H-Stack",category:"composition",param:null},{name:"vstack",label:"V-Stack",category:"composition",param:null},{name:"overlay",label:"Overlay",category:"composition",param:{label:"Alpha",min:0,max:255,step:1,defaultVal:255}},{name:"load",label:"Load",category:"structural",param:null},{name:"save",label:"Save",category:"structural",param:null},{name:"apply",label:"Apply",category:"structural",param:null}];function ne(e,o,n,t){let a=Ce.get(e);if(!a)throw new Error(`Unknown operation: "${e}"`);return a(o,n,t)}function ye(e,o,n){let t=j(e),a=e.width,r=e.height,l=(a-1)/2,i=(r-1)/2;for(let s=0;s<r;s++)for(let c=0;c<a;c++){let p;if(o==="circle"){let d=a/2,u=r/2,f=(c-l)/d,h=(s-i)/u,g=Math.sqrt(f*f+h*h);p=1-Math.max(0,Math.min(1,g))}else if(o==="ellipse"){let d=a/2,u=r/2,f=(c-l)/d,h=(s-i)/u,g=Math.sqrt(f*f+h*h);p=1-Math.max(0,Math.min(1,g))}else{let d=n??Math.round(Math.min(a,r)*.1),u=Math.max(0,Math.abs(c-l)-(a/2-d)),f=Math.max(0,Math.abs(s-i)-(r/2-d));p=Math.sqrt(u*u+f*f)<=d?1:0}let m=(s*a+c)*4;t.data[m+3]=Math.round(t.data[m+3]*p)}return t}function O(){return{version:3,ops:[],metadata:{}}}function N(e,o,n,t){let a=[o,n,t];return{...e,ops:[...e.ops,a]}}function F(e,o){if(o<0||o>=e.ops.length)return e;let n=e.ops.filter((t,a)=>a!==o);return{...e,ops:n}}function W(e){return JSON.stringify({version:e.version,ops:e.ops,metadata:e.metadata})}function Ee(e,o,n){let t=Array.from(o.values());if(t.length===0)throw new Error(`${e} requires at least one image in context`);if(t.length===1)return t[0];if(e==="hstack")return t.reduce((i,s)=>Z(i,s));if(e==="vstack")return t.reduce((i,s)=>ee(i,s));let a=n[0]??0,r=n[1]??0,l=n[2]??255;return t.reduce((i,s)=>te(i,s,a,r,l))}function A(e,o){let n=new Map(o),t=n.size>0?n.keys().next().value:null,a=[];a.push({opIndex:-1,opName:"load",image:t!==null?n.get(t):{data:new Uint8ClampedArray(0),width:0,height:0},context:new Map(n)});for(let r=0;r<e.ops.length;r++){let[l,i,s]=e.ops[r];try{if(l==="load"){let d=s.as??`image${n.size}`,u=s.src??d,f=o.get(u)??o.get(d);if(!f)throw new Error(`load: no initial image found for src "${u}"`);n.set(d,f),t=d,a.push({opIndex:r,opName:l,image:n.get(t),context:new Map(n)});continue}if(l==="save"||l==="apply"){a.push({opIndex:r,opName:l,image:t!==null?n.get(t):{data:new Uint8ClampedArray(0),width:0,height:0},context:new Map(n)});continue}if(l==="hstack"||l==="vstack"||l==="overlay"){let d=Ee(l,n,i),u=t??"composed";n.set(u,d),t=u,a.push({opIndex:r,opName:l,image:d,context:new Map(n)});continue}let c=s.on??t;if(!c)throw new Error(`No target image for op "${l}"`);let p=n.get(c);if(!p)throw new Error(`No image labeled "${c}" in context`);let m=ne(l,p,i,s);n.set(c,m),t=c,a.push({opIndex:r,opName:l,image:m,context:new Map(n)})}catch(c){let p=c instanceof Error?c.message:String(c);a.push({opIndex:r,opName:l,image:t!==null&&n.has(t)?n.get(t):{data:new Uint8ClampedArray(0),width:0,height:0},context:new Map(n),error:p})}}return{snapshots:a,context:{images:n,cursor:t}}}function $(e,o=80,n=60){if(e.width===0||e.height===0){let u=document.createElement("canvas");return u.width=1,u.height=1,u.toDataURL()}let t=document.createElement("canvas");t.width=e.width,t.height=e.height;let a=t.getContext("2d");if(!a)throw new Error("pixelBufferToDataURL: could not get 2d context");let r=new ImageData(new Uint8ClampedArray(e.data),e.width,e.height);a.putImageData(r,0,0);let l=o/e.width,i=n/e.height,s=Math.min(1,l,i),c=Math.max(1,Math.round(e.width*s)),p=Math.max(1,Math.round(e.height*s));if(c===e.width&&p===e.height)return t.toDataURL();let m=document.createElement("canvas");m.width=c,m.height=p;let d=m.getContext("2d");if(!d)throw new Error("pixelBufferToDataURL: could not get 2d context for dst");return d.drawImage(t,0,0,c,p),m.toDataURL()}function Me(e,o){let n=e.slice(0,o+1).filter(t=>t.opIndex>=0).map(t=>({op:t.opName}));return JSON.stringify(n,null,2)}function ae(e,o,n,t){let a=document.createElement("div");a.className="chop-node",e.error&&a.classList.add("chop-node-error"),n.activeIndex!==void 0&&n.activeIndex===o&&a.classList.add("chop-node-active"),a.style.position="relative";let r=document.createElement("div");r.className="chop-node-label",r.textContent=e.opName,a.appendChild(r);let l=document.createElement("img");l.className="chop-node-thumb",l.alt=e.opName;try{l.src=$(e.image)}catch{}a.appendChild(l);let i=document.createElement("div");i.className="chop-node-info",e.error&&(i.textContent=e.error),a.appendChild(i);let s=document.createElement("div");s.className="chop-node-popover",s.style.position="absolute",s.style.bottom="100%",s.style.left="50%",s.style.transform="translateX(-50%)",s.style.display="none",s.style.zIndex="10";let c=document.createElement("pre");if(c.textContent=Me(t,o),s.appendChild(c),a.appendChild(s),a.addEventListener("mouseenter",()=>{s.style.display="block"}),a.addEventListener("mouseleave",()=>{s.style.display="none"}),n.onNodeClick){let p=n.onNodeClick;a.addEventListener("click",()=>p(o)),a.style.cursor="pointer"}return a}function q(){let e=document.createElement("div");return e.className="chop-pipe-symbol",e.textContent="|",e}function oe(e,o,n){for(let t=0;t<o.length;t++)t>0&&e.appendChild(q()),e.appendChild(ae(o[t],t,n,o))}function Pe(e){let o=new Set,n=[];for(let t of e)for(let a of t.context.keys())o.has(a)||(o.add(a),n.push(a));return n}function we(e){return e==="hstack"||e==="vstack"||e==="overlay"}function Se(e,o,n){let t=Pe(o);if(t.length<=1){let i=document.createElement("div");i.className="chop-pipeline-track",oe(i,o,n),e.appendChild(i);return}let a=new Map;for(let i of t){let s=document.createElement("div");s.className="chop-pipeline-track chop-pipeline-track--multi";let c=document.createElement("span");c.className="chop-track-label",c.textContent=i,s.appendChild(c),a.set(i,s),e.appendChild(s)}let r=null,l=!1;for(let i=0;i<o.length;i++){let s=o[i],c=ae(s,i,n,o);if(l){i>0&&r.appendChild(q()),r.appendChild(c);continue}if(we(s.opName)){c.classList.add("chop-composition-span");for(let d of a.values()){d.appendChild(q());let u=c.cloneNode(!0);d.appendChild(u)}r=document.createElement("div"),r.className="chop-pipeline-track",e.appendChild(r),l=!0;continue}let p=t[0];if(i>0){let d=o[i-1];for(let u of t){let f=d.context.get(u);if(s.context.get(u)!==f){p=u;break}}}let m=a.get(p);if(m)m.appendChild(q()),m.appendChild(c);else{let d=a.values().next().value;d.appendChild(q()),d.appendChild(c)}}}function U(e,o,n={}){for(;e.firstChild;)e.removeChild(e.firstChild);if(o.length===0)return;let t=document.createElement("div");if(t.className="chop-pipeline-scroll",n.multiTrack)Se(t,o,n);else{let a=document.createElement("div");a.className="chop-pipeline-track",oe(a,o,n),t.appendChild(a)}e.appendChild(t)}function V(e,o){let n=document.createElement("button");return n.className="chop-op-btn",n.textContent=e.label,n.setAttribute("aria-label",e.label),n.addEventListener("click",()=>o(e)),n}function re(e,o){let n=document.createElement("div");n.className="chop-param-slider";let t=document.createElement("span");t.className="chop-slider-label",t.textContent=e.label,n.appendChild(t);let a=document.createElement("input");a.type="range",a.className="chop-slider",a.min=String(e.min),a.max=String(e.max),a.step=String(e.step),a.value=String(e.defaultVal),n.appendChild(a);let r=document.createElement("span");return r.className="chop-slider-value",r.textContent=String(e.defaultVal),n.appendChild(r),a.addEventListener("input",()=>{let l=parseFloat(a.value);r.textContent=String(l),o(l)}),{container:n,getValue:()=>parseFloat(a.value)}}function X(e,o,n){let t=o,a=document.createElement("div");a.className="chop-image-slot";let r=document.createElement("span");r.className="chop-slot-label",r.textContent=e,a.appendChild(r);let l=document.createElement("img");l.className="chop-slot-preview",l.alt=e;try{l.src=$(o)}catch{}a.appendChild(l);let i=document.createElement("input");return i.type="file",i.accept="image/*",i.className="chop-slot-upload",a.appendChild(i),i.addEventListener("change",()=>{let s=i.files?.[0];if(!s)return;let c=new FileReader;c.onload=p=>{let m=p.target?.result;if(!m)return;let d=new Image;d.onload=()=>{let u=document.createElement("canvas");u.width=d.width,u.height=d.height;let f=u.getContext("2d");if(!f)return;f.drawImage(d,0,0);let h=f.getImageData(0,0,d.width,d.height),g=T(d.width,d.height);g.data.set(h.data),t=g,l.src=u.toDataURL(),n(g)},d.src=m},c.readAsDataURL(s)}),{container:a,getImage:()=>t}}function _(e,o){let n=document.createElement("div");n.className="chop-json-display";let t=document.createElement("pre");t.className="chop-json-pre",t.dataset.mode=e,o||(t.style.display="none"),n.appendChild(t);let a=null;if(!o){a=document.createElement("button"),a.className="chop-json-toggle",a.textContent="Show JSON";let l=!1;a.addEventListener("click",()=>{l=!l,t.style.display=l?"block":"none",a.textContent=l?"Hide JSON":"Show JSON"}),n.insertBefore(a,t)}return{container:n,update:l=>{if(e==="ops-only")try{let i=JSON.parse(l);t.textContent=JSON.stringify(i.ops??l,null,2)}catch{t.textContent=l}else t.textContent=l},toggle:a}}function Be(e=150,o=100){let n=T(e,o);for(let t=0;t<o;t++){let a=t/(o-1),r=Math.round(10+a*235),l=Math.round(10+a*120),i=Math.round(80-a*70);for(let s=0;s<e;s++){let c=(t*e+s)*4;n.data[c+0]=r,n.data[c+1]=l,n.data[c+2]=i,n.data[c+3]=255}}return n}function Ne(e=150,o=100,n=15){let t=T(e,o);for(let a=0;a<o;a++)for(let r=0;r<e;r++){let l=Math.floor(r/n),i=Math.floor(a/n),c=(l+i)%2===0?220:40,p=(a*e+r)*4;t.data[p+0]=c,t.data[p+1]=c,t.data[p+2]=c,t.data[p+3]=255}return t}function ke(e=150,o=100){let n=T(e,o),t=(e-1)/2,a=(o-1)/2,r=Math.sqrt(t*t+a*a);for(let l=0;l<o;l++)for(let i=0;i<e;i++){let s=i-t,c=l-a,p=Math.min(1,Math.sqrt(s*s+c*c)/r),m=Math.round(240-p*230),d=Math.round(180-p*160),u=Math.round(40+p*40),f=(l*e+i)*4;n.data[f+0]=m,n.data[f+1]=d,n.data[f+2]=u,n.data[f+3]=255}return n}var L=[{name:"sunset",generate:Be},{name:"checkerboard",generate:Ne},{name:"radial",generate:ke}];function le(e){let o=L[0].generate(),n=k.filter(u=>u.category==="transform"),t=document.createElement("div");t.className="chop-controls";let a=document.createElement("label");a.textContent="Operation: ",a.className="chop-op-label";let r=document.createElement("select");r.className="chop-op-select";for(let u of n){let f=document.createElement("option");f.value=u.name,f.textContent=u.label,r.appendChild(f)}a.appendChild(r),t.appendChild(a);let l=document.createElement("div");l.className="chop-slider-area",t.appendChild(l),e.appendChild(t);let i=document.createElement("div");i.className="chop-pipeline-area",e.appendChild(i);let s=0,c=null;function p(){return n.find(u=>u.name===r.value)??n[0]}function m(){let u=p(),f=O();if(u.param!==null){let M=c?c():u.param.defaultVal;f=N(f,u.name,[M],{})}else f=N(f,u.name,[],{});let h=new Map([["input",o]]),{snapshots:g}=A(f,h);U(i,g)}function d(){for(;l.firstChild;)l.removeChild(l.firstChild);c=null;let u=p();if(u.param!==null){let f=u.param,{container:h,getValue:g}=re(f,()=>{m()});c=g,l.appendChild(h)}m()}r.addEventListener("change",()=>{d()}),d()}function ie(e){let o=L[0].generate(),n=k.filter(p=>p.category==="transform"),t=document.createElement("div");t.className="chop-palette";let a=document.createElement("div");a.className="chop-pipeline-area";let{container:r,update:l,toggle:i}=_("ops-only",!1),s=O();function c(){let p=new Map([["input",o]]),{snapshots:m}=A(s,p);U(a,m,{onNodeClick:d=>{if(d===0)return;let u=m[d].opIndex;u<0||(s=F(s,u),c())}}),l(JSON.stringify({ops:s.ops},null,2))}for(let p of n){let m=V(p,d=>{let u=d.param!==null?[d.param.defaultVal]:[];s=N(s,d.name,u,{}),c()});t.appendChild(m)}e.appendChild(t),e.appendChild(a),i&&e.appendChild(i),e.appendChild(r),c()}function se(e){let o=L[0].generate(),n=L[1].generate(),t=k.filter(v=>v.category==="transform"),a=k.filter(v=>v.category==="composition"),r=O(),l=o,i=n,s=document.createElement("div");s.className="chop-slots-row";let{container:c,getImage:p}=X("Background",o,v=>{l=v,x()}),m=document.createElement("input");m.type="text",m.className="chop-as-input",m.value="bg",m.setAttribute("aria-label","--as label for background"),c.appendChild(m);let{container:d,getImage:u}=X("Foreground",n,v=>{i=v,x()}),f=document.createElement("input");f.type="text",f.className="chop-as-input",f.value="fg",f.setAttribute("aria-label","--as label for foreground"),d.appendChild(f),s.appendChild(c),s.appendChild(d),e.appendChild(s);let h=document.createElement("div");h.className="chop-target-row";let g=document.createElement("label");g.textContent="--on: ",g.className="chop-target-label";let M=document.createElement("select");M.className="chop-target-select";function I(){for(;M.firstChild;)M.removeChild(M.firstChild);let v=[m.value,f.value];for(let C of v){let S=document.createElement("option");S.value=C,S.textContent=C,M.appendChild(S)}}I(),m.addEventListener("input",()=>{I()}),f.addEventListener("input",()=>{I()}),g.appendChild(M),h.appendChild(g),e.appendChild(h);let w=document.createElement("div");w.className="chop-palette";let D=document.createElement("span");D.className="chop-palette-heading",D.textContent="Transforms: ",w.appendChild(D);for(let v of t){let C=V(v,S=>{let J=S.param!==null?[S.param.defaultVal]:[],G={on:M.value};r=N(r,S.name,J,G),x()});w.appendChild(C)}e.appendChild(w);let B=document.createElement("div");B.className="chop-palette";let P=document.createElement("span");P.className="chop-palette-heading",P.textContent="Compose: ",B.appendChild(P);for(let v of a){let C=V(v,S=>{let J=S.param!==null?[S.param.defaultVal]:[];r=N(r,S.name,J,{}),x()});B.appendChild(C)}e.appendChild(B);let z=document.createElement("button");z.className="chop-reset-btn",z.textContent="Reset",z.addEventListener("click",()=>{r=O(),x()}),e.appendChild(z);let R=document.createElement("div");R.className="chop-pipeline-area",e.appendChild(R);let{container:y,update:E,toggle:b}=_("ops-only",!1);b&&e.appendChild(b),e.appendChild(y);function x(){l=p(),i=u();let v=m.value||"bg",C=f.value||"fg",S=new Map([[v,l],[C,i]]),{snapshots:J}=A(r,S);U(R,J,{multiTrack:!0,onNodeClick:G=>{if(G===0)return;let Y=J[G].opIndex;Y<0||(r=F(r,Y),x())}}),E(JSON.stringify(r.ops,null,2))}x()}function ce(e){let o=k.filter(c=>c.category==="transform"),n=O(),t=document.createElement("div");t.className="chop-palette";for(let c of o){let p=V(c,m=>{let d=m.param!==null?[m.param.defaultVal]:[];n=N(n,m.name,d,{}),s()});t.appendChild(p)}e.appendChild(t);let a=document.createElement("button");a.className="chop-reset-btn",a.textContent="Reset",a.addEventListener("click",()=>{n=O(),s()}),e.appendChild(a);let{container:r,update:l}=_("full",!0);e.appendChild(r);let i=document.createElement("div");i.className="chop-apply-area",e.appendChild(i);function s(){for(l(W(n));i.firstChild;)i.removeChild(i.firstChild);for(let c of L){let p=c.generate(),m=document.createElement("div");m.className="chop-apply-card";let d=document.createElement("div");d.className="chop-apply-card-label",d.textContent=c.name,m.appendChild(d);let u=document.createElement("div");u.className="chop-mini-pipeline",m.appendChild(u);let f=new Map([["input",p]]),{snapshots:h}=A(n,f);U(u,h,{onNodeClick:g=>{if(g===0)return;let M=h[g].opIndex;M<0||(n=F(n,M),s())}}),i.appendChild(m)}}s()}var de=4;function pe(e){let o=k.filter(y=>y.category==="transform"),n=k.filter(y=>y.category==="composition"),t=O(),a=[],r=document.createElement("div");r.className="chop-slots-section",e.appendChild(r);let l=document.createElement("div");l.className="chop-slots-row",r.appendChild(l);function i(y){let E=L[y%L.length],b=E.generate(),x=E.name,v=document.createElement("div");v.className="chop-slot-wrapper";let C=document.createElement("input");C.type="text",C.className="chop-as-input",C.value=x,C.setAttribute("aria-label","--as label");let{container:S,getImage:J}=X(x,b,()=>{R()});C.addEventListener("input",()=>{d(),R()}),v.appendChild(S),v.appendChild(C),l.appendChild(v);let G={label:x,getImage:J,labelInput:C};a.push(G),d(),R()}let s=document.createElement("button");s.className="chop-add-image-btn",s.textContent="Add Image",s.addEventListener("click",()=>{a.length<de&&(i(a.length),a.length>=de&&(s.disabled=!0))}),r.appendChild(s);let c=document.createElement("div");c.className="chop-target-row";let p=document.createElement("label");p.textContent="--on: ",p.className="chop-target-label";let m=document.createElement("select");m.className="chop-target-select",p.appendChild(m),c.appendChild(p),e.appendChild(c);function d(){let y=m.value;for(;m.firstChild;)m.removeChild(m.firstChild);for(let E of a){let b=E.labelInput.value||E.label,x=document.createElement("option");x.value=b,x.textContent=b,b===y&&(x.selected=!0),m.appendChild(x)}}let u=document.createElement("div");u.className="chop-palette-section";let f=document.createElement("div");f.className="chop-palette";let h=document.createElement("span");h.className="chop-palette-heading",h.textContent="Transforms: ",f.appendChild(h);for(let y of o){let E=V(y,b=>{let x=b.param!==null?[b.param.defaultVal]:[],v={on:m.value};t=N(t,b.name,x,v),R()});f.appendChild(E)}let g=document.createElement("div");g.className="chop-palette";let M=document.createElement("span");M.className="chop-palette-heading",M.textContent="Compose: ",g.appendChild(M);for(let y of n){let E=V(y,b=>{let x=b.param!==null?[b.param.defaultVal]:[];t=N(t,b.name,x,{}),R()});g.appendChild(E)}u.appendChild(f),u.appendChild(g),e.appendChild(u);let I=document.createElement("div");I.className="chop-pipeline-area",e.appendChild(I);let{container:w,update:D,toggle:B}=_("full",!1);B&&e.appendChild(B),e.appendChild(w);let P=document.createElement("button");P.className="chop-download-btn",P.textContent="Download",P.addEventListener("click",()=>{let y=z(),{context:E}=A(t,y),b=E.cursor;if(!b)return;let x=E.images.get(b);if(x)try{let v=$(x,x.width,x.height),C=document.createElement("a");C.href=v,C.download="chop-result.png",document.body.appendChild(C),C.click(),document.body.removeChild(C)}catch{}}),e.appendChild(P);function z(){let y=new Map;for(let E of a){let b=E.labelInput.value||E.label;y.set(b,E.getImage())}return y}function R(){let y=z();if(y.size===0)return;let{snapshots:E}=A(t,y);U(I,E,{multiTrack:a.length>1,onNodeClick:b=>{if(b===0)return;let x=E[b].opIndex;x<0||(t=F(t,x),R())}}),D(W(t))}i(0)}function me(){let e=document.getElementById("chop-one-thing");e&&le(e);let o=document.getElementById("chop-the-pipe");o&&ie(o);let n=document.getElementById("chop-more-than-one");n&&se(n);let t=document.getElementById("chop-programs-data");t&&ce(t);let a=document.getElementById("chop-sandbox");a&&pe(a)}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",me):me();})();

</script>
]]></content:encoded><category>unix-philosophy</category><category>image-processing</category><category>interactive</category></item></channel></rss>