<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:media="http://search.yahoo.com/mrss/"><channel><title>Sicp on metafunctor</title><link>https://metafunctor.com/tags/sicp/</link><description>Research Engineer &amp; Computer Scientist - Machine Learning, Statistical Computing, Open Source Development</description><language>en-us</language><managingEditor>lex@metafunctor.com (Alex Towell)</managingEditor><webMaster>lex@metafunctor.com (Alex Towell)</webMaster><lastBuildDate>Wed, 18 Feb 2026 18:55:44 -0600</lastBuildDate><atom:link href="https://metafunctor.com/tags/sicp/" rel="self" type="application/rss+xml"/><image><url>https://metafunctor.com/images/og-image.jpg</url><title>metafunctor</title><link>https://metafunctor.com/</link></image><item><title>Building Languages to Solve Problems</title><link>https://metafunctor.com/post/2026-01-19-dsls-sicp/</link><pubDate>Mon, 19 Jan 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2026-01-19-dsls-sicp/</guid><description>When a problem is complex enough, the solution is often to build a language for that problem. SICP's most powerful idea.</description><content:encoded><![CDATA[<p>Chapter 4 of <em>Structure and Interpretation of Computer Programs</em> begins with one of the most profound insights in all of programming: <em>the most powerful technique for controlling complexity is metalinguistic abstraction</em>—the establishment of new languages.</p>
<p>Not libraries. Not frameworks. <em>Languages</em>.</p>
<p>When you&rsquo;ve abstracted enough of a problem domain into primitives, combination rules, and naming mechanisms, you haven&rsquo;t just written code—you&rsquo;ve created a new way of thinking about the problem. The domain becomes expressible. And once something is expressible, it becomes manipulable, debuggable, and shareable.</p>
<h2 id="what-is-metalinguistic-abstraction">What Is Metalinguistic Abstraction?</h2>
<p>The key distinction is between <em>using</em> a language and <em>creating</em> one. A library gives you functions to call. A language gives you a grammar for expressing ideas.</p>
<p>Consider the difference:</p>
<p><strong>Library approach</strong>: Call <code>db.execute(&quot;SELECT * FROM users WHERE age &gt; 21&quot;)</code></p>
<p><strong>Language approach</strong>: Write <code>SELECT * FROM users WHERE age &gt; 21</code></p>
<p>SQL isn&rsquo;t a library. It&rsquo;s a language—with primitives (tables, columns), means of combination (joins, unions, subqueries), and means of abstraction (views, CTEs). These three elements—primitives, combination, abstraction—are SICP&rsquo;s fundamental criteria for any language, and they&rsquo;re what separates a DSL from a mere API.</p>
<p>Other examples abound:</p>
<ul>
<li><strong>Regular expressions</strong>: primitives (characters, character classes), combination (concatenation, alternation), abstraction (groups, backreferences)</li>
<li><strong>Make</strong>: primitives (targets, prerequisites), combination (dependency chains), abstraction (pattern rules, variables)</li>
<li><strong>CSS selectors</strong>: primitives (elements, classes, IDs), combination (descendant, child, sibling), abstraction (custom properties, mixins in preprocessors)</li>
</ul>
<p>In each case, the language captures the <em>essential structure</em> of the problem domain in a way that raw code cannot.</p>
<h2 id="the-three-requirements">The Three Requirements</h2>
<p>SICP identifies three necessary components for any language:</p>
<ol>
<li><strong>Primitives</strong>: What are the basic elements that cannot be broken down further?</li>
<li><strong>Means of combination</strong>: How do you build compound elements from simpler ones?</li>
<li><strong>Means of abstraction</strong>: How do you name and reuse patterns?</li>
</ol>
<p>When designing a DSL, these questions guide everything. Get them wrong and you have a clunky API. Get them right and the domain becomes <em>thinkable</em> in your language.</p>
<p>Consider an expression language for symbolic math:</p>
<ul>
<li><strong>Primitives</strong>: numbers, symbols, operators</li>
<li><strong>Combination</strong>: function application <code>(+ x 1)</code>, nested expressions <code>(* (+ x 1) 2)</code></li>
<li><strong>Abstraction</strong>: named rules, rulesets, engines</li>
</ul>
<p>Or a query language for JSON documents:</p>
<ul>
<li><strong>Primitives</strong>: field access, array indexing, literals</li>
<li><strong>Combination</strong>: boolean operators, path composition</li>
<li><strong>Abstraction</strong>: named queries, parameterized patterns</li>
</ul>
<h2 id="the-closure-property">The Closure Property</h2>
<p>Beyond the three requirements, SICP emphasizes a crucial design principle: the <strong>closure property</strong>. This isn&rsquo;t about closures in the functional programming sense—it&rsquo;s about algebraic closure: <em>the result of combining things should be the same kind of thing</em>.</p>
<p>In Scheme, combining procedures yields a procedure. You can pass it, return it, and combine it again without special cases. This enables arbitrary composition.</p>
<p>In a well-designed DSL:</p>
<ul>
<li>Combining queries yields a query</li>
<li>Combining transformations yields a transformation</li>
<li>Combining rules yields a ruleset</li>
</ul>
<p>When closure holds, your language has compositional depth. Users can build arbitrarily complex structures from simple pieces, and the cognitive load stays constant because they&rsquo;re always working with the same kind of thing.</p>
<h2 id="dsls-in-my-projects">DSLs in My Projects</h2>
<p>Several projects in this codebase embody these principles:</p>
<h3 id="rerum-rules-as-data">Rerum: Rules as Data</h3>
<p><a href="/post/2025-12-16-rerum/">Rerum</a> is a pattern matching and term rewriting library with a declarative rule DSL:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">@add-zero[100]: (+ ?x 0) =&gt; :x
</span></span><span class="line"><span class="cl">@mul-one[100]:  (* ?x 1) =&gt; :x
</span></span></code></pre></div><p>Rules are data—loadable, combinable, inspectable. Engines compose:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">normalize</span> <span class="o">=</span> <span class="n">expand</span> <span class="o">&gt;&gt;</span> <span class="n">simplify</span>  <span class="c1"># Sequence</span>
</span></span><span class="line"><span class="cl"><span class="n">combined</span> <span class="o">=</span> <span class="n">algebra</span> <span class="o">|</span> <span class="n">calculus</span>   <span class="c1"># Union</span>
</span></span></code></pre></div><p>Combining engines yields an engine. Closure holds.</p>
<h3 id="jsonl-algebra-relational-algebra-for-json">jsonl-algebra: Relational Algebra for JSON</h3>
<p><a href="/post/2024-12-jsonl-algebra/">jsonl-algebra</a> lifts relational algebra concepts to nested JSON:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ja <span class="s1">&#39;select(name, age) | where(.age &gt; 21) | sort(name)&#39;</span> data.jsonl
</span></span></code></pre></div><p>Each operation takes a relation and returns a relation. Pipes compose operations. The Unix philosophy meets relational algebra, and closure means you can chain operations arbitrarily.</p>
<h3 id="accumux-compositional-statistics">Accumux: Compositional Statistics</h3>
<p><a href="/post/2024-03-01-accumux/">Accumux</a> treats running statistics as composable monoids:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">stats</span> <span class="o">=</span> <span class="n">count</span><span class="p">()</span> <span class="o">&amp;</span> <span class="n">mean</span><span class="p">(</span><span class="s2">&#34;x&#34;</span><span class="p">)</span> <span class="o">&amp;</span> <span class="n">variance</span><span class="p">(</span><span class="s2">&#34;x&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">result</span> <span class="o">=</span> <span class="n">fold</span><span class="p">(</span><span class="n">stats</span><span class="p">,</span> <span class="n">data_stream</span><span class="p">)</span>
</span></span></code></pre></div><p>Combining accumulators yields an accumulator. Parallel streams can be folded independently and merged—the monoid laws guarantee correctness.</p>
<h3 id="dotsuitejaf-path-expressions-over-documents">dotsuite/JAF: Path Expressions over Documents</h3>
<p>The JAF path expression language provides boolean algebra over document structure:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">.metadata.author &amp; (.status == &#34;published&#34; | .featured)
</span></span></code></pre></div><p>Combining predicates yields a predicate. The algebra is closed.</p>
<h2 id="when-to-build-a-dsl">When to Build a DSL</h2>
<p>Not every problem needs a language. Here are signs you might need one:</p>
<p><strong>You should consider a DSL when:</strong></p>
<ul>
<li>The same structural patterns keep appearing across your code</li>
<li>Domain experts need to express logic without writing code</li>
<li>Configuration has grown complex enough to need validation and tooling</li>
<li>You want transformations to be inspectable, serializable, or version-controlled</li>
</ul>
<p><strong>You probably don&rsquo;t need a DSL when:</strong></p>
<ul>
<li>It&rsquo;s a one-off script with no reuse</li>
<li>Simple CRUD operations suffice</li>
<li>The domain doesn&rsquo;t have clear compositional structure</li>
<li>A configuration file would be simpler</li>
</ul>
<p>The litmus test: if you find yourself building a recursive interpreter by accident—parsing strings, handling nested cases, managing state—you&rsquo;re already building a language. You might as well do it intentionally.</p>
<h2 id="the-payoff">The Payoff</h2>
<p>The real benefit of metalinguistic abstraction isn&rsquo;t just cleaner code. It&rsquo;s that the problem domain becomes <em>thinkable</em>.</p>
<p>When you have a language for expressing transformations, you can reason about transformations. You can ask: what happens if I apply this rule before that one? What&rsquo;s the minimal set of rules that covers this domain? Are these two rulesets equivalent?</p>
<p>When you have a language for expressing queries, you can optimize queries, explain query plans, and catch errors at parse time rather than runtime.</p>
<p>When you have a language for expressing configuration, you can validate configurations, diff configurations, and generate configurations from higher-level specifications.</p>
<p>The language makes the domain explicit. And explicit domains are controllable domains.</p>
<p>This is why SICP puts metalinguistic abstraction at the pinnacle of the book. Not because it&rsquo;s technically harder than everything else—but because it&rsquo;s the technique that makes everything else possible.</p>
<hr>
<p><em>This post is part of a <a href="/series/sicp/">series on SICP</a>, exploring how the ideas from Structure and Interpretation of Computer Programs appear in modern programming practice.</em></p>
]]></content:encoded><category>sicp</category><category>dsl</category><category>metalinguistic-abstraction</category><category>programming-languages</category><category>functional-programming</category></item><item><title>compositional.mle: SICP-Inspired Optimization</title><link>https://metafunctor.com/post/2025-12-17-compositional-mle/</link><pubDate>Wed, 17 Dec 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2025-12-17-compositional-mle/</guid><description>An R package where solvers are first-class functions that compose through chaining, racing, and restarts.</description><content:encoded><![CDATA[<p>I&rsquo;ve recently updated <a href="https://github.com/queelius/compositional.mle">compositional.mle</a>, an R package for maximum likelihood estimation that takes seriously the idea that <strong>optimization strategies should compose</strong>.</p>
<h2 id="the-core-insight">The Core Insight</h2>
<p>Most optimization libraries treat solvers as monolithic procedures: you call <code>optim()</code>, pass some options, and hope for the best. If you want to try multiple methods, you write loops. If you want coarse-to-fine optimization, you manually wire the output of one solver to the input of another.</p>
<p><code>compositional.mle</code> takes a different approach, inspired by SICP&rsquo;s treatment of procedures as first-class citizens. The package provides:</p>
<ol>
<li><strong>Primitive solvers</strong> - <code>gradient_ascent()</code>, <code>newton_raphson()</code>, <code>bfgs()</code>, <code>nelder_mead()</code></li>
<li><strong>Composition operators</strong> - <code>%&gt;&gt;%</code> (sequential chaining), <code>%|%</code> (parallel racing), <code>with_restarts()</code></li>
<li><strong>Closure property</strong> - Combining solvers yields a solver</li>
</ol>
<p>That last point is crucial. When you chain two solvers together, the result is itself a solver with the same interface. This means compositions can be further composed, stored in variables, passed to functions, or used anywhere a solver is expected.</p>
<h2 id="what-this-looks-like">What This Looks Like</h2>
<p>Define your problem once:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="n">problem</span> <span class="o">&lt;-</span> <span class="nf">mle_problem</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">  <span class="n">loglike</span> <span class="o">=</span> <span class="kr">function</span><span class="p">(</span><span class="n">theta</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kr">if</span> <span class="p">(</span><span class="n">theta[2]</span> <span class="o">&lt;=</span> <span class="m">0</span><span class="p">)</span> <span class="kr">return</span><span class="p">(</span><span class="o">-</span><span class="kc">Inf</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="nf">sum</span><span class="p">(</span><span class="nf">dnorm</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">theta[1]</span><span class="p">,</span> <span class="n">theta[2]</span><span class="p">,</span> <span class="n">log</span> <span class="o">=</span> <span class="kc">TRUE</span><span class="p">))</span>
</span></span><span class="line"><span class="cl">  <span class="p">},</span>
</span></span><span class="line"><span class="cl">  <span class="n">score</span> <span class="o">=</span> <span class="kr">function</span><span class="p">(</span><span class="n">theta</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">mu</span> <span class="o">&lt;-</span> <span class="n">theta[1]</span><span class="p">;</span> <span class="n">sigma</span> <span class="o">&lt;-</span> <span class="n">theta[2]</span><span class="p">;</span> <span class="n">n</span> <span class="o">&lt;-</span> <span class="nf">length</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="nf">c</span><span class="p">(</span><span class="nf">sum</span><span class="p">(</span><span class="n">x</span> <span class="o">-</span> <span class="n">mu</span><span class="p">)</span> <span class="o">/</span> <span class="n">sigma^2</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">      <span class="o">-</span><span class="n">n</span> <span class="o">/</span> <span class="n">sigma</span> <span class="o">+</span> <span class="nf">sum</span><span class="p">((</span><span class="n">x</span> <span class="o">-</span> <span class="n">mu</span><span class="p">)</span><span class="n">^2</span><span class="p">)</span> <span class="o">/</span> <span class="n">sigma^3</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">  <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span></code></pre></div><p>Then compose strategies declaratively:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="c1"># Global search -&gt; local refinement -&gt; final polish</span>
</span></span><span class="line"><span class="cl"><span class="n">strategy</span> <span class="o">&lt;-</span> <span class="nf">grid_search</span><span class="p">(</span><span class="n">lower</span> <span class="o">=</span> <span class="nf">c</span><span class="p">(</span><span class="m">-10</span><span class="p">,</span> <span class="m">0.5</span><span class="p">),</span> <span class="n">upper</span> <span class="o">=</span> <span class="nf">c</span><span class="p">(</span><span class="m">10</span><span class="p">,</span> <span class="m">5</span><span class="p">),</span> <span class="n">n</span> <span class="o">=</span> <span class="m">5</span><span class="p">)</span> <span class="o">%&gt;&gt;%</span>
</span></span><span class="line"><span class="cl">  <span class="nf">gradient_ascent</span><span class="p">(</span><span class="n">max_iter</span> <span class="o">=</span> <span class="m">50</span><span class="p">)</span> <span class="o">%&gt;&gt;%</span>
</span></span><span class="line"><span class="cl">  <span class="nf">newton_raphson</span><span class="p">(</span><span class="n">max_iter</span> <span class="o">=</span> <span class="m">20</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">result</span> <span class="o">&lt;-</span> <span class="nf">strategy</span><span class="p">(</span><span class="n">problem</span><span class="p">,</span> <span class="n">theta0</span> <span class="o">=</span> <span class="nf">c</span><span class="p">(</span><span class="m">0</span><span class="p">,</span> <span class="m">1</span><span class="p">))</span>
</span></span></code></pre></div><p>Or race multiple approaches:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="c1"># Try all methods, keep the best</span>
</span></span><span class="line"><span class="cl"><span class="n">strategy</span> <span class="o">&lt;-</span> <span class="nf">gradient_ascent</span><span class="p">()</span> <span class="o">%|%</span> <span class="nf">bfgs</span><span class="p">()</span> <span class="o">%|%</span> <span class="nf">nelder_mead</span><span class="p">()</span>
</span></span></code></pre></div><p>Or handle multimodal landscapes:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="c1"># Random restarts to escape local optima</span>
</span></span><span class="line"><span class="cl"><span class="n">strategy</span> <span class="o">&lt;-</span> <span class="nf">with_restarts</span><span class="p">(</span><span class="nf">gradient_ascent</span><span class="p">(),</span> <span class="n">n</span> <span class="o">=</span> <span class="m">10</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                          <span class="n">sampler</span> <span class="o">=</span> <span class="nf">uniform_sampler</span><span class="p">(</span><span class="n">lower</span><span class="p">,</span> <span class="n">upper</span><span class="p">))</span>
</span></span></code></pre></div><h2 id="the-sicp-connection">The SICP Connection</h2>
<p>This design directly applies lessons from <em>Structure and Interpretation of Computer Programs</em>:</p>
<p><strong>Primitives</strong>: The base solvers are well-defined building blocks with clear contracts. <code>gradient_ascent()</code> returns a solver that uses steepest ascent. <code>nelder_mead()</code> returns a derivative-free simplex solver.</p>
<p><strong>Means of Combination</strong>: The operators <code>%&gt;&gt;%</code>, <code>%|%</code>, and <code>with_restarts()</code> combine solvers into new solvers. Sequential chaining feeds the output of one solver as input to the next. Racing runs solvers in parallel and picks the winner.</p>
<p><strong>Abstraction</strong>: Solver factories hide implementation details behind a consistent interface. Users work with the solver abstraction, not with specific algorithms.</p>
<p><strong>Closure</strong>: The key insight. Because composition produces objects of the same type as the inputs, the language of solvers is <em>closed</em> under composition. You can build arbitrarily complex strategies from simple parts.</p>
<h2 id="relationship-to-algebraicmle">Relationship to algebraic.mle</h2>
<p>This package complements <a href="/projects/algebraic.mle/">algebraic.mle</a>, which provides algebraic operations on MLE <em>results</em>. Where <code>algebraic.mle</code> lets you compose likelihood functions and manipulate fitted models, <code>compositional.mle</code> focuses on the <em>process</em> of finding those estimates.</p>
<p>The two work together:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="c1"># compositional.mle: find the estimate</span>
</span></span><span class="line"><span class="cl"><span class="n">result</span> <span class="o">&lt;-</span> <span class="nf">strategy</span><span class="p">(</span><span class="n">problem</span><span class="p">,</span> <span class="n">theta0</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># algebraic.mle: work with the fitted model</span>
</span></span><span class="line"><span class="cl"><span class="nf">confint</span><span class="p">(</span><span class="n">result</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="nf">coef</span><span class="p">(</span><span class="n">result</span><span class="p">)</span>
</span></span></code></pre></div><h2 id="try-it">Try It</h2>
<p>Install from GitHub:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="n">devtools</span><span class="o">::</span><span class="nf">install_github</span><span class="p">(</span><span class="s">&#34;queelius/compositional.mle&#34;</span><span class="p">)</span>
</span></span></code></pre></div><p>Documentation at <a href="https://queelius.github.io/compositional.mle/">queelius.github.io/compositional.mle</a>.</p>
<p>The design philosophy here extends beyond statistics. Any domain with multiple solution strategies can benefit from composable primitives with closure under composition. The question is always: what are your primitives, and how should they combine?</p>
]]></content:encoded><category>r-package</category><category>statistics</category><category>mle</category><category>optimization</category><category>sicp</category><category>functional-programming</category><category>dsl</category><category>api-design</category></item><item><title>Rerum: A Pattern Matching and Term Rewriting Library</title><link>https://metafunctor.com/post/2025-12-16-rerum/</link><pubDate>Tue, 16 Dec 2025 06:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2025-12-16-rerum/</guid><description>Introducing rerum - a Python library for symbolic computation with a readable DSL, powerful pattern matching, and a security-conscious architecture that separates rules from computation.</description><content:encoded><![CDATA[<p><strong>Rerum</strong> (Rewriting Expressions via Rules Using Morphisms) is a Python library I&rsquo;ve been developing for pattern matching and term rewriting. It&rsquo;s designed to make symbolic computation accessible through a readable DSL while maintaining a security-conscious architecture.</p>
<h2 id="the-problem-with-symbolic-computation">The Problem with Symbolic Computation</h2>
<p>Traditional symbolic math systems often have two problems: either they&rsquo;re monolithic (like Mathematica) with everything built-in, or they require writing complex recursive traversals every time you want to transform an expression. What I wanted was something in between: a simple, extensible system where transformation rules are <em>data</em> that can be loaded, combined, and reasoned about.</p>
<h2 id="the-sicp-connection">The SICP Connection</h2>
<p>This design reflects a core insight from <em>Structure and Interpretation of Computer Programs</em>: when a problem domain is complex enough, the solution is often to build a language for that domain. Rerum&rsquo;s rule DSL doesn&rsquo;t just simplify symbolic computation—it makes the transformation logic inspectable, composable, and safe.</p>
<p>The engine composition operators (<code>&gt;&gt;</code> for sequencing, <code>|</code> for union) ensure <strong>closure</strong>: combining engines yields an engine. This is the same principle that makes Scheme&rsquo;s procedures powerful—you can pass them, return them, and combine them without special cases. In rerum, transformation strategies are first-class citizens.</p>
<h2 id="a-readable-dsl">A Readable DSL</h2>
<p>At the heart of rerum is a domain-specific language for defining rewrite rules:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl"># Algebraic simplification
</span></span><span class="line"><span class="cl">@add-zero[100] &#34;x + 0 = x&#34;: (+ ?x 0) =&gt; :x
</span></span><span class="line"><span class="cl">@mul-one[100]:  (* ?x 1) =&gt; :x
</span></span><span class="line"><span class="cl">@mul-zero[100]: (* ?x 0) =&gt; 0
</span></span></code></pre></div><p>Each rule has:</p>
<ul>
<li><strong>A name</strong>: <code>@add-zero</code> for debugging and tracing</li>
<li><strong>Optional priority</strong>: <code>[100]</code> determines firing order when multiple rules match</li>
<li><strong>Optional description</strong>: Human-readable explanation</li>
<li><strong>A pattern</strong>: <code>(+ ?x 0)</code> matches addition with zero</li>
<li><strong>A skeleton</strong>: <code>:x</code> is the replacement</li>
</ul>
<p>The pattern syntax is expressive:</p>
<table>
  <thead>
      <tr>
          <th>Syntax</th>
          <th>Meaning</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><code>?x</code></td>
          <td>Match anything, bind to x</td>
      </tr>
      <tr>
          <td><code>?x:const</code></td>
          <td>Match only numbers</td>
      </tr>
      <tr>
          <td><code>?x:var</code></td>
          <td>Match only symbols</td>
      </tr>
      <tr>
          <td><code>?x:free(v)</code></td>
          <td>Match expressions not containing v</td>
      </tr>
      <tr>
          <td><code>?x...</code></td>
          <td>Variadic - capture remaining arguments</td>
      </tr>
  </tbody>
</table>
<h2 id="symbolic-differentiation-in-15-lines">Symbolic Differentiation in 15 Lines</h2>
<p>Here&rsquo;s a calculus ruleset that computes symbolic derivatives:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-gdscript3" data-lang="gdscript3"><span class="line"><span class="cl"><span class="p">[</span><span class="n">basic</span><span class="o">-</span><span class="n">derivatives</span><span class="p">]</span>
</span></span><span class="line"><span class="cl"><span class="err">@</span><span class="n">dd</span><span class="o">-</span><span class="k">const</span><span class="p">[</span><span class="mi">100</span><span class="p">]:</span> <span class="p">(</span><span class="n">dd</span> <span class="err">?</span><span class="n">c</span><span class="p">:</span><span class="k">const</span> <span class="err">?</span><span class="n">v</span><span class="p">:</span><span class="k">var</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="mi">0</span>
</span></span><span class="line"><span class="cl"><span class="err">@</span><span class="n">dd</span><span class="o">-</span><span class="k">var</span><span class="o">-</span><span class="n">same</span><span class="p">[</span><span class="mi">100</span><span class="p">]:</span> <span class="p">(</span><span class="n">dd</span> <span class="err">?</span><span class="n">x</span><span class="p">:</span><span class="k">var</span> <span class="err">?</span><span class="n">x</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="mi">1</span>
</span></span><span class="line"><span class="cl"><span class="err">@</span><span class="n">dd</span><span class="o">-</span><span class="k">var</span><span class="o">-</span><span class="n">diff</span><span class="p">[</span><span class="mi">90</span><span class="p">]:</span> <span class="p">(</span><span class="n">dd</span> <span class="err">?</span><span class="n">y</span><span class="p">:</span><span class="k">var</span> <span class="err">?</span><span class="n">x</span><span class="p">:</span><span class="k">var</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="mi">0</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="p">[</span><span class="n">rules</span><span class="p">]</span>
</span></span><span class="line"><span class="cl"><span class="err">@</span><span class="n">dd</span><span class="o">-</span><span class="n">sum</span><span class="p">:</span> <span class="p">(</span><span class="n">dd</span> <span class="p">(</span><span class="o">+</span> <span class="err">?</span><span class="n">f</span> <span class="err">?</span><span class="n">g</span><span class="p">)</span> <span class="err">?</span><span class="n">v</span><span class="p">:</span><span class="k">var</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">(</span><span class="o">+</span> <span class="p">(</span><span class="n">dd</span> <span class="p">:</span><span class="n">f</span> <span class="p">:</span><span class="n">v</span><span class="p">)</span> <span class="p">(</span><span class="n">dd</span> <span class="p">:</span><span class="n">g</span> <span class="p">:</span><span class="n">v</span><span class="p">))</span>
</span></span><span class="line"><span class="cl"><span class="err">@</span><span class="n">dd</span><span class="o">-</span><span class="n">product</span><span class="p">:</span> <span class="p">(</span><span class="n">dd</span> <span class="p">(</span><span class="o">*</span> <span class="err">?</span><span class="n">f</span> <span class="err">?</span><span class="n">g</span><span class="p">)</span> <span class="err">?</span><span class="n">v</span><span class="p">:</span><span class="k">var</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">(</span><span class="o">+</span> <span class="p">(</span><span class="o">*</span> <span class="p">(</span><span class="n">dd</span> <span class="p">:</span><span class="n">f</span> <span class="p">:</span><span class="n">v</span><span class="p">)</span> <span class="p">:</span><span class="n">g</span><span class="p">)</span> <span class="p">(</span><span class="o">*</span> <span class="p">:</span><span class="n">f</span> <span class="p">(</span><span class="n">dd</span> <span class="p">:</span><span class="n">g</span> <span class="p">:</span><span class="n">v</span><span class="p">)))</span>
</span></span><span class="line"><span class="cl"><span class="err">@</span><span class="n">dd</span><span class="o">-</span><span class="n">power</span><span class="p">:</span> <span class="p">(</span><span class="n">dd</span> <span class="p">(</span><span class="o">^</span> <span class="err">?</span><span class="n">f</span> <span class="err">?</span><span class="n">n</span><span class="p">:</span><span class="k">const</span><span class="p">)</span> <span class="err">?</span><span class="n">v</span><span class="p">:</span><span class="k">var</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">(</span><span class="o">*</span> <span class="p">:</span><span class="n">n</span> <span class="p">(</span><span class="o">*</span> <span class="p">(</span><span class="o">^</span> <span class="p">:</span><span class="n">f</span> <span class="p">(</span><span class="o">-</span> <span class="p">:</span><span class="n">n</span> <span class="mi">1</span><span class="p">))</span> <span class="p">(</span><span class="n">dd</span> <span class="p">:</span><span class="n">f</span> <span class="p">:</span><span class="n">v</span><span class="p">)))</span>
</span></span><span class="line"><span class="cl"><span class="err">@</span><span class="n">dd</span><span class="o">-</span><span class="nb">exp</span><span class="p">:</span> <span class="p">(</span><span class="n">dd</span> <span class="p">(</span><span class="nb">exp</span> <span class="err">?</span><span class="n">f</span><span class="p">)</span> <span class="err">?</span><span class="n">v</span><span class="p">:</span><span class="k">var</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">(</span><span class="o">*</span> <span class="p">(</span><span class="nb">exp</span> <span class="p">:</span><span class="n">f</span><span class="p">)</span> <span class="p">(</span><span class="n">dd</span> <span class="p">:</span><span class="n">f</span> <span class="p">:</span><span class="n">v</span><span class="p">))</span>
</span></span><span class="line"><span class="cl"><span class="err">@</span><span class="n">dd</span><span class="o">-</span><span class="nb">log</span><span class="p">:</span> <span class="p">(</span><span class="n">dd</span> <span class="p">(</span><span class="n">ln</span> <span class="err">?</span><span class="n">f</span><span class="p">)</span> <span class="err">?</span><span class="n">v</span><span class="p">:</span><span class="k">var</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">(</span><span class="o">/</span> <span class="p">(</span><span class="n">dd</span> <span class="p">:</span><span class="n">f</span> <span class="p">:</span><span class="n">v</span><span class="p">)</span> <span class="p">:</span><span class="n">f</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="err">@</span><span class="n">dd</span><span class="o">-</span><span class="nb">sin</span><span class="p">:</span> <span class="p">(</span><span class="n">dd</span> <span class="p">(</span><span class="nb">sin</span> <span class="err">?</span><span class="n">f</span><span class="p">)</span> <span class="err">?</span><span class="n">v</span><span class="p">:</span><span class="k">var</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">(</span><span class="o">*</span> <span class="p">(</span><span class="nb">cos</span> <span class="p">:</span><span class="n">f</span><span class="p">)</span> <span class="p">(</span><span class="n">dd</span> <span class="p">:</span><span class="n">f</span> <span class="p">:</span><span class="n">v</span><span class="p">))</span>
</span></span><span class="line"><span class="cl"><span class="err">@</span><span class="n">dd</span><span class="o">-</span><span class="nb">cos</span><span class="p">:</span> <span class="p">(</span><span class="n">dd</span> <span class="p">(</span><span class="nb">cos</span> <span class="err">?</span><span class="n">f</span><span class="p">)</span> <span class="err">?</span><span class="n">v</span><span class="p">:</span><span class="k">var</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="p">(</span><span class="o">*</span> <span class="p">(</span><span class="o">-</span> <span class="p">(</span><span class="nb">sin</span> <span class="p">:</span><span class="n">f</span><span class="p">))</span> <span class="p">(</span><span class="n">dd</span> <span class="p">:</span><span class="n">f</span> <span class="p">:</span><span class="n">v</span><span class="p">))</span>
</span></span></code></pre></div><p>With these rules loaded:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">rerum</span> <span class="kn">import</span> <span class="n">RuleEngine</span><span class="p">,</span> <span class="n">E</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">engine</span> <span class="o">=</span> <span class="n">RuleEngine</span><span class="o">.</span><span class="n">from_file</span><span class="p">(</span><span class="s2">&#34;calculus.rules&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># d/dx(x^2) = 2x</span>
</span></span><span class="line"><span class="cl"><span class="n">engine</span><span class="p">(</span><span class="n">E</span><span class="p">(</span><span class="s2">&#34;(dd (^ x 2) x)&#34;</span><span class="p">))</span>  <span class="c1"># =&gt; (* 2 (* (^ x 1) 1))</span>
</span></span></code></pre></div><p>The result needs simplification (another ruleset), but the differentiation itself is completely declarative.</p>
<h2 id="the-security-model-rules-vs-preludes">The Security Model: Rules vs. Preludes</h2>
<p>A key architectural decision in rerum is the separation between <strong>rules</strong> (untrusted, serializable) and <strong>preludes</strong> (trusted Python code). Rules define structural transformations; they can reference operations via the <code>(! op args...)</code> compute form, but those operations must be explicitly provided by the host.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">rerum</span> <span class="kn">import</span> <span class="n">RuleEngine</span><span class="p">,</span> <span class="n">ARITHMETIC_PRELUDE</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">engine</span> <span class="o">=</span> <span class="p">(</span><span class="n">RuleEngine</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">    <span class="o">.</span><span class="n">with_prelude</span><span class="p">(</span><span class="n">ARITHMETIC_PRELUDE</span><span class="p">)</span>  <span class="c1"># Enables +, -, *, /, ^</span>
</span></span><span class="line"><span class="cl">    <span class="o">.</span><span class="n">load_dsl</span><span class="p">(</span><span class="s1">&#39;&#39;&#39;
</span></span></span><span class="line"><span class="cl"><span class="s1">        @fold-add: (+ ?a ?b) =&gt; (! + :a :b)
</span></span></span><span class="line"><span class="cl"><span class="s1">                   when (! and (! const? :a) (! const? :b))
</span></span></span><span class="line"><span class="cl"><span class="s1">    &#39;&#39;&#39;</span><span class="p">))</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">engine</span><span class="p">(</span><span class="n">E</span><span class="p">(</span><span class="s2">&#34;(+ 1 2)&#34;</span><span class="p">))</span>  <span class="c1"># =&gt; 3 (computed by prelude)</span>
</span></span><span class="line"><span class="cl"><span class="n">engine</span><span class="p">(</span><span class="n">E</span><span class="p">(</span><span class="s2">&#34;(+ x 2)&#34;</span><span class="p">))</span>  <span class="c1"># =&gt; (+ x 2) (no change - x isn&#39;t const)</span>
</span></span></code></pre></div><p>This means you can safely load rules from untrusted sources. They can only invoke operations you&rsquo;ve explicitly allowed. No arbitrary code execution.</p>
<h2 id="conditional-guards">Conditional Guards</h2>
<p>Rules can have guards - conditions that must be satisfied for the rule to fire:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">@abs-pos: (abs ?x) =&gt; :x when (! &gt; :x 0)
</span></span><span class="line"><span class="cl">@abs-neg: (abs ?x) =&gt; (! - 0 :x) when (! &lt; :x 0)
</span></span><span class="line"><span class="cl">@abs-zero: (abs ?x) =&gt; 0 when (! = :x 0)
</span></span></code></pre></div><p>Guards use the same <code>(! ...)</code> compute syntax as skeletons, with access to predicates like <code>const?</code>, <code>var?</code>, <code>list?</code>, and comparison operators.</p>
<h2 id="rewriting-strategies">Rewriting Strategies</h2>
<p>Different problems need different traversal strategies:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="c1"># exhaustive (default): Apply rules until no more apply</span>
</span></span><span class="line"><span class="cl"><span class="n">result</span> <span class="o">=</span> <span class="n">engine</span><span class="p">(</span><span class="n">expr</span><span class="p">,</span> <span class="n">strategy</span><span class="o">=</span><span class="s2">&#34;exhaustive&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># once: Apply at most one rule anywhere</span>
</span></span><span class="line"><span class="cl"><span class="n">result</span> <span class="o">=</span> <span class="n">engine</span><span class="p">(</span><span class="n">expr</span><span class="p">,</span> <span class="n">strategy</span><span class="o">=</span><span class="s2">&#34;once&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># bottomup: Simplify children before parents</span>
</span></span><span class="line"><span class="cl"><span class="n">result</span> <span class="o">=</span> <span class="n">engine</span><span class="p">(</span><span class="n">expr</span><span class="p">,</span> <span class="n">strategy</span><span class="o">=</span><span class="s2">&#34;bottomup&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># topdown: Try parent before children</span>
</span></span><span class="line"><span class="cl"><span class="n">result</span> <span class="o">=</span> <span class="n">engine</span><span class="p">(</span><span class="n">expr</span><span class="p">,</span> <span class="n">strategy</span><span class="o">=</span><span class="s2">&#34;topdown&#34;</span><span class="p">)</span>
</span></span></code></pre></div><h2 id="engine-composition">Engine Composition</h2>
<p>Engines can be combined in various ways:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">expand</span> <span class="o">=</span> <span class="n">RuleEngine</span><span class="o">.</span><span class="n">from_dsl</span><span class="p">(</span><span class="s2">&#34;@square: (square ?x) =&gt; (* :x :x)&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">simplify</span> <span class="o">=</span> <span class="n">RuleEngine</span><span class="o">.</span><span class="n">from_file</span><span class="p">(</span><span class="s2">&#34;algebra.rules&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Sequence: expand first, then simplify</span>
</span></span><span class="line"><span class="cl"><span class="n">normalize</span> <span class="o">=</span> <span class="n">expand</span> <span class="o">&gt;&gt;</span> <span class="n">simplify</span>
</span></span><span class="line"><span class="cl"><span class="n">normalize</span><span class="p">(</span><span class="n">E</span><span class="p">(</span><span class="s2">&#34;(square 3)&#34;</span><span class="p">))</span>  <span class="c1"># =&gt; 9</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Union: combine all rules</span>
</span></span><span class="line"><span class="cl"><span class="n">combined</span> <span class="o">=</span> <span class="n">algebra</span> <span class="o">|</span> <span class="n">calculus</span>
</span></span></code></pre></div><h2 id="interactive-repl">Interactive REPL</h2>
<p>The CLI includes an interactive mode for exploration:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">$ rerum
</span></span><span class="line"><span class="cl">rerum&gt; @add-zero: <span class="o">(</span>+ ?x 0<span class="o">)</span> <span class="o">=</span>&gt; :x
</span></span><span class="line"><span class="cl">Added <span class="m">1</span> rule<span class="o">(</span>s<span class="o">)</span>
</span></span><span class="line"><span class="cl">rerum&gt; <span class="o">(</span>+ y 0<span class="o">)</span>
</span></span><span class="line"><span class="cl">y
</span></span><span class="line"><span class="cl">rerum&gt; :load examples/calculus.rules
</span></span><span class="line"><span class="cl">Loaded <span class="m">12</span> rule<span class="o">(</span>s<span class="o">)</span>
</span></span><span class="line"><span class="cl">rerum&gt; :trace on
</span></span><span class="line"><span class="cl">rerum&gt; <span class="o">(</span>dd <span class="o">(</span>^ x 3<span class="o">)</span> x<span class="o">)</span>
</span></span><span class="line"><span class="cl"><span class="o">=</span>&gt; <span class="o">(</span>* <span class="m">3</span> <span class="o">(</span>* <span class="o">(</span>^ x 2<span class="o">)</span> 1<span class="o">))</span>
</span></span><span class="line"><span class="cl">Trace: dd-power
</span></span></code></pre></div><p>Scripts can be run directly or piped:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">$ rerum calc.rerum
</span></span><span class="line"><span class="cl">$ <span class="nb">echo</span> <span class="s2">&#34;(+ 1 2)&#34;</span> <span class="p">|</span> rerum -r algebra.rules -p full -q
</span></span><span class="line"><span class="cl"><span class="m">3</span>
</span></span></code></pre></div><h2 id="connection-to-other-projects">Connection to Other Projects</h2>
<p>Rerum is part of a family of related projects:</p>
<ul>
<li><strong><a href="/projects/xtk/">xtk</a></strong> - Expression Toolkit with MCP server integration for Claude Code</li>
<li><strong><a href="/projects/symlik/">symlik</a></strong> - Symbolic likelihood models using term rewriting for automatic differentiation of statistical models</li>
<li><strong><a href="/projects/jsl/">jsl</a></strong> - A network-native language where code is JSON, using similar s-expression structure</li>
</ul>
<p>The common thread is treating symbolic expressions as first-class citizens that can be transformed, serialized, and reasoned about programmatically.</p>
<h2 id="getting-started">Getting Started</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">pip install rerum
</span></span></code></pre></div><div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">rerum</span> <span class="kn">import</span> <span class="n">RuleEngine</span><span class="p">,</span> <span class="n">E</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">engine</span> <span class="o">=</span> <span class="n">RuleEngine</span><span class="o">.</span><span class="n">from_dsl</span><span class="p">(</span><span class="s1">&#39;&#39;&#39;
</span></span></span><span class="line"><span class="cl"><span class="s1">    @add-zero: (+ ?x 0) =&gt; :x
</span></span></span><span class="line"><span class="cl"><span class="s1">    @mul-one: (* ?x 1) =&gt; :x
</span></span></span><span class="line"><span class="cl"><span class="s1">    @mul-zero: (* ?x 0) =&gt; 0
</span></span></span><span class="line"><span class="cl"><span class="s1">&#39;&#39;&#39;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="nb">print</span><span class="p">(</span><span class="n">engine</span><span class="p">(</span><span class="n">E</span><span class="p">(</span><span class="s2">&#34;(* (+ x 0) 0)&#34;</span><span class="p">)))</span>  <span class="c1"># =&gt; 0</span>
</span></span></code></pre></div><p>The full documentation is at the <a href="https://github.com/queelius/rerum">GitHub repository</a>. Check out the <code>examples/</code> directory for complete rulesets covering algebra, calculus, and number theory.</p>
<h2 id="why-rerum">Why &ldquo;Rerum&rdquo;?</h2>
<p>The name comes from the Latin phrase <em>&ldquo;rerum natura&rdquo;</em> (the nature of things) - Lucretius&rsquo;s famous work on atomic theory and natural philosophy. It felt fitting for a library about transforming the fundamental structure of expressions.</p>
]]></content:encoded><category>symbolic-computation</category><category>term-rewriting</category><category>pattern-matching</category><category>dsl</category><category>python</category><category>computer-algebra</category><category>functional-programming</category><category>sicp</category></item><item><title>chop: When Every Command Returns the Same Kind of Thing</title><link>https://metafunctor.com/post/2025-07-15-chop/</link><pubDate>Tue, 15 Jul 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2025-07-15-chop/</guid><description>27 image commands, one constraint: read JSON, write JSON. The closure property as a generative design principle.</description><content:encoded><![CDATA[<p>Section 2.2 of <em>Structure and Interpretation of Computer Programs</em> introduces the <strong>closure property</strong>: the result of combining things should be the same kind of thing you started with. <code>cons</code> two values and you get a pair — which you can <code>cons</code> again. This is what makes recursive data structures possible. Without it, you get flat records. With it, you get trees, lists, nested structure of arbitrary depth.</p>
<p>Abelson and Sussman are careful to distinguish this from <em>lexical</em> closures (functions that capture their environment). Algebraic closure is about the <em>type signature</em> of combination: if the output type matches the input type, composition is unlimited.</p>
<p>Most discussions of closure treat it as a property to <em>verify</em> — you check whether your algebra is closed and move on. But closure is more powerful than that. It&rsquo;s a <strong>design method</strong>: choose a type, force every operation to consume and produce that type, and see what emerges. The constraint does the creative work.</p>
<p><a href="https://github.com/queelius/chop"><strong>chop</strong></a> is an image-manipulation CLI built on exactly this principle. 27 commands. One rule: read JSON from stdin, write JSON to stdout.</p>
<h2 id="the-problem-with-image-pipelines">The Problem with Image Pipelines</h2>
<p>Traditional image CLIs violate closure. ImageMagick consumes a file and produces a file. Each invocation is terminal — the output is pixels, not something you can pipe into further processing without going back to disk. Composition happens through flag accumulation inside a single command, not through the shell&rsquo;s native composition mechanism.</p>
<p>You can&rsquo;t <code>tee</code> a midpoint. You can&rsquo;t save a half-finished pipeline as a recipe and apply it later. You can&rsquo;t branch.</p>
<p>The SICP parallel is direct: if <code>cons</code> produced an atom instead of a pair, you could build flat structures but not recursive ones. If an image command produces pixels instead of a composable description, you can build single transformations but not pipelines.</p>
<h2 id="one-constraint-json-in-json-out">One Constraint: JSON In, JSON Out</h2>
<p>Every <code>chop</code> command reads a <code>PipelineState</code> JSON object from stdin, appends one operation, and writes the updated JSON to stdout. The wire format carries no image data — only a recipe:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;version&#34;</span><span class="p">:</span> <span class="mi">3</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;ops&#34;</span><span class="p">:</span> <span class="p">[</span>
</span></span><span class="line"><span class="cl">    <span class="p">[</span><span class="s2">&#34;load&#34;</span><span class="p">,</span> <span class="p">[</span><span class="s2">&#34;photo.jpg&#34;</span><span class="p">],</span> <span class="p">{}],</span>
</span></span><span class="line"><span class="cl">    <span class="p">[</span><span class="s2">&#34;resize&#34;</span><span class="p">,</span> <span class="p">[</span><span class="s2">&#34;50%&#34;</span><span class="p">],</span> <span class="p">{}],</span>
</span></span><span class="line"><span class="cl">    <span class="p">[</span><span class="s2">&#34;pad&#34;</span><span class="p">,</span> <span class="p">[</span><span class="mi">10</span><span class="p">],</span> <span class="p">{</span><span class="nt">&#34;color&#34;</span><span class="p">:</span> <span class="s2">&#34;white&#34;</span><span class="p">}]</span>
</span></span><span class="line"><span class="cl">  <span class="p">],</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;metadata&#34;</span><span class="p">:</span> <span class="p">{}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Each operation is a <code>[name, args, kwargs]</code> triple. The pipeline accumulates operations as data. Here&rsquo;s what this looks like in practice:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">chop load photo.jpg <span class="p">|</span> chop resize 50% <span class="p">|</span> chop save out.png
</span></span></code></pre></div><p>And here&rsquo;s what flows between the pipes — the actual stdout of <code>chop resize 50%</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="p">{</span><span class="nt">&#34;version&#34;</span><span class="p">:</span><span class="mi">3</span><span class="p">,</span><span class="nt">&#34;ops&#34;</span><span class="p">:[[</span><span class="s2">&#34;load&#34;</span><span class="p">,[</span><span class="s2">&#34;photo.jpg&#34;</span><span class="p">],{}],[</span><span class="s2">&#34;resize&#34;</span><span class="p">,[</span><span class="s2">&#34;50%&#34;</span><span class="p">],{}]],</span><span class="nt">&#34;metadata&#34;</span><span class="p">:{}}</span>
</span></span></code></pre></div><p>The type signature is <code>PipelineState -&gt; PipelineState</code>. Every command, without exception, consumes and produces the same kind of thing. Closure holds.</p>
<h2 id="what-emerges">What Emerges</h2>
<p>The interesting part isn&rsquo;t the constraint itself — it&rsquo;s what the constraint produces. Four capabilities fall out of the design that were never planned individually.</p>
<h3 id="non-terminal-save">Non-Terminal Save</h3>
<p>In most CLIs, <code>save</code> is a terminus. In <code>chop</code>, <code>save</code> materializes the pipeline (touches pixels for the first time), writes the file as a side effect to stderr, and then <em>outputs its JSON state like every other command</em>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">chop load photo.jpg <span class="p">|</span> chop save full.png <span class="p">|</span> chop resize 50% <span class="p">|</span> chop save thumb.png
</span></span></code></pre></div><p>Two output files from a single pipeline. This works because <code>save</code> isn&rsquo;t special — it follows the same contract as <code>resize</code> or <code>blur</code>. The uniform type signature means there&rsquo;s no distinction between &ldquo;intermediate&rdquo; and &ldquo;terminal&rdquo; commands. Every command is both.</p>
<h3 id="recipes-as-data">Recipes as Data</h3>
<p>What happens if you build a pipeline without <code>load</code>? You get an unbound program — a sequence of operations with no input image:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">chop resize 50% <span class="p">|</span> chop pad <span class="m">10</span> <span class="p">|</span> chop grayscale &gt; recipe.json
</span></span></code></pre></div><p>The resulting JSON is a first-class value:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="p">{</span><span class="nt">&#34;version&#34;</span><span class="p">:</span><span class="mi">3</span><span class="p">,</span><span class="nt">&#34;ops&#34;</span><span class="p">:[[</span><span class="s2">&#34;resize&#34;</span><span class="p">,[</span><span class="s2">&#34;50%&#34;</span><span class="p">],{}],[</span><span class="s2">&#34;pad&#34;</span><span class="p">,[</span><span class="mi">10</span><span class="p">],{}],[</span><span class="s2">&#34;grayscale&#34;</span><span class="p">,[],{}]],</span><span class="nt">&#34;metadata&#34;</span><span class="p">:{}}</span>
</span></span></code></pre></div><p>Apply it later with <code>apply</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">chop load photo.jpg <span class="p">|</span> chop apply recipe.json <span class="p">|</span> chop save out.png
</span></span></code></pre></div><p>This is SICP Chapter 4&rsquo;s programs-as-data principle. A pipeline without <code>load</code> is a function waiting for an argument. The <code>apply</code> command is function application. And because recipes are JSON, they&rsquo;re inspectable, version-controllable, and shareable — you can commit a <code>thumbnail-recipe.json</code> alongside your build scripts.</p>
<h3 id="multi-image-composition">Multi-Image Composition</h3>
<p>The JSON format naturally extends to labeled images. Load with <code>--as</code> to name images; target specific images with <code>--on</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">chop load --as <span class="nb">bg</span> photo.jpg <span class="p">|</span> chop load --as <span class="nb">fg</span> logo.png <span class="se">\
</span></span></span><span class="line"><span class="cl">    <span class="p">|</span> chop resize 50% --on <span class="nb">fg</span> <span class="se">\
</span></span></span><span class="line"><span class="cl">    <span class="p">|</span> chop overlay <span class="nb">bg</span> <span class="nb">fg</span> <span class="se">\
</span></span></span><span class="line"><span class="cl">    <span class="p">|</span> chop save watermarked.png
</span></span></code></pre></div><p>Under the hood, <code>materialize()</code> maintains a labeled context — a dictionary mapping names to images with a cursor tracking the &ldquo;current&rdquo; image. Composition operations like <code>overlay</code>, <code>hstack</code>, <code>vstack</code>, and <code>grid</code> take label arguments and combine multiple images into one. The result goes back into the context, and the JSON contract never changes.</p>
<p>This wasn&rsquo;t designed as a separate feature. It&rsquo;s the same <code>PipelineState -&gt; PipelineState</code> contract accommodating richer operation types. The closure property scales.</p>
<h3 id="inspectable-intermediates">Inspectable Intermediates</h3>
<p>Because stdout is always valid JSON, standard Unix tools work at any point in the pipeline:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">chop load photo.jpg <span class="p">|</span> chop resize 50% <span class="p">|</span> tee step.json <span class="p">|</span> chop pad <span class="m">10</span> <span class="p">|</span> chop save out.png
</span></span></code></pre></div><p><code>tee</code> captures the intermediate state. <code>jq</code> can query it. <code>diff</code> can compare two pipeline states. <code>wc -c</code> tells you the recipe size. None of this required any design effort — it&rsquo;s a consequence of the output being structured text rather than binary pixels.</p>
<h2 id="lazy-evaluation">Lazy Evaluation</h2>
<p>Nothing in the pipe is an image. The JSON carries <code>[name, args, kwargs]</code> triples — a <em>description</em> of computation, not the computation itself. No pixels are loaded, no transforms applied, until a command explicitly materializes the pipeline.</p>
<p>Only two commands trigger materialization: <code>save</code> (to write a file) and <code>info</code> (to report dimensions and metadata). Everything else is pure recording.</p>
<p>This is SICP Chapter 3.5&rsquo;s insight about streams: separate the <em>description</em> of a computation from its <em>execution</em>. A pipeline of ten operations doesn&rsquo;t create ten intermediate images. It creates one JSON object with ten entries, then executes them all in a single pass at the end.</p>
<p>The <code>materialize()</code> method in <code>pipeline.py</code> walks the ops list, building a labeled image context as it goes — loading files, applying transforms, executing compositions — and returns the image at the cursor. It&rsquo;s the only eager path in the system.</p>
<h2 id="the-architecture">The Architecture</h2>
<p>The codebase is small and sharply factored:</p>
<ul>
<li>
<p><strong><code>output.py</code></strong> (19 lines): One function. Writes pipeline state as JSON to stdout. No conditions, no flags. This is where the closure constraint lives — and it&rsquo;s 19 lines because enforcing uniformity is trivial.</p>
</li>
<li>
<p><strong><code>operations.py</code></strong> (696 lines): Pure <code>Image -&gt; Image</code> functions. Sixteen single-image transforms (resize, crop, rotate, brightness, blur, etc.) and four multi-image compositions (hstack, vstack, overlay, grid). No I/O, no JSON, no state — just pixel manipulation.</p>
</li>
<li>
<p><strong><code>pipeline.py</code></strong> (311 lines): The <code>PipelineState</code> dataclass. JSON serialization, deserialization, and <code>materialize()</code> — the single method that turns a recipe into pixels.</p>
</li>
<li>
<p><strong><code>cli.py</code></strong> (645 lines): 27 commands, each roughly 10 lines. Read state, append one op, return state. The uniformity of the contract means every command handler has nearly identical structure.</p>
</li>
</ul>
<p>The total is under 1,700 lines. The interesting thing is the ratio: 696 lines of image operations, 19 lines enforcing the design constraint. The constraint is cheap to implement and expensive to violate — which is exactly what you want from an architectural decision.</p>
<hr>
<p>One design decision — every command reads JSON, writes JSON — and four capabilities emerge: non-terminal saves, reusable recipes, multi-image composition, and inspectable intermediates. Plus lazy evaluation for free, because the uniform representation turns out to be a recipe rather than a result.</p>
<p>SICP presents the closure property as something to notice about well-designed systems. But the deeper lesson is that closure is something to <em>impose</em>. Pick a type. Force everything through it. The constraint doesn&rsquo;t limit what you can build — it generates capabilities you wouldn&rsquo;t have thought to design.</p>
<p>The best design decisions are the ones you only have to make once.</p>
<hr>
<p><em>This post is part of a <a href="/series/sicp/">series on SICP</a>, exploring how the ideas from Structure and Interpretation of Computer Programs appear in modern programming practice.</em></p>
]]></content:encoded><category>sicp</category><category>dsl</category><category>unix-philosophy</category><category>python</category><category>image-processing</category><category>closure-property</category><category>lazy-evaluation</category></item><item><title>jsonl-algebra: Production-Grade Relational Algebra for Nested JSON</title><link>https://metafunctor.com/post/2024-12-jsonl-algebra/</link><pubDate>Wed, 18 Dec 2024 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-12-jsonl-algebra/</guid><description>A production-ready implementation of relational algebra for JSONL data with full support for nested structures. jsonl-algebra brings dotsuite's dotrelate concepts to production with streaming operations, schema inference, and composable pipelines.</description><content:encoded><![CDATA[<p><strong><a href="https://pypi.org/project/jsonl-algebra/">jsonl-algebra</a></strong> (command: <code>ja</code>) is a production-grade implementation of relational algebra for JSONL data. It&rsquo;s the full-featured, battle-tested realization of <a href="/post/2024-12-dotsuite/">dotsuite</a>&rsquo;s <strong>dotrelate</strong> component—bringing SQL-like operations to the command line with first-class support for nested JSON structures.</p>
<h2 id="the-relationship-to-dotsuite">The Relationship to Dotsuite</h2>
<p>In dotsuite&rsquo;s architecture, <strong>dotrelate</strong> provides relational operations on document collections:</p>
<ul>
<li><strong>dotrelate</strong>: Join, union, project, difference on collections</li>
<li><strong>Theoretical foundation</strong>: Relational algebra lifted to nested documents</li>
</ul>
<p><strong>jsonl-algebra (<code>ja</code>)</strong> is the production implementation of these concepts, with:</p>
<ul>
<li>✅ <strong>Published on PyPI</strong>: <code>pip install jsonl-algebra</code></li>
<li>✅ <strong>Battle-tested</strong>: Used in real-world data pipelines</li>
<li>✅ <strong>Feature-complete</strong>: All relational operations + aggregations</li>
<li>✅ <strong>Streaming</strong>: Process datasets larger than memory</li>
<li>✅ <strong>Schema tools</strong>: Inference and validation</li>
<li>✅ <strong>Interactive REPL</strong>: Build pipelines step-by-step</li>
</ul>
<h2 id="core-insight-relational-algebra-for-nested-data">Core Insight: Relational Algebra for Nested Data</h2>
<p>Traditional relational algebra assumes flat tables:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-sql" data-lang="sql"><span class="line"><span class="cl"><span class="k">SELECT</span><span class="w"> </span><span class="n">name</span><span class="p">,</span><span class="w"> </span><span class="n">age</span><span class="w"> </span><span class="k">FROM</span><span class="w"> </span><span class="n">users</span><span class="w"> </span><span class="k">WHERE</span><span class="w"> </span><span class="n">age</span><span class="w"> </span><span class="o">&gt;</span><span class="w"> </span><span class="mi">30</span><span class="w">
</span></span></span></code></pre></div><p>But modern JSON is deeply nested:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;user&#34;</span><span class="p">:</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&#34;id&#34;</span><span class="p">:</span> <span class="mi">1</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&#34;name&#34;</span><span class="p">:</span> <span class="s2">&#34;Alice&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&#34;address&#34;</span><span class="p">:</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">      <span class="nt">&#34;city&#34;</span><span class="p">:</span> <span class="s2">&#34;NYC&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">      <span class="nt">&#34;zip&#34;</span><span class="p">:</span> <span class="s2">&#34;10001&#34;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">  <span class="p">},</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;orders&#34;</span><span class="p">:</span> <span class="p">[</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span><span class="nt">&#34;id&#34;</span><span class="p">:</span> <span class="mi">101</span><span class="p">,</span> <span class="nt">&#34;amount&#34;</span><span class="p">:</span> <span class="mi">50</span><span class="p">}</span>
</span></span><span class="line"><span class="cl">  <span class="p">]</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p><strong>jsonl-algebra bridges this gap</strong> by extending relational algebra with <strong>dot notation</strong> for nested access:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ja <span class="k">select</span> <span class="s1">&#39;user.age &gt; 30&#39;</span> users.jsonl
</span></span><span class="line"><span class="cl">ja project user.name,user.address.city users.jsonl
</span></span><span class="line"><span class="cl">ja join users.jsonl orders.jsonl --on user.id<span class="o">=</span>customer_id
</span></span></code></pre></div><h2 id="the-five-core-operations">The Five Core Operations</h2>
<p>Relational algebra has five fundamental operations that form a complete algebra:</p>
<h3 id="1-selection-σ--filter-rows">1. Selection (σ) — Filter Rows</h3>
<p><strong>Mathematical notation</strong>: \(\sigma_{\text{predicate}}(R)\)</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Filter where status is &#34;active&#34;</span>
</span></span><span class="line"><span class="cl">ja <span class="k">select</span> <span class="s1">&#39;status == `&#34;active&#34;`&#39;</span> data.jsonl
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Filter on nested fields</span>
</span></span><span class="line"><span class="cl">ja <span class="k">select</span> <span class="s1">&#39;user.age &gt; 30&#39;</span> users.jsonl
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Complex boolean logic</span>
</span></span><span class="line"><span class="cl">ja <span class="k">select</span> <span class="s1">&#39;price &lt; 100 and category == `&#34;electronics&#34;`&#39;</span> products.jsonl
</span></span></code></pre></div><p><strong>Properties</strong>:</p>
<ul>
<li><strong>Commutative</strong>: \(\sigma_{p_1}(\sigma_{p_2}(R)) = \sigma_{p_2}(\sigma_{p_1}(R))\)</li>
<li><strong>Combinable</strong>: \(\sigma_{p_1}(\sigma_{p_2}(R)) = \sigma_{p_1 \land p_2}(R)\)</li>
</ul>
<h3 id="2-projection-π--selectcompute-columns">2. Projection (π) — Select/Compute Columns</h3>
<p><strong>Mathematical notation</strong>: \(\pi_{\text{columns}}(R)\)</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Pick specific fields</span>
</span></span><span class="line"><span class="cl">ja project id,name data.jsonl
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Access nested fields</span>
</span></span><span class="line"><span class="cl">ja project user.name,user.address.city users.jsonl
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Computed columns (coming soon)</span>
</span></span><span class="line"><span class="cl">ja project name,annual_income<span class="o">=</span>salary*12 employees.jsonl
</span></span></code></pre></div><p><strong>Properties</strong>:</p>
<ul>
<li><strong>Idempotent</strong> (for simple projections): \(\pi_a(\pi_{a,b}(R)) = \pi_a(R)\)</li>
</ul>
<h3 id="3-join---combine-relations">3. Join (⋈) — Combine Relations</h3>
<p><strong>Mathematical notation</strong>: \(R \bowtie_{\text{condition}} S\)</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Inner join on user ID</span>
</span></span><span class="line"><span class="cl">ja join users.jsonl orders.jsonl --on user.id<span class="o">=</span>customer_id
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Join on nested fields</span>
</span></span><span class="line"><span class="cl">ja join posts.jsonl comments.jsonl --on post.id<span class="o">=</span>comment.post_id
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Multiple join keys</span>
</span></span><span class="line"><span class="cl">ja join users.jsonl accounts.jsonl --on <span class="nv">id</span><span class="o">=</span>user_id,email<span class="o">=</span>account_email
</span></span></code></pre></div><p><strong>Properties</strong>:</p>
<ul>
<li><strong>Commutative</strong>: \(R \bowtie S = S \bowtie R\)</li>
<li><strong>Associative</strong>: \((R \bowtie S) \bowtie T = R \bowtie (S \bowtie T)\)</li>
</ul>
<p>This means you can join multiple files in any order:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">ja join users.jsonl orders.jsonl --on user.id<span class="o">=</span>customer_id <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> ja join - products.jsonl --on <span class="nv">product_id</span><span class="o">=</span>id
</span></span></code></pre></div><h3 id="4-union---combine-all-rows">4. Union (∪) — Combine All Rows</h3>
<p><strong>Mathematical notation</strong>: \(R \cup S\)</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Combine employees and contractors</span>
</span></span><span class="line"><span class="cl">ja union employees.jsonl contractors.jsonl
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Union multiple sources</span>
</span></span><span class="line"><span class="cl">ja union jan.jsonl feb.jsonl mar.jsonl
</span></span></code></pre></div><p><strong>Properties</strong>:</p>
<ul>
<li><strong>Commutative</strong>: \(R \cup S = S \cup R\)</li>
<li><strong>Associative</strong>: \((R \cup S) \cup T = R \cup (S \cup T)\)</li>
<li><strong>Identity</strong>: \(R \cup \emptyset = R\)</li>
</ul>
<h3 id="5-difference---set-subtraction">5. Difference (−) — Set Subtraction</h3>
<p><strong>Mathematical notation</strong>: \(R - S\)</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Find users without orders</span>
</span></span><span class="line"><span class="cl">ja difference users.jsonl customers_with_orders.jsonl --on id
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Set operations</span>
</span></span><span class="line"><span class="cl">ja difference all_items.jsonl sold_items.jsonl --on sku
</span></span></code></pre></div><p><strong>Properties</strong>:</p>
<ul>
<li><strong>Not commutative</strong>: \(R - S \neq S - R\)</li>
<li><strong>Anti-identity</strong>: \(R - \emptyset = R\), but \(\emptyset - R = \emptyset\)</li>
</ul>
<h2 id="aggregation-operations-extending-the-algebra">Aggregation Operations: Extending the Algebra</h2>
<p>While the five core operations are complete, practical data processing needs aggregation:</p>
<h3 id="groupby-γ--aggregate-by-groups">GroupBy (γ) — Aggregate by Groups</h3>
<p><strong>Mathematical notation</strong>: \(\gamma_{\text{group\_keys}, \text{aggregations}}(R)\)</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Count by category</span>
</span></span><span class="line"><span class="cl">ja groupby category --agg count products.jsonl
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Sum by customer</span>
</span></span><span class="line"><span class="cl">ja groupby customer_id --agg sum:amount orders.jsonl
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Multiple aggregations</span>
</span></span><span class="line"><span class="cl">ja groupby department --agg avg:salary,count,max:age employees.jsonl
</span></span></code></pre></div><p><strong>Supported aggregations</strong>:</p>
<ul>
<li><code>count</code>: Number of items</li>
<li><code>sum</code>: Sum of values</li>
<li><code>avg</code>: Average of values</li>
<li><code>min</code>/<code>max</code>: Minimum/maximum</li>
<li><code>list</code>: Collect all values into array</li>
</ul>
<h3 id="chained-grouping-an-innovation">Chained Grouping: An Innovation</h3>
<p>Traditional SQL requires nested subqueries for multi-level grouping. jsonl-algebra enables <strong>chained grouping</strong> through metadata preservation:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Multi-level grouping</span>
</span></span><span class="line"><span class="cl">cat sales.jsonl <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> ja groupby region <span class="se">\ </span>       <span class="c1"># First level</span>
</span></span><span class="line"><span class="cl">  <span class="p">|</span> ja groupby product <span class="se">\ </span>      <span class="c1"># Second level</span>
</span></span><span class="line"><span class="cl">  <span class="p">|</span> ja groupby month <span class="se">\ </span>        <span class="c1"># Third level</span>
</span></span><span class="line"><span class="cl">  <span class="p">|</span> ja agg <span class="nv">total</span><span class="o">=</span>sum:amount    <span class="c1"># Final aggregation</span>
</span></span></code></pre></div><p><strong>How it works</strong>: Each <code>groupby</code> adds metadata without aggregating:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;sale_id&#34;</span><span class="p">:</span> <span class="mi">101</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;region&#34;</span><span class="p">:</span> <span class="s2">&#34;east&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;product&#34;</span><span class="p">:</span> <span class="s2">&#34;laptop&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;month&#34;</span><span class="p">:</span> <span class="s2">&#34;jan&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;amount&#34;</span><span class="p">:</span> <span class="mi">1200</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;_groups&#34;</span><span class="p">:</span> <span class="p">[</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span><span class="nt">&#34;field&#34;</span><span class="p">:</span> <span class="s2">&#34;region&#34;</span><span class="p">,</span> <span class="nt">&#34;value&#34;</span><span class="p">:</span> <span class="s2">&#34;east&#34;</span><span class="p">},</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span><span class="nt">&#34;field&#34;</span><span class="p">:</span> <span class="s2">&#34;product&#34;</span><span class="p">,</span> <span class="nt">&#34;value&#34;</span><span class="p">:</span> <span class="s2">&#34;laptop&#34;</span><span class="p">},</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span><span class="nt">&#34;field&#34;</span><span class="p">:</span> <span class="s2">&#34;month&#34;</span><span class="p">,</span> <span class="nt">&#34;value&#34;</span><span class="p">:</span> <span class="s2">&#34;jan&#34;</span><span class="p">}</span>
</span></span><span class="line"><span class="cl">  <span class="p">],</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;_group_size&#34;</span><span class="p">:</span> <span class="mi">5</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;_group_index&#34;</span><span class="p">:</span> <span class="mi">0</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>This preserves the relational structure while tracking grouping hierarchy—a key innovation that enables composition.</p>
<h2 id="real-world-example-the-5-minute-tour">Real-World Example: The 5-Minute Tour</h2>
<p><strong>Setup</strong>: Two JSONL files</p>
<p><code>users.jsonl</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="p">{</span><span class="nt">&#34;user&#34;</span><span class="p">:</span> <span class="p">{</span><span class="nt">&#34;id&#34;</span><span class="p">:</span> <span class="mi">1</span><span class="p">,</span> <span class="nt">&#34;name&#34;</span><span class="p">:</span> <span class="s2">&#34;Alice&#34;</span><span class="p">}}</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span><span class="nt">&#34;user&#34;</span><span class="p">:</span> <span class="p">{</span><span class="nt">&#34;id&#34;</span><span class="p">:</span> <span class="mi">2</span><span class="p">,</span> <span class="nt">&#34;name&#34;</span><span class="p">:</span> <span class="s2">&#34;Bob&#34;</span><span class="p">}}</span>
</span></span></code></pre></div><p><code>orders.jsonl</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="p">{</span><span class="nt">&#34;order_id&#34;</span><span class="p">:</span> <span class="mi">101</span><span class="p">,</span> <span class="nt">&#34;customer_id&#34;</span><span class="p">:</span> <span class="mi">1</span><span class="p">,</span> <span class="nt">&#34;amount&#34;</span><span class="p">:</span> <span class="mi">50</span><span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span><span class="nt">&#34;order_id&#34;</span><span class="p">:</span> <span class="mi">102</span><span class="p">,</span> <span class="nt">&#34;customer_id&#34;</span><span class="p">:</span> <span class="mi">1</span><span class="p">,</span> <span class="nt">&#34;amount&#34;</span><span class="p">:</span> <span class="mi">75</span><span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span><span class="nt">&#34;order_id&#34;</span><span class="p">:</span> <span class="mi">103</span><span class="p">,</span> <span class="nt">&#34;customer_id&#34;</span><span class="p">:</span> <span class="mi">2</span><span class="p">,</span> <span class="nt">&#34;amount&#34;</span><span class="p">:</span> <span class="mi">120</span><span class="p">}</span>
</span></span></code></pre></div><p><strong>Goal</strong>: Total amount spent by each user</p>
<p><strong>Solution</strong>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Join users and orders, then aggregate</span>
</span></span><span class="line"><span class="cl">ja join users.jsonl orders.jsonl --on user.id<span class="o">=</span>customer_id <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> ja groupby user.name --agg sum:amount
</span></span></code></pre></div><p><strong>Output</strong>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="p">{</span><span class="nt">&#34;user.name&#34;</span><span class="p">:</span> <span class="s2">&#34;Alice&#34;</span><span class="p">,</span> <span class="nt">&#34;sum_amount&#34;</span><span class="p">:</span> <span class="mi">125</span><span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span><span class="nt">&#34;user.name&#34;</span><span class="p">:</span> <span class="s2">&#34;Bob&#34;</span><span class="p">,</span> <span class="nt">&#34;sum_amount&#34;</span><span class="p">:</span> <span class="mi">120</span><span class="p">}</span>
</span></span></code></pre></div><p>This demonstrates:</p>
<ul>
<li>✅ Nested field access (<code>user.id</code>, <code>user.name</code>)</li>
<li>✅ Relational join</li>
<li>✅ Aggregation</li>
<li>✅ Unix-style piping</li>
</ul>
<h2 id="additional-operations">Additional Operations</h2>
<h3 id="distinct--remove-duplicates">Distinct — Remove Duplicates</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Unique users</span>
</span></span><span class="line"><span class="cl">ja distinct users.jsonl
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Distinct on specific fields</span>
</span></span><span class="line"><span class="cl">ja distinct --key user.id,email contacts.jsonl
</span></span></code></pre></div><h3 id="sort--order-results">Sort — Order Results</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Sort by age ascending</span>
</span></span><span class="line"><span class="cl">ja sort age users.jsonl
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Sort descending</span>
</span></span><span class="line"><span class="cl">ja sort age:desc users.jsonl
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Multi-key sort</span>
</span></span><span class="line"><span class="cl">ja sort department,salary:desc employees.jsonl
</span></span></code></pre></div><h3 id="rename--field-aliasing">Rename — Field Aliasing</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Rename single field</span>
</span></span><span class="line"><span class="cl">ja rename <span class="nv">id</span><span class="o">=</span>user_id data.jsonl
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Rename nested fields</span>
</span></span><span class="line"><span class="cl">ja rename user.loc<span class="o">=</span>user.location data.jsonl
</span></span></code></pre></div><h3 id="headtail--limit-results">Head/Tail — Limit Results</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># First 10 rows</span>
</span></span><span class="line"><span class="cl">ja head <span class="m">10</span> data.jsonl
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Last 10 rows</span>
</span></span><span class="line"><span class="cl">ja tail <span class="m">10</span> data.jsonl
</span></span></code></pre></div><h2 id="schema-operations-inference-and-validation">Schema Operations: Inference and Validation</h2>
<h3 id="schema-inference">Schema Inference</h3>
<p>Automatically discover the structure of your data:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Infer JSON Schema</span>
</span></span><span class="line"><span class="cl">ja schema infer users.jsonl &gt; users_schema.json
</span></span></code></pre></div><p>Output:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;type&#34;</span><span class="p">:</span> <span class="s2">&#34;object&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;properties&#34;</span><span class="p">:</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&#34;id&#34;</span><span class="p">:</span> <span class="p">{</span><span class="nt">&#34;type&#34;</span><span class="p">:</span> <span class="s2">&#34;integer&#34;</span><span class="p">},</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&#34;name&#34;</span><span class="p">:</span> <span class="p">{</span><span class="nt">&#34;type&#34;</span><span class="p">:</span> <span class="s2">&#34;string&#34;</span><span class="p">},</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&#34;age&#34;</span><span class="p">:</span> <span class="p">{</span><span class="nt">&#34;type&#34;</span><span class="p">:</span> <span class="s2">&#34;integer&#34;</span><span class="p">},</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&#34;address&#34;</span><span class="p">:</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">      <span class="nt">&#34;type&#34;</span><span class="p">:</span> <span class="s2">&#34;object&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">      <span class="nt">&#34;properties&#34;</span><span class="p">:</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="nt">&#34;city&#34;</span><span class="p">:</span> <span class="p">{</span><span class="nt">&#34;type&#34;</span><span class="p">:</span> <span class="s2">&#34;string&#34;</span><span class="p">},</span>
</span></span><span class="line"><span class="cl">        <span class="nt">&#34;zip&#34;</span><span class="p">:</span> <span class="p">{</span><span class="nt">&#34;type&#34;</span><span class="p">:</span> <span class="s2">&#34;string&#34;</span><span class="p">}</span>
</span></span><span class="line"><span class="cl">      <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">  <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><h3 id="schema-validation">Schema Validation</h3>
<p>Validate data against a schema:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Check if data conforms to schema</span>
</span></span><span class="line"><span class="cl">ja schema validate users_schema.json users.jsonl
</span></span></code></pre></div><p>This is crucial for:</p>
<ul>
<li>Data quality checks</li>
<li>ETL pipeline validation</li>
<li>API contract testing</li>
</ul>
<h2 id="format-conversion-csv-integration">Format Conversion: CSV Integration</h2>
<h3 id="jsonl-to-csv">JSONL to CSV</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Flatten nested structure to CSV</span>
</span></span><span class="line"><span class="cl">ja to-csv users.jsonl &gt; users.csv
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Select specific fields</span>
</span></span><span class="line"><span class="cl">ja project user.name,user.age users.jsonl <span class="p">|</span> ja to-csv &gt; simple.csv
</span></span></code></pre></div><p><strong>Intelligent flattening</strong>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="p">{</span><span class="nt">&#34;user&#34;</span><span class="p">:</span> <span class="p">{</span><span class="nt">&#34;name&#34;</span><span class="p">:</span> <span class="s2">&#34;Alice&#34;</span><span class="p">,</span> <span class="nt">&#34;age&#34;</span><span class="p">:</span> <span class="mi">30</span><span class="p">}}</span>
</span></span></code></pre></div><p>Becomes:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csv" data-lang="csv"><span class="line"><span class="cl"><span class="s">user.name</span><span class="p">,</span><span class="s">user.age</span><span class="p">
</span></span></span><span class="line"><span class="cl"><span class="s">Alice</span><span class="p">,</span><span class="s">30</span><span class="p">
</span></span></span></code></pre></div><h3 id="csv-to-jsonl">CSV to JSONL</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Convert CSV to JSONL</span>
</span></span><span class="line"><span class="cl">ja from-csv users.csv &gt; users.jsonl
</span></span></code></pre></div><h2 id="interactive-repl">Interactive REPL</h2>
<p>Build and test pipelines interactively:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">$ ja repl users.jsonl
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">ja&gt; <span class="k">select</span> <span class="s1">&#39;age &gt; 25&#39;</span>
</span></span><span class="line"><span class="cl"><span class="c1"># Preview first few results...</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">ja&gt; project name,email
</span></span><span class="line"><span class="cl"><span class="c1"># Preview transformed data...</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">ja&gt; groupby department --agg avg:salary
</span></span><span class="line"><span class="cl"><span class="c1"># See aggregated results...</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">ja&gt; write output.jsonl
</span></span><span class="line"><span class="cl"><span class="c1"># Save pipeline results</span>
</span></span></code></pre></div><p>The REPL is perfect for:</p>
<ul>
<li>Exploring unfamiliar data</li>
<li>Testing query predicates</li>
<li>Iterative pipeline development</li>
<li>Teaching relational algebra concepts</li>
</ul>
<h2 id="command-line-examples">Command-Line Examples</h2>
<h3 id="log-analysis">Log Analysis</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Find error logs from auth service</span>
</span></span><span class="line"><span class="cl">ja <span class="k">select</span> <span class="s1">&#39;level == `&#34;ERROR&#34;` and service == `&#34;auth&#34;`&#39;</span> app.log.jsonl
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Count errors by service</span>
</span></span><span class="line"><span class="cl">ja <span class="k">select</span> <span class="s1">&#39;level == `&#34;ERROR&#34;`&#39;</span> app.log.jsonl <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> ja groupby service --agg count
</span></span></code></pre></div><h3 id="etl-pipeline">ETL Pipeline</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Extract, transform, load</span>
</span></span><span class="line"><span class="cl">ja <span class="k">select</span> <span class="s1">&#39;status == `&#34;completed&#34;`&#39;</span> sales.jsonl <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> ja project customer_id,amount,date <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> ja join - customers.jsonl --on <span class="nv">customer_id</span><span class="o">=</span>id <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> ja groupby customer.region --agg sum:amount <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> ja sort sum_amount:desc <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> ja head <span class="m">10</span>
</span></span></code></pre></div><h3 id="data-quality-checks">Data Quality Checks</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Find users without email</span>
</span></span><span class="line"><span class="cl">ja <span class="k">select</span> <span class="s1">&#39;not (email != `null`)&#39;</span> users.jsonl
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Find duplicate user IDs</span>
</span></span><span class="line"><span class="cl">ja groupby user_id users.jsonl <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> ja <span class="k">select</span> <span class="s1">&#39;_group_size &gt; 1&#39;</span>
</span></span></code></pre></div><h3 id="report-generation">Report Generation</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Monthly sales report</span>
</span></span><span class="line"><span class="cl">ja join orders.jsonl products.jsonl --on <span class="nv">product_id</span><span class="o">=</span>id <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> ja groupby month,category --agg sum:revenue,count:orders <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> ja sort month,sum_revenue:desc <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> ja to-csv &gt; monthly_report.csv
</span></span></code></pre></div><h2 id="python-api">Python API</h2>
<p>Use <code>ja</code> programmatically:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">ja.core</span> <span class="kn">import</span> <span class="n">read_jsonl</span><span class="p">,</span> <span class="n">join</span><span class="p">,</span> <span class="n">groupby_agg</span><span class="p">,</span> <span class="n">select_rows</span>
</span></span><span class="line"><span class="cl"><span class="kn">from</span> <span class="nn">ja.schema</span> <span class="kn">import</span> <span class="n">infer_schema</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Load data</span>
</span></span><span class="line"><span class="cl"><span class="n">users</span> <span class="o">=</span> <span class="n">read_jsonl</span><span class="p">(</span><span class="s2">&#34;users.jsonl&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">orders</span> <span class="o">=</span> <span class="n">read_jsonl</span><span class="p">(</span><span class="s2">&#34;orders.jsonl&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Join</span>
</span></span><span class="line"><span class="cl"><span class="n">joined</span> <span class="o">=</span> <span class="n">join</span><span class="p">(</span><span class="n">users</span><span class="p">,</span> <span class="n">orders</span><span class="p">,</span> <span class="n">on</span><span class="o">=</span><span class="p">[(</span><span class="s2">&#34;user.id&#34;</span><span class="p">,</span> <span class="s2">&#34;customer_id&#34;</span><span class="p">)])</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Filter</span>
</span></span><span class="line"><span class="cl"><span class="n">high_value</span> <span class="o">=</span> <span class="n">select_rows</span><span class="p">(</span><span class="n">joined</span><span class="p">,</span> <span class="s2">&#34;amount &gt; 100&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Aggregate</span>
</span></span><span class="line"><span class="cl"><span class="n">result</span> <span class="o">=</span> <span class="n">groupby_agg</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">    <span class="n">high_value</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">group_by_key</span><span class="o">=</span><span class="s2">&#34;user.name&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">aggregations</span><span class="o">=</span><span class="p">[(</span><span class="s2">&#34;sum&#34;</span><span class="p">,</span> <span class="s2">&#34;amount&#34;</span><span class="p">),</span> <span class="p">(</span><span class="s2">&#34;count&#34;</span><span class="p">,</span> <span class="kc">None</span><span class="p">)]</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="nb">print</span><span class="p">(</span><span class="nb">list</span><span class="p">(</span><span class="n">result</span><span class="p">))</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Schema inference</span>
</span></span><span class="line"><span class="cl"><span class="n">schema</span> <span class="o">=</span> <span class="n">infer_schema</span><span class="p">(</span><span class="n">users</span><span class="p">)</span>
</span></span></code></pre></div><h2 id="integration-with-ecosystem">Integration with Ecosystem</h2>
<p>jsonl-algebra works seamlessly with other tools:</p>
<h3 id="with-jaf-filtering">With JAF (filtering)</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># JAF for boolean filtering, ja for relational ops</span>
</span></span><span class="line"><span class="cl">jaf filter users.jsonl <span class="s1">&#39;(eq? @status &#34;active&#34;)&#39;</span> --eval <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> ja join - orders.jsonl --on <span class="nv">id</span><span class="o">=</span>user_id <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> ja groupby name --agg sum:amount
</span></span></code></pre></div><h3 id="with-jq-json-manipulation">With jq (JSON manipulation)</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># ja for relational ops, jq for complex transformations</span>
</span></span><span class="line"><span class="cl">ja join users.jsonl orders.jsonl --on <span class="nv">id</span><span class="o">=</span>user_id <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> jq <span class="s1">&#39;.metadata = {processed: now, version: &#34;1.0&#34;}&#39;</span>
</span></span></code></pre></div><h3 id="with-standard-unix-tools">With standard Unix tools</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Combine with grep, sort, uniq</span>
</span></span><span class="line"><span class="cl">ja project email users.jsonl <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> grep <span class="s1">&#39;@gmail.com&#39;</span> <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> sort <span class="se">\
</span></span></span><span class="line"><span class="cl">  <span class="p">|</span> uniq -c
</span></span></code></pre></div><h2 id="theoretical-foundation-relational-algebra">Theoretical Foundation: Relational Algebra</h2>
<p>jsonl-algebra implements Codd&rsquo;s relational algebra extended for nested data:</p>
<h3 id="completeness">Completeness</h3>
<p>The five operations (σ, π, ⋈, ∪, −) form a <strong>complete algebra</strong>—any relational query can be expressed using these primitives.</p>
<h3 id="closure-property">Closure Property</h3>
<p>Every operation produces a valid relation:</p>
\[
\text{Relation} \xrightarrow{\text{Operation}} \text{Relation}
\]<p>This ensures <strong>composability</strong>: <code>R | op1 | op2 | op3</code> always works.</p>
<h3 id="query-optimization">Query Optimization</h3>
<p>Because operations have well-defined properties, queries can be optimized:</p>
<p><strong>Selection pushdown</strong>:
</p>
\[
\sigma_p(R \bowtie S) \equiv (\sigma_p(R)) \bowtie S \quad \text{(if p only references R)}
\]<p><strong>Projection elimination</strong>:
</p>
\[
\pi_a(\pi_{a,b}(R)) \equiv \pi_a(R)
\]<p>These properties enable automatic query optimization (future work).</p>
<h2 id="nested-data-extension">Nested Data Extension</h2>
<p>Traditional relational algebra assumes flat tuples. jsonl-algebra extends this with:</p>
<h3 id="path-expressions">Path Expressions</h3>
<p>Replace column names with dot-separated paths:</p>
\[
\sigma_{\text{user.age} > 30}(R) \quad \text{vs} \quad \sigma_{\text{age} > 30}(R)
\]<h3 id="deep-equality">Deep Equality</h3>
<p>Join conditions can reference nested fields:</p>
\[
R \bowtie_{\text{user.id} = \text{order.customer\_id}} S
\]<h3 id="projection-flattening">Projection Flattening</h3>
<p>Projection can flatten nested structures:</p>
\[
\pi_{\text{user.name as name}, \text{user.address.city as city}}(R)
\]<h2 id="performance-characteristics">Performance Characteristics</h2>
<table>
  <thead>
      <tr>
          <th>Operation</th>
          <th>Memory</th>
          <th>Time</th>
          <th>Notes</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>select</strong></td>
          <td>O(1)</td>
          <td>O(n)</td>
          <td>Streaming filter</td>
      </tr>
      <tr>
          <td><strong>project</strong></td>
          <td>O(1)</td>
          <td>O(n)</td>
          <td>Field extraction</td>
      </tr>
      <tr>
          <td><strong>join</strong></td>
          <td>O(min(R,S))</td>
          <td>O(R×S)</td>
          <td>Hash join (smaller table in memory)</td>
      </tr>
      <tr>
          <td><strong>union</strong></td>
          <td>O(1)</td>
          <td>O(R+S)</td>
          <td>Streaming concatenation</td>
      </tr>
      <tr>
          <td><strong>distinct</strong></td>
          <td>O(k)</td>
          <td>O(n)</td>
          <td>k = unique items</td>
      </tr>
      <tr>
          <td><strong>groupby</strong></td>
          <td>O(g)</td>
          <td>O(n)</td>
          <td>g = number of groups</td>
      </tr>
      <tr>
          <td><strong>sort</strong></td>
          <td>O(n)</td>
          <td>O(n log n)</td>
          <td>External sort for large data</td>
      </tr>
  </tbody>
</table>
<p>jsonl-algebra is designed for:</p>
<ul>
<li>✅ <strong>Streaming operations</strong> where possible</li>
<li>✅ <strong>Bounded memory</strong> for most operations</li>
<li>✅ <strong>Efficient joins</strong> via hash tables</li>
<li>✅ <strong>External sorting</strong> for datasets larger than RAM</li>
</ul>
<h2 id="when-to-use-jsonl-algebra">When to Use jsonl-algebra</h2>
<p><strong>Use <code>ja</code> when</strong>:</p>
<ul>
<li>✅ Working with JSONL data</li>
<li>✅ Need SQL-like operations on nested JSON</li>
<li>✅ Building command-line data pipelines</li>
<li>✅ Joining multiple data sources</li>
<li>✅ Aggregating and grouping data</li>
<li>✅ Want schema inference/validation</li>
</ul>
<p><strong>Use something else when</strong>:</p>
<ul>
<li>❌ Need a persistent database → PostgreSQL, MongoDB</li>
<li>❌ Complex analytical queries → DuckDB, ClickHouse</li>
<li>❌ Real-time streaming → Apache Kafka, Flink</li>
<li>❌ Just filtering/mapping → JAF</li>
</ul>
<p><strong>Combine with</strong>:</p>
<ul>
<li><strong>JAF</strong>: Boolean filtering before relational ops</li>
<li><strong>jq</strong>: Complex JSON transformations</li>
<li><strong>SQL databases</strong>: Load processed data</li>
</ul>
<h2 id="design-philosophy">Design Philosophy</h2>
<p><strong>🎯 Unix Philosophy</strong>: Do one thing well (relational algebra)</p>
<p><strong>🔗 Composability</strong>: Every operation produces a valid relation</p>
<p><strong>📦 Streaming First</strong>: Process data without loading into memory</p>
<p><strong>🛡️ Type Safety</strong>: Schema inference and validation</p>
<p><strong>📚 Pedagogical</strong>: Clear mapping to mathematical operations</p>
<p><strong>⚡ Production-Ready</strong>: Battle-tested, published on PyPI</p>
<h2 id="quick-start">Quick Start</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># Install</span>
</span></span><span class="line"><span class="cl">pip install jsonl-algebra
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Basic usage</span>
</span></span><span class="line"><span class="cl">ja <span class="k">select</span> <span class="s1">&#39;age &gt; 30&#39;</span> users.jsonl
</span></span><span class="line"><span class="cl">ja project name,email users.jsonl
</span></span><span class="line"><span class="cl">ja join users.jsonl orders.jsonl --on <span class="nv">id</span><span class="o">=</span>user_id
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Interactive mode</span>
</span></span><span class="line"><span class="cl">ja repl data.jsonl
</span></span></code></pre></div><h2 id="comparison-dotsuite-vs-jsonl-algebra">Comparison: dotsuite vs jsonl-algebra</h2>
<table>
  <thead>
      <tr>
          <th>Feature</th>
          <th>dotsuite (dotrelate)</th>
          <th>jsonl-algebra (ja)</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>Status</strong></td>
          <td>Pedagogical concept</td>
          <td>Production tool</td>
      </tr>
      <tr>
          <td><strong>Distribution</strong></td>
          <td>Source code</td>
          <td>PyPI package</td>
      </tr>
      <tr>
          <td><strong>Operations</strong></td>
          <td>Basic join, union</td>
          <td>Full relational algebra</td>
      </tr>
      <tr>
          <td><strong>Aggregation</strong></td>
          <td>Planned</td>
          <td>Complete (sum, avg, count, etc.)</td>
      </tr>
      <tr>
          <td><strong>Schema</strong></td>
          <td>None</td>
          <td>Inference + validation</td>
      </tr>
      <tr>
          <td><strong>Interactive</strong></td>
          <td>No</td>
          <td>REPL included</td>
      </tr>
      <tr>
          <td><strong>Format conversion</strong></td>
          <td>No</td>
          <td>CSV support</td>
      </tr>
      <tr>
          <td><strong>Documentation</strong></td>
          <td>Concept docs</td>
          <td>Full CLI reference</td>
      </tr>
      <tr>
          <td><strong>Testing</strong></td>
          <td>Basic</td>
          <td>Comprehensive test suite</td>
      </tr>
  </tbody>
</table>
<p><strong>Recommendation</strong>: Learn relational concepts through dotsuite, use <code>ja</code> for production.</p>
<h2 id="resources">Resources</h2>
<ul>
<li><strong>PyPI</strong>: <a href="https://pypi.org/project/jsonl-algebra/">pypi.org/project/jsonl-algebra/</a></li>
<li><strong>Repository</strong>: <a href="https://github.com/queelius/jsonl-algebra">github.com/queelius/jsonl-algebra</a></li>
<li><strong>Quick Start</strong>: <a href="https://github.com/queelius/jsonl-algebra/blob/master/docs/quickstart.md">docs/quickstart.md</a></li>
<li><strong>Concepts</strong>: <a href="https://github.com/queelius/jsonl-algebra/blob/master/docs/concepts/jsonl-algebra.md">docs/concepts/jsonl-algebra.md</a></li>
</ul>
<h2 id="license">License</h2>
<p>MIT</p>
<hr>
<p><em>jsonl-algebra: Bringing the power of relational algebra to nested JSON, one operation at a time. From SQL&rsquo;s flat tables to JSON&rsquo;s nested structures, with full mathematical foundations.</em></p>
]]></content:encoded><category>Python</category><category>JSON</category><category>relational-algebra</category><category>ETL</category><category>data-processing</category><category>Unix-philosophy</category><category>command-line</category><category>sicp</category></item><item><title>Accumux: Compositional Online Statistical Reductions in C++</title><link>https://metafunctor.com/post/2024-03-01-accumux/</link><pubDate>Fri, 01 Mar 2024 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-03-01-accumux/</guid><description>A modern C++20 library for compositional online data reductions with numerically stable algorithms and algebraic composition.</description><content:encoded><![CDATA[<p><strong><a href="https://github.com/queelius/accumux">Accumux</a></strong> provides a framework for combining statistical accumulators using algebraic composition. Built on solid mathematical foundations (monoids, homomorphisms), it enables single-pass computation of complex statistical measures with optimal numerical stability.</p>
<h2 id="the-problem-multi-statistic-computation">The Problem: Multi-Statistic Computation</h2>
<p>Computing multiple statistics over large datasets typically requires:</p>
<ul>
<li>Multiple passes over the data, or</li>
<li>Hand-rolled code combining different algorithms, or</li>
<li>Numerical instability from naive implementations</li>
</ul>
<p>Accumux solves this with <strong>compositional accumulators</strong>: combine any accumulators with <code>+</code>, process data once, extract all results.</p>
<h2 id="quick-example">Quick Example</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="cp">#include</span> <span class="cpf">&#34;accumux/accumulators/kbn_sum.hpp&#34;</span><span class="cp">
</span></span></span><span class="line"><span class="cl"><span class="cp">#include</span> <span class="cpf">&#34;accumux/accumulators/welford.hpp&#34;</span><span class="cp">
</span></span></span><span class="line"><span class="cl"><span class="cp">#include</span> <span class="cpf">&#34;accumux/core/composition.hpp&#34;</span><span class="cp">
</span></span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">using</span> <span class="k">namespace</span> <span class="n">accumux</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// Compose accumulators with +
</span></span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">stats</span> <span class="o">=</span> <span class="n">kbn_sum</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span><span class="p">()</span> <span class="o">+</span> <span class="n">welford_accumulator</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// Single pass through data
</span></span></span><span class="line"><span class="cl"><span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span> <span class="n">data</span> <span class="o">=</span> <span class="p">{</span><span class="mf">1.0</span><span class="p">,</span> <span class="mf">2.0</span><span class="p">,</span> <span class="mf">3.0</span><span class="p">,</span> <span class="mf">4.0</span><span class="p">,</span> <span class="mf">5.0</span><span class="p">};</span>
</span></span><span class="line"><span class="cl"><span class="k">for</span> <span class="p">(</span><span class="k">const</span> <span class="k">auto</span><span class="o">&amp;</span> <span class="nl">value</span> <span class="p">:</span> <span class="n">data</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">stats</span> <span class="o">+=</span> <span class="n">value</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// Extract all results
</span></span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">sum</span> <span class="o">=</span> <span class="n">stats</span><span class="p">.</span><span class="n">get_first</span><span class="p">().</span><span class="n">eval</span><span class="p">();</span>           <span class="c1">// 15.0
</span></span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">mean</span> <span class="o">=</span> <span class="n">stats</span><span class="p">.</span><span class="n">get_second</span><span class="p">().</span><span class="n">mean</span><span class="p">();</span>         <span class="c1">// 3.0
</span></span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">variance</span> <span class="o">=</span> <span class="n">stats</span><span class="p">.</span><span class="n">get_second</span><span class="p">().</span><span class="n">sample_variance</span><span class="p">();</span>  <span class="c1">// 2.5
</span></span></span></code></pre></div><h2 id="numerically-stable-algorithms">Numerically Stable Algorithms</h2>
<p>Accumux uses proven algorithms that maintain accuracy even with ill-conditioned data:</p>
<h3 id="kahan-babuška-neumaier-summation">Kahan-Babuška-Neumaier Summation</h3>
<p>Standard floating-point summation loses precision:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="c1">// Naive sum fails on this
</span></span></span><span class="line"><span class="cl"><span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span> <span class="n">values</span> <span class="o">=</span> <span class="p">{</span><span class="mf">1.0</span><span class="p">,</span> <span class="mf">1e100</span><span class="p">,</span> <span class="mf">1.0</span><span class="p">,</span> <span class="o">-</span><span class="mf">1e100</span><span class="p">};</span>
</span></span><span class="line"><span class="cl"><span class="c1">// Naive: 0.0 (wrong!)
</span></span></span><span class="line"><span class="cl"><span class="c1">// KBN:   2.0 (correct!)
</span></span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">summer</span> <span class="o">=</span> <span class="n">kbn_sum</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span><span class="p">();</span>
</span></span><span class="line"><span class="cl"><span class="k">for</span> <span class="p">(</span><span class="k">auto</span> <span class="nl">v</span> <span class="p">:</span> <span class="n">values</span><span class="p">)</span> <span class="n">summer</span> <span class="o">+=</span> <span class="n">v</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="n">std</span><span class="o">::</span><span class="n">cout</span> <span class="o">&lt;&lt;</span> <span class="n">summer</span><span class="p">.</span><span class="n">eval</span><span class="p">();</span>  <span class="c1">// 2.0
</span></span></span></code></pre></div><h3 id="welfords-online-algorithm">Welford&rsquo;s Online Algorithm</h3>
<p>Computes mean and variance in a single pass without catastrophic cancellation:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">welford</span> <span class="o">=</span> <span class="n">welford_accumulator</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span><span class="p">();</span>
</span></span><span class="line"><span class="cl"><span class="k">for</span> <span class="p">(</span><span class="k">auto</span> <span class="nl">v</span> <span class="p">:</span> <span class="n">data</span><span class="p">)</span> <span class="n">welford</span> <span class="o">+=</span> <span class="n">v</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">welford</span><span class="p">.</span><span class="n">count</span><span class="p">();</span>           <span class="c1">// Number of samples
</span></span></span><span class="line"><span class="cl"><span class="n">welford</span><span class="p">.</span><span class="n">mean</span><span class="p">();</span>            <span class="c1">// Running mean
</span></span></span><span class="line"><span class="cl"><span class="n">welford</span><span class="p">.</span><span class="n">sample_variance</span><span class="p">();</span> <span class="c1">// Unbiased variance
</span></span></span><span class="line"><span class="cl"><span class="n">welford</span><span class="p">.</span><span class="n">sample_std_dev</span><span class="p">();</span>  <span class="c1">// Standard deviation
</span></span></span></code></pre></div><h3 id="minmax-tracking">Min/Max Tracking</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">minmax</span> <span class="o">=</span> <span class="n">minmax_accumulator</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span><span class="p">();</span>
</span></span><span class="line"><span class="cl"><span class="k">for</span> <span class="p">(</span><span class="k">auto</span> <span class="nl">v</span> <span class="p">:</span> <span class="n">data</span><span class="p">)</span> <span class="n">minmax</span> <span class="o">+=</span> <span class="n">v</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">minmax</span><span class="p">.</span><span class="n">min</span><span class="p">();</span>  <span class="c1">// Minimum value
</span></span></span><span class="line"><span class="cl"><span class="n">minmax</span><span class="p">.</span><span class="n">max</span><span class="p">();</span>  <span class="c1">// Maximum value
</span></span></span></code></pre></div><h2 id="algebraic-composition">Algebraic Composition</h2>
<p>The key insight: accumulators form a <strong>monoid</strong> under composition.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="c1">// Compose arbitrarily many accumulators
</span></span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">financial</span> <span class="o">=</span> <span class="n">kbn_sum</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span><span class="p">()</span> <span class="o">+</span>
</span></span><span class="line"><span class="cl">                 <span class="n">welford_accumulator</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span><span class="p">()</span> <span class="o">+</span>
</span></span><span class="line"><span class="cl">                 <span class="n">minmax_accumulator</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span> <span class="n">returns</span> <span class="o">=</span> <span class="p">{</span><span class="mf">0.05</span><span class="p">,</span> <span class="o">-</span><span class="mf">0.02</span><span class="p">,</span> <span class="mf">0.03</span><span class="p">,</span> <span class="mf">0.01</span><span class="p">,</span> <span class="o">-</span><span class="mf">0.01</span><span class="p">,</span> <span class="mf">0.04</span><span class="p">};</span>
</span></span><span class="line"><span class="cl"><span class="k">for</span> <span class="p">(</span><span class="k">auto</span> <span class="nl">ret</span> <span class="p">:</span> <span class="n">returns</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">financial</span> <span class="o">+=</span> <span class="n">ret</span><span class="p">;</span>  <span class="c1">// All three update simultaneously
</span></span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// Extract nested results
</span></span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">total</span> <span class="o">=</span> <span class="n">financial</span><span class="p">.</span><span class="n">get_first</span><span class="p">().</span><span class="n">eval</span><span class="p">();</span>
</span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">mean</span> <span class="o">=</span> <span class="n">financial</span><span class="p">.</span><span class="n">get_second</span><span class="p">().</span><span class="n">mean</span><span class="p">();</span>
</span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">volatility</span> <span class="o">=</span> <span class="n">financial</span><span class="p">.</span><span class="n">get_second</span><span class="p">().</span><span class="n">sample_std_dev</span><span class="p">();</span>
</span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">worst</span> <span class="o">=</span> <span class="n">financial</span><span class="p">.</span><span class="n">get_second</span><span class="p">().</span><span class="n">get_second</span><span class="p">().</span><span class="n">min</span><span class="p">();</span>
</span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">best</span> <span class="o">=</span> <span class="n">financial</span><span class="p">.</span><span class="n">get_second</span><span class="p">().</span><span class="n">get_second</span><span class="p">().</span><span class="n">max</span><span class="p">();</span>
</span></span></code></pre></div><h2 id="mathematical-foundation">Mathematical Foundation</h2>
<h3 id="monoid-structure">Monoid Structure</h3>
<p>Each accumulator type <code>A</code> forms a monoid:</p>
<ul>
<li><strong>Identity</strong>: Empty accumulator with no observations</li>
<li><strong>Binary operation</strong>: Merge two accumulators (combine their observations)</li>
</ul>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">a</span> <span class="o">=</span> <span class="n">welford_accumulator</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span><span class="p">();</span>
</span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">b</span> <span class="o">=</span> <span class="n">welford_accumulator</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// Process different data
</span></span></span><span class="line"><span class="cl"><span class="k">for</span> <span class="p">(</span><span class="k">auto</span> <span class="nl">v</span> <span class="p">:</span> <span class="n">data1</span><span class="p">)</span> <span class="n">a</span> <span class="o">+=</span> <span class="n">v</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">for</span> <span class="p">(</span><span class="k">auto</span> <span class="nl">v</span> <span class="p">:</span> <span class="n">data2</span><span class="p">)</span> <span class="n">b</span> <span class="o">+=</span> <span class="n">v</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// Merge results
</span></span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">combined</span> <span class="o">=</span> <span class="n">a</span> <span class="o">+</span> <span class="n">b</span><span class="p">;</span>  <span class="c1">// Equivalent to processing data1 ++ data2
</span></span></span></code></pre></div><h3 id="homomorphism-property">Homomorphism Property</h3>
<p>The composition operation preserves structure:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">(a + b).process(x) = a.process(x) + b.process(x)
</span></span></code></pre></div><p>This enables parallel processing: split data, accumulate in parallel, merge results.</p>
<h2 id="type-safety-with-c20-concepts">Type Safety with C++20 Concepts</h2>
<p>Invalid compositions fail at compile-time:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="c1">// Compile error: can&#39;t add incompatible accumulators
</span></span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">invalid</span> <span class="o">=</span> <span class="n">kbn_sum</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span><span class="p">()</span> <span class="o">+</span> <span class="n">kbn_sum</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span><span class="p">();</span>  <span class="c1">// Type mismatch!
</span></span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// OK: compatible types compose
</span></span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">valid</span> <span class="o">=</span> <span class="n">kbn_sum</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span><span class="p">()</span> <span class="o">+</span> <span class="n">welford_accumulator</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span><span class="p">();</span>
</span></span></code></pre></div><h2 id="use-cases">Use Cases</h2>
<ul>
<li><strong>Financial Analysis</strong>: Track returns, volatility, drawdowns in one pass</li>
<li><strong>Scientific Computing</strong>: Online statistics for streaming sensor data</li>
<li><strong>Machine Learning</strong>: Feature statistics during data preprocessing</li>
<li><strong>Monitoring Systems</strong>: Real-time metrics aggregation</li>
</ul>
<h2 id="performance">Performance</h2>
<ul>
<li><strong>O(1) space</strong> per accumulator (constant memory regardless of data size)</li>
<li><strong>O(n) time</strong> for n data points (single pass)</li>
<li><strong>Zero allocations</strong> during accumulation</li>
<li><strong>Header-only</strong>: No linking, no dependencies</li>
</ul>
<h2 id="installation">Installation</h2>
<p>Header-only—just include:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="cp">#include</span> <span class="cpf">&#34;accumux/accumulators/kbn_sum.hpp&#34;</span><span class="cp">
</span></span></span><span class="line"><span class="cl"><span class="cp">#include</span> <span class="cpf">&#34;accumux/accumulators/welford.hpp&#34;</span><span class="cp">
</span></span></span><span class="line"><span class="cl"><span class="cp">#include</span> <span class="cpf">&#34;accumux/core/composition.hpp&#34;</span><span class="cp">
</span></span></span></code></pre></div><p>Or with CMake:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cmake" data-lang="cmake"><span class="line"><span class="cl"><span class="nb">add_subdirectory</span><span class="p">(</span><span class="s">accumux</span><span class="p">)</span><span class="err">
</span></span></span><span class="line"><span class="cl"><span class="nb">target_link_libraries</span><span class="p">(</span><span class="s">your_target</span> <span class="s">PRIVATE</span> <span class="s">accumux::accumux</span><span class="p">)</span><span class="err">
</span></span></span></code></pre></div><h2 id="resources">Resources</h2>
<ul>
<li><strong>GitHub</strong>: <a href="https://github.com/queelius/accumux">github.com/queelius/accumux</a></li>
<li><strong>Paper</strong>: <a href="/papers/accumux/">AccuMux Technical Report</a></li>
</ul>
<hr>
<p><em>Accumux: Compose your statistics, compute in one pass, trust the math.</em></p>
]]></content:encoded><category>C++</category><category>statistics</category><category>numerical-computing</category><category>accumulators</category><category>functional-programming</category><category>header-only</category><category>sicp</category></item><item><title>algebraic.dist: Treating Distributions as First-Class Algebraic Objects in R</title><link>https://metafunctor.com/post/2024-05-01-algebraic.dist/</link><pubDate>Mon, 01 Feb 2021 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-05-01-algebraic.dist/</guid><description>An R package for treating probability distributions as first-class algebraic objects that compose through standard operations.</description><content:encoded><![CDATA[<p>Most statistical software treats probability distributions as <strong>static parameter sets</strong> you pass to sampling or density functions. <code>algebraic.dist</code> takes a different approach: <strong>distributions are algebraic objects</strong> that compose, transform, and combine using standard mathematical operations.</p>
<h2 id="the-core-idea">The Core Idea</h2>
<p>Instead of writing:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="n">x</span> <span class="o">&lt;-</span> <span class="nf">rnorm</span><span class="p">(</span><span class="m">1000</span><span class="p">,</span> <span class="n">mean</span><span class="o">=</span><span class="m">5</span><span class="p">,</span> <span class="n">sd</span><span class="o">=</span><span class="m">2</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">y</span> <span class="o">&lt;-</span> <span class="nf">rnorm</span><span class="p">(</span><span class="m">1000</span><span class="p">,</span> <span class="n">mean</span><span class="o">=</span><span class="m">3</span><span class="p">,</span> <span class="n">sd</span><span class="o">=</span><span class="m">1</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">z</span> <span class="o">&lt;-</span> <span class="n">x</span> <span class="o">+</span> <span class="n">y</span>  <span class="c1"># Just numeric vectors</span>
</span></span></code></pre></div><p>You write:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="n">X</span> <span class="o">&lt;-</span> <span class="nf">Normal</span><span class="p">(</span><span class="n">mean</span><span class="o">=</span><span class="m">5</span><span class="p">,</span> <span class="n">sd</span><span class="o">=</span><span class="m">2</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">Y</span> <span class="o">&lt;-</span> <span class="nf">Normal</span><span class="p">(</span><span class="n">mean</span><span class="o">=</span><span class="m">3</span><span class="p">,</span> <span class="n">sd</span><span class="o">=</span><span class="m">1</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">Z</span> <span class="o">&lt;-</span> <span class="n">X</span> <span class="o">+</span> <span class="n">Y</span>  <span class="c1"># A new distribution object!</span>
</span></span><span class="line"><span class="cl"><span class="nf">sample</span><span class="p">(</span><span class="n">Z</span><span class="p">,</span> <span class="m">1000</span><span class="p">)</span>
</span></span></code></pre></div><p>The sum <code>Z</code> <strong>knows</strong> it&rsquo;s <code>Normal(mean=8, sd=sqrt(5))</code> because the algebra works it out.</p>
<h2 id="why-this-matters">Why This Matters</h2>
<p><strong>1. Correctness Through Type Safety</strong></p>
<p>When you add two normal distributions numerically, you get samples from the sum. But you lose the <strong>distributional structure</strong>. With <code>algebraic.dist</code>, the result is still a distribution object with proper parameters.</p>
<p><strong>2. Symbolic Computation Before Sampling</strong></p>
<p>You can build complex distributional expressions and simplify them algebraically before ever drawing a sample:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="n">portfolio</span> <span class="o">&lt;-</span> <span class="m">0.6</span><span class="o">*</span><span class="n">StockA</span> <span class="o">+</span> <span class="m">0.4</span><span class="o">*</span><span class="n">StockB</span>
</span></span><span class="line"><span class="cl"><span class="n">risk</span> <span class="o">&lt;-</span> <span class="nf">sd</span><span class="p">(</span><span class="n">portfolio</span><span class="p">)</span>  <span class="c1"># Computed symbolically</span>
</span></span></code></pre></div><p><strong>3. Monte Carlo Without the Monte Carlo</strong></p>
<p>For distributions with known closed-form algebra (normal, exponential, certain mixtures), you don&rsquo;t need simulation—you just compute the exact answer.</p>
<h2 id="compositional-statistics">Compositional Statistics</h2>
<p>This is <strong>functional programming for probability theory</strong>. Distributions become composable building blocks:</p>
<ul>
<li><strong>Mixture models</strong>: <code>0.3*Normal(0,1) + 0.7*Normal(5,2)</code></li>
<li><strong>Transformed distributions</strong>: <code>exp(Normal(0,1))</code> is lognormal</li>
<li><strong>Conditional distributions</strong>: <code>X | (X &gt; 0)</code> for truncation</li>
</ul>
<h2 id="connection-to-my-research">Connection to My Research</h2>
<p>This package embodies a core theme in my work: <strong>computation should mirror mathematical structure</strong>.</p>
<p>Just as my oblivious computing research uses type theory to enforce privacy invariants, <code>algebraic.dist</code> uses algebraic types to enforce <strong>distributional invariants</strong>. The algebra tells you what operations are valid and what the results mean.</p>
<p><strong>Related projects</strong>:</p>
<ul>
<li><strong>algebraic.mle</strong>: Maximum likelihood estimation with algebraic specification</li>
<li><strong>numerical.mle</strong>: Numerical optimization for MLE when closed forms don&rsquo;t exist</li>
<li><strong>likelihood.model</strong>: Likelihood-based inference with compositional model building</li>
</ul>
<h2 id="technical-details">Technical Details</h2>
<ul>
<li><strong>Language</strong>: R</li>
<li><strong>Type system</strong>: S3 classes with method dispatch for operations</li>
<li><strong>Closed-form operations</strong>: Normal, exponential, gamma families</li>
<li><strong>Fallback</strong>: Monte Carlo for complex compositions</li>
<li><strong>Repository</strong>: <a href="https://github.com/queelius/algebraic.dist">github.com/queelius/algebraic.dist</a></li>
</ul>
<h2 id="the-bigger-picture">The Bigger Picture</h2>
<p>Most statistical software is <strong>imperative</strong>: you tell the computer what to do step-by-step. <code>algebraic.dist</code> is <strong>declarative</strong>: you describe the distributional relationships, and the computer figures out what to compute.</p>
<p>This mirrors the Unix philosophy: small composable pieces that do one thing well. Here, the &ldquo;one thing&rdquo; is: <strong>preserve distributional structure through transformations</strong>.</p>
]]></content:encoded><category>statistics</category><category>probability</category><category>distributions</category><category>R</category><category>functional programming</category><category>algebra</category><category>sicp</category></item><item><title>How Iterators Give You N+M Instead of N×M</title><link>https://metafunctor.com/post/2019-11-iterators-composition/</link><pubDate>Fri, 15 Nov 2019 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2019-11-iterators-composition/</guid><description>How iterators reduce the N×M algorithm-container problem to N+M by interposing an abstraction layer, following Stepanov's generic programming approach.</description><content:encoded><![CDATA[<p>The problem is combinatorial. You have N algorithms (sort, search, find, copy) and M containers (array, list, tree, hash table). The naive approach: implement each algorithm for each container. That&rsquo;s N×M implementations.</p>
<p>The insight is to <strong>interpose an abstraction layer</strong>.</p>
<h2 id="the-iterator-abstraction">The Iterator Abstraction</h2>
<p>Instead of algorithms knowing about containers directly, we define <em>iterator categories</em>—capabilities that algorithms require and containers provide:</p>
<p><strong>Input</strong>: Single-pass read. You can advance (<code>++</code>) and dereference (<code>*</code>), but once you move forward, you can&rsquo;t go back. Stream-like.</p>
<p><strong>Forward</strong>: Multi-pass. You can iterate multiple times; <code>begin()</code> always gives the same starting point.</p>
<p><strong>Bidirectional</strong>: Can go backward (<code>--</code>). Enables algorithms like reverse iteration.</p>
<p><strong>Random-access</strong>: Can jump anywhere (<code>+n</code>, <code>[]</code>). Enables binary search, sorting.</p>
<h2 id="a-true-input-iterator">A True Input Iterator</h2>
<p>The input iterator category exists for a reason. Here&rsquo;s a working example that reads entropy from <code>/dev/urandom</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="cp">#include</span> <span class="cpf">&lt;fstream&gt;</span><span class="cp">
</span></span></span><span class="line"><span class="cl"><span class="cp">#include</span> <span class="cpf">&lt;iterator&gt;</span><span class="cp">
</span></span></span><span class="line"><span class="cl"><span class="cp">#include</span> <span class="cpf">&lt;cstdint&gt;</span><span class="cp">
</span></span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">entropy_iterator</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">iterator_category</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">input_iterator_tag</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span>        <span class="o">=</span> <span class="kt">uint8_t</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">difference_type</span>   <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">ptrdiff_t</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">pointer</span>           <span class="o">=</span> <span class="k">const</span> <span class="kt">uint8_t</span><span class="o">*</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">reference</span>         <span class="o">=</span> <span class="kt">uint8_t</span><span class="p">;</span>  <span class="c1">// returns by value, not reference
</span></span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">ifstream</span><span class="o">*</span> <span class="n">source</span> <span class="o">=</span> <span class="k">nullptr</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="kt">uint8_t</span> <span class="n">byte</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="n">entropy_iterator</span><span class="p">()</span> <span class="o">=</span> <span class="k">default</span><span class="p">;</span>  <span class="c1">// sentinel (end iterator)
</span></span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">explicit</span> <span class="nf">entropy_iterator</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">ifstream</span><span class="o">&amp;</span> <span class="n">s</span><span class="p">)</span> <span class="o">:</span> <span class="n">source</span><span class="p">(</span><span class="o">&amp;</span><span class="n">s</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="o">++</span><span class="p">(</span><span class="o">*</span><span class="k">this</span><span class="p">);</span>  <span class="c1">// prime the first byte
</span></span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kt">uint8_t</span> <span class="k">operator</span><span class="o">*</span><span class="p">()</span> <span class="k">const</span> <span class="p">{</span> <span class="k">return</span> <span class="n">byte</span><span class="p">;</span> <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="n">entropy_iterator</span><span class="o">&amp;</span> <span class="k">operator</span><span class="o">++</span><span class="p">()</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">source</span> <span class="o">&amp;&amp;</span> <span class="n">source</span><span class="o">-&gt;</span><span class="n">good</span><span class="p">())</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">source</span><span class="o">-&gt;</span><span class="n">read</span><span class="p">(</span><span class="k">reinterpret_cast</span><span class="o">&lt;</span><span class="kt">char</span><span class="o">*&gt;</span><span class="p">(</span><span class="o">&amp;</span><span class="n">byte</span><span class="p">),</span> <span class="mi">1</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">source</span><span class="o">-&gt;</span><span class="n">good</span><span class="p">())</span> <span class="n">source</span> <span class="o">=</span> <span class="k">nullptr</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="o">*</span><span class="k">this</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="n">entropy_iterator</span> <span class="k">operator</span><span class="o">++</span><span class="p">(</span><span class="kt">int</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">auto</span> <span class="n">tmp</span> <span class="o">=</span> <span class="o">*</span><span class="k">this</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="o">++</span><span class="p">(</span><span class="o">*</span><span class="k">this</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">tmp</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kt">bool</span> <span class="k">operator</span><span class="o">==</span><span class="p">(</span><span class="k">const</span> <span class="n">entropy_iterator</span><span class="o">&amp;</span> <span class="n">other</span><span class="p">)</span> <span class="k">const</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">source</span> <span class="o">==</span> <span class="n">other</span><span class="p">.</span><span class="n">source</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>Use it like any input iterator:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="kt">int</span> <span class="nf">main</span><span class="p">()</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">ifstream</span> <span class="n">urandom</span><span class="p">(</span><span class="s">&#34;/dev/urandom&#34;</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">ios</span><span class="o">::</span><span class="n">binary</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">entropy_iterator</span> <span class="n">it</span><span class="p">(</span><span class="n">urandom</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="c1">// generate 16 random bytes
</span></span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">uint8_t</span><span class="o">&gt;</span> <span class="n">key</span><span class="p">(</span><span class="mi">16</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">copy_n</span><span class="p">(</span><span class="n">it</span><span class="p">,</span> <span class="mi">16</span><span class="p">,</span> <span class="n">key</span><span class="p">.</span><span class="n">begin</span><span class="p">());</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="c1">// or use in algorithms
</span></span></span><span class="line"><span class="cl">    <span class="kt">int</span> <span class="n">sum</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="mi">1000</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">,</span> <span class="o">++</span><span class="n">it</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">sum</span> <span class="o">+=</span> <span class="o">*</span><span class="n">it</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// sum ≈ 127500 (mean of uniform [0,255] × 1000)
</span></span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Each <code>++</code> consumes a fresh entropy byte from the kernel—you literally cannot iterate twice over the same sequence. This is why the input iterator category exists: some sources are inherently single-pass. Claiming forward iterator capabilities would be a lie.</p>
<p>The same pattern applies to network streams, sensor readings, and any source where data is consumed by reading it.</p>
<h2 id="the-payoff">The Payoff</h2>
<p>Now <code>binary_search</code> doesn&rsquo;t need to know about vectors, deques, or sorted arrays. It only needs random-access iterators. The algorithm expresses its requirements; the container provides capabilities. They compose through the iterator abstraction.</p>
<p>N algorithms + M containers = N+M implementations.</p>
<h2 id="why-this-matters">Why This Matters</h2>
<p>This is Abelson and Sussman&rsquo;s <em>abstraction barrier</em> from SICP, applied to C++. The iterator concept creates a clean separation: algorithms above, containers below, a stable interface between.</p>
<p>Stepanov understood that generic programming isn&rsquo;t about making things work with any type—it&rsquo;s about identifying the <strong>minimal requirements</strong> for each algorithm and expressing them precisely. The iterator hierarchy is that precision.</p>
<p>Every time you write <code>std::sort(v.begin(), v.end())</code>, you&rsquo;re benefiting from this N+M composition.</p>
<hr>
<p><em>The best abstractions don&rsquo;t just reduce code—they reduce the complexity of thought required to reason about systems.</em></p>
]]></content:encoded><category>c++</category><category>generic-programming</category><category>stepanov</category><category>iterators</category><category>sicp</category></item></channel></rss>