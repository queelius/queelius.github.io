<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:media="http://search.yahoo.com/mrss/"><channel><title>Probability on metafunctor</title><link>https://metafunctor.com/tags/probability/</link><description>Research Engineer &amp; Computer Scientist - Machine Learning, Statistical Computing, Open Source Development</description><language>en-us</language><managingEditor>lex@metafunctor.com (Alex Towell)</managingEditor><webMaster>lex@metafunctor.com (Alex Towell)</webMaster><lastBuildDate>Mon, 16 Mar 2026 10:20:10 -0500</lastBuildDate><atom:link href="https://metafunctor.com/tags/probability/" rel="self" type="application/rss+xml"/><image><url>https://metafunctor.com/images/og-image.jpg</url><title>metafunctor</title><link>https://metafunctor.com/</link></image><item><title>An Introduction to Probability Theory and Its Applications</title><link>https://metafunctor.com/media/an-introduction-to-probability-theory-and-its-applications/</link><pubDate>Wed, 17 Dec 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/media/an-introduction-to-probability-theory-and-its-applications/</guid><description/><content:encoded></content:encoded><category>probability</category><category>classic</category><category>reference</category></item><item><title>Introduction to Probability and Mathematical Statistics</title><link>https://metafunctor.com/media/hogg-mckean-craig/</link><pubDate>Wed, 17 Dec 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/media/hogg-mckean-craig/</guid><description>Notes Rigorous graduate-level probability + statistics; useful for inference and ML foundations.</description><content:encoded>&lt;h2 id="notes">Notes&lt;/h2>
&lt;p>Rigorous graduate-level probability + statistics; useful for inference and ML foundations.&lt;/p>
</content:encoded><category>probability</category><category>statistics</category><category>inference</category><category>graduate-level</category></item><item><title>Probability Theory: The Logic of Science</title><link>https://metafunctor.com/media/probability-theory-the-logic-of-science/</link><pubDate>Wed, 17 Dec 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/media/probability-theory-the-logic-of-science/</guid><description/><content:encoded></content:encoded><category>probability</category><category>bayesian</category><category>inference</category><category>classic</category></item><item><title>Alea: A Modern C++ Library for Algebraic Random Elements</title><link>https://metafunctor.com/papers/alea/</link><pubDate>Tue, 07 Oct 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/papers/alea/</guid><description> Open full page Download PDF</description><content:encoded><![CDATA[<style>
.iframe-wrap {
  width: 100%;
  max-width: 100%;
  margin: 0;
  padding: 0;
}
.iframe-wrap iframe {
  display: block;
  width: 100%;
  border: 0;
  min-height: 120vh;
}
.action-bar {
  margin: 0 0 1rem 0;
  padding: 0 1rem;
  display: flex;
  gap: .75rem;
  flex-wrap: wrap;
}
.action-bar a {
  text-decoration: none;
  padding: .5rem .75rem;
  border: 1px solid var(--color-border-default, #ddd);
  border-radius: .4rem;
  display: inline-block;
  color: var(--color-accent-primary, #007acc);
}
.action-bar a:hover {
  background: var(--color-bg-secondary, #f5f5f5);
}
</style>
<div class="action-bar">
  <a href="/latex/alea/index.html" target="_blank" rel="noopener">Open full page</a>
  <a href="/latex/alea/paper.pdf" target="_blank" rel="noopener">Download PDF</a>
</div>
<div class="iframe-wrap">
  <iframe id="ltx" src="/latex/alea/index.html" loading="lazy" scrolling="no"></iframe>
</div>
<script>
(function () {
  const iframe = document.getElementById('ltx');

  if (location.hash) {
    const u = new URL(iframe.src, location.origin);
    u.hash = location.hash;
    iframe.src = u.toString();
  }

  let lastScrollHeight = 0;

  function setHeight(h) {
    const newH = Math.ceil(h);
    if (newH > lastScrollHeight + 5) {
      iframe.style.height = newH + 'px';
      lastScrollHeight = newH;
    }
  }

  function measure(doc) {
    const h = Math.max(
      doc.body?.scrollHeight || 0,
      doc.documentElement?.scrollHeight || 0
    );
    if (h > 0) setHeight(h);
  }

  iframe.addEventListener('load', () => {
    const doc = iframe.contentDocument || iframe.contentWindow.document;
    if (!doc) return;

    setTimeout(() => measure(doc), 100);
    setTimeout(() => measure(doc), 500);

    doc.fonts?.ready?.then(() => {
      setTimeout(() => measure(doc), 50);
    }).catch(() => {});

    const images = Array.from(doc.images || []);
    if (images.length > 0) {
      let loadedCount = 0;
      images.forEach(img => {
        if (img.complete) {
          loadedCount++;
        } else {
          img.addEventListener('load', () => {
            loadedCount++;
            if (loadedCount === images.length) {
              setTimeout(() => measure(doc), 50);
            }
          }, { once: true });
        }
      });
    }

    let resizeTimeout;
    window.addEventListener('resize', () => {
      clearTimeout(resizeTimeout);
      resizeTimeout = setTimeout(() => {
        lastScrollHeight = 0;
        measure(doc);
      }, 300);
    });
  });
})();
</script>
]]></content:encoded><category>c++</category><category>probability</category><category>Monte Carlo</category><category>library</category><category>statistics</category><category>type erasure</category></item><item><title>The Bernoulli Model: A Probabilistic Framework for Data Structures and Types</title><link>https://metafunctor.com/post/bernoulli-boolean-model/</link><pubDate>Sat, 17 Jun 2023 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/bernoulli-boolean-model/</guid><description>The Bernoulli Model is a framework for reasoning about probabilistic data structures by treating noisy outputs as Bernoulli-distributed approximations of latent values, from Booleans to set-indicator functions.</description><content:encoded><![CDATA[<h2 id="motivation">Motivation</h2>
<p>The <strong>Bernoulli Model</strong> is a framework for thinking about probabilistic data structures and types of a particular sort. I developed this formalism primarily to build toward Oblivious Data Types (a separate document), but the core idea stands on its own: Bernoulli approximations have useful properties for reasoning about space/accuracy tradeoffs, and the formalism lets us track correctness through computations.</p>
<p>The Bernoulli Model also provides a way to think about existing probabilistic data structures. The Bloom filter, Count-Min sketch, and my own Bernoulli data type family all fit this framework. The family covers everything from sets (like Bloom filters) to maps, in a near-space-optimal way, with controlled accuracy/space tradeoffs.</p>
<h2 id="introduction-bernoulli-boolean">Introduction: Bernoulli Boolean</h2>
<p>The Boolean type $\mathrm{bool}$ models $\lbrace \mathrm{true},\mathrm{false}\rbrace$, or more compactly $\lbrace 0,1\rbrace$. This document replaces $\mathrm{bool}$ with a type $B_{\mathrm{bool}}$, a <em>noisy</em> Boolean. More generally, we can have a Bernoulli type for any type $T$, denoted $B_T$.</p>
<blockquote>
<p>As a special case, Bloom filters can be thought of as a Bernoulli type for the set indicator function, $1_A : X \mapsto \lbrace 0,1\rbrace$. More on that later.</p>
</blockquote>
<p>There are two types with fewer values than $\mathrm{bool}$: the absurd type $\mathrm{void}$ (no values, cannot be constructed) and the unit type $()$ (one value, also denoted $()$). Since $()$ has only one value, there is no uncertainty to model.</p>
<p>As degenerate cases: $B_{\mathrm{void}} \equiv \mathrm{void}$ and $B_{()} \equiv ()$.</p>
<p>The Boolean type has two values, so it is the first type where uncertainty can be introduced.</p>
<p>Every Bernoulli Model has an <em>order</em>, an integer greater than 1, describing the number of independent ways the generating process can produce errors. We write $B_T^{(k)}$ for a Bernoulli Model of type $T$ with order $k$. For $\mathrm{void}$ and $()$, the maximum order is 0. In general $T \equiv B_T^{(0)}$: if there are no ways to introduce errors, you just have the type itself.</p>
<p>Unless it matters, I drop the order and write $B_T$. The notation $B_T(x)$ denotes the Bernoulli approximation of latent value $x$. When $x$ is unobservable, $B_T(x)$ is a noisy measurement of it.</p>
<p>In the Bernoulli Boolean Model, $B_{\mathrm{bool}}(x)$ is a Bernoulli random variable with
</p>
$$
\Pr\lbrace  B_{\mathrm{bool}}(x) \neq x\rbrace = \varepsilon(x)
$$<p>
for each $x \in \lbrace 0,1\rbrace$, where $0 &lt; \varepsilon(x) &lt; 1$ is the error probability. In practice, $\varepsilon(x)$ is known or its expectation is known, and it can be set to balance space complexity against accuracy.</p>
<h2 id="binary-channels">Binary Channels</h2>
<p>The Bernoulli Boolean model maps onto two channel models:</p>
<ol>
<li>
<p><strong>Binary Symmetric Channel (First-order, $B_{\mathrm{bool}}^{(1)}$)</strong>: The error probability is the same for $1$ and $0$.</p>
</li>
<li>
<p><strong>Binary Asymmetric Channel (Second-order, $B_{\mathrm{bool}}^{(2)}$)</strong>: The error probability differs for $1$ and $0$.</p>
</li>
</ol>
<h2 id="false-positives-and-negatives">False Positives and Negatives</h2>
<p>Errors decompose into:</p>
<ol>
<li>$B_{\mathrm{bool}}(0) = 1$ is a <em>false negative</em>.</li>
<li>$B_{\mathrm{bool}}(1) = 0$ is a <em>false positive</em>.</li>
</ol>
<p>In the first-order model, these probabilities are equal. In the second-order model, they differ. A common special case of the second-order model: false negatives occur with probability 0 and false positives with probability $0 &lt; \varepsilon &lt; 1$. This is exactly the Bloom filter error model.</p>
<h2 id="prediction">Prediction</h2>
<p>$B_T(x)$ is correlated with the latent value $x$ and so provides information about it. Given $B_T(x)$, you can predict $x$ better than guessing, assuming the error rate is better than chance (for $B_{\mathrm{bool}}$, that means $\varepsilon(x) &lt; 0.5$). With no prior information, the maximum likelihood estimate of $x$ is just the observation $B_T(x)$.</p>
<p>Since $\varepsilon(x)$ is normally known, we can use Bayes&rsquo; rule:
</p>
$$
\Pr\lbrace X = x \mid B_T(x) = x \rbrace \propto \Pr\lbrace B_T(x) = x \mid X = x\rbrace \Pr\lbrace X = x\rbrace,
$$<p>
where $\Pr\lbrace X = x\rbrace$ is the prior and $\Pr\lbrace B_T(x) = x \mid X = x\rbrace$ is the probability the observation is correct. For $X = 1$ this is the true positive rate $\tau$, for $X = 0$ the true negative rate $\nu$.</p>
<p>In the first-order model with accuracy $\tau$:
</p>
$$
\Pr\lbrace x \mid B_{\mathrm{bool}}^{(1)}(x)\rbrace = \frac{\tau \Pr\lbrace X = x\rbrace}
    {\tau \Pr\lbrace X = x\rbrace + (1-\tau) (1-\Pr\lbrace X = x\rbrace)}.
$$<p>Dividing numerator and denominator by $\Pr\lbrace X = x\rbrace$:
</p>
$$
\Pr\lbrace x \mid B_{\mathrm{bool}}^{(1)}(x)\rbrace = \frac{\tau}{\tau + (1-\tau) (1/\Pr\lbrace X = x\rbrace - 1)}.
$$<p>Under maximum entropy ($\Pr\lbrace X = 1\rbrace = 0.5$), this simplifies to $\Pr\lbrace x \mid B_{\mathrm{bool}}^{(1)}(x)\rbrace = \tau$.</p>
<p>Given $n$ i.i.d. noisy measurements,
</p>
$$
\Bigl\lbrace  B_{1,\mathrm{bool}}^{(1)}(x), \ldots, B_{n,\mathrm{bool}}^{(1)}(x) \Bigr\rbrace,
$$<p>
the maximum likelihood estimate is the majority vote. As $n \to \infty$, the majority vote converges in probability to $x$.</p>
<p>This is not typically how Bernoulli Booleans arise in practice. Usually they show up as analytical results of probabilistic data structures framed in the Bernoulli Model.</p>
<h2 id="inducing-bernoulli-types">Inducing Bernoulli Types</h2>
<p>Now we generalize.</p>
<h3 id="unit-functions">Unit Functions</h3>
<p>The Bernoulli Model for value types like $\mathrm{bool}$ can be thought of as Bernoulli approximations of unit functions $() \mapsto X$. A unit function takes no input and maps to a constant. There are $|X|$ such functions.</p>
<p>Replacing $() \mapsto X$ with $B_X$ (equivalently $B_{() \mapsto X}$) allows for errors. The confusion matrix for $B_{() \mapsto \mathrm{bool}}^{(2)}$ is:</p>
<p>Table 1: Second-order Bernoulli Boolean Model for $() \mapsto \mathrm{bool}$</p>
<table>
  <thead>
      <tr>
          <th>$x / B_{\mathrm{bool}}^{(2)}$</th>
          <th>observe $1$</th>
          <th>observe $0$</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>latent</strong> $1$</td>
          <td>$\tau = 1-\eta$</td>
          <td>$\eta$</td>
      </tr>
      <tr>
          <td><strong>latent</strong> $0$</td>
          <td>$\varepsilon$</td>
          <td>$\nu = 1-\varepsilon$</td>
      </tr>
  </tbody>
</table>
<p>Observing $B_{() \mapsto \mathrm{bool}}(x) = 1$: the latent value is $1$ with probability $\tau$ (true positive) and $0$ with probability $\varepsilon$ (false positive). Observing $0$: latent is $1$ with probability $\eta$ (false negative) and $0$ with probability $\nu$ (true negative).</p>
<p>The maximum order is 2, since $\eta$ and $\varepsilon$ are independent and fully describe the model. Each row sums to 1 (total probability), so no additional free parameters are possible.</p>
<p>This is just the asymmetric binary channel, with different error rates for $1$ and $0$.</p>
<p>The first-order model sets $\epsilon = \varepsilon = \eta$:</p>
<p>Table 2: First-Order Bernoulli Boolean Model for $() \mapsto \mathrm{bool}$</p>
<table>
  <thead>
      <tr>
          <th>$x / B_{\mathrm{bool}}^{(1)}$</th>
          <th>observe $1$</th>
          <th>observe $0$</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>latent</strong> $1$</td>
          <td>$\tau =1-\epsilon$</td>
          <td>$\eta = \epsilon$</td>
      </tr>
      <tr>
          <td><strong>latent</strong> $0$</td>
          <td>$\varepsilon = \epsilon$</td>
          <td>$\nu = 1-\epsilon$</td>
      </tr>
  </tbody>
</table>
<p>One free parameter: the symmetric binary channel.</p>
<p>The zeroth-order model is deterministic:</p>
<p>Table 3: Zeroth-Order Bernoulli Boolean Model for $() \mapsto \mathrm{bool}$</p>
<table>
  <thead>
      <tr>
          <th>$x / B_{\mathrm{bool}}^{(1)}$</th>
          <th>observe $1$</th>
          <th>observe $0$</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>latent</strong> $1$</td>
          <td>1</td>
          <td>0</td>
      </tr>
      <tr>
          <td><strong>latent</strong> $0$</td>
          <td>0</td>
          <td>1</td>
      </tr>
  </tbody>
</table>
<p>Zero free parameters. No uncertainty.</p>
<h4 id="prediction-boolean-values">Prediction: Boolean Values</h4>
<p>Applying Bayes&rsquo; rule to the first-order model $B_{\mathrm{bool}}^{(1)}$, we compute $\Pr\lbrace X = 1 | B_{\rm{bool}}^{(1)} = 1\rbrace$:
</p>
$$
\Pr\lbrace X = 1 | B_{\rm{bool}}^{(1)} = 1\rbrace =
    \frac{
        \Pr\lbrace B_{\rm{bool}}^{(1)} =1 | X=1\rbrace \Pr\lbrace X = 1\rbrace
    }
    {
        \Pr\lbrace B_{\rm{bool}}^{(1)} = 1\rbrace
    }
$$<p>
From the confusion matrix, $\Pr\lbrace B_{\rm{bool}}^{(1)}=1 | X = 1\rbrace = \tau = 1 - \epsilon$:
</p>
$$
\Pr\lbrace X=1 | B_{\rm{bool}}^{(1)}=1\rbrace = \frac
    {(1-\epsilon) \Pr\lbrace X = 1\rbrace}
    {\Pr\lbrace B_{\rm{bool}}^{(1)}=1\rbrace}
$$<p>
By the total probability theorem:
</p>
$$
\begin{split}
\Pr\lbrace B_{\rm{bool}}^{(1)} = 1\rbrace = \Pr\lbrace
    B_{\rm{bool}}^{(1)}=1 | X=1\rbrace \Pr\lbrace X = 1\rbrace + \\
    \Pr\lbrace B_{\rm{bool}}^{(1)}=1 | X=0\rbrace \Pr\lbrace X = 0\rbrace.
\end{split}
$$<p>
Substituting confusion matrix values:
</p>
$$
\Pr\lbrace B_{\rm{bool}}^{(1)} = 1\rbrace = (1-\epsilon) \Pr\lbrace X = 1\rbrace + \epsilon \Pr\lbrace X = 0\rbrace.
$$<p>Substituting back and simplifying (dividing numerator and denominator by $\Pr\lbrace X = 1\rbrace$):
</p>
$$
\Pr\lbrace X=1 | B_{\rm{bool}}^{(1)}=1\rbrace = \frac
    {1-\epsilon}
    {1-\epsilon (1 - q/(1-q))}
$$<p>
where $q = \Pr\lbrace X = 0\rbrace$.</p>
<p>Evaluating at interesting points:</p>
<ol>
<li>At $q = 0$: result is $1$. We know the latent value is $1$ with certainty (prior says so), so the observation does not matter.</li>
<li>As $q \to 1$: result goes to $0$. We know the latent value is $0$ with certainty, so again the observation does not matter.</li>
<li>At $q = 0.5$: result is $1-\epsilon$. Maximum entropy case, maximum ignorance about the latent value.</li>
</ol>
<h2 id="unary-bernoulli-functions">Unary Bernoulli Functions</h2>
<p>Now we expand to unary functions.</p>
<h3 id="lifting-unary-functions">Lifting Unary Functions</h3>
<p>The space of all functions $f : \rm{bool} \mapsto \rm{bool}$ is:</p>
<p>Table 4: All possible functions of type $\rm{bool} \mapsto \rm{bool}$</p>
<table>
  <thead>
      <tr>
          <th>$f$</th>
          <th>$f(\rm{true})$</th>
          <th>$f(\rm{false})$</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>$\rm{id}$</td>
          <td>$\rm{true}$</td>
          <td>$\rm{false}$</td>
      </tr>
      <tr>
          <td>$\rm{not}$</td>
          <td>$\rm{false}$</td>
          <td>$\rm{true}$</td>
      </tr>
      <tr>
          <td>$\rm{true}$</td>
          <td>$\rm{true}$</td>
          <td>$\rm{true}$</td>
      </tr>
      <tr>
          <td>$\rm{false}$</td>
          <td>$\rm{false}$</td>
          <td>$\rm{false}$</td>
      </tr>
  </tbody>
</table>
<p>What happens when we replace Boolean inputs with Bernoulli Boolean values? That is: what is $\Pr\lbrace f\bigl(B_{\rm{bool}}^{(1)}(x)\bigr) = f(x)\rbrace$?</p>
<p>The constant functions ($\mathrm{true}$ and $\mathrm{false}$) are trivial. They always produce the same output regardless of input, so Bernoulli noise in the input does not matter: $\mathrm{true} : B_{\rm{bool}}^{(k)} \mapsto B_{\rm{bool}}^{(0)}$.</p>
<p>The interesting cases are $\mathrm{id}$ and $\mathrm{not}$. If $\Pr\lbrace B_{\rm{bool}}^{(1)}(x) = x\rbrace = p$, then feeding $B_{\rm{bool}}^{(1)}(\mathrm{true})$ into $\mathrm{id}$ gives the correct output with probability $p$ and the wrong output with probability $1-p$. Same for $\mathrm{false}$ inputs.</p>
<p>Since the output is correct with probability $p$, it is itself a Bernoulli Boolean. We have monadically lifted $\mathrm{id} : \mathrm{bool} \mapsto \mathrm{bool}$ to $B_{\mathrm{bool}}^{(1)} \mapsto B_{\mathrm{bool}}^{(1)}$.</p>
<p>Note that this is not a Bernoulli Model of the function $B_{\mathrm{bool} \mapsto \mathrm{bool}}$. It is a function of type $B_{\mathrm{bool}} \mapsto B_{\mathrm{bool}}$. The distinction matters.</p>
<p>Consider $\mathrm{true} : \mathrm{bool} \mapsto \mathrm{bool}$. Feeding it a Bernoulli Boolean gives a known-correct output (no latent values). But a Bernoulli <em>approximation</em> of the function $\mathrm{true}$ is a different concept entirely. The type is $B_{\mathrm{bool} \mapsto \mathrm{bool}}$, and the latent function is unobservable. The approximation $B_{\mathrm{bool} \mapsto \mathrm{bool}}(\mathrm{true})$ is observable and provides information about the latent function.</p>
<p>There are 4 functions of type $\mathrm{bool} \mapsto \mathrm{bool}$. The full confusion matrix for the highest-order model, $B_{\mathrm{bool} \mapsto \mathrm{bool}}^{(12)}$:</p>
<p>Table 5: Bernoulli Model for $\mathrm{bool} \mapsto \mathrm{bool}$</p>
<table>
  <thead>
      <tr>
          <th></th>
          <th>observe $\mathrm{id}$</th>
          <th>observe $\mathrm{not}$</th>
          <th>observe $\mathrm{true}$</th>
          <th>observe $\mathrm{false}$</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>latent $\mathrm{id}$</td>
          <td>$p_{1 1}$</td>
          <td>$p_{1 2}$</td>
          <td>$p_{1 3}$</td>
          <td>$p_{1 4}$</td>
      </tr>
      <tr>
          <td>latent $\mathrm{not}$</td>
          <td>$p_{2 1}$</td>
          <td>$p_{2 2}$</td>
          <td>$p_{2 3}$</td>
          <td>$p_{2 4}$</td>
      </tr>
      <tr>
          <td>latent $\mathrm{true}$</td>
          <td>$p_{3 1}$</td>
          <td>$p_{3 2}$</td>
          <td>$p_{3 3}$</td>
          <td>$p_{3 4}$</td>
      </tr>
      <tr>
          <td>latent $\mathrm{false}$</td>
          <td>$p_{4 1}$</td>
          <td>$p_{4 2}$</td>
          <td>$p_{4 3}$</td>
          <td>$p_{4 4}$</td>
      </tr>
  </tbody>
</table>
<p>Each row sums to 1, so we have at most $4(4-1) = 12$ degrees of freedom. That is the maximum order. In practice I drop the order and write $B_{\mathrm{bool} \mapsto \mathrm{bool}}$ (propagating error rates with interval arithmetic).</p>
<p>The first-order model, with maximum entropy given error rate $\epsilon$:</p>
<p>Table 6: First-order Bernoulli Model for $\mathrm{bool} \mapsto \mathrm{bool}$</p>
<table>
  <thead>
      <tr>
          <th></th>
          <th>observe $\mathrm{id}$</th>
          <th>observe $\mathrm{not}$</th>
          <th>observe $\mathrm{true}$</th>
          <th>observe $\mathrm{false}$</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td><strong>latent</strong> $\mathrm{id}$</td>
          <td>$1-\epsilon$</td>
          <td>$\epsilon/3$</td>
          <td>$\epsilon/3$</td>
          <td>$\epsilon/3$</td>
      </tr>
      <tr>
          <td><strong>latent</strong> $\mathrm{not}$</td>
          <td>$\epsilon/3$</td>
          <td>$1-\epsilon$</td>
          <td>$\epsilon/3$</td>
          <td>$\epsilon/3$</td>
      </tr>
      <tr>
          <td><strong>latent</strong> $\mathrm{true}$</td>
          <td>$\epsilon/3$</td>
          <td>$\epsilon/3$</td>
          <td>$1-\epsilon$</td>
          <td>$\epsilon/3$</td>
      </tr>
      <tr>
          <td><strong>latent</strong> $\mathrm{false}$</td>
          <td>$\epsilon/3$</td>
          <td>$\epsilon/3$</td>
          <td>$\epsilon/3$</td>
          <td>$1-\epsilon$</td>
      </tr>
  </tbody>
</table>
<p>When we have a Bernoulli approximation of some latent function, we want to store error information in the output so it propagates through computation. The output becomes a Bernoulli Boolean: the Bernoulli Model generates a function of type $\mathrm{bool} \mapsto B_{\mathrm{bool}}$. In our implementation, we built a type system using interval arithmetic to propagate error rates.</p>
<p>The Bernoulli Model on $\mathrm{bool} \mapsto \mathrm{bool}$ does not change the input type. You can also feed Bernoulli Booleans as input, which usually produces even higher-order Bernoulli Booleans as output.</p>
<p>Since functions are values, we can ask: what is the probability the observed function equals the latent function?
</p>
$$
\Pr\lbrace  B_{\mathrm{bool} \mapsto \mathrm{bool}}(f) = f\rbrace.
$$<p>Function equality means each input maps to the same output:
</p>
$$
\begin{split}
\Pr\lbrace B_{\mathrm{bool}\mapsto \mathrm{bool}}^{(1)}(\mathrm{id})(\mathrm{true}) = \mathrm{id}(\mathrm{true}) \rbrace \times \\ \Pr\lbrace B_{\mathrm{bool}\mapsto \mathrm{bool}}^{(1)}(\mathrm{id})(\mathrm{false}) = \mathrm{id}(\mathrm{false}) \rbrace
\end{split}
$$<p>From the confusion matrix, this product is $1-\epsilon$. Normally, the process generating the Bernoulli Model is defined in terms of these per-input probabilities.</p>
<h3 id="higher-order-bernoulli-models">Higher-Order Bernoulli Models</h3>
<p>Higher order means more parameters to estimate, but also more capacity to approximate the latent function. In the confusion matrix, we want the diagonal close to 1. For off-diagonal elements, functions more similar to the latent should have larger probabilities than dissimilar ones. This is just loss minimization.</p>
<h2 id="set-indicator-functions">Set-Indicator Functions</h2>
<p>A set-indicator function maps elements to Booleans: $X \mapsto \mathrm{bool}$, returning true if the input is in the set.</p>
<p>The Bloom filter is a second-order Bernoulli model of the set-indicator function. False negatives are impossible (probability 0), false positives occur with probability $\varepsilon$. Technically $\varepsilon$ is the <em>expected</em> false positive rate; the true rate is a random variable that usually cannot be computed exactly unless $X$ is finite.</p>
<h3 id="hashset-approximate-set-indicator-functions">HashSet: Approximate Set-Indicator Functions</h3>
<p>Suppose we have a cryptographic hash function $h : X \mapsto \lbrace 0,1\rbrace^n$. We define a $\mathrm{HashSet}$:</p>
<ul>
<li>Given a set $A$ to approximately represent.</li>
<li>Find a seed $s$ such that $h(x s) = 0^n$ for all $x \in A$.</li>
<li>For $x \notin A$, by the properties of $h$, $h(x s) = 0^n$ with probability $2^{-n}$ (false positive).</li>
<li>Define membership as $x \in A \equiv h(x s) = 0^n$.</li>
</ul>
<p>Finding the seed requires that all $x \in A$ hash to $0^n$. Each trial succeeds with probability $\prod_{j=1}^{|A|} 2^{-n} = 2^{-n|A|}$, so we need an expected $2^{n|A|}$ trials. We store just the seed: $n|A|$ bits total, or $n$ bits per element. Since $\varepsilon = 2^{-n}$, the space is $-\log_2 \varepsilon$ bits per element.</p>
<p>This achieves the information-theoretic lower bound, but at exponential time complexity. Better algorithms exist at a cost to space.</p>
<h3 id="bernoulli-model-for-set-indicator-functions">Bernoulli Model for Set-Indicator Functions</h3>
<p>The number of functions $X \mapsto \mathrm{bool}$ is $2^{|X|}$. These are all possible set-indicator functions, and we can approximate them with $B_{X \mapsto \mathrm{bool}}$.</p>
<p>The $\mathrm{HashSet}$ construction induces this Bernoulli Model. False positives at rate $\epsilon$, false negatives at rate $0$. The Bernoulli Model on the function also specifies a Bernoulli Model on the Boolean output:
</p>
$$
    \in : X \times \mathcal{P}(X) \mapsto B_{\mathrm{bool}}.
$$<p>Consider $X = \lbrace 1,2\rbrace$ and $A = \lbrace 2\rbrace$. The confusion matrix:</p>
<p>Table 7: Bernoulli Model for $X \mapsto \mathrm{bool}$</p>
<table>
  <thead>
      <tr>
          <th></th>
          <th>$1_\emptyset$</th>
          <th>$1_{\lbrace 1\rbrace}$</th>
          <th>$1_{\lbrace 2\rbrace}$</th>
          <th>$1_{\lbrace 1,2\rbrace}$</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>$1_\emptyset$</td>
          <td>$(1-\epsilon)^2$</td>
          <td>$\epsilon(1-\epsilon)$</td>
          <td>$\epsilon(1-\epsilon)$</td>
          <td>$\epsilon^2$</td>
      </tr>
      <tr>
          <td>$1_{\lbrace 1\rbrace}$</td>
          <td>$0$</td>
          <td>$1-\epsilon$</td>
          <td>$0$</td>
          <td>$\epsilon$</td>
      </tr>
      <tr>
          <td>$1_{\lbrace 2\rbrace}$</td>
          <td>$0$</td>
          <td>$0$</td>
          <td>$1-\epsilon$</td>
          <td>$\epsilon$</td>
      </tr>
      <tr>
          <td>$1_{\lbrace 1,2\rbrace}$</td>
          <td>$0$</td>
          <td>$0$</td>
          <td>$0$</td>
          <td>$1$</td>
      </tr>
  </tbody>
</table>
<p>We do not know the latent set $A = \lbrace 2\rbrace$. We only have the approximation $B_{X \mapsto \mathrm{bool}}(A)$, and we use it as a stand-in for $A$.</p>
<p>In row 3 (latent $\lbrace 2\rbrace$), the first two columns are zero. This makes sense: no false negatives, only false positives.</p>
<p>With probability $1-\epsilon$ the output is correct, with probability $\epsilon$ it is wrong. This is a Bernoulli Set, and we can use it to provide information about the latent set.</p>
<p>On equality: $\Pr\lbrace B_{X \mapsto \mathrm{bool}}(A) = A\rbrace = 1-\epsilon$. But typically people care about point queries: what is the probability that a positive test result is correct?
</p>
$$
\Pr\lbrace x \in B_{X \mapsto \mathrm{bool}}(A)\rbrace.
$$<p>The true positive rate is 1 (by construction), the false positive rate is $\epsilon$. Membership tests yield Bernoulli Boolean values:
</p>
$$
    x \in B_{X \mapsto \mathrm{bool}}(A) : X \mapsto \mathrm{bool}^{(2)}.
$$<h2 id="conclusion">Conclusion</h2>
<p>The Bernoulli Model is a way to think about uncertainty in function outputs and how that uncertainty propagates through computation. The uncertainty typically arises from trading space for accuracy. More space means a closer approximation to the latent function.</p>
]]></content:encoded><category>probabilistic-data-structures</category><category>bloom-filter</category><category>probability</category><category>type-theory</category></item><item><title>algebraic.dist: Distributions as Algebraic Objects in R</title><link>https://metafunctor.com/post/2024-05-01-algebraic.dist/</link><pubDate>Mon, 01 Feb 2021 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-05-01-algebraic.dist/</guid><description>An R package that treats probability distributions as algebraic objects. They compose through standard operations. The algebra preserves distributional structure.</description><content:encoded><![CDATA[<p>Most statistical software treats probability distributions as parameter sets you pass to sampling or density functions. <code>algebraic.dist</code> takes a different approach. Distributions are algebraic objects that compose, transform, and combine through standard mathematical operations.</p>
<h2 id="the-idea">The Idea</h2>
<p>Instead of this:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="n">x</span> <span class="o">&lt;-</span> <span class="nf">rnorm</span><span class="p">(</span><span class="m">1000</span><span class="p">,</span> <span class="n">mean</span><span class="o">=</span><span class="m">5</span><span class="p">,</span> <span class="n">sd</span><span class="o">=</span><span class="m">2</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">y</span> <span class="o">&lt;-</span> <span class="nf">rnorm</span><span class="p">(</span><span class="m">1000</span><span class="p">,</span> <span class="n">mean</span><span class="o">=</span><span class="m">3</span><span class="p">,</span> <span class="n">sd</span><span class="o">=</span><span class="m">1</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">z</span> <span class="o">&lt;-</span> <span class="n">x</span> <span class="o">+</span> <span class="n">y</span>  <span class="c1"># Just numeric vectors</span>
</span></span></code></pre></div><p>You write:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="n">X</span> <span class="o">&lt;-</span> <span class="nf">Normal</span><span class="p">(</span><span class="n">mean</span><span class="o">=</span><span class="m">5</span><span class="p">,</span> <span class="n">sd</span><span class="o">=</span><span class="m">2</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">Y</span> <span class="o">&lt;-</span> <span class="nf">Normal</span><span class="p">(</span><span class="n">mean</span><span class="o">=</span><span class="m">3</span><span class="p">,</span> <span class="n">sd</span><span class="o">=</span><span class="m">1</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">Z</span> <span class="o">&lt;-</span> <span class="n">X</span> <span class="o">+</span> <span class="n">Y</span>  <span class="c1"># A new distribution object!</span>
</span></span><span class="line"><span class="cl"><span class="nf">sample</span><span class="p">(</span><span class="n">Z</span><span class="p">,</span> <span class="m">1000</span><span class="p">)</span>
</span></span></code></pre></div><p>The sum <code>Z</code> knows it is <code>Normal(mean=8, sd=sqrt(5))</code> because the algebra works it out. You never lost the distributional structure.</p>
<h2 id="why-it-matters">Why It Matters</h2>
<p>When you add two normal distributions numerically, you get samples from the sum. But you lose the distribution. With <code>algebraic.dist</code>, the result is still a distribution object with proper parameters and you can keep composing.</p>
<p>You can build complex distributional expressions and simplify them algebraically before ever drawing a sample:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-r" data-lang="r"><span class="line"><span class="cl"><span class="n">portfolio</span> <span class="o">&lt;-</span> <span class="m">0.6</span><span class="o">*</span><span class="n">StockA</span> <span class="o">+</span> <span class="m">0.4</span><span class="o">*</span><span class="n">StockB</span>
</span></span><span class="line"><span class="cl"><span class="n">risk</span> <span class="o">&lt;-</span> <span class="nf">sd</span><span class="p">(</span><span class="n">portfolio</span><span class="p">)</span>  <span class="c1"># Computed symbolically</span>
</span></span></code></pre></div><p>For distributions with known closed-form algebra (normal, exponential, certain mixtures), you do not need simulation. You just compute the exact answer. Monte Carlo without the Monte Carlo.</p>
<h2 id="composition">Composition</h2>
<p>This is functional programming applied to probability theory. Distributions become composable building blocks:</p>
<ul>
<li><strong>Mixture models</strong>: <code>0.3*Normal(0,1) + 0.7*Normal(5,2)</code></li>
<li><strong>Transformed distributions</strong>: <code>exp(Normal(0,1))</code> is lognormal</li>
<li><strong>Conditional distributions</strong>: <code>X | (X &gt; 0)</code> for truncation</li>
</ul>
<p>The idea is that computation should mirror mathematical structure. If the math says you can add two normals and get a normal, the code should do the same thing and give you a normal back, not a vector of samples.</p>
<p>This connects to a broader theme in my work. Just as my oblivious computing research uses type theory to enforce privacy invariants, <code>algebraic.dist</code> uses algebraic types to enforce distributional invariants. The algebra tells you what operations are valid and what the results mean.</p>
<h2 id="implementation">Implementation</h2>
<ul>
<li><strong>Language</strong>: R</li>
<li><strong>Type system</strong>: S3 classes with method dispatch for operations</li>
<li><strong>Closed-form operations</strong>: Normal, exponential, gamma families</li>
<li><strong>Fallback</strong>: Monte Carlo for compositions without closed forms</li>
<li><strong>Repository</strong>: <a href="https://github.com/queelius/algebraic.dist">github.com/queelius/algebraic.dist</a></li>
</ul>
<h2 id="related-packages">Related Packages</h2>
<ul>
<li><strong>algebraic.mle</strong>: Maximum likelihood estimation with algebraic specification</li>
<li><strong>numerical.mle</strong>: Numerical optimization for MLE when closed forms do not exist</li>
<li><strong>likelihood.model</strong>: Likelihood-based inference with compositional model building</li>
</ul>
<p>Most statistical software is imperative. You tell it what to do step by step. <code>algebraic.dist</code> is declarative. You describe the distributional relationships and the computer figures out what to compute. Small composable pieces that do one thing well: preserve distributional structure through transformations.</p>
]]></content:encoded><category>statistics</category><category>probability</category><category>distributions</category><category>R</category><category>functional programming</category><category>algebra</category><category>sicp</category></item><item><title>Algebra over distribution (random elements) objects</title><link>https://metafunctor.com/papers/algebraic.dist/</link><pubDate>Mon, 01 Jun 2020 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/papers/algebraic.dist/</guid><description> Open full page Download PDF</description><content:encoded><![CDATA[<style>
.iframe-wrap {
  width: 100%;
  max-width: 100%;
  margin: 0;
  padding: 0;
}
.iframe-wrap iframe {
  display: block;
  width: 100%;
  border: 0;
  min-height: 120vh;
}
.action-bar {
  margin: 0 0 1rem 0;
  padding: 0 1rem;
  display: flex;
  gap: .75rem;
  flex-wrap: wrap;
}
.action-bar a {
  text-decoration: none;
  padding: .5rem .75rem;
  border: 1px solid var(--color-border-default, #ddd);
  border-radius: .4rem;
  display: inline-block;
  color: var(--color-accent-primary, #007acc);
}
.action-bar a:hover {
  background: var(--color-bg-secondary, #f5f5f5);
}
</style>
<div class="action-bar">
  <a href="/latex/algebraic.dist/index.html" target="_blank" rel="noopener">Open full page</a>
  <a href="/latex/algebraic.dist/" target="_blank" rel="noopener">Download PDF</a>
</div>
<div class="iframe-wrap">
  <iframe id="ltx" src="/latex/algebraic.dist/index.html" loading="lazy" scrolling="no"></iframe>
</div>
<script>
(function () {
  const iframe = document.getElementById('ltx');

  if (location.hash) {
    const u = new URL(iframe.src, location.origin);
    u.hash = location.hash;
    iframe.src = u.toString();
  }

  let lastScrollHeight = 0;

  function setHeight(h) {
    const newH = Math.ceil(h);
    if (newH > lastScrollHeight + 5) {
      iframe.style.height = newH + 'px';
      lastScrollHeight = newH;
    }
  }

  function measure(doc) {
    const h = Math.max(
      doc.body?.scrollHeight || 0,
      doc.documentElement?.scrollHeight || 0
    );
    if (h > 0) setHeight(h);
  }

  iframe.addEventListener('load', () => {
    const doc = iframe.contentDocument || iframe.contentWindow.document;
    if (!doc) return;

    setTimeout(() => measure(doc), 100);
    setTimeout(() => measure(doc), 500);

    doc.fonts?.ready?.then(() => {
      setTimeout(() => measure(doc), 50);
    }).catch(() => {});

    const images = Array.from(doc.images || []);
    if (images.length > 0) {
      let loadedCount = 0;
      images.forEach(img => {
        if (img.complete) {
          loadedCount++;
        } else {
          img.addEventListener('load', () => {
            loadedCount++;
            if (loadedCount === images.length) {
              setTimeout(() => measure(doc), 50);
            }
          }, { once: true });
        }
      });
    }

    let resizeTimeout;
    window.addEventListener('resize', () => {
      clearTimeout(resizeTimeout);
      resizeTimeout = setTimeout(() => {
        lastScrollHeight = 0;
        measure(doc);
      }, 300);
    });
  });
})();
</script>
]]></content:encoded><category>statistics</category><category>probability</category><category>distributions</category><category>R</category></item></channel></rss>