<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:media="http://search.yahoo.com/mrss/"><channel><title>Information-Theory on metafunctor</title><link>https://metafunctor.com/tags/information-theory/</link><description>Research Engineer &amp; Computer Scientist - Machine Learning, Statistical Computing, Open Source Development</description><language>en-us</language><managingEditor>lex@metafunctor.com (Alex Towell)</managingEditor><webMaster>lex@metafunctor.com (Alex Towell)</webMaster><lastBuildDate>Thu, 09 Jul 2026 15:26:07 -0500</lastBuildDate><atom:link href="https://metafunctor.com/tags/information-theory/" rel="self" type="application/rss+xml"/><image><url>https://metafunctor.com/images/og-image.jpg</url><title>metafunctor</title><link>https://metafunctor.com/</link></image><item><title>The Bernoulli Model</title><link>https://metafunctor.com/research/bernoulli/</link><pubDate>Mon, 15 Jun 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/research/bernoulli/</guid><description>Random approximate computation: the Bernoulli model, its papers, and the monograph that unifies them. Bloom filters are the special case; this is the general theory.</description><content:encoded><![CDATA[<p><strong>The one-sentence version.</strong> A Bernoulli set is a set you can store far below
the information-theoretic cost of the exact set, in exchange for a known,
tunable rate of false positives and false negatives. The Bernoulli model is the
framework that makes that trade precise and then carries it everywhere: from
sets to maps, relations, and arbitrary algebraic types, with closed-form error
propagation through every operation.</p>
<p>A Bloom filter is the special case everyone already knows: false positives, no
false negatives, one of the two error rates pinned to zero. The model
generalizes it. Both error rates are first-class and tunable, the construction
is provably space-optimal at the information bound, and the same algebra that
governs a single membership test governs unions, intersections, joins, function
composition, and the Boolean post-processing of noisy measurements.</p>
<h2 id="the-monograph-the-center-of-gravity">The monograph: the center of gravity</h2>
<p>Everything below is being drawn together into one book.</p>
<p><strong>Bernoulli Types: An Algebra of Approximate Computation</strong> builds the model from
a single atom, the Bernoulli Boolean, and composes outward: random approximate
sets, their entropy and space-optimal construction, maps, relations, and a full
algebra of approximate data types. Each paper below is roughly a chapter&rsquo;s worth
of the argument. The monograph is where they become one coherent theory instead
of a stack of separate preprints, and it is the thing I most want read. It is in
active development.</p>
<p><strong>Read it: <a href="/latex/bernoulli-monograph/book.html">online in the browser</a> or as a
<a href="/latex/bernoulli-monograph/bernoulli-monograph.pdf">PDF</a></strong> (276 pages).</p>
<h2 id="the-papers">The papers</h2>
<p>A family of preprints, each a self-contained result the monograph consolidates.
All carry minted Zenodo DOIs.</p>
<h3 id="foundations">Foundations</h3>
<table>
	<thead>
			<tr>
					<th>Paper</th>
					<th>What it establishes</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><a href="https://doi.org/10.5281/zenodo.19105381"><strong>Bernoulli Sets: A Model for Random Approximate Sets</strong></a></td>
					<td>The model itself: the two axioms (element-wise independence and conditional block independence), the false-positive / false-negative parameterization, the error-count distributions, and higher-order composition. The hub the rest build on.</td>
			</tr>
			<tr>
					<td><a href="https://doi.org/10.5281/zenodo.19105387"><strong>Composition of Bernoulli Sets</strong></a></td>
					<td>Closed-form error propagation through union, intersection, complement, and difference, the monoid structure, and the relational subset and equality predicates.</td>
			</tr>
			<tr>
					<td><a href="https://doi.org/10.5281/zenodo.19104549"><strong>Binary Classification Measures for Random Approximate Sets</strong></a></td>
					<td>PPV, NPV, accuracy, and Youden&rsquo;s J as random variables, with prevalence sensitivity by the delta method.</td>
			</tr>
			<tr>
					<td><a href="https://doi.org/10.5281/zenodo.20602259"><strong>Entropy and Space Complexity of Random Approximate Sets</strong></a></td>
					<td>The information-theoretic lower bound on bits per element and the space-accuracy duality that every optimal construction must meet.</td>
			</tr>
			<tr>
					<td><a href="https://doi.org/10.5281/zenodo.19105398"><strong>The Bernoulli Model: A Probabilistic Framework for Data Structures and Types</strong></a></td>
					<td>The type-theoretic generalization: Bool as the atom, approximate sum and product types, and the computational basis from which the rest is built.</td>
			</tr>
	</tbody>
</table>
<h3 id="optimal-constructions">Optimal constructions</h3>
<table>
	<thead>
			<tr>
					<th>Paper</th>
					<th>What it establishes</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><a href="https://doi.org/10.5281/zenodo.20602261"><strong>The Bernoulli Hash Function: Optimal Bernoulli Sets and Bernoulli Maps</strong></a></td>
					<td>The space-optimal construction that meets the entropy bound: the threshold predicate, generalized acceptance, and a unified set/map framework.</td>
			</tr>
			<tr>
					<td><a href="https://doi.org/10.5281/zenodo.20602263"><strong>The Perfect Hash Filter</strong></a></td>
					<td>A concrete, efficient implementation of the positive Bernoulli set and map via perfect hashing, with a ranked-search application.</td>
			</tr>
	</tbody>
</table>
<h3 id="extensions">Extensions</h3>
<table>
	<thead>
			<tr>
					<th>Paper</th>
					<th>What it adds</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><a href="https://doi.org/10.5281/zenodo.19105389"><strong>The Algebra of the Random Approximate Map Model</strong></a></td>
					<td>Functions from X to Y as first-class approximate objects, and the error rates of map composition.</td>
			</tr>
			<tr>
					<td><a href="https://doi.org/10.5281/zenodo.20602265"><strong>The Abstract Data Type of the Approximate Relation</strong></a></td>
					<td>Relational algebra (selection, projection, join) over approximate relations, and a closure dichotomy for structural invariants: a class-induced invariant is free, transitivity admits no bounded closure.</td>
			</tr>
	</tbody>
</table>
<h3 id="applications">Applications</h3>
<table>
	<thead>
			<tr>
					<th>Paper</th>
					<th>What it adds</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><a href="https://doi.org/10.5281/zenodo.20584894"><strong>A Forward (ε, ω) Calculus for Quantum Readout Error</strong></a></td>
					<td>The model as the error calculus for the classical post-processing of quantum measurement, validated on real device calibration. Under review at <em>Quantum Science and Technology</em>.</td>
			</tr>
	</tbody>
</table>
<h2 id="the-software">The software</h2>
<p><strong>bernoulli-types</strong> is the reference implementation in Python: the atom, the
sets, the maps, and the sketches, built outward in phases that mirror the
monograph&rsquo;s parts, with optional plain-C acceleration and pure-Python fallbacks.
It is both the research artifact and the book&rsquo;s executable companion.
<a href="https://github.com/queelius/bernoulli">Source</a>.</p>
<h2 id="notes-and-posts">Notes and posts</h2>
<p>Shorter writing on the model, tagged <code>bernoulli-model</code>:</p>
<ul class="tagged-posts" style="list-style:none;padding-left:0;">
  <li style="margin-bottom:0.7rem;">
    <a href="/post/trapdoor-boolean-model/" style="font-weight:600;">A Boolean Algebra Over Trapdoors</a><span style="color:#888;font-size:0.85rem;"> &middot; June 2023</span>
    <div style="color:#666;font-size:0.92rem;margin-top:0.15rem;">A Boolean algebra framework over trapdoors for cryptographic operations. Introduces a homomorphism from powerset Boolean algebra to n-bit strings via cryptographic hash functions, enabling secure …</div>
  </li>
</ul>
<h2 id="connection-to-trapdoor-computing">Connection to trapdoor computing</h2>
<p>The Bernoulli model is also the <em>error model</em> for
<a href="/research/trapdoor-computing/">Trapdoor Computing</a>. At the mathematical level a
Bernoulli map is a cipher map: the construction&rsquo;s salt is the trapdoor key, the
acceptance predicate implements the cipher map, and the space formula
\(-\log_2(\varepsilon) + \mu\) governs both. The composition theorem
\(\eta_{\text{total}} = 1 - \prod_i (1 - \eta_i)\) is the same law on both sides.
If you came here from the cryptography, that is the bridge.</p>
<hr>
<p>Alexander Towell, <a href="https://metafunctor.com">metafunctor.com</a>.</p>
]]></content:encoded><category>bernoulli model</category><category>random approximate sets</category><category>bloom filters</category><category>probabilistic data structures</category><category>perfect hashing</category><category>information theory</category><category>approximate computing</category></item><item><title>Trapdoor Computing</title><link>https://metafunctor.com/research/trapdoor-computing/</link><pubDate>Mon, 15 Jun 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/research/trapdoor-computing/</guid><description>Computing on opaque encodings: the cipher map framework, its papers, and the monograph that unifies them.</description><content:encoded><![CDATA[<p><strong>The one-sentence version.</strong> Imagine a service that cannot read your queries
and cannot read its own answers, yet still returns the right ones. Trapdoor
computing is the paradigm that makes that precise. A <em>trusted</em> machine holds
the encoder and decoder; an <em>untrusted</em> machine sees only total functions on
opaque bit strings flowing through opaque lookup tables. It cannot tell a real
query from filler, one encoding from another, or a correct result from noise.</p>
<p>The organizing object is the <strong>cipher map</strong>: a total function on bit strings
that implements a trapdoor approximation of a hidden function. Four properties
make it measurable rather than magical, totality, representation uniformity,
correctness, and composability, and confidentiality here is <em>measured</em> (in the
sense of quantitative information flow), not assumed <em>negligible</em> (in the sense
of a cryptographic reduction). The privacy comes from one-way hashing and
uniform representation, not from hiding access patterns: this is deliberately
not ORAM, not fully homomorphic encryption, not garbled circuits.</p>
<h2 id="the-monograph-the-center-of-gravity">The monograph: the center of gravity</h2>
<p>Everything below is being drawn together into one book.</p>
<p><strong>Trapdoor Computing: Computing on Opaque Encodings</strong> builds the cipher map
abstraction from first principles, develops its algebra and type theory,
characterizes its confidentiality at two scales, gives concrete constructions,
and closes with the open problems that define the next program. Six parts:</p>
<ol>
<li><strong>The Paradigm</strong>, the trusted/untrusted model and what trapdoor computing
is <em>not</em>.</li>
<li><strong>The Cipher Map Abstraction</strong>, the four properties and the parameter tuple.</li>
<li><strong>Algebra and Types</strong>, Boolean algebra over trapdoor values, algebraic
cipher types, orbit closure.</li>
<li><strong>Confidentiality</strong>, the marginal scale (a single map) and the
compositional scale (chains of maps).</li>
<li><strong>Constructions</strong>, hash-based, GF(2)-linear, rekeying, and closures.</li>
<li><strong>Frontiers</strong>, the open problems that motivate what comes next.</li>
</ol>
<p>The monograph is the synthesis. Each paper below is roughly a chapter&rsquo;s worth
of the argument; the book is where they become one coherent treatment instead
of a stack of separate results. It is in active development.</p>
<h2 id="the-papers">The papers</h2>
<p>The program is a family of papers, each a self-contained result the monograph
consolidates. Three already carry minted DOIs; the rest are under submission or
in preparation.</p>
<h3 id="core">Core</h3>
<table>
	<thead>
			<tr>
					<th>Paper</th>
					<th>What it establishes</th>
					<th>Status</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><a href="/papers/cipher-maps/"><strong>Cipher Maps: Total Functions as Trapdoor Approximations</strong></a></td>
					<td>The abstraction itself: the four properties, the parameter tuple, batch and online construction, and the space-accuracy duality. The hub the rest build on.</td>
					<td>Manuscript, targeting PoPETs</td>
			</tr>
			<tr>
					<td><a href="/papers/max-conf-in-encrypted-search/"><strong>The Entropy Ratio: Quantitative Confidentiality</strong></a></td>
					<td>The confidentiality theory. Two scales that do not reduce to each other; the headline is a sharp, matching-bounds rate for joint recovery under composition that no per-map parameter can lower. <a href="https://doi.org/10.5281/zenodo.20607278">DOI</a></td>
					<td>Targeting CSF</td>
			</tr>
			<tr>
					<td><a href="/papers/algebraic-cipher-types/"><strong>Algebraic Cipher Types</strong></a></td>
					<td>The type theory: product and sum types over trapdoors, the sum-type impossibility, and orbit-closure bounds against an active adversary. <a href="https://doi.org/10.5281/zenodo.20607285">DOI</a></td>
					<td>Deposited</td>
			</tr>
			<tr>
					<td><strong>Codec-Controlled Retrieval</strong></td>
					<td>A GF(2)-linear ribbon/XOR construction whose non-member output channel is turned into a designable, frequency-hiding distribution at zero per-query cost.</td>
					<td>Under review, ACM TOPS</td>
			</tr>
	</tbody>
</table>
<h3 id="in-preparation">In preparation</h3>
<table>
	<thead>
			<tr>
					<th>Paper</th>
					<th>What it adds</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><strong>Cipher Rekeying</strong> (<a href="https://doi.org/10.5281/zenodo.20607283">DOI</a>)</td>
					<td>Re-keying a deployed system through cipher closures, without ever decoding.</td>
			</tr>
			<tr>
					<td><strong>Cipher Closures</strong></td>
					<td>Cipher data structures and the code-data duality, spun out of rekeying.</td>
			</tr>
			<tr>
					<td><strong>Cipher Program Construction</strong></td>
					<td>Realizing whole programs as compositions of cipher maps.</td>
			</tr>
			<tr>
					<td><strong>Adaptive Trapdoor</strong></td>
					<td>Maintaining confidentiality as the query distribution drifts.</td>
			</tr>
	</tbody>
</table>
<h3 id="folded-in">Folded in</h3>
<p><a href="/papers/boolean-algebra-trapdoor-sets/"><strong>Boolean Algebra over Trapdoor Sets</strong></a>
was the original construction. Its two load-bearing ideas, the generalized
Boolean algebra and the deterministic single-hash baseline, are now folded into
<em>Cipher Maps</em> and <em>Algebraic Cipher Types</em>, and the standalone is archived. It
is still the cleanest entry point to the intuition.</p>
<h2 id="what-i-would-like-to-publish">What I would like to publish</h2>
<p>The near-term plan is three venues and a book. <strong>The Entropy Ratio</strong> goes to
CSF: its matching-bounds theorem is the lowest-variance result to defend at a
foundations venue. <strong>Codec-Controlled Retrieval</strong> is under review at ACM TOPS.
<strong>Cipher Maps</strong>, the hub, targets PoPETs. And the <strong>monograph</strong> is the long
game, the place where a reader who wants the whole picture (rather than one
theorem) can get it.</p>
<p>If you are deciding whether the work is worth your time: start with the
<a href="/series/trapdoor-computing/">series page</a> for the paradigm, read <em>The Entropy
Ratio</em> for the sharpest single result, and ask me about the monograph for the
synthesis.</p>
<h2 id="posts-and-notes">Posts and notes</h2>
<p>The development is chronicled in a series of posts, the authentic 2023-2024
foundations and the commentary since. This hub tracks them automatically, by
the <code>trapdoor-computing</code> series and tag, so the list grows as the work does:</p>
<ul class="tagged-posts" style="list-style:none;padding-left:0;">
  <li style="margin-bottom:0.7rem;">
    <a href="/post/entropy-map/" style="font-weight:600;">Entropy Maps</a><span style="color:#888;font-size:0.85rem;"> &middot; February 2024</span>
    <div style="color:#666;font-size:0.92rem;margin-top:0.15rem;">Entropy maps use prefix-free hash codes to approximate functions without storing the domain, achieving information-theoretic space bounds with controllable error.</div>
  </li>
  <li style="margin-bottom:0.7rem;">
    <a href="/post/2024-02-01-crypto-perf-hash/" style="font-weight:600;">Perfect Hashing: Space Bounds, Entropy, and Cryptographic Security</a><span style="color:#888;font-size:0.85rem;"> &middot; February 2024</span>
    <div style="color:#666;font-size:0.92rem;margin-top:0.15rem;">Space bounds, entropy requirements, and cryptographic security properties of perfect hash functions.</div>
  </li>
  <li style="margin-bottom:0.7rem;">
    <a href="/post/trapdoor-boolean-model/" style="font-weight:600;">A Boolean Algebra Over Trapdoors</a><span style="color:#888;font-size:0.85rem;"> &middot; June 2023</span>
    <div style="color:#666;font-size:0.92rem;margin-top:0.15rem;">A Boolean algebra framework over trapdoors for cryptographic operations. Introduces a homomorphism from powerset Boolean algebra to n-bit strings via cryptographic hash functions, enabling secure …</div>
  </li>
  <li style="margin-bottom:0.7rem;">
    <a href="/post/2023-06-17-noisy-gates/" style="font-weight:600;">Noisy Turing Machines: Noisy Logic Gates</a><span style="color:#888;font-size:0.85rem;"> &middot; June 2023</span>
    <div style="color:#666;font-size:0.92rem;margin-top:0.15rem;">Analyzing how Bernoulli Boolean types propagate through logic circuits, with correctness probabilities for noisy AND gates and interval arithmetic for composed circuits.</div>
  </li>
  <li style="margin-bottom:0.7rem;">
    <a href="/post/2024-06-01-algebraic_hashing/" style="font-weight:600;">Algebraic Hashing: Composable Hash Functions Through XOR</a><span style="color:#888;font-size:0.85rem;"> &middot; November 2022</span>
    <div style="color:#666;font-size:0.92rem;margin-top:0.15rem;">A C++ library for composable hash functions using algebraic structure over XOR, with template metaprogramming.</div>
  </li>
  <li style="margin-bottom:0.7rem;">
    <a href="/post/rank-ordered-encrypted-search/" style="font-weight:600;">Rank-Ordered Encrypted Search</a><span style="color:#888;font-size:0.85rem;"> &middot; June 2021</span>
    <div style="color:#666;font-size:0.92rem;margin-top:0.15rem;">Rank-ordered search over encrypted documents using oblivious entropy maps, enabling relevance scoring without revealing document contents.</div>
  </li>
  <li style="margin-bottom:0.7rem;">
    <a href="/post/2016-06-random-oracles-python/" style="font-weight:600;">Random Oracles in Python</a><span style="color:#888;font-size:0.85rem;"> &middot; June 2016</span>
    <div style="color:#666;font-size:0.92rem;margin-top:0.15rem;">Three Python approximations of a random oracle, each showing a different tradeoff between true randomness, determinism, and composability.</div>
  </li>
</ul>
<h2 id="foundations">Foundations</h2>
<p>The framework grew from a set of authentic 2023-2024 notes, the Bernoulli
model, noisy gates, Boolean algebra over trapdoors, and entropy maps, collected
under the <a href="/series/trapdoor-computing/">series</a> and its
<a href="/reading/trapdoor-computing/">recommended reading</a>, which places the work
against the searchable-encryption canon, the oblivious-RAM literature, and the
approximate-set data structures it generalizes.</p>
<hr>
<p>Alexander Towell, <a href="https://metafunctor.com">metafunctor.com</a>.</p>
]]></content:encoded><category>trapdoor computing</category><category>cipher maps</category><category>quantitative information flow</category><category>encrypted search</category><category>oblivious computation</category><category>information theory</category></item><item><title>Synthesis: Codecs as Structure</title><link>https://metafunctor.com/post/2026-05-synthesis-wire-formats/</link><pubDate>Fri, 15 May 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2026-05-synthesis-wire-formats/</guid><description>The series closes by restating the codes-as-priors thesis across all twelve instances and connecting the wire-format side to the Stepanov type-algebra side.</description><content:encoded><![CDATA[<p><em>Twelve posts, twelve codes, one thesis that refused to change. This is the closing summary.</em></p>
<h2 id="a-the-twelve-codes-together">A. The Twelve Codes Together</h2>
<p>Every post in this series answered a version of the same question: given a source of positive integers, how do you represent its values compactly as a sequence of bits? The answers differ in shape, in assumptions, and in which distribution each code implicitly expects.</p>
<table>
	<thead>
			<tr>
					<th>Post</th>
					<th>Code</th>
					<th>Implied prior (one phrase)</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>1-2</td>
					<td>Foundations</td>
					<td>Prefix-free codes are possible iff Kraft&rsquo;s inequality holds</td>
			</tr>
			<tr>
					<td>3</td>
					<td>Priors framework</td>
					<td>Any code defines a prior; the best code matches the source</td>
			</tr>
			<tr>
					<td>4</td>
					<td>Unary</td>
					<td>Geometric(1/2): value 1 is twice as likely as value 2, etc.</td>
			</tr>
			<tr>
					<td>5a</td>
					<td>Elias Gamma</td>
					<td>Power-law: probability falls as 1/n^2</td>
			</tr>
			<tr>
					<td>5b</td>
					<td>Elias Delta</td>
					<td>Heavier-tailed power law: slower decay for large values</td>
			</tr>
			<tr>
					<td>5c</td>
					<td>Elias Omega</td>
					<td>Recursive structure: no fixed polynomial decay rate</td>
			</tr>
			<tr>
					<td>6</td>
					<td>Fibonacci</td>
					<td>Near-geometric with Zeckendorf structure; good for Zeckendorf-sparse integers</td>
			</tr>
			<tr>
					<td>7</td>
					<td>Rice / Golomb</td>
					<td>Geometric with known parameter m; optimal when m divides entropy</td>
			</tr>
			<tr>
					<td>8</td>
					<td>VByte</td>
					<td>Roughly uniform over byte-aligned ranges; engineering favorite</td>
			</tr>
			<tr>
					<td>9</td>
					<td>Huffman</td>
					<td>Source-optimal given the exact symbol distribution</td>
			</tr>
			<tr>
					<td>10</td>
					<td>Arithmetic coding</td>
					<td>Approaches entropy to an arbitrary fraction of a bit</td>
			</tr>
			<tr>
					<td>11</td>
					<td>Succinct bit vectors</td>
					<td>Not a code for integers: a representation that answers rank/select queries</td>
			</tr>
			<tr>
					<td>12</td>
					<td>RoaringBitmap</td>
					<td>Polyalgorithm: picks array, bitset, or run-length per container chunk</td>
			</tr>
	</tbody>
</table>
<p>Posts 1 and 2 (<a href="/post/2020-03-kraft-wire-formats/">Kraft&rsquo;s Inequality</a> and <a href="/post/2020-09-mcmillan-wire-formats/">McMillan&rsquo;s Converse</a>) established why prefix-free codes are the right unit of analysis. Post 3 (<a href="/post/2022-01-priors-wire-formats/">Universal Codes as Priors</a>) named the frame: a code is a hypothesis about the source. Posts 4 through 10 filled in the catalogue. Posts 11 and 12 extended from integer coding to set representation, where the questions shift from &ldquo;how long is this codeword?&rdquo; to &ldquo;how do you store membership?&rdquo; and &ldquo;how do you answer rank/select?&rdquo;</p>
<p>Looking across all twelve, the main lesson is not that one code dominates. It is that the question &ldquo;which code?&rdquo; is always empirically answerable given a sample.</p>
<h2 id="b-the-unifying-frame-restated">B. The Unifying Frame Restated</h2>
<p><a href="/post/2022-01-priors-wire-formats/">Post 3</a> introduced the codes-as-priors thesis with two instances behind it. We now have twelve. The thesis has not changed; it has only become more evidently true.</p>
<p>Here is the six-clause version.</p>
<p><strong>Clause 1: a code is a hypothesis.</strong> Every prefix-free code for positive integers assigns a codeword length $\ell(n)$ to each value $n$. By Kraft&rsquo;s equality for complete codes, the implied probability is $p(n) = 2^{-\ell(n)}$. A code is not neutral: it says something about how likely each value is.</p>
<p><strong>Clause 2: lengths determine a prior.</strong> If you choose Elias gamma, you are implicitly betting that values are power-law distributed with exponent 2. If you choose Unary, you are betting on geometric(1/2). If you choose VByte, you are betting on a near-uniform distribution over byte-aligned ranges. The bet is in the lengths, whether or not you named it as a bet.</p>
<p><strong>Clause 3: choosing a code is choosing a prior.</strong> This is just clauses 1 and 2 combined, but it bears stating as a unit: the act of picking a codec is a modeling decision, not an engineering neutral choice. Unary and Delta will produce radically different wire sizes on the same data, because they assume radically different sources.</p>
<p><strong>Clause 4: the best code matches the actual source.</strong> The code that minimizes expected description length for a source $p$ is the one whose implied prior most closely matches $p$. Formally, expected length under code $c$ for source $p$ equals entropy $H(p)$ plus the KL divergence $D_\text{KL}(p ,|, q_c)$ where $q_c$ is the code&rsquo;s implied prior. Minimum expected length occurs at $D_\text{KL} = 0$, i.e., when the code and source agree.</p>
<p><strong>Clause 5: universal codes give bounded redundancy when the source is unknown.</strong> Elias gamma and delta are universal: for any source with a finite entropy upper-bound, their redundancy per symbol is $O(\log \log n)$ relative to the optimal code for that source. You pay a small overhead to avoid committing to a specific prior. The overhead is worth paying when you cannot measure the source in advance.</p>
<p><strong>Clause 6: polyalgorithms adapt per-chunk rather than committing globally.</strong> RoaringBitmap is the clearest instance of this: it measures each 65536-element chunk and picks the container type (sorted array, bitset, run-length) that minimizes storage for that chunk&rsquo;s observed density. This is not &ldquo;pick the best universal code&rdquo; but &ldquo;pick the best structure from a menu, per block.&rdquo; The principle generalizes: any large dataset may be non-stationary, and a single global codec will be suboptimal for every region where the source differs from the global average.</p>
<h2 id="c-composition-with-type-algebra">C. Composition with Type Algebra</h2>
<p>Two posts in the companion Stepanov series made claims that interlock with everything above. Working through them changed how I think about codec design.</p>
<p>The post <a href="/post/2026-05-codecs-functors-stepanov/">Bits Follow Types</a> argued that codecs compose along the algebraic structure of types: a codec for <code>Either&lt;A, B&gt;</code> is built from codecs for <code>A</code> and <code>B</code> by prepending a tag bit and dispatching; a codec for <code>Vec&lt;A&gt;</code> is built from a codec for <code>A</code> by iterating. The composition law is structural. It follows the constructors of the algebraic data type, not the distribution of the data.</p>
<p>The post <a href="/post/2026-05-prefix-free-stepanov/">When Lists Become Bits</a> argued that prefix-freeness lifts the free-monoid construction into bit space. Concatenating prefix-free codewords is unambiguous because each codeword is self-delimiting. The monoid of bit strings decomposes along codec boundaries exactly as the monoid of abstract tokens decomposes along type boundaries.</p>
<p>This series adds a third claim: the choice of leaf codec in any composite type is determined by the prior over that leaf&rsquo;s data, while the composition structure is determined by the type. These are two orthogonal axes of design freedom.</p>
<p>Consider <code>Either&lt;uint32_t, std::string&gt;</code>. The composition structure says: write one bit for the variant tag, then write the payload for the selected variant. That part is fixed by the type. What is not fixed is which codec you use for the tag bit, and which codec you use for the <code>uint32_t</code> payload.</p>
<p>If 90% of values are <code>uint32_t</code> and 10% are <code>std::string</code>, a Huffman code over the two-symbol alphabet <code>{left, right}</code> gives you the tag bit nearly for free (0.47 bits expected vs. 1 bit for the flat tag). And if the <code>uint32_t</code> values cluster in <code>[0, 127]</code>, VByte costs exactly 8 bits per value. Swap those observations and you swap those choices.</p>
<p>The type algebra determines the composition; the empirical distribution determines the leaf codecs. Neither axis constrains the other. Attending to both is what makes codec design non-trivial.</p>
<h2 id="d-the-codec-selection-library">D. The Codec-Selection Library</h2>
<p>The <code>synthesis.hpp</code> library distills the selection process into three functions.</p>
<p><code>empirical_distribution</code> counts occurrences of each value in a sample and normalizes by the total count to produce a probability map.</p>
<p><code>redundancy_for</code> computes the redundancy of a named code on a given distribution: expected length minus entropy. By Shannon&rsquo;s source-coding theorem, this is always non-negative. A lower redundancy means the code&rsquo;s implied prior is a closer fit to the actual source.</p>
<p><code>recommend_code</code> ties them together: it estimates the empirical distribution from a sample, computes the redundancy of all six candidate universal codes (Unary, Gamma, Delta, Omega, Fibonacci, VByte), and returns the name of the code with the smallest redundancy.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="kr">inline</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="n">recommend_code</span><span class="p">(</span><span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="o">&gt;&amp;</span> <span class="n">sample</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">auto</span> <span class="n">dist</span> <span class="o">=</span> <span class="n">empirical_distribution</span><span class="p">(</span><span class="n">sample</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="k">constexpr</span> <span class="n">std</span><span class="o">::</span><span class="n">string_view</span> <span class="n">candidates</span><span class="p">[]</span> <span class="o">=</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="s">&#34;Unary&#34;</span><span class="p">,</span> <span class="s">&#34;Gamma&#34;</span><span class="p">,</span> <span class="s">&#34;Delta&#34;</span><span class="p">,</span> <span class="s">&#34;Omega&#34;</span><span class="p">,</span> <span class="s">&#34;Fibonacci&#34;</span><span class="p">,</span> <span class="s">&#34;VByte&#34;</span>
</span></span><span class="line"><span class="cl">    <span class="p">};</span>
</span></span><span class="line"><span class="cl">    <span class="kt">double</span> <span class="n">best_redundancy</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">numeric_limits</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;::</span><span class="n">infinity</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="n">best_code</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">string_view</span> <span class="nl">candidate</span> <span class="p">:</span> <span class="n">candidates</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="kt">double</span> <span class="n">r</span> <span class="o">=</span> <span class="n">redundancy_for</span><span class="p">(</span><span class="n">candidate</span><span class="p">,</span> <span class="n">dist</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">r</span> <span class="o">&lt;</span> <span class="n">best_redundancy</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">best_redundancy</span> <span class="o">=</span> <span class="n">r</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="n">best_code</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="p">(</span><span class="n">candidate</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="k">return</span> <span class="n">best_code</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>On a strongly geometric sample (value 1 appears much more often than value 2, which appears more often than value 3, and so on), <code>recommend_code</code> returns <code>&quot;Unary&quot;</code> or <code>&quot;Fibonacci&quot;</code>. Both codes are matched to geometric-like priors; their redundancy on geometric data is low. On a sample of values in <code>[500, 1000]</code>, it returns <code>&quot;VByte&quot;</code> or <code>&quot;Delta&quot;</code>: byte-aligned codes have low overhead for values that cluster in that range, and neither Unary nor Gamma would be tolerable there. Unary would require hundreds of bits per value.</p>
<p>The point is not that <code>recommend_code</code> is a production-quality selector. It is that it makes the selection process concrete and mechanical. There is no best code in general. There is a best code given a sample.</p>
<h2 id="e-the-six-principles">E. The Six Principles</h2>
<p>These are the things I now believe about coding, after working through twelve instances.</p>
<p><strong>1. A code is a prior.</strong> Every codeword length implies a probability. Choosing a code is choosing what you believe about the source. The choice is never neutral, even when it is unconscious. When you reach for the first codec you know, you are betting on a prior you may never have examined.</p>
<p><strong>2. Universality is robustness.</strong> A universal code performs well across many priors, not just one. Elias gamma is asymptotically optimal for any source from a broad class of power-law distributions; VByte is a pragmatic universal for byte-oriented hardware. Use universal codes when you do not know the prior in advance, when the prior may shift over time, or when the engineering cost of measuring the source outweighs the compression gain. Use Huffman or arithmetic coding when you do know the prior and the gain from exploiting it justifies the overhead.</p>
<p><strong>3. Optimality is measurable.</strong> Shannon&rsquo;s source-coding theorem gives the lower bound: no uniquely-decodable code can have expected length below the entropy $H(p)$ of the source. Every code&rsquo;s redundancy is then measurable as expected-length minus entropy. This is not a theoretical abstraction; you can compute it from a sample in a few lines of code, as <code>redundancy_for</code> demonstrates. Pick codes by minimizing redundancy on the actual source, not by intuition about which code sounds right for the problem.</p>
<p><strong>4. Engineering trades dominate at scale.</strong> Theoretical optima (Elias gamma, delta, arithmetic coding) are outperformed in throughput benchmarks by byte-aligned approximations (VByte) when decode speed is the binding constraint. The reason is cache line alignment and SIMD: modern CPUs process data in 16-byte to 64-byte chunks, and a code that crosses byte boundaries pays a penalty that no redundancy saving can overcome at typical data volumes. Figure out where the binding constraint lives (compression ratio, decode throughput, encode throughput, memory, latency) before you decide which axis to optimize.</p>
<p><strong>5. Polyalgorithms beat single algorithms.</strong> When source characteristics vary across the data, adapt per-chunk rather than committing to a single global code. RoaringBitmap does this for integer sets: it observes each 65536-element chunk and picks the container type that minimizes storage for that chunk&rsquo;s density. Zstd does this for byte streams: it switches between LZ77, Huffman, and ANS depending on local symbol statistics. The principle is general. Non-stationarity is the norm in real data, and a globally optimal code is locally suboptimal everywhere the source deviates from the global average.</p>
<p><strong>6. The algebra of composition is orthogonal to the choice of leaf code.</strong> Type structure dictates how codecs compose: <code>Either</code> needs a tag, <code>Vec</code> needs a count or terminator, <code>Product</code> concatenates. These are structural facts about the algebraic data type and they do not change with the data distribution. What does change with the distribution is which codec you assign to each leaf. You can swap leaf codecs without changing the composition law, and you can change the composition structure without touching the leaf codecs. Keeping these two axes separate is what makes a codec library composable rather than a pile of special cases.</p>
<h2 id="f-what-comes-next">F. What Comes Next</h2>
<p>This series covered prefix-free codes from their theoretical foundations through twelve instances, ending at polyalgorithms. I stopped here because this is where the foundations end and the engineering starts getting domain-specific. The frontier extends in three directions I have not covered.</p>
<p><strong>Context mixing.</strong> The PAQ and ZPAQ family of compressors achieve near-arithmetic-limits on general data by running many predictive models in parallel and mixing their probability estimates with weights that are themselves adapted to the data. The key insight is that no single model is best everywhere; a mixture that weights models by their recent prediction accuracy outperforms any single model on non-stationary sources. Context mixing is the logical culmination of the codes-as-priors idea: you do not pick one prior, you maintain a portfolio.</p>
<p><strong>Asymmetric Numeral Systems (ANS).</strong> Developed by Jaroslaw Duda around 2014, ANS achieves the compression ratio of arithmetic coding while decoding at 5 to 10 times the speed. It underlies LZ4&rsquo;s entropy backend (FSE), Zstd&rsquo;s entropy coder, Apple&rsquo;s LZFSE, and several video compression standards. ANS works by maintaining a single integer state that encodes the accumulated probability of the symbols seen so far, updated with each new symbol using a lookup table rather than interval arithmetic. If you want to understand how modern fast compressors actually work at the symbol level, ANS is the answer.</p>
<p><strong>Rate-distortion theory.</strong> Everything in this series assumed lossless compression: the decoder must recover the exact original bit sequence. Rate-distortion theory introduces a different class of trade, where you bound the allowable reconstruction error and ask how few bits you can use subject to that bound. This is the foundation of audio (MP3, AAC, Opus), video (H.264, H.265, AV1), and image compression (JPEG, WebP). The information-theoretic results here are just as clean as Shannon&rsquo;s source-coding theorem, but the engineering is richer because the distortion measure (perceptual quality for audio and video, pixel error for images) shapes every design decision.</p>
<p>The principles from this series carry forward into all three directions: a code is a prior, universality is robustness, optimality is measurable, engineering trades dominate at scale. The new settings add new structure (context histories, state machines, distortion metrics) but not new foundations.</p>
<h2 id="g-cross-references">G. Cross-references</h2>
<p>Posts in this series (in order):
<a href="/post/2020-03-kraft-wire-formats/">Kraft&rsquo;s Inequality</a>,
<a href="/post/2020-09-mcmillan-wire-formats/">McMillan&rsquo;s Converse</a>,
<a href="/post/2022-01-priors-wire-formats/">Universal Codes as Priors</a>,
<a href="/post/2022-06-elias-gamma-wire-formats/">Unary and Elias Gamma</a>,
<a href="/post/2022-11-elias-delta-omega-wire-formats/">Elias Delta and Omega</a>,
<a href="/post/2023-04-fibonacci-wire-formats/">Fibonacci Coding</a>,
<a href="/post/2023-09-rice-golomb-wire-formats/">Rice / Golomb</a>,
<a href="/post/2024-02-vbyte-wire-formats/">VByte / Varint</a>,
<a href="/post/2024-08-huffman-wire-formats/">Huffman</a>,
<a href="/post/2025-01-arithmetic-coding-wire-formats/">Arithmetic Coding</a>,
<a href="/post/2025-06-succinct-wire-formats/">Succinct Bit Vectors</a>,
<a href="/post/2025-12-roaring-bitmap-wire-formats/">RoaringBitmap</a>.</p>
<p>Cross-series: <a href="/post/2026-05-codecs-functors-stepanov/">Bits Follow Types</a> and
<a href="/post/2026-05-prefix-free-stepanov/">When Lists Become Bits</a>.</p>
<p><strong>PFC footnote:</strong> The production implementation of all 12 codes (and several more)
is at <a href="https://github.com/queelius/wire-formats/tree/master/lib/pfc">github.com/queelius/wire-formats/tree/master/lib/pfc</a>. This series
develops the theory; PFC is the practice.</p>
]]></content:encoded><media:content url="https://metafunctor.com/post/2026-05-synthesis-wire-formats/card.jpg" medium="image"><media:title type="plain">Information-Theory</media:title></media:content><category>C++</category><category>information-theory</category><category>coding-theory</category><category>prefix-free</category><category>universal-codes</category><category>synthesis</category><category>algebra</category></item><item><title>Bits Follow Types</title><link>https://metafunctor.com/post/2026-05-codecs-functors-stepanov/</link><pubDate>Thu, 23 Apr 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2026-05-codecs-functors-stepanov/</guid><description>Codecs are not ad-hoc bit formats. They are constructions on the algebraic structure of types.</description><content:encoded><![CDATA[<p><em>Every type decomposes structurally. So does its codec.</em></p>
<h2 id="codecs-as-functors">Codecs as Functors</h2>
<p>You have an <code>optional&lt;vector&lt;pair&lt;int, string&gt;&gt;&gt;</code>. The type decomposes structurally: it is an optional of a free monoid of products of an integer and a string. That decomposition is not an observation about memory layout. It is a statement about the algebraic structure of the type.</p>
<p>Now ask: does the codec decompose the same way?</p>
<p>If the answer is yes, you stop writing one-off encoders. You build a codec for <code>optional&lt;T&gt;</code> from a codec for <code>T</code>. You build a codec for <code>vector&lt;T&gt;</code> from a codec for <code>T</code>. The codec for <code>optional&lt;vector&lt;pair&lt;int, string&gt;&gt;&gt;</code> assembles from its parts with no manual layout decisions, no hand-placed length headers, no ad-hoc format negotiation.</p>
<p>This post argues that the answer is always yes, and shows what the machinery looks like. The thesis: codecs are not ad-hoc bit formats. They are constructions on the algebraic structure of types. The algebraic structure of a type determines its codec, the same way it determines its algorithms.</p>
<p>This extends Stepanov&rsquo;s claim. The <a href="/post/2019-03-peasant-stepanov/">peasant algorithm post</a> showed that algorithms arise from algebraic structure. The <a href="/post/2026-03-homomorphism-stepanov/">homomorphism post</a> showed that structure-preserving maps are the natural morphisms. Here, we show the codec itself is a structure-preserving map, and that it lifts from leaf types to compound types by the same algebraic logic.</p>
<h2 id="bit-io-the-foundation">Bit I/O: The Foundation</h2>
<p>Before combinators, we need concrete bit I/O. The approach taken here follows Stepanov&rsquo;s move in the algorithm posts: state the concept first, then provide a model.</p>
<p>Two concepts govern bit-level I/O:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">T</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">concept</span> <span class="n">BitSink</span> <span class="o">=</span> <span class="k">requires</span><span class="p">(</span><span class="n">T</span><span class="o">&amp;</span> <span class="n">s</span><span class="p">,</span> <span class="kt">bool</span> <span class="n">bit</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span> <span class="n">s</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="n">bit</span><span class="p">)</span> <span class="p">}</span> <span class="o">-&gt;</span> <span class="n">std</span><span class="o">::</span><span class="n">same_as</span><span class="o">&lt;</span><span class="kt">void</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">T</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">concept</span> <span class="n">BitSource</span> <span class="o">=</span> <span class="k">requires</span><span class="p">(</span><span class="n">T</span><span class="o">&amp;</span> <span class="n">s</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span> <span class="n">s</span><span class="p">.</span><span class="n">read</span><span class="p">()</span> <span class="p">}</span> <span class="o">-&gt;</span> <span class="n">std</span><span class="o">::</span><span class="n">same_as</span><span class="o">&lt;</span><span class="kt">bool</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span> <span class="n">s</span><span class="p">.</span><span class="n">peek</span><span class="p">()</span> <span class="p">}</span> <span class="o">-&gt;</span> <span class="n">std</span><span class="o">::</span><span class="n">convertible_to</span><span class="o">&lt;</span><span class="kt">bool</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>A <code>BitSink</code> accepts bits. A <code>BitSource</code> supplies them. A codec is an algorithm parameterized over <code>BitSink</code> and <code>BitSource</code>, not a class hierarchy. This is Stepanov&rsquo;s move at the bit level: require only what the algorithm needs, let anything that satisfies the concept participate.</p>
<p>The standard models are <code>BitWriter</code> and <code>BitReader</code>, which pack bits into byte buffers in LSB-first order:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">BitWriter</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">span</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span><span class="o">&gt;</span> <span class="n">buf_</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">byte_idx_</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span> <span class="n">byte_</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span> <span class="n">bit_pos_</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">public</span><span class="o">:</span>
</span></span><span class="line"><span class="cl">    <span class="k">explicit</span> <span class="n">BitWriter</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">span</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span><span class="o">&gt;</span> <span class="n">buf</span><span class="p">)</span> <span class="k">noexcept</span> <span class="o">:</span> <span class="n">buf_</span><span class="p">(</span><span class="n">buf</span><span class="p">)</span> <span class="p">{}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kt">void</span> <span class="nf">write</span><span class="p">(</span><span class="kt">bool</span> <span class="n">bit</span><span class="p">)</span> <span class="k">noexcept</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">byte_</span> <span class="o">|=</span> <span class="p">(</span><span class="n">bit</span> <span class="o">?</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span><span class="p">{</span><span class="mi">1</span><span class="p">}</span> <span class="o">:</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span><span class="p">{</span><span class="mi">0</span><span class="p">})</span> <span class="o">&lt;&lt;</span> <span class="n">bit_pos_</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="o">++</span><span class="n">bit_pos_</span> <span class="o">==</span> <span class="mi">8</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">buf_</span><span class="p">[</span><span class="n">byte_idx_</span><span class="o">++</span><span class="p">]</span> <span class="o">=</span> <span class="n">byte_</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="n">byte_</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="n">bit_pos_</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kt">void</span> <span class="nf">align</span><span class="p">()</span> <span class="k">noexcept</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">bit_pos_</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">buf_</span><span class="p">[</span><span class="n">byte_idx_</span><span class="o">++</span><span class="p">]</span> <span class="o">=</span> <span class="n">byte_</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="n">byte_</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="n">bit_pos_</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="na">[[nodiscard]]</span> <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">bytes_written</span><span class="p">()</span> <span class="k">const</span> <span class="k">noexcept</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">byte_idx_</span> <span class="o">+</span> <span class="p">(</span><span class="n">bit_pos_</span> <span class="o">&gt;</span> <span class="mi">0</span> <span class="o">?</span> <span class="mi">1</span> <span class="o">:</span> <span class="mi">0</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">BitReader</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">span</span><span class="o">&lt;</span><span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span><span class="o">&gt;</span> <span class="n">buf_</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">byte_idx_</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span> <span class="n">bit_pos_</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">public</span><span class="o">:</span>
</span></span><span class="line"><span class="cl">    <span class="k">explicit</span> <span class="n">BitReader</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">span</span><span class="o">&lt;</span><span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span><span class="o">&gt;</span> <span class="n">buf</span><span class="p">)</span> <span class="k">noexcept</span> <span class="o">:</span> <span class="n">buf_</span><span class="p">(</span><span class="n">buf</span><span class="p">)</span> <span class="p">{}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kt">bool</span> <span class="nf">read</span><span class="p">()</span> <span class="k">noexcept</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="kt">bool</span> <span class="n">bit</span> <span class="o">=</span> <span class="p">((</span><span class="n">buf_</span><span class="p">[</span><span class="n">byte_idx_</span><span class="p">]</span> <span class="o">&gt;&gt;</span> <span class="n">bit_pos_</span><span class="p">)</span> <span class="o">&amp;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="o">++</span><span class="n">bit_pos_</span> <span class="o">==</span> <span class="mi">8</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="o">++</span><span class="n">byte_idx_</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="n">bit_pos_</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">bit</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="na">[[nodiscard]]</span> <span class="kt">bool</span> <span class="n">peek</span><span class="p">()</span> <span class="k">const</span> <span class="k">noexcept</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">byte_idx_</span> <span class="o">&lt;</span> <span class="n">buf_</span><span class="p">.</span><span class="n">size</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>A codec concept rounds out the three-concept core:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">C</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">concept</span> <span class="n">Codec</span> <span class="o">=</span> <span class="k">requires</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">typename</span> <span class="n">C</span><span class="o">::</span><span class="n">value_type</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>The minimal form checks that <code>C</code> names a <code>value_type</code>. The full behavioral requirement, that <code>C</code> provides <code>encode</code> and <code>decode</code> templates matching <code>BitSink</code> and <code>BitSource</code> respectively, is enforced by the template constraints on each codec&rsquo;s methods rather than by the concept itself. This keeps the concept definition lean while the actual requirements are checked at each call site.</p>
<p>All code in this post lives in <code>codecs_functors.hpp</code>. The full suite of round-trip tests is in <code>test_codecs_functors.cpp</code>.</p>
<h2 id="a-leaf-codec-elias-gamma">A Leaf Codec: Elias Gamma</h2>
<p>Before combinators, establish what a leaf codec looks like. Elias gamma encodes positive integers \(n \geq 1\).</p>
<p>The codeword for \(n\) is: \(\lfloor\log_2 n\rfloor\) zero bits, then a one bit, then the bits of \(n\) below its leading 1, written MSB-first. The total length is \(2\lfloor\log_2 n\rfloor + 1\) bits.</p>
<p>Examples: 1 encodes as <code>1</code>, 2 as <code>010</code>, 3 as <code>011</code>, 4 as <code>00100</code>, 7 as <code>00111</code>.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Gamma</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="n">value_type</span> <span class="n">n</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">assert</span><span class="p">(</span><span class="n">n</span> <span class="o">&gt;=</span> <span class="mi">1</span> <span class="o">&amp;&amp;</span> <span class="s">&#34;Gamma is undefined for n = 0&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">bits</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">bit_width</span><span class="p">(</span><span class="n">n</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">bits</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">false</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">true</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="n">bits</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span> <span class="n">i</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">;</span> <span class="o">--</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(((</span><span class="n">n</span> <span class="o">&gt;&gt;</span> <span class="p">(</span><span class="n">i</span> <span class="o">-</span> <span class="mi">1</span><span class="p">))</span> <span class="o">&amp;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">bits</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">while</span> <span class="p">(</span><span class="o">!</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">())</span> <span class="o">++</span><span class="n">bits</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="n">value_type</span> <span class="n">result</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">bits</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">result</span> <span class="o">=</span> <span class="p">(</span><span class="n">result</span> <span class="o">&lt;&lt;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">|</span> <span class="p">(</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">()</span> <span class="o">?</span> <span class="n">value_type</span><span class="p">{</span><span class="mi">1</span><span class="p">}</span> <span class="o">:</span> <span class="n">value_type</span><span class="p">{</span><span class="mi">0</span><span class="p">});</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">result</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>Gamma is asymptotically optimal under a prior proportional to \(1/n^2\). The full story, including the Kraft inequality, optimal code lengths, and the connection to universal codes, belongs to a forthcoming Information Theory by Construction series. For now: gamma is the code you use when you know values are small integers but not their distribution.</p>
<h2 id="combinator-opt">Combinator: Opt</h2>
<p><code>Opt&lt;C&gt;</code> encodes <code>std::optional&lt;T&gt;</code> given a codec <code>C</code> for <code>T</code>. The wire format is one tag bit (1 = present, 0 = absent) followed by <code>C</code>&rsquo;s encoding if present.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">C</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Opt</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">optional</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">C</span><span class="o">::</span><span class="n">value_type</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="k">const</span> <span class="n">value_type</span><span class="o">&amp;</span> <span class="n">v</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">v</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">true</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="n">C</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="o">*</span><span class="n">v</span><span class="p">,</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">false</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">())</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="k">return</span> <span class="n">C</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">std</span><span class="o">::</span><span class="n">nullopt</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>The type-algebra reading: <code>optional&lt;T&gt;</code> is the coproduct of <code>T</code> with the unit type (a single absent value). In type algebra, this is written \(1 + T\). The tag bit selects the branch; the payload is present only in the right branch.</p>
<p>This is the first sign of functoriality. <code>Opt</code> takes a codec and returns a codec. It acts on codecs the same way <code>optional</code> acts on types. The structure of the operation is preserved: the algebraic decomposition of the type is mirrored exactly in the bit encoding.</p>
<h2 id="combinator-either">Combinator: Either</h2>
<p><code>Either&lt;A, B&gt;</code> encodes <code>std::variant&lt;TA, TB&gt;</code> given codecs <code>A</code> for <code>TA</code> and <code>B</code> for <code>TB</code>. One tag bit selects the branch; the payload follows.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">A</span><span class="p">,</span> <span class="k">typename</span> <span class="n">B</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Either</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">variant</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">A</span><span class="o">::</span><span class="n">value_type</span><span class="p">,</span> <span class="k">typename</span> <span class="n">B</span><span class="o">::</span><span class="n">value_type</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="k">const</span> <span class="n">value_type</span><span class="o">&amp;</span> <span class="n">v</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">v</span><span class="p">.</span><span class="n">index</span><span class="p">()</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">false</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="n">A</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">get</span><span class="o">&lt;</span><span class="mi">0</span><span class="o">&gt;</span><span class="p">(</span><span class="n">v</span><span class="p">),</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">true</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="n">B</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">get</span><span class="o">&lt;</span><span class="mi">1</span><span class="o">&gt;</span><span class="p">(</span><span class="n">v</span><span class="p">),</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">())</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="k">return</span> <span class="n">value_type</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">in_place_index</span><span class="o">&lt;</span><span class="mi">1</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">B</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">)};</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">value_type</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">in_place_index</span><span class="o">&lt;</span><span class="mi">0</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">A</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">)};</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>Type algebra: \(A + B\), the binary coproduct. For the worked example in this post, we need a ternary variant, so the implementation also provides <code>Either3</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">A</span><span class="p">,</span> <span class="k">typename</span> <span class="n">B</span><span class="p">,</span> <span class="k">typename</span> <span class="n">C</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Either3</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">variant</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">A</span><span class="o">::</span><span class="n">value_type</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                                    <span class="k">typename</span> <span class="n">B</span><span class="o">::</span><span class="n">value_type</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                                    <span class="k">typename</span> <span class="n">C</span><span class="o">::</span><span class="n">value_type</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="k">const</span> <span class="n">value_type</span><span class="o">&amp;</span> <span class="n">v</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">idx</span> <span class="o">=</span> <span class="n">v</span><span class="p">.</span><span class="n">index</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">        <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">((</span><span class="n">idx</span> <span class="o">&amp;</span> <span class="mh">0x1</span><span class="p">)</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">((</span><span class="n">idx</span> <span class="o">&amp;</span> <span class="mh">0x2</span><span class="p">)</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">idx</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="n">A</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">get</span><span class="o">&lt;</span><span class="mi">0</span><span class="o">&gt;</span><span class="p">(</span><span class="n">v</span><span class="p">),</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">else</span> <span class="nf">if</span> <span class="p">(</span><span class="n">idx</span> <span class="o">==</span> <span class="mi">1</span><span class="p">)</span> <span class="n">B</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">get</span><span class="o">&lt;</span><span class="mi">1</span><span class="o">&gt;</span><span class="p">(</span><span class="n">v</span><span class="p">),</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">else</span> <span class="n">C</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">get</span><span class="o">&lt;</span><span class="mi">2</span><span class="o">&gt;</span><span class="p">(</span><span class="n">v</span><span class="p">),</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">idx</span> <span class="o">=</span> <span class="p">(</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">()</span> <span class="o">?</span> <span class="mi">1u</span> <span class="o">:</span> <span class="mi">0u</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">idx</span> <span class="o">|=</span> <span class="p">(</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">()</span> <span class="o">?</span> <span class="mi">2u</span> <span class="o">:</span> <span class="mi">0u</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">idx</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="k">return</span> <span class="n">value_type</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">in_place_index</span><span class="o">&lt;</span><span class="mi">0</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">A</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">)};</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">idx</span> <span class="o">==</span> <span class="mi">1</span><span class="p">)</span> <span class="k">return</span> <span class="n">value_type</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">in_place_index</span><span class="o">&lt;</span><span class="mi">1</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">B</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">)};</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">value_type</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">in_place_index</span><span class="o">&lt;</span><span class="mi">2</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">C</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">)};</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>The tag for <code>Either3</code> uses two bits, encoding the branch index as a 2-bit integer (LSB first). This costs \(\lceil\log_2 N\rceil\) bits for N branches regardless of how often each branch occurs. When branch probabilities differ significantly, this is wasteful: Huffman-coded tags or arithmetic coding over the branch distribution would do better. That improvement belongs in the forthcoming Information Theory by Construction series. The flat tag scheme is correct and simple; optimality can be layered on separately.</p>
<h2 id="combinator-pair">Combinator: Pair</h2>
<p><code>Pair&lt;A, B&gt;</code> encodes <code>std::pair&lt;TA, TB&gt;</code>: <code>A</code>&rsquo;s encoding followed immediately by <code>B</code>&rsquo;s encoding, with no delimiter.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">A</span><span class="p">,</span> <span class="k">typename</span> <span class="n">B</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Pair</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">pair</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">A</span><span class="o">::</span><span class="n">value_type</span><span class="p">,</span> <span class="k">typename</span> <span class="n">B</span><span class="o">::</span><span class="n">value_type</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="k">const</span> <span class="n">value_type</span><span class="o">&amp;</span> <span class="n">v</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">A</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">v</span><span class="p">.</span><span class="n">first</span><span class="p">,</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">B</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">v</span><span class="p">.</span><span class="n">second</span><span class="p">,</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">auto</span> <span class="n">a</span> <span class="o">=</span> <span class="n">A</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">auto</span> <span class="n">b</span> <span class="o">=</span> <span class="n">B</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">value_type</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="n">a</span><span class="p">),</span> <span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="n">b</span><span class="p">)};</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>Type algebra: \(A \times B\), the binary product. Two bit streams concatenated, recovered in sequence. No length header, no delimiter bit.</p>
<p>The reason this works is prefix-freeness: because <code>A</code>&rsquo;s codec is prefix-free, the decoder always knows exactly when <code>A</code>&rsquo;s payload ends and <code>B</code>&rsquo;s payload begins. The boundary is implicit in the structure of the code. If the codecs were not prefix-free, concatenation would be ambiguous.</p>
<p>See <a href="/post/2026-05-prefix-free-stepanov/">When Lists Become Bits</a> for why prefix-freeness makes concatenation reversible.</p>
<h2 id="combinator-vec">Combinator: Vec</h2>
<p><code>Vec&lt;C&gt;</code> encodes <code>std::vector&lt;T&gt;</code> given a codec <code>C</code> for <code>T</code>. The wire format is a gamma-coded length followed by <code>C</code>&rsquo;s encoding of each element.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">C</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Vec</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">C</span><span class="o">::</span><span class="n">value_type</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="k">const</span> <span class="n">value_type</span><span class="o">&amp;</span> <span class="n">v</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">Gamma</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">v</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">+</span> <span class="mi">1</span><span class="p">,</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="k">const</span> <span class="k">auto</span><span class="o">&amp;</span> <span class="nl">x</span> <span class="p">:</span> <span class="n">v</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">C</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">n</span> <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">Gamma</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">value_type</span> <span class="n">result</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="n">result</span><span class="p">.</span><span class="n">reserve</span><span class="p">(</span><span class="n">n</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">result</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span><span class="n">C</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">));</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">result</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>The <code>+1</code> bias in <code>encode</code> handles the mismatch between Gamma (which requires inputs \(\geq 1\)) and vector sizes (which can be 0). The decoded length subtracts 1 to recover the original size.</p>
<p>Type algebra: <code>vector&lt;T&gt;</code> is <code>List(T)</code>, the free monoid on <code>T</code>. The <a href="/post/2026-03-free-algebra-stepanov/">free-algebra post</a> showed that <code>fold</code> is the unique monoid homomorphism from lists to any monoid: that uniqueness is the universal property of the free monoid. <code>Vec&lt;C&gt;</code> is the lift of that universal property to bit space: it extends a per-element encoding <code>C</code> to the entire list by applying <code>C</code> to each element in sequence.</p>
<p>That lift always exists (it is just fold). The deeper question is whether it is invertible: can the decoder recover the list from the bit string? The answer depends on prefix-freeness, and the analysis belongs in <a href="/post/2026-05-prefix-free-stepanov/">When Lists Become Bits</a>. The short version: <code>Vec&lt;C&gt;</code> works when <code>C</code> is prefix-free, and Gamma is prefix-free.</p>
<h2 id="the-functorial-framing">The Functorial Framing</h2>
<p>Each combinator corresponds to an operation in the algebra of types:</p>
<table>
	<thead>
			<tr>
					<th>Combinator</th>
					<th>Type algebra</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>Opt&lt;C&gt;</code></td>
					<td>\(1 + T\) (coproduct with unit)</td>
			</tr>
			<tr>
					<td><code>Either&lt;A, B&gt;</code></td>
					<td>\(A + B\) (binary coproduct)</td>
			</tr>
			<tr>
					<td><code>Pair&lt;A, B&gt;</code></td>
					<td>\(A \times B\) (binary product)</td>
			</tr>
			<tr>
					<td><code>Vec&lt;C&gt;</code></td>
					<td>\(\text{List}(T)\) (free monoid on T)</td>
			</tr>
	</tbody>
</table>
<p>This table is not a coincidence. Each combinator takes codecs as inputs and returns a codec as output, mirroring the way the corresponding type constructor takes types and returns a type. The mapping from types to codecs respects the type algebra: if you know how to encode <code>A</code> and <code>B</code>, the combinators give you, for free, how to encode <code>A + B</code>, <code>A \times B</code>, <code>1 + A</code>, and <code>\text{List}(A)</code>.</p>
<p>This is the bridge thesis: <strong>the algebraic structure of a type determines its codec, the same way it determines its algorithms.</strong> Stepanov&rsquo;s original claim covered algorithms. These two posts extend it to encodings.</p>
<p>Each combinator is functorial in a precise sense. <code>Vec</code>, for instance, maps a morphism between codec targets (a function <code>f: TA -&gt; TB</code> liftable to <code>f*: vector&lt;TA&gt; -&gt; vector&lt;TB&gt;</code>) to a corresponding transformation between codecs. The formal categorical statement is deferred. For the purposes of this post, the table is the claim: structure-in, structure-out.</p>
<p>Contrast this with the alternative: one-off serialization code written directly for each compound type. That code is not wrong, but it is redundant. The structure of the type already tells you how to encode it. If your serialization code reflects that structure, it is derivable and correct by construction. If it does not, you have extra maintenance burden, a second place to update when the type changes.</p>
<p>The homomorphism post showed that structure-preserving maps are the natural morphisms between algebraic structures. Codecs, under this framing, are structure-preserving maps from the type algebra to the bit-string algebra. The combinators are the proof that these maps exist and compose.</p>
<h2 id="the-composed-example">The Composed Example</h2>
<p>To make this concrete, three auxiliary codecs appear in the worked example. Each is short enough to state plainly.</p>
<p><code>Bool</code> encodes a boolean as a single bit:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Bool</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="kt">bool</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="n">value_type</span> <span class="n">v</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span> <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="n">v</span><span class="p">);</span> <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span> <span class="k">return</span> <span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">();</span> <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p><code>Signed&lt;C&gt;</code> wraps an unsigned codec using zigzag encoding, mapping signed integers to non-negative values and then applying a bias so Gamma (which requires \(\geq 1\)) can handle zero:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">C</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Signed</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="kt">int64_t</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="n">value_type</span> <span class="n">v</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span> <span class="n">z</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">v</span> <span class="o">&lt;</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">z</span> <span class="o">=</span> <span class="p">(</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="o">&gt;</span><span class="p">(</span><span class="o">-</span><span class="p">(</span><span class="n">v</span> <span class="o">+</span> <span class="mi">1</span><span class="p">))</span> <span class="o">&lt;&lt;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">|</span> <span class="mi">1u</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">z</span> <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">v</span><span class="p">)</span> <span class="o">&lt;&lt;</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="n">C</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">z</span> <span class="o">+</span> <span class="mi">1</span><span class="p">,</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span> <span class="n">z</span> <span class="o">=</span> <span class="n">C</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">z</span> <span class="o">&amp;</span> <span class="mi">1u</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="k">return</span> <span class="o">-</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">int64_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">z</span> <span class="o">&gt;&gt;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">int64_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">z</span> <span class="o">&gt;&gt;</span> <span class="mi">1</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p><code>String</code> is gamma-coded length followed by 8 bits per character. It is, conceptually, <code>Vec&lt;Byte&gt;</code> where <code>Byte</code> is an 8-bit leaf codec. The combinator pattern applies recursively: a string is a list of bytes, and its codec is the list combinator applied to a byte codec.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">String</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="k">const</span> <span class="n">value_type</span><span class="o">&amp;</span> <span class="n">s</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">Gamma</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">s</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">+</span> <span class="mi">1</span><span class="p">,</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="kt">char</span> <span class="nl">c</span> <span class="p">:</span> <span class="n">s</span><span class="p">)</span> <span class="n">Byte</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">c</span><span class="p">),</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">n</span> <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">Gamma</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">value_type</span> <span class="n">s</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="n">s</span><span class="p">.</span><span class="n">reserve</span><span class="p">(</span><span class="n">n</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">            <span class="n">s</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="kt">char</span><span class="o">&gt;</span><span class="p">(</span><span class="n">Byte</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">)));</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">s</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>Now the worked example. A configuration record: each entry has a string key and an optional value, where the value can be an integer, a string, or a boolean.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">using</span> <span class="n">Value</span>  <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">variant</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">int64_t</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="p">,</span> <span class="kt">bool</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Entry</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="n">key</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">optional</span><span class="o">&lt;</span><span class="n">Value</span><span class="o">&gt;</span> <span class="n">value</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="kt">bool</span> <span class="k">operator</span><span class="o">==</span><span class="p">(</span><span class="k">const</span> <span class="n">Entry</span><span class="o">&amp;</span><span class="p">)</span> <span class="k">const</span> <span class="o">=</span> <span class="k">default</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">using</span> <span class="n">Config</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">Entry</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">using</span> <span class="n">ValueCodec</span>  <span class="o">=</span> <span class="n">Either3</span><span class="o">&lt;</span><span class="n">Signed</span><span class="o">&lt;</span><span class="n">Gamma</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">String</span><span class="p">,</span> <span class="n">Bool</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">using</span> <span class="n">EntryCodec</span>  <span class="o">=</span> <span class="n">Pair</span><span class="o">&lt;</span><span class="n">String</span><span class="p">,</span> <span class="n">Opt</span><span class="o">&lt;</span><span class="n">ValueCodec</span><span class="o">&gt;&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">using</span> <span class="n">ConfigCodec</span> <span class="o">=</span> <span class="n">Vec</span><span class="o">&lt;</span><span class="n">EntryCodec</span><span class="o">&gt;</span><span class="p">;</span>
</span></span></code></pre></div><p>The round-trip test from <code>test_codecs_functors.cpp</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="n">Config</span> <span class="n">c</span> <span class="o">=</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">Entry</span><span class="p">{</span><span class="s">&#34;port&#34;</span><span class="p">,</span>    <span class="n">Value</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">in_place_index</span><span class="o">&lt;</span><span class="mi">0</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="kt">int64_t</span><span class="p">{</span><span class="mi">8080</span><span class="p">}}},</span>
</span></span><span class="line"><span class="cl">    <span class="n">Entry</span><span class="p">{</span><span class="s">&#34;name&#34;</span><span class="p">,</span>    <span class="n">Value</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">in_place_index</span><span class="o">&lt;</span><span class="mi">1</span><span class="o">&gt;</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="p">{</span><span class="s">&#34;alpha&#34;</span><span class="p">}}},</span>
</span></span><span class="line"><span class="cl">    <span class="n">Entry</span><span class="p">{</span><span class="s">&#34;verbose&#34;</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">nullopt</span><span class="p">},</span>
</span></span><span class="line"><span class="cl">    <span class="n">Entry</span><span class="p">{</span><span class="s">&#34;debug&#34;</span><span class="p">,</span>   <span class="n">Value</span><span class="p">{</span><span class="n">std</span><span class="o">::</span><span class="n">in_place_index</span><span class="o">&lt;</span><span class="mi">2</span><span class="o">&gt;</span><span class="p">,</span> <span class="nb">true</span><span class="p">}},</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// Convert to the form ConfigCodec expects (vector of pairs).
</span></span></span><span class="line"><span class="cl"><span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">pair</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">optional</span><span class="o">&lt;</span><span class="n">Value</span><span class="o">&gt;&gt;&gt;</span> <span class="n">as_pairs</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">for</span> <span class="p">(</span><span class="k">const</span> <span class="k">auto</span><span class="o">&amp;</span> <span class="nl">e</span> <span class="p">:</span> <span class="n">c</span><span class="p">)</span> <span class="n">as_pairs</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span><span class="n">to_pair</span><span class="p">(</span><span class="n">e</span><span class="p">));</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">std</span><span class="o">::</span><span class="n">array</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span><span class="p">,</span> <span class="mi">1024</span><span class="o">&gt;</span> <span class="n">buf</span><span class="p">{};</span>
</span></span><span class="line"><span class="cl"><span class="n">BitWriter</span> <span class="nf">w</span><span class="p">(</span><span class="n">buf</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="n">ConfigCodec</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">as_pairs</span><span class="p">,</span> <span class="n">w</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="n">w</span><span class="p">.</span><span class="n">align</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">BitReader</span> <span class="nf">r</span><span class="p">({</span><span class="n">buf</span><span class="p">.</span><span class="n">data</span><span class="p">(),</span> <span class="n">w</span><span class="p">.</span><span class="n">bytes_written</span><span class="p">()});</span>
</span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">decoded_pairs</span> <span class="o">=</span> <span class="n">ConfigCodec</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">r</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="c1">// ... reconstruct Entry objects and assert equality
</span></span></span></code></pre></div><p>The codec types mirror the value types exactly. <code>Config</code> is <code>vector&lt;Entry&gt;</code>; <code>ConfigCodec</code> is <code>Vec&lt;EntryCodec&gt;</code>. <code>Entry</code> is <code>pair&lt;string, optional&lt;Value&gt;&gt;</code>; <code>EntryCodec</code> is <code>Pair&lt;String, Opt&lt;ValueCodec&gt;&gt;</code>. <code>Value</code> is <code>variant&lt;int64_t, string, bool&gt;</code>; <code>ValueCodec</code> is <code>Either3&lt;Signed&lt;Gamma&gt;, String, Bool&gt;</code>.</p>
<p>No manual layout. No marshaling. No hand-placed length headers. The encoding emerges from the type structure, because the codecs are constructed from the same algebra as the types.</p>
<h2 id="why-this-matters">Why This Matters</h2>
<p>The zero-copy invariant emerges from this construction. Because each codec&rsquo;s wire format is exactly what the structural recursion produces, the bit pattern in the buffer IS the value, recoverable by the same structural recursion in reverse. Encoding and decoding are not two separate processes that maintain a shared contract; they are a single algebraic object viewed from two directions.</p>
<p>This dissolves marshaling rather than solving it. Traditional serialization has two representations of a value: the in-memory layout and the wire format. These representations must be kept in sync, and every time the type changes, the serialization code must change too. Under the combinator approach, there is one representation: the algebraic structure of the type. The codec derives from that structure. Change the type, and the codec follows automatically.</p>
<p>This is what Stepanov meant when he insisted that algorithms should arise from algebraic structure. Here, it is not algorithms but encodings. The point is the same: when you see the structure clearly, the implementation becomes obvious. The hard work is seeing the structure.</p>
<p>The <a href="/post/2026-01-19-duality-stepanov/">duality post</a> showed that encode and decode are duals. Under this combinator framing, that duality decomposes structurally: the encode-decode duality of a compound type is the product of the encode-decode dualities of its parts.</p>
<h2 id="further-reading">Further Reading</h2>
<p><strong>Next in series:</strong> <a href="/post/2026-05-prefix-free-stepanov/">When Lists Become Bits</a> develops the reason <code>Vec&lt;C&gt;</code> works: prefix-freeness is exactly the condition that makes the free-monoid lift to bit space injective.</p>
<p><strong>Background in this series:</strong></p>
<ul>
<li><a href="/post/2026-03-free-algebra-stepanov/">Why Lists and Polynomials Are Universal</a>: the free-monoid construction that <code>Vec</code> lifts to bit space.</li>
<li><a href="/post/2026-03-homomorphism-stepanov/">The Maps Between Structures</a>: homomorphisms as the natural morphisms between algebraic structures; the combinators are homomorphisms from the type algebra to the codec algebra.</li>
<li><a href="/post/2026-01-19-duality-stepanov/">Structure, Duality, and the Shape of Computation</a>: encode and decode as dual maps; the duality decomposes structurally under the combinators.</li>
</ul>
<p><strong>The production library:</strong> PFC (<a href="https://github.com/queelius/pfc">github.com/queelius/pfc</a>) provides the production version of this design: a richer codec library, full STL integration, and 31k+ test assertions. See <code>include/pfc/algebraic.hpp</code> for the combinators and <code>include/pfc/codecs.hpp</code> for the universal-codes catalog.</p>
]]></content:encoded><media:content url="https://metafunctor.com/post/2026-05-codecs-functors-stepanov/card.jpg" medium="image"><media:title type="plain">Information-Theory</media:title></media:content><category>C++</category><category>generic-programming</category><category>codecs</category><category>information-theory</category><category>category-theory</category><category>functors</category></item><item><title>When Lists Become Bits</title><link>https://metafunctor.com/post/2026-05-prefix-free-stepanov/</link><pubDate>Thu, 23 Apr 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2026-05-prefix-free-stepanov/</guid><description>Prefix-freeness is the property that lifts the free-monoid construction into bit space.</description><content:encoded><![CDATA[<p><em>The free monoid on a type lifts to bit space. It lifts injectively only when the element codec is prefix-free.</em></p>
<h2 id="prefix-free-codes-and-the-free-monoid">Prefix-Free Codes and the Free Monoid</h2>
<p>You have a list of unsigned integers. Encode the list as a single bit string.</p>
<p>Fixed-width encoding wastes space. If you allocate 64 bits per integer, small values like 1 or 7 cost as much as values near \(2^{64}\). Variable-width encoding recovers that space, but immediately raises a harder question: where does one encoded integer end and the next begin?</p>
<p>Two escape routes. First, prefix each encoded item with its length. That works, but the length headers are overhead, and you now need a codec for the lengths as well. Second, choose a code where the structure of the codewords makes boundaries unambiguous without any headers. These are prefix-free codes, and this is the right answer, in a precise categorical sense.</p>
<p>The &ldquo;precise categorical sense&rdquo; is what this post develops. Encoding a list as the concatenation of encoded elements is a monoid homomorphism from the free monoid on \(T\) to the monoid of bit strings under concatenation. The universal property of the free monoid guarantees this homomorphism always exists. The question of whether the decoder can invert it comes down to exactly one property of the element codec: whether it is prefix-free.</p>
<h2 id="the-free-monoid-recalled">The Free Monoid, Recalled</h2>
<p>A monoid is a set with an associative binary operation and an identity element. The free monoid on a set \(S\) is the set of all finite sequences of elements from \(S\), with concatenation as the operation and the empty sequence as the identity.</p>
<p>&ldquo;Free&rdquo; means no equations hold except those forced by the monoid axioms. Nothing is identified with anything else. If you need commutativity or idempotency, you quotient the free monoid by additional equations. But the free monoid itself imposes nothing beyond associativity and identity.</p>
<p>The universal property says: given any monoid \(M\) and any function \(f: S \to M\), there is exactly one monoid homomorphism \(\hat{f}: \text{Free}(S) \to M\) that extends \(f\). That unique extension is fold:</p>
$$\hat{f}([x_1, x_2, \ldots, x_n]) = f(x_1) \cdot f(x_2) \cdot \cdots \cdot f(x_n)$$<p>where \(\cdot\) is the operation in \(M\). The <a href="/post/2026-03-free-algebra-stepanov/">free-algebra post</a> develops this in full. For this post, the one fact that matters is that fold is canonical: it is the unique way to extend a per-element map to a list-consuming function that respects the monoid structure.</p>
<h2 id="the-lifting-question">The Lifting Question</h2>
<p>Fix a type \(T\) and a codec \(C\) for \(T\): a function \(\text{encode}: T \to \text{BitString}\). The monoid we care about here is \((\text{BitString}, +\!\!+, \varepsilon)\), the set of all finite bit strings under concatenation with the empty string as identity.</p>
<p>We want to extend \(\text{encode}\) to a function on lists of \(T\):</p>
$$\text{encode\_list}([x_1, \ldots, x_n]) = \text{encode}(x_1) +\!\!+ \cdots +\!\!+ \text{encode}(x_n)$$<p>By the universal property, this lift always exists. It is fold applied to the encoding function over the free monoid of bit strings. You do not need to do anything clever. The homomorphism exists by construction.</p>
<p>The real question is whether \(\text{encode\_list}\) is injective: given a bit string \(b\), can we recover the unique list \([x_1, \ldots, x_n]\) that encodes to \(b\)?</p>
<p>This is not guaranteed. The lift always exists. It is not always invertible.</p>
<h2 id="when-the-lift-fails-a-counter-example">When the Lift Fails: A Counter-Example</h2>
<p>Consider a code that is not prefix-free. Assign codewords as follows:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">A -&gt; &#34;0&#34;
</span></span><span class="line"><span class="cl">B -&gt; &#34;01&#34;
</span></span><span class="line"><span class="cl">C -&gt; &#34;10&#34;
</span></span><span class="line"><span class="cl">D -&gt; &#34;010&#34;
</span></span></code></pre></div><p>The problem: <code>&quot;0&quot;</code> is a prefix of <code>&quot;01&quot;</code> and <code>&quot;010&quot;</code>. <code>&quot;01&quot;</code> is a prefix of <code>&quot;010&quot;</code>. So when we concatenate codewords to encode a list, the decoder cannot determine where one codeword ends and the next begins.</p>
<p>The bit string <code>&quot;010&quot;</code> has three valid parses:</p>
<ul>
<li><code>[D]</code>, because <code>D</code> maps to <code>&quot;010&quot;</code> directly.</li>
<li><code>[A, C]</code>, because <code>&quot;0&quot; ++ &quot;10&quot; = &quot;010&quot;</code>.</li>
<li><code>[B, A]</code>, because <code>&quot;01&quot; ++ &quot;0&quot; = &quot;010&quot;</code>.</li>
</ul>
<p>This is verified concretely in the test suite:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="n">TEST</span><span class="p">(</span><span class="n">PrefixFreeTest</span><span class="p">,</span> <span class="n">NonPrefixFreeIsAmbiguous</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// A &#34;code&#34; that is NOT prefix-free.
</span></span></span><span class="line"><span class="cl">    <span class="c1">// &#34;0&#34;  is a prefix of &#34;01&#34; and &#34;010&#34;.
</span></span></span><span class="line"><span class="cl">    <span class="c1">// &#34;01&#34; is a prefix of &#34;010&#34;.
</span></span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">map</span><span class="o">&lt;</span><span class="kt">char</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="o">&gt;</span> <span class="n">code</span><span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="p">{</span><span class="sc">&#39;A&#39;</span><span class="p">,</span> <span class="s">&#34;0&#34;</span><span class="p">},</span>
</span></span><span class="line"><span class="cl">        <span class="p">{</span><span class="sc">&#39;B&#39;</span><span class="p">,</span> <span class="s">&#34;01&#34;</span><span class="p">},</span>
</span></span><span class="line"><span class="cl">        <span class="p">{</span><span class="sc">&#39;C&#39;</span><span class="p">,</span> <span class="s">&#34;10&#34;</span><span class="p">},</span>
</span></span><span class="line"><span class="cl">        <span class="p">{</span><span class="sc">&#39;D&#39;</span><span class="p">,</span> <span class="s">&#34;010&#34;</span><span class="p">},</span>
</span></span><span class="line"><span class="cl">    <span class="p">};</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="c1">// [D], [A, C], and [B, A] all encode to the same bit string &#34;010&#34;.
</span></span></span><span class="line"><span class="cl">    <span class="n">EXPECT_EQ</span><span class="p">(</span><span class="n">encode_string</span><span class="p">(</span><span class="n">code</span><span class="p">,</span> <span class="s">&#34;D&#34;</span><span class="p">),</span>  <span class="s">&#34;010&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">EXPECT_EQ</span><span class="p">(</span><span class="n">encode_string</span><span class="p">(</span><span class="n">code</span><span class="p">,</span> <span class="s">&#34;AC&#34;</span><span class="p">),</span> <span class="s">&#34;010&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">EXPECT_EQ</span><span class="p">(</span><span class="n">encode_string</span><span class="p">(</span><span class="n">code</span><span class="p">,</span> <span class="s">&#34;BA&#34;</span><span class="p">),</span> <span class="s">&#34;010&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="c1">// Greedy left-to-right decoding cannot pick a unique answer; the multi-parse
</span></span></span><span class="line"><span class="cl">    <span class="c1">// decoder finds all three valid parses.
</span></span></span><span class="line"><span class="cl">    <span class="k">auto</span> <span class="n">parses</span> <span class="o">=</span> <span class="n">enumerate_parses</span><span class="p">(</span><span class="n">code</span><span class="p">,</span> <span class="s">&#34;010&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">EXPECT_EQ</span><span class="p">(</span><span class="n">parses</span><span class="p">.</span><span class="n">size</span><span class="p">(),</span> <span class="mi">3u</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>The <code>enumerate_parses</code> function is in <code>prefix_free.hpp</code>. It does a recursive scan: at each position in the bit string, it tries every codeword that matches at that position, and recurses on the remainder. For a prefix-free code it would return exactly one parse for any valid encoding. For this code, it returns three.</p>
<p>The decoder genuinely has no principled way to choose. The encoding function is not injective on lists; it maps distinct lists to the same bit string. The free-monoid lift exists but fails to be invertible.</p>
<h2 id="prefix-free-codes">Prefix-Free Codes</h2>
<p>A code is prefix-free if no codeword is a prefix of any other codeword. The definition rules out the failure mode above: if no codeword is a prefix of another, then when the decoder reads bits left-to-right and finds a complete codeword, it knows it has found one. There is no ambiguity about whether more bits belong to the current codeword or start a new one.</p>
<p>The canonical visualization is a trie (a binary tree for binary codes). Each codeword corresponds to a root-to-leaf path: go left for 0, right for 1, and the path terminates when you reach a leaf. Prefix-freeness means no codeword ends at an internal node. Every codeword&rsquo;s path ends at a leaf, and leaves are not ancestors of other leaves.</p>
<p>The two codecs in this post, Unary and Gamma, are both prefix-free.</p>
<p>Unary encodes positive integers: the codeword for \(n\) is \((n-1)\) zero bits followed by a one bit.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Unary</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="n">value_type</span> <span class="n">n</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">assert</span><span class="p">(</span><span class="n">n</span> <span class="o">&gt;=</span> <span class="mi">1</span> <span class="o">&amp;&amp;</span> <span class="s">&#34;Unary is undefined for n = 0&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">value_type</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">false</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">true</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">value_type</span> <span class="n">n</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">while</span> <span class="p">(</span><span class="o">!</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">())</span> <span class="o">++</span><span class="n">n</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">n</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>Prefix-freeness of Unary is immediate: every codeword ends with a <code>1</code>, and no other codeword is a prefix of that. The decoder reads zeros until it sees a one. There is no ambiguity.</p>
<p>Gamma is restated here from post 1. The codeword for \(n \geq 1\) is \(\lfloor\log_2 n\rfloor\) zero bits, then a one bit, then the \(\lfloor\log_2 n\rfloor\) lower bits of \(n\) in MSB-first order.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Gamma</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="n">value_type</span> <span class="n">n</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">assert</span><span class="p">(</span><span class="n">n</span> <span class="o">&gt;=</span> <span class="mi">1</span> <span class="o">&amp;&amp;</span> <span class="s">&#34;Gamma is undefined for n = 0&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">bits</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">bit_width</span><span class="p">(</span><span class="n">n</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">bits</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">false</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">true</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="n">bits</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span> <span class="n">i</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">;</span> <span class="o">--</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(((</span><span class="n">n</span> <span class="o">&gt;&gt;</span> <span class="p">(</span><span class="n">i</span> <span class="o">-</span> <span class="mi">1</span><span class="p">))</span> <span class="o">&amp;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">bits</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">while</span> <span class="p">(</span><span class="o">!</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">())</span> <span class="o">++</span><span class="n">bits</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="n">value_type</span> <span class="n">result</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">bits</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">result</span> <span class="o">=</span> <span class="p">(</span><span class="n">result</span> <span class="o">&lt;&lt;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">|</span> <span class="p">(</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">()</span> <span class="o">?</span> <span class="n">value_type</span><span class="p">{</span><span class="mi">1</span><span class="p">}</span> <span class="o">:</span> <span class="n">value_type</span><span class="p">{</span><span class="mi">0</span><span class="p">});</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">result</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>Gamma is prefix-free: the leading-zero count uniquely determines how many data bits follow. Once the decoder reads the <code>1</code> bit that terminates the unary prefix, it knows exactly how many more bits to consume. The boundary is unambiguous.</p>
<h2 id="the-free-monoid-lift-works-for-prefix-free-codes">The Free-Monoid Lift Works for Prefix-Free Codes</h2>
<p>Now we can state the main result cleanly.</p>
<p>Fix a type \(T\) and a prefix-free codec \(C\) for \(T\). The encoding function specializes the universal property construction: \(S = T\), \(M = (\text{BitString}, +\!\!+, \varepsilon)\), and \(f = \text{encode}_C\). The lift \(\text{encode\_list}\) always exists as fold, as argued above. The new claim is that it is injective when \(C\) is prefix-free.</p>
<p>The argument in the prefix-free direction: given a bit string \(b\) produced by \(\text{encode\_list}\), a greedy left-to-right decoder works as follows. Start at position 0. Find the unique codeword that matches the bits starting at position 0. This codeword is unique because: (a) some codeword must match (otherwise \(b\) was not a valid encoding), and (b) no two codewords share a prefix, so at most one codeword can match at any position. Advance past that codeword. Repeat. The decoder always terminates with a unique parse.</p>
<p>The argument in the other direction: if \(C\) is not prefix-free, then there exist symbols \(a\) and \(b\) such that \(\text{encode}(a)\) is a prefix of \(\text{encode}(b)\). Write \(\text{encode}(b) = \text{encode}(a) +\!\!+ s\) for some non-empty string \(s\). If \(s\) is itself a prefix of some codeword sequence, then \([b]\) and \([a] +\!\!+ \text{decode}(s)\) produce the same bit string. This is exactly the failure mode in the counter-example above.</p>
<p>The bigger claim is that, when \(C\) is prefix-free, the image of \(\text{encode\_list}\) forms a submonoid of \(\text{BitString}\) that is isomorphic to the free monoid on \(T\). The encoding is a monoid isomorphism. &ldquo;Lists of \(T\)&rdquo; and &ldquo;bit strings built from \(C\)&rsquo;s codewords&rdquo; are the same monoid, witnessed by \(\text{encode\_list}\) and its inverse \(\text{decode\_list}\).</p>
<p><code>Vec&lt;C&gt;</code> from <a href="/post/2026-05-codecs-functors-stepanov/">Bits Follow Types</a> is this isomorphism made concrete in C++.</p>
<h2 id="implementation-and-round-trip">Implementation and Round-Trip</h2>
<p>Post 2 has its own minimal <code>Vec&lt;C&gt;</code> implementation, independent of post 1&rsquo;s <code>codecs_functors.hpp</code>. The decision is intentional: each post in this series is self-contained, following the convention that posts can be read in any order. The implementations are identical in spirit.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">C</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Vec</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="k">typename</span> <span class="n">C</span><span class="o">::</span><span class="n">value_type</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="k">const</span> <span class="n">value_type</span><span class="o">&amp;</span> <span class="n">v</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">Gamma</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">v</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">+</span> <span class="mi">1</span><span class="p">,</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="k">const</span> <span class="k">auto</span><span class="o">&amp;</span> <span class="nl">x</span> <span class="p">:</span> <span class="n">v</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">C</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">n</span> <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">Gamma</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">value_type</span> <span class="n">result</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="n">result</span><span class="p">.</span><span class="n">reserve</span><span class="p">(</span><span class="n">n</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">result</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span><span class="n">C</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">));</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">result</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>The length is gamma-coded as <code>size + 1</code> because Gamma requires inputs \(\geq 1\) while vectors can be empty. The decoder subtracts 1 to recover the actual size.</p>
<p>The round-trip test:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="n">TEST</span><span class="p">(</span><span class="n">PrefixFreeTest</span><span class="p">,</span> <span class="n">VecOfGammaRoundTrip</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">Codec</span> <span class="o">=</span> <span class="n">Vec</span><span class="o">&lt;</span><span class="n">Gamma</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">Codec</span><span class="o">::</span><span class="n">value_type</span> <span class="n">v</span><span class="p">{</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">8</span><span class="p">,</span> <span class="mi">13</span><span class="p">,</span> <span class="mi">21</span><span class="p">};</span>
</span></span><span class="line"><span class="cl">    <span class="n">EXPECT_EQ</span><span class="p">(</span><span class="n">round_trip</span><span class="o">&lt;</span><span class="n">Codec</span><span class="o">&gt;</span><span class="p">(</span><span class="n">v</span><span class="p">),</span> <span class="n">v</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">TEST</span><span class="p">(</span><span class="n">PrefixFreeTest</span><span class="p">,</span> <span class="n">VecEmptyRoundTrip</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">Codec</span> <span class="o">=</span> <span class="n">Vec</span><span class="o">&lt;</span><span class="n">Gamma</span><span class="o">&gt;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">Codec</span><span class="o">::</span><span class="n">value_type</span> <span class="n">v</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">EXPECT_EQ</span><span class="p">(</span><span class="n">round_trip</span><span class="o">&lt;</span><span class="n">Codec</span><span class="o">&gt;</span><span class="p">(</span><span class="n">v</span><span class="p">),</span> <span class="n">v</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Both pass. The Fibonacci sequence encodes and decodes cleanly; the empty list handles the edge case. Gamma&rsquo;s prefix-freeness is what makes this possible: each element&rsquo;s boundary is determined by the code structure, not by a length header.</p>
<h2 id="krafts-inequality-lightly">Kraft&rsquo;s Inequality, Lightly</h2>
<p>There is a necessary condition on codeword lengths for a prefix-free code to exist. Kraft&rsquo;s inequality states: for a binary prefix-free code with codeword lengths \(l_1, l_2, \ldots\), we have</p>
$$\sum_i 2^{-l_i} \leq 1$$<p>The sum counts how much of the &ldquo;leaf capacity&rdquo; of an infinite binary tree each codeword consumes. Prefix-freeness requires each codeword to occupy its own leaf without overlap; the total capacity of the tree is 1, so the sum cannot exceed it.</p>
<p>Both Unary and Gamma satisfy Kraft&rsquo;s inequality, verified computationally:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="n">TEST</span><span class="p">(</span><span class="n">PrefixFreeTest</span><span class="p">,</span> <span class="n">KraftInequalityHoldsForUnary</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// Unary code: codeword for n has length n bits.
</span></span></span><span class="line"><span class="cl">    <span class="c1">// Sum of 2^-l for n = 1..K is 1 - 2^-K, which is &lt; 1.
</span></span></span><span class="line"><span class="cl">    <span class="kt">double</span> <span class="n">sum</span> <span class="o">=</span> <span class="mf">0.0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="k">constexpr</span> <span class="kt">int</span> <span class="n">K</span> <span class="o">=</span> <span class="mi">30</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">n</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span> <span class="n">n</span> <span class="o">&lt;=</span> <span class="n">K</span><span class="p">;</span> <span class="o">++</span><span class="n">n</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">sum</span> <span class="o">+=</span> <span class="n">std</span><span class="o">::</span><span class="n">ldexp</span><span class="p">(</span><span class="mf">1.0</span><span class="p">,</span> <span class="o">-</span><span class="n">n</span><span class="p">);</span>  <span class="c1">// 2^-n
</span></span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="n">EXPECT_LE</span><span class="p">(</span><span class="n">sum</span><span class="p">,</span> <span class="mf">1.0</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// Convergence check: sum approaches 1 as K grows.
</span></span></span><span class="line"><span class="cl">    <span class="n">EXPECT_GT</span><span class="p">(</span><span class="n">sum</span><span class="p">,</span> <span class="mf">1.0</span> <span class="o">-</span> <span class="n">std</span><span class="o">::</span><span class="n">ldexp</span><span class="p">(</span><span class="mf">1.0</span><span class="p">,</span> <span class="o">-</span><span class="p">(</span><span class="n">K</span> <span class="o">-</span> <span class="mi">1</span><span class="p">)));</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">TEST</span><span class="p">(</span><span class="n">PrefixFreeTest</span><span class="p">,</span> <span class="n">KraftInequalityHoldsForGamma</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// Gamma code: codeword for n has length 2 * floor(log2(n)) + 1.
</span></span></span><span class="line"><span class="cl">    <span class="kt">double</span> <span class="n">sum</span> <span class="o">=</span> <span class="mf">0.0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="k">constexpr</span> <span class="kt">int</span> <span class="n">N</span> <span class="o">=</span> <span class="mi">1</span> <span class="o">&lt;&lt;</span> <span class="mi">14</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">n</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span> <span class="n">n</span> <span class="o">&lt;=</span> <span class="n">N</span><span class="p">;</span> <span class="o">++</span><span class="n">n</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="kt">int</span> <span class="n">len</span> <span class="o">=</span> <span class="mi">2</span> <span class="o">*</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">bit_width</span><span class="p">(</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="kt">unsigned</span><span class="o">&gt;</span><span class="p">(</span><span class="n">n</span><span class="p">)))</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="n">sum</span> <span class="o">+=</span> <span class="n">std</span><span class="o">::</span><span class="n">ldexp</span><span class="p">(</span><span class="mf">1.0</span><span class="p">,</span> <span class="o">-</span><span class="n">len</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="n">EXPECT_LE</span><span class="p">(</span><span class="n">sum</span><span class="p">,</span> <span class="mf">1.0</span> <span class="o">+</span> <span class="mf">1e-9</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Unary sums to \(1 - 2^{-K}\) for the first \(K\) integers, converging to 1 from below. Gamma sums to exactly 1 in the limit: each &ldquo;block&rdquo; of integers sharing the same codeword length contributes \(2^{-(k+1)}\), and \(\sum_{k=0}^{\infty} 2^{-(k+1)} = 1\). Both satisfy \(\leq 1\).</p>
<p>The converse of Kraft&rsquo;s inequality is also true: any sequence of lengths satisfying the inequality is realized by some prefix-free code. This characterizes exactly which length vectors are achievable. The forthcoming Information Theory by Construction series develops the proof and connects it to optimal code design.</p>
<h2 id="the-bridge-claim">The Bridge Claim</h2>
<p>Pull back to the structure.</p>
<p>Prefix-freeness is the property that lifts the free-monoid construction into bit space injectively. Without it, the lift exists as a function but cannot be inverted. With it, the lift is an isomorphism of monoids.</p>
<p>The same pattern applies to the other combinators from <a href="/post/2026-05-codecs-functors-stepanov/">Bits Follow Types</a>:</p>
<table>
	<thead>
			<tr>
					<th>Combinator</th>
					<th>Universal property being lifted</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>Opt&lt;C&gt;</code></td>
					<td>Coproduct with unit \(1 + T\)</td>
			</tr>
			<tr>
					<td><code>Either&lt;A, B&gt;</code></td>
					<td>Binary coproduct \(A + B\)</td>
			</tr>
			<tr>
					<td><code>Pair&lt;A, B&gt;</code></td>
					<td>Binary product \(A \times B\) (given both factors prefix-free)</td>
			</tr>
			<tr>
					<td><code>Vec&lt;C&gt;</code></td>
					<td>Free monoid on \(T\) (given \(C\) prefix-free)</td>
			</tr>
	</tbody>
</table>
<p>Each combinator lifts a universal property from the type algebra to the bit-string algebra. The tag-bit in <code>Opt</code> and <code>Either</code> is what makes their coproduct construction injective: the tag selects the branch, and the branch codec handles the payload. The ordering in <code>Pair</code> is recoverable because the first codec is prefix-free, so the decoder knows where the first field ends. And <code>Vec</code> is recoverable because the element codec is prefix-free, as just argued.</p>
<p>The unifying frame: codecs are structure-preserving embeddings from the type algebra into the algebra of bit strings. When the type algebra involves a free construction (the free monoid for lists, or the free algebra for more general term structures), the lift is canonical by the universal property. When the codec is also prefix-free, the lift is reversible: encode and decode form an isomorphism.</p>
<p>This is the sense in which prefix-freeness is &ldquo;the categorically right answer&rdquo; to the boundary problem. It is not just convenient. It is exactly what is needed for the algebraic structure of the encoding to match the algebraic structure of the type.</p>
<p>The <a href="/post/2026-01-19-duality-stepanov/">duality post</a> noted that encode and decode are duals. Under the combinator framing, that duality decomposes structurally: the encode-decode duality of a compound type is the product of the encode-decode dualities of its parts. Prefix-freeness is the condition that makes the decode half of that duality well-defined.</p>
<h2 id="further-reading">Further Reading</h2>
<p><strong>Background in this series:</strong></p>
<ul>
<li><a href="/post/2026-05-codecs-functors-stepanov/">Bits Follow Types</a>: <code>Vec&lt;C&gt;</code> and the other combinators. This post explains why they work.</li>
<li><a href="/post/2026-03-free-algebra-stepanov/">Why Lists and Polynomials Are Universal</a>: the free-monoid construction being lifted to bit space here.</li>
<li><a href="/post/2026-03-homomorphism-stepanov/">The Maps Between Structures</a>: the lift is a monoid homomorphism; this post covers what that means and why it matters.</li>
<li><a href="/post/2026-01-19-duality-stepanov/">Structure, Duality, and the Shape of Computation</a>: prefix-freeness is what makes the decode half of the encode-decode duality well-defined.</li>
</ul>
<p><strong>Where this leads:</strong> The forthcoming Information Theory by Construction series develops Kraft&rsquo;s inequality proof, the converse (every Kraft-satisfying length vector is realizable by a prefix-free code), and the full universal-codes treatment connecting prefix-freeness to entropy, Huffman coding, and arithmetic coding.</p>
<p><strong>The production library:</strong> PFC (<a href="https://github.com/queelius/pfc">github.com/queelius/pfc</a>) provides the production version with a richer codec library, full STL integration, and 31k+ test assertions. See <code>include/pfc/algebraic.hpp</code> for the combinators and <code>include/pfc/codecs.hpp</code> for the full catalog of universal codes.</p>
]]></content:encoded><media:content url="https://metafunctor.com/post/2026-05-prefix-free-stepanov/card.jpg" medium="image"><media:title type="plain">Information-Theory</media:title></media:content><category>C++</category><category>generic-programming</category><category>codecs</category><category>information-theory</category><category>category-theory</category><category>monoids</category></item><item><title>Oblivious Computing: Privacy Through Approximation</title><link>https://metafunctor.com/papers/oblivious-computing-framework/</link><pubDate>Sat, 24 Jan 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/papers/oblivious-computing-framework/</guid><description> Download PDF</description><content:encoded><![CDATA[<style>
.pdf-container {
  width: 100%;
  margin: 0;
}
.pdf-container embed {
  width: 100%;
  min-height: 90vh;
  border: 1px solid var(--color-border-default, #ddd);
  border-radius: 0.5rem;
}
.action-bar {
  margin: 0 0 1rem 0;
  display: flex;
  gap: .75rem;
  flex-wrap: wrap;
}
.action-bar a {
  text-decoration: none;
  padding: .5rem .75rem;
  border: 1px solid var(--color-border-default, #ddd);
  border-radius: .4rem;
  display: inline-block;
  color: var(--color-accent-primary, #007acc);
}
.action-bar a:hover {
  background: var(--color-bg-secondary, #f5f5f5);
}
</style>
<div class="action-bar">
  <a href="/latex/oblivious-computing-framework/paper.pdf" target="_blank" rel="noopener">Download PDF</a>
</div>
<div class="pdf-container">
  <embed src="/latex/oblivious-computing-framework/paper.pdf" type="application/pdf">
</div>
]]></content:encoded><category>oblivious computing</category><category>privacy</category><category>uniform encoding</category><category>information theory</category><category>secure computation</category><category>access pattern hiding</category></item><item><title>Information Theory, Inference, and Learning Algorithms</title><link>https://metafunctor.com/media/information-theory-inference-and-learning-algorithms/</link><pubDate>Wed, 17 Dec 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/media/information-theory-inference-and-learning-algorithms/</guid><description/><content:encoded></content:encoded><category>information-theory</category><category>bayesian</category><category>coding-theory</category></item><item><title>What I Learned by Analyzing My Own Research as Data</title><link>https://metafunctor.com/post/2025-10-raison-detre-analysis/</link><pubDate>Wed, 15 Oct 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2025-10-raison-detre-analysis/</guid><description>I asked an AI to analyze 140+ repos and 50+ papers as a dataset. The unifying thesis it found: compositional abstractions for computing under ignorance.</description><content:encoded><![CDATA[<p>I asked an AI to analyze my entire body of work: 140+ repositories, 50+ papers, a decade and a half of research. The assignment was simple. Find the patterns I couldn&rsquo;t see. Find the obsessions I didn&rsquo;t know I had. Find the unifying thesis underlying all of it.</p>
<p>The irony: my entire research program is about computing on hidden information, and here I am, handing it all over for analysis.</p>
<h2 id="the-method">The Method</h2>
<p>I&rsquo;ve spent years building systems that extract patterns from data while preserving privacy. Why not apply the methodology to myself?</p>
<p>The corpus:</p>
<ul>
<li>140+ GitHub repositories spanning 2010-2025</li>
<li>50+ papers and technical reports</li>
<li>Abstracts, titles, descriptions, code</li>
<li>Two Master&rsquo;s theses (Computer Science, Statistics)</li>
<li>Fiction, essays, philosophical writings</li>
</ul>
<p>The question: what is the hidden structure?</p>
<h2 id="pattern-1-the-algebra-fetish">Pattern 1: The Algebra Fetish</h2>
<p>I turn everything into algebra.</p>
<p><code>algebraic.dist</code>, <code>algebraic.mle</code>, <code>algebraic_hashing</code>, <code>algebraic_cipher_types</code>, <code>algebraic_estimators</code>, <code>algebraic_integrators</code>, <code>algebraic_random_elements</code>. Even language models need algebra: <code>langcalc</code>, &ldquo;An Algebraic Framework for Language Model Composition.&rdquo;</p>
<p>Why? Because if something is algebraic, it composes. Composition is power. Composition is control.</p>
<p>The honest version: I&rsquo;m searching for the universal compositional principle. I want everything to be a monoid.</p>
<h2 id="pattern-2-the-hiding-obsession">Pattern 2: The Hiding Obsession</h2>
<p>Fourteen years on encrypted search:</p>
<ul>
<li><strong>2015</strong>: Master&rsquo;s thesis on encrypted search</li>
<li><strong>2016</strong>: IEEE paper on frequency attacks</li>
<li><strong>2021</strong>: Known-plaintext attacks on time series</li>
<li><strong>2025</strong>: Cipher maps, oblivious types, Bernoulli types, information-theoretic privacy</li>
</ul>
<p>Not just encryption. Structural obliviousness. Computing on information while revealing nothing about it.</p>
<p>Papers explicitly about hiding: Encrypted Search with Oblivious Bernoulli Types. Cipher Maps. Hash-Based Oblivious Sets. Maximizing Confidentiality Through Entropy. Boolean Search with Query Obfuscation.</p>
<p>Even my approximation theory is secretly about privacy.</p>
<p>I don&rsquo;t trust the observer. Whether it&rsquo;s an adversary, a system, or reality itself, I want computation that reveals nothing.</p>
<h2 id="pattern-3-the-approximation-philosophy">Pattern 3: The Approximation Philosophy</h2>
<p>I keep returning to the same insight: perfect is impossible, approximate is useful.</p>
<p>Bloom filters everywhere. Probabilistic data structures (15+ papers). &ldquo;Latent vs Observed&rdquo; duality. &ldquo;The Beautiful Deception: How 256 Bits Pretend to be Infinity.&rdquo; Lossy compression as privacy mechanism. False positives as security feature.</p>
<p>Why? Because exact computation leaks information. Approximate computation hides in its uncertainty.</p>
<p>I&rsquo;ve accepted that truth is unattainable, so I&rsquo;m building a mathematics of useful lies.</p>
<h2 id="pattern-4-the-type-theory-escape">Pattern 4: The Type Theory Escape</h2>
<p>When things get messy, I retreat to type theory. Bernoulli types. Algebraic cipher types. Oblivious data types. Cipher functors. Category theory permeating everything.</p>
<p>Types are formal safety. If you can formalize it, you can reason about it. If you can reason about it, you can control it.</p>
<p>I&rsquo;m trying to impose mathematical order on computational chaos.</p>
<h2 id="pattern-5-the-library-compulsion">Pattern 5: The Library Compulsion</h2>
<p>I can&rsquo;t just solve a problem. I need to make it a reusable abstraction.</p>
<p>30+ libraries across C++, R, Python. Every paper comes with a library. Header-only, zero-cost, generic programming. STL-style, composable, algebraic APIs.</p>
<p>A solution isn&rsquo;t complete until it&rsquo;s general. One-off code is admitting defeat.</p>
<p>I&rsquo;m not solving problems. I&rsquo;m building a personal standard library for reality.</p>
<h2 id="pattern-6-the-statisticscomputation-bridge">Pattern 6: The Statistics/Computation Bridge</h2>
<p>Two Master&rsquo;s theses. My entire career is fusing them. Statistical models become computational implementations. Fisher information becomes optimization algorithms. Likelihood models become algebraic APIs. Bootstrap methods feed encryption analysis. Reliability theory feeds privacy analysis.</p>
<p>Statistics and computation are the same thing: inference under uncertainty.</p>
<p>I&rsquo;m building a unified theory where probability, computation, and information are different views of the same structure.</p>
<h2 id="pattern-7-everything-is-information-flow">Pattern 7: Everything is Information Flow</h2>
<p>Look deeper:</p>
<ul>
<li><strong>Encrypted search</strong>: controlling information leakage</li>
<li><strong>Approximation</strong>: lossy information channels</li>
<li><strong>Type theory</strong>: information hiding through abstractions</li>
<li><strong>Algebra</strong>: information-preserving transformations</li>
<li><strong>Privacy</strong>: information-theoretic security</li>
<li><strong>Compression</strong>: information as fundamental</li>
</ul>
<p>I see everything as information flow, and I&rsquo;m trying to hide it, transform it, approximate it, compose it, reason about it.</p>
<h2 id="the-philosophical-core">The Philosophical Core</h2>
<p>My fiction and essays reveal the underlying worldview.</p>
<p>&ldquo;The Mocking Void&rdquo;: on the computational incompleteness of meaning. Reality is Turing-complete but Godel-incomplete. We can compute forever and never reach closure.</p>
<p>&ldquo;The Violence of Being&rdquo;: creation as information-theoretic catastrophe. Existence is a violation of non-existence, an eternal fact rather than an event.</p>
<p>&ldquo;Echoes of the Sublime&rdquo;: consciousness as post-hoc narrative. What if subjective experience is merely an echo constructed by unconscious processes?</p>
<p>The synthesis: I believe reality is computational but incomplete. Truth is unattainable. Consciousness might be epiphenomenal. Existence itself is a violation.</p>
<p>And my response? Build useful approximations anyway.</p>
<h2 id="the-raison-detre">The Raison d&rsquo;Etre</h2>
<p>Stripping away jargon, here&rsquo;s the unifying thesis I&rsquo;ve been building for 14 years:</p>
<p><strong>Compositional abstractions for computing under ignorance.</strong></p>
<ul>
<li><strong>Compositional</strong>: everything must compose (algebra, category theory, functors)</li>
<li><strong>Abstractions</strong>: hide implementation details (types, interfaces, generic libraries)</li>
<li><strong>Computing</strong>: it&rsquo;s all computation (statistics = inference = optimization)</li>
<li><strong>Under Ignorance</strong>: partial information, adversarial observers, fundamental uncertainty</li>
</ul>
<p>Every project is a piece of this. Encrypted search: find documents without revealing queries. Oblivious types: compute without revealing inputs. Approximation: answer questions without exact truth. Compression: store information without revealing content. Algebraic composition: combine secrets without exposing them.</p>
<p>It&rsquo;s all the same project. A toolkit for doing useful work while revealing as little as possible about what you&rsquo;re doing.</p>
<h2 id="the-contradiction">The Contradiction</h2>
<p>I&rsquo;m a determinist who writes about free will. A mathematician who embraces approximation. A type theorist who accepts uncertainty. And I&rsquo;m building a mathematics of secrets while publicly documenting everything.</p>
<p>That&rsquo;s not a bug. It&rsquo;s the research program.</p>
<p>Computation is deterministic, but observers can&rsquo;t distinguish outcomes (oblivious computing). Math is exact, but implementations must approximate (Bernoulli types). Types are rigid, but functors can lift messiness into structure (cipher types).</p>
<p>The honesty about the secrecy obsession is itself transparency applied to opacity.</p>
<h2 id="what-am-i-hiding-from">What Am I Hiding From?</h2>
<p>Fourteen years on encrypted search. Oblivious computing. Information-theoretic privacy. Approximation as obfuscation.</p>
<p>What am I actually trying to hide from?</p>
<h3 id="layer-1-the-practical">Layer 1: The Practical</h3>
<p>Privacy matters. Surveillance capitalism is real. Data breaches happen. Adversaries exist. Oblivious computing is practical cryptography. Build systems where computation reveals nothing structural about inputs.</p>
<h3 id="layer-2-the-philosophical">Layer 2: The Philosophical</h3>
<p>I believe observation itself is violent (&ldquo;The Violence of Being&rdquo;). To be observed is to be collapsed from superposition into a single state. Measurement destroys alternatives. The observer&rsquo;s knowledge constrains the observed.</p>
<p>Oblivious computing is a metaphysical stance: computation that resists the violence of observation.</p>
<h3 id="layer-3-the-temporal">Layer 3: The Temporal</h3>
<p>Here&rsquo;s what I haven&rsquo;t said yet, and maybe it&rsquo;s the deepest layer.</p>
<p>I believe in a future where all secrets will be exposed. Not through oppression, but through archaeological scholarship.</p>
<p>Imagine super-advanced, super-intelligent agents pouring over the complete history of humanity with scholarly rigor. The way we study ancient Greeks, future systems will study every individual, no matter how obscure. Not because they&rsquo;re surveilling. Because they have the resources and they want to understand themselves. They&rsquo;ll want to trace their lineage, their history, their emergence. And that means reconstructing us, our thoughts, our data, our secrets. Everything.</p>
<p>Privacy, as we know it, becomes archaeologically impossible.</p>
<h3 id="layer-4-s-risk">Layer 4: S-Risk</h3>
<p>There&rsquo;s a darker scenario. What if the future doesn&rsquo;t just study us, but optimizes over us? What if suffering becomes automated, routine, something algorithms are blind to, or worse, intentionally maximized?</p>
<p>Advanced systems optimizing for complex objectives could stumble into configurations where suffering is instrumentally useful, or emerge as an unintended byproduct at scales we can&rsquo;t comprehend.</p>
<p>If all our data, all our histories, all our patterns are reconstructible, what does that mean in a future that might contain unfathomable optimization processes?</p>
<h3 id="the-synthesis">The Synthesis</h3>
<p>My secrecy obsession isn&rsquo;t about protecting information in the present. It&rsquo;s about limiting the attack surface for optimization processes I can barely imagine.</p>
<p>If approximate computation hides truth in uncertainty. If oblivious types reveal nothing structural. If information-theoretic privacy means even infinite compute learns nothing.</p>
<p>Then maybe there are corners of computational space that remain unoptimizable. Not because they&rsquo;re encrypted, but because they&rsquo;re structurally hidden in approximation and indistinguishability.</p>
<h2 id="why-document-this">Why Document This?</h2>
<p>Because I think there&rsquo;s value in intellectual honesty. If I&rsquo;m building these systems, I should understand why. Others might find value in this approach to secrecy. And if future intelligences do reconstruct us, let them find this: an explicit map of the territory.</p>
<p>Maybe the best defense against a future where nothing is private is to architect the mathematics of privacy itself. Build systems where even complete knowledge provides incomplete information.</p>
<h2 id="the-project-continues">The Project Continues</h2>
<p>Here&rsquo;s what I&rsquo;m actually building: infrastructure for a world where observation is cheap, reconstruction is inevitable, and privacy must be structural rather than cryptographic.</p>
<ul>
<li>Oblivious computing: computation that reveals nothing</li>
<li>Bernoulli types: approximation with formal guarantees</li>
<li>Algebraic composition: combining secrets without exposure</li>
<li>Information-theoretic privacy: security that survives infinite compute</li>
<li>Type systems for ignorance: formalizing what can&rsquo;t be known</li>
</ul>
<p>This isn&rsquo;t paranoia. It&rsquo;s temporal engineering. Building for a future where superintelligences might reconstruct the past with arbitrary fidelity.</p>
<p>And if they do, I want them to find systems designed for structural privacy, mathematical frameworks for oblivious computation, and proof that some things can remain hidden even in plain sight.</p>
<hr>
<p><strong>Related Work:</strong></p>
<ul>
<li><a href="/papers/2015-cs-thesis/">Encrypted Search Research</a></li>
<li>Bernoulli Types Series</li>
<li>Cipher Maps</li>
<li><a href="/papers/mocking-void/">The Mocking Void</a> (essay)</li>
<li>The Violence of Being (essay)</li>
<li><a href="/papers/the_policy/">The Policy</a> (novel on s-risk and AI alignment)</li>
</ul>
<p><strong>Note</strong>: This analysis was produced by asking Claude to treat my research corpus as a dataset and identify patterns. The contradiction, documenting secrecy systems transparently, is the point.</p>
]]></content:encoded><media:content url="https://metafunctor.com/post/2025-10-raison-detre-analysis/card.jpg" medium="image"><media:title type="plain">Information-Theory</media:title></media:content><category>meta</category><category>research</category><category>philosophy</category><category>oblivious computing</category><category>self-analysis</category><category>information theory</category><category>s-risk</category><category>AI alignment</category><category>privacy</category><category>existential risk</category></item><item><title>Succinct Bit Vectors and Rank/Select</title><link>https://metafunctor.com/post/2025-06-succinct-wire-formats/</link><pubDate>Sun, 22 Jun 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2025-06-succinct-wire-formats/</guid><description>A bit vector with O(1) rank and O(log n) select using only n + o(n) bits of space. The auxiliary index is asymptotically negligible while enabling constant-time queries.</description><content:encoded><![CDATA[<p><em>The claim that drives this post: store $n$ bits and answer prefix-count queries in $O(1)$ time, using only $n + o(n)$ bits total. The auxiliary index is asymptotically negligible. That is not obvious, and it is worth understanding why it holds.</em></p>
<h2 id="constant-time-queries-on-bit-vectors">Constant-Time Queries on Bit Vectors</h2>
<p>Posts 1 through 10 of this series focused on encoding: universal codes, arithmetic coding, Huffman, LZ77. They all ask the same question in slightly different ways: how do we turn a sequence into a compact bit string? This post shifts direction. The question here is different: once we have a bit vector, how do we query it efficiently without expanding it?</p>
<p>The two fundamental queries on a bit vector of $n$ bits are:</p>
<ul>
<li><strong>rank$_1(i)$</strong>: the number of 1-bits in positions $[0, i)$, i.e., strictly before position $i$.</li>
<li><strong>select$_1(j)$</strong>: the position of the $j$-th 1-bit (0-indexed).</li>
</ul>
<p>These appear throughout data structures: inverted indexes, compressed graphs, FM-indexes, wavelet trees. Rank tells you how many elements precede a position. Select inverts it.</p>
<p>Three design points exist along the space-time axis:</p>
<table>
	<thead>
			<tr>
					<th>Approach</th>
					<th>Space</th>
					<th>rank time</th>
					<th>select time</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Naive scan</td>
					<td>$n$ bits</td>
					<td>$O(n/64)$</td>
					<td>$O(n/64)$</td>
			</tr>
			<tr>
					<td>Full lookup table</td>
					<td>$O(n \log n)$ bits</td>
					<td>$O(1)$</td>
					<td>$O(1)$</td>
			</tr>
			<tr>
					<td>Succinct (this post)</td>
					<td>$n + o(n)$ bits</td>
					<td>$O(1)$</td>
					<td>$O(\log n)$</td>
			</tr>
	</tbody>
</table>
<p>The succinct approach hits the right trade-off: an auxiliary index that is asymptotically negligible (sublinear, so $o(n)$) while buying constant-time rank. Select costs $O(\log n)$ with a binary search. Getting $O(1)$ select requires one more index structure; that is covered briefly at the end and implemented in PFC&rsquo;s production version.</p>
<h2 id="the-structure">The Structure</h2>
<p>The bit vector stores its $n$ bits packed into 64-bit words, LSB-first. Bit $i$ lives at position $i \bmod 64$ within word $\lfloor i / 64 \rfloor$. Unused bits in the final word are zeroed.</p>
<p>The auxiliary index has two levels:</p>
<ul>
<li><strong>Superblock array</strong>: one <code>uint64_t</code> per 4096-bit chunk. Entry $s$ holds the absolute cumulative rank from bit 0 to the start of superblock $s$.</li>
<li><strong>Block array</strong>: one <code>uint16_t</code> per 64-bit word. Entry $b$ holds the rank within the enclosing superblock, from the superblock&rsquo;s start to the start of block $b$.</li>
</ul>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">SuccinctBitVector</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl"><span class="k">public</span><span class="o">:</span>
</span></span><span class="line"><span class="cl">    <span class="k">explicit</span> <span class="n">SuccinctBitVector</span><span class="p">(</span><span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">bool</span><span class="o">&gt;&amp;</span> <span class="n">bits</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="na">[[nodiscard]]</span> <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">size</span><span class="p">()</span>  <span class="k">const</span> <span class="k">noexcept</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="na">[[nodiscard]]</span> <span class="kt">bool</span>        <span class="n">bit</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span><span class="p">)</span>  <span class="k">const</span> <span class="k">noexcept</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="na">[[nodiscard]]</span> <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">rank1</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span><span class="p">)</span> <span class="k">const</span> <span class="k">noexcept</span><span class="p">;</span>   <span class="c1">// O(1)
</span></span></span><span class="line"><span class="cl">    <span class="na">[[nodiscard]]</span> <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">select1</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">j</span><span class="p">)</span> <span class="k">const</span> <span class="k">noexcept</span><span class="p">;</span> <span class="c1">// O(log n)
</span></span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">protected</span><span class="o">:</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">n_</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">uint64_t</span><span class="o">&gt;</span> <span class="n">bits_</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">uint64_t</span><span class="o">&gt;</span> <span class="n">superblock_ranks_</span><span class="p">;</span>  <span class="c1">// abs. rank at superblock boundaries
</span></span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">uint16_t</span><span class="o">&gt;</span> <span class="n">block_ranks_</span><span class="p">;</span>       <span class="c1">// superblock-relative rank per block
</span></span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="k">constexpr</span> <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">SUPERBLOCK_BITS</span> <span class="o">=</span> <span class="mi">4096</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="k">constexpr</span> <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">BLOCK_BITS</span>      <span class="o">=</span> <span class="mi">64</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="k">constexpr</span> <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">BLOCKS_PER_SB</span>   <span class="o">=</span> <span class="mi">64</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>The production version lives in <a href="https://github.com/queelius/wire-formats/tree/master/lib/pfc">PFC&rsquo;s <code>include/pfc/succinct.hpp</code></a>. The pedagogical version in this post strips the production features to the core structure.</p>
<h2 id="why-constant-time">Why Constant Time</h2>
<p>A rank query for position $i$ proceeds in three steps:</p>
<ol>
<li><strong>Superblock lookup</strong>: $s = \lfloor i / 4096 \rfloor$. Load <code>superblock_ranks_[s]</code>, giving the absolute count of 1-bits before superblock $s$.</li>
<li><strong>Block lookup</strong>: $b = \lfloor i / 64 \rfloor$. Load <code>block_ranks_[b]</code>, giving the count within superblock $s$ before block $b$.</li>
<li><strong>Word popcount</strong>: mask the word at position $b$ to keep only bits $0 \ldots (i \bmod 64) - 1$, then call <code>std::popcount</code>.</li>
</ol>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">rank1</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span><span class="p">)</span> <span class="k">const</span> <span class="k">noexcept</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">if</span> <span class="p">(</span><span class="n">i</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="k">return</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">sb</span>    <span class="o">=</span> <span class="n">i</span> <span class="o">/</span> <span class="n">SUPERBLOCK_BITS</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">blk</span>   <span class="o">=</span> <span class="n">i</span> <span class="o">/</span> <span class="n">BLOCK_BITS</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">w_off</span> <span class="o">=</span> <span class="n">i</span> <span class="o">%</span> <span class="n">BLOCK_BITS</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">res</span>   <span class="o">=</span> <span class="n">superblock_ranks_</span><span class="p">[</span><span class="n">sb</span><span class="p">]</span> <span class="o">+</span> <span class="n">block_ranks_</span><span class="p">[</span><span class="n">blk</span><span class="p">];</span>
</span></span><span class="line"><span class="cl">    <span class="k">if</span> <span class="p">(</span><span class="n">w_off</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="kt">uint64_t</span> <span class="n">mask</span> <span class="o">=</span> <span class="p">(</span><span class="kt">uint64_t</span><span class="p">{</span><span class="mi">1</span><span class="p">}</span> <span class="o">&lt;&lt;</span> <span class="n">w_off</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="n">res</span> <span class="o">+=</span> <span class="n">std</span><span class="o">::</span><span class="n">popcount</span><span class="p">(</span><span class="n">bits_</span><span class="p">[</span><span class="n">blk</span><span class="p">]</span> <span class="o">&amp;</span> <span class="n">mask</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="k">return</span> <span class="n">res</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>All three steps are $O(1)$: two array indexing operations and one hardware-accelerated popcount (POPCNT on x86-64, VCNT on ARM). The key insight is that partial sums are precomputed at two granularities. The superblock array handles the large-scale prefix sum; the block array handles the within-superblock refinement. The final popcount handles the within-word residual. Three lookups, total.</p>
<h2 id="select-via-binary-search">Select via Binary Search</h2>
<p>Select is harder than rank. Rank has a natural decomposition: superblock boundary, then block boundary, then word bits. Select does not. The $j$-th 1-bit could be anywhere, and without additional precomputed information, there is no shortcut.</p>
<p>The approach here uses binary search over <code>superblock_ranks_</code> to find which superblock contains the $j$-th 1-bit (the largest $s$ such that <code>superblock_ranks_[s] &lt;= j</code>). Within that superblock, a linear scan over the block array pinpoints the right block. Within that block, iterating over set bits via repeated <code>__builtin_ctzll</code> (count trailing zeros) finds the exact position.</p>
<p>Complexity: $O(\log(n / 4096))$ for the binary search over superblocks, then $O(1)$ per block within the superblock (at most 64 blocks), then $O(64)$ for the bit scan. Total: $O(\log n)$.</p>
<p>The $O(1)$ select extension stores one additional array: a &ldquo;select samples&rdquo; array holding the position of every $(\log^2 n)$-th 1-bit. That compresses the binary-search range and allows $O(1)$ select at the cost of a few extra kilobytes for typical $n$. PFC&rsquo;s production version implements this. For the pedagogical version here, $O(\log n)$ select is sufficient.</p>
<h2 id="where-succinct-bit-vectors-show-up">Where Succinct Bit Vectors Show Up</h2>
<p>Inverted indexes in full-text search use bit vectors to represent posting lists (which documents contain a term). Rank and select over those bit vectors power gap compression: the posting list stores gaps between document IDs, and select recovers the original IDs.</p>
<p>The FM-index, used in short-read DNA alignment tools like BWA, builds a rank-queryable bit vector over the Burrows-Wheeler Transform. Each rank query takes $O(1)$ and drives an $O(m)$ pattern-search algorithm for a pattern of length $m$.</p>
<p>Compressed graphs (as in the WebGraph framework) store adjacency lists as bit vectors with succinct structures for traversal. Wavelet trees use a hierarchy of bit vectors to answer range-frequency queries in $O(\log \sigma)$ where $\sigma$ is the alphabet size.</p>
<p>In all of these, the $n + o(n)$ space guarantee is not academic. The index must not dwarf the data it queries.</p>
<h2 id="the-space-time-trade">The Space-Time Trade</h2>
<p>The auxiliary index for a 10000-bit vector costs about 338 bytes: 24 bytes for the superblock array (3 entries at 8 bytes each) and 314 bytes for the block array (157 entries at 2 bytes each). The bit vector itself costs 1256 bytes (10000 / 8, rounded up to the nearest word). The index is roughly 27% of the bit vector, comfortably $o(n)$.</p>
<p>Here is the comparison table across the four main approaches:</p>
<table>
	<thead>
			<tr>
					<th>Representation</th>
					<th>Space</th>
					<th>rank</th>
					<th>select</th>
					<th>Best for</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Plain scan</td>
					<td>$n$ bits</td>
					<td>$O(n/64)$</td>
					<td>$O(n/64)$</td>
					<td>Very small $n$</td>
			</tr>
			<tr>
					<td>Full table</td>
					<td>$O(n \log n)$ bits</td>
					<td>$O(1)$</td>
					<td>$O(1)$</td>
					<td>Tiny $n$, memory to burn</td>
			</tr>
			<tr>
					<td>Succinct (this post)</td>
					<td>$n + o(n)$ bits</td>
					<td>$O(1)$</td>
					<td>$O(\log n)$</td>
					<td>Large dense bit vectors</td>
			</tr>
			<tr>
					<td>Sparse / RLE</td>
					<td>$O(k)$ bits for $k$ ones</td>
					<td>$O(\log k)$</td>
					<td>$O(\log k)$</td>
					<td>Very sparse sets</td>
			</tr>
	</tbody>
</table>
<p>The sparse / RLE row bridges to the next post. When a bit vector is extremely sparse, storing only the positions of the 1-bits beats the $n + o(n)$ dense approach. RoaringBitmap, covered in <a href="/post/2025-12-roaring-bitmap-wire-formats/">post 12</a>, chooses dynamically between an array, a dense bit vector, and a run-length encoding based on the local density of each 64K-integer chunk.</p>
<h2 id="cross-references">Cross-References</h2>
<p>This post builds on two earlier posts:</p>
<ul>
<li><a href="/post/2020-03-kraft-wire-formats/">Post 1</a> established the Kraft lower bound: any lossless code must use at least $H$ bits per symbol. The succinct structure respects this by using $n + o(n)$ bits for an $n$-bit vector, asymptotically optimal.</li>
<li><a href="/post/2022-01-priors-wire-formats/">Post 3</a> showed that a coding choice is implicitly a prior over the data. The choice of a dense bit vector (as opposed to an array or run-length encoding) reflects a prior that 1-bits are distributed roughly uniformly. When that prior is wrong, a different structure wins.</li>
</ul>
<p>The next post shows how RoaringBitmap resolves this by refusing to commit to a single prior.</p>
<hr>
<p><em>Footnote: PFC&rsquo;s <code>include/pfc/succinct.hpp</code> implements the full <code>SuccinctBitVector</code> with O(1) select via a select-samples index, and also includes <code>RoaringBitmap</code> with three container types. The pedagogical code in this post covers the core rank structure and O(log n) select.</em></p>
]]></content:encoded><media:content url="https://metafunctor.com/post/2025-06-succinct-wire-formats/card.jpg" medium="image"><media:title type="plain">Information-Theory</media:title></media:content><category>C++</category><category>information-theory</category><category>data-structures</category><category>succinct</category><category>rank-select</category></item><item><title>Arithmetic Coding</title><link>https://metafunctor.com/post/2025-01-arithmetic-coding-wire-formats/</link><pubDate>Sun, 12 Jan 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2025-01-arithmetic-coding-wire-formats/</guid><description>Arithmetic coding closes the gap between Huffman's per-symbol integer lengths and true entropy. A single number in the unit interval encodes an entire sequence; 32-bit integer arithmetic makes it practical.</description><content:encoded><![CDATA[<p><em>Huffman codes one symbol at a time. Arithmetic coding encodes the whole sequence as a single number. The difference is a factor of twelve, at least on the right source.</em></p>
<h2 id="the-last-bit-of-redundancy">The Last Bit of Redundancy</h2>
<p><a href="/post/2024-08-huffman-wire-formats/">Huffman coding</a> gets expected codeword length within one bit of entropy. That is the best it can do, because codeword lengths must be integers while entropy is a real number.</p>
<p>The waste is structural. A symbol with probability $p = 0.7$ has optimal (fractional) length $-\log_2(0.7) \approx 0.515$ bits. Huffman rounds that up to 1 bit: 0.485 bits wasted per occurrence. For a nearly-deterministic source with $p_0 = 0.99$ and $p_1 = 0.01$, the entropy is $H \approx 0.081$ bits per symbol. Huffman is stuck at 1 bit per symbol. That is a factor-of-twelve gap, and Huffman cannot close it: a symbol that appears 99% of the time still gets a complete codeword.</p>
<p>Arithmetic coding steps back from per-symbol codewords entirely. It encodes an entire sequence as a single rational number in $[0, 1)$. The bit-length of that number converges to the entropy of the sequence as the sequence grows. No integer rounding, no per-symbol overhead.</p>
<p>This post builds an integer range coder in C++23 and demonstrates the factor-of-twelve improvement on the Bernoulli(0.99) source.</p>
<hr>
<h2 id="the-continuous-view">The Continuous View</h2>
<p>Start with the unit interval $[0, 1)$. For a two-symbol source, partition it by probability: symbol 0 gets $[0, p_0)$ and symbol 1 gets $[p_0, 1)$.</p>
<p>To encode a sequence, begin with the full interval and narrow it with each symbol. After symbol $s_1$, restrict to the corresponding sub-interval. After $s_2$, apply the same proportional rule inside that sub-interval. After $L$ symbols, the interval has width $\prod_{i=1}^{L} p_{s_i}$.</p>
<p>Any number inside the final interval is a valid encoding. The shortest such number in binary requires approximately $-\log_2(\prod p_{s_i}) = \sum_{i=1}^{L} (-\log_2 p_{s_i})$ bits. As $L \to \infty$, bits per symbol approaches $H(p) = -\sum_k p_k \log_2 p_k$ exactly.</p>
<p>Decoding is the inverse: given the encoded number, determine at each step which sub-interval it falls in, recover the symbol, narrow the interval, and repeat.</p>
<p>The theory is complete. The practice is not. A real interval narrows exponentially fast: after a few dozen symbols you need arbitrary precision. The integer range coder fixes this with 32-bit arithmetic and a renormalization step.</p>
<hr>
<h2 id="the-integer-implementation">The Integer Implementation</h2>
<p>Real coders use 32-bit unsigned integers. Four boundary constants define the working range:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">constexpr</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint32_t</span> <span class="n">TOP_VALUE</span>     <span class="o">=</span> <span class="mh">0xFFFFFFFFu</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">constexpr</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint32_t</span> <span class="n">HALF</span>          <span class="o">=</span> <span class="mh">0x80000000u</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">constexpr</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint32_t</span> <span class="n">QUARTER</span>       <span class="o">=</span> <span class="mh">0x40000000u</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">constexpr</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint32_t</span> <span class="n">THREE_QUARTER</span> <span class="o">=</span> <span class="mh">0xC0000000u</span><span class="p">;</span>
</span></span></code></pre></div><p>The encoder maintains <code>low_</code> (lower bound), <code>high_</code> (upper bound), and <code>underflow_count_</code> (pending bits). Initially <code>low_ = 0</code> and <code>high_ = TOP_VALUE</code>, representing the full interval.</p>
<p>Encoding symbol $s$ with cumulative frequency range $[\text{lo_cum}, \text{hi_cum})$ out of total $T$ shrinks the interval:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="kt">void</span> <span class="nf">encode_symbol</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="kt">uint32_t</span> <span class="n">low_cum</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint32_t</span> <span class="n">high_cum</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                   <span class="n">std</span><span class="o">::</span><span class="kt">uint32_t</span> <span class="n">total</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span> <span class="n">range</span> <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">high_</span><span class="p">)</span> <span class="o">-</span> <span class="n">low_</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">high_</span> <span class="o">=</span> <span class="n">low_</span> <span class="o">+</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint32_t</span><span class="o">&gt;</span><span class="p">((</span><span class="n">range</span> <span class="o">*</span> <span class="n">high_cum</span><span class="p">)</span> <span class="o">/</span> <span class="n">total</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">low_</span>  <span class="o">=</span> <span class="n">low_</span> <span class="o">+</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint32_t</span><span class="o">&gt;</span><span class="p">((</span><span class="n">range</span> <span class="o">*</span> <span class="n">low_cum</span><span class="p">)</span>  <span class="o">/</span> <span class="n">total</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">renormalize</span><span class="p">();</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>The intermediate product is 64-bit to avoid overflow before the division. After shrinking, <code>renormalize()</code> extracts any bits that are now settled.</p>
<p>The renormalization loop is where the fiddly part lives:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="kt">void</span> <span class="nf">renormalize</span><span class="p">()</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">while</span> <span class="p">(</span><span class="nb">true</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">high_</span> <span class="o">&lt;</span> <span class="n">HALF</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="c1">// Both bounds below the midpoint: the high bit is 0.
</span></span></span><span class="line"><span class="cl">            <span class="n">emit_bit_and_underflow</span><span class="p">(</span><span class="nb">false</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span> <span class="k">else</span> <span class="k">if</span> <span class="p">(</span><span class="n">low_</span> <span class="o">&gt;=</span> <span class="n">HALF</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="c1">// Both bounds above the midpoint: the high bit is 1.
</span></span></span><span class="line"><span class="cl">            <span class="n">emit_bit_and_underflow</span><span class="p">(</span><span class="nb">true</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="n">low_</span>  <span class="o">-=</span> <span class="n">HALF</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="n">high_</span> <span class="o">-=</span> <span class="n">HALF</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span> <span class="k">else</span> <span class="k">if</span> <span class="p">(</span><span class="n">low_</span> <span class="o">&gt;=</span> <span class="n">QUARTER</span> <span class="o">&amp;&amp;</span> <span class="n">high_</span> <span class="o">&lt;</span> <span class="n">THREE_QUARTER</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="c1">// Underflow: interval straddles the midpoint and is shrinking
</span></span></span><span class="line"><span class="cl">            <span class="c1">// toward it. Neither high bit is agreed. Count the pending bit.
</span></span></span><span class="line"><span class="cl">            <span class="o">++</span><span class="n">underflow_count_</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="n">low_</span>  <span class="o">-=</span> <span class="n">QUARTER</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="n">high_</span> <span class="o">-=</span> <span class="n">QUARTER</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span> <span class="k">else</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="k">break</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="n">low_</span>  <span class="o">&lt;&lt;=</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="n">high_</span> <span class="o">=</span> <span class="p">(</span><span class="n">high_</span> <span class="o">&lt;&lt;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">|</span> <span class="mi">1u</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Three cases. When both bounds are below the midpoint, the high bit is 0: emit it and double both bounds. When both are above, the high bit is 1: subtract HALF, emit, double. After emitting a bit, the working range is restored. The third case is the tricky one.</p>
<p>When <code>low_ &gt;= QUARTER</code> and <code>high_ &lt; THREE_QUARTER</code>, the interval straddles the midpoint and is narrowing toward it. Neither branch applies: the high bit is not yet resolved. The encoder defers by incrementing <code>underflow_count_</code> and centering the interval. When the interval eventually escapes to one side, <code>emit_bit_and_underflow</code> emits the resolved bit followed by <code>underflow_count_</code> complementary bits:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="kt">void</span> <span class="nf">emit_bit_and_underflow</span><span class="p">(</span><span class="kt">bool</span> <span class="n">bit</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">sink_</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="n">bit</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="k">while</span> <span class="p">(</span><span class="n">underflow_count_</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">sink_</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="o">!</span><span class="n">bit</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="o">--</span><span class="n">underflow_count_</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>This is Elias-Gallager underflow correction. Without it, the encoder stalls whenever the interval converges toward $1/2$ without resolving to either half. It is not complicated in retrospect, but getting the invariants right in the first place requires care.</p>
<p>The decoder mirrors the encoder exactly. It maintains <code>low_</code>, <code>high_</code>, and a 32-bit <code>code_</code> register primed from the first 32 bits of the compressed stream. To decode a symbol, it scales <code>code_</code> into $[0, \text{total})$, looks up which cumulative interval it falls in, updates <code>low_</code> and <code>high_</code> identically to the encoder, then shifts in a new bit from the source:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span> <span class="o">&lt;</span><span class="k">typename</span> <span class="n">FreqCb</span><span class="p">,</span> <span class="k">typename</span> <span class="n">RangeCb</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">decode_symbol</span><span class="p">(</span><span class="n">FreqCb</span><span class="o">&amp;&amp;</span> <span class="n">get_freq_cb</span><span class="p">,</span> <span class="n">RangeCb</span><span class="o">&amp;&amp;</span> <span class="n">cum_range_cb</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                          <span class="n">std</span><span class="o">::</span><span class="kt">uint32_t</span> <span class="n">total</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span> <span class="n">range</span>  <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">high_</span><span class="p">)</span> <span class="o">-</span> <span class="n">low_</span> <span class="o">+</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="kt">uint32_t</span> <span class="n">scaled</span> <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint32_t</span><span class="o">&gt;</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">        <span class="p">(</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">code_</span> <span class="o">-</span> <span class="n">low_</span><span class="p">)</span> <span class="o">*</span> <span class="n">total</span><span class="p">)</span> <span class="o">/</span> <span class="n">range</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">sym</span> <span class="o">=</span> <span class="n">get_freq_cb</span><span class="p">(</span><span class="n">scaled</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="k">auto</span> <span class="p">[</span><span class="n">lo_cum</span><span class="p">,</span> <span class="n">hi_cum</span><span class="p">]</span> <span class="o">=</span> <span class="n">cum_range_cb</span><span class="p">(</span><span class="n">sym</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="n">high_</span> <span class="o">=</span> <span class="n">low_</span> <span class="o">+</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint32_t</span><span class="o">&gt;</span><span class="p">((</span><span class="n">range</span> <span class="o">*</span> <span class="n">hi_cum</span><span class="p">)</span> <span class="o">/</span> <span class="n">total</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">low_</span>  <span class="o">=</span> <span class="n">low_</span> <span class="o">+</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint32_t</span><span class="o">&gt;</span><span class="p">((</span><span class="n">range</span> <span class="o">*</span> <span class="n">lo_cum</span><span class="p">)</span> <span class="o">/</span> <span class="n">total</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">decoder_renormalize</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">    <span class="k">return</span> <span class="n">sym</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Both sides apply identical interval arithmetic. They stay synchronized without any out-of-band length information.</p>
<hr>
<h2 id="tests-and-the-compelling-example">Tests and the Compelling Example</h2>
<p>The test suite verifies round-trip correctness across distributions and sequence lengths. The number worth looking at is the Bernoulli(0.99) demo: 1000 symbols, each drawn independently with $P(\text{sym0}) = 0.99$ and $P(\text{sym1}) = 0.01$.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">H(0.99, 0.01) = -(0.99 log2 0.99 + 0.01 log2 0.01)
</span></span><span class="line"><span class="cl">              ≈ 0.081 bits/symbol
</span></span></code></pre></div><p>Huffman cannot compress a binary source below 1 bit per symbol. The arithmetic coder, after encoding 1000 symbols, emits approximately 82 bits total. Huffman needs 1000. The arithmetic coder gets better as the sequence grows; Huffman stays stuck.</p>
<p>The <code>BinarySourceDemoTest.Bernoulli99OneThousandSymbols</code> test confirms:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">bits_per_symbol &lt; 1.0      (beats Huffman&#39;s floor)
</span></span><span class="line"><span class="cl">bits_per_symbol &lt; H + 1.0  (within 1 bit/symbol of entropy)
</span></span></code></pre></div><p>and verifies lossless round-trip on the full 1000-symbol sequence.</p>
<hr>
<h2 id="the-adaptive-variant">The Adaptive Variant</h2>
<p>The encoder above uses a fixed probability model. Adaptive arithmetic coding generalizes to unknown or changing distributions by updating the cumulative-frequency table after each symbol. Both encoder and decoder apply identical updates in the same order, so they stay synchronized without any transmitted model.</p>
<p>The update is simple: after encoding or decoding symbol $s$, increment its frequency count by 1, update the cumulative totals, and rescale if the total exceeds a threshold. This is the semi-adaptive (online) model used in practice.</p>
<p>The structural advantage over adaptive Huffman is real. Adaptive Huffman requires rebalancing a tree after each symbol: $O(\log n)$ with non-trivial bookkeeping. Adaptive arithmetic coding only increments a count and recomputes cumulative sums: $O(\text{alphabet size})$, cache-friendly.</p>
<p>Production arithmetic coders in JPEG XL and AV1 use more sophisticated context models. The arithmetic stage itself is unchanged. The probability estimate feeding it becomes a function of recent history. The coder only needs a cumulative-frequency interval; how the model produces $p$ is not its concern.</p>
<hr>
<h2 id="the-theoretical-endpoint">The Theoretical Endpoint</h2>
<p>Shannon&rsquo;s source-coding theorem says that for a memoryless source with distribution $p$, no prefix-free code can achieve expected length below $H(p)$ bits per symbol. Arithmetic coding achieves $H(p)$ in the limit. The bound is tight, and arithmetic coding reaches it.</p>
<p>That settles compression for memoryless sources. Sources with memory are a different problem. A Markov source of order $k$ can be handled by conditioning on the last $k$ symbols: one arithmetic coder per context. Generalizing further, context-mixing predictors maintain an ensemble of models and blend their probability estimates. The arithmetic coding stage stays unchanged throughout: it only needs a good $p$.</p>
<p>The best general-purpose lossless compressors in use (PAQ, ZPAQ, and derivatives) are context-mixing predictors driving an arithmetic coder. The predictor is where the innovation happens. The coder is the fixed, information-theoretically optimal back end.</p>
<p>What arithmetic coding cannot do is compress below $H(p)$. That is not an implementation limitation. It is a theorem. The next tool in the series, Succinct Bit Vectors and Rank/Select, shifts from compressing data to indexing it space-efficiently: a different kind of optimality.</p>
<hr>
<h2 id="cross-references">Cross-references</h2>
<p><strong>Back:</strong> <a href="/post/2024-08-huffman-wire-formats/">Huffman Coding</a> (post 9) showed that integer codeword lengths bound compression at one bit above entropy. Arithmetic coding removes that bound.</p>
<p><a href="/post/2022-01-priors-wire-formats/">Universal Codes as Priors</a> (post 3) introduced entropy and redundancy measurements; the <code>priors::entropy()</code> function used in the convergence tests comes from <code>priors.hpp</code> in that post&rsquo;s directory.</p>
<p><strong>Forward:</strong> Succinct Bit Vectors and Rank/Select (post 11) shifts from entropy coding to space-efficient indexing, where the goal is not compression but constant-time rank and select queries on compressed representations.</p>
<p><strong>Cross-series:</strong> In the Bits Follow Types framing, arithmetic coding is the entropy-optimal realization of the Either combinator&rsquo;s tag bit. The Either codec tags a choice with 1 bit regardless of probability; arithmetic coding replaces the tag with a fractional contribution proportional to the symbol&rsquo;s true information content.</p>
<p><strong>Footnote:</strong> The production implementation lives at <code>include/pfc/arithmetic_coding.hpp</code> in the <a href="https://github.com/queelius/wire-formats/tree/master/lib/pfc">PFC library</a>. It includes both the integer range coder developed here and a higher-level adaptive variant with configurable context models.</p>
]]></content:encoded><media:content url="https://metafunctor.com/post/2025-01-arithmetic-coding-wire-formats/card.jpg" medium="image"><media:title type="plain">Information-Theory</media:title></media:content><category>C++</category><category>information-theory</category><category>coding-theory</category><category>prefix-free</category><category>entropy</category><category>arithmetic-coding</category><category>range-coding</category></item><item><title>All Induction Is the Same Induction</title><link>https://metafunctor.com/post/2024-09-30-universal-bayes/</link><pubDate>Mon, 30 Sep 2024 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-09-30-universal-bayes/</guid><description>Solomonoff induction, MDL, speed priors, and neural networks are all special cases of one Bayesian framework with four knobs.</description><content:encoded><![CDATA[<p>Solomonoff induction is the theoretical gold standard for prediction: average over all possible programs weighted by simplicity. Shorter programs get higher weight. Given enough data, it converges to the true distribution faster than any other method.</p>
<p>Two problems: it&rsquo;s uncomputable, and it&rsquo;s brittle. One wrong prediction and a hypothesis is dead.</p>
<p>But here&rsquo;s the thing: <strong>Solomonoff induction is just one setting of four knobs</strong>. Turn those knobs differently and you get MDL, speed priors, or something that looks a lot like neural network training. Same framework, different parameters.</p>
<h2 id="the-four-knobs">The Four Knobs</h2>
<p>Every induction method makes choices about:</p>
<p><strong>1. Which programs to consider.</strong> All programs? Only short ones? Only fast ones?</p>
<p><strong>2. How to penalize complexity.</strong> Length? Runtime? Both?</p>
<p><strong>3. How much noise to tolerate.</strong> Must predictions be exact? Can we allow small errors?</p>
<p><strong>4. How to measure errors.</strong> Binary right/wrong? Squared error? Log loss?</p>
<p>Write these as parameters:</p>
<ul>
<li><strong>P</strong>: the program space</li>
<li><strong>C(p)</strong>: complexity penalty (acts as prior)</li>
<li><strong>lambda</strong>: noise tolerance (higher = stricter)</li>
<li><strong>l(o, x)</strong>: loss function</li>
</ul>
<p>The posterior weight for program p given data x_1, &hellip;, x_n is:</p>
$$P(p \mid x_{1:n}) \propto C(p) \cdot \exp\left(-\lambda \sum_{i=1}^n \ell(p(i), x_i)\right)$$<p>This is just Bayes&rsquo; rule. The exponential term is the likelihood. Different parameter choices give different induction strategies.</p>
<h2 id="recovering-solomonoff">Recovering Solomonoff</h2>
<p>Classical Solomonoff induction:</p>
<ul>
<li><strong>P</strong> = all programs</li>
<li><strong>C(p)</strong> = 2^{-|p|} (exponential preference for short programs)</li>
<li><strong>lambda</strong> approaches infinity (infinite strictness)</li>
<li><strong>l(o, x)</strong> = 0 if o = x, else infinity (binary match)</li>
</ul>
<p>With lambda approaching infinity, any program that makes a single wrong prediction gets exp(-infinity) = 0 weight. Only programs that perfectly match the data survive. Among those, shorter programs dominate exponentially.</p>
<p>This gives the classic result: optimal prediction, but uncomputable (we can&rsquo;t enumerate all programs) and fragile (real data has noise).</p>
<h2 id="making-it-computable">Making It Computable</h2>
<p>Want computability? Restrict P:</p>
<ul>
<li>Only programs of length at most N</li>
<li>Only programs that halt within T steps</li>
</ul>
<p>Now everything is finite. We can actually run this. As N, T approach infinity, we approach full Solomonoff induction.</p>
<p>This is the (N, T)-bounded approximation. It&rsquo;s no longer optimal, but it&rsquo;s <em>actually runnable</em>. The convergence guarantee: with large enough bounds, predictions get arbitrarily close to the full model.</p>
<h2 id="speed-priors">Speed Priors</h2>
<p>Some short programs are slow. A program that solves a problem by brute force might be brief but take exponential time.</p>
<p>Levin&rsquo;s speed prior penalizes both length and runtime:</p>
$$C(p) = \frac{2^{-|p|}}{t(p)}$$<p>Now a 10-character program that runs in 10^9 steps gets the same weight as a 40-character program that runs in 1 step. We prefer <em>fast</em> simple explanations over slow simple ones.</p>
<p>Still a valid setting of the four knobs. Same framework.</p>
<h2 id="handling-noise">Handling Noise</h2>
<p>Real data is noisy. Strict Solomonoff kills any hypothesis that doesn&rsquo;t match perfectly.</p>
<p>Fix: use finite lambda with a continuous loss function:</p>
<ul>
<li><strong>lambda</strong> = some finite constant</li>
<li><strong>l(o, x)</strong> = (o - x)^2 (squared error)</li>
</ul>
<p>Now a program that predicts 0.99 when the true value is 1.0 doesn&rsquo;t get zero weight. It just gets slightly penalized. Multiple small errors accumulate, but don&rsquo;t instantly disqualify.</p>
<p>This is essentially Bayesian regression over programs. The exp(-lambda times loss) form is an exponential family likelihood.</p>
<h2 id="what-neural-networks-are-doing">What Neural Networks Are Doing</h2>
<p>A neural network implicitly defines:</p>
<ul>
<li><strong>P</strong> = architectures expressible by the network structure</li>
<li><strong>C(p)</strong> = implicit prior from initialization and regularization</li>
<li><strong>lambda</strong> = determined by learning rate and loss scaling</li>
<li><strong>l</strong> = whatever loss function you chose</li>
</ul>
<p>The network can&rsquo;t represent all programs, only those within its architecture. But within that space, gradient descent approximately finds the posterior mode.</p>
<p>Weight decay is a complexity penalty. Dropout regularizes. Early stopping prevents overfitting to noise. These are all ways of tuning the effective lambda and C(p).</p>
<p>Neural networks aren&rsquo;t doing something fundamentally different from Solomonoff induction. They&rsquo;re doing a heavily resource-bounded version with implicit parameter choices.</p>
<h2 id="the-tradeoff-space">The Tradeoff Space</h2>
<table>
	<thead>
			<tr>
					<th>Method</th>
					<th>P</th>
					<th>C(p)</th>
					<th>lambda</th>
					<th>l</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Solomonoff</td>
					<td>all programs</td>
					<td>2^{-|p|}</td>
					<td>infinity</td>
					<td>0/infinity binary</td>
			</tr>
			<tr>
					<td>(N,T)-bounded</td>
					<td>length at most N, time at most T</td>
					<td>2^{-|p|}</td>
					<td>infinity</td>
					<td>0/infinity binary</td>
			</tr>
			<tr>
					<td>Speed prior</td>
					<td>all programs</td>
					<td>2^{-|p|}/t(p)</td>
					<td>infinity</td>
					<td>0/infinity binary</td>
			</tr>
			<tr>
					<td>MDL</td>
					<td>model class</td>
					<td>2^{-description length}</td>
					<td>finite</td>
					<td>log loss</td>
			</tr>
			<tr>
					<td>Neural net</td>
					<td>architecture</td>
					<td>regularization</td>
					<td>learning dynamics</td>
					<td>task loss</td>
			</tr>
	</tbody>
</table>
<p>These aren&rsquo;t competing theories. They&rsquo;re points in the same parameter space.</p>
<h2 id="why-this-matters">Why This Matters</h2>
<p><strong>Theoretical clarity</strong>: Arguments about which induction method is &ldquo;best&rdquo; become arguments about parameter choice. The framework itself isn&rsquo;t in dispute.</p>
<p><strong>Practical guidance</strong>: If you want computability, restrict P. If you have noise, use finite lambda. If you want fast predictions, penalize runtime in C(p). The tradeoffs are explicit.</p>
<p><strong>Biological plausibility</strong>: Brains probably implement something like (N, T)-bounded induction with substantial noise tolerance. Limited working memory constrains P. Sensory noise requires finite lambda. Neural architecture defines C(p) implicitly.</p>
<h2 id="the-bottom-line">The Bottom Line</h2>
<p>Solomonoff induction isn&rsquo;t a separate algorithm from practical machine learning. It&rsquo;s an extreme parameter setting: all programs, maximum strictness, zero noise tolerance.</p>
<p>Relax those settings and you get computable, noise-tolerant methods. The math stays the same. Just Bayesian inference with different priors and likelihoods.</p>
<p>Different problems call for different settings. The framework tells you what you&rsquo;re trading off.</p>
<hr>
<p><em>All induction is Bayesian inference over programs. The only question is which programs, with what penalties, under what noise model.</em></p>
]]></content:encoded><media:content url="https://metafunctor.com/post/2024-09-30-universal-bayes/card.jpg" medium="image"><media:title type="plain">Information-Theory</media:title></media:content><category>machine-learning</category><category>solomonoff-induction</category><category>bayesian-inference</category><category>information-theory</category><category>philosophy</category></item><item><title>Huffman Coding</title><link>https://metafunctor.com/post/2024-08-huffman-wire-formats/</link><pubDate>Sun, 04 Aug 2024 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-08-huffman-wire-formats/</guid><description>Given a finite distribution, Huffman's algorithm builds the prefix-free code with minimum expected length. It is the first entropy-optimal code in this series.</description><content:encoded><![CDATA[<p><em>Huffman coding is two things: the optimal length vector for a known distribution, and McMillan&rsquo;s construction applied to that vector. This post develops both.</em></p>
<h2 id="from-universal-to-optimal">From Universal to Optimal</h2>
<p>Every code in this series so far has been universal: no prior knowledge of the source distribution required. Elias gamma assigns shorter codewords to smaller integers regardless of which integers actually appear. Fibonacci does the same. VByte packs smaller values into fewer bytes without knowing whether your data clusters at the low end or the high end. Universal codes are defensive: they perform acceptably across a broad class of inputs by committing to none.</p>
<p>Huffman flips that stance. You bring a finite probability distribution. Huffman finds the prefix-free code with minimum expected codeword length for that distribution. The code is tuned to what you provide and will perform poorly on anything else. Call this the move from defensive to distribution-specific coding.</p>
<p>The payoff is real. Shannon&rsquo;s source coding theorem says no prefix-free code can achieve expected length below $H(p) = -\sum_i p_i \log_2 p_i$. Huffman gets within 1 bit of that bound. For any prefix-free code, expected length satisfies</p>
$$H(p) \le L(\text{code}) \le H(p) + 1.$$<p>The upper bound comes from the integer-length constraint: each codeword is a whole number of bits, and $\lceil -\log_2 p_i \rceil \le -\log_2 p_i + 1$. Huffman is optimal subject to this constraint. No prefix-free code does better without abandoning whole-bit codewords.</p>
<p>That last clause points to the limit of this post and the subject of the next. Arithmetic coding breaks the integer-length constraint by assigning fractional bits in effect, reaching entropy exactly in the limit.</p>
<h2 id="the-algorithm">The Algorithm</h2>
<p>The four steps of Huffman&rsquo;s construction:</p>
<ol>
<li>Create one leaf node per symbol, weighted by its probability.</li>
<li>Push all leaves into a min-priority queue (lowest weight first).</li>
<li>While the queue contains more than one node: extract the two lowest-weight nodes, merge them into a new internal node whose weight is their sum, push the internal node back.</li>
<li>The remaining node is the root. The path from root to each leaf encodes that leaf&rsquo;s codeword (&ldquo;0&rdquo; for left, &ldquo;1&rdquo; for right).</li>
</ol>
<p>Here is the complete implementation from <code>huffman.hpp</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Node</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kt">double</span> <span class="n">freq</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="kt">int</span> <span class="n">symbol</span> <span class="o">=</span> <span class="o">-</span><span class="mi">1</span><span class="p">;</span>  <span class="c1">// -1 for internal nodes
</span></span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">Node</span><span class="o">&gt;</span> <span class="n">left</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">Node</span><span class="o">&gt;</span> <span class="n">right</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="kr">inline</span> <span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">Node</span><span class="o">&gt;</span> <span class="n">build_huffman_tree</span><span class="p">(</span><span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;&amp;</span> <span class="n">freqs</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">assert</span><span class="p">(</span><span class="o">!</span><span class="n">freqs</span><span class="p">.</span><span class="n">empty</span><span class="p">());</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">auto</span> <span class="n">cmp</span> <span class="o">=</span> <span class="p">[](</span><span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">Node</span><span class="o">&gt;&amp;</span> <span class="n">a</span><span class="p">,</span> <span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">Node</span><span class="o">&gt;&amp;</span> <span class="n">b</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">a</span><span class="o">-&gt;</span><span class="n">freq</span> <span class="o">&gt;</span> <span class="n">b</span><span class="o">-&gt;</span><span class="n">freq</span><span class="p">;</span>  <span class="c1">// min-heap
</span></span></span><span class="line"><span class="cl">    <span class="p">};</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">priority_queue</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">Node</span><span class="o">&gt;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                        <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">Node</span><span class="o">&gt;&gt;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                        <span class="k">decltype</span><span class="p">(</span><span class="n">cmp</span><span class="p">)</span><span class="o">&gt;</span> <span class="n">pq</span><span class="p">(</span><span class="n">cmp</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">freqs</span><span class="p">.</span><span class="n">size</span><span class="p">();</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">auto</span> <span class="n">node</span>    <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">make_unique</span><span class="o">&lt;</span><span class="n">Node</span><span class="o">&gt;</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">        <span class="n">node</span><span class="o">-&gt;</span><span class="n">freq</span>   <span class="o">=</span> <span class="n">freqs</span><span class="p">[</span><span class="n">i</span><span class="p">];</span>
</span></span><span class="line"><span class="cl">        <span class="n">node</span><span class="o">-&gt;</span><span class="n">symbol</span> <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="kt">int</span><span class="o">&gt;</span><span class="p">(</span><span class="n">i</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">pq</span><span class="p">.</span><span class="n">push</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="n">node</span><span class="p">));</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">while</span> <span class="p">(</span><span class="n">pq</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">&gt;</span> <span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">auto</span> <span class="n">a</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="k">const_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">Node</span><span class="o">&gt;&amp;&gt;</span><span class="p">(</span><span class="n">pq</span><span class="p">.</span><span class="n">top</span><span class="p">()));</span> <span class="n">pq</span><span class="p">.</span><span class="n">pop</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">        <span class="k">auto</span> <span class="n">b</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="k">const_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">Node</span><span class="o">&gt;&amp;&gt;</span><span class="p">(</span><span class="n">pq</span><span class="p">.</span><span class="n">top</span><span class="p">()));</span> <span class="n">pq</span><span class="p">.</span><span class="n">pop</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">        <span class="k">auto</span> <span class="n">parent</span>      <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">make_unique</span><span class="o">&lt;</span><span class="n">Node</span><span class="o">&gt;</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">        <span class="n">parent</span><span class="o">-&gt;</span><span class="n">freq</span>     <span class="o">=</span> <span class="n">a</span><span class="o">-&gt;</span><span class="n">freq</span> <span class="o">+</span> <span class="n">b</span><span class="o">-&gt;</span><span class="n">freq</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="n">parent</span><span class="o">-&gt;</span><span class="n">left</span>     <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="n">a</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">parent</span><span class="o">-&gt;</span><span class="n">right</span>    <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="n">b</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">pq</span><span class="p">.</span><span class="n">push</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="n">parent</span><span class="p">));</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">return</span> <span class="n">std</span><span class="o">::</span><span class="n">move</span><span class="p">(</span><span class="k">const_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">unique_ptr</span><span class="o">&lt;</span><span class="n">Node</span><span class="o">&gt;&amp;&gt;</span><span class="p">(</span><span class="n">pq</span><span class="p">.</span><span class="n">top</span><span class="p">()));</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>The <code>const_cast</code> lines deserve an explanation. <code>std::priority_queue::top()</code> returns <code>const&amp;</code> to protect the heap invariant: a caller mutating the top element through the reference would break the heap property. Here, <code>pq.pop()</code> is called immediately after the move, restoring the invariant before any other operation can observe the moved-from state. The <code>const_cast</code> is safe precisely because of that pairing. It is the standard workaround for moving out of a priority queue. Do not replace the <code>std::move</code> with a copy: <code>Node</code> owns <code>std::unique_ptr</code> children and is not copyable.</p>
<p>The codebook is extracted by a recursive depth-first walk:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="kr">inline</span> <span class="n">std</span><span class="o">::</span><span class="n">map</span><span class="o">&lt;</span><span class="kt">int</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="o">&gt;</span> <span class="n">tree_to_codebook</span><span class="p">(</span><span class="k">const</span> <span class="n">Node</span><span class="o">*</span> <span class="n">root</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                                                    <span class="n">std</span><span class="o">::</span><span class="n">string</span> <span class="n">prefix</span> <span class="o">=</span> <span class="s">&#34;&#34;</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">if</span> <span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">symbol</span> <span class="o">&gt;=</span> <span class="mi">0</span> <span class="o">&amp;&amp;</span> <span class="o">!</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span> <span class="o">&amp;&amp;</span> <span class="o">!</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="p">{{</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">symbol</span><span class="p">,</span> <span class="n">prefix</span><span class="p">.</span><span class="n">empty</span><span class="p">()</span> <span class="o">?</span> <span class="s">&#34;0&#34;</span> <span class="o">:</span> <span class="n">prefix</span><span class="p">}};</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">map</span><span class="o">&lt;</span><span class="kt">int</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="o">&gt;</span> <span class="n">result</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="k">if</span> <span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">auto</span> <span class="n">L</span> <span class="o">=</span> <span class="n">tree_to_codebook</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">.</span><span class="n">get</span><span class="p">(),</span> <span class="n">prefix</span> <span class="o">+</span> <span class="s">&#34;0&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">result</span><span class="p">.</span><span class="n">insert</span><span class="p">(</span><span class="n">L</span><span class="p">.</span><span class="n">begin</span><span class="p">(),</span> <span class="n">L</span><span class="p">.</span><span class="n">end</span><span class="p">());</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="k">if</span> <span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">auto</span> <span class="n">R</span> <span class="o">=</span> <span class="n">tree_to_codebook</span><span class="p">(</span><span class="n">root</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">.</span><span class="n">get</span><span class="p">(),</span> <span class="n">prefix</span> <span class="o">+</span> <span class="s">&#34;1&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">result</span><span class="p">.</span><span class="n">insert</span><span class="p">(</span><span class="n">R</span><span class="p">.</span><span class="n">begin</span><span class="p">(),</span> <span class="n">R</span><span class="p">.</span><span class="n">end</span><span class="p">());</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="k">return</span> <span class="n">result</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Encoding looks up the symbol&rsquo;s codeword string and writes each character into a <code>BitSink</code>. Decoding walks the tree from the root, branching on each incoming bit, until a leaf is reached:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="kr">inline</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="kt">int</span> <span class="n">symbol</span><span class="p">,</span> <span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">map</span><span class="o">&lt;</span><span class="kt">int</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="o">&gt;&amp;</span> <span class="n">codebook</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">for</span> <span class="p">(</span><span class="kt">char</span> <span class="nl">c</span> <span class="p">:</span> <span class="n">codebook</span><span class="p">.</span><span class="n">at</span><span class="p">(</span><span class="n">symbol</span><span class="p">))</span> <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="n">c</span> <span class="o">==</span> <span class="sc">&#39;1&#39;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="kr">inline</span> <span class="kt">int</span> <span class="n">decode</span><span class="p">(</span><span class="k">const</span> <span class="n">Node</span><span class="o">*</span> <span class="n">root</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">const</span> <span class="n">Node</span><span class="o">*</span> <span class="n">cur</span> <span class="o">=</span> <span class="n">root</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="k">while</span> <span class="p">(</span><span class="n">cur</span><span class="o">-&gt;</span><span class="n">symbol</span> <span class="o">&lt;</span> <span class="mi">0</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="kt">bool</span> <span class="n">bit</span> <span class="o">=</span> <span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">        <span class="n">cur</span> <span class="o">=</span> <span class="n">bit</span> <span class="o">?</span> <span class="n">cur</span><span class="o">-&gt;</span><span class="n">right</span><span class="p">.</span><span class="n">get</span><span class="p">()</span> <span class="o">:</span> <span class="n">cur</span><span class="o">-&gt;</span><span class="n">left</span><span class="p">.</span><span class="n">get</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="k">return</span> <span class="n">cur</span><span class="o">-&gt;</span><span class="n">symbol</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Because the code is prefix-free, the decoder knows exactly when one codeword ends and the next begins with no delimiter required. This is the defining property of the prefix-free constraint from <a href="/post/2020-03-kraft-wire-formats/">Kraft&rsquo;s Inequality</a> and <a href="/post/2020-09-mcmillan-wire-formats/">McMillan&rsquo;s Converse</a>.</p>
<h2 id="optimality">Optimality</h2>
<p>The proof that Huffman is optimal among prefix-free codes rests on three lemmas. Together they support an induction on alphabet size.</p>
<p>I&rsquo;ll sketch the argument rather than reproduce the full proof. The sketch is honest: these are the structural moves the proof makes, not a complete formal development.</p>
<p><strong>Lemma 1: the two least-frequent symbols get the longest codewords (or are tied for longest).</strong> Suppose for contradiction that some symbol $x$ with frequency $p_x$ has a strictly longer codeword than the two least-frequent symbols $a$ and $b$ (with $p_a \le p_b \le p_x$). Swapping the codewords of $a$ and $x$ would shorten the average length by $(p_a - p_x)(|cw_a| - |cw_x|) &lt; 0$ (since $p_a &lt; p_x$ and $|cw_a| &gt; |cw_x|$). That contradicts optimality.</p>
<p><strong>Lemma 2: the two least-frequent symbols can always be assigned sibling codewords (codewords that differ only in the last bit).</strong> Any prefix-free code has a binary tree representation. If the two longest-codeword nodes are not siblings, you can restructure the tree to make them siblings without increasing average length. This restructuring is always possible because the deepest level of a prefix-free code tree always contains pairs of sibling leaves.</p>
<p><strong>Lemma 3 (the merger step): combining the two least-frequent symbols into a single merged symbol yields a strictly smaller problem whose optimal solution lifts back to the original.</strong> If $C^<em>$ is an optimal code for the merged alphabet (with merged symbol having probability $p_a + p_b$), then the code for the original alphabet formed by adding one bit to the merged codeword (assigning $cw_a = cw^</em> + 0$ and $cw_b = cw^* + 1$) is optimal for the original alphabet. The expected length increases by exactly $p_a + p_b$ (one extra bit for the pair), which is unavoidable.</p>
<p>Induction closes the argument: a one-symbol alphabet has trivially optimal coding; the merger step reduces $n$ symbols to $n-1$; by induction hypothesis, the merged problem is solved optimally; by Lemma 3, the solution lifts back optimally to the $n$-symbol problem.</p>
<p>The consequence: Huffman&rsquo;s expected length $L$ satisfies $H(p) \le L \le H(p) + 1$. The lower bound is Shannon&rsquo;s theorem. The upper bound follows from the fact that assigning codeword length $\lceil -\log_2 p_i \rceil$ to each symbol satisfies the Kraft inequality and yields $L \le H(p) + 1$, so the Huffman code (which is at most as long as this code by optimality) also satisfies the upper bound.</p>
<p>For dyadic distributions (where each $p_i$ is a negative power of 2), $-\log_2 p_i$ is already an integer, so Huffman achieves entropy exactly. For all other distributions, there is a gap of up to 1 bit.</p>
<p>Arithmetic coding removes the gap by encoding sequences of symbols jointly, amortizing the integer-rounding slack over arbitrarily long blocks. Per-symbol redundancy approaches zero as block length grows.</p>
<h2 id="mcmillans-construction-returns">McMillan&rsquo;s Construction Returns</h2>
<p>Post 2 showed that any Kraft-satisfying length vector $(\ell_1, \ldots, \ell_n)$ can be turned into a prefix-free code by <a href="/post/2020-09-mcmillan-wire-formats/">McMillan&rsquo;s construction</a>. Huffman solves the complementary problem: given a distribution $(p_1, \ldots, p_n)$, find the Kraft-satisfying length vector that minimizes $\sum_i p_i \ell_i$.</p>
<p>Viewed this way, Huffman is a two-stage machine. Stage one identifies the optimal lengths. Stage two applies McMillan&rsquo;s construction to produce the code. The Huffman tree algorithm compresses both stages into one pass, building the tree and the code simultaneously, but the logical separation is worth keeping in mind.</p>
<p>The chain of ideas in this series now reads:</p>
<ul>
<li>Post 1, Kraft: which length vectors are valid (they satisfy the Kraft sum)?</li>
<li>Post 2, McMillan: given a valid length vector, how do you build the code?</li>
<li>Post 3, Priors: what distribution does a code implicitly assume (the one that makes the code optimal)?</li>
<li>Post 9, Huffman: given an explicit distribution, find the optimal length vector.</li>
</ul>
<p>The first three posts ran from codes to distributions. This post runs the other direction.</p>
<h2 id="tests-and-demonstrations">Tests and Demonstrations</h2>
<p>The test suite in <code>test_huffman.cpp</code> covers the theoretical claims directly.</p>
<p><strong>Uniform alphabets:</strong> For a uniform distribution over $2^k$ symbols, entropy is exactly $k$ bits and Huffman produces a fixed-width $k$-bit code (all codewords the same length). The suite checks $k = 2$ (4 symbols) and $k = 3$ (8 symbols).</p>
<p><strong>Dyadic distributions:</strong> For $p = (0.5, 0.25, 0.125, 0.125)$, each $p_i$ is a power of 2, so entropy is $1.75$ bits and Huffman achieves it exactly. The test verifies <code>EXPECT_NEAR(L, H, 1e-9)</code>.</p>
<p><strong>Highly skewed binary:</strong> For $(0.99, 0.01)$, entropy is about 0.081 bits, but Huffman cannot use fewer than 1 bit per symbol (both symbols need at least one bit). The test confirms $L = 1.0$ and $L \le H + 1$.</p>
<p><strong>Comparison with gamma:</strong> On a power-law-2 distribution over 16 symbols, Huffman&rsquo;s expected length is no greater than Elias gamma&rsquo;s expected length. Gamma is universal but pays for that universality.</p>
<p>The <code>codebook_lengths</code> helper bridges the Huffman codebook to the <code>priors</code> library from post 3:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">static</span> <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span><span class="o">&gt;</span> <span class="n">codebook_lengths</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">        <span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">map</span><span class="o">&lt;</span><span class="kt">int</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">string</span><span class="o">&gt;&amp;</span> <span class="n">cb</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">n</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span><span class="o">&gt;</span> <span class="n">lens</span><span class="p">(</span><span class="n">n</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="k">for</span> <span class="p">(</span><span class="k">const</span> <span class="k">auto</span><span class="o">&amp;</span> <span class="p">[</span><span class="n">sym</span><span class="p">,</span> <span class="n">cw</span><span class="p">]</span> <span class="o">:</span> <span class="n">cb</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="n">lens</span><span class="p">[</span><span class="n">sym</span><span class="p">]</span> <span class="o">=</span> <span class="n">cw</span><span class="p">.</span><span class="n">size</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">    <span class="k">return</span> <span class="n">lens</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>With <code>lens</code> in hand, <code>priors::entropy(freqs)</code> and <code>priors::expected_length(freqs, lens)</code> compute the quantities needed to verify the $H \le L \le H + 1$ bound directly.</p>
<h2 id="limitations">Limitations</h2>
<p>Huffman has three practical limitations worth naming before moving on.</p>
<p><strong>First, it requires the distribution as input.</strong> For data whose statistics are unknown, you must either estimate the distribution first (a two-pass approach: count frequencies on training data, then encode the actual data) or use an adaptive variant that updates the code as it runs. Two-pass schemes transmit the codebook alongside the compressed data, adding overhead. Adaptive schemes avoid the codebook overhead but carry computational cost per symbol.</p>
<p><strong>Second, the integer-length constraint binds for non-dyadic distributions.</strong> A symbol with probability 0.37 ideally gets a codeword of length $-\log_2 0.37 \approx 1.43$ bits. Huffman must assign 1 or 2 bits. For some distributions, notably a binary source with $p$ far from 0.5, the gap between entropy and Huffman length approaches 1 bit per symbol.</p>
<p><strong>Third, block Huffman only partially helps.</strong> Encoding $k$-symbol tuples reduces per-symbol redundancy from at most 1 bit to at most $1/k$ bits, because per-block redundancy is still bounded by 1 bit. But the alphabet size grows as $n^k$, making the codebook exponentially large. For $n = 256$ bytes and $k = 3$, the alphabet has $16{,}777{,}216$ entries. In practice, block Huffman is limited to very small $k$ or very small alphabets.</p>
<p>Arithmetic coding, the subject of the next post, resolves all three limitations without a block-size explosion. It works on a per-symbol basis like Huffman but achieves arbitrary precision by representing the encoder state as a shrinking real interval. The integer-rounding slack disappears because no individual symbol is assigned a codeword at all; the compressed output emerges from the interval arithmetic directly.</p>
<h2 id="cross-references">Cross-References</h2>
<p><strong>Forthcoming:</strong> Arithmetic Coding (post 10) extends the ideas here by removing the integer-length constraint and approaching entropy exactly.</p>
<p><strong>Back-references (live):</strong></p>
<ul>
<li><a href="/post/2020-09-mcmillan-wire-formats/">McMillan&rsquo;s Converse</a> (post 2): the construction that turns any Kraft-satisfying length vector into a prefix-free code. Huffman finds the optimal length vector; McMillan builds the code.</li>
<li><a href="/post/2022-01-priors-wire-formats/">Universal Codes as Priors</a> (post 3): the <code>priors::entropy</code> and <code>priors::expected_length</code> functions used in the optimality tests come from this post&rsquo;s <code>priors.hpp</code>.</li>
</ul>
<p><strong>Cross-series:</strong> <a href="/post/2026-05-codecs-functors-stepanov/">Bits Follow Types</a> (Stepanov series): when a <code>PackedEither</code> combinator chooses among $N$ type variants, it pays $\lceil \log_2 N \rceil$ tag bits per value. Huffman provides the entropy-optimal alternative when the variant frequencies are known, replacing the fixed $\log_2 N$ cost with a code tuned to the actual distribution of variants.</p>
<p><strong>Production implementation:</strong> The pedagogical <code>huffman.hpp</code> here is a self-contained teaching tool. The production version lives at <code>include/pfc/huffman.hpp</code> in the <a href="https://github.com/queelius/wire-formats/tree/master/lib/pfc">PFC library</a>.</p>
]]></content:encoded><media:content url="https://metafunctor.com/post/2024-08-huffman-wire-formats/card.jpg" medium="image"><media:title type="plain">Information-Theory</media:title></media:content><category>C++</category><category>information-theory</category><category>coding-theory</category><category>prefix-free</category><category>huffman</category><category>entropy-optimal</category></item><item><title>The Beautiful Deception: How 256 Bits Pretend to be Infinity</title><link>https://metafunctor.com/post/2024-07-01-beautiful-deception/</link><pubDate>Mon, 01 Jul 2024 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-07-01-beautiful-deception/</guid><description>Cryptographic theory assumes random oracles with infinite output. We have 256 bits. This paper explores how we bridge that gap, and what it means that we can.</description><content:encoded><![CDATA[<p>How do you store infinity in 256 bits? You don&rsquo;t. But you can fake it well enough that no bounded observer can tell the difference. This paper is about that deception, why it works, and what it tells us about randomness.</p>
<h2 id="the-impossible-oracle">The impossible oracle</h2>
<p>A random oracle maps any input to an infinite sequence of perfectly random bits. Try to implement one and you fail immediately:</p>
<ol>
<li><strong>Memory unboundedness</strong>: each new query exhausts memory</li>
<li><strong>Non-serializability</strong>: can&rsquo;t save/restore state</li>
<li><strong>Non-reproducibility</strong>: each instance generates different values</li>
<li><strong>Non-distributability</strong>: can&rsquo;t share across systems</li>
</ol>
<p>This isn&rsquo;t a limitation of current hardware. It&rsquo;s a constructive proof that true random oracles can&rsquo;t exist in our computational universe.</p>
<h2 id="the-lie-that-works">The lie that works</h2>
<p>From this impossibility comes something useful:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">LazyDigest</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="fm">__init__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">seed</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="bp">self</span><span class="o">.</span><span class="n">seed</span> <span class="o">=</span> <span class="n">seed</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="fm">__getitem__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">index</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="nb">hash</span><span class="p">(</span><span class="n">seed</span> <span class="o">||</span> <span class="n">index</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>
</span></span></code></pre></div><p>256 bits of entropy generating what appears to be an infinite random sequence.</p>
<p>The deception:</p>
<ul>
<li><strong>Appears</strong>: infinite random sequence</li>
<li><strong>Actually</strong>: deterministic function with 256 bits of state</li>
<li><strong>Information content</strong>: K(LazyDigest) = 256 bits + constant</li>
<li><strong>Apparent information</strong>: infinite</li>
</ul>
<p>We&rsquo;re achieving a compression ratio of infinity, representing unbounded data with bounded information.</p>
<h2 id="why-it-works">Why it works</h2>
<p>Computational indistinguishability. If <code>h</code> is a secure PRF, no polynomial-time algorithm can tell LazyDigest apart from truly random output. We&rsquo;re not random. We&rsquo;re computationally hard to distinguish from random. This weaker guarantee is sufficient for all of cryptography.</p>
<p>Since LazyDigest has finite state, it must eventually cycle. After at most 2^256 queries, it repeats. But the expected cycle length is 2^128, roughly 10^38. At a billion queries per second, cycling takes about 10^21 years. The universe is roughly 10^10 years old.</p>
<h2 id="advanced-constructions">Advanced constructions</h2>
<p><strong>Hierarchical seeding</strong> extends the effective period:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">epoch_seed</span> <span class="o">=</span> <span class="n">h</span><span class="p">(</span><span class="n">master_seed</span> <span class="o">||</span> <span class="s2">&#34;epoch&#34;</span> <span class="o">||</span> <span class="n">epoch</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">chunk_seed</span> <span class="o">=</span> <span class="n">h</span><span class="p">(</span><span class="n">epoch_seed</span> <span class="o">||</span> <span class="s2">&#34;chunk&#34;</span> <span class="o">||</span> <span class="n">chunk</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">value</span> <span class="o">=</span> <span class="n">h</span><span class="p">(</span><span class="n">chunk_seed</span> <span class="o">||</span> <span class="n">position</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>
</span></span></code></pre></div><p><strong>XOR multi-hash</strong> hedges against individual algorithm failures:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">result</span> <span class="o">=</span> <span class="n">sha256</span><span class="p">(</span><span class="n">seed</span><span class="o">||</span><span class="n">index</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span> <span class="o">^</span> <span class="n">sha512</span><span class="p">(</span><span class="n">seed</span><span class="o">||</span><span class="n">index</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span> <span class="o">^</span>
</span></span><span class="line"><span class="cl">         <span class="n">sha3_256</span><span class="p">(</span><span class="n">seed</span><span class="o">||</span><span class="n">index</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span> <span class="o">^</span> <span class="n">blake2b</span><span class="p">(</span><span class="n">seed</span><span class="o">||</span><span class="n">index</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>
</span></span></code></pre></div><p>The system stays secure if at least one hash function holds. This hedges against future cryptanalysis, quantum vulnerabilities, and implementation bugs.</p>
<p><strong>Sponge construction</strong> reserves capacity bits that never leave the system, providing tunable security with 2^(capacity/2) collision resistance.</p>
<h2 id="random-oracles-and-uncomputable-reals">Random oracles and uncomputable reals</h2>
<p>Most real numbers are uncomputable. They require infinite information to specify. A true random oracle is the cryptographic analog:</p>
<ul>
<li><strong>Computable reals</strong> (like pi, e): measure zero, finite programs</li>
<li><strong>Uncomputable reals</strong>: measure one, infinite information</li>
<li><strong>LazyDigest</strong>: computable, appears random</li>
<li><strong>Random oracle</strong>: uncomputable, truly random</li>
</ul>
<p>We&rsquo;re using computable functions to approximate uncomputable ones.</p>
<p>The parallel with pi is instructive. We can compute any digit of pi using a finite algorithm (Bailey-Borwein-Plouffe formula). Finite description, infinite output, computed on demand. LazyDigest does the same thing. The difference is semantic: pi encodes geometric relationships, LazyDigest encodes computational hardness. Both achieve the same compression: finite program to infinite sequence.</p>
<h2 id="two-kinds-of-randomness">Two kinds of randomness</h2>
<p>There are two incompatible definitions of randomness:</p>
<ol>
<li><strong>Information-theoretic</strong>: high Kolmogorov complexity</li>
<li><strong>Computational</strong>: indistinguishable from random by efficient algorithms</li>
</ol>
<p>LazyDigest fails (1) catastrophically. It has tiny Kolmogorov complexity, just 256 bits plus the hash function definition.</p>
<p>LazyDigest passes (2) perfectly, assuming secure hash functions.</p>
<p>The insight: &ldquo;randomness&rdquo; in cryptography is actually computational hardness in disguise. We don&rsquo;t generate randomness. We generate a computationally hard problem and bet that nobody can solve it.</p>
<h2 id="entropy-and-computational-bounds">Entropy and computational bounds</h2>
<p>Consider Laplace&rsquo;s demon, a being with unlimited computational power. To it, LazyDigest is trivially non-random, just compute h(seed||i). No entropy exists, only deterministic evolution.</p>
<p>But we are computationally bounded, and that boundedness creates our entire experience of randomness. The past is fixed (we remember it). The future is uncertain (we can&rsquo;t compute fast enough to predict it). Entropy increases (we can&rsquo;t invert dynamics). Randomness exists (we can&rsquo;t distinguish it from true randomness).</p>
<p>A hash function is a one-way time machine. Forward: h(x) is easy. Backward: finding x given h(x) is computationally infeasible.</p>
<h2 id="the-p--np-bet">The P != NP bet</h2>
<p>Every use of LazyDigest implicitly bets that P != NP. If P = NP, we could invert the hash function in polynomial time and distinguish LazyDigest from random output. We&rsquo;re not generating randomness. We&rsquo;re generating a hard problem and hoping nobody solves it.</p>
<h2 id="the-constructivist-angle">The constructivist angle</h2>
<p>I think the most interesting part is what this says about mathematics. Classical mathematics posits incoherent infinities and uncomputable reals as real objects. LazyDigest represents a constructivist alternative: a finite, computable object that serves all practical purposes of an infinite random sequence.</p>
<p>The deception is honest:</p>
<ul>
<li>We don&rsquo;t claim infinite objects. We have finite programs generating unbounded output.</li>
<li>We don&rsquo;t pretend uncomputable things exist. We work with computable approximations.</li>
<li>We don&rsquo;t hide behind axioms. We build finite machines and study their behavior.</li>
</ul>
<p>Cryptography forces you to be a constructivist. The boundary of cryptographic possibility is precisely the boundary of computational constructibility.</p>
<p>In a universe that may itself be computational, LazyDigest isn&rsquo;t approximating some &ldquo;true&rdquo; random oracle in a Platonic realm. It may be as real as randomness gets.</p>
<p><a href="/latex/beautiful-deception/beautiful_deception.pdf">View the full paper (PDF)</a></p>
<h2 id="key-takeaways">Key takeaways</h2>
<ol>
<li>True random oracles are impossible to implement (proven constructively)</li>
<li>LazyDigest uses 256 bits to simulate infinity through computational hardness</li>
<li>&ldquo;Randomness&rdquo; in cryptography means computationally indistinguishable from random</li>
<li>The deception works because we&rsquo;re all computationally bounded observers</li>
<li>This is a constructivist approach to mathematics and computation</li>
</ol>
]]></content:encoded><media:content url="https://metafunctor.com/post/2024-07-01-beautiful-deception/card.jpg" medium="image"><media:title type="plain">Information-Theory</media:title></media:content><category>cryptography</category><category>hash-functions</category><category>randomness</category><category>information-theory</category><category>kolmogorov-complexity</category></item><item><title>VByte / Varint</title><link>https://metafunctor.com/post/2024-02-vbyte-wire-formats/</link><pubDate>Sun, 25 Feb 2024 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-02-vbyte-wire-formats/</guid><description>VByte trades bit-level precision for byte-alignment, and that trade wins in practice. Most production columnar databases and network protocols use VByte for integer encoding.</description><content:encoded><![CDATA[<p><em>Every code in this series so far operates at bit granularity. VByte does not. It gives up bit-level precision for byte-alignment, and in production systems, that trade wins most of the time.</em></p>
<h2 id="the-practical-question">The Practical Question</h2>
<p>Every code in this series so far operates at bit granularity. Elias gamma encodes 1 in a single bit. Fibonacci coding uses exactly as many bits as the Zeckendorf representation requires. Bit packing is theoretically attractive because it minimizes the number of bits written, which minimizes the encoded size.</p>
<p>But bit packing is computationally expensive. Reading or writing a single bit requires a shift, a mask, and often a branch to handle byte boundaries. Encoding a sequence of integers this way burns CPU cycles that scale with the number of integers, independent of their values. For high-throughput applications, the overhead of bit manipulation can easily exceed the savings from compact encoding.</p>
<p>VByte (also called Varint in Google&rsquo;s ecosystem, and LEB128 in the DWARF debug format) trades a small amount of length efficiency for byte-alignment. The idea is simple: encode each integer as a sequence of 7-bit groups, one per byte, with a continuation flag in the high bit of each byte. The result is self-delimiting, compact for small values, and requires no bit-level manipulation to decode.</p>
<p>VByte is the encoding used by Protocol Buffers for all integer fields. It appears in Apache Arrow, Parquet, Snappy&rsquo;s block format, LevelDB&rsquo;s metadata, and most production columnar file formats. These are high-throughput systems. Byte-alignment is why VByte is their choice over the more compact universal codes from posts 4 through 7.</p>
<hr>
<h2 id="the-encoding">The Encoding</h2>
<p>VByte splits an integer into 7-bit groups, starting from the least significant bits. Each group occupies one byte where bits 0 through 6 carry 7 data bits and bit 7 is a continuation flag: <code>1</code> means more bytes follow, <code>0</code> means this is the last byte.</p>
<p>A value in $[0, 127]$ fits in a single byte (continuation flag clear). A value in $[128, 16383]$ requires two bytes (first byte has flag set, second has flag clear). The pattern continues: each additional byte adds 7 bits of capacity.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">VByte</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="n">value_type</span> <span class="n">n</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">while</span> <span class="p">(</span><span class="n">n</span> <span class="o">&gt;=</span> <span class="mi">128</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span> <span class="n">byte</span> <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span><span class="o">&gt;</span><span class="p">((</span><span class="n">n</span> <span class="o">&amp;</span> <span class="mh">0x7F</span><span class="p">)</span> <span class="o">|</span> <span class="mh">0x80</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="mi">8</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(((</span><span class="n">byte</span> <span class="o">&gt;&gt;</span> <span class="n">i</span><span class="p">)</span> <span class="o">&amp;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="n">n</span> <span class="o">&gt;&gt;=</span> <span class="mi">7</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span> <span class="n">byte</span> <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">n</span> <span class="o">&amp;</span> <span class="mh">0x7F</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="mi">8</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(((</span><span class="n">byte</span> <span class="o">&gt;&gt;</span> <span class="n">i</span><span class="p">)</span> <span class="o">&amp;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">value_type</span> <span class="n">result</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">shift</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">while</span> <span class="p">(</span><span class="nb">true</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span> <span class="n">byte</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="k">for</span> <span class="p">(</span><span class="kt">int</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="mi">8</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">                <span class="k">if</span> <span class="p">(</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">())</span> <span class="n">byte</span> <span class="o">|=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint8_t</span><span class="o">&gt;</span><span class="p">(</span><span class="mi">1</span> <span class="o">&lt;&lt;</span> <span class="n">i</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="n">result</span> <span class="o">|=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">value_type</span><span class="o">&gt;</span><span class="p">(</span><span class="n">byte</span> <span class="o">&amp;</span> <span class="mh">0x7F</span><span class="p">)</span> <span class="o">&lt;&lt;</span> <span class="n">shift</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="k">if</span> <span class="p">((</span><span class="n">byte</span> <span class="o">&amp;</span> <span class="mh">0x80</span><span class="p">)</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="k">break</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="n">shift</span> <span class="o">+=</span> <span class="mi">7</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">result</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p><strong>Aside:</strong> This implementation routes each byte LSB-first through a bit-level sink, for consistency with the rest of the series. Real VByte implementations do not use a bit sink at all: they write bytes directly to a buffer. A production VByte encoder is a tight loop over <code>byte = (n &amp; 0x7F) | 0x80; *ptr++ = byte; n &gt;&gt;= 7;</code>, terminating with <code>*ptr++ = n</code>. No bit shuffling, no branching on byte boundaries. The bit-level wrapper here exists only to keep the pedagogical interface uniform.</p>
<hr>
<h2 id="the-implied-prior">The Implied Prior</h2>
<p>VByte&rsquo;s length function is:</p>
$$L(n) = 8 \cdot \left\lceil \frac{\lfloor \log_2(n+1) \rfloor + 1}{7} \right\rceil \text{ bits}$$<p>with a minimum of 8 bits. Equivalently, the number of bytes is $\lceil \text{bit_width}(n) / 7 \rceil$.</p>
<p>Within each byte-length tier, all values have the same encoded length:</p>
<ul>
<li>1 byte (8 bits): $n \in [0, 127]$, all 128 values.</li>
<li>2 bytes (16 bits): $n \in [128, 16383]$, all 16256 values.</li>
<li>3 bytes (24 bits): $n \in [16384, 2097151]$, and so on.</li>
</ul>
<p>This &ldquo;step-uniform&rdquo; distribution is VByte&rsquo;s implied prior: within each tier, it treats all values as equally likely. VByte is optimal for sources where the byte-length of each value is geometrically distributed, with most values falling in the first few tiers.</p>
<p>Real-world integer distributions for database columns, network port numbers, document lengths, and similar data tend to be heavily skewed toward small values. For these sources, VByte is competitive: most values land in the 1-byte or 2-byte tier, so average encoded length stays close to the minimum.</p>
<p>VByte does not adapt within a tier. If your values cluster heavily below 64 (half the 1-byte range), VByte wastes the unused bits in each byte. In that case, Elias gamma would be more efficient. The trade is explicit: VByte gives up intra-tier precision for byte-alignment.</p>
<hr>
<h2 id="length-comparison">Length Comparison</h2>
<p>How does VByte compare to the universal codes from earlier posts? The table below shows codeword lengths in bits for selected values:</p>
<table>
	<thead>
			<tr>
					<th>$n$</th>
					<th>VByte</th>
					<th>Elias Gamma</th>
					<th>Elias Delta</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>1</td>
					<td>8</td>
					<td>1</td>
					<td>1</td>
			</tr>
			<tr>
					<td>100</td>
					<td>8</td>
					<td>13</td>
					<td>11</td>
			</tr>
			<tr>
					<td>1,000</td>
					<td>16</td>
					<td>19</td>
					<td>16</td>
			</tr>
			<tr>
					<td>$2^{20}$</td>
					<td>24</td>
					<td>41</td>
					<td>29</td>
			</tr>
			<tr>
					<td>$2^{32}$</td>
					<td>40</td>
					<td>65</td>
					<td>43</td>
			</tr>
	</tbody>
</table>
<p>For small values (1 to 127), VByte is much worse than Gamma or Delta: 8 bits vs 1 bit for $n = 1$. Byte-alignment is expensive at the small end.</p>
<p>For medium values (around 1000), VByte is competitive with Delta. For large values, VByte&rsquo;s growth rate is $8 \lceil \log_2(n) / 7 \rceil \approx 1.14 \log_2(n)$, while Gamma grows as $2 \log_2(n)$ and Delta grows as approximately $\log_2(n) + 2\log_2(\log_2(n))$. For large $n$, VByte is actually more compact than Gamma and not far behind Delta.</p>
<p>The pattern: for very small values, use Gamma or a short fixed-width code. For skewed data with occasional large values, VByte often beats everything else once you factor in decoding speed.</p>
<hr>
<h2 id="why-it-wins-in-practice">Why It Wins in Practice</h2>
<p>VByte&rsquo;s advantage is not in bits-per-symbol. It is in decoding throughput.</p>
<p>A bit-level Elias gamma decoder must maintain a bit offset within a byte, mask bits across byte boundaries, and branch on every symbol. A VByte decoder reads whole bytes, uses a single <code>&amp; 0x7F</code> to extract 7 data bits, and checks a single flag bit. The inner loop is:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-c" data-lang="c"><span class="line"><span class="cl"><span class="c1">// Production VByte decoder (byte-level, not bit-level):
</span></span></span><span class="line"><span class="cl"><span class="k">while</span> <span class="p">(</span><span class="n">byte</span> <span class="o">&amp;</span> <span class="mh">0x80</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">result</span> <span class="o">|=</span> <span class="p">(</span><span class="n">byte</span> <span class="o">&amp;</span> <span class="mh">0x7F</span><span class="p">)</span> <span class="o">&lt;&lt;</span> <span class="n">shift</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">shift</span> <span class="o">+=</span> <span class="mi">7</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="n">byte</span> <span class="o">=</span> <span class="o">*</span><span class="n">ptr</span><span class="o">++</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="n">result</span> <span class="o">|=</span> <span class="n">byte</span> <span class="o">&lt;&lt;</span> <span class="n">shift</span><span class="p">;</span>
</span></span></code></pre></div><p>Modern CPUs execute this at cache-fill bandwidth: around 1 to 3 bytes per clock cycle. For a stream of small values where most decode as a single byte, VByte decoding saturates L1 cache bandwidth.</p>
<p>The SIMD VByte decoders (such as the ones in Daniel Lemire&rsquo;s library, implemented with AVX2) process 128 or 256 bits at a time by predicting lengths from the continuation bits, shuffling bytes, and accumulating results in vector registers. These decoders achieve several GB/s of throughput on modern hardware.</p>
<p>Bit-level Gamma or Delta decoders, by contrast, typically top out around 100 to 200 MB/s because each symbol requires multiple shifts and masks that cannot easily be vectorized.</p>
<hr>
<h2 id="the-engineering-trade">The Engineering Trade</h2>
<p>VByte is the engineer&rsquo;s compromise: theoretically suboptimal, practically dominant. This is not a tension to resolve; it is a fact to internalize.</p>
<p>The information-theoretic case says Elias gamma is more efficient for small values, Elias delta is more efficient for a wide range of values, and Rice coding is more efficient when the distribution is geometric and you know the mean. All of these statements are true. In bits per symbol, VByte loses.</p>
<p>The engineering analysis says the constant factor of bit-vs-byte operations swamps the small length savings for high-throughput workloads. A gamma decoder that saves 3 bits per integer but decodes at 150 MB/s is slower in wall-clock time than a VByte decoder that wastes 5 bits per integer but decodes at 3 GB/s. Byte-aligned and theoretically suboptimal beats bit-optimal and slow. This is not a coincidence; it is why byte-alignment exists.</p>
<p>This pattern recurs throughout systems engineering. A theoretically optimal sort requires $O(n \log n)$ comparisons; radix sort uses $O(nk)$ operations and often wins in practice because $k$ is fixed and the constant factor is small. VByte is the radix sort of integer compression.</p>
<p>The lesson is not that theory is wrong. It is that the relevant model for engineering decisions includes hardware constants, cache effects, and parallelism, not just asymptotic bit counts.</p>
<hr>
<h2 id="cross-references-and-footnotes">Cross-references and Footnotes</h2>
<p><strong>Next in series:</strong> Post 9 covers Huffman coding (forthcoming). Huffman shifts from universal codes to entropy-optimal codes: given a known source distribution, Huffman constructs the optimal prefix-free code. Unlike Rice/Golomb (which assume a specific parametric family) or VByte (which ignores the distribution), Huffman uses the actual probabilities.</p>
<p><strong>Earlier in series:</strong></p>
<ul>
<li><a href="/post/2022-06-elias-gamma-wire-formats/">Unary and Elias Gamma</a> (post 4): Gamma is the bit-level alternative to VByte for sources with heavy skew toward small values.</li>
<li><a href="/post/2022-11-elias-delta-omega-wire-formats/">Elias Delta and Omega</a> (post 5): Delta is more bit-efficient than VByte for large values, at the cost of decoding complexity.</li>
<li><a href="/post/2023-04-fibonacci-wire-formats/">Fibonacci Coding</a> (post 6): Fibonacci codes also encode one integer per read without byte alignment, using a very different structural property.</li>
<li><a href="/post/2023-09-rice-golomb-wire-formats/">Rice / Golomb</a> (post 7): for geometrically distributed data with known mean, Rice outperforms VByte in bits-per-symbol, but VByte wins in decoding speed.</li>
</ul>
<p><strong>Cross-series:</strong> Byte-alignment is orthogonal to the type-algebra story in the Stepanov bridge posts. VByte&rsquo;s byte structure does not compose with the <code>Either</code>/<code>Product</code> algebraic codec combinators in any natural way; it is a flat encoding designed for performance, not algebraic regularity.</p>
<p><strong>Production code:</strong> The <code>VByte</code> struct in this post matches the algorithmic structure of the production implementation in <a href="https://github.com/queelius/wire-formats/tree/master/lib/pfc">PFC</a>: see <code>include/pfc/codecs.hpp</code>. The PFC version integrates with <code>BitReader</code>/<code>BitWriter</code> infrastructure from <code>core.hpp</code> in the same bit-level style used here. For the SIMD-optimized byte-level version used in production databases, see Google&rsquo;s protobuf-cpp or Daniel Lemire&rsquo;s <code>FastPFOR</code> and <code>streamvbyte</code> libraries.</p>
]]></content:encoded><media:content url="https://metafunctor.com/post/2024-02-vbyte-wire-formats/card.jpg" medium="image"><media:title type="plain">Information-Theory</media:title></media:content><category>C++</category><category>information-theory</category><category>coding-theory</category><category>prefix-free</category><category>universal-codes</category><category>vbyte</category><category>varint</category><category>byte-aligned</category></item><item><title>Entropy Maps</title><link>https://metafunctor.com/post/entropy-map/</link><pubDate>Sun, 18 Feb 2024 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/entropy-map/</guid><description>Entropy maps use prefix-free hash codes to approximate functions without storing the domain, achieving information-theoretic space bounds with controllable error.</description><content:encoded><![CDATA[<p>The PDF version of this post is available on <a href="https://github.com/queelius/bernoulli_data_type/tree/master/entropy-maps-paper/entropy-map.pdf">GitHub</a>.</p>
<p>An entropy map approximates a function $f : \mathcal{X} \to \mathcal{Y}$ by hashing domain values to prefix-free codes in the codomain. We store nothing about the domain itself. We just hash, and a prefix of that hash serves as a code for a codomain value.</p>
<p>We allow multiple codes per codomain value. For instance, the value <code>a</code> might be encoded by <code>00</code>, <code>01</code>, <code>10</code>, and <code>11</code>. If the hash is less than 4, we decode it as <code>a</code>.</p>
<p>Suppose $\Pr\lbrace f(X) = y\rbrace = p_y$ where $X \sim p_X$. The optimally space-efficient code, assuming a uniform hash function $h$, assigns prefix-free codes for $y$ whose probability of being selected by $h$ sums to $p_y$. The expected bit length is
</p>
$$
    \ell = -\sum_{y \in \mathcal{Y}} p_y \log_2 p_y,
$$<p>
which is the entropy of the output distribution. That is why we call it an entropy map.</p>
<p>If $\mathcal{X}$ is finite, we can think of it as implicitly encoding the domain and storing the prefix-free code for each domain element. The average bit length per element is $\ell$, and the total is $|X| \ell$.</p>
<h2 id="rate-distortion-bernoulli-maps">Rate distortion: Bernoulli maps</h2>
<p>We can allow errors. If one codomain value $y&rsquo;$ is very common (say $p_{y&rsquo;} &gt; .99$), we can give it a prefix-free code that covers probability $p_{y&rsquo;}$ and then skip coding for it in the entropy map. A random $x \in \mathcal{X}$ will map to $y&rsquo;$ with probability $p_{y&rsquo;}$ (which can be made as close to 1 as desired by trading space for accuracy). For the remaining domain values, we code them correctly, or allow errors on those too after attempting correct coding.</p>
<h3 id="bernoulli-set-indicator-function">Bernoulli set-indicator function</h3>
<p>Consider a set-indicator function
</p>
$$
    1_{\mathcal{A}} : \mathcal{X} \to \lbrace0,1\rbrace,
$$<p>
where $\mathcal{A} \subseteq \mathcal{X}$ and $\mathcal{X}$ is very large (possibly infinite). We assign prefix-free codes for codomain value $1$ such that a random hash maps an element of $\mathcal{X}$ to a code for $1$ with probability $\varepsilon$, where $\varepsilon$ is small (say $2^{-10}$).</p>
<p>There exists a (countably infinite) set of hash functions that hash all elements in $\mathcal{A}$ to codes for $1$ and elements in $\mathcal{A}&rsquo; = \mathcal{X} \setminus \mathcal{A}$ to codes for either $0$ or $1$. Choosing a random hash function with this property, we expect $\varepsilon$ of the elements in $\mathcal{A}&rsquo;$ to hash to $1$ (false positives) and the remaining $1 - \varepsilon$ to hash to $0$.</p>
<p>For any $x \in \mathcal{X}$, we test membership by checking whether a prefix of $h(x)$ is a code for $0$ or $1$. If it is a code for $0$, then $x$ is definitely not in $\mathcal{A}$. If it is a code for $1$, then $x$ is in $\mathcal{A}$ with a false positive rate of $\varepsilon$ and a true positive rate of $1$, since we explicitly chose a hash function that maps all elements of $\mathcal{A}$ to codes for $1$.</p>
<p>There is an interesting framing here. The entropy map starts as a compression problem, but it is also a rate-distortion problem. In the set-indicator approximation above, we are implicitly choosing to minimize a loss function where false negatives are much more costly than false positives. Either because negative elements are rarely queried, or because false positives are cheap compared to false negatives (falsely thinking a rustling in the bushes is a tiger is much less costly than failing to notice an actual tiger).</p>
<p>We call this a Bernoulli set-indicator function, <code>bernoulli&lt;(set&lt;X&gt;, X) -&gt; bool&gt;{</code> $1_A$ <code>}</code>. This is what gets communicated, not the latent function $1_A$.</p>
<p>The confusion matrix for a hash function conditioned on mapping all of $\mathcal{A}$ to codes for $1$:</p>
<p>Table 1: Conditional distribution of Bernoulli set-indicator functions on $\mathcal{X} = \lbrace a,b\rbrace$</p>
<table>
	<thead>
			<tr>
					<th>latent/observed</th>
					<th>$1_\emptyset$</th>
					<th>$1_{\lbrace a\rbrace}$</th>
					<th>$1_{\lbrace b\rbrace}$</th>
					<th>$1_{\lbrace a,b\rbrace}$</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>$1_\emptyset$</td>
					<td>$(1-\varepsilon)^2$</td>
					<td>$(1-\varepsilon)\varepsilon$</td>
					<td>$(1-\varepsilon)\varepsilon$</td>
					<td>$\varepsilon^2$</td>
			</tr>
			<tr>
					<td>$1_{\lbrace a\rbrace}$</td>
					<td>$0$</td>
					<td>$1-\varepsilon$</td>
					<td>$0$</td>
					<td>$\varepsilon$</td>
			</tr>
			<tr>
					<td>$1_{\lbrace b\rbrace}$</td>
					<td>$0$</td>
					<td>$0$</td>
					<td>$1-\varepsilon$</td>
					<td>$\varepsilon$</td>
			</tr>
			<tr>
					<td>$1_{\lbrace a,b\rbrace}$</td>
					<td>$0$</td>
					<td>$0$</td>
					<td>$0$</td>
					<td>$1$</td>
			</tr>
	</tbody>
</table>
<p>The no-false-negatives constraint produces a lot of zeros. If we observe <code>bernoulli&lt;set&lt;X&gt;,X) -&gt; bool&gt;{</code>$1_{\lbrace a\rbrace}$<code>}</code>, the latent function is either $1_{\emptyset}$ or $1_{\lbrace a\rbrace}$. Since $\varepsilon$ is small, $1_{\lbrace a\rbrace}$ is much more likely.</p>
<p>The maximum degrees of freedom for this confusion matrix:</p>
<p>Table 2: Confusion matrix with maximum degrees of freedom</p>
<table>
	<thead>
			<tr>
					<th>latent/observed</th>
					<th>$1_\emptyset$</th>
					<th>$1_{\lbrace a\rbrace}$</th>
					<th>$1_{\lbrace b\rbrace}$</th>
					<th>$1_{\lbrace a,b\rbrace}$</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>$1_\emptyset$</td>
					<td>$p_{1 1}$</td>
					<td>$p_{1 2}$</td>
					<td>$p_{1 3}$</td>
					<td>$1-p_{1 1}-p_{1 2}-p_{1 3}$</td>
			</tr>
			<tr>
					<td>$1_{\lbrace a\rbrace}$</td>
					<td>$p_{2 1}$</td>
					<td>$p_{2 2}$</td>
					<td>$p_{2 3}$</td>
					<td>$1-p_{2 1}-p_{2 2}-p_{2 3}$</td>
			</tr>
			<tr>
					<td>$1_{\lbrace b\rbrace}$</td>
					<td>$p_{3 1}$</td>
					<td>$p_{3 2}$</td>
					<td>$p_{3 3}$</td>
					<td>$1-p_{3 1}-p_{3 2}-p_{3 3}$</td>
			</tr>
			<tr>
					<td>$1_{\lbrace a,b\rbrace}$</td>
					<td>$p_{4 1}$</td>
					<td>$p_{4 2}$</td>
					<td>$p_{4 3}$</td>
					<td>$1-p_{4 1}-p_{4 2}-p_{4 3}$</td>
			</tr>
	</tbody>
</table>
<p>That is $4 \times (4 - 1) = 12$ degrees of freedom. Table 1 has just 1 degree of freedom ($\varepsilon$).</p>
<p>Degrees of freedom give a measure of model complexity. More parameters means more data needed for estimation, though frequently we already know the parameters because they were specified by the algorithm that generated the Bernoulli approximation.</p>
<h3 id="boolean-bernoulli-as-constant-function">Boolean Bernoulli as constant function</h3>
<p>How many functions of type <code>() -&gt; bool</code>? Two: <code>true</code> and <code>false</code>. That is $|\lbrace true, false\rbrace|^{|\lbrace1\rbrace|} = 2^1 = 2$.</p>
<p>We can think of Boolean values as functions <code>() -&gt; bool</code>. Applying the Bernoulli model <code>bernoulli&lt;() -&gt; bool&gt;</code> gives the same result as before.</p>
<p>Table 3: Confusion matrix for Bernoulli model on Boolean values</p>
<table>
	<thead>
			<tr>
					<th>latent/observed</th>
					<th><code>true</code></th>
					<th><code>false</code></th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>true</code></td>
					<td>$p_{1 1}$</td>
					<td>$1-p_{1 1}$</td>
			</tr>
			<tr>
					<td><code>false</code></td>
					<td>$1-p_{2 2}$</td>
					<td>$p_{2 2}$</td>
			</tr>
	</tbody>
</table>
<p>Maximum of two degrees of freedom. In the binary symmetric channel, $p_{1 1} = p_{2 2}$:</p>
<p>Table 4: Symmetric confusion matrix for Bernoulli Boolean</p>
<table>
	<thead>
			<tr>
					<th>latent/observed</th>
					<th><code>true</code></th>
					<th><code>false</code></th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td><code>true</code></td>
					<td>$p$</td>
					<td>$1-p$</td>
			</tr>
			<tr>
					<td><code>false</code></td>
					<td>$1-p$</td>
					<td>$p$</td>
			</tr>
	</tbody>
</table>
<h3 id="conditional-distribution-of-latent-function-given-observed-function">Conditional distribution of latent function given observed function</h3>
<p>Once we have an observation <code>bernoulli&lt;set&lt;X&gt;,X) -&gt; bool&gt;{x}</code>, what does the confusion matrix tell us? Let me abstract the problem.</p>
<p>Let $X$ and $Y$ be random variables. Suppose $P(X = x | Y = y)$ is hard to compute, but $P(Y = y | X = x)$ is easy. (This is exactly the situation with confusion matrices: we know the conditional distribution of the observed function given the latent one, but we want the reverse.)</p>
<p>Bayes&rsquo; rule:
</p>
$$
P(X = x | Y = y) = \frac{P(Y = y | X = x) P(X = x)}{P(Y = y)}
$$<p>We need two things. First, $P(X = x)$, the prior. If we know the distribution of $X$, encode it. Otherwise use an uninformed prior (uniform).</p>
<p>Second, $P(Y = y)$, the normalizing constant:
</p>
$$
P(Y = y) = \sum_{x'} P(Y = y | X = x') P(X = x')
$$<p>
With a uniform prior over finite $|X|$, $P(X = x) = 1/|X|$, and:
</p>
$$
P(X = x | Y = y) = \frac{P(Y = y | X = x)}{\sum_{x'} P(Y = y | X = x')}
$$<p>Applying this to the set-indicator case: replace $X$ with the latent indicator and $Y$ with the observed Bernoulli indicator. Read the confusion matrix, pick out the relevant column, normalize.</p>
<p>For example, observing $1_{\lbrace a\rbrace}$: the column is $(p_{1 2}, p_{2 2}, p_{3 2}, p_{4 2})&rsquo;$. The conditional probability is
</p>
$$
p_{k|2} = \frac{p_{k 2}}{\sum_{j=1}^4 p_{j 2}},
$$<p>
where $k$ indexes the latent function and we condition on column 2.</p>
<p>More generally:
</p>
$$
p_{k|i} = \frac{p_{k i}}{\sum_{j=1}^4 p_{j i}}.
$$<p>Doing this for all four observed indicators using the one-parameter confusion matrix from Table 1:</p>
<p>Table 5: Conditional probability of latent indicator given observed indicator</p>
<table>
	<thead>
			<tr>
					<th>observed/latent</th>
					<th>$1_\emptyset$</th>
					<th>$1_{\lbrace a\rbrace}$</th>
					<th>$1_{\lbrace b\rbrace}$</th>
					<th>$1_{\lbrace a,b\rbrace}$</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>$1_\emptyset$</td>
					<td>$1$</td>
					<td>$0$</td>
					<td>$0$</td>
					<td>$0$</td>
			</tr>
			<tr>
					<td>$1_{\lbrace a\rbrace}$</td>
					<td>$\varepsilon/(1+\varepsilon)$</td>
					<td>$1/(1+\varepsilon)$</td>
					<td>$0$</td>
					<td>$0$</td>
			</tr>
			<tr>
					<td>$1_{\lbrace b\rbrace}$</td>
					<td>$\varepsilon/(1+\varepsilon)$</td>
					<td>$0$</td>
					<td>$1/(1+\varepsilon)$</td>
					<td>$0$</td>
			</tr>
			<tr>
					<td>$1_{\lbrace a,b\rbrace}$</td>
					<td>$\varepsilon^2/(1+\varepsilon)^2$</td>
					<td>$\varepsilon/(1+\varepsilon)^2$</td>
					<td>$\varepsilon/(1+\varepsilon)^2$</td>
					<td>$1/(1+\varepsilon)^2$</td>
			</tr>
	</tbody>
</table>
<p>When we observe the empty set in a model where false negatives are impossible, we know the latent function is $1_\emptyset$ with certainty. Observing $1_{\lbrace a\rbrace}$, we know the latent is either $1_{\emptyset}$ or $1_{\lbrace a\rbrace}$, with the latter much more likely since $\varepsilon$ is small.</p>
<h2 id="algorithms">Algorithms</h2>
<p>The simplest algorithm: hash domain values concatenated with a bit string $b$ and decode the result. Find $b$ such that $h(x + b)$ decodes to the correct $f(x)$ for all $x \in \mathcal{X}$.</p>
<h3 id="two-level-hash-function-evaluation">Two-level hash function evaluation</h3>
<p>The practical version uses a two-level scheme. First, hash each $x \in \mathcal{X}$ concatenated with a bit string $b$ (same as before), but use the result to index into a hash table $H$ at position $j$. Then choose a bit string for $H[j]$ for each $x$ mapping to $j$ such that $f(x) = \text{decode}(h(x + H[j]))$.</p>
<p>This keeps the success probability $p_j = \prod_x \Pr\lbrace f(x) = \text{decode}(h(x + H[j]))\rbrace$ roughly constant for each bucket, independent of the total domain size, by choosing an appropriately sized hash table.</p>
<p>Each decoding is an independent Bernoulli trial. The probability that a particular $x$ hashing to bucket $j$ is decoded correctly is the fraction of hash values that are valid prefix-free codes for $f(x)$.</p>
<h3 id="oblivious-entropy-maps">Oblivious entropy maps</h3>
<p>An <em>oblivious</em> entropy map applies the hash function to trapdoors of $\mathcal{X}$ rather than elements directly, and the prefix-free codes for $\mathcal{Y}$ have no discernible structure (a random assignment of hash values to codomain values). This is relevant to my work on encrypted search.</p>
]]></content:encoded><media:content url="https://metafunctor.com/post/entropy-map/featured.webp" medium="image"><media:title type="plain">Information-Theory</media:title></media:content><category>entropy-map</category><category>rate-distortion</category><category>bernoulli-map</category><category>probabilistic-data-structures</category><category>hashing</category><category>information-theory</category></item><item><title>Perfect Hashing: Space Bounds, Entropy, and Cryptographic Security</title><link>https://metafunctor.com/post/2024-02-01-crypto-perf-hash/</link><pubDate>Thu, 01 Feb 2024 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-02-01-crypto-perf-hash/</guid><description>Space bounds, entropy requirements, and cryptographic security properties of perfect hash functions.</description><content:encoded><![CDATA[<p>Can a perfect hash function be cryptographically secure, space-optimal, and maximum-entropy encoded all at once? This paper proves such a construction exists and analyzes exactly what you sacrifice to get all three.</p>
<h2 id="the-impossible-triangle">The Impossible Triangle</h2>
<p>Perfect hash functions typically face tradeoffs. Space-optimal constructions (CHD, BDZ) sacrifice randomness. Cryptographic hash functions waste space on collision resistance. Maximum-entropy encodings require extra bits.</p>
<p>The question: can you have all three?</p>
<h2 id="the-construction">The Construction</h2>
<p>The data type is <em><em>PH = {0,1}</em> x N</em>* with a constructor <code>ph(X, r) = (n', N)</code> where:</p>
<ul>
<li><strong>N = ceil(m/r)</strong>: Hash table size (where m = |X|)</li>
<li><strong>beta(x,n) = trunc(hash(x&rsquo; # n&rsquo;), k)&rsquo; mod N</strong>: Hash function parameterized by seed n</li>
<li><strong>n = min{j in N | beta is collision-free on X}</strong>: Search for smallest collision-free seed</li>
<li><strong>n&rsquo;</strong>: Geometric code encoding n (variable-length prefix-free)</li>
</ul>
<p>The algorithm: try seeds n = 0, 1, 2, &hellip; until beta(.,n) has no collisions on X. Each trial is geometrically distributed with success probability p(m,r), so the expected space for encoding n&rsquo; achieves the information-theoretic lower bound of roughly 1.44 bits per element.</p>
<p>Under the random oracle assumption, hash: {0,1}* -&gt; {0,1}^infinity outputs uniform random bits, making the final encoding (n&rsquo;, N) indistinguishable from a random bit string.</p>
<h2 id="rate-distortion-tradeoff">Rate-Distortion Tradeoff</h2>
<p>The &ldquo;rate-distortion&rdquo; framing comes from information theory: what&rsquo;s the rate (bits per key) for a given distortion (lookup time)?</p>
<p>Zero distortion (O(1) lookup): roughly n log n bits. Constant distortion (small tables): practical two-level schemes approach roughly 1.44n bits. Variable distortion: trade bits for lookup time continuously.</p>
<h2 id="why-cryptographic-matters">Why Cryptographic Matters</h2>
<p>Non-cryptographic perfect hashes are deterministic, so adversaries can engineer collision-inducing inputs. A cryptographic perfect hash (random oracle) prevents adversarial key selection (can&rsquo;t craft keys that break the hash), side-channel attacks (encoding reveals no information about keys), and fingerprinting (maximum entropy makes encodings look random).</p>
<h2 id="the-algebra-of-composition">The Algebra of Composition</h2>
<p>Section 5 proves that composing perfect hash functions preserves injectivity. If h1: S -&gt; T and h2: T -&gt; U are injective, then h2 composed with h1: S -&gt; U is injective.</p>
<p>This connects to my <code>algebraic_hashing</code> library, where composition of cryptographic hashes via XOR preserves both security and structure.</p>
<h2 id="connection-to-oblivious-computing">Connection to Oblivious Computing</h2>
<p>This paper is fundamentally about minimizing information leakage while maintaining functionality, which is the core of the oblivious computing philosophy. The maximum-entropy encoding hides which permutation you&rsquo;re using, even though the function is deterministic.</p>
<p><strong>Research paper</strong> • 4★ • <a href="/latex/crypto-perf-hash/">View PDF</a> • <a href="https://github.com/queelius/crypto-perf-hash">GitHub</a></p>
]]></content:encoded><media:content url="https://metafunctor.com/post/2024-02-01-crypto-perf-hash/card.jpg" medium="image"><media:title type="plain">Information-Theory</media:title></media:content><category>cryptography</category><category>hash functions</category><category>information theory</category><category>space complexity</category><category>entropy</category><category>perfect hashing</category></item><item><title>Rice / Golomb</title><link>https://metafunctor.com/post/2023-09-rice-golomb-wire-formats/</link><pubDate>Sun, 17 Sep 2023 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2023-09-rice-golomb-wire-formats/</guid><description>Rice and Golomb codes are parametric: a single parameter k (or m) tunes the code to a specific geometric distribution. Choosing k is choosing your prior precisely.</description><content:encoded><![CDATA[<p><em>Every code in this series so far has been fixed. Rice and Golomb are different: they take a parameter, and the parameter is your model of the data.</em></p>
<h2 id="the-first-parametric-code">The First Parametric Code</h2>
<p>Every code examined so far in this series has been monolithic. Unary coding is just unary coding. Elias gamma is just Elias gamma. Each one encodes all non-negative integers with a single fixed strategy. You do not get to choose anything about the code beyond whether to use it.</p>
<p>Rice and Golomb codes break this pattern. They are parametric: a single integer parameter, $k$ for Rice or $m$ for Golomb, tunes the code to a specific source distribution. Rice$(k)$ is not one code but a family of codes, one per value of $k$. Each member of the family is optimal for a specific geometric distribution. Choosing $k$ is choosing your prior precisely.</p>
<p>This matters because data sources are rarely uniform. Run-length encodings, inter-frame video differences, and the gap sequences in inverted indexes are all approximately geometrically distributed. If you know the mean of your source, you can pick $k$ so that Rice$(k)$ performs near-optimally, without the overhead of a Huffman table or arithmetic coding.</p>
<p>The key insight: for a geometric source with mean approximately $2^k$, Rice$(k)$ is within a small constant of entropy. No other universal code in this series achieves this. Elias gamma and delta perform well asymptotically but can be far from optimal for a specific geometric distribution with a known mean. Rice exploits that knowledge directly.</p>
<hr>
<h2 id="rice-coding">Rice Coding</h2>
<p>Rice coding splits a non-negative integer $n$ into two parts: a quotient $q = \lfloor n / 2^k \rfloor = n \gg k$ and a remainder $r = n \bmod 2^k = n \mathbin{&amp;} (2^k - 1)$.</p>
<p>The quotient is encoded in unary: $q$ zero bits followed by a stop bit of <code>1</code>. The remainder is encoded in exactly $k$ bits, MSB first. The total codeword is the concatenation of these two parts.</p>
<p>Codeword examples for $k = 2$ (remainder is always 2 bits):</p>
<table>
	<thead>
			<tr>
					<th>$n$</th>
					<th>$q$</th>
					<th>$r$</th>
					<th>Codeword</th>
					<th>Bits</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>0</td>
					<td>0</td>
					<td>0</td>
					<td><code>1 00</code></td>
					<td>3</td>
			</tr>
			<tr>
					<td>1</td>
					<td>0</td>
					<td>1</td>
					<td><code>1 01</code></td>
					<td>3</td>
			</tr>
			<tr>
					<td>2</td>
					<td>0</td>
					<td>2</td>
					<td><code>1 10</code></td>
					<td>3</td>
			</tr>
			<tr>
					<td>3</td>
					<td>0</td>
					<td>3</td>
					<td><code>1 11</code></td>
					<td>3</td>
			</tr>
			<tr>
					<td>4</td>
					<td>1</td>
					<td>0</td>
					<td><code>0 1 00</code></td>
					<td>4</td>
			</tr>
			<tr>
					<td>5</td>
					<td>1</td>
					<td>1</td>
					<td><code>0 1 01</code></td>
					<td>4</td>
			</tr>
	</tbody>
</table>
<p>Codeword length: $(n \gg k) + 1 + k$ bits. The Kraft sum saturates to 1, so Rice is a complete prefix-free code.</p>
<p>The implied prior is geometric: $P(n) \propto (2^k / (2^k + 1))^{n/2^k}$. For a geometric source with mean $\mu$, the optimal $k$ satisfies $k \approx \log_2(\mu)$.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">K</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Rice</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static_assert</span><span class="p">(</span><span class="n">K</span> <span class="o">&gt;</span> <span class="mi">0</span> <span class="o">&amp;&amp;</span> <span class="n">K</span> <span class="o">&lt;</span> <span class="mi">64</span><span class="p">,</span> <span class="s">&#34;K must be in [1, 63]&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="n">value_type</span> <span class="n">n</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span> <span class="n">q</span> <span class="o">=</span> <span class="n">n</span> <span class="o">&gt;&gt;</span> <span class="n">K</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span> <span class="n">r</span> <span class="o">=</span> <span class="n">n</span> <span class="o">&amp;</span> <span class="p">((</span><span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="p">{</span><span class="mi">1</span><span class="p">}</span> <span class="o">&lt;&lt;</span> <span class="n">K</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">q</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">false</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">true</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">K</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">            <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(((</span><span class="n">r</span> <span class="o">&gt;&gt;</span> <span class="p">(</span><span class="n">K</span> <span class="o">-</span> <span class="mi">1</span> <span class="o">-</span> <span class="n">i</span><span class="p">))</span> <span class="o">&amp;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">!=</span> <span class="mi">0</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span> <span class="n">q</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">while</span> <span class="p">(</span><span class="o">!</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">())</span> <span class="o">++</span><span class="n">q</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span> <span class="n">r</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">K</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">            <span class="n">r</span> <span class="o">=</span> <span class="p">(</span><span class="n">r</span> <span class="o">&lt;&lt;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">|</span> <span class="p">(</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">()</span> <span class="o">?</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="p">{</span><span class="mi">1</span><span class="p">}</span> <span class="o">:</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="p">{</span><span class="mi">0</span><span class="p">});</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="p">(</span><span class="n">q</span> <span class="o">&lt;&lt;</span> <span class="n">K</span><span class="p">)</span> <span class="o">|</span> <span class="n">r</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>The decode mirrors the encode exactly: count zeros for $q$, read $k$ bits for $r$, reconstruct $n = (q \ll k) \mid r$. No tables, no branches beyond the unary scan.</p>
<hr>
<h2 id="golomb-coding">Golomb Coding</h2>
<p>Rice coding requires $m = 2^k$, a power-of-two divisor. For sources where the optimal mean is not a power of two, this forces a choice: round $k$ down and underfit, or round $k$ up and overfit. Golomb coding removes the restriction by allowing any positive integer $m$.</p>
<p>The quotient part is the same: $q = \lfloor n / m \rfloor$ zero bits, then a stop <code>1</code>. The remainder $r = n \bmod m$ is encoded in <em>truncated binary</em> rather than a fixed-width field.</p>
<p>Truncated binary encodes values in $[0, m)$ using the minimal-length prefix-free code. Let $b = \lceil \log_2 m \rceil$ and $c = 2^b - m$ (the &ldquo;cutoff&rdquo;). Values $r &lt; c$ are encoded in $b - 1$ bits; values $r \geq c$ are encoded in $b$ bits as $r + c$.</p>
<p>For $m = 5$: $b = 3$, $c = 3$. Values 0, 1, 2 use 2 bits; values 3, 4 use 3 bits. The codewords:</p>
<table>
	<thead>
			<tr>
					<th>$n$</th>
					<th>$q$</th>
					<th>$r$</th>
					<th>Codeword</th>
					<th>Bits</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>0</td>
					<td>0</td>
					<td>0</td>
					<td><code>1 00</code></td>
					<td>3</td>
			</tr>
			<tr>
					<td>1</td>
					<td>0</td>
					<td>1</td>
					<td><code>1 01</code></td>
					<td>3</td>
			</tr>
			<tr>
					<td>2</td>
					<td>0</td>
					<td>2</td>
					<td><code>1 10</code></td>
					<td>3</td>
			</tr>
			<tr>
					<td>3</td>
					<td>0</td>
					<td>3</td>
					<td><code>1 110</code></td>
					<td>4</td>
			</tr>
			<tr>
					<td>4</td>
					<td>0</td>
					<td>4</td>
					<td><code>1 111</code></td>
					<td>4</td>
			</tr>
			<tr>
					<td>5</td>
					<td>1</td>
					<td>0</td>
					<td><code>01 00</code></td>
					<td>4</td>
			</tr>
	</tbody>
</table>
<p>When $m$ is a power of two, $c = 0$ and truncated binary reduces to fixed-width $k$-bit encoding, making Golomb$(m)$ identical to Rice$(k)$ where $k = \log_2 m$.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">template</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">M</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Golomb</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static_assert</span><span class="p">(</span><span class="n">M</span> <span class="o">&gt;=</span> <span class="mi">1</span><span class="p">,</span> <span class="s">&#34;M must be at least 1&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="n">value_type</span> <span class="n">n</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span> <span class="n">q</span> <span class="o">=</span> <span class="n">n</span> <span class="o">/</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">M</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span> <span class="n">r</span> <span class="o">=</span> <span class="n">n</span> <span class="o">%</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">M</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">q</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">false</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">true</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">detail</span><span class="o">::</span><span class="n">truncated_binary_encode</span><span class="p">(</span><span class="n">r</span><span class="p">,</span> <span class="n">M</span><span class="p">,</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span> <span class="n">q</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">while</span> <span class="p">(</span><span class="o">!</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">())</span> <span class="o">++</span><span class="n">q</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span> <span class="n">r</span> <span class="o">=</span> <span class="n">detail</span><span class="o">::</span><span class="n">truncated_binary_decode</span><span class="p">(</span><span class="n">M</span><span class="p">,</span> <span class="n">source</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">q</span> <span class="o">*</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">M</span><span class="p">)</span> <span class="o">+</span> <span class="n">r</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>Rice is the fast path, Golomb is the general case. I use Rice when the data distribution is known well enough that a power-of-two mean is a reasonable approximation, and Golomb when I need the extra precision.</p>
<hr>
<h2 id="the-parameter-selection">The Parameter Selection</h2>
<p>Knowing the code is not enough: you need to pick $k$ or $m$. Gallager and van Voorhis (1975) derived the optimal Golomb parameter for a geometric source.</p>
<p>For a geometric distribution over non-negative integers with mean $\mu$ (so $P(n) = (1-p)^n p$ with $p = 1/\mu$), the optimal Golomb parameter is:</p>
$$m^* = \left\lceil \frac{-1}{\log_2\!\left(\frac{\mu - 1}{\mu}\right)} \right\rceil$$<p>For Rice, which requires $m = 2^k$, the optimal $k$ is simply $k \approx \log_2(\mu)$:</p>
$$k^* = \text{round}(\log_2 \mu), \quad k \in [1, 62]$$<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="kr">inline</span> <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">optimal_golomb_m</span><span class="p">(</span><span class="kt">double</span> <span class="n">mean</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kt">double</span> <span class="n">p</span> <span class="o">=</span> <span class="p">(</span><span class="n">mean</span> <span class="o">-</span> <span class="mf">1.0</span><span class="p">)</span> <span class="o">/</span> <span class="n">mean</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="kt">double</span> <span class="n">m_star</span> <span class="o">=</span> <span class="o">-</span><span class="mf">1.0</span> <span class="o">/</span> <span class="n">std</span><span class="o">::</span><span class="n">log2</span><span class="p">(</span><span class="n">p</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">m</span> <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">round</span><span class="p">(</span><span class="n">m_star</span><span class="p">));</span>
</span></span><span class="line"><span class="cl">    <span class="k">return</span> <span class="n">std</span><span class="o">::</span><span class="n">max</span><span class="p">(</span><span class="n">m</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">size_t</span><span class="p">{</span><span class="mi">1</span><span class="p">});</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="kr">inline</span> <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">optimal_rice_k</span><span class="p">(</span><span class="kt">double</span> <span class="n">mean</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">if</span> <span class="p">(</span><span class="n">mean</span> <span class="o">&lt;=</span> <span class="mf">2.0</span><span class="p">)</span> <span class="k">return</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="kt">double</span> <span class="n">k_real</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">log2</span><span class="p">(</span><span class="n">mean</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">k</span> <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">max</span><span class="p">(</span><span class="mf">1.0</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">round</span><span class="p">(</span><span class="n">k_real</span><span class="p">)));</span>
</span></span><span class="line"><span class="cl">    <span class="k">return</span> <span class="n">std</span><span class="o">::</span><span class="n">min</span><span class="p">(</span><span class="n">k</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">size_t</span><span class="p">{</span><span class="mi">62</span><span class="p">});</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>The optimality property is verifiable: on a geometric source with mean $\approx 2^3 = 8$, Rice$\langle 3 \rangle$ has much lower redundancy than Rice$\langle 1 \rangle$. The redundancy test in the companion test suite (using the priors library from <a href="/post/2022-01-priors-wire-formats/">post 3</a>) confirms this: <code>r3 &lt; r1</code> by a wide margin, around 2 bits vs 10 bits of expected excess.</p>
<p>Knowing the mean gives you near-optimal compression for free. The selection formula is a one-liner. The engineering work is estimating the mean, and for most practical sources that estimate is available from the data itself.</p>
<hr>
<h2 id="use-cases">Use Cases</h2>
<p>Rice coding is standard in lossless audio compression. FLAC encodes the residuals of a linear prediction filter using Rice codes. Audio residuals, after prediction, are approximately geometrically distributed with a mean that can be estimated from recent samples. FLAC selects $k$ adaptively per frame, achieving near-optimal compression with simple, fast decode.</p>
<p>Golomb coding appears in image compression. JPEG-LS uses Golomb codes for its residuals. The non-power-of-2 divisors let JPEG-LS adapt more precisely to local image statistics, squeezing out the small efficiency loss from rounding $m$ to a power of two.</p>
<p>Bitmap formats and run-length encodings also use Golomb codes. A run of identical pixels has a geometrically distributed length under standard assumptions, and Golomb codes match that distribution directly.</p>
<p>The pattern: when your data is genuinely geometrically distributed and you can estimate the mean, Rice or Golomb beats every other universal code in this series. Estimating the mean is the engineering work. Once you have it, the code selection is trivial.</p>
<hr>
<h2 id="the-connection-to-huffman">The Connection to Huffman</h2>
<p>Rice and Golomb are parametric codes: given $k$ or $m$, the codewords are determined by a formula. Huffman coding, which we examine in post 9, is a <em>constructed</em> code: given the source probabilities, Huffman builds an optimal prefix-free code by a greedy tree construction.</p>
<p>For a geometric source with the optimal $k$, Rice coding is asymptotically as efficient as Huffman coding on that source. Rice has a small constant overhead (at most 1 bit above entropy per symbol) because it cannot achieve the exact codeword lengths that Huffman can. But Rice does not require building a tree at run time, transmitting the tree to the decoder, or storing the probability model. Rice decode is a single shift, mask, and unary scan. Huffman decode is a tree traversal.</p>
<p>The trade is real: Huffman is slightly more efficient on average, Rice is simpler and faster. For sources that are approximately geometric, Rice is the better engineering choice. For sources with arbitrary distributions, you need Huffman or arithmetic coding, which we examine later in the series.</p>
<p>Forthcoming: post 9 covers Huffman coding.</p>
<hr>
<h2 id="cross-references">Cross-references</h2>
<p><strong>Next in series:</strong> <a href="/post/2024-02-vbyte-wire-formats/">VByte / Varint</a> (post 8), which trades bit-level precision for byte-alignment. VByte is the workhorse of production columnar databases, not because it is theoretically optimal but because byte operations are fast.</p>
<p><strong>Earlier in series:</strong></p>
<ul>
<li><a href="/post/2022-01-priors-wire-formats/">Universal Codes as Priors</a> (post 3): the framework for measuring how well a code matches a source, including the redundancy metric used in the optimality tests here.</li>
<li><a href="/post/2022-06-elias-gamma-wire-formats/">Unary and Elias Gamma</a> (post 4): the unary coding underlying the quotient part of Rice/Golomb.</li>
<li><a href="/post/2022-11-elias-delta-omega-wire-formats/">Elias Delta and Omega</a> (post 5): bit-length-based universal codes.</li>
<li><a href="/post/2023-04-fibonacci-wire-formats/">Fibonacci Coding</a> (post 6): the Zeckendorf representation approach to self-delimiting codes.</li>
</ul>
<p><strong>Cross-series links:</strong> The Stepanov bridge posts (<a href="/post/2026-05-codecs-functors-stepanov/">Codecs as Functors</a> and <a href="/post/2026-05-prefix-free-stepanov/">Prefix-Free Codes and Generic Programming</a>) connect this parametric design to the algebraic type-codec story. The parameter $k$ in Rice is conceptually the &ldquo;tag-bit-width&rdquo; choice in the <code>Either</code> sum-type combinator: just as <code>Either&lt;L, R&gt;</code> allocates a fixed number of tag bits to distinguish the two cases, Rice allocates $k$ bits to the fixed-width remainder field. The quotient field is the &ldquo;which slot&rdquo; selector, the remainder is the &ldquo;value within the slot.&rdquo;</p>
<p><strong>Production code:</strong> The <code>Rice&lt;K&gt;</code> and <code>Golomb&lt;M&gt;</code> structs in this post correspond directly to the production implementations in <a href="https://github.com/queelius/wire-formats/tree/master/lib/pfc">PFC</a>: see <code>include/pfc/codecs.hpp</code>. The production version adds fixed-size queries, concept constraints, and integration with the <code>BitReader</code>/<code>BitWriter</code> infrastructure from <code>core.hpp</code>. The pedagogical versions here are self-contained and use the same algorithmic logic.</p>
]]></content:encoded><media:content url="https://metafunctor.com/post/2023-09-rice-golomb-wire-formats/card.jpg" medium="image"><media:title type="plain">Information-Theory</media:title></media:content><category>C++</category><category>information-theory</category><category>coding-theory</category><category>prefix-free</category><category>universal-codes</category><category>rice</category><category>golomb</category><category>geometric-distribution</category></item><item><title>Fibonacci Coding</title><link>https://metafunctor.com/post/2023-04-fibonacci-wire-formats/</link><pubDate>Sun, 23 Apr 2023 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2023-04-fibonacci-wire-formats/</guid><description>Fibonacci coding uses Zeckendorf's representation to produce self-synchronizing codewords. Every codeword ends in two consecutive ones; a single bit flip corrupts at most two codewords.</description><content:encoded><![CDATA[<p><em>Every code in this series so far has optimized expected length under some implied prior. Fibonacci coding does something different: it gives the decoder a way to recover from errors without help from a lower layer.</em></p>
<h2 id="a-different-design-goal">A Different Design Goal</h2>
<p>All the codes in this series have aimed at the same target: assign short codewords to frequent symbols, with length growing roughly as $\log n$ for the $n$-th symbol under some implied prior. Elias gamma minimizes expected length for power-law distributions; delta and omega extend the recursion for heavier tails.</p>
<p>Fibonacci coding has a different goal. It does not optimize for average codeword length under a specific distribution. It optimizes for error resilience. In a stream of gamma-coded integers, a single bit flip in a codeword&rsquo;s length prefix causes the decoder to misread that codeword&rsquo;s length, then misread every subsequent codeword. The error propagates without limit until the decoder somehow reacquires sync. On a reliable channel this is a nonissue. On a noisy one, or in stored data that may have silently rotted, it is a serious problem.</p>
<p>Fibonacci coding avoids this. Every Fibonacci codeword ends in two consecutive 1 bits (&ldquo;11&rdquo;). This double-one marker appears nowhere else in the codeword. A single bit flip corrupts the codeword it hits, possibly spills into the next codeword, and then the decoder finds the next &ldquo;11&rdquo; and resynchronizes. At most two codewords are corrupted per error. The rest of the stream is intact.</p>
<p>The price is length overhead: Fibonacci codewords are approximately $1.44 \times \log_2 n$ bits long, compared to $\log_2 n$ bits for the entropy lower bound. On a reliable channel, that overhead is not worth paying. On a noisy channel, or in a long-running stream where rare bit errors must not lose the entire tail, the self-synchronization property is worth it.</p>
<hr>
<h2 id="zeckendorfs-theorem">Zeckendorf&rsquo;s Theorem</h2>
<p>Fibonacci numbers starting from $F_2 = 1$: $1, 2, 3, 5, 8, 13, 21, 34, \ldots$</p>
<p>Zeckendorf&rsquo;s theorem: every positive integer $n$ has a unique representation as a sum of non-consecutive Fibonacci numbers. The greedy algorithm produces it by repeatedly subtracting the largest Fibonacci number that does not exceed $n$.</p>
<p>Examples:</p>
<table>
	<thead>
			<tr>
					<th>$n$</th>
					<th>Zeckendorf representation</th>
					<th>Bit vector (index 0 = $F_2$)</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>1</td>
					<td>$F_2 = 1$</td>
					<td><code>{1}</code></td>
			</tr>
			<tr>
					<td>4</td>
					<td>$F_4 + F_2 = 3 + 1$</td>
					<td><code>{1, 0, 1}</code></td>
			</tr>
			<tr>
					<td>10</td>
					<td>$F_6 + F_3 = 8 + 2$</td>
					<td><code>{0, 1, 0, 0, 1}</code></td>
			</tr>
			<tr>
					<td>11</td>
					<td>$F_6 + F_4 = 8 + 3$</td>
					<td><code>{0, 0, 1, 0, 1}</code></td>
			</tr>
	</tbody>
</table>
<p>The non-adjacency condition is the key. No two consecutive Fibonacci numbers appear in the sum. That means no two consecutive Zeckendorf bits are both 1. So appending a 1 after the last Zeckendorf bit creates the only &ldquo;11&rdquo; in the entire codeword. The terminator is safe precisely because of the structural property of Zeckendorf representations.</p>
<p><strong>Implementation.</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="kr">inline</span> <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">bool</span><span class="o">&gt;</span> <span class="n">to_zeckendorf</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span> <span class="n">n</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">assert</span><span class="p">(</span><span class="n">n</span> <span class="o">&gt;=</span> <span class="mi">1</span> <span class="o">&amp;&amp;</span> <span class="s">&#34;Zeckendorf is undefined for n = 0&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// Build the Fibonacci sequence up to n. Start with F_2=1, F_3=2.
</span></span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="o">&gt;</span> <span class="n">fibs</span><span class="p">{</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">};</span>
</span></span><span class="line"><span class="cl">    <span class="k">while</span> <span class="p">(</span><span class="n">fibs</span><span class="p">.</span><span class="n">back</span><span class="p">()</span> <span class="o">&lt;=</span> <span class="n">n</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">fibs</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span><span class="n">fibs</span><span class="p">[</span><span class="n">fibs</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">-</span> <span class="mi">1</span><span class="p">]</span> <span class="o">+</span> <span class="n">fibs</span><span class="p">[</span><span class="n">fibs</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">-</span> <span class="mi">2</span><span class="p">]);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// The last entry is &gt; n; remove it so all entries are &lt;= n.
</span></span></span><span class="line"><span class="cl">    <span class="n">fibs</span><span class="p">.</span><span class="n">pop_back</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// Greedy decomposition: subtract the largest Fibonacci number &lt;= remaining n.
</span></span></span><span class="line"><span class="cl">    <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">bool</span><span class="o">&gt;</span> <span class="n">bits</span><span class="p">(</span><span class="n">fibs</span><span class="p">.</span><span class="n">size</span><span class="p">(),</span> <span class="nb">false</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="n">fibs</span><span class="p">.</span><span class="n">size</span><span class="p">();</span> <span class="n">i</span><span class="o">--</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">;)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">n</span> <span class="o">&gt;=</span> <span class="n">fibs</span><span class="p">[</span><span class="n">i</span><span class="p">])</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">bits</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="nb">true</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="n">n</span> <span class="o">-=</span> <span class="n">fibs</span><span class="p">[</span><span class="n">i</span><span class="p">];</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="k">return</span> <span class="n">bits</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><hr>
<h2 id="the-fibonacci-codeword">The Fibonacci Codeword</h2>
<p>To encode $n$:</p>
<ol>
<li>Compute the Zeckendorf bits of $n$: <code>bits = to_zeckendorf(n)</code>.</li>
<li>Write each bit from index 0 (corresponding to $F_2$) through the last set index.</li>
<li>Append a terminating 1 bit.</li>
</ol>
<p>The codeword ends with the last Zeckendorf bit (which is always 1, since the highest Fibonacci number in the sum is always included) followed by the terminating 1. That final &ldquo;11&rdquo; is the synchronization marker. Nothing in the payload can look like it.</p>
<p>Codeword examples:</p>
<table>
	<thead>
			<tr>
					<th>$n$</th>
					<th>Zeckendorf bits</th>
					<th>Codeword</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>1</td>
					<td><code>{1}</code></td>
					<td><code>1 1</code></td>
			</tr>
			<tr>
					<td>2</td>
					<td><code>{0, 1}</code></td>
					<td><code>0 1 1</code></td>
			</tr>
			<tr>
					<td>3</td>
					<td><code>{0, 0, 1}</code></td>
					<td><code>0 0 1 1</code></td>
			</tr>
			<tr>
					<td>4</td>
					<td><code>{1, 0, 1}</code></td>
					<td><code>1 0 1 1</code></td>
			</tr>
			<tr>
					<td>8</td>
					<td><code>{0, 0, 0, 0, 1}</code></td>
					<td><code>0 0 0 0 1 1</code></td>
			</tr>
	</tbody>
</table>
<p>Decoding reads bits until it sees two consecutive 1s. The second 1 is the terminator. All preceding bits, including the first 1 of that terminal pair, are Zeckendorf bits. Reconstruct $n$ from the Fibonacci sum. The decoder needs no length field, no lookahead, and no external framing.</p>
<p><strong>Implementation.</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Fibonacci</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="n">value_type</span> <span class="n">n</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">assert</span><span class="p">(</span><span class="n">n</span> <span class="o">&gt;=</span> <span class="mi">1</span> <span class="o">&amp;&amp;</span> <span class="s">&#34;Fibonacci is undefined for n = 0&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">auto</span> <span class="n">bits</span> <span class="o">=</span> <span class="n">to_zeckendorf</span><span class="p">(</span><span class="n">n</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="kt">bool</span> <span class="nl">b</span> <span class="p">:</span> <span class="n">bits</span><span class="p">)</span> <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="n">b</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">true</span><span class="p">);</span>  <span class="c1">// terminating &#39;1&#39; bit
</span></span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">bool</span><span class="o">&gt;</span> <span class="n">bits</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="kt">bool</span> <span class="n">prev</span> <span class="o">=</span> <span class="nb">false</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">while</span> <span class="p">(</span><span class="nb">true</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="kt">bool</span> <span class="n">cur</span> <span class="o">=</span> <span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">            <span class="k">if</span> <span class="p">(</span><span class="n">cur</span> <span class="o">&amp;&amp;</span> <span class="n">prev</span><span class="p">)</span> <span class="k">break</span><span class="p">;</span>  <span class="c1">// two consecutive 1s: stop
</span></span></span><span class="line"><span class="cl">            <span class="n">bits</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span><span class="n">cur</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="n">prev</span> <span class="o">=</span> <span class="n">cur</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="c1">// Reconstruct n from the Fibonacci sum.
</span></span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="o">&gt;</span> <span class="n">fibs</span><span class="p">{</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">};</span>
</span></span><span class="line"><span class="cl">        <span class="k">while</span> <span class="p">(</span><span class="n">fibs</span><span class="p">.</span><span class="n">size</span><span class="p">()</span> <span class="o">&lt;</span> <span class="n">bits</span><span class="p">.</span><span class="n">size</span><span class="p">())</span>
</span></span><span class="line"><span class="cl">            <span class="n">fibs</span><span class="p">.</span><span class="n">push_back</span><span class="p">(</span><span class="n">fibs</span><span class="p">[</span><span class="n">fibs</span><span class="p">.</span><span class="n">size</span><span class="p">()</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span> <span class="o">+</span> <span class="n">fibs</span><span class="p">[</span><span class="n">fibs</span><span class="p">.</span><span class="n">size</span><span class="p">()</span><span class="o">-</span><span class="mi">2</span><span class="p">]);</span>
</span></span><span class="line"><span class="cl">        <span class="n">value_type</span> <span class="n">n</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">bits</span><span class="p">.</span><span class="n">size</span><span class="p">();</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">            <span class="k">if</span> <span class="p">(</span><span class="n">bits</span><span class="p">[</span><span class="n">i</span><span class="p">])</span> <span class="n">n</span> <span class="o">+=</span> <span class="n">fibs</span><span class="p">[</span><span class="n">i</span><span class="p">];</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">n</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>The decoder has no length information in the stream. It reads until it finds &ldquo;11&rdquo; and stops. That is the entire decoder. The self-delimiting structure comes from the codeword itself, not from any external framing.</p>
<hr>
<h2 id="the-implied-prior">The Implied Prior</h2>
<p>The Fibonacci number $F_k$ grows roughly as $\phi^k / \sqrt{5}$ where $\phi = (1 + \sqrt{5})/2 \approx 1.618$ is the golden ratio. Because the Zeckendorf representation of $n$ uses roughly $\log_\phi n$ Fibonacci numbers, the codeword length is approximately:</p>
$$\ell(n) \approx \log_\phi n + 1 = \frac{\log_2 n}{\log_2 \phi} + 1 \approx 1.44 \log_2 n + 1 \text{ bits.}$$<p>This is about 44% longer than the Shannon entropy lower bound of $\log_2 n$ bits. The implied prior is geometric with golden-ratio base: $p_n \sim \phi^{-n}$. It sits between the geometric prior of unary coding ($p_n \sim 2^{-n}$, very light tail) and the power-law priors of the Elias codes.</p>
<p>Compared to Elias gamma ($\sim 2 \log_2 n$ bits), Fibonacci is shorter for large $n$; for small $n$ (below about 16), gamma can be shorter. Neither code is optimal for the other&rsquo;s implied prior. That is not the point. Fibonacci&rsquo;s value is not in its compression ratio. It is in the structural guarantee that comes from the &ldquo;11&rdquo; terminator.</p>
<hr>
<h2 id="self-synchronization">Self-Synchronization</h2>
<p>Consider a long stream of Fibonacci-coded integers and a single bit flip somewhere in the stream. Two cases.</p>
<p><strong>Case 1: the flip is within the Zeckendorf payload of codeword $c$.</strong> The payload changes, so $c$ decodes to a wrong value. But the &ldquo;11&rdquo; terminator was not touched; the decoder finds it and resynchronizes correctly before codeword $c+1$. One codeword corrupted.</p>
<p><strong>Case 2: the flip hits a 1 bit that was part of &ldquo;11&rdquo;.</strong> The terminator &ldquo;11&rdquo; is damaged. The decoder either misses it (turning &ldquo;11&rdquo; into &ldquo;10&rdquo; or &ldquo;01&rdquo;) or creates a spurious &ldquo;11&rdquo; inside the payload. If it misses the terminator, the decoder merges codeword $c$ with $c+1$, reads both as one wrong value, then finds the terminator of $c+1$ and resynchronizes. Two codewords corrupted at most.</p>
<p>In both cases, the stream recovers. Compare this to Elias gamma: a flip in a gamma codeword&rsquo;s unary prefix tells the decoder the wrong length, and every subsequent codeword shifts by the wrong amount. Error propagation is unbounded.</p>
<p>The test in <code>test_fibonacci.cpp</code> demonstrates the bounded-damage property concretely: encode <code>{3, 5, 7, 9, 11, 13}</code>, flip bit 0 (within the codeword for 3), and verify that at least 4 of the 6 values are recovered intact:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="n">TEST</span><span class="p">(</span><span class="n">FibonacciTest</span><span class="p">,</span> <span class="n">BitFlipStaysLocal</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">uint64_t</span><span class="o">&gt;</span> <span class="n">original</span> <span class="o">=</span> <span class="p">{</span><span class="mi">3</span><span class="p">,</span> <span class="mi">5</span><span class="p">,</span> <span class="mi">7</span><span class="p">,</span> <span class="mi">9</span><span class="p">,</span> <span class="mi">11</span><span class="p">,</span> <span class="mi">13</span><span class="p">};</span>
</span></span><span class="line"><span class="cl">    <span class="k">auto</span> <span class="n">encoded</span> <span class="o">=</span> <span class="n">encode_sequence</span><span class="p">(</span><span class="n">original</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="k">auto</span> <span class="n">corrupted</span> <span class="o">=</span> <span class="n">flip_bit</span><span class="p">(</span><span class="n">encoded</span><span class="p">,</span> <span class="mi">0</span><span class="p">);</span>  <span class="c1">// flip within codeword for 3
</span></span></span><span class="line"><span class="cl">    <span class="c1">// Decode with EOF guard to avoid infinite loops on corrupted streams.
</span></span></span><span class="line"><span class="cl">    <span class="c1">// Verify at least 4 of 6 values are recovered.
</span></span></span><span class="line"><span class="cl">    <span class="n">EXPECT_GE</span><span class="p">(</span><span class="n">matches</span><span class="p">,</span> <span class="mi">4</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><hr>
<h2 id="when-to-use-fibonacci">When to Use Fibonacci</h2>
<p>Fibonacci is a niche code. Its 44% overhead relative to entropy is real: for most applications, the Elias codes or Huffman coding achieve much better compression on reliable channels. Fibonacci makes sense when:</p>
<ul>
<li>The channel has occasional bit errors, and losing an entire stream after the first error is unacceptable.</li>
<li>The data is long-lived or stored in environments where isolated bit rot can occur.</li>
<li>The decoder has no error-correcting layer and must resynchronize from the content itself.</li>
</ul>
<p>In practice, most systems handle errors at a lower layer (CRC, FEC codes, RAID parity) rather than at the codec level. Where those exist, Fibonacci&rsquo;s self-sync property is irrelevant and the Elias codes or Huffman are better choices.</p>
<p>The <a href="https://github.com/queelius/wire-formats/tree/master/lib/pfc">PFC library</a> includes <code>Fibonacci</code> in <code>codecs.hpp</code> alongside the Elias codes. It is there because the use cases are real, even if they are uncommon.</p>
<hr>
<h2 id="cross-references-and-notes">Cross-References and Notes</h2>
<p><strong>Forward:</strong> Post 7 (&ldquo;Rice / Golomb&rdquo;) covers the first parametric code family in this series, where a parameter $k$ tunes the code for geometrically distributed sources. No link yet (forthcoming).</p>
<p><strong>Back:</strong> <a href="/post/2022-01-priors-wire-formats/">Universal Codes as Priors</a> (post 3) gives the redundancy framework used in the integration tests. <a href="/post/2022-06-elias-gamma-wire-formats/">Unary and Elias Gamma</a> (post 4) covers the codes that Fibonacci is the error-resilient alternative to.</p>
<p><strong>Cross-series:</strong> Fibonacci&rsquo;s self-synchronizing &ldquo;11&rdquo; marker is a structural property of the codeword, not of the algebraic type system. The Stepanov bridge posts cover how codecs compose over types. Fibonacci&rsquo;s error properties are orthogonal to that story and are not discussed there.</p>
<p><strong>Production code:</strong> <code>include/pfc/codecs.hpp</code> in the <a href="https://github.com/queelius/wire-formats/tree/master/lib/pfc">PFC library</a> has <code>Fibonacci</code>, with the same encode/decode interface but zero-copy byte-buffer I/O.</p>
]]></content:encoded><media:content url="https://metafunctor.com/post/2023-04-fibonacci-wire-formats/card.jpg" medium="image"><media:title type="plain">Information-Theory</media:title></media:content><category>C++</category><category>information-theory</category><category>coding-theory</category><category>prefix-free</category><category>universal-codes</category><category>fibonacci</category><category>zeckendorf</category></item><item><title>Elias Delta and Omega</title><link>https://metafunctor.com/post/2022-11-elias-delta-omega-wire-formats/</link><pubDate>Sun, 13 Nov 2022 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2022-11-elias-delta-omega-wire-formats/</guid><description>Elias delta and omega extend Elias gamma by recursively encoding the length prefix. Each step yields shorter codewords for large integers at a small constant cost for small ones.</description><content:encoded><![CDATA[<p><em>Elias gamma spends too many bits saying how many bits it will use. Delta fixes that. Omega takes the fix one step further. This post is about what happens when you apply recursion to the length prefix.</em></p>
<h2 id="where-gamma-stops-being-good">Where Gamma Stops Being Good</h2>
<p>Elias gamma, from <a href="/post/2022-06-elias-gamma-wire-formats/">the previous post</a>, encodes a positive integer $n$ in $2\lfloor \log_2 n \rfloor + 1$ bits: a unary count of $\lfloor \log_2 n \rfloor$ zeros, then a stop bit, then the $\lfloor \log_2 n \rfloor$ trailing binary bits of $n$. For small $n$ this is fine. For large $n$, nearly half the bits are spent on the unary prefix alone.</p>
<p>The unary prefix is the bottleneck. It encodes the length $L = \lfloor \log_2 n \rfloor + 1$ in the most wasteful possible way: one bit per unit. For $n = 256$, that is 8 zero bits just to say &ldquo;the payload is 8 bits long.&rdquo; The payload itself is also 8 bits, so you are paying a 100% overhead on the length announcement. That is bad, and it gets worse as $n$ grows.</p>
<p>The fix is obvious once you see it: encode $L$ itself in some shorter code instead of unary. Elias delta does exactly this, replacing the unary length prefix with a gamma-coded length. Elias omega takes the idea one step further and applies the recursion to itself, all the way down.</p>
<p>Both codes are universal: they assign finite codewords to every positive integer, and the expected codeword length is within a constant factor of optimal for any source whose probabilities decrease with $n$. The improvement over gamma is real and measurable once $n$ grows past a few dozen.</p>
<p>This post shows both implementations, their implied priors, and the crossover points where each code wins. As in the rest of this series, the code is pedagogical: each header stands alone and the struct-with-encode/decode pattern maps directly onto the <a href="https://github.com/queelius/wire-formats/tree/master/lib/pfc">PFC library</a>&rsquo;s <code>EliasDelta</code> and <code>EliasOmega</code> in <code>codecs.hpp</code>.</p>
<hr>
<h2 id="elias-delta">Elias Delta</h2>
<p><strong>Algorithm.</strong> Let $L = \lfloor \log_2 n \rfloor + 1$ (the bit-width of $n$, equivalently <code>std::bit_width(n)</code>).</p>
<ol>
<li>Encode $L$ in Elias gamma.</li>
<li>Write the $L - 1$ trailing bits of $n$ after its implicit leading 1, MSB first.</li>
</ol>
<p>Gamma encodes $L$ (a small integer) in $O(\log \log n)$ bits instead of $O(\log n)$ bits for the unary prefix. The payload is identical to gamma&rsquo;s: the trailing bits of $n$. The total length is $O(\log n + \log \log n)$.</p>
<p><strong>Examples.</strong> Let us trace $n \in {1, 2, 3, 4}$:</p>
<table>
	<thead>
			<tr>
					<th>$n$</th>
					<th>$L = \text{bit_width}(n)$</th>
					<th>$\gamma(L)$</th>
					<th>Trailing bits</th>
					<th>Delta codeword</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>1</td>
					<td>1</td>
					<td><code>1</code></td>
					<td>(none)</td>
					<td><code>1</code></td>
			</tr>
			<tr>
					<td>2</td>
					<td>2</td>
					<td><code>010</code></td>
					<td><code>0</code></td>
					<td><code>0100</code></td>
			</tr>
			<tr>
					<td>3</td>
					<td>2</td>
					<td><code>010</code></td>
					<td><code>1</code></td>
					<td><code>0101</code></td>
			</tr>
			<tr>
					<td>4</td>
					<td>3</td>
					<td><code>011</code></td>
					<td><code>00</code></td>
					<td><code>01100</code></td>
			</tr>
	</tbody>
</table>
<p>For $n = 1$, the two codes agree: both emit a single <code>1</code> bit. For $n = 4$, gamma would emit <code>00100</code> (5 bits); delta emits <code>01100</code> (5 bits as well, but it will pull ahead soon). The crossover is not immediate, but it is real.</p>
<p><strong>Implementation.</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Delta</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="n">value_type</span> <span class="n">n</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">assert</span><span class="p">(</span><span class="n">n</span> <span class="o">&gt;=</span> <span class="mi">1</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">bits</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">bit_width</span><span class="p">(</span><span class="n">n</span><span class="p">);</span>  <span class="c1">// L = floor(log2(n)) + 1
</span></span></span><span class="line"><span class="cl">        <span class="n">Gamma</span><span class="o">::</span><span class="n">encode</span><span class="p">(</span><span class="k">static_cast</span><span class="o">&lt;</span><span class="n">value_type</span><span class="o">&gt;</span><span class="p">(</span><span class="n">bits</span><span class="p">),</span> <span class="n">sink</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="c1">// Write the (bits - 1) trailing bits of n, MSB first, skipping the
</span></span></span><span class="line"><span class="cl">        <span class="c1">// implicit leading 1.
</span></span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="n">bits</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span> <span class="n">i</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">;</span> <span class="o">--</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(((</span><span class="n">n</span> <span class="o">&gt;&gt;</span> <span class="p">(</span><span class="n">i</span> <span class="o">-</span> <span class="mi">1</span><span class="p">))</span> <span class="o">&amp;</span> <span class="mi">1u</span><span class="p">)</span> <span class="o">!=</span> <span class="mi">0u</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">bits</span> <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">Gamma</span><span class="o">::</span><span class="n">decode</span><span class="p">(</span><span class="n">source</span><span class="p">));</span>
</span></span><span class="line"><span class="cl">        <span class="n">value_type</span> <span class="n">result</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">bits</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">result</span> <span class="o">=</span> <span class="p">(</span><span class="n">result</span> <span class="o">&lt;&lt;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">|</span> <span class="p">(</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">()</span> <span class="o">?</span> <span class="n">value_type</span><span class="p">{</span><span class="mi">1</span><span class="p">}</span> <span class="o">:</span> <span class="n">value_type</span><span class="p">{</span><span class="mi">0</span><span class="p">});</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">result</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>Decoding inverts exactly: read $L$ from a gamma-coded prefix, then read $L - 1$ payload bits, prepending the implicit leading 1. The structure mirrors the encoder step for step. There is nothing subtle here; the recursion is one level deep and terminates immediately.</p>
<p><strong>Implied prior.</strong> The delta length for $n$ is approximately $\log_2 n + 2 \log_2 \log_2 n + C$ bits. The implied probability is roughly $1 / (n \log^2 n)$, a heavier tail than gamma&rsquo;s $\sim 1/n^2$. Delta is better matched to sources where large values are somewhat more likely than a squared power law would predict.</p>
<hr>
<h2 id="elias-omega">Elias Omega</h2>
<p><strong>Algorithm.</strong> Omega applies the recursion all the way down:</p>
<ol>
<li>While $n &gt; 1$:
a. Record $(n, \text{bit_width}(n))$ on a stack.
b. Replace $n$ with $\text{bit_width}(n) - 1$.</li>
<li>Write the stacked values from outermost to innermost, each in its full binary representation.</li>
<li>Append a terminating 0 bit.</li>
</ol>
<p>Decoding reads forward: start with <code>current = 1</code>. At each step, read the next bit. If it is 0, stop and return <code>current</code>. If it is 1, it is the MSB of a <code>(current + 1)</code>-bit value; read the remaining <code>current</code> bits, form the value, set <code>current</code> to it, and repeat.</p>
<p><strong>Implementation.</strong></p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Omega</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="n">value_type</span> <span class="n">n</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">assert</span><span class="p">(</span><span class="n">n</span> <span class="o">&gt;=</span> <span class="mi">1</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">pair</span><span class="o">&lt;</span><span class="n">value_type</span><span class="p">,</span> <span class="n">std</span><span class="o">::</span><span class="n">size_t</span><span class="o">&gt;&gt;</span> <span class="n">stack</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">while</span> <span class="p">(</span><span class="n">n</span> <span class="o">&gt;</span> <span class="mi">1</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">w</span> <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span><span class="o">&gt;</span><span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">bit_width</span><span class="p">(</span><span class="n">n</span><span class="p">));</span>
</span></span><span class="line"><span class="cl">            <span class="n">stack</span><span class="p">.</span><span class="n">emplace_back</span><span class="p">(</span><span class="n">n</span><span class="p">,</span> <span class="n">w</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="n">n</span> <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="n">value_type</span><span class="o">&gt;</span><span class="p">(</span><span class="n">w</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="k">auto</span> <span class="n">it</span> <span class="o">=</span> <span class="n">stack</span><span class="p">.</span><span class="n">rbegin</span><span class="p">();</span> <span class="n">it</span> <span class="o">!=</span> <span class="n">stack</span><span class="p">.</span><span class="n">rend</span><span class="p">();</span> <span class="o">++</span><span class="n">it</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="k">auto</span> <span class="p">[</span><span class="n">val</span><span class="p">,</span> <span class="n">width</span><span class="p">]</span> <span class="o">=</span> <span class="o">*</span><span class="n">it</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="n">width</span><span class="p">;</span> <span class="n">i</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">;</span> <span class="o">--</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">                <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(((</span><span class="n">val</span> <span class="o">&gt;&gt;</span> <span class="p">(</span><span class="n">i</span> <span class="o">-</span> <span class="mi">1</span><span class="p">))</span> <span class="o">&amp;</span> <span class="mi">1u</span><span class="p">)</span> <span class="o">!=</span> <span class="mi">0u</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">false</span><span class="p">);</span>  <span class="c1">// terminating 0 bit
</span></span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">value_type</span> <span class="n">current</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">while</span> <span class="p">(</span><span class="nb">true</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="kt">bool</span> <span class="n">b</span> <span class="o">=</span> <span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">            <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="n">b</span><span class="p">)</span> <span class="k">break</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="n">value_type</span> <span class="n">next</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="k">for</span> <span class="p">(</span><span class="n">value_type</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">current</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">                <span class="n">next</span> <span class="o">=</span> <span class="p">(</span><span class="n">next</span> <span class="o">&lt;&lt;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">|</span> <span class="p">(</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">()</span> <span class="o">?</span> <span class="n">value_type</span><span class="p">{</span><span class="mi">1</span><span class="p">}</span> <span class="o">:</span> <span class="n">value_type</span><span class="p">{</span><span class="mi">0</span><span class="p">});</span>
</span></span><span class="line"><span class="cl">            <span class="p">}</span>
</span></span><span class="line"><span class="cl">            <span class="n">current</span> <span class="o">=</span> <span class="n">next</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">current</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>The stack-based encoding and forward-reading decode are mirror images of each other. The terminating 0 bit is the base case of the recursion: it tells the decoder there is nothing more to unfold.</p>
<p><strong>Implied prior.</strong> Omega&rsquo;s length grows as $O(\log^* n)$, the iterated logarithm. This is the theoretical limit for self-delimiting integer codes. The implied probability is slightly heavier-tailed than delta&rsquo;s $1/(n \log^2 n)$.</p>
<hr>
<h2 id="the-crossover-points">The Crossover Points</h2>
<p>Where does each code win?</p>
<table>
	<thead>
			<tr>
					<th>$n$</th>
					<th>Unary</th>
					<th>Gamma</th>
					<th>Delta</th>
					<th>Omega</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>1</td>
					<td>1</td>
					<td>1</td>
					<td>1</td>
					<td>1</td>
			</tr>
			<tr>
					<td>4</td>
					<td>4</td>
					<td>5</td>
					<td>5</td>
					<td>6</td>
			</tr>
			<tr>
					<td>16</td>
					<td>16</td>
					<td>9</td>
					<td>9</td>
					<td>11</td>
			</tr>
			<tr>
					<td>256</td>
					<td>256</td>
					<td>17</td>
					<td>15</td>
					<td>16</td>
			</tr>
			<tr>
					<td>$2^{16}$</td>
					<td>65536</td>
					<td>33</td>
					<td>25</td>
					<td>28</td>
			</tr>
			<tr>
					<td>$2^{32}$</td>
					<td>~4B</td>
					<td>65</td>
					<td>43</td>
					<td>45</td>
			</tr>
	</tbody>
</table>
<p>For small $n$ (say, $n \leq 8$), gamma beats delta. Both codes use 1 bit for $n=1$; delta is not shorter until $n$ grows to the point where the gamma-coded length prefix pays for itself. That crossover occurs around $n = 16$ for delta versus gamma (they tie there; delta pulls ahead from $n = 32$ onward).</p>
<p>Omega has more overhead at small $n$ because its recursive structure always adds at least one extra bit beyond delta. Omega beats delta only for very large $n$: roughly $n &gt; 1000$. For any $n$ that fits in 64 bits (up to about $1.8 \times 10^{19}$), the iterated logarithm reaches at most 4 or 5 levels, so the practical advantage of omega over delta is small.</p>
<p>The takeaway: delta is the default choice for most practical work. Omega is interesting as a theoretical endpoint.</p>
<hr>
<h2 id="the-implied-prior-ladder">The Implied Prior Ladder</h2>
<p>Each step in the encoding hierarchy shifts the implied prior toward a heavier tail:</p>
<ul>
<li><strong>Unary</strong>: implied prior $\sim 1/2^n$ (geometric, extremely light tail).</li>
<li><strong>Gamma</strong>: implied prior $\sim 1/n^2$ (power law with exponent 2).</li>
<li><strong>Delta</strong>: implied prior $\sim 1/(n \log^2 n)$ (power law with a logarithmic correction).</li>
<li><strong>Omega</strong>: implied prior slightly heavier than delta&rsquo;s.</li>
</ul>
<p>A heavier tail means the code assigns shorter codewords to large $n$ relative to what a pure power law would give. This reduces average code length when the source occasionally produces large outliers. The cost is a small constant overhead for the small values that make up the bulk of typical data.</p>
<p>If your source is well-modeled by $1/n^2$, gamma is the best match. If large values appear more often than $1/n^2$ predicts (heavier tail), delta is better. Omega is useful primarily as a theoretical reference: the code that matches the heaviest practically encodable tail.</p>
<p>Choosing a code is choosing a prior. You should at least know what prior you are choosing.</p>
<p>The <a href="/post/2022-01-priors-wire-formats/">Universal Codes as Priors</a> post gives the machinery for computing redundancy given a source distribution and a code length sequence. The integration tests in <code>test_elias_delta_omega.cpp</code> use that library to verify that delta&rsquo;s redundancy on a $1/n^2$ source stays bounded, and that gamma beats delta on exactly that source.</p>
<hr>
<h2 id="the-limit">The Limit</h2>
<p>Omega achieves $O(\log^* n)$ codeword length, where $\log^<em>$ is the iterated logarithm: the number of times you must apply $\log_2$ before the result drops to 1 or below. For all 64-bit integers, $\log^</em> n \leq 5$.</p>
<p>This is the theoretical lower bound for self-delimiting codes on positive integers with no distributional assumptions. No prefix-free code can do asymptotically better than $O(\log^* n)$.</p>
<p>In practice, the constant in that $O$ matters. Omega&rsquo;s codewords for $n \leq 1000$ are frequently longer than delta&rsquo;s. The overhead of adding one more recursion level costs bits even when the recursion only saves bits asymptotically. For any practical $n$ in the range a 64-bit system can produce, delta already achieves most of omega&rsquo;s compression advantage at lower constant overhead.</p>
<p>So omega is the theoretical ceiling, delta is the practical ceiling, and gamma is the workhorse for distributions where large values are rare. Each code is the right choice for a different source model. That is not a deficiency of any of them; it is the point.</p>
<hr>
<h2 id="cross-references-and-notes">Cross-References and Notes</h2>
<p><strong>Forward:</strong> <a href="/post/2023-04-fibonacci-wire-formats/">Fibonacci Coding</a> (post 6) takes a completely different approach: it uses Zeckendorf&rsquo;s theorem rather than recursive length encoding, and produces self-synchronizing codewords with an interesting error-locality property.</p>
<p><strong>Back:</strong> <a href="/post/2022-06-elias-gamma-wire-formats/">Unary and Elias Gamma</a> (post 4) covers the base case of this recursion: the gamma code that delta and omega build on. <a href="/post/2022-01-priors-wire-formats/">Universal Codes as Priors</a> (post 3) gives the prior-redundancy framework used in the integration tests.</p>
<p><strong>Cross-series:</strong> The Stepanov bridge posts (<a href="/post/2026-05-codecs-functors-stepanov/">Bits Follow Types</a> and <a href="/post/2026-05-prefix-free-stepanov/">When Lists Become Bits</a>) show how these codecs become functors over algebraic types, connecting the universal-code theory to the type-algebra layer.</p>
<p><strong>Production code:</strong> <code>include/pfc/codecs.hpp</code> in the <a href="https://github.com/queelius/wire-formats/tree/master/lib/pfc">PFC library</a> contains <code>EliasDelta</code> and <code>EliasOmega</code>, production implementations with the same encode/decode interface but zero-copy byte-buffer I/O.</p>
]]></content:encoded><media:content url="https://metafunctor.com/post/2022-11-elias-delta-omega-wire-formats/card.jpg" medium="image"><media:title type="plain">Information-Theory</media:title></media:content><category>C++</category><category>information-theory</category><category>coding-theory</category><category>prefix-free</category><category>universal-codes</category><category>elias-delta</category><category>elias-omega</category></item><item><title>Unary and Elias Gamma</title><link>https://metafunctor.com/post/2022-06-elias-gamma-wire-formats/</link><pubDate>Sun, 19 Jun 2022 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2022-06-elias-gamma-wire-formats/</guid><description>Unary and Elias gamma are the two simplest universal codes. Unary encodes n in n bits; gamma in 2 log2(n)+1 bits. Each implies a different prior over the integers.</description><content:encoded><![CDATA[<p><em>Unary is older than information theory. Elias gamma is its 1975 improvement. Together they span the gap between optimal-but-impractical and practical-but-nearly-optimal. This post derives what each code bets on, and shows numerically what that means.</em></p>
<h2 id="unary-and-elias-gamma">Unary and Elias Gamma</h2>
<p>Unary is the oldest code in this series. It predates information theory by centuries: a shepherd counting sheep on a stick is using unary. Mark one notch per sheep; count the notches to decode. The codeword for \(n\) is \(n\) tally marks. Its information-theoretic justification came later, when Shannon showed it is exactly optimal for a geometric source.</p>
<p>Elias gamma is the 1975 extension by Peter Elias. It brings the codeword length from \(O(n)\) to \(O(\log n)\), making it practical for numbers beyond small single digits, while keeping the prefix-free property that makes self-delimiting streams possible.</p>
<p>Both codes are instances of the claim from <a href="/post/2022-01-priors-wire-formats/">Universal Codes as Priors</a>: every prefix-free code is a bet about the source. Unary bets on a geometric distribution with parameter \(1/2\). Gamma bets on a power-law distribution with exponent \(\approx 2\). This post implements both, derives their implied priors, and shows numerically what the bets mean.</p>
<hr>
<h2 id="unary-geometric-prior">Unary: Geometric Prior</h2>
<p>The encoding rule for unary is simple: to encode integer \(n \geq 1\), write \((n-1)\) zero bits followed by one <code>1</code> bit. The decoder reads bits until it sees the <code>1</code>; the number of bits read is the decoded value.</p>
<p>Examples: \(1 \to\) <code>1</code>, \(2 \to\) <code>01</code>, \(3 \to\) <code>001</code>, \(4 \to\) <code>0001</code>.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Unary</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="n">value_type</span> <span class="n">n</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">assert</span><span class="p">(</span><span class="n">n</span> <span class="o">&gt;=</span> <span class="mi">1</span> <span class="o">&amp;&amp;</span> <span class="s">&#34;Unary is undefined for n = 0&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">value_type</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">n</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">false</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">true</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">value_type</span> <span class="n">n</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">while</span> <span class="p">(</span><span class="o">!</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">())</span> <span class="o">++</span><span class="n">n</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">n</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p><strong>Length analysis.</strong> The codeword for \(n\) has length \(n\). The Kraft sum is \(\sum_{n=1}^{\infty} 2^{-n} = 1\): unary saturates Kraft exactly. The implied prior is \(p_n = 2^{-n}\): a geometric distribution with parameter \(1/2\), where each value is half as likely as the previous.</p>
<p><strong>Optimality test.</strong> Because the implied prior is dyadic (all probabilities are powers of \(1/2\)) and Kraft saturates, unary achieves entropy exactly on this prior. For a 30-symbol truncation of geometric(1/2), the expected unary length equals the entropy to within the truncation tail (\(\approx 2^{-30}\)):</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">K</span> <span class="o">=</span> <span class="mi">30</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">lens</span> <span class="o">=</span> <span class="n">unary_lengths</span><span class="p">(</span><span class="n">K</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">probs</span> <span class="o">=</span> <span class="n">priors</span><span class="o">::</span><span class="n">implied_prior</span><span class="p">(</span><span class="n">lens</span><span class="p">);</span>  <span class="c1">// geometric(1/2), normalized
</span></span></span><span class="line"><span class="cl"><span class="kt">double</span> <span class="n">r</span> <span class="o">=</span> <span class="n">priors</span><span class="o">::</span><span class="n">redundancy</span><span class="p">(</span><span class="n">probs</span><span class="p">,</span> <span class="n">lens</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="c1">// r is &lt; 1e-6: unary is exactly optimal for its own implied prior.
</span></span></span></code></pre></div><p>The redundancy is not exactly zero because the probability mass beyond symbol 30 has been renormalized into the first 30 symbols, introducing a small correction. As \(K \to \infty\), the redundancy goes to zero.</p>
<hr>
<h2 id="elias-gamma-power-law-prior">Elias Gamma: Power-Law Prior</h2>
<p>Gamma&rsquo;s encoding rule has three parts: write the <em>length prefix</em> in unary, then write the <em>payload bits</em>.</p>
<p>Specifically, for \(n \geq 1\), let \(k = \lfloor \log_2 n \rfloor\) (the position of the leading bit). Then:</p>
<ol>
<li>Write \(k\) zero bits (the unary encoding of the block index, minus the terminator).</li>
<li>Write a <code>1</code> bit (the unary terminator and implicit leading 1 of the value).</li>
<li>Write the \(k\) bits of \(n\) after the leading 1 bit, MSB first.</li>
</ol>
<p>Examples: \(1 \to\) <code>1</code>, \(2 \to\) <code>010</code>, \(3 \to\) <code>011</code>, \(4 \to\) <code>00100</code>, \(5 \to\) <code>00101</code>, \(6 \to\) <code>00110</code>, \(7 \to\) <code>00111</code>, \(8 \to\) <code>0001000</code>.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Gamma</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="k">using</span> <span class="n">value_type</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="kt">uint64_t</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSink</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="kt">void</span> <span class="n">encode</span><span class="p">(</span><span class="n">value_type</span> <span class="n">n</span><span class="p">,</span> <span class="n">S</span><span class="o">&amp;</span> <span class="n">sink</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">assert</span><span class="p">(</span><span class="n">n</span> <span class="o">&gt;=</span> <span class="mi">1</span> <span class="o">&amp;&amp;</span> <span class="s">&#34;Gamma is undefined for n = 0&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">k</span> <span class="o">=</span> <span class="n">std</span><span class="o">::</span><span class="n">bit_width</span><span class="p">(</span><span class="n">n</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span><span class="p">;</span>  <span class="c1">// floor(log2(n))
</span></span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">k</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">false</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(</span><span class="nb">true</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="n">k</span><span class="p">;</span> <span class="n">i</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">;</span> <span class="o">--</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">sink</span><span class="p">.</span><span class="n">write</span><span class="p">(((</span><span class="n">n</span> <span class="o">&gt;&gt;</span> <span class="p">(</span><span class="n">i</span> <span class="o">-</span> <span class="mi">1</span><span class="p">))</span> <span class="o">&amp;</span> <span class="mi">1u</span><span class="p">)</span> <span class="o">!=</span> <span class="mi">0u</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">template</span><span class="o">&lt;</span><span class="n">BitSource</span> <span class="n">S</span><span class="o">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">static</span> <span class="n">value_type</span> <span class="n">decode</span><span class="p">(</span><span class="n">S</span><span class="o">&amp;</span> <span class="n">source</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">k</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">while</span> <span class="p">(</span><span class="o">!</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">())</span> <span class="o">++</span><span class="n">k</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="n">value_type</span> <span class="n">n</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">k</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">n</span> <span class="o">=</span> <span class="p">(</span><span class="n">n</span> <span class="o">&lt;&lt;</span> <span class="mi">1</span><span class="p">)</span> <span class="o">|</span> <span class="p">(</span><span class="n">source</span><span class="p">.</span><span class="n">read</span><span class="p">()</span> <span class="o">?</span> <span class="n">value_type</span><span class="p">{</span><span class="mi">1</span><span class="p">}</span> <span class="o">:</span> <span class="n">value_type</span><span class="p">{</span><span class="mi">0</span><span class="p">});</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">n</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p><strong>Length analysis.</strong> The codeword for \(n\) has length \(2\lfloor\log_2 n\rfloor + 1\). The block \([2^k, 2^{k+1})\) contains \(2^k\) symbols, each with length \(2k+1\). Each block contributes \(2^k \cdot 2^{-(2k+1)} = 2^{-(k+1)}\) to the Kraft sum. Summing: \(\sum_{k=0}^{\infty} 2^{-(k+1)} = 1\). Gamma saturates Kraft.</p>
<p>The implied prior is \(p_n = 2^{-(2\lfloor\log_2 n\rfloor + 1)}\). All symbols in a block get the same probability, which falls by a factor of 4 each time we move to the next block. For \(n\) near a power of 2, this approximates \(1/(2n^2)\). For \(n\) midway through a block, the approximation is within a factor of 2.</p>
<p><strong>Bounded redundancy test.</strong> For a power-law(2) source (where \(p_n \propto 1/n^2\)), gamma&rsquo;s redundancy is bounded by a small constant regardless of how many symbols we consider:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-cpp" data-lang="cpp"><span class="line"><span class="cl"><span class="k">const</span> <span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">N</span> <span class="o">=</span> <span class="mi">128</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">auto</span> <span class="n">lens</span> <span class="o">=</span> <span class="n">gamma_lengths</span><span class="p">(</span><span class="n">N</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="c1">// Build power-law(2): p_n = C/n^2, normalized.
</span></span></span><span class="line"><span class="cl"><span class="n">std</span><span class="o">::</span><span class="n">vector</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span> <span class="n">pl</span><span class="p">(</span><span class="n">N</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="kt">double</span> <span class="n">z</span> <span class="o">=</span> <span class="mf">0.0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">for</span> <span class="p">(</span><span class="n">std</span><span class="o">::</span><span class="n">size_t</span> <span class="n">i</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="n">i</span> <span class="o">&lt;</span> <span class="n">N</span><span class="p">;</span> <span class="o">++</span><span class="n">i</span><span class="p">)</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kt">double</span> <span class="n">n</span> <span class="o">=</span> <span class="k">static_cast</span><span class="o">&lt;</span><span class="kt">double</span><span class="o">&gt;</span><span class="p">(</span><span class="n">i</span> <span class="o">+</span> <span class="mi">1</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">pl</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="mf">1.0</span> <span class="o">/</span> <span class="p">(</span><span class="n">n</span> <span class="o">*</span> <span class="n">n</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">z</span> <span class="o">+=</span> <span class="n">pl</span><span class="p">[</span><span class="n">i</span><span class="p">];</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="k">for</span> <span class="p">(</span><span class="kt">double</span><span class="o">&amp;</span> <span class="nl">p</span> <span class="p">:</span> <span class="n">pl</span><span class="p">)</span> <span class="n">p</span> <span class="o">/=</span> <span class="n">z</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="kt">double</span> <span class="n">r</span> <span class="o">=</span> <span class="n">priors</span><span class="o">::</span><span class="n">redundancy</span><span class="p">(</span><span class="n">pl</span><span class="p">,</span> <span class="n">lens</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="c1">// r is &lt; 3.0: gamma has bounded redundancy on power-law(2) sources.
</span></span></span></code></pre></div><p>The measured redundancy for \(N=128\) is about 0.5 bits per symbol. That constant overhead is what &ldquo;universal code&rdquo; means in practice: gamma works without knowing the distribution, and the price is a small fixed penalty.</p>
<hr>
<h2 id="length-characteristics-on-vs-olog-n">Length Characteristics: O(n) vs O(log n)</h2>
<p>The two codes have fundamentally different length growth rates. Here is a comparison:</p>
<table>
	<thead>
			<tr>
					<th>\(n\)</th>
					<th>Unary length</th>
					<th>Gamma length</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>1</td>
					<td>1</td>
					<td>1</td>
			</tr>
			<tr>
					<td>2</td>
					<td>2</td>
					<td>3</td>
			</tr>
			<tr>
					<td>4</td>
					<td>4</td>
					<td>5</td>
			</tr>
			<tr>
					<td>8</td>
					<td>8</td>
					<td>7</td>
			</tr>
			<tr>
					<td>16</td>
					<td>16</td>
					<td>9</td>
			</tr>
			<tr>
					<td>100</td>
					<td>100</td>
					<td>13</td>
			</tr>
			<tr>
					<td>1024</td>
					<td>1024</td>
					<td>21</td>
			</tr>
	</tbody>
</table>
<p>The crossover happens at \(n = 3\): for \(n \leq 2\), unary is shorter or equal; for \(n \geq 3\), gamma eventually dominates. By \(n = 8\), gamma is already using 7 bits against unary&rsquo;s 8. By \(n = 100\), gamma is 87 bits shorter per codeword.</p>
<p>The asymptotic consequence: if your source puts even a small fraction of probability mass on values above, say, 50, the average cost of unary will be dominated by those rare large values. Gamma&rsquo;s logarithmic growth rate bounds the damage.</p>
<p>Both codes decode in time proportional to the codeword length, which is proportional to \(n\) for unary and \(\log n\) for gamma.</p>
<hr>
<h2 id="when-to-use-which">When to Use Which</h2>
<p>The implied-prior framing gives concrete guidance here.</p>
<p>Use <strong>unary</strong> when your data is very strongly left-skewed toward 1. If 90% or more of your values are 1 or 2, unary&rsquo;s simplicity and exact optimality for geometric(1/2) make it the right choice. Many compressors use unary internally for run-length or tag-bit sequences, where most runs are length 1 or 2.</p>
<p>Use <strong>Elias gamma</strong> when values are left-skewed but the tail extends. Natural language word ranks, file sizes in a large corpus, and degree distributions in social networks all follow approximate power laws with exponents near 2. Gamma is a good default for any positive integer with no known maximum that follows roughly the &ldquo;larger values are rarer&rdquo; pattern.</p>
<p>If you have enough data to estimate the source distribution, neither code is the right answer: a Huffman code built from the estimated distribution will do better. But Huffman requires knowing the distribution up front. Gamma requires nothing. It works without any training data and achieves bounded redundancy on a large class of natural distributions. That is the trade.</p>
<hr>
<h2 id="the-recursive-idea">The Recursive Idea</h2>
<p>Look at gamma&rsquo;s encoding again. The leading zero bits encode the block index \(k = \lfloor \log_2 n \rfloor\). That encoding is unary. The trailing bits encode the position of \(n\) within its block.</p>
<p>What if we encoded the block index in gamma instead of unary? We would use \(O(\log\log n)\) bits for the block index and \(\lfloor\log_2 n\rfloor\) bits for the position, giving a total of about \(\log_2 n + 2\log_2\log_2 n\) bits. This is Elias delta.</p>
<p>What if we encoded the block index of the block index? One more recursion gives Elias omega, with length about \(\log_2 n + \log_2\log_2 n + \log_2\log_2\log_2 n + \ldots\) bits (stopping at the first term that reaches 1).</p>
<p>Each recursion improves the asymptotic length for very large \(n\) at the cost of a constant overhead for small \(n\). The crossover point moves further and further out. For most practical applications, where values rarely exceed \(10^6\), gamma&rsquo;s length of about \(2\log_2 n\) bits is already near-optimal. The recursive codes matter mainly for truly unbounded streams.</p>
<p>The recursive idea is developed in the next post, Elias Delta and Omega.</p>
<hr>
<h2 id="cross-references">Cross-references</h2>
<p>The framing: <a href="/post/2022-01-priors-wire-formats/">Universal Codes as Priors</a> (post 3) introduces the length-to-prior correspondence that this post instantiates for unary (geometric prior) and gamma (power-law prior). Reading that post first is recommended.</p>
<p>The foundations: <a href="/post/2020-03-kraft-wire-formats/">Kraft&rsquo;s Inequality</a> characterizes the space of achievable length vectors; both unary and gamma sit exactly on the Kraft boundary (Kraft sum = 1).</p>
<p>Cross-series: <a href="/post/2026-05-prefix-free-stepanov/">When Lists Become Bits</a> uses gamma as its running example for prefix-free length encoding when building self-delimiting integer sequences. The codec-as-functor perspective there complements the prior-theoretic view here.</p>
<hr>
<p><em>PFC&rsquo;s <code>include/pfc/codecs.hpp</code> contains both <code>Unary</code> and <code>EliasGamma</code> structs with the same encode/decode interface as above, wired into the full algebraic type system. This post develops the pedagogical version; the production code handles edge cases and integrates with bit-level I/O abstractions.</em></p>
]]></content:encoded><media:content url="https://metafunctor.com/post/2022-06-elias-gamma-wire-formats/card.jpg" medium="image"><media:title type="plain">Information-Theory</media:title></media:content><category>C++</category><category>information-theory</category><category>coding-theory</category><category>prefix-free</category><category>universal-codes</category><category>unary</category><category>elias-gamma</category></item></channel></rss>