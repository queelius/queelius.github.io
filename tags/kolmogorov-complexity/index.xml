<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:media="http://search.yahoo.com/mrss/"><channel><title>Kolmogorov-Complexity on metafunctor</title><link>https://metafunctor.com/tags/kolmogorov-complexity/</link><description>Research Engineer &amp; Computer Scientist - Machine Learning, Statistical Computing, Open Source Development</description><language>en-us</language><managingEditor>lex@metafunctor.com (Alex Towell)</managingEditor><webMaster>lex@metafunctor.com (Alex Towell)</webMaster><lastBuildDate>Wed, 18 Feb 2026 19:12:50 -0600</lastBuildDate><atom:link href="https://metafunctor.com/tags/kolmogorov-complexity/" rel="self" type="application/rss+xml"/><image><url>https://metafunctor.com/images/og-image.jpg</url><title>metafunctor</title><link>https://metafunctor.com/</link></image><item><title>An Introduction to Kolmogorov Complexity and Its Applications</title><link>https://metafunctor.com/media/li-vitanyi-kolmogorov/</link><pubDate>Wed, 17 Dec 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/media/li-vitanyi-kolmogorov/</guid><description>Notes The standard comprehensive reference on Kolmogorov complexity. Essential for understanding Solomonoff induction.</description><content:encoded>&lt;h2 id="notes">Notes&lt;/h2>
&lt;p>The standard comprehensive reference on Kolmogorov complexity. Essential for understanding Solomonoff induction.&lt;/p>
</content:encoded><category>kolmogorov-complexity</category><category>algorithmic-information</category><category>compressibility</category><category>reference</category></item><item><title>Complexity-Based Induction Systems: Comparisons and Convergence Theorems</title><link>https://metafunctor.com/media/solomonoff-complexity-induction/</link><pubDate>Wed, 17 Dec 2025 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/media/solomonoff-complexity-induction/</guid><description>Notes Later work on convergence properties of Solomonoff induction.</description><content:encoded>&lt;h2 id="notes">Notes&lt;/h2>
&lt;p>Later work on convergence properties of Solomonoff induction.&lt;/p>
</content:encoded><category>algorithmic-information</category><category>kolmogorov-complexity</category><category>convergence</category></item><item><title>The Beautiful Deception: How 256 Bits Pretend to be Infinity</title><link>https://metafunctor.com/post/2024-07-01-beautiful-deception/</link><pubDate>Mon, 01 Jul 2024 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2024-07-01-beautiful-deception/</guid><description>How do you store infinity in 256 bits? An exploration of the fundamental deception at the heart of cryptography: using finite information to simulate infinite randomness.</description><content:encoded><![CDATA[<p><strong>How do you store infinity in 256 bits?</strong></p>
<p>This paper explores a fundamental paradox in cryptography: cryptographic theory assumes random oracles that produce infinite sequences of random bits, yet we only have computers with finite memory. The answer? We can&rsquo;t implement true random oracles. Instead, we create an elaborate and beautiful deception.</p>
<h2 id="the-impossible-oracle">The Impossible Oracle</h2>
<p>Random oracles are functions that map any input to an infinite sequence of perfectly random bits. But trying to implement one (<code>OracleDigest</code>) fails catastrophically:</p>
<ol>
<li><strong>Memory Unboundedness</strong>: Each new query exhausts memory</li>
<li><strong>Non-Serializability</strong>: Cannot save/restore state</li>
<li><strong>Non-Reproducibility</strong>: Each instance generates different values</li>
<li><strong>Non-Distributability</strong>: Cannot share across systems</li>
</ol>
<p>This isn&rsquo;t a bug—it&rsquo;s a <strong>constructive proof</strong> that true random oracles cannot exist in our computational universe.</p>
<h2 id="the-beautiful-lie-lazydigest">The Beautiful Lie: LazyDigest</h2>
<p>From this impossibility emerges something remarkable:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">LazyDigest</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="fm">__init__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">seed</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="bp">self</span><span class="o">.</span><span class="n">seed</span> <span class="o">=</span> <span class="n">seed</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="fm">__getitem__</span><span class="p">(</span><span class="bp">self</span><span class="p">,</span> <span class="n">index</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="nb">hash</span><span class="p">(</span><span class="n">seed</span> <span class="o">||</span> <span class="n">index</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>
</span></span></code></pre></div><p>Just <strong>256 bits of entropy</strong> generating what appears to be an <strong>infinite random sequence</strong>.</p>
<h3 id="the-core-deception">The Core Deception</h3>
<ul>
<li><strong>Appears</strong>: Infinite random sequence</li>
<li><strong>Actually</strong>: Deterministic function with 256 bits of state</li>
<li><strong>Information content</strong>: K(LazyDigest) = 256 bits + constant</li>
<li><strong>Apparent information</strong>: Infinite</li>
</ul>
<p>We&rsquo;re achieving a compression ratio of <strong>infinity</strong>—representing unbounded data with bounded information.</p>
<h2 id="why-the-deception-works">Why the Deception Works</h2>
<p><strong>Computational indistinguishability</strong>: If <code>h</code> is a secure PRF, no polynomial-time distinguisher can tell LazyDigest apart from truly random output.</p>
<p>We&rsquo;re not random—we&rsquo;re <strong>computationally hard to distinguish from random</strong>. This weaker guarantee is sufficient for cryptography.</p>
<h3 id="the-inevitable-cycle">The Inevitable Cycle</h3>
<p>Since LazyDigest has finite state, it must eventually repeat. After at most 2^256 queries, it will cycle.</p>
<p><strong>But</strong> 2^128 (expected cycle length) ≈ 10^38. At one billion queries per second, it would take 10^21 years—<strong>far longer than the age of the universe</strong>.</p>
<h2 id="advanced-constructions">Advanced Constructions</h2>
<h3 id="hierarchical-seeding">Hierarchical Seeding</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">epoch_seed</span> <span class="o">=</span> <span class="n">h</span><span class="p">(</span><span class="n">master_seed</span> <span class="o">||</span> <span class="s2">&#34;epoch&#34;</span> <span class="o">||</span> <span class="n">epoch</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">chunk_seed</span> <span class="o">=</span> <span class="n">h</span><span class="p">(</span><span class="n">epoch_seed</span> <span class="o">||</span> <span class="s2">&#34;chunk&#34;</span> <span class="o">||</span> <span class="n">chunk</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">value</span> <span class="o">=</span> <span class="n">h</span><span class="p">(</span><span class="n">chunk_seed</span> <span class="o">||</span> <span class="n">position</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>
</span></span></code></pre></div><p>Extends the effective period by structuring state hierarchically.</p>
<h3 id="xor-multi-hash-defense-in-depth">XOR Multi-Hash (Defense in Depth)</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="n">result</span> <span class="o">=</span> <span class="n">sha256</span><span class="p">(</span><span class="n">seed</span><span class="o">||</span><span class="n">index</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span> <span class="err">⊕</span> <span class="n">sha512</span><span class="p">(</span><span class="n">seed</span><span class="o">||</span><span class="n">index</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span> <span class="err">⊕</span>
</span></span><span class="line"><span class="cl">         <span class="n">sha3_256</span><span class="p">(</span><span class="n">seed</span><span class="o">||</span><span class="n">index</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span> <span class="err">⊕</span> <span class="n">blake2b</span><span class="p">(</span><span class="n">seed</span><span class="o">||</span><span class="n">index</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>
</span></span></code></pre></div><p>System remains secure if <strong>at least one</strong> hash function is secure. Hedges against:</p>
<ul>
<li>Future cryptanalysis breaking individual algorithms</li>
<li>Quantum computing vulnerabilities</li>
<li>Implementation bugs</li>
</ul>
<h3 id="sponge-construction">Sponge Construction</h3>
<p>Reserves capacity bits that never leave the system, providing tunable security with 2^(capacity/2) collision resistance.</p>
<h2 id="deep-connections">Deep Connections</h2>
<h3 id="random-oracles-as-uncomputable-reals">Random Oracles as Uncomputable Reals</h3>
<p>Most real numbers are <strong>uncomputable</strong>—they require infinite information to specify. A true random oracle is the cryptographic analog of an uncomputable real:</p>
<ul>
<li><strong>Computable reals</strong> (like π, e): Measure zero, finite programs</li>
<li><strong>Uncomputable reals</strong>: Measure one, infinite information</li>
<li><strong>LazyDigest</strong>: Computable, appears random</li>
<li><strong>Random oracle</strong>: Uncomputable, truly random</li>
</ul>
<p>We&rsquo;re using computable functions to approximate uncomputable ones.</p>
<h3 id="lazy-evaluation-and-mathematical-constants">Lazy Evaluation and Mathematical Constants</h3>
<p>Consider π: we can compute any digit using a finite algorithm (Bailey-Borwein-Plouffe formula). This is lazy evaluation in its purest form:</p>
<ul>
<li><strong>Finite description</strong>: Algorithm is hundreds of bytes</li>
<li><strong>Infinite output</strong>: Arbitrarily many digits</li>
<li><strong>On-demand computation</strong>: Only compute what&rsquo;s observed</li>
</ul>
<p>LazyDigest parallels this exactly. The difference is semantic:</p>
<ul>
<li><strong>π</strong> encodes geometric relationships</li>
<li><strong>LazyDigest</strong> encodes computational hardness</li>
</ul>
<p>Both achieve the same compression: <strong>finite program → infinite sequence</strong>.</p>
<h2 id="randomness-as-computational-hardness">Randomness as Computational Hardness</h2>
<p>We have two incompatible definitions of randomness:</p>
<ol>
<li><strong>Information-theoretic</strong>: High Kolmogorov complexity</li>
<li><strong>Computational</strong>: Indistinguishable from random by efficient algorithms</li>
</ol>
<p>LazyDigest <strong>fails</strong> (1) catastrophically—it has tiny Kolmogorov complexity.
LazyDigest <strong>passes</strong> (2) perfectly—assuming secure hash functions.</p>
<p><strong>The profound insight</strong>: &ldquo;Randomness&rdquo; in cryptography is actually <strong>computational hardness</strong> in disguise.</p>
<h2 id="entropy-times-arrow-and-laplaces-demon">Entropy, Time&rsquo;s Arrow, and Laplace&rsquo;s Demon</h2>
<p>Consider a being with unlimited computational power (Laplace&rsquo;s demon):</p>
<ul>
<li>LazyDigest is trivially non-random—just compute <code>h(seed||i)</code></li>
<li>No entropy exists—only deterministic evolution</li>
<li>Past and future are equally accessible</li>
</ul>
<p>But <strong>we are computationally bounded</strong>, which creates our entire experience:</p>
<ul>
<li>Past is fixed (we remember it)</li>
<li>Future is uncertain (cannot compute fast enough)</li>
<li>Entropy increases (cannot invert dynamics)</li>
<li><strong>Randomness exists</strong> (cannot distinguish from true random)</li>
</ul>
<p>A hash function is a &ldquo;fast-forward only&rdquo; time machine:</p>
<ul>
<li><strong>Forward</strong>: <code>h(x)</code> is easy</li>
<li><strong>Backward</strong>: Finding <code>x</code> given <code>h(x)</code> is computationally infeasible</li>
</ul>
<h2 id="the-p--np-bet">The P ≠ NP Bet</h2>
<p>Every use of LazyDigest implicitly bets that <strong>P ≠ NP</strong>. If P = NP, we could invert the hash function in polynomial time, distinguishing LazyDigest from random.</p>
<p>We&rsquo;re not generating randomness—we&rsquo;re <strong>generating a computationally hard problem and hoping nobody can solve it</strong>.</p>
<h2 id="applications">Applications</h2>
<p><strong>Deterministic Test Data</strong>: Reproducible infinite test streams
<strong>Memory-Hard KDF</strong>: Force memory access patterns for password hashing
<strong>Blockchain PoW</strong>: Model mining as searching the output space</p>
<h2 id="the-beautiful-paradox">The Beautiful Paradox</h2>
<p>LazyDigest embodies a beautiful paradox:</p>
<ul>
<li><strong>Simple</strong>: Just hash seed concatenated with index</li>
<li><strong>Complex</strong>: Output appears completely random</li>
<li><strong>Finite</strong>: Only 256 bits of true information</li>
<li><strong>Infinite</strong>: Can generate unbounded output</li>
<li><strong>Deterministic</strong>: Same seed always gives same sequence</li>
<li><strong>Unpredictable</strong>: Cannot predict next value without seed</li>
</ul>
<p>This paradox is <strong>the heart of modern cryptography</strong>: simple deterministic processes that appear complex and random to bounded observers.</p>
<h2 id="a-constructivist-victory">A Constructivist Victory</h2>
<p>Perhaps most profoundly, LazyDigest represents a victory for constructivism in mathematics. While classical mathematics struggles with incoherent infinities and uncomputable reals, we&rsquo;ve built something better: <strong>a finite, computable object that serves all practical purposes of an infinite random sequence</strong>.</p>
<p>The &ldquo;deception&rdquo; is beautiful because it&rsquo;s <strong>honest</strong>:</p>
<ul>
<li>We don&rsquo;t claim infinite objects—we have finite programs generating unbounded output</li>
<li>We don&rsquo;t pretend uncomputable things exist—we work with computable approximations</li>
<li>We don&rsquo;t hide behind axioms—we build finite machines and study their behavior</li>
</ul>
<p>Cryptography forces us to be constructivists. The boundary of cryptographic possibility is precisely the boundary of computational constructibility.</p>
<p>In a universe that may itself be computational, LazyDigest isn&rsquo;t approximating some &ldquo;true&rdquo; random oracle in a Platonic realm. <strong>It may be as real as randomness gets</strong>.</p>
<p><a href="/latex/beautiful-deception/beautiful_deception.pdf">View the full paper (PDF)</a></p>
<h2 id="key-takeaways">Key Takeaways</h2>
<ol>
<li>True random oracles are <strong>impossible to implement</strong> (proven constructively)</li>
<li>LazyDigest uses <strong>256 bits to simulate infinity</strong> through computational hardness</li>
<li>&ldquo;Randomness&rdquo; in cryptography means <strong>computationally indistinguishable from random</strong></li>
<li>The deception works because we&rsquo;re all <strong>computationally bounded observers</strong></li>
<li>This represents a <strong>constructivist approach</strong> to mathematics and computation</li>
</ol>
]]></content:encoded><category>cryptography</category><category>hash-functions</category><category>randomness</category><category>information-theory</category><category>kolmogorov-complexity</category></item><item><title>Solomonoff Induction: The Theoretical Ideal</title><link>https://metafunctor.com/post/2012-03-15-solomonoff-induction-prediction/</link><pubDate>Thu, 15 Mar 2012 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2012-03-15-solomonoff-induction-prediction/</guid><description>Why the optimal predictor is incomputable—and what we can learn from it anyway</description><content:encoded><![CDATA[<p>In the <a href="/post/2010-06-01-intro-sequential-prediction/">previous post</a>, we defined sequential prediction: given x₁, &hellip;, xₙ₋₁, predict xₙ. But what&rsquo;s the <em>best possible</em> predictor? Is there a theoretical gold standard against which we can measure all practical approaches?</p>
<p>Yes—and it&rsquo;s called <strong>Solomonoff induction</strong>. The catch: it&rsquo;s incomputable.</p>
<h2 id="the-dream-of-universal-prediction">The Dream of Universal Prediction</h2>
<p>Imagine a predictor that could:</p>
<ul>
<li>Learn any computable pattern</li>
<li>Converge to the true distribution faster than any competitor</li>
<li>Never be fooled by spurious correlations</li>
<li>Automatically apply Occam&rsquo;s razor</li>
</ul>
<p>This isn&rsquo;t science fiction—Solomonoff described exactly such a predictor in 1964. The only problem is that implementing it would require solving the halting problem.</p>
<p>Understanding <em>why</em> this ideal exists and <em>why</em> it&rsquo;s incomputable illuminates the fundamental limits of learning and helps explain what practical algorithms are approximating.</p>
<h2 id="kolmogorov-complexity">Kolmogorov Complexity</h2>
<p>Before we can understand Solomonoff induction, we need <strong>Kolmogorov complexity</strong>.</p>
<p>The Kolmogorov complexity K(x) of a string x is the length of the shortest program that outputs x:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">K(x) = min { |p| : U(p) = x }
</span></span></code></pre></div><p>where U is a universal Turing machine and |p| is the length of program p in bits.</p>
<h3 id="intuition">Intuition</h3>
<p>Consider two strings:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">A: 010101010101010101010101010101010101...
</span></span><span class="line"><span class="cl">B: 011001110010110001011010011101000111...
</span></span></code></pre></div><p>String A has low Kolmogorov complexity: <code>print(&quot;01&quot; * 50)</code> is short.
String B (random bits) has high complexity: the shortest program is essentially &ldquo;print the string.&rdquo;</p>
<p>Kolmogorov complexity captures the intuitive notion of &ldquo;randomness&rdquo; or &ldquo;patternlessness.&rdquo; A string is random if and only if it&rsquo;s incompressible—if there&rsquo;s no program shorter than the string itself.</p>
<h3 id="key-properties">Key Properties</h3>
<ol>
<li>
<p><strong>Invariance</strong>: K(x) depends on the choice of universal machine only up to a constant. Different machines give K values that differ by at most O(1).</p>
</li>
<li>
<p><strong>Uncomputability</strong>: K(x) is not computable. There&rsquo;s no algorithm that takes x and returns K(x). (Proof: if K were computable, we could solve the halting problem.)</p>
</li>
<li>
<p><strong>Upper bound</strong>: K(x) ≤ |x| + O(1). You can always just &ldquo;print the string.&rdquo;</p>
</li>
</ol>
<h2 id="algorithmic-probability">Algorithmic Probability</h2>
<p>Solomonoff&rsquo;s <strong>algorithmic probability</strong> (or universal prior) assigns probability to each string x:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">M(x) = Σ 2^(-|p|) for all programs p that output x
</span></span></code></pre></div><p>This sums over <em>all</em> programs that produce x, weighted by 2^(-length). Shorter programs contribute more.</p>
<p>The key insight: <strong>simpler explanations get more probability mass</strong>.</p>
<p>If x can be produced by a 10-bit program, it gets at least 2^(-10) = 0.001 probability. If the shortest program producing y is 1000 bits, y gets at most 2^(-1000) ≈ 0 probability.</p>
<h2 id="solomonoffs-universal-prior">Solomonoff&rsquo;s Universal Prior</h2>
<p>For sequential prediction, Solomonoff proposed using M as a prior over infinite sequences. The probability of the next bit being 1 given history h is:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">P(1 | h) = M(h·1) / M(h)
</span></span></code></pre></div><p>where h·1 denotes h followed by 1, and M(h) is the probability of sequences starting with h.</p>
<p>This is just Bayes&rsquo; rule: condition the universal prior on what we&rsquo;ve seen.</p>
<h2 id="why-its-optimal">Why It&rsquo;s Optimal</h2>
<p><strong>Theorem</strong> (Solomonoff, 1964): Solomonoff induction converges to the true distribution faster than any computable predictor, for any computable source.</p>
<p>More precisely, if the true distribution is μ and we denote Solomonoff&rsquo;s predictor by M, then the expected total squared prediction error is bounded:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">E_μ[ Σₙ (M(xₙ | x&lt;n) - μ(xₙ | x&lt;n))² ] ≤ K(μ) × ln(2)
</span></span></code></pre></div><p>where K(μ) is the Kolmogorov complexity of the true distribution.</p>
<p>This means:</p>
<ul>
<li>Total error is finite, bounded by complexity of the true source</li>
<li>Per-symbol error goes to zero</li>
<li>Convergence is faster for &ldquo;simpler&rdquo; true distributions</li>
</ul>
<h2 id="why-its-incomputable">Why It&rsquo;s Incomputable</h2>
<p>Solomonoff induction requires:</p>
<ol>
<li>
<p><strong>Enumerating all programs</strong>: We&rsquo;d need to consider every possible Turing machine.</p>
</li>
<li>
<p><strong>Solving the halting problem</strong>: To compute M(x), we need to know which programs halt and output x. But determining whether a program halts is undecidable.</p>
</li>
<li>
<p><strong>Summing infinitely many terms</strong>: Even if each term were computable, we&rsquo;d need to sum over infinitely many programs.</p>
</li>
</ol>
<p>The halting problem alone makes exact computation impossible. There&rsquo;s no algorithm that computes M(x) for arbitrary x.</p>
<h2 id="practical-implications">Practical Implications</h2>
<p>If we can&rsquo;t compute Solomonoff induction, why study it?</p>
<h3 id="1-it-sets-the-standard">1. It Sets the Standard</h3>
<p>Solomonoff induction is what we&rsquo;re <em>approximating</em> when we build practical predictors. It tells us what optimal looks like, even if we can&rsquo;t achieve it.</p>
<h3 id="2-it-justifies-occams-razor">2. It Justifies Occam&rsquo;s Razor</h3>
<p>The success of Solomonoff induction explains why preferring simpler hypotheses works. It&rsquo;s not just a heuristic—it&rsquo;s provably optimal (in a well-defined sense).</p>
<h3 id="3-it-guides-algorithm-design">3. It Guides Algorithm Design</h3>
<p>Good practical algorithms should:</p>
<ul>
<li>Have an implicit simplicity bias</li>
<li>Perform some form of model averaging</li>
<li>Be universal over some model class</li>
</ul>
<p>Context Tree Weighting, which we&rsquo;ll explore later in this series, can be seen as Solomonoff induction restricted to tree-generating programs.</p>
<h2 id="computable-approximations">Computable Approximations</h2>
<p>Since Solomonoff itself is out of reach, we use restricted model classes:</p>
<table>
  <thead>
      <tr>
          <th>Method</th>
          <th>Model Class</th>
          <th>Universality</th>
      </tr>
  </thead>
  <tbody>
      <tr>
          <td>Solomonoff</td>
          <td>All computable</td>
          <td>Yes (but incomputable)</td>
      </tr>
      <tr>
          <td>CTW</td>
          <td>Tree sources</td>
          <td>No, but includes Markov</td>
      </tr>
      <tr>
          <td>PPM</td>
          <td>Variable-order Markov</td>
          <td>No, heuristic</td>
      </tr>
      <tr>
          <td>Neural LMs</td>
          <td>Learned representations</td>
          <td>No, but very flexible</td>
      </tr>
  </tbody>
</table>
<p>CTW is particularly elegant: it performs exact Bayesian inference over all tree structures, giving provable optimality for a rich subclass of computable sources.</p>
<h2 id="the-hierarchy-of-predictors">The Hierarchy of Predictors</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">Solomonoff Induction (incomputable, optimal)
</span></span><span class="line"><span class="cl">        ↓
</span></span><span class="line"><span class="cl">    Restricted Model Classes
</span></span><span class="line"><span class="cl">        ↓
</span></span><span class="line"><span class="cl">┌───────┴────────┐
</span></span><span class="line"><span class="cl">CTW             PPM        (efficient, provable for subclass)
</span></span><span class="line"><span class="cl">(tree sources)  (heuristic)
</span></span><span class="line"><span class="cl">        ↓
</span></span><span class="line"><span class="cl">   N-gram Models           (simple, limited)
</span></span><span class="line"><span class="cl">        ↓
</span></span><span class="line"><span class="cl">   Neural LMs              (flexible, data-hungry)
</span></span></code></pre></div><p>Each step down trades some generality for computability. CTW is special because it maintains Bayesian optimality within its (quite general) model class.</p>
<h2 id="connection-to-learning-theory">Connection to Learning Theory</h2>
<p>Solomonoff induction connects to broader themes in machine learning:</p>
<p><strong>No Free Lunch</strong>: Any computable predictor will fail on some computable sequences. Solomonoff is the best we can do &ldquo;on average&rdquo; under the algorithmic probability measure.</p>
<p><strong>Bias-Variance</strong>: Solomonoff&rsquo;s implicit prior implements perfect regularization—complex hypotheses are downweighted but not excluded.</p>
<p><strong>PAC Learning</strong>: Solomonoff converges with finite expected error, satisfying a form of probably approximately correct learning.</p>
<h2 id="key-takeaways">Key Takeaways</h2>
<ul>
<li><strong>Solomonoff induction</strong> is the theoretically optimal predictor for any computable sequence</li>
<li>It assigns <strong>algorithmic probability</strong>: shorter programs → higher probability</li>
<li>It&rsquo;s <strong>incomputable</strong> due to the halting problem</li>
<li>Practical algorithms approximate it via <strong>restricted model classes</strong></li>
<li>CTW achieves Solomonoff-like optimality for <strong>tree-structured sources</strong></li>
<li>Understanding the ideal helps us design and evaluate practical methods</li>
</ul>
<h2 id="whats-next">What&rsquo;s Next</h2>
<p>The <a href="/post/2014-09-20-bayesian-prediction-framework/">next post</a> develops the Bayesian framework that underlies both Solomonoff induction and its practical approximations like CTW. We&rsquo;ll see how model averaging—maintaining distributions over hypotheses rather than committing to one—is the key to robust prediction.</p>
<h2 id="further-reading">Further Reading</h2>
<ul>
<li>Solomonoff, R. J. (1964). &ldquo;A formal theory of inductive inference.&rdquo; <em>Information and Control</em>.</li>
<li>Li, M., &amp; Vitányi, P. (2008). <em>An Introduction to Kolmogorov Complexity and Its Applications</em>.</li>
<li>Hutter, M. (2005). <em>Universal Artificial Intelligence: Sequential Decisions Based on Algorithmic Probability</em>.</li>
</ul>
]]></content:encoded><category>sequential-prediction</category><category>solomonoff</category><category>kolmogorov-complexity</category><category>algorithmic-information</category><category>prediction</category><category>computability</category></item></channel></rss>