<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:media="http://search.yahoo.com/mrss/"><channel><title>Hugo on metafunctor</title><link>https://metafunctor.com/tags/hugo/</link><description>Research Engineer &amp; Computer Scientist - Machine Learning, Statistical Computing, Open Source Development</description><language>en-us</language><managingEditor>lex@metafunctor.com (Alex Towell)</managingEditor><webMaster>lex@metafunctor.com (Alex Towell)</webMaster><lastBuildDate>Fri, 13 Feb 2026 23:13:25 -0600</lastBuildDate><atom:link href="https://metafunctor.com/tags/hugo/" rel="self" type="application/rss+xml"/><image><url>https://metafunctor.com/images/og-image.jpg</url><title>metafunctor</title><link>https://metafunctor.com/</link></image><item><title>pagevault: Client-Side Encryption for Static Sites</title><link>https://metafunctor.com/post/2026-02-13-pagevault/</link><pubDate>Fri, 13 Feb 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2026-02-13-pagevault/</guid><description>How I built a tool that password-protects HTML content with real encryption — no backend required. Covers the architecture, interesting design constraints like the closure property, multi-user key wrapping, and a plugin system for rendering encrypted files in the browser.</description><content:encoded><![CDATA[<p>I run a static site on GitHub Pages. No backend, no server, no database. That simplicity is the whole point — until you want to share something semi-private.</p>
<p>Maybe it&rsquo;s client-confidential notes mixed in with public documentation. Maybe it&rsquo;s solutions to homework problems that should be behind a password. Maybe it&rsquo;s personal thoughts in a blog post that you want to share with specific people but not the entire internet.</p>
<p>The usual answer is &ldquo;add a backend.&rdquo; But I didn&rsquo;t want to give up static hosting just because a few paragraphs needed a password.</p>
<p>So I built <a href="https://github.com/queelius/pagevault"><strong>pagevault</strong></a>.</p>
<hr>
<h2 id="the-core-idea">The Core Idea</h2>
<p>pagevault encrypts <em>regions</em> of HTML files — not the whole page, just the parts you mark. Public navigation, styling, and scripts remain untouched. Only the sensitive sections become ciphertext, replaced with a password prompt that decrypts in the browser using the <a href="https://developer.mozilla.org/en-US/docs/Web/API/Web_Crypto_API">Web Crypto API</a>.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-html" data-lang="html"><span class="line"><span class="cl"><span class="p">&lt;</span><span class="nt">header</span><span class="p">&gt;</span>Public navigation stays visible<span class="p">&lt;/</span><span class="nt">header</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="p">&lt;</span><span class="nt">pagevault</span> <span class="na">hint</span><span class="o">=</span><span class="s">&#34;Contact me for the password&#34;</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="p">&lt;</span><span class="nt">h2</span><span class="p">&gt;</span>Private thoughts on this topic<span class="p">&lt;/</span><span class="nt">h2</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="p">&lt;</span><span class="nt">p</span><span class="p">&gt;</span>This section is encrypted with AES-256-GCM...<span class="p">&lt;/</span><span class="nt">p</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="p">&lt;/</span><span class="nt">pagevault</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="p">&lt;</span><span class="nt">footer</span><span class="p">&gt;</span>Public footer stays visible<span class="p">&lt;/</span><span class="nt">footer</span><span class="p">&gt;</span>
</span></span></code></pre></div><p>After running <code>pagevault lock</code>, the <code>&lt;pagevault&gt;</code> block becomes an opaque blob of base64. Visitors see a styled password prompt. If they have the password, the content decrypts instantly in their browser. If they don&rsquo;t, they see nothing — not even the size of what they&rsquo;re missing (with the <code>--pad</code> flag).</p>
<p>Here&rsquo;s a real example — a team dashboard where public metrics are visible but financial data is encrypted:</p>
<p><img src="dashboard-locked.png" alt="Dashboard with encrypted region showing password prompt between public sections"></p>
<p>After entering the password, the protected sections decrypt in-place:</p>
<p><img src="dashboard-unlocked.png" alt="Same dashboard after decryption, showing Internal Revenue and Infrastructure Costs"></p>
<p>The output is a single self-contained HTML file. No JavaScript CDN, no external CSS, no runtime dependencies. You can email it, upload it to any static host, or open it from a local filesystem. Everything needed for decryption is embedded in the file.</p>
<hr>
<h2 id="encryption-details">Encryption Details</h2>
<p>The crypto is intentionally non-configurable:</p>
<ul>
<li><strong>AES-256-GCM</strong> (AEAD — authenticated encryption with associated data)</li>
<li><strong>PBKDF2-SHA256</strong> with 310,000 iterations (OWASP 2023 recommendation)</li>
<li><strong>16-byte random salt</strong>, <strong>12-byte random IV</strong> per encryption</li>
<li><strong>v2 format</strong> with content-encryption-key (CEK) wrapping</li>
</ul>
<p>I chose to fix these parameters rather than expose them as config options. Every pagevault file has the same security properties. No foot-guns, no &ldquo;I accidentally set iterations to 1.&rdquo;</p>
<p>The iteration count is stored in the encrypted payload, so future versions can increase it without breaking backward compatibility. Old files decrypt at their original iteration count; new files get the latest.</p>
<h3 id="why-cek-key-wrapping">Why CEK Key-Wrapping?</h3>
<p>Early versions derived a key directly from the password and encrypted content with it. This works fine for single-user encryption, but breaks down with multiple users.</p>
<p>If Alice and Bob each need their own password, naive multi-password means encrypting the content twice — doubling the payload size and making re-encryption expensive when passwords change.</p>
<p>The CEK model solves this cleanly. A random content-encryption key encrypts the actual data once. Then the CEK itself is wrapped (encrypted) separately for each user&rsquo;s password. Adding a new user only wraps one small key blob. Changing a password only re-wraps one key blob. The bulk content stays untouched.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">Content  ──encrypt with CEK──&gt;  Ciphertext (one copy)
</span></span><span class="line"><span class="cl">CEK  ──wrap with Alice&#39;s key──&gt;  Key Blob A
</span></span><span class="line"><span class="cl">CEK  ──wrap with Bob&#39;s key────&gt;  Key Blob B
</span></span></code></pre></div><p>The <code>pagevault sync</code> command re-wraps keys when you add or remove users. For full key rotation (new CEK), there&rsquo;s <code>pagevault sync --rekey</code>.</p>
<hr>
<h2 id="the-closure-property">The Closure Property</h2>
<p>This is my favorite design constraint. <strong>The output of <code>pagevault lock</code> must be valid input to <code>pagevault lock</code>.</strong></p>
<p>This means you can encrypt a page for Alice, then encrypt <em>different sections</em> of the same page for Bob, and everything just works. Alice sees her content. Bob sees his. The page round-trips cleanly through multiple encryption passes.</p>
<p>Why does this matter? Because it eliminates the need for a first-class &ldquo;multi-tier access&rdquo; feature. You don&rsquo;t need pagevault to understand access levels. You just run it twice with different passwords on different sections. Composability replaces configuration.</p>
<p>In practice, this constraint forces discipline throughout the codebase. The HTML manipulation layer (BeautifulSoup/lxml) must preserve structure perfectly. The injected JavaScript must be idempotent — a page with two decryption runtimes shouldn&rsquo;t conflict. Attributes must round-trip through lock/unlock cycles without mutation.</p>
<p>Every release runs roundtrip tests: lock, then lock again, then unlock twice. If any content is lost or corrupted, the test fails.</p>
<hr>
<h2 id="beyond-html-encrypting-arbitrary-files">Beyond HTML: Encrypting Arbitrary Files</h2>
<p>Once you can encrypt HTML regions, the next question is obvious: what about PDFs? Images? Entire directories?</p>
<p>pagevault handles this by wrapping non-HTML files into encrypted HTML payloads:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">pagevault lock report.pdf              <span class="c1"># → _locked/report.pdf.html</span>
</span></span><span class="line"><span class="cl">pagevault lock presentation.pptx       <span class="c1"># → _locked/presentation.pptx.html</span>
</span></span><span class="line"><span class="cl">pagevault lock mysite/ --site          <span class="c1"># → _locked/mysite.html</span>
</span></span></code></pre></div><p>The encrypted HTML includes the file&rsquo;s contents as a base64 payload. When decrypted in the browser, a <strong>viewer plugin</strong> renders the content inline — images display as images, PDFs render in an embedded viewer, Markdown gets formatted with headings and code blocks.</p>
<p>Here&rsquo;s what a password-protected file looks like before and after decryption:</p>
<p><img src="file-locked.png" alt="Encrypted file showing password prompt with lock icon and filename"></p>
<p>After entering the password, the viewer renders the content with a toolbar showing the filename, file size, and a download button:</p>
<p><img src="markdown-decrypted.png" alt="Decrypted markdown file with rendered headings, lists, and code blocks"></p>
<p>The text viewer renders plain text with line numbers — useful for meeting notes, logs, or config files:</p>
<p><img src="text-decrypted.png" alt="Decrypted text file with line numbers and monospace rendering"></p>
<h3 id="viewer-plugin-system">Viewer Plugin System</h3>
<p>The viewer architecture follows a MIME-type dispatch model. Each viewer plugin declares which MIME types it handles:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="k">class</span> <span class="nc">ImageViewer</span><span class="p">(</span><span class="n">ViewerPlugin</span><span class="p">):</span>
</span></span><span class="line"><span class="cl">    <span class="n">name</span> <span class="o">=</span> <span class="s2">&#34;image&#34;</span>
</span></span><span class="line"><span class="cl">    <span class="n">mime_types</span> <span class="o">=</span> <span class="p">[</span><span class="s2">&#34;image/*&#34;</span><span class="p">]</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">def</span> <span class="nf">js</span><span class="p">(</span><span class="bp">self</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="s2">&#34;&#34;&#34;async function(container, blob, url, meta, toolbar) {
</span></span></span><span class="line"><span class="cl"><span class="s2">            const img = document.createElement(&#39;img&#39;);
</span></span></span><span class="line"><span class="cl"><span class="s2">            img.src = url;
</span></span></span><span class="line"><span class="cl"><span class="s2">            container.appendChild(img);
</span></span></span><span class="line"><span class="cl"><span class="s2">        }&#34;&#34;&#34;</span>
</span></span></code></pre></div><p>Seven viewers ship built-in: Image, PDF, HTML, Text, Markdown, Audio, and Video. Each lives in its own file under <code>viewers/builtins/</code>, and the registry discovers them automatically by scanning the directory — no registration boilerplate required.</p>
<p>Custom viewers work the same way. Point <code>viewers_dir</code> in your <code>.pagevault.yaml</code> at a directory of <code>.py</code> files that define <code>ViewerPlugin</code> subclasses, and they&rsquo;re picked up automatically. User viewers override builtins on name collision, so you can replace any built-in viewer with your own implementation.</p>
<p>Resolution follows a priority chain: exact MIME match first, then wildcard patterns (e.g., <code>image/*</code>), then a download fallback. The viewer&rsquo;s JavaScript, CSS, and any dependencies are embedded directly into the encrypted HTML — maintaining the self-contained property even with custom viewers.</p>
<p>Security is enforced at multiple layers. Plugin names and MIME types are validated via regex at class definition time (<code>__init_subclass__</code>), re-validated at the injection point (defense-in-depth), and all JS/CSS content is escaped to prevent <code>&lt;/script&gt;</code> breakout. The HTML viewer renders in a sandboxed iframe (<code>sandbox='allow-same-origin'</code>, no <code>allow-scripts</code>).</p>
<hr>
<h2 id="cli-design">CLI Design</h2>
<p>pagevault follows a mark-then-lock workflow:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl"><span class="c1"># 1. Mark what to encrypt (in-place, content stays plaintext)</span>
</span></span><span class="line"><span class="cl">pagevault mark index.html -s <span class="s2">&#34;#secret&#34;</span> --hint <span class="s2">&#34;Ask admin&#34;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># 2. Encrypt marked regions (outputs to _locked/)</span>
</span></span><span class="line"><span class="cl">pagevault lock index.html
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># 3. Deploy _locked/ to your static host</span>
</span></span></code></pre></div><p>The <code>lock</code> command is unified — it detects whether the input is HTML (encrypt marked regions), a non-HTML file (wrap as encrypted HTML), or a directory with <code>--site</code> (bundle as a single-page encrypted site).</p>
<p>A few commands that turned out to be surprisingly useful:</p>
<ul>
<li><strong><code>pagevault info</code></strong> — inspect an encrypted file without the password. Shows encryption metadata, number of key blobs, viewer information, content hash.</li>
<li><strong><code>pagevault check -p PASSWORD</code></strong> — fast password verification. Does one PBKDF2 derivation and one key unwrap, returns exit code 0 or 1. Useful in CI scripts.</li>
<li><strong><code>pagevault unlock --stdout</code></strong> — pipe decrypted content to stdout. Combined with shell pipelines, this makes pagevault a building block:</li>
</ul>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">pagevault unlock report.pdf.html --stdout -p <span class="s2">&#34;</span><span class="nv">$SECRET</span><span class="s2">&#34;</span> &gt; report.pdf
</span></span></code></pre></div><hr>
<h2 id="what-it-defends-against-and-what-it-doesnt">What It Defends Against (and What It Doesn&rsquo;t)</h2>
<p>pagevault protects against casual snooping (web scrapers, search engines, curious visitors) and determined offline attacks (someone downloads the HTML and runs dictionary attacks against the PBKDF2-wrapped payload). AES-256-GCM with 310k PBKDF2 iterations makes brute-force expensive.</p>
<p>It does <strong>not</strong> defend against browser-level adversaries (malicious extensions, XSS on the same origin) or hosting providers modifying the served HTML. The threat model is &ldquo;your static host is honest but the public internet is not.&rdquo; This is the right tradeoff for static hosting — if you need protection against a malicious host, you need a different architecture entirely.</p>
<hr>
<h2 id="try-it">Try It</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-bash" data-lang="bash"><span class="line"><span class="cl">pip install pagevault
</span></span><span class="line"><span class="cl">pagevault config init
</span></span><span class="line"><span class="cl">pagevault mark page.html -s <span class="s2">&#34;.private&#34;</span>
</span></span><span class="line"><span class="cl">pagevault lock page.html
</span></span></code></pre></div><p>The project is on <a href="https://github.com/queelius/pagevault">GitHub</a>. It&rsquo;s MIT-licensed, has 530+ tests, and is designed to be a building block — composable, self-contained, no server required.</p>
<hr>
<p><em>Static sites shouldn&rsquo;t need backends just because some content needs a password.</em></p>]]></content:encoded><category>pagevault</category><category>encryption</category><category>static-sites</category><category>python</category><category>web-crypto</category><category>open-source</category><category>hugo</category></item></channel></rss>