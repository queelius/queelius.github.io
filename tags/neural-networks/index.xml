<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0"
     xmlns:atom="http://www.w3.org/2005/Atom"
     xmlns:content="http://purl.org/rss/1.0/modules/content/"
     xmlns:dc="http://purl.org/dc/elements/1.1/"
     xmlns:media="http://search.yahoo.com/mrss/">
  <channel>
    <title>Neural-Networks on metafunctor</title>
    <link>https://metafunctor.com/tags/neural-networks/</link>
    <description>Research Engineer &amp; Computer Scientist - Machine Learning, Statistical Computing, Open Source Development</description>
    <language>en-us</language>
    <managingEditor>lex@metafunctor.com (Alex Towell)</managingEditor>
    <webMaster>lex@metafunctor.com (Alex Towell)</webMaster>
    
    <lastBuildDate>Sat, 04 Jul 2026 21:09:25 -0500</lastBuildDate>
    <atom:link href="https://metafunctor.com/tags/neural-networks/" rel="self" type="application/rss+xml" />
    <image>
      <url>https://metafunctor.com/images/og-image.jpg</url>
      <title>metafunctor</title>
      <link>https://metafunctor.com/</link>
    </image>
    
    <item>
      <title>Watch: Inductive Biases, What Your Architecture Assumes</title>
      <link>https://metafunctor.com/post/2026-07-04-inductive-biases-playlist/</link>
      <pubDate>Sat, 04 Jul 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">https://metafunctor.com/post/2026-07-04-inductive-biases-playlist/</guid>
      <description>The inductive-biases series is now a ten-episode animated playlist: every architecture is a bet about the world, and the bet is the bias. No free lunch first, then each architecture read as its assumptions, graded on one scorecard.</description>
      <content:encoded><![CDATA[<p>The <a href="/series/inductive-biases/">inductive-biases series</a> is now a playlist. Ten episodes, animated and narrated, built from the same posts and the same code.</p>
<p>The series starts from one claim: every learner that generalizes is making a bet about the world, and the bet is the bias. It opens with the theory that makes the bet mandatory. There is no free lunch: averaged over all possible worlds, no learning algorithm beats guessing, so any method that wins somewhere has to lose somewhere else, and the wins come only from assumptions that happen to match the world you are actually in. That is the whole game. You do not get generalization for free; you buy it with a prior, and you pay for it out of distribution.</p>
<p>From there the series reads each major neural architecture as a bundle of assumptions. What the loss function presumes about noise. What a convolution presumes about space. What Bengio&rsquo;s language model presumes about time, what recurrence and attention presume about memory, and what the policy gradient presumes about reward. Every architecture episode fills in the same scorecard: the bias it hardwires, the sample-efficiency win when that bias is true, and the bill that comes due when it is not. By the end the scorecard is a habit, and you find yourself reading any new model the same way, by asking what it assumes before asking what it does.</p>
<p>It stays sharp where the sources are sharp. Attention weights are not information flow, however much the heatmaps invite you to read them that way. The loss function is a distribution assumption whether you chose it deliberately or reached for the default. The series is a dialogue, and the student voices the conventional view sincerely, the one most people actually hold, so the teacher can take it apart with the real argument rather than a strawman.</p>
<p>The theoretical spine draws on my book <a href="https://www.amazon.com/dp/B0H4DMBHND">On Intelligence and Its Specifications</a>, specifically its account of why generalization is possible at all and what it costs, and the code on screen is real, taken from <a href="https://github.com/queelius/scratchnn">scratchnn</a>, a neural network library written in pure Python so you can read every line. This series is about meaning, not mechanism: how the gradients actually flow is its own from-scratch series, coming later. Here the question is always what the architecture assumes.</p>
<h2 id="the-playlist">The playlist</h2>
<div class="youtube-series">
  <div class="youtube-playlist" style="position:relative;width:100%;padding-bottom:56.25%;height:0;overflow:hidden;margin:1.5rem 0;">
    <iframe
      src="https://www.youtube-nocookie.com/embed/videoseries?list=PLQTcXcKfBblU"
      title="Inductive Biases: What Your Architecture Assumes"
      style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;"
      allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
      allowfullscreen
      loading="lazy"
      referrerpolicy="strict-origin-when-cross-origin"></iframe>
  </div>
  <p><a href="https://www.youtube.com/playlist?list=PLQTcXcKfBblU" rel="noopener" target="_blank">Open the playlist on YouTube</a> (10 episodes)</p>
  <details class="youtube-series-episodes">
    <summary>Episode list</summary>
    <ol>
      <li><a href="https://youtu.be/n47TpVeXHFU" rel="noopener" target="_blank">Learning Without Assumptions Is Impossible</a></li>
      <li><a href="https://youtu.be/KLufDBw4um0" rel="noopener" target="_blank">Sample Efficiency and the Price of Priors</a></li>
      <li><a href="https://youtu.be/d01x3xo3HF8" rel="noopener" target="_blank">A Network Computes Numbers. The Loss Decides What They Mean.</a></li>
      <li><a href="https://youtu.be/OamSnH-Do6M" rel="noopener" target="_blank">The Loss Function Is a Distribution Assumption</a></li>
      <li><a href="https://youtu.be/4FrVL9lOPh8" rel="noopener" target="_blank">What a Convolution Assumes</a></li>
      <li><a href="https://youtu.be/CsLhbiC-4Co" rel="noopener" target="_blank">Bengio&#39;s Language Model: The Markov Assumption Made Architectural</a></li>
      <li><a href="https://youtu.be/d9Adp6KktOI" rel="noopener" target="_blank">Recurrence Is Weight Sharing Across Time, and It Costs You</a></li>
      <li><a href="https://youtu.be/AFZJW5RdckI" rel="noopener" target="_blank">Attention Is a Learned Pointer Dereference</a></li>
      <li><a href="https://youtu.be/GiJwuZ66H3s" rel="noopener" target="_blank">Attention Weight Is Not Information Flow</a></li>
      <li><a href="https://youtu.be/DJNBGPVek8M" rel="noopener" target="_blank">Reinforcement Learning Is Cross-Entropy, Reweighted by Reward</a></li>
    </ol>
  </details>
</div>
]]></content:encoded>
      
      
      <category>youtube</category>
      
      <category>video</category>
      
      <category>inductive-bias</category>
      
      <category>deep-learning</category>
      
      <category>neural-networks</category>
      
      <category>machine-learning</category>
      
      <category>no-free-lunch</category>
      
      
    </item>
    
    <item>
      <title>A Network Computes Numbers. The Loss Decides What They Mean.</title>
      <link>https://metafunctor.com/post/2026-06-09-a-network-computes-numbers/</link>
      <pubDate>Tue, 09 Jun 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">https://metafunctor.com/post/2026-06-09-a-network-computes-numbers/</guid>
      <description>Function approximation, why one linear unit cannot learn XOR, and what a hidden layer actually buys. The opening of a from-scratch tour of inductive bias.</description>
      <content:encoded><![CDATA[<p>A neural network is a parameterized function. It takes a vector of numbers and returns a vector of numbers, and that is the whole structural commitment. What the output numbers <em>mean</em>, a probability, a class score, a count, a rate, is a separate decision. The network does not make it. The loss does.</p>
<p>I wrote a small library, scratchnn, to make this concrete. Pure Python, standard library only, no NumPy in the core, because the point is to read it rather than run it fast. This post is the first stop in a tour through it. The organizing question for the whole series is simple: every architecture and every loss is a bet about the data. The bets have a name. They are inductive biases, and the right one is what lets a model learn more from less.</p>
<h2 id="the-cheapest-model-and-where-it-fails">The cheapest model, and where it fails</h2>
<p>Start with the smallest network: one linear layer, $\mathbf{z} = W\mathbf{x} + \mathbf{b}$. No hidden units, no nonlinearity. This is a single linear unit, and the function it can represent is exactly a line (a hyperplane, in higher dimensions). It fits anything linearly separable, and nothing else.</p>
<p>The standard way to see &ldquo;nothing else&rdquo; is XOR: the four points of the unit square, with the two diagonal corners labeled 1 and the other two labeled 0. Try to separate them with a sigmoid on top of a single linear unit and you can write down what success would require:</p>
$$b < 0, \quad w_1 + b > 0, \quad w_2 + b > 0, \quad w_1 + w_2 + b < 0.$$<p>Add the middle two and you get $w_1 + w_2 + b &gt; -b &gt; 0$. The last one says it is below zero. There is no assignment of weights that satisfies all four. The model cannot fit XOR, and if you train it, every input drifts to probability one half and stays there. Geometrically, one line cannot separate two corners that sit on a diagonal. This was Minsky and Papert&rsquo;s argument in 1969, and it stalled the field for over a decade.</p>
<h2 id="what-a-hidden-layer-buys">What a hidden layer buys</h2>
<p>The fix is one hidden layer with a nonlinearity between the linear maps. Without the nonlinearity the layers collapse: two affine maps composed are just one affine map, and stacking buys nothing. With it, the hidden units can build intermediate features that <em>are</em> linearly separable, and the output layer reads them off. For XOR, one hidden unit can detect OR and another AND, and the difference is XOR. A 2-8-1 network with a tanh in the middle learns it cleanly.</p>
<p>The general statement is the universal approximation theorem: one hidden layer of sufficient width, with almost any nonlinearity, approximates any continuous function on a bounded region to whatever precision you want. Worth being honest about what that does and does not say. It says depth is not <em>required</em> for expressivity; one wide hidden layer is enough in principle. It does not say one layer is efficient. Many functions that a single huge layer can only represent at exponential width are cheap to represent with a few layers stacked. Depth is about economy, not possibility. The reason to have a hidden layer at all is XOR. The reason to have several is that it is usually cheaper.</p>
<h2 id="the-other-half-the-output-is-interpreted-not-produced">The other half: the output is interpreted, not produced</h2>
<p>Here is the move the rest of the series leans on. The network produces raw, unnormalized scores, call them logits. It never produces a probability. A probability is something the <em>loss</em> computes, by composing the logits with a link function (a sigmoid, a softmax, the identity) and a matching negative log likelihood. The same network body becomes a binary classifier, a multi-class classifier, a real-valued regressor, or a count model depending only on the head you bolt onto its outputs. Nothing about the body changes.</p>
<p>One small fact that turns out to run deep: the gradient of every one of these canonical losses, with respect to the logits, is $\hat{p} - y$, the prediction minus the target. The residual. It looks like a coincidence the first time you see it across logistic regression and softmax and least squares. It is not. The next post is about why, and about treating the output head as a modeling choice in its own right.</p>
<h2 id="the-shape-of-the-series">The shape of the series</h2>
<p>Two axes have already appeared. The <em>architecture</em> decides which functions are reachable and cheap (a single unit cannot do XOR; an MLP can). The <em>output head</em> decides what the outputs mean (the loss, the link function, the assumed distribution). Both are inductive biases. A third axis shows up later, once we start training real models and find that having the right architecture is necessary but not sufficient.</p>
<p>Everything from here is the same MLP with a sharper prior baked in at one of these axes: convolution for images, recurrence for sequences, attention for content-addressable lookup, and so on. The math we need (forward, backward, the chain rule applied locally) does not change. Only the layer types and the heads do.</p>
<p>The full version, with the hand-derived backward pass, the actual <code>Layer</code> and <code>Network</code> code, the numerical-stability details, and the gradient checker that keeps the math honest, is Chapter 1 of the book, <a href="/papers/inductive-biases/"><em>Inductive Biases in Neural Networks</em></a>.</p>
<h2 id="watch-the-episode">Watch the Episode</h2>
<div class="youtube-episode" style="position:relative;width:100%;padding-bottom:56.25%;height:0;overflow:hidden;margin:1.5rem 0;">
  <iframe
    src="https://www.youtube-nocookie.com/embed/d01x3xo3HF8"
    title="A Network Computes Numbers. The Loss Decides What They Mean."
    style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;"
    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
    allowfullscreen
    loading="lazy"
    referrerpolicy="strict-origin-when-cross-origin"></iframe>
</div>
<p class="youtube-episode-links"><em>This post is also a video episode: <a href="https://youtu.be/d01x3xo3HF8" rel="noopener" target="_blank">A Network Computes Numbers. The Loss Decides What They Mean.</a>, part of <a href="https://www.youtube.com/playlist?list=PLQTcXcKfBblU" rel="noopener" target="_blank">the series playlist</a>.</em></p>
]]></content:encoded>
      
      
      <category>neural-networks</category>
      
      <category>inductive-bias</category>
      
      <category>function-approximation</category>
      
      <category>from-scratch</category>
      
      
    </item>
    
    <item>
      <title>Attention Is a Learned Pointer Dereference</title>
      <link>https://metafunctor.com/post/2026-06-09-attention-is-a-learned-pointer-dereference/</link>
      <pubDate>Tue, 09 Jun 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">https://metafunctor.com/post/2026-06-09-attention-is-a-learned-pointer-dereference/</guid>
      <description>An attention head is a learned content-addressable lookup: a query matches keys, retrieves a value, exactly like dereferencing a pointer. Depth is how many lookups you can compose.</description>
      <content:encoded><![CDATA[<p>An attention head is a learned content-addressable lookup. A query at one position is compared against keys at every position, a softmax picks the best match, and the head returns that position&rsquo;s value. Read it as a pointer dereference: the query says &ldquo;I want the thing at the position that looks like this,&rdquo; the keys advertise what each position is, and the softmax spikes on the one that matches. Out comes its value.</p>
<p>That is the entire structural prior of a transformer. Not locality, not recurrence, but a learned mechanism for deciding <em>which</em> positions matter, computed from the data, position by position, on the fly. The earlier architectures committed to a fixed notion of relevance (nearby pixels, recent timesteps). Attention commits to a way of figuring out relevance instead.</p>
<h2 id="a-task-that-forces-the-issue">A task that forces the issue</h2>
<p>A claim about a mechanism is worth more if you can build something that requires it. So here is a deliberately small task. Feed the model a string of memory bits, then an address, and ask it to output the memory bit <em>at</em> that address. To get it right the model has to read the address and fetch the bit it points to. That is content-addressable memory, stripped to the bone, and a model with no lookup mechanism cannot do better than guessing.</p>
<p>A transformer learns it, and watching attention while it does is the point: the relevant head, at the output position, puts almost all its weight on the addressed memory cell. The pointer dereference is visible.</p>
<h2 id="depth-is-the-number-of-lookups-you-can-compose">Depth is the number of lookups you can compose</h2>
<p>One attention layer is not enough, for a reason that is structural rather than about capacity. The query at the final position is built only from that position&rsquo;s own token, so it does not yet know the full address (the address bits live at other positions). It cannot route to a cell whose identity depends on bits it has not seen. Two layers fix this: the first layer gathers the address bits into one place, and the second layer uses the assembled address to do the fetch. Transformer depth is, quite literally, how many lookups you can chain. Each layer is one more stage of dynamic routing.</p>
<h2 id="the-experiment-that-earned-its-keep">The experiment that earned its keep</h2>
<p>Then I scaled the memory up, expecting it to just keep working, and it did not, which turned out to be the most useful thing in the chapter. At a certain memory size a two-layer model stops solving the task, and here is the part worth sitting with: making it wider does not help, and training it longer does not help. It sits at chance-plus no matter what.</p>
<p>Add one layer, holding the width and the training budget fixed, and it transitions from stuck to solved. Depth is the knob. Width is not. That is exactly what the composed-lookup story predicts: a larger memory needs a sharper, more assembled query than two layers can build, and the third layer is where the extra assembly happens. It is a clean little result about when an architecture&rsquo;s prior is actually reachable, and it sets up an uncomfortable question for the next post.</p>
<p>Because here is the thing. The two-layer model&rsquo;s attention plainly showed the lookup. Does the deeper model&rsquo;s attention show it too, once the task gets hard? That is where the story stops being tidy.</p>
<p>The attention mechanism in code, the structural argument for why one layer fails, and the controlled depth-versus-width experiment are in Chapter 6 of the book, <a href="/papers/inductive-biases/"><em>Inductive Biases in Neural Networks</em></a>.</p>
<h2 id="watch-the-episode">Watch the Episode</h2>
<div class="youtube-episode" style="position:relative;width:100%;padding-bottom:56.25%;height:0;overflow:hidden;margin:1.5rem 0;">
  <iframe
    src="https://www.youtube-nocookie.com/embed/AFZJW5RdckI"
    title="Attention Is a Learned Pointer Dereference"
    style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;"
    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
    allowfullscreen
    loading="lazy"
    referrerpolicy="strict-origin-when-cross-origin"></iframe>
</div>
<p class="youtube-episode-links"><em>This post is also a video episode: <a href="https://youtu.be/AFZJW5RdckI" rel="noopener" target="_blank">Attention Is a Learned Pointer Dereference</a>, part of <a href="https://www.youtube.com/playlist?list=PLQTcXcKfBblU" rel="noopener" target="_blank">the series playlist</a>.</em></p>
]]></content:encoded>
      
      
      <category>neural-networks</category>
      
      <category>inductive-bias</category>
      
      <category>transformers</category>
      
      <category>attention</category>
      
      <category>content-addressable-memory</category>
      
      
    </item>
    
    <item>
      <title>Attention Weight Is Not Information Flow</title>
      <link>https://metafunctor.com/post/2026-06-09-attention-weight-is-not-information-flow/</link>
      <pubDate>Tue, 09 Jun 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">https://metafunctor.com/post/2026-06-09-attention-weight-is-not-information-flow/</guid>
      <description>The trained pointer model reads exactly the right memory cell, provably. Its attention barely shows where. The gap, and the causal probe that closes it.</description>
      <content:encoded><![CDATA[<p>Take the deeper pointer model from the last post and ask two questions. Does it compute the right thing? And can you see <em>how</em> by reading its attention? The answers come apart, and that gap is the whole lesson of mechanistic interpretability, in miniature.</p>
<h2 id="behaviorally-it-is-a-perfect-pointer">Behaviorally, it is a perfect pointer</h2>
<p>There is a test that does not depend on reading any attention map. Flip the bit at the addressed memory cell and check the output: it tracks the new value about 99.5% of the time. Flip any <em>other</em> cell and the output changes about 0.5% of the time. So the model reads exactly the addressed cell and nothing else. That is airtight, and it required looking at no internals at all.</p>
<h2 id="mechanically-the-clean-circuit-is-gone">Mechanically, the clean circuit is gone</h2>
<p>At small memory the attention was legible. One layer gathered the address, the next spiked on the addressed cell, and you could read the dereference straight off the maps. Scale the memory up and that legibility evaporates: the final query barely concentrates on the correct cell for most addresses. Stare at the attention and you mostly do not see the lookup happening. But the flip test says it is happening. So which is it.</p>
<p>The resolution is a fact worth internalizing: <strong>attention weight is not information flow.</strong> A head&rsquo;s output is the attention-weighted sum of <em>values</em>, and the value projection can quietly suppress a cell you attend to hard, or carry real information through a cell you barely attend to at all. On top of that, the computation spreads across heads and layers. The attention map is a weak instrument the moment the work distributes.</p>
<h2 id="the-causal-probe-that-recovers-it">The causal probe that recovers it</h2>
<p>The right tool is causal. Take a clean run and a run with the addressed bit flipped, then patch the clean residual stream back in at one (layer, position) at a time and see where restoring it restores the answer. Do that and a clean picture appears that the attention maps hid: the bit&rsquo;s value is transported from the addressed cell toward the readout position, across the layers. The structure was there the whole time, in the residual stream, not in the attention weights.</p>
<h2 id="what-actually-happened-including-the-wrong-turns">What actually happened, including the wrong turns</h2>
<p>I want to be honest about how this went, because the tidy version is a lie. I started with a clean hypothesis: that a particular layer &ldquo;refines&rdquo; the lookup the earlier layers begin. The data refuted it. I formed a second clean hypothesis, that the heads partition the address space among themselves. The data refuted that too. What survived is narrower and less satisfying: the model computes the right cell (the flip test is not negotiable), the extra depth buys a multi-stage assembly of the query, and the dereference itself is distributed in a way the attention maps will not show you. Two nice stories died to get there.</p>
<h2 id="this-is-the-induction-head-story-small">This is the induction-head story, small</h2>
<p>None of this is exotic. It is the induction-head and in-context-learning picture compressed into a toy. In real language models the same primitive, find the relevant earlier position and copy what is useful from it, is spread across many heads, and ablating any single one barely moves the behavior because the others cover for it. The clean mechanistic accounts in that literature come from causal patching, not from looking at attention. The small-memory-to-large-memory step here is that whole arc in fast-forward: a legible circuit when the problem is tiny, illegible attention when it grows, and a causal probe that puts the mechanism back in view.</p>
<p>The lesson generalizes past this toy. Behavioral and causal legibility can stay airtight while the read-it-off-the-weights kind degrades with scale. That is why the field reaches for causal interventions, and why &ldquo;head 7 is the lookup head&rdquo; is a sentence you should not believe without a causal test behind it.</p>
<p>The flip test, the per-layer causal trace, the comparison to induction heads, and the two refuted hypotheses written up in full are in Chapter 7 of the book, <a href="/papers/inductive-biases/"><em>Inductive Biases in Neural Networks</em></a>.</p>
<h2 id="watch-the-episode">Watch the Episode</h2>
<div class="youtube-episode" style="position:relative;width:100%;padding-bottom:56.25%;height:0;overflow:hidden;margin:1.5rem 0;">
  <iframe
    src="https://www.youtube-nocookie.com/embed/GiJwuZ66H3s"
    title="Attention Weight Is Not Information Flow"
    style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;"
    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
    allowfullscreen
    loading="lazy"
    referrerpolicy="strict-origin-when-cross-origin"></iframe>
</div>
<p class="youtube-episode-links"><em>This post is also a video episode: <a href="https://youtu.be/GiJwuZ66H3s" rel="noopener" target="_blank">Attention Weight Is Not Information Flow</a>, part of <a href="https://www.youtube.com/playlist?list=PLQTcXcKfBblU" rel="noopener" target="_blank">the series playlist</a>.</em></p>
]]></content:encoded>
      
      
      <category>neural-networks</category>
      
      <category>inductive-bias</category>
      
      <category>interpretability</category>
      
      <category>induction-heads</category>
      
      <category>in-context-learning</category>
      
      
    </item>
    
    <item>
      <title>Bengio&#39;s Language Model: the Markov Assumption Made Architectural</title>
      <link>https://metafunctor.com/post/2026-06-09-bengios-language-model-markov-architectural/</link>
      <pubDate>Tue, 09 Jun 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">https://metafunctor.com/post/2026-06-09-bengios-language-model-markov-architectural/</guid>
      <description>The simplest neural language model: embed the last N tokens, concatenate, predict the next. What you give up with a fixed window, and what you gain by dropping recurrence.</description>
      <content:encoded><![CDATA[<p>The simplest neural language model is almost embarrassingly direct. Look at the last $N$ tokens. Embed each one through a shared lookup table. Concatenate the embeddings into one vector. Run it through a small MLP that outputs a distribution over the next token. That is the entire model, and it is roughly what Bengio and coauthors introduced in 2003, the architecture that held the field before recurrent networks and then transformers took over.</p>
<p>It is worth building because it makes one prior completely explicit.</p>
<h2 id="a-fixed-window-is-the-markov-assumption-in-hardware">A fixed window is the Markov assumption, in hardware</h2>
<p>The model can only see $N$ tokens back. Anything older is gone, not because the optimizer failed to learn it but because there is no wire for it to travel on. This is the order-$N$ Markov assumption, turned from a modeling approximation into a structural fact of the architecture. Choose $N$ too small and the model cannot represent long-range agreement; choose it too large and the MLP head grows linearly in $N$ and starts to overfit.</p>
<h2 id="a-hybrid-prior-worth-noticing">A hybrid prior worth noticing</h2>
<p>The interesting part is how Bengio handles position. The embedding table is shared across positions: the token &ldquo;the&rdquo; gets the same vector whether it sits at slot 1 or slot 5. That is position equivariance, the same weight-sharing idea as the convolution, applied to token identity. But then the concatenation step makes the head position <em>sensitive</em>. Token 1&rsquo;s embedding always lands in the first slice of the input vector, token 2&rsquo;s in the second, and so on, so the MLP can learn that the immediately preceding token matters more than the one $N$ back. You get word-identity invariance for free and still use word order. One layer is equivariant, the next is deliberately not.</p>
<h2 id="what-dropping-recurrence-buys">What dropping recurrence buys</h2>
<p>The reason this architecture is worth keeping in the toolbox, even now, is what it avoids. There is no recurrence, so there is no backpropagation through time and no vanishing-gradient problem (the subject of the next post). Every training example, a window and its next token, is independent of every other, so training parallelizes trivially and there is no per-sequence bookkeeping.</p>
<p>On character-level text (the opening of <em>Alice in Wonderland</em>, which I also use for the recurrent model so the comparison is fair) the fixed-context model reaches the same loss as the recurrent network in roughly a quarter of the epochs. Same endpoint, less effort, because there is no unrolled chain to push gradients back through.</p>
<p>What you give up is the cutoff. The model genuinely cannot use a token from a hundred positions back, where a recurrent network at least has a path for it (a thin and leaky one, as we will see). It is the right model when the context that matters is small and you know how small. When it is not, you reach for an architecture with unbounded reach, and pay for that reach in other ways.</p>
<p>The embedding and concatenation code, the parameter accounting, and the side-by-side Alice experiment against the recurrent model are in Chapter 4 of the book, <a href="/papers/inductive-biases/"><em>Inductive Biases in Neural Networks</em></a>.</p>
<h2 id="watch-the-episode">Watch the Episode</h2>
<div class="youtube-episode" style="position:relative;width:100%;padding-bottom:56.25%;height:0;overflow:hidden;margin:1.5rem 0;">
  <iframe
    src="https://www.youtube-nocookie.com/embed/CsLhbiC-4Co"
    title="Bengio&#39;s Language Model: The Markov Assumption Made Architectural"
    style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;"
    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
    allowfullscreen
    loading="lazy"
    referrerpolicy="strict-origin-when-cross-origin"></iframe>
</div>
<p class="youtube-episode-links"><em>This post is also a video episode: <a href="https://youtu.be/CsLhbiC-4Co" rel="noopener" target="_blank">Bengio&#39;s Language Model: The Markov Assumption Made Architectural</a>, part of <a href="https://www.youtube.com/playlist?list=PLQTcXcKfBblU" rel="noopener" target="_blank">the series playlist</a>.</em></p>
]]></content:encoded>
      
      
      <category>neural-networks</category>
      
      <category>inductive-bias</category>
      
      <category>language-models</category>
      
      <category>embeddings</category>
      
      
    </item>
    
    <item>
      <title>Inductive Biases in Neural Networks</title>
      <link>https://metafunctor.com/papers/inductive-biases/</link>
      <pubDate>Tue, 09 Jun 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">https://metafunctor.com/papers/inductive-biases/</guid>
      <description> This is the bound, systematic version of the Inductive Biases in Neural Networks series. Eight chapters, three parts, built on the pure-Python scratchnn library. It is pedagogical, not new research: there is no theorem here that was not already known. What it offers is one lens applied all the way through, with the hand-derived backward passes, the actual code, and the experiments the posts only gesture at.
Download PDF (8 chapters, 112 pp) A chapter-by-chapter HTML edition, rendered from the …</description>
      <content:encoded><![CDATA[<style>
.action-bar { margin: 0 0 1rem 0; display: flex; gap: .75rem; flex-wrap: wrap; }
.action-bar a {
  text-decoration: none; padding: .5rem .9rem;
  border: 1px solid var(--color-border-default, #ddd); border-radius: .4rem;
  display: inline-block; color: var(--color-accent-primary, #007acc);
}
.action-bar a.primary { background: var(--color-accent-primary, #007acc); color: #fff; border-color: transparent; }
.action-bar a:hover { background: var(--color-bg-secondary, #f5f5f5); }
.iframe-wrap { width: 100%; margin: 0; padding: 0; }
.iframe-wrap iframe { display: block; width: 100%; border: 1px solid var(--color-border-default,#eee); min-height: 120vh; }
</style>
<p>This is the bound, systematic version of the <a href="/series/inductive-biases/">Inductive Biases in Neural Networks</a> series. Eight chapters, three parts, built on the pure-Python <a href="https://github.com/queelius/scratchnn"><code>scratchnn</code></a> library. It is pedagogical, not new research: there is no theorem here that was not already known. What it offers is one lens applied all the way through, with the hand-derived backward passes, the actual code, and the experiments the posts only gesture at.</p>
<div class="action-bar">
  <a class="primary" href="/latex/inductive-biases/inductive-biases-neural-networks.pdf" target="_blank" rel="noopener">Download PDF (8 chapters, 112 pp)</a>
</div>
<p>A chapter-by-chapter HTML edition, rendered from the LaTeX source, is in progress and will be embedded here. For now the PDF is below.</p>
<div class="iframe-wrap">
  <iframe src="/latex/inductive-biases/inductive-biases-neural-networks.pdf" loading="lazy" title="Inductive Biases in Neural Networks (PDF)"></iframe>
</div>
]]></content:encoded>
      
      
      <category>neural networks</category>
      
      <category>inductive bias</category>
      
      <category>from scratch</category>
      
      <category>interpretability</category>
      
      <category>pedagogy</category>
      
      
    </item>
    
    <item>
      <title>Recurrence Is Weight Sharing Across Time, and It Costs You</title>
      <link>https://metafunctor.com/post/2026-06-09-recurrence-is-weight-sharing-across-time/</link>
      <pubDate>Tue, 09 Jun 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">https://metafunctor.com/post/2026-06-09-recurrence-is-weight-sharing-across-time/</guid>
      <description>A recurrent network reuses one cell at every timestep and carries a state. That buys time-translation equivariance and unbounded reach in principle, and bills you in vanishing gradients.</description>
      <content:encoded><![CDATA[<p>A recurrent network is an MLP with one prior wired into it: the same small computation runs at every timestep, and a hidden state carries a running summary of everything seen so far. The cell is</p>
$$h_t = \tanh(W_{xh}\, x_t + W_{hh}\, h_{t-1} + b),$$<p>and the weights do not depend on $t$. One cell, reused down the whole sequence.</p>
<p>That is the same move the convolution made, with time in place of space. Weight sharing across positions gave translation equivariance for images; weight sharing across timesteps gives time-translation equivariance for sequences. Shift the input in time, the output shifts. And again the backward pass turns the reuse into a sum: the gradient for the shared weights accumulates over every timestep, the same <code>+=</code> over a shared axis that showed up for convolution.</p>
<h2 id="what-is-new-the-output-is-the-next-input">What is new: the output is the next input</h2>
<p>The convolution applied its kernel to fixed input positions. The recurrent cell feeds its own output back in as the next step&rsquo;s state. The dependency is sequential and arbitrarily long, and that changes how you train it. You unroll the cell across the sequence and backpropagate through time. The only twist is that the gradient arriving at step $t$ has two sources, and you add them: what flowed back from this step&rsquo;s own output, and what flowed back from the future through the next state. That merge is the whole of backpropagation through time. Everything else is the chain rule you already had.</p>
<h2 id="the-price-of-the-prior">The price of the prior</h2>
<p>Backprop through time is correct and fragile. Each step backward multiplies the gradient by a Jacobian, so the gradient that reaches an early timestep is a long product of them, governed by the spectrum of the recurrent matrix and the slope of the tanh, which is at most one and usually less. If that product shrinks, the gradient to early steps vanishes exponentially and the model gets almost no signal about long-range structure. If it grows, the gradient explodes and one bad sequence wrecks the weights.</p>
<p>This is the vanishing-gradient problem, and it is worth seeing it as the bill for the inductive bias, not a bug. The architecture has a path from the distant past to the present, through the state. The learning dynamics make that path hard to use. The cell can in principle remember something from a hundred steps ago; in practice a vanilla recurrent network reaches maybe a few dozen.</p>
<h2 id="what-it-looks-like">What it looks like</h2>
<p>Train a character-level recurrent network on the opening of <em>Alice in Wonderland</em> and the samples walk a recognizable arc. First random characters. Then a plausible character distribution: vowels and consonants alternate, spaces fall at about the right spacing. Then word-like fragments and short real words. Then text that is locally English and drifts in meaning across longer spans, because the long-range memory is exactly what the vanishing gradient denies it. The arc is the lesson. The absolute quality, at this scale and in pure Python, is modest, and I would rather say that plainly than dress it up.</p>
<p>The standard fixes are gated cells, the LSTM and friends, which add a path the gradient can travel without attenuation, and gradient clipping for the exploding side. The honest throughline is that the same Jacobian-product problem is what motivated gated recurrence and, eventually, attention, which throws out the sequential state entirely.</p>
<p>The cell&rsquo;s forward and backward-through-time code, the Jacobian argument for vanishing gradients, and the full Alice experiment are in Chapter 5 of the book, <a href="/papers/inductive-biases/"><em>Inductive Biases in Neural Networks</em></a>.</p>
<h2 id="watch-the-episode">Watch the Episode</h2>
<div class="youtube-episode" style="position:relative;width:100%;padding-bottom:56.25%;height:0;overflow:hidden;margin:1.5rem 0;">
  <iframe
    src="https://www.youtube-nocookie.com/embed/d9Adp6KktOI"
    title="Recurrence Is Weight Sharing Across Time, and It Costs You"
    style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;"
    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
    allowfullscreen
    loading="lazy"
    referrerpolicy="strict-origin-when-cross-origin"></iframe>
</div>
<p class="youtube-episode-links"><em>This post is also a video episode: <a href="https://youtu.be/d9Adp6KktOI" rel="noopener" target="_blank">Recurrence Is Weight Sharing Across Time, and It Costs You</a>, part of <a href="https://www.youtube.com/playlist?list=PLQTcXcKfBblU" rel="noopener" target="_blank">the series playlist</a>.</em></p>
]]></content:encoded>
      
      
      <category>neural-networks</category>
      
      <category>inductive-bias</category>
      
      <category>recurrent-networks</category>
      
      <category>backpropagation-through-time</category>
      
      <category>vanishing-gradients</category>
      
      
    </item>
    
    <item>
      <title>Reinforcement Learning Is Cross-Entropy, Reweighted by Reward</title>
      <link>https://metafunctor.com/post/2026-06-09-reinforcement-learning-is-cross-entropy-reweighted/</link>
      <pubDate>Tue, 09 Jun 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">https://metafunctor.com/post/2026-06-09-reinforcement-learning-is-cross-entropy-reweighted/</guid>
      <description>When the only signal is a number at the end of a trajectory. How REINFORCE turns out to be the same gradient as classification, scaled by return, and where the theory tops out.</description>
      <content:encoded><![CDATA[<p>Everything in the series so far had a label for every example. You knew the right answer and measured how far off the prediction was. Reinforcement learning changes the signal. You no longer get told the right action at each step. You act over a whole trajectory, and at the end you get a single number, the reward. All of the learning has to be backed out of that one number spread over many decisions.</p>
<p>That sounds like a different subject. It is mostly the same one, with the training signal reshaped.</p>
<h2 id="a-policy-is-a-classifier-over-actions">A policy is a classifier over actions</h2>
<p>A policy is just a softmax over what to do next, the same output head as a classifier. If you knew the correct action at each step, you would train it with cross-entropy, the $\hat{p} - y$ gradient from the first posts. You do not know the correct action. That is the entire difficulty, and REINFORCE&rsquo;s answer to it is almost cheeky: use the action you actually took as if it were the label, and weight its gradient by the return you got. Trajectories that went well push the policy toward the actions they took; trajectories that went badly push away. It is cross-entropy, reweighted by reward. The same gradient, scaled by how things turned out.</p>
<p>That reframing is why the whole toolkit carries over. The body is still an MLP. The head is still a softmax. Only the training signal changed shape, from a per-example target to a scalar over a sequence.</p>
<h2 id="credit-assignment-is-the-hard-part">Credit assignment is the hard part</h2>
<p>The reward arrives at the end, but a trajectory is a chain of decisions, and the obvious question is which of them earned the reward. REINFORCE&rsquo;s answer, scale every action in the trajectory by the total return, is unbiased and very noisy. Most of the apparatus of modern RL (baselines, value functions, advantage estimates) exists to cut that noise without biasing the estimate. The honest one-line summary of the field is: the gradient is easy, the variance is the problem.</p>
<h2 id="it-learns-from-almost-nothing">It learns from almost nothing</h2>
<p>The demonstration is REINFORCE on a five-by-five gridworld with a single sparse reward at the goal. Early on the agent wanders. By the end it takes a near-optimal path, the return climbing close to the best achievable. It is a small thing, but it learns to navigate from nothing but a number delivered at the end of each attempt, which is worth pausing on.</p>
<h2 id="where-the-theory-tops-out">Where the theory tops out</h2>
<p>The theoretical optimum has a name: AIXI. Bolt together Solomonoff induction (the optimal predictor, which is incomputable), Bayesian decision theory, and reward maximization, and you get the agent that does the best possible thing in any computable environment. It is also completely incomputable, which is the same lesson that runs under all of learning. The ideal is out of reach. Every practical method is an approximation. And the assumptions baked into the approximation, the inductive biases, are what decide what it actually learns. In RL those assumptions are the reward shaping, the policy architecture, and the exploration strategy. All priors, all bets.</p>
<p>That closes the loop the series opened with. There are three places you encode assumptions about a problem: the architecture (which functions are reachable), the output head (what the outputs mean), and the training signal (what counts as success). Supervised learning fixes the third as a per-example label. Reinforcement learning keeps the first two and rewrites the third into a reward over trajectories. The inductive-bias lens does not change. Only where you spend the bet does.</p>
<p>The REINFORCE derivation as reweighted cross-entropy, the gridworld agent, and the AIXI framing are in Chapter 8 of the book, <a href="/papers/inductive-biases/"><em>Inductive Biases in Neural Networks</em></a>.</p>
<h2 id="watch-the-episode">Watch the Episode</h2>
<div class="youtube-episode" style="position:relative;width:100%;padding-bottom:56.25%;height:0;overflow:hidden;margin:1.5rem 0;">
  <iframe
    src="https://www.youtube-nocookie.com/embed/DJNBGPVek8M"
    title="Reinforcement Learning Is Cross-Entropy, Reweighted by Reward"
    style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;"
    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
    allowfullscreen
    loading="lazy"
    referrerpolicy="strict-origin-when-cross-origin"></iframe>
</div>
<p class="youtube-episode-links"><em>This post is also a video episode: <a href="https://youtu.be/DJNBGPVek8M" rel="noopener" target="_blank">Reinforcement Learning Is Cross-Entropy, Reweighted by Reward</a>, part of <a href="https://www.youtube.com/playlist?list=PLQTcXcKfBblU" rel="noopener" target="_blank">the series playlist</a>.</em></p>
]]></content:encoded>
      
      
      <category>neural-networks</category>
      
      <category>inductive-bias</category>
      
      <category>reinforcement-learning</category>
      
      <category>policy-gradient</category>
      
      <category>aixi</category>
      
      
    </item>
    
    <item>
      <title>The Loss Function Is a Distribution Assumption</title>
      <link>https://metafunctor.com/post/2026-06-09-the-loss-is-a-distribution-assumption/</link>
      <pubDate>Tue, 09 Jun 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">https://metafunctor.com/post/2026-06-09-the-loss-is-a-distribution-assumption/</guid>
      <description>Choosing a loss is choosing a distribution for your output. Why every supervised network is a maximum-likelihood estimator, and why the gradient is always the residual.</description>
      <content:encoded><![CDATA[<p>People treat the loss function as a tuning detail, something you pick from a menu and move on. It is not a detail. Choosing a loss is choosing a probability distribution for your output. Once you see that, a lot of scattered facts collapse into one.</p>
<p>The previous post left the network producing raw scores (logits) and handed the job of interpretation to the loss. This post is about what that interpretation actually commits you to.</p>
<h2 id="every-supervised-network-is-a-maximum-likelihood-estimator">Every supervised network is a maximum-likelihood estimator</h2>
<p>The setup is always the same. The network emits logits. A link function maps them to the parameter of some distribution over the output, and the loss is the negative log likelihood of the data under that distribution. Minimizing the loss is maximizing likelihood. The loss is not arbitrary; it is whatever distribution you assumed, written as a penalty.</p>
<p>So the menu of losses is really a menu of assumptions:</p>
<ul>
<li>Binary outcome, <strong>Bernoulli</strong>: sigmoid link, binary cross-entropy.</li>
<li>One of $K$ classes, <strong>Categorical</strong>: softmax link, cross-entropy.</li>
<li>A real number, <strong>Gaussian</strong> with constant variance: identity link, mean squared error.</li>
<li>A count, <strong>Poisson</strong>: log link, Poisson negative log likelihood.</li>
</ul>
<p>Mean squared error is not &ldquo;the regression loss.&rdquo; It is the assumption that your targets are Gaussian with constant variance. If that is wrong, MSE is the wrong prior, and you pay for it.</p>
<h2 id="the-gradient-is-always-the-residual">The gradient is always the residual</h2>
<p>Here is the fact that looks like a coincidence and is not. For every pair above, the gradient of the loss with respect to the logits is</p>
$$\frac{\partial L}{\partial \mathbf{z}} = \hat{p} - y,$$<p>the predicted mean minus the target. Logistic regression, softmax, least squares, Poisson regression: the same shape every time.</p>
<p>It is not luck. These are all exponential-family distributions paired with their <em>canonical</em> link, and &ldquo;the gradient is the residual&rdquo; is the defining property of the canonical link. You pick the distribution, the matching link comes with it, and the clean gradient falls out. When you can write the backward pass once and reuse it across five losses without changing a line, this is the reason.</p>
<h2 id="why-this-is-an-inductive-bias">Why this is an inductive bias</h2>
<p>The output head is a bet about the data on the response side, the same way the architecture is a bet on the input side. Get it right and you need less data. Get it wrong and the model fights the geometry of the problem.</p>
<p>Counts are the clean example. Fit counts with MSE and the model will cheerfully predict negative rates and assume the noise is symmetric and constant, neither of which is true for a Poisson process. Use the log link and Poisson NLL and the predicted rate is positive by construction and the variance grows with the mean, because that is what counts do.</p>
<h2 id="two-heads-worth-knowing-past-the-basics">Two heads worth knowing past the basics</h2>
<p>The first is the <strong>heteroscedastic Gaussian</strong>: instead of predicting only the mean, the network predicts the mean and the variance. The output is two numbers, and the loss lets the model say how unsure it is, input by input. Uncertainty stops being a constant you assume and becomes something the model reports. I built this one in scratchnn; it is a small change to the head and a slightly longer gradient.</p>
<p>The second is the <strong>mixture density</strong> head: predict a mixture of Gaussians rather than one, for outputs that are genuinely multimodal, where the right answer is &ldquo;either roughly here or roughly there&rdquo; and the average of the two is wrong. I do not build that one in the library. I describe its shape and stop, because the point is that it is the same idea, a richer assumed distribution, and not a new kind of machinery.</p>
<p>The body of the network never changes through any of this. Only the head, and the assumption it encodes, does. That separation, the network proposes logits and the loss disposes meaning, is the organizing principle the rest of the series keeps leaning on.</p>
<p>The full catalogue, the exponential-family derivation of the canonical-link result, and the actual loss-class code (each one is three small methods) are in Chapter 2 of the book, <a href="/papers/inductive-biases/"><em>Inductive Biases in Neural Networks</em></a>.</p>
<h2 id="watch-the-episode">Watch the Episode</h2>
<div class="youtube-episode" style="position:relative;width:100%;padding-bottom:56.25%;height:0;overflow:hidden;margin:1.5rem 0;">
  <iframe
    src="https://www.youtube-nocookie.com/embed/OamSnH-Do6M"
    title="The Loss Function Is a Distribution Assumption"
    style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;"
    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
    allowfullscreen
    loading="lazy"
    referrerpolicy="strict-origin-when-cross-origin"></iframe>
</div>
<p class="youtube-episode-links"><em>This post is also a video episode: <a href="https://youtu.be/OamSnH-Do6M" rel="noopener" target="_blank">The Loss Function Is a Distribution Assumption</a>, part of <a href="https://www.youtube.com/playlist?list=PLQTcXcKfBblU" rel="noopener" target="_blank">the series playlist</a>.</em></p>
]]></content:encoded>
      
      
      <category>neural-networks</category>
      
      <category>inductive-bias</category>
      
      <category>maximum-likelihood</category>
      
      <category>generalized-linear-models</category>
      
      <category>loss-functions</category>
      
      
    </item>
    
    <item>
      <title>What a Convolution Assumes</title>
      <link>https://metafunctor.com/post/2026-06-09-what-a-convolution-assumes/</link>
      <pubDate>Tue, 09 Jun 2026 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">https://metafunctor.com/post/2026-06-09-what-a-convolution-assumes/</guid>
      <description>A convolution is a bet about images: nearby pixels matter together, and a feature detector should fire anywhere. How to test whether a model is actually using that bet.</description>
      <content:encoded><![CDATA[<p>A convolution is two assumptions made structural. <strong>Locality</strong>: a unit looks at a small patch, not the whole image. <strong>Translation equivariance</strong>: the same patch detector runs at every position, so a thing learned in one corner is known in every corner. Weight sharing is how you bake both into the architecture instead of hoping the model discovers them.</p>
<p>The previous posts treated the architecture as a choice about which functions are reachable. The convolution is the first architecture in this series that commits to a real symmetry of the data, and it is worth being precise about what that commitment is and how you check whether it is paying off.</p>
<h2 id="one-kernel-reused-everywhere">One kernel, reused everywhere</h2>
<p>A convolutional layer has one small kernel and slides it across the image, applying the same weights at every position. That is the whole definition. An ordinary linear layer gives every output its own weights tied to specific input positions; the convolution gives every position the same weights. Translating the input just slides the output. The equivariance is a property of the operator, true at initialization and after training, not something the model has to learn.</p>
<p>There is a tidy consequence in the backward pass. Because one kernel weight is read at every position in the forward pass, the gradient for that weight is a sum over every position in the backward pass. Weight sharing in the forward direction and gradient accumulation in the backward direction are the same fact seen from two sides. This <code>+=</code> over a shared axis is a motif that comes back later for recurrence (sharing across time) and attention.</p>
<p>The immediate payoff is parameters. A small CNN matches a larger MLP on a digit-classification task with a fraction of the weights, because it does not relearn &ldquo;an edge looks the same in the top-left as in the bottom-right&rdquo; separately for every location. It learns the edge once.</p>
<h2 id="how-to-tell-the-bias-is-doing-work">How to tell the bias is doing work</h2>
<p>Matching an MLP with fewer parameters is suggestive, not proof. Maybe the CNN is just getting by. The honest way to check whether the locality prior is actually load-bearing is to break it and watch what happens.</p>
<p>Take the pixels of each image and scramble them with one fixed random permutation, the same shuffle for every image, then retrain. To a human the images are now noise. To the MLP, almost nothing changes: it treated the pixels as an unordered bag of features anyway, so a fixed relabeling is invisible. To the CNN, the locality assumption is now a lie. Neighbors in the scrambled image are not neighbors in the digit, so a small kernel sees an arbitrary handful of unrelated pixels. The CNN&rsquo;s accuracy drops more than the MLP&rsquo;s.</p>
<p>That gap is the measurement. The direction (the CNN is hurt more) confirms the prior is real; the size of the gap is how much the prior was contributing. This is what it looks like to <em>measure</em> an inductive bias rather than just assert one, and it generalizes: if removing a structural assumption costs you nothing, the model was not using it.</p>
<p>One honest caveat. On the small pre-pooled digit set I use, the gap is modest, because the dataset&rsquo;s own preprocessing already did most of the spatial work. On raw pixels the gap is large. The method is the lesson; the magnitude depends on how much structure survives into your inputs.</p>
<p>The actual <code>Conv2D</code> forward and backward (the <code>+=</code> accumulation written out), the parameter-count comparison, and the full permuted-pixel experiment are in Chapter 3 of the book, <a href="/papers/inductive-biases/"><em>Inductive Biases in Neural Networks</em></a>.</p>
<h2 id="watch-the-episode">Watch the Episode</h2>
<div class="youtube-episode" style="position:relative;width:100%;padding-bottom:56.25%;height:0;overflow:hidden;margin:1.5rem 0;">
  <iframe
    src="https://www.youtube-nocookie.com/embed/4FrVL9lOPh8"
    title="What a Convolution Assumes"
    style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;"
    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
    allowfullscreen
    loading="lazy"
    referrerpolicy="strict-origin-when-cross-origin"></iframe>
</div>
<p class="youtube-episode-links"><em>This post is also a video episode: <a href="https://youtu.be/4FrVL9lOPh8" rel="noopener" target="_blank">What a Convolution Assumes</a>, part of <a href="https://www.youtube.com/playlist?list=PLQTcXcKfBblU" rel="noopener" target="_blank">the series playlist</a>.</em></p>
]]></content:encoded>
      
      
      <category>neural-networks</category>
      
      <category>inductive-bias</category>
      
      <category>convolutional-networks</category>
      
      <category>translation-equivariance</category>
      
      <category>weight-sharing</category>
      
      
    </item>
    
    <item>
      <title>The Architecture Is the Prior</title>
      <link>https://metafunctor.com/post/2026-03-15-rl-architecture/</link>
      <pubDate>Sun, 15 Mar 2026 00:02:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">https://metafunctor.com/post/2026-03-15-rl-architecture/</guid>
      <description>Part 3 of What Your RL Algorithm Actually Assumes — the architecture decides what kind of features can be learned, and that decision is a Bayesian prior over value functions.</description>
      <content:encoded><![CDATA[<p>The last post showed that hand-picked features can turn a hopeless tabular agent into one that generalizes across an entire grid after a few thousand episodes. Distance to the nearest item. Normalized coordinates. A handful of scalars, chosen by someone who&rsquo;d looked at the problem and thought hard about what mattered. That works &mdash; if you pick the right ones. If you know the domain. If the relevant structure is the kind of thing a human can articulate.</p>
<p>But what if you don&rsquo;t? What if the problem is complex enough that you can&rsquo;t say in advance which combinations of raw inputs will matter? What if the right features aren&rsquo;t &ldquo;distance to item&rdquo; but something subtle &mdash; a pattern in how walls cluster, a spatial relationship between the exit and the player&rsquo;s trajectory, a nonlinear interaction the linear agent can&rsquo;t see by construction? Neural networks learn their own features. But they don&rsquo;t learn from nothing. Before a single gradient step is taken, you&rsquo;ve already made a decision that shapes everything that follows: you chose the architecture. And the architecture decides what <em>kind</em> of features can be learned at all.</p>
<h2 id="neural-function-approximation">Neural function approximation</h2>
<p>A neural network is just a parameterized function. It takes the raw state representation as input &mdash; the same feature vector the linear agent uses &mdash; and outputs Q-values for each action. The difference is what happens in between. Where the linear agent computes a weighted sum of its inputs directly, a neural network passes the inputs through one or more hidden layers first. Each hidden layer applies a linear transformation followed by a nonlinearity, producing an intermediate representation. The output layer then reads off Q-values from that representation.</p>
<p>The hidden layer is where the magic happens, and it&rsquo;s worth being precise about what &ldquo;magic&rdquo; means here. Each hidden unit computes a nonlinear function of the inputs. During training, the weights to each unit are adjusted so that the unit&rsquo;s activation becomes useful for predicting value. Units don&rsquo;t learn features we hand-specified &mdash; they learn features that minimize prediction error. They discover what to pay attention to. That&rsquo;s powerful when the right features aren&rsquo;t obvious, and it&rsquo;s also the source of the architecture&rsquo;s assumptions, as we&rsquo;ll see.</p>
<p>This learned generalization comes at a cost. The network needs experience to discover useful features. Early in training, its hidden units are computing essentially random functions of the input &mdash; nothing meaningful. Only after enough gradient updates do the features crystallize into something useful. The linear agent, given good hand-crafted features, starts generalizing from episode one. The neural agent has to earn its generalization from scratch. This is the sample efficiency trade-off at the heart of representation learning.</p>
<h2 id="the-mlp-in-this-demo">The MLP in this demo</h2>
<p>The neural agent running in this post uses a two-layer MLP: the simplest possible deep network. The input is the same feature vector used by the linear agent. The first layer (the hidden layer) multiplies the input by a weight matrix, adds a bias, and applies ReLU &mdash; zeroing out any negative values, keeping the rest. The second layer reads the hidden activations and produces one Q-value per action. Nothing else. No convolutions, no attention, no recurrence.</p>
<p>The hidden layer has 20 neurons. Each neuron learns one feature &mdash; some nonlinear combination of the inputs that ends up being useful for predicting value. With 20 learned features feeding into 4 action outputs, the network has roughly 200 parameters in total. That&rsquo;s still a tiny model. But those 200 parameters are doing something qualitatively different from the linear agent&rsquo;s handful of weights: they&rsquo;re discovering, through gradient descent, which nonlinear patterns in the state predict future reward. The features aren&rsquo;t fixed; they&rsquo;re learned. The architecture just determines the space of features that can be learned &mdash; which brings us to the real point.</p>
<h2 id="three-agents-one-maze">Three agents, one maze</h2>
<p>Watch the three agents train side-by-side on the same maze. The tabular agent memorizes individual cells &mdash; watch how its value heatmap fills in slowly, state by state, with no structure to it. The linear agent generalizes across distance features &mdash; its heatmap has smooth gradients from the moment it starts updating, because updating the weight for &ldquo;distance to item&rdquo; immediately affects every state. The neural agent learns spatial patterns the others can&rsquo;t see. Use sparse walls to give the neural agent space to show its advantage.</p>
<link rel="stylesheet" href="/css/agentum-widgets.css"><div id="agentum-compare-a0f3a074"></div><script type="module">
import { createCompareWidget } from '../../js/agentum/widgets.js';
import { TabularQ } from '../../js/agentum/algorithms/tabular-q.js';
import { LinearQ } from '../../js/agentum/algorithms/linear-q.js';
import { NeuralQ } from '../../js/agentum/algorithms/neural-q.js';
createCompareWidget(document.getElementById('agentum-compare-a0f3a074'), {
    agents: [
        { name: 'Tabular Q', createAgent: () => new TabularQ() },
        { name: 'Linear Q', createAgent: () => new LinearQ() },
        { name: 'Neural Q', createAgent: () => new NeuralQ({ hiddenDim: 20 }) },
    ],
    gridSize: 10, nItems: 3, wallDensity: 'sparse', seed: 42
});
</script>
<p>The key difference shows up in generalization. After a few hundred episodes, restart the agents and change the grid size. The tabular agent forgets everything &mdash; a new grid is an entirely new problem. The linear agent retains its weights and keeps generalizing from its features, which are grid-size independent. The neural agent also retains its weights, and may even have learned something about the spatial structure of the problem that transfers.</p>
<h2 id="train-once-play-anywhere">Train once, play anywhere</h2>
<p>Here&rsquo;s a concrete way to probe what each agent actually learned. Hit <strong>Play</strong> to train all three agents for a few hundred episodes on the current layout. Then hit <strong>New Layout</strong> to regenerate the grid with a completely different arrangement of walls, items, and the exit &mdash; the agents keep their trained weights but face a maze they&rsquo;ve never seen. Now hit <strong>Watch</strong> to see each agent navigate the new layout using pure exploitation: no random exploration, just the policy they learned.</p>
<p>What you&rsquo;ll see is the generalization gap made visible. The tabular agent will likely stumble &mdash; the new positions don&rsquo;t appear in its Q-table, so it has no learned values for the states it actually encounters, and falls back to arbitrary choices. The linear agent will often transfer reasonably well, because its features (&ldquo;distance to nearest item,&rdquo; &ldquo;normalized x position&rdquo;) are layout-independent: the weights it learned about what those features predict still apply on the new grid. The neural agent sits somewhere in between &mdash; it has learned nonlinear combinations of those same features, and some of those combinations will transfer while others won&rsquo;t, depending on how well its hidden-layer representations happened to capture genuinely layout-invariant structure.</p>
<p>This is what sample efficiency looks like in practice. The tabular agent memorized a map; New Layout throws that map away. The linear agent learned principles; New Layout tests whether those principles generalize. The neural agent learned something in between &mdash; richer than explicit principles, but not as brittle as memorization. How well it transfers depends entirely on which features the hidden layer discovered, which in turn depends on the architecture&rsquo;s inductive bias. The architecture determined what <em>could</em> be learned, and training determined what <em>was</em>.</p>
<h2 id="architecture--prior--inductive-bias">Architecture = Prior = Inductive Bias</h2>
<p>Here is the claim, stated plainly: <strong>the architecture is the Bayesian prior over value functions.</strong> Not a metaphor. Not an analogy. When you choose a two-layer MLP, you are saying &ldquo;I believe the value function can be expressed as smooth, nonlinear combinations of the input features.&rdquo; You are ruling out value functions that can&rsquo;t be expressed that way. Every architectural decision you make narrows the hypothesis space &mdash; the set of value functions the agent can even represent &mdash; and that narrowing is exactly what a prior does.</p>
<p>Consider what different architectures are saying. A fully-connected MLP says &ldquo;I believe any input feature could interact with any other input feature in determining value &mdash; and those interactions might be nonlinear.&rdquo; A CNN says &ldquo;I believe spatial locality matters &mdash; nearby cells have related values, and the same patterns can appear anywhere on the grid.&rdquo; A transformer says &ldquo;I believe attention patterns matter &mdash; some subset of the input should attend to some other subset, and that relationship is dynamic.&rdquo; A recurrent network says &ldquo;I believe the history of states matters, not just the current state.&rdquo; These aren&rsquo;t just implementation choices. They&rsquo;re commitments. They&rsquo;re bets about the structure of the problem.</p>
<p>In Bayesian terms: you choose a prior, then learning updates it. An algorithm with a strong, well-matched prior can learn from very little data. An algorithm with a weak or mismatched prior needs to see much more. Tabular Q-learning has an almost degenerate prior &mdash; it says &ldquo;every state is completely independent of every other state,&rdquo; which is a very strong prior in the other direction: a prior that prevents any generalization at all. Linear approximation has a prior that says &ldquo;value is a linear function of these specific features.&rdquo; The MLP prior is weaker &mdash; it allows nonlinear combinations &mdash; but still constrains the hypothesis space in ways that make learning possible. The Solomonoff prior would place weight on every computable value function, weighted by simplicity. That&rsquo;s the ideal no practical system can compute. We&rsquo;re all approximating it with our architectural choices.</p>
<h2 id="what-the-net-cant-learn">What the net can&rsquo;t learn</h2>
<p>Our tiny MLP can discover nonlinear patterns the linear agent can&rsquo;t represent. Given enough training data, it can learn that a particular configuration of walls and items near the player&rsquo;s position calls for a specific action, even if that pattern can&rsquo;t be expressed as a linear combination of distances. That&rsquo;s real. But the MLP is also blind in a way a convolutional network wouldn&rsquo;t be. Our MLP treats position <code>(3, 4)</code> and position <code>(3, 5)</code> as equally unrelated dimensions. It doesn&rsquo;t know they&rsquo;re adjacent. It doesn&rsquo;t know that adjacent cells tend to have similar values. It can <em>learn</em> that they do &mdash; but only if it sees enough data from both positions to draw the connection. A CNN would bake that assumption in from the start: spatial locality is the prior, not something to be learned.</p>
<p>On a structured maze, a CNN-like architecture would notice that the grid has spatial regularity &mdash; that the value of a cell is related to the values of its neighbors, that walls form connected structures, that the distance to the exit changes smoothly across the grid. Our MLP can model any of this in principle, but it has to spend parameters and training data discovering regularities that a CNN gets for free from its architecture. The inductive bias of the CNN matches the structure of the problem. The MLP&rsquo;s inductive bias is weaker. This is the no-free-lunch theorem made concrete: a weaker prior requires more data, and a prior that matches the problem&rsquo;s structure is worth more than any algorithm improvement.</p>
<h2 id="what-comes-next">What comes next</h2>
<p>Three posts in, the pattern is clear. Tabular: no assumptions about state similarity, and you pay for it in sample complexity. Linear: explicit assumptions baked into hand-crafted features, efficient when you&rsquo;re right and limited when you&rsquo;re wrong. Neural: assumptions baked into architecture, flexible within that architecture but blind outside it. Every algorithm is making bets. Every representation is a prior. The final post pulls this thread to its end: what would it look like to make the <em>ideal</em> assumptions &mdash; and what does that tell us about everything we&rsquo;ve been building? <a href="/post/2026-03-15-rl-synthesis/">Next: What You Assume vs. What You Compute.</a></p>
]]></content:encoded>
      
      
      <category>reinforcement-learning</category>
      
      <category>neural-networks</category>
      
      <category>inductive-bias</category>
      
      <category>machine-learning</category>
      
      
    </item>
    
    <item>
      <title>Deep Learning</title>
      <link>https://metafunctor.com/media/deep-learning/</link>
      <pubDate>Wed, 17 Dec 2025 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">https://metafunctor.com/media/deep-learning/</guid>
      <description></description>
      <content:encoded><![CDATA[]]></content:encoded>
      
      
      <category>deep-learning</category>
      
      <category>neural-networks</category>
      
      <category>textbook</category>
      
      
    </item>
    
    <item>
      <title>Deep Learning with Python</title>
      <link>https://metafunctor.com/media/deep-learning-with-python/</link>
      <pubDate>Wed, 17 Dec 2025 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">https://metafunctor.com/media/deep-learning-with-python/</guid>
      <description></description>
      <content:encoded><![CDATA[]]></content:encoded>
      
      
      <category>deep-learning</category>
      
      <category>keras</category>
      
      <category>neural-networks</category>
      
      <category>practical</category>
      
      
    </item>
    
    <item>
      <title>Deep Learning: An Introduction for Applied Mathematicians</title>
      <link>https://metafunctor.com/media/deep-learning-applied-math/</link>
      <pubDate>Wed, 17 Dec 2025 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">https://metafunctor.com/media/deep-learning-applied-math/</guid>
      <description>Notes Perspective of deep learning from applied math. Bridges math with neural nets.
</description>
      <content:encoded><![CDATA[<h2 id="notes">Notes</h2>
<p>Perspective of deep learning from applied math. Bridges math with neural nets.</p>
]]></content:encoded>
      
      
      <category>deep-learning</category>
      
      <category>applied-math</category>
      
      <category>neural-networks</category>
      
      
    </item>
    
    <item>
      <title>Dive into Deep Learning</title>
      <link>https://metafunctor.com/media/dive-into-deep-learning/</link>
      <pubDate>Wed, 17 Dec 2025 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">https://metafunctor.com/media/dive-into-deep-learning/</guid>
      <description>Notes Free open-source deep learning book with code and math integrated. Interactive deep learning resource with runnable code.
</description>
      <content:encoded><![CDATA[<h2 id="notes">Notes</h2>
<p>Free open-source deep learning book with code and math integrated. Interactive deep learning resource with runnable code.</p>
]]></content:encoded>
      
      
      <category>deep-learning</category>
      
      <category>neural-networks</category>
      
      <category>free-textbook</category>
      
      <category>interactive</category>
      
      
    </item>
    
    <item>
      <title>Learning Fuzzy Logic: Automatic Rule Discovery Through Differentiable Circuits</title>
      <link>https://metafunctor.com/post/2025-10-07-fuzzy-circuit/</link>
      <pubDate>Tue, 07 Oct 2025 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">https://metafunctor.com/post/2025-10-07-fuzzy-circuit/</guid>
      <description>Learning fuzzy membership functions and inference rules automatically through gradient descent on soft circuits, instead of hand-crafting them.</description>
      <content:encoded><![CDATA[<p>Fuzzy logic is good for reasoning under uncertainty, but it has a bottleneck: you need domain experts to define the rules.</p>
<p>What if fuzzy systems could learn their own rules from data?</p>
<h2 id="the-traditional-fuzzy-logic-bottleneck">The Traditional Fuzzy Logic Bottleneck</h2>
<p>Classic fuzzy systems require:</p>
<ol>
<li><strong>Membership functions</strong>: &ldquo;How hot is hot?&rdquo;</li>
<li><strong>Inference rules</strong>: &ldquo;If temp is hot AND humidity is high THEN&hellip;&rdquo;</li>
<li><strong>Defuzzification</strong>: Converting fuzzy outputs to crisp values</li>
</ol>
<p>This means:</p>
<ul>
<li>Domain expertise (expensive)</li>
<li>Trial and error (time-consuming)</li>
<li>Manual tuning (brittle)</li>
</ul>
<p>In practice, fuzzy logic is often abandoned in favor of neural networks. You lose interpretability, but at least you don&rsquo;t need a domain expert hand-crafting rules.</p>
<h2 id="the-idea-fuzzy-soft-circuits">The Idea: Fuzzy Soft Circuits</h2>
<p>We present a framework that:</p>
<ul>
<li>Represents fuzzy systems as <strong>differentiable computational graphs</strong></li>
<li><strong>Learns</strong> membership functions and rules via <strong>gradient descent</strong></li>
<li>Keeps the <strong>interpretability</strong> of traditional fuzzy systems</li>
</ul>
<h3 id="key-innovation-soft-gates">Key Innovation: Soft Gates</h3>
<p>Traditional circuits use hard logic gates (AND, OR, NOT). We use soft, differentiable approximations:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="c1"># Traditional (non-differentiable)</span>
</span></span><span class="line"><span class="cl"><span class="n">AND</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">)</span> <span class="o">=</span> <span class="nb">min</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="n">OR</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">)</span> <span class="o">=</span> <span class="nb">max</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Soft (differentiable)</span>
</span></span><span class="line"><span class="cl"><span class="n">soft_AND</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">)</span> <span class="o">=</span> <span class="n">a</span> <span class="o">*</span> <span class="n">b</span>
</span></span><span class="line"><span class="cl"><span class="n">soft_OR</span><span class="p">(</span><span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">)</span> <span class="o">=</span> <span class="n">a</span> <span class="o">+</span> <span class="n">b</span> <span class="o">-</span> <span class="n">a</span><span class="o">*</span><span class="n">b</span>
</span></span><span class="line"><span class="cl"><span class="n">soft_NOT</span><span class="p">(</span><span class="n">a</span><span class="p">)</span> <span class="o">=</span> <span class="mi">1</span> <span class="o">-</span> <span class="n">a</span>
</span></span></code></pre></div><p>These are differentiable but approximate the same semantics. That means backpropagation works.</p>
<h2 id="the-architecture">The Architecture</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">Input Features
</span></span><span class="line"><span class="cl">     |
</span></span><span class="line"><span class="cl">Fuzzification Layer (learnable membership functions)
</span></span><span class="line"><span class="cl">     |
</span></span><span class="line"><span class="cl">Soft Circuit Layer (learnable fuzzy rules)
</span></span><span class="line"><span class="cl">     |
</span></span><span class="line"><span class="cl">Aggregation Layer (learnable combination)
</span></span><span class="line"><span class="cl">     |
</span></span><span class="line"><span class="cl">Defuzzification Layer
</span></span><span class="line"><span class="cl">     |
</span></span><span class="line"><span class="cl">Output
</span></span></code></pre></div><p>Every component is differentiable. Train end-to-end with backpropagation.</p>
<h2 id="automatic-rule-discovery">Automatic Rule Discovery</h2>
<p>The system discovers rules like:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">IF temperature is {learned_high} AND humidity is {learned_humid}
</span></span><span class="line"><span class="cl">THEN discomfort is {learned_uncomfortable}
</span></span></code></pre></div><p>Where the membership functions <code>{learned_high}</code>, <code>{learned_humid}</code>, etc. are learned from data, not hand-crafted.</p>
<h2 id="why-not-just-use-a-neural-network">Why Not Just Use a Neural Network?</h2>
<p>Fair question. Fuzzy soft circuits give you things neural networks don&rsquo;t:</p>
<ul>
<li><strong>Interpretability</strong>: You can extract and read the learned rules</li>
<li><strong>Sample efficiency</strong>: The structured inductive bias helps with limited data</li>
<li><strong>Domain integration</strong>: You can incorporate expert knowledge as priors</li>
<li><strong>Uncertainty quantification</strong>: Fuzzy truth values are meaningful</li>
</ul>
<p>Neural networks give you a black box. You need large datasets. Incorporating domain knowledge is hard. Uncertainty requires special techniques.</p>
<p>If you need both learning and interpretability, fuzzy soft circuits sit in a useful spot.</p>
<h2 id="training-process">Training Process</h2>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-python" data-lang="python"><span class="line"><span class="cl"><span class="c1"># Initialize random fuzzy circuit</span>
</span></span><span class="line"><span class="cl"><span class="n">circuit</span> <span class="o">=</span> <span class="n">FuzzySoftCircuit</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">    <span class="n">n_inputs</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">n_rules</span><span class="o">=</span><span class="mi">10</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">n_outputs</span><span class="o">=</span><span class="mi">1</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Train with gradient descent</span>
</span></span><span class="line"><span class="cl"><span class="k">for</span> <span class="n">epoch</span> <span class="ow">in</span> <span class="n">epochs</span><span class="p">:</span>
</span></span><span class="line"><span class="cl">    <span class="c1"># Forward pass</span>
</span></span><span class="line"><span class="cl">    <span class="n">predictions</span> <span class="o">=</span> <span class="n">circuit</span><span class="p">(</span><span class="n">inputs</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="c1"># Compute loss</span>
</span></span><span class="line"><span class="cl">    <span class="n">loss</span> <span class="o">=</span> <span class="n">mse</span><span class="p">(</span><span class="n">predictions</span><span class="p">,</span> <span class="n">targets</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="c1"># Backward pass (automatic differentiation)</span>
</span></span><span class="line"><span class="cl">    <span class="n">loss</span><span class="o">.</span><span class="n">backward</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="c1"># Update membership functions and rules</span>
</span></span><span class="line"><span class="cl">    <span class="n">optimizer</span><span class="o">.</span><span class="n">step</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1"># Extract learned rules</span>
</span></span><span class="line"><span class="cl"><span class="n">rules</span> <span class="o">=</span> <span class="n">circuit</span><span class="o">.</span><span class="n">extract_rules</span><span class="p">()</span>
</span></span><span class="line"><span class="cl"><span class="nb">print</span><span class="p">(</span><span class="n">rules</span><span class="p">)</span>  <span class="c1"># Human-readable fuzzy rules!</span>
</span></span></code></pre></div><h2 id="experimental-results">Experimental Results</h2>
<p>On benchmark datasets:</p>
<ul>
<li><strong>HVAC control</strong>: 15% energy reduction vs. hand-crafted rules</li>
<li><strong>Medical diagnosis</strong>: 92% accuracy with only 500 training examples</li>
<li><strong>Industrial control</strong>: Matched expert-designed systems after 1 hour of training</li>
</ul>
<h2 id="rule-visualization">Rule Visualization</h2>
<p>The learned membership functions can be plotted:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">Temperature:
</span></span><span class="line"><span class="cl">  Cold:   [0C --________-- 15C ..................... 40C]
</span></span><span class="line"><span class="cl">  Warm:   [0C ........ 15C --________-- 25C ........ 40C]
</span></span><span class="line"><span class="cl">  Hot:    [0C ........................ 25C --________-- 40C]
</span></span></code></pre></div><p>You can see and understand what the system learned. That&rsquo;s the whole point.</p>
<h2 id="applications">Applications</h2>
<p>This framework fits anywhere you need both learning and interpretability:</p>
<ul>
<li><strong>Control systems</strong> (HVAC, industrial automation)</li>
<li><strong>Medical diagnosis</strong> (interpretable predictions)</li>
<li><strong>Financial modeling</strong> (explainable risk assessment)</li>
<li><strong>Robotics</strong> (learning from demonstration with transparency)</li>
</ul>
<h2 id="future-directions">Future Directions</h2>
<ul>
<li>Multi-objective optimization (accuracy + interpretability + sparsity)</li>
<li>Incorporating temporal/sequential fuzzy logic</li>
<li>Transfer learning between fuzzy systems</li>
<li>Formal verification of learned rules</li>
</ul>
<h2 id="read-the-full-paper">Read the Full Paper</h2>
<p>For mathematical foundations, training algorithms, and comprehensive experiments:</p>
<blockquote>
<p><a href="/latex/fuzzy-circuit/">View Paper</a></p>
</blockquote>
<p>Contents:</p>
<ul>
<li>Soft gate definitions and properties</li>
<li>Gradient flow analysis</li>
<li>Training algorithms and optimization techniques</li>
<li>Benchmarks on 10+ datasets</li>
<li>Comparison with neural networks and hand-crafted fuzzy systems</li>
<li>Rule extraction and interpretation methods</li>
<li>Ablation studies on circuit architecture</li>
</ul>
]]></content:encoded>
      
      
      <category>fuzzy-logic</category>
      
      <category>machine-learning</category>
      
      <category>differentiable-programming</category>
      
      <category>soft-circuits</category>
      
      <category>neural-networks</category>
      
      
    </item>
    
    <item>
      <title>Neural Language Models: From RNNs to Transformers</title>
      <link>https://metafunctor.com/post/2024-09-20-neural-language-models-transformers/</link>
      <pubDate>Fri, 20 Sep 2024 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">https://metafunctor.com/post/2024-09-20-neural-language-models-transformers/</guid>
      <description>The evolution of neural sequence prediction, and how it connects to classical methods</description>
      <content:encoded><![CDATA[<p>GPT, Claude, LLaMA: these systems achieve remarkable performance on tasks that seemed impossible a decade ago. But they&rsquo;re still doing the same thing every language model does, just with a lot more parameters. In this post I trace the architectural evolution from feedforward networks to transformers, connecting modern LLMs back to the information-theoretic foundations from earlier in this series.</p>
<h2 id="the-core-task">The Core Task</h2>
<p>Every neural language model solves the same fundamental problem:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">Given x_1, x_2, ..., x_{n-1}, estimate P(x_n | x_1, ..., x_{n-1})
</span></span></code></pre></div><p>This is exactly the sequential prediction problem from <a href="/post/2010-06-01-intro-sequential-prediction/">our first post</a>. The difference is how neural models parameterize the conditional distribution.</p>
<h2 id="evolution-of-architectures">Evolution of Architectures</h2>
<h3 id="feedforward-neural-lms-bengio-et-al-2003">Feedforward Neural LMs (Bengio et al., 2003)</h3>
<p>The first neural language models used feedforward networks:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">Input: one-hot encodings of last N words
</span></span><span class="line"><span class="cl">     |
</span></span><span class="line"><span class="cl">Embedding layer: learn dense representations
</span></span><span class="line"><span class="cl">     |
</span></span><span class="line"><span class="cl">Hidden layers: nonlinear transformations
</span></span><span class="line"><span class="cl">     |
</span></span><span class="line"><span class="cl">Output: softmax over vocabulary
</span></span></code></pre></div><p>This is essentially a neural n-gram. Fixed context window, but with learned representations that allow similar contexts to share information.</p>
<p><strong>Key innovation</strong>: Word embeddings. Instead of treating each word as an atomic symbol, learn a dense vector where similar words have similar representations.</p>
<h3 id="recurrent-neural-networks-rnns">Recurrent Neural Networks (RNNs)</h3>
<p>RNNs process sequences with a hidden state that evolves over time:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">h_t = f(W_h * h_{t-1} + W_x * x_t)
</span></span><span class="line"><span class="cl">y_t = softmax(W_y * h_t)
</span></span></code></pre></div><p>The hidden state h_t theoretically carries information from the entire history, not just the last N words.</p>
<p><strong>Problems</strong>:</p>
<ul>
<li><strong>Vanishing gradients</strong>: Gradients shrink exponentially, limiting effective memory</li>
<li><strong>Sequential computation</strong>: Can&rsquo;t parallelize, each step depends on the previous</li>
<li><strong>Practical context</strong>: Only about 10-20 tokens effectively remembered</li>
</ul>
<h3 id="long-short-term-memory-lstm">Long Short-Term Memory (LSTM)</h3>
<p>LSTMs add <strong>gating mechanisms</strong> to control information flow:</p>
<ul>
<li><strong>Forget gate</strong>: What to discard from cell state</li>
<li><strong>Input gate</strong>: What new information to add</li>
<li><strong>Output gate</strong>: What to expose to the next layer</li>
</ul>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">f_t = sigma(W_f . [h_{t-1}, x_t] + b_f)     # forget gate
</span></span><span class="line"><span class="cl">i_t = sigma(W_i . [h_{t-1}, x_t] + b_i)     # input gate
</span></span><span class="line"><span class="cl">c~_t = tanh(W_c . [h_{t-1}, x_t] + b_c)     # candidate cell state
</span></span><span class="line"><span class="cl">c_t = f_t * c_{t-1} + i_t * c~_t             # new cell state
</span></span><span class="line"><span class="cl">o_t = sigma(W_o . [h_{t-1}, x_t] + b_o)     # output gate
</span></span><span class="line"><span class="cl">h_t = o_t * tanh(c_t)                        # hidden state
</span></span></code></pre></div><p>LSTMs capture longer dependencies than vanilla RNNs but still struggle beyond roughly 100 tokens.</p>
<h3 id="transformers-vaswani-et-al-2017">Transformers (Vaswani et al., 2017)</h3>
<p>The transformer architecture abandoned recurrence entirely:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">Attention(Q, K, V) = softmax(QK^T / sqrt(d_k)) V
</span></span></code></pre></div><p><strong>Key innovations</strong>:</p>
<ol>
<li>
<p><strong>Self-attention</strong>: Every position attends to every other position directly. No sequential bottleneck.</p>
</li>
<li>
<p><strong>Parallelization</strong>: All positions computed simultaneously during training. Massive speedups on GPUs.</p>
</li>
<li>
<p><strong>Positional encoding</strong>: Since there&rsquo;s no recurrence, position information must be explicitly injected.</p>
</li>
</ol>
<p>The attention mechanism computes: &ldquo;For each position, how relevant is every other position?&rdquo; This allows direct information flow across arbitrary distances.</p>
<p><strong>Complexity trade-off</strong>: O(n^2) computation and memory for sequence length n. Long contexts are expensive.</p>
<h2 id="modern-large-language-models">Modern Large Language Models</h2>
<h3 id="scale">Scale</h3>
<p>The scaling has been remarkable:</p>
<table>
	<thead>
			<tr>
					<th>Model</th>
					<th>Parameters</th>
					<th>Training Data</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>GPT-2 (2019)</td>
					<td>1.5B</td>
					<td>40GB text</td>
			</tr>
			<tr>
					<td>GPT-3 (2020)</td>
					<td>175B</td>
					<td>570GB text</td>
			</tr>
			<tr>
					<td>GPT-4 (2023)</td>
					<td>~1T?</td>
					<td>Unknown</td>
			</tr>
			<tr>
					<td>LLaMA (2023)</td>
					<td>7B-70B</td>
					<td>1-1.4T tokens</td>
			</tr>
			<tr>
					<td>Claude 3 (2024)</td>
					<td>Unknown</td>
					<td>Unknown</td>
			</tr>
	</tbody>
</table>
<p>Each generation scales parameters, data, and compute roughly together.</p>
<h3 id="training-paradigm">Training Paradigm</h3>
<p>Modern LLM training has three phases:</p>
<ol>
<li>
<p><strong>Pretraining</strong>: Predict next token on massive text corpora (books, web, code). Learn general language understanding.</p>
</li>
<li>
<p><strong>Supervised Fine-tuning (SFT)</strong>: Train on curated examples of helpful assistant behavior.</p>
</li>
<li>
<p><strong>RLHF</strong>: Reinforcement Learning from Human Feedback. Train a reward model from human preferences, then optimize the LLM against this reward.</p>
</li>
</ol>
<p>The result: models that are not just good predictors, but helpful assistants.</p>
<h3 id="emergent-capabilities">Emergent Capabilities</h3>
<p>Scale brings emergent abilities:</p>
<ul>
<li><strong>In-context learning</strong>: Learn from examples in the prompt without weight updates</li>
<li><strong>Chain-of-thought reasoning</strong>: Produce intermediate steps that improve final answers</li>
<li><strong>Instruction following</strong>: Generalize from instructions, not just examples</li>
<li><strong>Code generation</strong>: Write and debug programs</li>
</ul>
<p>These weren&rsquo;t explicitly trained for. They emerged from scale.</p>
<h2 id="connection-to-classical-methods">Connection to Classical Methods</h2>
<p>Despite their complexity, neural LMs are still doing sequential prediction:</p>
<h3 id="the-compression-connection">The Compression Connection</h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">Cross-entropy loss = -Sum log P(x_n | x_{&lt;n})
</span></span></code></pre></div><p>This is identical to the code length in arithmetic coding. Neural LMs are implicitly learning to compress.</p>
<p>State-of-the-art LLMs achieve roughly 1.5 bits per character on English text, approaching the estimated entropy of English.</p>
<h3 id="what-neural-models-learn">What Neural Models Learn</h3>
<p>Neural LMs implicitly learn:</p>
<ul>
<li><strong>Syntax and grammar</strong> (structure)</li>
<li><strong>Semantic relationships</strong> (meaning)</li>
<li><strong>World knowledge</strong> (facts)</li>
<li><strong>Reasoning patterns</strong> (debated)</li>
</ul>
<p>They can be seen as learning a very flexible, high-capacity approximation to what CTW does with a restricted model class.</p>
<h3 id="the-inductive-bias-trade-off">The Inductive Bias Trade-off</h3>
<table>
	<thead>
			<tr>
					<th>Method</th>
					<th>Inductive Bias</th>
					<th>Data Needed</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>CTW</td>
					<td>Tree structures</td>
					<td>Thousands</td>
			</tr>
			<tr>
					<td>N-grams</td>
					<td>Fixed context</td>
					<td>Millions</td>
			</tr>
			<tr>
					<td>Neural LMs</td>
					<td>Minimal</td>
					<td>Billions</td>
			</tr>
	</tbody>
</table>
<p>Neural models trade inductive bias for flexibility. With enough data, this wins. With limited data, CTW wins.</p>
<h2 id="limitations-of-neural-lms">Limitations of Neural LMs</h2>
<p>Modern LLMs are remarkable, but the limitations are real:</p>
<h3 id="hallucination">Hallucination</h3>
<p>LLMs generate plausible but false content. They model the <em>form</em> of text perfectly but have no grounding in truth.</p>
<h3 id="no-uncertainty-quantification">No Uncertainty Quantification</h3>
<p>Unlike Bayesian methods (CTW), neural LMs produce point estimates. They&rsquo;re confidently wrong with no warning.</p>
<h3 id="black-box">Black Box</h3>
<p>We don&rsquo;t understand how they work internally. Mechanistic interpretability is an active research area but we&rsquo;re far from complete understanding.</p>
<h3 id="compute-cost">Compute Cost</h3>
<p>Training GPT-4 reportedly cost over $100M. Inference at scale requires significant GPU resources.</p>
<h3 id="alignment-challenges">Alignment Challenges</h3>
<p>Making LLMs reliably helpful, harmless, and honest is an unsolved problem. RLHF helps but doesn&rsquo;t guarantee safety.</p>
<h2 id="when-to-use-neural-lms-vs-ctw">When to Use Neural LMs vs. CTW</h2>
<h3 id="use-neural-lms-when">Use Neural LMs When:</h3>
<ul>
<li>Large vocabulary (natural language, code)</li>
<li>Massive training data available</li>
<li>Long-range dependencies matter</li>
<li>State-of-the-art performance required</li>
<li>Can afford compute costs</li>
</ul>
<h3 id="use-ctw-when">Use CTW When:</h3>
<ul>
<li>Binary or small alphabet</li>
<li>Limited training data</li>
<li>Need theoretical guarantees</li>
<li>Interpretability required</li>
<li>Compute constrained</li>
<li>The source is plausibly tree-structured</li>
</ul>
<h2 id="hybrid-approaches">Hybrid Approaches</h2>
<p>The future may combine strengths:</p>
<ol>
<li>
<p><strong>CTW for low-resource adaptation</strong>: Use CTW-style Bayesian methods when fine-tuning data is limited.</p>
</li>
<li>
<p><strong>Compression-based model selection</strong>: Use description length (related to CTW) to choose among neural models.</p>
</li>
<li>
<p><strong>Bayesian neural networks</strong>: Add uncertainty estimation to neural predictions.</p>
</li>
<li>
<p><strong>Neural architecture search</strong>: Learn architectures with appropriate inductive biases.</p>
</li>
</ol>
<h2 id="key-takeaways">Key Takeaways</h2>
<ul>
<li>Neural LMs evolved from feedforward to RNN to LSTM to Transformer</li>
<li><strong>Self-attention</strong> enables direct long-range dependencies</li>
<li><strong>Scale</strong> (parameters, data, compute) drives emergent capabilities</li>
<li>Neural LMs are still doing <strong>sequential prediction</strong>, just with massive flexibility</li>
<li><strong>Trade-off</strong>: Flexibility vs. sample efficiency</li>
<li>CTW and neural LMs serve different regimes</li>
</ul>
<h2 id="whats-next">What&rsquo;s Next</h2>
<p>The <a href="/post/2025-01-15-ctw-experimental-results/">final post</a> presents experimental results validating CTW&rsquo;s theoretical properties. We&rsquo;ll see that when depth matches the true Markov order, CTW achieves the Bayes-optimal accuracy, confirming theory with practice.</p>
<h2 id="further-reading">Further Reading</h2>
<ul>
<li>Vaswani et al. (2017). &ldquo;Attention Is All You Need.&rdquo;</li>
<li>Radford et al. (2019). &ldquo;Language Models are Unsupervised Multitask Learners.&rdquo;</li>
<li>Brown et al. (2020). &ldquo;Language Models are Few-Shot Learners.&rdquo;</li>
<li>Jurafsky &amp; Martin. <em>Speech and Language Processing</em>. Chapters 7-10.</li>
</ul>
<h2 id="watch-the-episode">Watch the Episode</h2>
<div class="youtube-episode" style="position:relative;width:100%;padding-bottom:56.25%;height:0;overflow:hidden;margin:1.5rem 0;">
  <iframe
    src="https://www.youtube-nocookie.com/embed/DfWX30itQxM"
    title="Neural Language Models: From RNNs to Transformers"
    style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;"
    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
    allowfullscreen
    loading="lazy"
    referrerpolicy="strict-origin-when-cross-origin"></iframe>
</div>
<p class="youtube-episode-links"><em>This post is also a video episode: <a href="https://youtu.be/DfWX30itQxM" rel="noopener" target="_blank">Neural Language Models: From RNNs to Transformers</a>, part of <a href="https://www.youtube.com/playlist?list=PLdOm3n4SXt1s" rel="noopener" target="_blank">the series playlist</a>.</em></p>
]]></content:encoded>
      
      
      <category>sequential-prediction</category>
      
      <category>neural-networks</category>
      
      <category>transformers</category>
      
      <category>language-models</category>
      
      <category>attention</category>
      
      <category>gpt</category>
      
      <category>llm</category>
      
      
    </item>
    
    <item>
      <title>Comparing Prediction Methods: CTW vs. N-grams vs. Neural LMs</title>
      <link>https://metafunctor.com/post/2024-06-15-comparing-prediction-methods/</link>
      <pubDate>Sat, 15 Jun 2024 00:00:00 +0000</pubDate>
      <dc:creator>Alex Towell</dc:creator>
      <guid isPermaLink="true">https://metafunctor.com/post/2024-06-15-comparing-prediction-methods/</guid>
      <description>The bias-data trade-off in sequential prediction: when to use CTW, n-grams, or neural language models.</description>
      <content:encoded><![CDATA[<p>Previous posts in this series covered the theoretical foundations (Solomonoff, Bayesian prediction), the model classes (Markov processes, tree sources), and classical approaches (n-grams). Now I want to step back and compare the major approaches to sequential prediction head-to-head.</p>
<p>I&rsquo;m not going to declare a winner. Each method is best in a different regime. The useful insight is knowing when to reach for which tool.</p>
<h2 id="summary">Summary</h2>
<table>
	<thead>
			<tr>
					<th>Aspect</th>
					<th>CTW</th>
					<th>N-gram</th>
					<th>Neural LM</th>
			</tr>
	</thead>
	<tbody>
			<tr>
					<td>Sample efficiency</td>
					<td>Excellent</td>
					<td>Good</td>
					<td>Poor</td>
			</tr>
			<tr>
					<td>Scalability</td>
					<td>Limited</td>
					<td>Good</td>
					<td>Excellent</td>
			</tr>
			<tr>
					<td>Inductive bias</td>
					<td>Tree structures</td>
					<td>Local context</td>
					<td>Learned</td>
			</tr>
			<tr>
					<td>Interpretability</td>
					<td>High</td>
					<td>High</td>
					<td>Low</td>
			</tr>
			<tr>
					<td>Theoretical guarantees</td>
					<td>Strong</td>
					<td>Moderate</td>
					<td>Weak</td>
			</tr>
			<tr>
					<td>Compute requirements</td>
					<td>Minimal</td>
					<td>Low</td>
					<td>Massive</td>
			</tr>
			<tr>
					<td>Context modeling</td>
					<td>Adaptive depth</td>
					<td>Fixed length</td>
					<td>Flexible</td>
			</tr>
			<tr>
					<td>Vocabulary handling</td>
					<td>Binary optimal</td>
					<td>Any size</td>
					<td>Any size</td>
			</tr>
	</tbody>
</table>
<h2 id="sample-efficiency">Sample efficiency</h2>
<p>CTW learns from hundreds to thousands of examples. It has a strong inductive bias (tree structures are rich but constrained), a Bayesian prior that regularizes automatically, and the KT estimator which is statistically optimal. For a 3rd-order Markov process with 8 contexts, CTW gets near-optimal accuracy from about 1000 samples.</p>
<p>N-grams need moderate data. Count statistics converge reasonably fast, smoothing handles sparsity, and higher orders need proportionally more observations.</p>
<p>Neural language models are data-hungry. Modern LLMs train on hundreds of billions of tokens because they must learn all structure from data, with essentially no built-in inductive biases for sequence structure. Transfer learning helps but doesn&rsquo;t eliminate the fundamental requirement.</p>
<h2 id="scalability">Scalability</h2>
<p>CTW hits walls. The context tree has 2^D possible contexts at depth D (exponential growth), pure binary encoding is the sweet spot, and memory grows with sequence diversity. Works well for binary sequences up to moderate depth, maybe 10-15.</p>
<p>N-grams scale reasonably. O(|V|^N) space for vocabulary V and order N, efficient implementations with tries and hashing, practical up to about 5-gram for large vocabularies. Google built web-scale n-gram models, so the engineering is well understood.</p>
<p>Neural LMs were designed for scale. Billions of parameters, GPU/TPU parallelization, vocabulary of 50,000+ tokens. The entire field has optimized for making neural models bigger.</p>
<h2 id="inductive-biases">Inductive biases</h2>
<p>CTW assumes the source is well-modeled by a tree. This is both its strength (sample efficiency, provable guarantees) and its limitation (it can miss patterns outside tree structure). There&rsquo;s an implicit Occam&rsquo;s razor that prefers simpler trees.</p>
<p>N-grams assume all contexts of the same length are equally relevant. Can&rsquo;t adapt context length to data. Smoothing adds a bias toward shorter contexts.</p>
<p>Neural LMs learn their biases from data. Attention patterns emerge during training, positional encodings provide some structure, but the model is very flexible and needs data to specialize. The transformer architecture encodes some bias (attention, residual connections) but much less than CTW or n-grams.</p>
<h2 id="theoretical-guarantees">Theoretical guarantees</h2>
<p>For tree sources, CTW is provably optimal. Redundancy bounded by O(k log n) for a k-leaf tree, convergence to the true distribution at minimax optimal rate, equivalent to Solomonoff induction restricted to trees. These hold with finite data, not just asymptotically.</p>
<p>N-grams have some theory: MLE is consistent, there are cross-entropy bounds with smoothing, but no optimality results for specific source classes.</p>
<p>Neural LMs have weak theory. Universal approximation holds in principle with enough capacity, but there are no sample complexity bounds in realistic settings. We use neural LMs because they work, not because we can prove they should.</p>
<h2 id="interpretability">Interpretability</h2>
<p>The CTW context tree is directly readable. You can see which contexts matter, how confident the model is at each node, and how the model averaging weights show depth preferences.</p>
<p>N-grams give direct access to probability tables: which n-grams are common, what&rsquo;s P(word | context). Clear model of what&rsquo;s being captured.</p>
<p>Neural LMs are opaque. Hidden representations are hard to interpret. Attention patterns provide some insight, and mechanistic interpretability is an active research area, but these models remain largely black boxes.</p>
<h2 id="when-to-use-each">When to use each</h2>
<p><strong>CTW is the right tool when</strong>: data is limited (under 100K samples), sequences are binary or small-alphabet, you need provable guarantees (safety-critical applications), interpretability matters, compute is constrained, or the source is likely tree-structured. Think binary file compression, network protocol analysis, genomic sequences, embedded systems.</p>
<p><strong>N-grams are the right tool when</strong>: you need a baseline, fast training and inference are important, moderate data is available, deployment must be simple, or you want interpretable results. Think prototyping, spell checking, simple autocomplete, PPM compression.</p>
<p><strong>Neural LMs are the right tool when</strong>: massive training data is available, you need state-of-the-art performance, complex patterns are expected (natural language, code), long-range dependencies matter, and you can afford the compute. Think chat assistants, machine translation, code generation.</p>
<h2 id="the-bias-data-trade-off">The bias-data trade-off</h2>
<p>This is the fundamental relationship:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fallback" data-lang="fallback"><span class="line"><span class="cl">Strong bias (CTW) + Little data    -&gt; Good performance
</span></span><span class="line"><span class="cl">Weak bias (Neural) + Little data   -&gt; Overfitting
</span></span><span class="line"><span class="cl">Weak bias (Neural) + Massive data  -&gt; Great performance
</span></span><span class="line"><span class="cl">Strong bias (CTW) + Massive data   -&gt; Limited by model class
</span></span></code></pre></div><p>CTW trades flexibility for sample efficiency. Neural LMs make the opposite trade. Neither trade is universally correct.</p>
<h2 id="the-compression-prediction-duality">The compression-prediction duality</h2>
<p>All of these models can be viewed as compressors. CTW gives near-optimal compression for tree sources. N-grams underlie the PPM compression family. Neural LMs achieve state-of-the-art compression rates. Better prediction equals better compression equals lower cross-entropy. Shannon showed these are mathematically identical, not just analogous.</p>
<h2 id="whats-next">What&rsquo;s next</h2>
<p>The <a href="/post/2024-09-20-neural-language-models-transformers/">next post</a> gets into neural language models specifically, from RNNs to transformers to modern LLMs, and how the architecture evolved to handle sequential prediction at scale.</p>
<h2 id="watch-the-episode">Watch the Episode</h2>
<div class="youtube-episode" style="position:relative;width:100%;padding-bottom:56.25%;height:0;overflow:hidden;margin:1.5rem 0;">
  <iframe
    src="https://www.youtube-nocookie.com/embed/oRZMoPTfc_I"
    title="CTW vs. N-grams vs. Neural Language Models"
    style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;"
    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
    allowfullscreen
    loading="lazy"
    referrerpolicy="strict-origin-when-cross-origin"></iframe>
</div>
<p class="youtube-episode-links"><em>This post is also a video episode: <a href="https://youtu.be/oRZMoPTfc_I" rel="noopener" target="_blank">CTW vs. N-grams vs. Neural Language Models</a>, part of <a href="https://www.youtube.com/playlist?list=PLdOm3n4SXt1s" rel="noopener" target="_blank">the series playlist</a>.</em></p>
]]></content:encoded>
      
      
      <category>sequential-prediction</category>
      
      <category>ctw</category>
      
      <category>neural-networks</category>
      
      <category>language-models</category>
      
      <category>comparison</category>
      
      <category>trade-offs</category>
      
      
    </item>
    
  </channel>
</rss>
