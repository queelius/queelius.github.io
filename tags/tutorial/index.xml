<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:media="http://search.yahoo.com/mrss/"><channel><title>Tutorial on metafunctor</title><link>https://metafunctor.com/tags/tutorial/</link><description>Research Engineer &amp; Computer Scientist - Machine Learning, Statistical Computing, Open Source Development</description><language>en-us</language><managingEditor>lex@metafunctor.com (Alex Towell)</managingEditor><webMaster>lex@metafunctor.com (Alex Towell)</webMaster><lastBuildDate>Fri, 20 Mar 2026 15:44:14 -0500</lastBuildDate><atom:link href="https://metafunctor.com/tags/tutorial/" rel="self" type="application/rss+xml"/><image><url>https://metafunctor.com/images/og-image.jpg</url><title>metafunctor</title><link>https://metafunctor.com/</link></image><item><title>Terminal 101: Learn Linux in Your Browser</title><link>https://metafunctor.com/post/2026-03-18-terminal-101/</link><pubDate>Wed, 18 Mar 2026 00:00:00 +0000</pubDate><dc:creator>Alex Towell</dc:creator><guid isPermaLink="true">https://metafunctor.com/post/2026-03-18-terminal-101/</guid><description>Type real commands into a virtual filesystem. Five lessons from 'where am I?' to piping tools together.</description><content:encoded><![CDATA[<p>You&rsquo;ve seen developers type cryptic commands into black screens. It looks like magic &ndash; or maybe like an artifact from the 1970s that refuses to die. But the terminal is neither. It&rsquo;s a conversation with your computer, and it takes about 10 minutes to learn the basics.</p>
<p>Below is a real terminal. It runs a virtual filesystem in your browser &ndash; no installation, nothing to break. Type the commands it suggests, and by the end you&rsquo;ll understand what all those black screens are about.</p>
<div id="terminal-101"></div>
<style>
.terminal-101 {
  background: #1a1a2e;
  border-radius: 8px;
  box-shadow: 0 4px 24px rgba(0, 0, 0, 0.5);
  min-height: 70vh;
  max-height: 85vh;
  overflow-y: auto;
  padding: 16px;
  font-family: 'SF Mono', 'Cascadia Code', 'Fira Code', Consolas, monospace;
  font-size: 14px;
  line-height: 1.5;
  color: #e0e0e0;
  cursor: text;
  outline: none;
}

.term-line {
  white-space: pre-wrap;
  word-wrap: break-word;
  min-height: 1.2em;
}

.term-normal {
  color: #e0e0e0;
}

.term-lesson {
  color: #66d9ef;
}

.term-error {
  color: #f92672;
}

.term-philosophy {
  color: #e6db74;
  font-style: italic;
  padding-left: 1em;
}

.term-success {
  color: #a6e22e;
}

.term-prompt {
  color: #e0e0e0;
}

.term-input-line {
  display: flex;
  align-items: center;
}

.term-prompt-text {
  color: #50fa7b;
}

.term-input {
  color: #f8f8f2;
  outline: none;
}

.term-cursor {
  display: inline-block;
  width: 8px;
  height: 1.1em;
  background: #e0e0e0;
  vertical-align: text-bottom;
  animation: term-blink 1s step-end infinite;
}

@keyframes term-blink {
  50% {
    opacity: 0;
  }
}

.term-input-line.disabled {
  display: none;
}

</style>
<script>
"use strict";(()=>{var o=class{constructor(t){this.inputBuffer="";this.history=[];this.historyIndex=-1;this.savedInput="";this.promptText="$ ";this.enabled=!1;this.resolveInput=null;this.container=t,this.container.classList.add("terminal-101"),this.container.setAttribute("tabindex","0"),this.output=document.createElement("div"),this.output.className="term-output",this.container.appendChild(this.output),this.inputLine=document.createElement("div"),this.inputLine.className="term-input-line disabled",this.promptSpan=document.createElement("span"),this.promptSpan.className="term-prompt-text",this.promptSpan.textContent=this.promptText,this.inputSpan=document.createElement("span"),this.inputSpan.className="term-input",this.cursorSpan=document.createElement("span"),this.cursorSpan.className="term-cursor",this.inputLine.appendChild(this.promptSpan),this.inputLine.appendChild(this.inputSpan),this.inputLine.appendChild(this.cursorSpan),this.container.appendChild(this.inputLine),this.container.addEventListener("click",()=>this.container.focus()),this.container.addEventListener("keydown",e=>this.handleKeydown(e))}writeLine(t,e="normal"){let s=document.createElement("div");s.className=`term-line term-${e}`,s.textContent=t,this.output.appendChild(s),this.scrollToBottom()}writeLines(t){for(let{text:e,style:s}of t)this.writeLine(e,s)}clear(){for(;this.output.firstChild;)this.output.removeChild(this.output.firstChild)}setPrompt(t){this.promptText=t,this.promptSpan.textContent=t}enable(){this.enabled=!0,this.inputLine.classList.remove("disabled"),this.container.focus()}disable(){this.enabled=!1,this.inputLine.classList.add("disabled")}getInput(){return new Promise(t=>{this.resolveInput=t})}handleKeydown(t){if(!(!this.enabled||!this.resolveInput))if(t.key==="Enter"){t.preventDefault();let e=this.inputBuffer;this.writeLine(this.promptText+e,"prompt"),e.trim()&&this.history.push(e),this.inputBuffer="",this.inputSpan.textContent="",this.historyIndex=-1,this.savedInput="";let s=this.resolveInput;this.resolveInput=null,s(e)}else if(t.key==="Backspace")t.preventDefault(),this.inputBuffer=this.inputBuffer.slice(0,-1),this.inputSpan.textContent=this.inputBuffer;else if(t.key==="ArrowUp"){if(t.preventDefault(),this.history.length===0)return;this.historyIndex===-1?(this.savedInput=this.inputBuffer,this.historyIndex=this.history.length-1):this.historyIndex>0&&(this.historyIndex-=1),this.inputBuffer=this.history[this.historyIndex],this.inputSpan.textContent=this.inputBuffer}else if(t.key==="ArrowDown"){if(t.preventDefault(),this.historyIndex===-1)return;this.historyIndex<this.history.length-1?(this.historyIndex+=1,this.inputBuffer=this.history[this.historyIndex]):(this.historyIndex=-1,this.inputBuffer=this.savedInput),this.inputSpan.textContent=this.inputBuffer}else t.key==="Tab"?t.preventDefault():t.key.length===1&&!t.ctrlKey&&!t.altKey&&!t.metaKey&&(t.preventDefault(),this.inputBuffer+=t.key,this.inputSpan.textContent=this.inputBuffer)}scrollToBottom(){this.container.scrollTop=this.container.scrollHeight}};var a=class{constructor(){this.pyodide=null;this._state="loading";this._error=""}get state(){return this._state}get error(){return this._error}async init(t){try{t?.("Loading Python runtime..."),typeof loadPyodide>"u"&&await this.loadScript("https://cdn.jsdelivr.net/pyodide/v0.27.0/full/pyodide.js"),t?.("Initializing Python..."),this.pyodide=await loadPyodide(),t?.("Loading filesystem engine..."),this.loadDagshellFiles(),t?.("Setting up virtual filesystem..."),this.pyodide.runPython(`from dagshell.dagshell_fluent import DagShell
from dagshell.command_parser import CommandParser
from dagshell.terminal import CommandExecutor

shell = DagShell()
executor = CommandExecutor(shell)
parser = CommandParser()

def execute(cmd):
    try:
        parsed = parser.parse(cmd)
        result = executor.execute(parsed)
        return {'text': result.text or '', 'exit_code': result.exit_code}
    except Exception as e:
        return {'text': f"Error: {e}", 'exit_code': 1}

def get_cwd():
    return shell._cwd

def fs_exists(path):
    return shell.fs.exists(path)

def read_file(path):
    try:
        return shell.fs.read(path).decode('utf-8', errors='replace')
    except Exception:
        return None
`),this.pyodide.runPython(`# Set starting directory
shell.cd('/home/user')

# Pre-seed files
shell.echo('Welcome to the terminal!').out('/home/user/notes.txt')
shell.mkdir('/home/user/projects')
shell.mkdir('/home/user/projects/src')
shell.echo('My first project').out('/home/user/projects/README.md')
shell.echo("print('hello world')").out('/home/user/projects/src/hello.py')

# Server log for Lessons 4 and 5
log_lines = """2026-03-01 08:12:04 INFO  Server started on port 8080
2026-03-01 08:12:05 INFO  Database connection established
2026-03-01 08:15:22 INFO  User alice logged in
2026-03-01 08:17:41 WARN  Slow query detected (2.3s)
2026-03-01 08:22:10 INFO  Processing batch job #1042
2026-03-01 08:23:55 ERROR Connection refused to cache server
2026-03-01 08:24:01 WARN  Falling back to direct database queries
2026-03-01 08:30:12 INFO  User bob logged in
2026-03-01 08:31:44 INFO  Batch job #1042 completed
2026-03-01 08:45:03 WARN  Memory usage at 85%
2026-03-01 08:52:18 ERROR Disk write failed: /var/log/app.log
2026-03-01 09:01:30 INFO  Scheduled backup started
2026-03-01 09:05:44 INFO  Backup completed successfully
2026-03-01 09:12:07 ERROR Timeout waiting for API response (gateway)
2026-03-01 09:15:00 INFO  Health check passed""".strip()

shell.echo(log_lines).out('/home/user/projects/server.log')

# Reset cwd to home
shell.cd('/home/user')
`),this._state="ready"}catch(e){throw this._state="error",this._error=String(e),e}}execute(t){let e=this.pyodide.globals.get("execute")(t),s=e.get("text"),r=e.get("exit_code");return e.destroy(),{text:s,exitCode:r}}getCwd(){return this.pyodide.globals.get("get_cwd")()}fsExists(t){return this.pyodide.globals.get("fs_exists")(t)}async reset(){this.pyodide.runPython(`
shell = DagShell()
executor = CommandExecutor(shell)
`),this.pyodide.runPython(`# Set starting directory
shell.cd('/home/user')

# Pre-seed files
shell.echo('Welcome to the terminal!').out('/home/user/notes.txt')
shell.mkdir('/home/user/projects')
shell.mkdir('/home/user/projects/src')
shell.echo('My first project').out('/home/user/projects/README.md')
shell.echo("print('hello world')").out('/home/user/projects/src/hello.py')

# Server log for Lessons 4 and 5
log_lines = """2026-03-01 08:12:04 INFO  Server started on port 8080
2026-03-01 08:12:05 INFO  Database connection established
2026-03-01 08:15:22 INFO  User alice logged in
2026-03-01 08:17:41 WARN  Slow query detected (2.3s)
2026-03-01 08:22:10 INFO  Processing batch job #1042
2026-03-01 08:23:55 ERROR Connection refused to cache server
2026-03-01 08:24:01 WARN  Falling back to direct database queries
2026-03-01 08:30:12 INFO  User bob logged in
2026-03-01 08:31:44 INFO  Batch job #1042 completed
2026-03-01 08:45:03 WARN  Memory usage at 85%
2026-03-01 08:52:18 ERROR Disk write failed: /var/log/app.log
2026-03-01 09:01:30 INFO  Scheduled backup started
2026-03-01 09:05:44 INFO  Backup completed successfully
2026-03-01 09:12:07 ERROR Timeout waiting for API response (gateway)
2026-03-01 09:15:00 INFO  Health check passed""".strip()

shell.echo(log_lines).out('/home/user/projects/server.log')

# Reset cwd to home
shell.cd('/home/user')
`)}loadDagshellFiles(){let t=this.pyodide.FS;try{t.mkdir("/lib")}catch{}try{t.mkdir("/lib/python")}catch{}try{t.mkdir("/lib/python/dagshell")}catch{}for(let[e,s]of Object.entries(u))t.writeFile(`/lib/python/${e}`,s);this.pyodide.runPython("import sys; sys.path.insert(0, '/lib/python')")}loadScript(t){return new Promise((e,s)=>{let r=document.createElement("script");r.src=t,r.onload=()=>e(),r.onerror=()=>s(new Error(`Failed to load ${t}`)),document.head.appendChild(r)})}},u={"dagshell/__init__.py":`# Stubbed __init__.py for Pyodide (skips scheme_interpreter)
from .dagshell import FileSystem, FileNode, DirNode, DeviceNode, Mode, FileHandle, Node
from .dagshell_fluent import DagShell, CommandResult
from .command_parser import Command, CommandParser, Pipeline, CommandGroup, Redirect, RedirectType
from .terminal import CommandExecutor
`,"dagshell/dagshell.py":`#!/usr/bin/env python3
"""
dagshell - A content-addressable virtual filesystem implemented as a DAG.

Core philosophy:
- Every filesystem object is immutable and content-addressed
- Changes create new nodes, preserving history
- Simple, composable operations following Unix philosophy
"""

import base64
import builtins
import hashlib
import json
import os
import time
from collections import defaultdict
from dataclasses import dataclass, field, asdict
from enum import IntEnum
from typing import Dict, List, Optional, Set, Tuple, Union


class Mode(IntEnum):
    """Unix-style file permissions."""
    # File types
    IFREG = 0o100000  # regular file
    IFDIR = 0o040000  # directory
    IFCHR = 0o020000  # character device
    IFLNK = 0o120000  # symbolic link

    # Permissions
    IRUSR = 0o400  # owner read
    IWUSR = 0o200  # owner write
    IXUSR = 0o100  # owner execute
    IRGRP = 0o040  # group read
    IWGRP = 0o020  # group write
    IXGRP = 0o010  # group execute
    IROTH = 0o004  # other read
    IWOTH = 0o002  # other write
    IXOTH = 0o001  # other execute

    # Common combinations
    FILE_DEFAULT = IFREG | IRUSR | IWUSR | IRGRP | IROTH  # 0o100644
    DIR_DEFAULT = IFDIR | IRUSR | IWUSR | IXUSR | IRGRP | IXGRP | IROTH | IXOTH  # 0o040755
    SYMLINK_DEFAULT = IFLNK | 0o777  # symlinks typically have 0o777 permissions


@dataclass(frozen=True)
class Node:
    """Base class for all filesystem nodes."""
    mode: int
    uid: int = 1000
    gid: int = 1000
    mtime: float = field(default_factory=time.time)

    def compute_hash(self) -> str:
        """Compute SHA256 hash of this node including all metadata."""
        # Create a stable string representation
        data = json.dumps(self.to_dict(), sort_keys=True)
        return hashlib.sha256(data.encode()).hexdigest()

    def to_dict(self) -> dict:
        """Convert node to dictionary for serialization."""
        return asdict(self)

    def is_file(self) -> bool:
        """Check if this is a regular file."""
        return (self.mode & 0o170000) == Mode.IFREG

    def is_dir(self) -> bool:
        """Check if this is a directory."""
        return (self.mode & 0o170000) == Mode.IFDIR

    def is_device(self) -> bool:
        """Check if this is a device."""
        return (self.mode & 0o170000) == Mode.IFCHR

    def is_symlink(self) -> bool:
        """Check if this is a symbolic link."""
        return (self.mode & 0o170000) == Mode.IFLNK


@dataclass(frozen=True)
class FileNode(Node):
    """Regular file node."""
    content: bytes = b""

    def __init__(self, content: Union[str, bytes] = b"", mode: int = Mode.FILE_DEFAULT,
                 uid: int = 1000, gid: int = 1000, mtime: Optional[float] = None):
        if isinstance(content, str):
            content = content.encode('utf-8')
        object.__setattr__(self, 'content', content)
        object.__setattr__(self, 'mode', mode)
        object.__setattr__(self, 'uid', uid)
        object.__setattr__(self, 'gid', gid)
        object.__setattr__(self, 'mtime', mtime or time.time())

    def to_dict(self) -> dict:
        d = super().to_dict()
        # Store content as base64 for JSON serialization
        d['content'] = base64.b64encode(self.content).decode('ascii')
        d['type'] = 'file'
        return d


@dataclass(frozen=True)
class DirNode(Node):
    """Directory node containing references to child nodes."""
    children: Dict[str, str] = field(default_factory=dict)  # name -> hash

    def __init__(self, children: Optional[Dict[str, str]] = None,
                 mode: int = Mode.DIR_DEFAULT, uid: int = 1000,
                 gid: int = 1000, mtime: Optional[float] = None):
        object.__setattr__(self, 'children', children or {})
        object.__setattr__(self, 'mode', mode)
        object.__setattr__(self, 'uid', uid)
        object.__setattr__(self, 'gid', gid)
        object.__setattr__(self, 'mtime', mtime or time.time())

    def to_dict(self) -> dict:
        d = super().to_dict()
        d['type'] = 'dir'
        return d

    def with_child(self, name: str, hash: str) -> 'DirNode':
        """Return a new DirNode with an additional child."""
        new_children = dict(self.children)
        new_children[name] = hash
        return DirNode(new_children, self.mode, self.uid, self.gid)

    def without_child(self, name: str) -> 'DirNode':
        """Return a new DirNode without the specified child."""
        new_children = dict(self.children)
        new_children.pop(name, None)
        return DirNode(new_children, self.mode, self.uid, self.gid)


@dataclass(frozen=True)
class DeviceNode(Node):
    """Virtual device node."""
    device_type: str = ""  # 'null', 'zero', 'random'

    def __init__(self, device_type: str, mode: int = Mode.IFCHR | 0o666,
                 uid: int = 0, gid: int = 0, mtime: Optional[float] = None):
        object.__setattr__(self, 'device_type', device_type)
        object.__setattr__(self, 'mode', mode)
        object.__setattr__(self, 'uid', uid)
        object.__setattr__(self, 'gid', gid)
        object.__setattr__(self, 'mtime', mtime or time.time())

    def read(self, size: int = 1024) -> bytes:
        """Read from the device."""
        if self.device_type == 'null':
            return b''
        elif self.device_type == 'zero':
            return b'\\x00' * size
        elif self.device_type == 'random':
            return os.urandom(size)
        else:
            raise ValueError(f"Unknown device type: {self.device_type}")

    def write(self, data: bytes) -> int:
        """Write to the device (always succeeds for these devices)."""
        if self.device_type == 'null':
            return len(data)  # Black hole - accepts everything
        return 0

    def to_dict(self) -> dict:
        d = super().to_dict()
        d['type'] = 'device'
        return d


@dataclass(frozen=True)
class SymlinkNode(Node):
    """Symbolic link node pointing to another path."""
    target: str = ""  # Path that the symlink points to

    def __init__(self, target: str, mode: int = Mode.SYMLINK_DEFAULT,
                 uid: int = 1000, gid: int = 1000, mtime: Optional[float] = None):
        object.__setattr__(self, 'target', target)
        object.__setattr__(self, 'mode', mode)
        object.__setattr__(self, 'uid', uid)
        object.__setattr__(self, 'gid', gid)
        object.__setattr__(self, 'mtime', mtime or time.time())

    def to_dict(self) -> dict:
        d = super().to_dict()
        d['type'] = 'symlink'
        return d


class FileSystem:
    """
    Content-addressable virtual filesystem.

    The filesystem is a DAG where each node is identified by its content hash.
    Path mappings are maintained separately from the DAG structure.
    """

    def __init__(self):
        # The DAG: hash -> Node
        self.nodes: Dict[str, Node] = {}

        # Path index: absolute path -> hash
        self.paths: Dict[str, str] = {}

        # Reference counting for garbage collection
        self.refs: Dict[str, int] = defaultdict(int)

        # Deleted paths (soft delete until purge)
        self.deleted: Set[str] = set()

        # Initialize root directory
        self._init_filesystem()

    def _init_filesystem(self):
        """Initialize the filesystem with root and basic structure."""
        # Create root directory
        root = DirNode()
        root_hash = self._add_node(root)
        self.paths['/'] = root_hash

        # Create /etc with user/group files
        etc = DirNode()

        # Create /etc/passwd
        passwd_content = b"""root:x:0:0:root:/root:/bin/sh
user:x:1000:1000:Default User:/home/user:/bin/sh
alice:x:1001:1001:Alice:/home/alice:/bin/sh
bob:x:1002:1002:Bob:/home/bob:/bin/sh"""
        passwd_node = FileNode(passwd_content, mode=Mode.FILE_DEFAULT)
        passwd_hash = self._add_node(passwd_node)
        etc = etc.with_child('passwd', passwd_hash)

        # Create /etc/group
        group_content = b"""root:x:0:
user:x:1000:
alice:x:1001:
bob:x:1002:
developers:x:2000:alice,bob"""
        group_node = FileNode(group_content, mode=Mode.FILE_DEFAULT)
        group_hash = self._add_node(group_node)
        etc = etc.with_child('group', group_hash)

        etc_hash = self._add_node(etc)
        root = root.with_child('etc', etc_hash)
        self.paths['/etc'] = etc_hash
        self.paths['/etc/passwd'] = passwd_hash
        self.paths['/etc/group'] = group_hash

        # Create /dev with devices
        dev = DirNode()
        dev_hash = self._add_node(dev)

        # Add virtual devices
        for device_type in ['null', 'zero', 'random']:
            device = DeviceNode(device_type)
            device_hash = self._add_node(device)
            dev = dev.with_child(device_type, device_hash)

        # Update dev directory with devices
        dev_hash = self._add_node(dev)

        # Update root with dev and etc
        root = root.with_child('dev', dev_hash)
        root_hash = self._add_node(root)
        self.paths['/'] = root_hash
        self.paths['/dev'] = dev_hash

        # Map device paths
        for device_type in ['null', 'zero', 'random']:
            device_hash = dev.children[device_type]
            self.paths[f'/dev/{device_type}'] = device_hash

    def _add_node(self, node: Node) -> str:
        """Add a node to the DAG and return its hash."""
        node_hash = node.compute_hash()
        if node_hash not in self.nodes:
            self.nodes[node_hash] = node
        return node_hash

    def _resolve_path(self, path: str, follow_symlinks: bool = True,
                      _visited: Optional[set] = None) -> Optional[str]:
        """Resolve a path to a node hash, optionally following symlinks.

        Args:
            path: The path to resolve
            follow_symlinks: If True, follow symlinks to their target
            _visited: Internal set to track visited symlinks (loop detection)

        Returns:
            The hash of the resolved node, or None if not found.
        """
        path = os.path.normpath(path)
        if path in self.deleted:
            return None

        node_hash = self.paths.get(path)
        if not node_hash:
            return None

        if not follow_symlinks:
            return node_hash

        # Check if it's a symlink and follow it
        node = self.nodes[node_hash]
        if node.is_symlink():
            # Initialize visited set for loop detection
            if _visited is None:
                _visited = set()

            if path in _visited:
                # Symlink loop detected
                return None
            _visited.add(path)

            # Resolve the symlink target
            target = node.target
            if not target.startswith('/'):
                # Relative symlink - resolve relative to symlink's directory
                parent_dir = os.path.dirname(path)
                target = os.path.normpath(os.path.join(parent_dir, target))

            # Recursively resolve (might be a chain of symlinks)
            return self._resolve_path(target, follow_symlinks=True, _visited=_visited)

        return node_hash

    def _get_parent_path(self, path: str) -> Tuple[str, str]:
        """Split path into parent directory and basename."""
        path = os.path.normpath(path)
        if path == '/':
            return None, '/'
        parent = os.path.dirname(path)
        name = os.path.basename(path)
        return parent, name

    # Core filesystem operations

    def open(self, path: str, mode: str = 'r') -> Optional['FileHandle']:
        """Open a file and return a handle."""
        node_hash = self._resolve_path(path)
        if not node_hash:
            if 'w' in mode or 'a' in mode:
                # Create new file
                return self._create_file(path, b'', mode)
            return None

        node = self.nodes[node_hash]
        if node.is_dir():
            raise IsADirectoryError(f"{path} is a directory")

        return FileHandle(self, path, node, mode)

    def read(self, path: str) -> Optional[bytes]:
        """Read entire file contents."""
        handle = self.open(path, 'r')
        if handle:
            return handle.read()
        return None

    def write(self, path: str, content: Union[str, bytes], mtime: Optional[float] = None) -> bool:
        """Write content to a file (creates if doesn't exist)."""
        if isinstance(content, str):
            content = content.encode('utf-8')

        parent_path, name = self._get_parent_path(path)
        if parent_path is None:
            return False

        parent_hash = self._resolve_path(parent_path)
        if not parent_hash:
            return False

        parent = self.nodes[parent_hash]
        if not parent.is_dir():
            return False

        # Create new file node
        file_node = FileNode(content, mtime=mtime)
        file_hash = self._add_node(file_node)

        # Update parent directory
        new_parent = parent.with_child(name, file_hash)
        new_parent_hash = self._add_node(new_parent)

        # Update paths
        self.paths[parent_path] = new_parent_hash
        self.paths[path] = file_hash
        self.deleted.discard(path)

        return True

    def mkdir(self, path: str, mode: int = Mode.DIR_DEFAULT) -> bool:
        """Create a directory."""
        if self._resolve_path(path):
            return False  # Already exists

        parent_path, name = self._get_parent_path(path)
        if parent_path is None:
            return False

        parent_hash = self._resolve_path(parent_path)
        if not parent_hash:
            return False

        parent = self.nodes[parent_hash]
        if not parent.is_dir():
            return False

        # Create new directory
        dir_node = DirNode(mode=mode)
        dir_hash = self._add_node(dir_node)

        # Update parent
        new_parent = parent.with_child(name, dir_hash)
        new_parent_hash = self._add_node(new_parent)

        # Update paths
        self.paths[parent_path] = new_parent_hash
        self.paths[path] = dir_hash
        self.deleted.discard(path)

        return True

    def symlink(self, target: str, link_path: str) -> bool:
        """Create a symbolic link at link_path pointing to target.

        Args:
            target: The path the symlink points to (can be relative or absolute)
            link_path: The path where the symlink will be created

        Returns:
            True if successful, False otherwise.
        """
        if link_path in self.paths:
            return False  # Link path already exists

        # Get parent directory
        parent_path, link_name = self._get_parent_path(link_path)
        parent_hash = self._resolve_path(parent_path)
        if not parent_hash:
            return False

        parent = self.nodes[parent_hash]
        if not parent.is_dir():
            return False

        # Create symlink node (target is stored as-is, resolved at access time)
        symlink_node = SymlinkNode(target)
        symlink_hash = self._add_node(symlink_node)

        # Add to parent
        new_parent = parent.with_child(link_name, symlink_hash)
        new_parent_hash = self._add_node(new_parent)

        # Update paths
        self.paths[parent_path] = new_parent_hash
        self.paths[link_path] = symlink_hash

        return True

    def readlink(self, path: str) -> Optional[str]:
        """Read the target of a symbolic link.

        Args:
            path: Path to the symlink

        Returns:
            The target path string, or None if not a symlink.
        """
        node_hash = self._resolve_path(path, follow_symlinks=False)
        if not node_hash:
            return None

        node = self.nodes[node_hash]
        if not node.is_symlink():
            return None

        return node.target

    def _clone_node(self, node: Node, mode: int, uid: int, gid: int) -> Optional[Node]:
        """Create a copy of a node with updated mode, uid, and gid.

        Returns None if the node type is unrecognized.
        """
        if node.is_file():
            return FileNode(content=node.content, mode=mode,
                            uid=uid, gid=gid, mtime=node.mtime)
        elif node.is_dir():
            return DirNode(children=node.children, mode=mode,
                           uid=uid, gid=gid, mtime=node.mtime)
        elif node.is_symlink():
            return SymlinkNode(target=node.target, mode=mode,
                               uid=uid, gid=gid, mtime=node.mtime)
        elif node.is_device():
            return DeviceNode(device_type=node.device_type, mode=mode,
                              uid=uid, gid=gid, mtime=node.mtime)
        return None

    def _update_parent_reference(self, path: str, child_name: str, child_hash: str) -> None:
        """Update the parent directory to reference a new child hash."""
        parent_path, _ = self._get_parent_path(path)
        if not parent_path:
            return

        parent_hash = self._resolve_path(parent_path)
        if not parent_hash:
            return

        parent = self.nodes[parent_hash]
        if not parent.is_dir():
            return

        new_parent = parent.with_child(child_name, child_hash)
        new_parent_hash = self._add_node(new_parent)
        self.paths[parent_path] = new_parent_hash

    def chmod(self, path: str, mode: int) -> bool:
        """Change file mode bits.

        Args:
            path: Path to file/directory
            mode: New permission bits (e.g., 0o755)

        Returns:
            True if successful, False otherwise.
        """
        node_hash = self._resolve_path(path, follow_symlinks=False)
        if not node_hash:
            return False

        node = self.nodes[node_hash]

        # Preserve file type bits, update permission bits
        type_bits = node.mode & 0o170000
        new_mode = type_bits | (mode & 0o7777)

        new_node = self._clone_node(node, new_mode, node.uid, node.gid)
        if new_node is None:
            return False

        new_hash = self._add_node(new_node)
        self.paths[path] = new_hash

        _, name = self._get_parent_path(path)
        self._update_parent_reference(path, name, new_hash)

        return True

    def chown(self, path: str, uid: Optional[int] = None, gid: Optional[int] = None) -> bool:
        """Change file owner and group.

        Args:
            path: Path to file/directory
            uid: New user ID (None to keep current)
            gid: New group ID (None to keep current)

        Returns:
            True if successful, False otherwise.
        """
        node_hash = self._resolve_path(path, follow_symlinks=False)
        if not node_hash:
            return False

        node = self.nodes[node_hash]

        new_uid = uid if uid is not None else node.uid
        new_gid = gid if gid is not None else node.gid

        new_node = self._clone_node(node, node.mode, new_uid, new_gid)
        if new_node is None:
            return False

        new_hash = self._add_node(new_node)
        self.paths[path] = new_hash

        _, name = self._get_parent_path(path)
        self._update_parent_reference(path, name, new_hash)

        return True

    def ls(self, path: str = '/') -> Optional[List[str]]:
        """List directory contents."""
        node_hash = self._resolve_path(path)
        if not node_hash:
            return None

        node = self.nodes[node_hash]
        if not node.is_dir():
            return None

        return sorted(node.children.keys())

    def rm(self, path: str) -> bool:
        """Soft delete - removes path mapping but keeps data."""
        if path == '/':
            return False  # Cannot delete root

        if path not in self.paths:
            return False

        parent_path, name = self._get_parent_path(path)
        parent_hash = self._resolve_path(parent_path)
        if parent_hash:
            parent = self.nodes[parent_hash]
            if parent.is_dir() and name in parent.children:
                # Remove from parent directory
                new_parent = parent.without_child(name)
                new_parent_hash = self._add_node(new_parent)
                self.paths[parent_path] = new_parent_hash

        # Mark as deleted
        self.deleted.add(path)
        del self.paths[path]
        return True

    def purge(self) -> int:
        """Garbage collection - remove unreferenced nodes."""
        # Build reference graph
        referenced = set()

        def mark_referenced(hash: str):
            if hash in referenced:
                return
            referenced.add(hash)
            node = self.nodes.get(hash)
            if node and node.is_dir():
                for child_hash in node.children.values():
                    mark_referenced(child_hash)

        # Mark all nodes reachable from active paths
        for hash in self.paths.values():
            mark_referenced(hash)

        # Remove unreferenced nodes
        to_remove = set(self.nodes.keys()) - referenced
        for hash in to_remove:
            del self.nodes[hash]

        # Clear deleted set
        self.deleted.clear()

        return len(to_remove)

    def _node_type_str(self, node: Node) -> str:
        """Return the type string for a node."""
        if node.is_file():
            return 'file'
        elif node.is_dir():
            return 'dir'
        elif node.is_symlink():
            return 'symlink'
        else:
            return 'device'

    def stat(self, path: str) -> Optional[dict]:
        """Get file/directory statistics."""
        node_hash = self._resolve_path(path)
        if not node_hash:
            return None

        node = self.nodes[node_hash]
        return {
            'type': self._node_type_str(node),
            'mode': node.mode,
            'uid': node.uid,
            'gid': node.gid,
            'mtime': node.mtime,
            'size': len(node.content) if node.is_file() else 0,
            'hash': node_hash
        }

    def exists(self, path: str) -> bool:
        """Check if a path exists."""
        return self._resolve_path(path) is not None

    def _create_file(self, path: str, content: bytes, mode: str, mtime: Optional[float] = None) -> Optional['FileHandle']:
        """Create a new file."""
        parent_path, name = self._get_parent_path(path)
        if parent_path is None:
            return None

        parent_hash = self._resolve_path(parent_path)
        if not parent_hash:
            return None

        parent = self.nodes[parent_hash]
        if not parent.is_dir():
            return None

        # Create file node
        file_node = FileNode(content, mtime=mtime)
        file_hash = self._add_node(file_node)

        # Update parent
        new_parent = parent.with_child(name, file_hash)
        new_parent_hash = self._add_node(new_parent)

        # Update paths
        self.paths[parent_path] = new_parent_hash
        self.paths[path] = file_hash
        self.deleted.discard(path)

        return FileHandle(self, path, file_node, mode)

    # User and permission management

    def lookup_user(self, username: str) -> Tuple[int, int]:
        """Look up user ID and primary group ID from /etc/passwd."""
        passwd_hash = self._resolve_path('/etc/passwd')
        if not passwd_hash:
            # Default if no passwd file
            return (1000, 1000) if username == 'user' else (0, 0)

        passwd_node = self.nodes[passwd_hash]
        content = passwd_node.content.decode('utf-8')

        for line in content.strip().split('\\n'):
            parts = line.split(':')
            if parts[0] == username:
                return (int(parts[2]), int(parts[3]))

        # Default for unknown users
        return (1000, 1000)

    def get_user_groups(self, username: str) -> Set[int]:
        """Get all group IDs for a user from /etc/group."""
        uid, primary_gid = self.lookup_user(username)
        groups = {primary_gid}

        group_hash = self._resolve_path('/etc/group')
        if not group_hash:
            return groups

        group_node = self.nodes[group_hash]
        content = group_node.content.decode('utf-8')

        for line in content.strip().split('\\n'):
            parts = line.split(':')
            if len(parts) >= 4:
                gid = int(parts[2])
                members = parts[3].split(',') if parts[3] else []
                if username in members:
                    groups.add(gid)

        return groups

    # Permission categories: map any read/write/execute permission to its category
    _READ_PERMS = frozenset({Mode.IRUSR, Mode.IRGRP, Mode.IROTH})
    _WRITE_PERMS = frozenset({Mode.IWUSR, Mode.IWGRP, Mode.IWOTH})
    _EXEC_PERMS = frozenset({Mode.IXUSR, Mode.IXGRP, Mode.IXOTH})

    def _check_mode_bits(self, mode: int, permission: int, shift: int) -> bool:
        """Check a permission category (read/write/exec) at a given shift level.

        Args:
            mode: The full mode bits of the node
            permission: The permission being requested
            shift: Bit shift for scope (6=owner, 3=group, 0=other)
        """
        if permission in self._READ_PERMS:
            return bool(mode & (0o4 << shift))
        elif permission in self._WRITE_PERMS:
            return bool(mode & (0o2 << shift))
        elif permission in self._EXEC_PERMS:
            return bool(mode & (0o1 << shift))
        return False

    def check_permission(self, path: str, uid: int, gids: Set[int], permission: int) -> bool:
        """
        Check if user has specific permission for a path.

        Args:
            path: File/directory path
            uid: User ID
            gids: Set of group IDs the user belongs to
            permission: Permission bit to check (e.g., Mode.IRUSR, Mode.IWUSR)

        Returns:
            True if permission granted, False otherwise
        """
        node_hash = self._resolve_path(path)
        if not node_hash:
            return False

        node = self.nodes[node_hash]
        mode = node.mode

        # Root user (uid 0) has all permissions
        if uid == 0:
            return True

        # Owner permissions
        if uid == node.uid:
            return self._check_mode_bits(mode, permission, 6)

        # Group permissions
        if node.gid in gids:
            return self._check_mode_bits(mode, permission, 3)

        # Other permissions
        return self._check_mode_bits(mode, permission, 0)

    def export_to_real(self, target_path: str, preserve_permissions: bool = True,
                      uid_map: Optional[Dict[int, int]] = None,
                      gid_map: Optional[Dict[int, int]] = None) -> int:
        """
        Export the virtual filesystem to a real filesystem.

        Args:
            target_path: Target directory for export
            preserve_permissions: Whether to preserve file modes
            uid_map: Mapping from virtual UIDs to real UIDs
            gid_map: Mapping from virtual GIDs to real GIDs

        Returns:
            Number of files/directories exported
        """
        # Default mappings (virtual -> real)
        if uid_map is None:
            uid_map = {0: 0, 1000: os.getuid(), 1001: os.getuid(), 1002: os.getuid()}
        if gid_map is None:
            gid_map = {0: 0, 1000: os.getgid(), 1001: os.getgid(), 1002: os.getgid()}

        # Create target directory if needed
        os.makedirs(target_path, exist_ok=True)

        exported = 0

        # Export all paths
        for vpath in sorted(self.paths.keys()):
            if vpath in self.deleted:
                continue

            node_hash = self.paths[vpath]
            node = self.nodes[node_hash]

            # Skip root directory itself
            if vpath == '/':
                continue

            # Calculate real path
            real_path = os.path.join(target_path, vpath.lstrip('/'))

            if node.is_dir():
                # Create directory
                os.makedirs(real_path, exist_ok=True)
                exported += 1

                if preserve_permissions:
                    # Set directory permissions (remove file type bits)
                    mode = node.mode & 0o777
                    try:
                        os.chmod(real_path, mode)
                    except Exception:
                        pass  # Ignore permission errors

            elif node.is_file():
                # Create parent directory if needed
                parent_dir = os.path.dirname(real_path)
                os.makedirs(parent_dir, exist_ok=True)

                # Write file content
                with builtins.open(real_path, 'wb') as f:
                    f.write(node.content)
                exported += 1

                if preserve_permissions:
                    # Set file permissions (remove file type bits)
                    mode = node.mode & 0o777
                    try:
                        os.chmod(real_path, mode)
                    except Exception:
                        pass  # Ignore permission errors

                # Try to set ownership if running as root
                if os.geteuid() == 0:
                    real_uid = uid_map.get(node.uid, node.uid)
                    real_gid = gid_map.get(node.gid, node.gid)
                    try:
                        os.chown(real_path, real_uid, real_gid)
                    except Exception:
                        pass  # Ignore if can't change ownership

        return exported

    def import_from_real(self, source_path: str, target_path: str = '/',
                         preserve_permissions: bool = True,
                         uid: Optional[int] = None,
                         gid: Optional[int] = None) -> int:
        """
        Import files from the real filesystem into the virtual filesystem.

        Args:
            source_path: Source file/directory on the real filesystem
            target_path: Target path in the virtual filesystem
            preserve_permissions: Whether to preserve file modes from real filesystem
            uid: Override UID for imported files (default: 1000)
            gid: Override GID for imported files (default: 1000)

        Returns:
            Number of files/directories imported

        Raises:
            FileNotFoundError: If source_path doesn't exist
            ValueError: If target_path is invalid
        """
        if uid is None:
            uid = 1000
        if gid is None:
            gid = 1000

        # Check if source exists
        if not os.path.exists(source_path):
            raise FileNotFoundError(f"Source path not found: {source_path}")

        # Normalize target path
        if not target_path.startswith('/'):
            raise ValueError(f"Target path must be absolute: {target_path}")

        imported = 0

        # If source is a file
        if os.path.isfile(source_path):
            # Read content
            with builtins.open(source_path, 'rb') as f:
                content = f.read()

            # Get mode if preserving permissions
            mode = Mode.FILE_DEFAULT
            if preserve_permissions:
                stat = os.stat(source_path)
                mode = Mode.IFREG | (stat.st_mode & 0o777)

            # Write to virtual filesystem
            self.write(target_path, content)

            # Update permissions if needed
            if target_path in self.paths:
                node_hash = self.paths[target_path]
                node = self.nodes[node_hash]
                # Create new node with updated permissions
                new_node = FileNode(
                    content=node.content,
                    mode=mode,
                    uid=uid,
                    gid=gid,
                    mtime=node.mtime
                )
                new_hash = self._add_node(new_node)
                self.paths[target_path] = new_hash
                imported += 1

        # If source is a directory
        elif os.path.isdir(source_path):
            # Walk the directory tree
            for root, dirs, files in os.walk(source_path):
                # Calculate relative path from source
                rel_path = os.path.relpath(root, source_path)
                if rel_path == '.':
                    virt_root = target_path
                else:
                    virt_root = os.path.join(target_path, rel_path).replace('\\\\', '/')

                # Create directory in virtual filesystem
                if virt_root != '/':
                    mode = Mode.DIR_DEFAULT
                    if preserve_permissions:
                        stat = os.stat(root)
                        mode = Mode.IFDIR | (stat.st_mode & 0o777)
                    self.mkdir(virt_root)

                    # Update permissions
                    if virt_root in self.paths:
                        node_hash = self.paths[virt_root]
                        node = self.nodes[node_hash]
                        new_node = DirNode(
                            mode=mode,
                            uid=uid,
                            gid=gid,
                            mtime=node.mtime
                        )
                        new_hash = self._add_node(new_node)
                        self.paths[virt_root] = new_hash
                    imported += 1

                # Import files in this directory
                for filename in files:
                    real_file_path = os.path.join(root, filename)
                    virt_file_path = os.path.join(virt_root, filename).replace('\\\\', '/')

                    # Read content
                    with builtins.open(real_file_path, 'rb') as f:
                        content = f.read()

                    # Get mode if preserving permissions
                    mode = Mode.FILE_DEFAULT
                    if preserve_permissions:
                        stat = os.stat(real_file_path)
                        mode = Mode.IFREG | (stat.st_mode & 0o777)

                    # Write to virtual filesystem
                    self.write(virt_file_path, content)

                    # Update permissions
                    if virt_file_path in self.paths:
                        node_hash = self.paths[virt_file_path]
                        node = self.nodes[node_hash]
                        new_node = FileNode(
                            content=node.content,
                            mode=mode,
                            uid=uid,
                            gid=gid,
                            mtime=node.mtime
                        )
                        new_hash = self._add_node(new_node)
                        self.paths[virt_file_path] = new_hash
                    imported += 1

        else:
            raise ValueError(f"Source path is neither file nor directory: {source_path}")

        return imported

    # Serialization

    def to_json(self) -> str:
        """Serialize filesystem to JSON."""
        data = {
            'nodes': {hash: node.to_dict() for hash, node in self.nodes.items()},
            'paths': self.paths,
            'deleted': list(self.deleted)
        }
        return json.dumps(data, indent=2)

    @classmethod
    def from_json(cls, json_str: str) -> 'FileSystem':
        """Deserialize filesystem from JSON."""
        data = json.loads(json_str)
        fs = cls.__new__(cls)
        fs.nodes = {}
        fs.paths = data['paths']
        fs.deleted = set(data.get('deleted', []))
        fs.refs = defaultdict(int)

        # Reconstruct nodes
        for hash, node_data in data['nodes'].items():
            node_type = node_data.get('type', 'file')

            if node_type == 'file':
                content = base64.b64decode(node_data.get('content', ''))
                node = FileNode(content=content, **{k: v for k, v in node_data.items() if k not in ('type', 'content')})
            elif node_type == 'dir':
                node = DirNode(**{k: v for k, v in node_data.items() if k != 'type'})
            elif node_type == 'device':
                node = DeviceNode(**{k: v for k, v in node_data.items() if k != 'type'})
            elif node_type == 'symlink':
                node = SymlinkNode(**{k: v for k, v in node_data.items() if k != 'type'})
            else:
                continue

            fs.nodes[hash] = node

        return fs


class FileHandle:
    """Handle for reading/writing files."""

    def __init__(self, fs: FileSystem, path: str, node: Node, mode: str):
        self.fs = fs
        self.path = path
        self.node = node
        self.mode = mode
        self.position = 0
        self.buffer = bytearray(node.content if node.is_file() else b'')

        if 'a' in mode:
            self.position = len(self.buffer)

    def read(self, size: int = -1) -> bytes:
        """Read from file."""
        if 'r' not in self.mode and '+' not in self.mode:
            raise IOError("File not opened for reading")

        if self.node.is_device():
            return self.node.read(size if size > 0 else 1024)

        if size < 0:
            result = bytes(self.buffer[self.position:])
        else:
            result = bytes(self.buffer[self.position:self.position + size])

        self.position += len(result)
        return result

    def write(self, data: Union[str, bytes]) -> int:
        """Write to file."""
        if 'w' not in self.mode and 'a' not in self.mode and '+' not in self.mode:
            raise IOError("File not opened for writing")

        if isinstance(data, str):
            data = data.encode('utf-8')

        if self.node.is_device():
            return self.node.write(data)

        if 'w' in self.mode and self.position == 0:
            self.buffer = bytearray(data)
            self.position = len(data)
        else:
            # Extend buffer if necessary
            end_pos = self.position + len(data)
            if end_pos > len(self.buffer):
                self.buffer.extend(b'\\x00' * (end_pos - len(self.buffer)))

            self.buffer[self.position:end_pos] = data
            self.position = end_pos

        return len(data)

    def close(self):
        """Close file and commit changes."""
        if 'w' in self.mode or 'a' in self.mode or ('+' in self.mode and self.buffer != self.node.content):
            # Save changes
            self.fs.write(self.path, bytes(self.buffer))

    def __enter__(self):
        return self

    def __exit__(self, *args):
        self.close()


# Convenience functions for a more Unix-like API

_default_fs = None

def get_fs() -> FileSystem:
    """Get the default filesystem instance."""
    global _default_fs
    if _default_fs is None:
        _default_fs = FileSystem()
    return _default_fs

def open(path: str, mode: str = 'r') -> Optional[FileHandle]:
    """Open a file."""
    return get_fs().open(path, mode)

def read(path: str) -> Optional[bytes]:
    """Read file contents."""
    return get_fs().read(path)

def write(path: str, content: Union[str, bytes], mtime: Optional[float] = None) -> bool:
    """Write content to file."""
    return get_fs().write(path, content, mtime)

def mkdir(path: str) -> bool:
    """Create directory."""
    return get_fs().mkdir(path)

def ls(path: str = '/') -> Optional[List[str]]:
    """List directory."""
    return get_fs().ls(path)

def rm(path: str) -> bool:
    """Remove file/directory."""
    return get_fs().rm(path)

def purge() -> int:
    """Run garbage collection."""
    return get_fs().purge()

def exists(path: str) -> bool:
    """Check if path exists."""
    return get_fs().exists(path)

def stat(path: str) -> Optional[dict]:
    """Get file statistics."""
    return get_fs().stat(path)`,"dagshell/dagshell_fluent.py":`#!/usr/bin/env python3
"""
Fluent API for dagshell - A composable, chainable interface for terminal emulation.

This module provides a fluent, Unix-pipe-like API that serves as the foundation
for terminal emulation. It follows the principle of small, composable operations
that can be chained together.

Core Design Principles:
- Stateful operations that maintain context (pwd, env)
- Method chaining for pipe-like composition
- Dual nature: returns Python objects or redirects to virtual FS
- Clean separation between command execution and output handling
"""

import datetime
import difflib
import fnmatch
import os
import re
from dataclasses import dataclass
from pathlib import PurePosixPath
from typing import Any, Callable, Dict, Iterator, List, Optional, Union

from . import dagshell


@dataclass
class CommandResult:
    """
    Represents the result of a command execution.

    This class enables method chaining and provides both Python object
    access and virtual filesystem redirection capabilities.
    """
    data: Any  # The actual data (bytes, list, dict, etc.)
    text: Optional[str] = None  # Text representation for output
    exit_code: int = 0
    _shell: Optional['DagShell'] = None  # Reference to shell for context

    def __str__(self) -> str:
        """String representation of the result."""
        if self.text is not None:
            return self.text
        elif isinstance(self.data, bytes):
            return self.data.decode('utf-8', errors='replace')
        elif isinstance(self.data, list):
            return '\\n'.join(str(item) for item in self.data)
        elif isinstance(self.data, dict):
            return '\\n'.join(f"{k}: {v}" for k, v in self.data.items())
        else:
            return str(self.data)

    def __bytes__(self) -> bytes:
        """Bytes representation of the result."""
        if isinstance(self.data, bytes):
            return self.data
        else:
            return str(self).encode('utf-8')

    def __iter__(self) -> Iterator:
        """Allow iteration over list results."""
        if isinstance(self.data, (list, tuple)):
            return iter(self.data)
        elif isinstance(self.data, str):
            return iter(self.data.splitlines())
        elif isinstance(self.data, bytes):
            return iter(self.data.decode('utf-8', errors='replace').splitlines())
        else:
            return iter([self.data])

    def lines(self) -> List[str]:
        """Get result as list of lines."""
        return list(self)

    def out(self, path: str) -> 'CommandResult':
        """Redirect output to a file in the virtual filesystem."""
        # Resolve path relative to shell's current directory if available
        if self._shell and not path.startswith('/'):
            path = os.path.join(self._shell._cwd, path)

        # Use shell's filesystem if available, otherwise fall back to global
        if self._shell:
            # Ensure parent directories exist (create nested if necessary)
            parent_path = os.path.dirname(path)
            if parent_path and parent_path != '/' and not self._shell.fs.exists(parent_path):
                # Create parent directories recursively
                self._shell.mkdir(parent_path, parents=True)

            content = bytes(self)
            self._shell.fs.write(path, content)
        else:
            # Fall back to global filesystem
            parent_path = os.path.dirname(path)
            if parent_path and parent_path != '/' and not dagshell.exists(parent_path):
                dagshell.mkdir(parent_path)

            content = bytes(self)
            dagshell.write(path, content)
        return self

    def append(self, path: str) -> 'CommandResult':
        """Append output to a file in the virtual filesystem."""
        # Resolve path relative to shell's current directory if available
        if self._shell and not path.startswith('/'):
            path = os.path.join(self._shell._cwd, path)

        # Use shell's filesystem if available, otherwise fall back to global
        if self._shell:
            # Ensure parent directories exist (create nested if necessary)
            parent_path = os.path.dirname(path)
            if parent_path and parent_path != '/' and not self._shell.fs.exists(parent_path):
                # Create parent directories recursively
                self._shell.mkdir(parent_path, parents=True)

            # Read existing content from shell's filesystem
            existing = self._shell.fs.read(path) if self._shell.fs.exists(path) else b''
            content = existing + bytes(self)
            self._shell.fs.write(path, content)
        else:
            # Fall back to global filesystem
            parent_path = os.path.dirname(path)
            if parent_path and parent_path != '/' and not dagshell.exists(parent_path):
                dagshell.mkdir(parent_path)

            existing = dagshell.read(path) if dagshell.exists(path) else b''
            content = existing + bytes(self)
            dagshell.write(path, content)
        return self


class DagShell:
    """
    Fluent, stateful shell-like interface for dagshell.

    This class provides a chainable API that mimics Unix shell commands
    while maintaining state like current directory and environment variables.
    """

    def __init__(self, fs: Optional[dagshell.FileSystem] = None):
        """Initialize the shell with optional filesystem."""
        self.fs = fs or dagshell.get_fs()
        self._cwd = '/'
        self._env = {
            'PATH': '/bin:/usr/bin:/usr/local/bin',
            'HOME': '/home/user',
            'USER': 'user',
            'SHELL': '/bin/dagshell',
            'PWD': '/'
        }
        self._last_result: Optional[CommandResult] = None
        self._dir_stack: List[str] = []  # Stack for pushd/popd
        self._history: List[str] = []  # Command history
        self._history_max: int = 1000  # Maximum history size
        self._oldpwd = '/'
        # Ensure HOME directory exists (create parents as needed)
        home = self._env['HOME']
        if not self.fs.exists(home):
            parts = home.strip('/').split('/')
            current = ''
            for part in parts:
                current = current + '/' + part
                if not self.fs.exists(current):
                    self.fs.mkdir(current)

    def _add_to_history(self, command: str) -> None:
        """Add a command to history."""
        if command and command.strip():
            self._history.append(command.strip())
            # Trim history if too long
            if len(self._history) > self._history_max:
                self._history = self._history[-self._history_max:]

    def history(self, n: Optional[int] = None) -> CommandResult:
        """Display command history.

        Usage:
            history [N]

        Options:
            N                      Number of recent commands to show

        Returns:
            Command history list.
        """
        if n is None:
            hist = self._history.copy()
        else:
            hist = self._history[-n:] if n > 0 else []

        # Format like bash history with line numbers
        lines = [f"{i+1}  {cmd}" for i, cmd in enumerate(hist)]
        return self._make_result(hist, '\\n'.join(lines))

    def _make_result(self, data: Any, text: Optional[str] = None, exit_code: int = 0) -> CommandResult:
        """Create a CommandResult with shell context."""
        result = CommandResult(data=data, text=text, exit_code=exit_code, _shell=self)
        self._last_result = result
        return result

    def _resolve_path(self, path: str) -> str:
        """Resolve relative paths to absolute paths and normalize them."""
        # Construct the full path
        if not path.startswith('/'):
            # Relative path - join with current directory
            full_path = str(PurePosixPath(self._cwd) / path)
        else:
            # Absolute path
            full_path = path

        # Normalize the path (resolve . and .. components)
        # Split into parts and manually resolve . and ..
        parts = full_path.split('/')
        normalized = []

        for part in parts:
            if part == '' or part == '.':
                # Skip empty parts and current directory references
                continue
            elif part == '..':
                # Go up one level if possible
                if normalized:
                    normalized.pop()
            else:
                # Regular directory/file name
                normalized.append(part)

        # Reconstruct the path
        result = '/' + '/'.join(normalized) if normalized else '/'
        return result

    def _ensure_parent_dirs(self, path: str) -> None:
        """Ensure all parent directories exist, creating them if needed.

        Args:
            path: The directory path to ensure exists.
        """
        # Split the path into components
        parts = path.strip('/').split('/')
        current = ''
        for part in parts:
            current = f'{current}/{part}'
            if not self.fs.exists(current):
                self.fs.mkdir(current)

    def _read_input_lines(self, paths: tuple) -> List[str]:
        """Read lines from files or from the last result (stdin).

        If paths are given, reads and splits each file's content into lines.
        Otherwise, falls back to self._last_result as stdin.
        """
        if paths:
            lines = []
            for path in paths:
                resolved = self._resolve_path(path)
                content = self.fs.read(resolved)
                if content:
                    text = content.decode('utf-8', errors='replace')
                    lines.extend(text.splitlines())
            return lines

        if self._last_result:
            return self._last_result.lines()
        return []

    def _glob_match(self, pattern: str, path: str = None) -> List[str]:
        """Match files using glob patterns."""
        path = self._resolve_path(path or self._cwd)

        # Handle wildcards in the pattern
        if '*' in pattern or '?' in pattern or '[' in pattern:
            files = self.fs.ls(path) or []
            return [f for f in files if fnmatch.fnmatch(f, pattern)]
        else:
            # No wildcard, return as-is if exists
            full_path = self._resolve_path(os.path.join(path, pattern))
            if self.fs.exists(full_path):
                return [pattern]
            return []

    # State inspection methods

    def pwd(self) -> CommandResult:
        """Print working directory.

        Usage:
            pwd

        Options:
            None

        Examples:
            pwd                    # Show current directory

        Returns:
            The absolute path of the current working directory.
        """
        return self._make_result(data=self._cwd, text=self._cwd)

    def env(self, var: Optional[str] = None) -> CommandResult:
        """Display environment variables.

        Usage:
            env [VARIABLE]

        Options:
            VARIABLE               Name of specific variable to display

        Examples:
            env                    # Show all environment variables
            env USER               # Show value of USER variable
            env PATH               # Show value of PATH variable

        Returns:
            Environment variable value(s).
        """
        if var:
            value = self._env.get(var, '')
            return self._make_result(data=value, text=value)
        return self._make_result(data=dict(self._env))

    def setenv(self, var: str, value: str) -> 'DagShell':
        """Set environment variable.

        Usage:
            setenv VARIABLE VALUE

        Options:
            VARIABLE               Variable name
            VALUE                  Variable value

        Examples:
            setenv PATH /usr/bin   # Set PATH variable
            setenv EDITOR vim      # Set default editor
            setenv DEBUG true      # Set debug flag

        Returns:
            Self for method chaining.
        """
        self._env[var] = value
        if var == 'PWD':
            self._cwd = value
        return self

    def whoami(self) -> CommandResult:
        """Print effective user name.

        Usage:
            whoami

        Examples:
            whoami                 # Show current username

        Returns:
            Current username.
        """
        user = self._env.get('USER', 'user')
        return self._make_result(user, user)

    def id(self, user: Optional[str] = None) -> CommandResult:
        """Print user and group IDs.

        Usage:
            id [USER]

        Options:
            USER                   Username to query (default: current user)

        Examples:
            id                     # Show current user's IDs
            id alice               # Show alice's IDs

        Returns:
            User and group ID information.
        """
        if user is None:
            user = self._env.get('USER', 'user')

        uid, gid = self.fs.lookup_user(user)
        groups = self.fs.get_user_groups(user)

        # Format like: uid=1000(user) gid=1000(user) groups=1000(user),2000(developers)
        group_list = ','.join(f'{g}' for g in sorted(groups))
        text = f'uid={uid}({user}) gid={gid} groups={group_list}'

        return self._make_result({'uid': uid, 'gid': gid, 'groups': list(groups)}, text)

    def stat(self, path: str) -> CommandResult:
        """Display file status.

        Usage:
            stat FILE

        Options:
            FILE                   File or directory to examine

        Examples:
            stat /etc/passwd       # Show file information
            stat /home             # Show directory information

        Returns:
            Detailed file status information.
        """
        resolved = self._resolve_path(path)
        info = self.fs.stat(resolved)

        if info is None:
            return self._make_result(
                None,
                f"stat: cannot statx '{path}': No such file or directory",
                exit_code=1
            )

        # Format output like stat command
        file_type = info['type']
        mode = info['mode']
        mode_str = self._format_mode(mode)
        size = info.get('size', 0)
        uid = info['uid']
        gid = info['gid']
        mtime = info['mtime']

        mtime_str = datetime.datetime.fromtimestamp(mtime).strftime('%Y-%m-%d %H:%M:%S')

        lines = [
            f"  File: {path}",
            f"  Size: {size}\\tType: {file_type}",
            f"Access: ({oct(mode & 0o7777)}/{mode_str})\\tUid: {uid}\\tGid: {gid}",
            f"Modify: {mtime_str}",
        ]

        return self._make_result(info, '\\n'.join(lines))

    def _format_mode(self, mode: int) -> str:
        """Format mode bits as rwx string."""
        result = ''

        # File type
        if mode & 0o170000 == 0o040000:
            result += 'd'
        elif mode & 0o170000 == 0o120000:
            result += 'l'
        elif mode & 0o170000 == 0o020000:
            result += 'c'
        else:
            result += '-'

        # Owner
        result += 'r' if mode & 0o400 else '-'
        result += 'w' if mode & 0o200 else '-'
        result += 'x' if mode & 0o100 else '-'

        # Group
        result += 'r' if mode & 0o040 else '-'
        result += 'w' if mode & 0o020 else '-'
        result += 'x' if mode & 0o010 else '-'

        # Other
        result += 'r' if mode & 0o004 else '-'
        result += 'w' if mode & 0o002 else '-'
        result += 'x' if mode & 0o001 else '-'

        return result

    # Navigation methods

    def cd(self, path: str = None) -> 'DagShell':
        """Change the current directory.

        Usage:
            cd [PATH]

        Options:
            PATH                   Directory to change to (default: HOME)

        Examples:
            cd                     # Go to home directory
            cd /usr/bin            # Go to /usr/bin
            cd ..                  # Go to parent directory
            cd ~/documents         # Go to documents in home

        Returns:
            Self for method chaining.
        """
        if path is None:
            path = self._env.get('HOME', '/')

        if path == '-':
            path = self._oldpwd

        new_path = self._resolve_path(path)

        # Check if directory exists
        if not self.fs.exists(new_path):
            # Should we raise an error or return self for chaining?
            # For fluent API, we'll set exit code but continue
            self._last_result = CommandResult(
                data=None,
                text=f"cd: {path}: No such file or directory",
                exit_code=1
            )
            return self

        # Check if it's a directory
        stat = self.fs.stat(new_path)
        if stat['type'] != 'dir':
            self._last_result = CommandResult(
                data=None,
                text=f"cd: {path}: Not a directory",
                exit_code=1
            )
            return self

        old_cwd = self._cwd
        self._cwd = new_path
        self._env['PWD'] = new_path
        self._oldpwd = old_cwd
        self._env['OLDPWD'] = old_cwd
        self._last_result = CommandResult(data=new_path, text='', exit_code=0)
        return self

    def pushd(self, path: str) -> 'DagShell':
        """Push current directory onto stack and change to new directory.

        Usage:
            pushd PATH

        Options:
            PATH                   Directory to change to

        Examples:
            pushd /tmp             # Push cwd, go to /tmp
            pushd ~/projects       # Push cwd, go to projects

        Returns:
            Self for method chaining.
        """
        # Save current directory on stack
        self._dir_stack.append(self._cwd)

        # Change to new directory
        new_path = self._resolve_path(path)

        if not self.fs.exists(new_path):
            # Pop the directory we just pushed since cd failed
            self._dir_stack.pop()
            self._last_result = CommandResult(
                data=None,
                text=f"pushd: {path}: No such file or directory",
                exit_code=1
            )
            return self

        stat = self.fs.stat(new_path)
        if stat['type'] != 'dir':
            self._dir_stack.pop()
            self._last_result = CommandResult(
                data=None,
                text=f"pushd: {path}: Not a directory",
                exit_code=1
            )
            return self

        self._cwd = new_path
        self._env['PWD'] = new_path

        # Return stack info like bash does
        stack_display = ' '.join([new_path] + list(reversed(self._dir_stack)))
        self._last_result = CommandResult(data=self._dir_stack.copy(), text=stack_display, exit_code=0)
        return self

    def popd(self) -> 'DagShell':
        """Pop directory from stack and change to it.

        Usage:
            popd

        Examples:
            popd                   # Return to previously pushed directory

        Returns:
            Self for method chaining.
        """
        if not self._dir_stack:
            self._last_result = CommandResult(
                data=None,
                text="popd: directory stack empty",
                exit_code=1
            )
            return self

        # Pop directory from stack
        prev_dir = self._dir_stack.pop()

        # Change to it
        self._cwd = prev_dir
        self._env['PWD'] = prev_dir

        # Return stack info
        stack_display = ' '.join([prev_dir] + list(reversed(self._dir_stack)))
        self._last_result = CommandResult(data=self._dir_stack.copy(), text=stack_display, exit_code=0)
        return self

    def dirs(self) -> CommandResult:
        """Display directory stack.

        Usage:
            dirs

        Returns:
            Current directory stack.
        """
        stack = [self._cwd] + list(reversed(self._dir_stack))
        return self._make_result(stack, ' '.join(stack))

    # File operations

    def ls(self, path: Optional[str] = None,
           all: bool = False, long: bool = False) -> CommandResult:
        """List directory contents.

        Usage:
            ls [OPTIONS] [PATH]

        Options:
            -a, --all              Show hidden files (starting with .)
            -l, --long             Use long listing format
            PATH                   Directory to list (default: current)

        Examples:
            ls                     # List current directory
            ls /usr                # List /usr directory
            ls -a                  # Show all files including hidden
            ls -la /tmp            # Long format with hidden files

        Returns:
            List of files and directories.
        """
        target_path = self._resolve_path(path or self._cwd)

        if not self.fs.exists(target_path):
            return self._make_result(
                data=[],
                text=f"ls: {path}: No such file or directory",
                exit_code=2
            )

        stat = self.fs.stat(target_path)

        if stat['type'] == 'file':
            # If it's a file, just return the filename
            files = [os.path.basename(target_path)]
        else:
            # List directory contents
            files = self.fs.ls(target_path) or []

            # Filter hidden files unless -a flag
            if not all:
                files = [f for f in files if not f.startswith('.')]

        if long:
            # Detailed listing with actual permissions
            detailed = []
            for f in files:
                full_path = os.path.join(target_path, f)
                fstat = self.fs.stat(full_path)
                if fstat:
                    mode_str = self._format_mode(fstat.get('mode', 0o100644))
                    size = fstat.get('size', 0)
                    detailed.append(f"{mode_str}  1 user user {size:8} {f}")
            return self._make_result(data=files, text='\\n'.join(detailed))

        return self._make_result(data=files, text='\\n'.join(files))

    def tree(self, path: Optional[str] = None) -> CommandResult:
        """Display directory structure as a tree with ASCII art.

        Usage:
            tree [PATH]

        Options:
            PATH                   Directory to display (default: current)

        Examples:
            tree                   # Show current directory tree
            tree /project          # Show /project tree

        Returns:
            Tree representation of directory structure.
        """
        target_path = self._resolve_path(path or self._cwd)

        if not self.fs.exists(target_path):
            return self._make_result(
                data=[],
                text=f"tree: {path}: No such file or directory",
                exit_code=2
            )

        lines = [target_path]
        self._tree_recursive(target_path, '', lines)
        text = '\\n'.join(lines)
        return self._make_result(data=lines, text=text)

    def _tree_recursive(self, path: str, prefix: str, lines: List[str]) -> None:
        """Recursively build tree lines with ASCII art connectors."""
        entries = self.fs.ls(path) or []
        entries = [e for e in entries if not e.startswith('.')]
        entries.sort()
        for i, entry in enumerate(entries):
            is_last = (i == len(entries) - 1)
            connector = '\\u2514\\u2500\\u2500 ' if is_last else '\\u251c\\u2500\\u2500 '
            lines.append(f"{prefix}{connector}{entry}")
            full_path = os.path.join(path, entry)
            if self.fs.exists(full_path):
                stat = self.fs.stat(full_path)
                if stat and stat['type'] == 'dir':
                    extension = '    ' if is_last else '\\u2502   '
                    self._tree_recursive(full_path, prefix + extension, lines)

    def cat(self, *paths: str) -> CommandResult:
        """Concatenate and display files.

        Usage:
            cat [FILE...]

        Options:
            FILE                   File(s) to display

        Examples:
            cat file.txt           # Display contents of file.txt
            cat f1.txt f2.txt      # Concatenate multiple files
            echo "text" | cat      # Display piped input

        Returns:
            File contents as text.
        """
        if not paths:
            # Read from stdin (last result)
            if self._last_result:
                return self._last_result
            else:
                result = CommandResult(data=b'', text='', exit_code=0)
                self._last_result = result
                return result

        contents = []
        for path in paths:
            resolved = self._resolve_path(path)
            content = self.fs.read(resolved)
            if content is None:
                result = CommandResult(
                    data=b'',
                    text=f"cat: {path}: No such file or directory",
                    exit_code=1
                )
                self._last_result = result
                return result
            contents.append(content)

        data = b''.join(contents)
        # Provide text representation for bytes data
        text = data.decode('utf-8', errors='replace')
        return self._make_result(data=data, text=text)

    def echo(self, *args: str, n: bool = False) -> CommandResult:
        """Display a line of text.

        Usage:
            echo [OPTIONS] [STRING...]

        Options:
            -n                     Do not output trailing newline
            STRING                 Text to display

        Examples:
            echo "Hello World"     # Print Hello World
            echo -n "No newline"   # Print without newline
            echo $USER             # Print environment variable

        Returns:
            The echoed text.
        """
        text = ' '.join(str(arg) for arg in args)
        if not n:
            text += '\\n'
        return self._make_result(data=text.encode('utf-8'), text=text.rstrip())

    def touch(self, path: str) -> 'DagShell':
        """Create empty file or update timestamp.

        Usage:
            touch FILE

        Options:
            FILE                   File to create or update

        Examples:
            touch newfile.txt      # Create empty file
            touch existing.txt     # Update timestamp
            touch /tmp/marker      # Create file with absolute path

        Returns:
            Self for method chaining.
        """
        resolved = self._resolve_path(path)
        if not self.fs.exists(resolved):
            self.fs.write(resolved, b'')
        # TODO: Update mtime if exists
        return self

    def mkdir(self, path: str, parents: bool = False) -> 'DagShell':
        """Create directories.

        Usage:
            mkdir [OPTIONS] DIRECTORY

        Options:
            -p, --parents          Create parent directories as needed
            DIRECTORY              Directory name to create

        Examples:
            mkdir mydir            # Create directory
            mkdir -p a/b/c         # Create nested directories
            mkdir /tmp/test        # Create with absolute path

        Returns:
            Self for method chaining.
        """
        resolved = self._resolve_path(path)

        if parents:
            # Create parent directories as needed
            parts = PurePosixPath(resolved).parts
            current = '/'
            for part in parts[1:]:  # Skip root '/'
                current = os.path.join(current, part)
                if not self.fs.exists(current):
                    self.fs.mkdir(current)
        else:
            if not self.fs.mkdir(resolved):
                self._last_result = CommandResult(
                    data=None,
                    text=f"mkdir: cannot create directory '{path}': File exists or parent missing",
                    exit_code=1
                )
        return self

    def rm(self, path: str, recursive: bool = False, force: bool = False) -> 'DagShell':
        """Remove files or directories.

        Usage:
            rm [OPTIONS] FILE

        Options:
            -r, --recursive        Remove directories recursively
            -f, --force            Ignore nonexistent files
            FILE                   File or directory to remove

        Examples:
            rm file.txt            # Remove a file
            rm -r mydir            # Remove directory recursively
            rm -rf /tmp/cache      # Force remove directory

        Returns:
            Self for method chaining.
        """
        resolved = self._resolve_path(path)

        if not self.fs.exists(resolved) and not force:
            self._last_result = CommandResult(
                data=None,
                text=f"rm: cannot remove '{path}': No such file or directory",
                exit_code=1
            )
            return self

        if recursive:
            self._rm_recursive(resolved)
        else:
            self.fs.rm(resolved)
        return self

    def cp(self, src: str, dst: str) -> 'DagShell':
        """Copy files or directories.

        Usage:
            cp SOURCE DEST

        Options:
            SOURCE                 Source file/directory
            DEST                   Destination path

        Examples:
            cp file1.txt file2.txt # Copy file
            cp doc.txt /tmp/       # Copy to directory
            cp -r dir1 dir2        # Copy directory (when implemented)

        Returns:
            Self for method chaining.
        """
        src_path = self._resolve_path(src)
        dst_path = self._resolve_path(dst)

        content = self.fs.read(src_path)
        if content is None:
            self._last_result = CommandResult(
                data=None,
                text=f"cp: cannot stat '{src}': No such file or directory",
                exit_code=1
            )
            return self

        # Check if destination is a directory
        dst_stat = self.fs.stat(dst_path)
        if dst_stat and dst_stat['type'] == 'dir':
            # If dst is a directory, copy src into it with the same name
            src_name = os.path.basename(src_path)
            dst_path = os.path.join(dst_path, src_name)

        self.fs.write(dst_path, content)
        return self

    def mv(self, src: str, dst: str) -> 'DagShell':
        """Move or rename files and directories.

        Usage:
            mv SOURCE DEST

        Options:
            SOURCE                 Source file/directory
            DEST                   Destination path

        Examples:
            mv old.txt new.txt     # Rename file
            mv file.txt /tmp/      # Move to directory
            mv dir1 dir2           # Rename directory

        Returns:
            Self for method chaining.
        """
        src_path = self._resolve_path(src)
        dst_path = self._resolve_path(dst)

        if not self.fs.exists(src_path):
            self._last_result = CommandResult(
                data=None,
                text=f"mv: cannot stat '{src}': No such file or directory",
                exit_code=1
            )
            return self

        src_stat = self.fs.stat(src_path)

        # Check if destination is an existing directory
        dst_stat = self.fs.stat(dst_path)
        if dst_stat and dst_stat['type'] == 'dir':
            # Move source into the directory
            src_name = os.path.basename(src_path)
            dst_path = os.path.join(dst_path, src_name)

        if src_stat and src_stat['type'] == 'dir':
            # Moving a directory - need to recursively copy then delete
            self._copy_dir_recursive(src_path, dst_path)
            self._rm_recursive(src_path)
        else:
            # Moving a file - simple copy and delete
            content = self.fs.read(src_path)
            if content is not None:
                self.fs.write(dst_path, content)
                self.fs.rm(src_path)

        return self

    def _copy_dir_recursive(self, src: str, dst: str) -> None:
        """Recursively copy a directory."""
        # Create destination directory
        self.fs.mkdir(dst)

        # Copy all children
        children = self.fs.ls(src)
        if children:
            for child in children:
                src_child = os.path.join(src, child)
                dst_child = os.path.join(dst, child)
                child_stat = self.fs.stat(src_child)

                if child_stat and child_stat['type'] == 'dir':
                    self._copy_dir_recursive(src_child, dst_child)
                else:
                    content = self.fs.read(src_child)
                    if content is not None:
                        self.fs.write(dst_child, content)

    def _rm_recursive(self, path: str) -> None:
        """Recursively remove a directory and its contents."""
        stat = self.fs.stat(path)
        if stat and stat['type'] == 'dir':
            children = self.fs.ls(path)
            if children:
                for child in children:
                    child_path = os.path.join(path, child)
                    self._rm_recursive(child_path)
        self.fs.rm(path)

    def ln(self, target: str, link_name: str, symbolic: bool = False) -> 'DagShell':
        """Create links between files.

        Usage:
            ln [OPTIONS] TARGET LINK_NAME

        Options:
            -s, --symbolic         Create symbolic link instead of hard link
            TARGET                 The file/directory to link to
            LINK_NAME              Name of the link to create

        Examples:
            ln file.txt hardlink.txt        # Create hard link
            ln -s /etc/passwd pw            # Create symbolic link
            ln -s ../shared data            # Relative symbolic link

        Notes:
            Hard links share the same content hash in the DAG.
            Symbolic links store the target path and resolve at access time.

        Returns:
            Self for method chaining.
        """
        resolved_target = self._resolve_path(target)
        link_path = self._resolve_path(link_name)

        if symbolic:
            # Create symbolic link - target is stored as-is (can be relative)
            if not self.fs.symlink(target, link_path):
                self._last_result = CommandResult(
                    data=None,
                    text=f"ln: failed to create symbolic link '{link_name}'",
                    exit_code=1
                )
        else:
            # Create hard link - both paths point to same content hash
            if not self.fs.exists(resolved_target):
                self._last_result = CommandResult(
                    data=None,
                    text=f"ln: failed to access '{target}': No such file or directory",
                    exit_code=1
                )
                return self

            stat = self.fs.stat(resolved_target)
            if stat and stat['type'] == 'dir':
                self._last_result = CommandResult(
                    data=None,
                    text=f"ln: '{target}': hard link not allowed for directory",
                    exit_code=1
                )
                return self

            # Read content and write to new path (same hash = hard link in DAG)
            content = self.fs.read(resolved_target)
            if content is not None:
                self.fs.write(link_path, content)

        return self

    def readlink(self, path: str) -> CommandResult:
        """Print the target of a symbolic link.

        Usage:
            readlink SYMLINK

        Examples:
            readlink /usr/bin/python    # Show where symlink points

        Returns:
            The symlink target path.
        """
        resolved = self._resolve_path(path)
        target = self.fs.readlink(resolved)

        if target is None:
            return self._make_result(
                None,
                f"readlink: {path}: Not a symbolic link",
                exit_code=1
            )

        return self._make_result(target, target)

    def chmod(self, mode: str, path: str) -> 'DagShell':
        """Change file mode bits.

        Usage:
            chmod MODE PATH

        Options:
            MODE                   Octal mode (e.g., 755, 644) or symbolic (e.g., +x, u+w)
            PATH                   File or directory to modify

        Examples:
            chmod 755 script.sh            # Set rwxr-xr-x
            chmod 644 file.txt             # Set rw-r--r--
            chmod +x script.sh             # Add execute for all
            chmod u+w,g-w file.txt         # Symbolic mode

        Returns:
            Self for method chaining.
        """
        resolved = self._resolve_path(path)

        # Parse mode - handle octal or symbolic
        if mode.isdigit() or (mode.startswith('0') and mode[1:].isdigit()):
            # Octal mode
            new_mode = int(mode, 8)
        else:
            # Symbolic mode - parse +x, u+w, etc.
            stat = self.fs.stat(resolved)
            if not stat:
                self._last_result = CommandResult(
                    data=None,
                    text=f"chmod: cannot access '{path}': No such file or directory",
                    exit_code=1
                )
                return self

            current_mode = stat['mode'] & 0o777
            new_mode = self._parse_symbolic_mode(mode, current_mode)

        if not self.fs.chmod(resolved, new_mode):
            self._last_result = CommandResult(
                data=None,
                text=f"chmod: cannot access '{path}': No such file or directory",
                exit_code=1
            )
        return self

    def _parse_symbolic_mode(self, mode_str: str, current: int) -> int:
        """Parse symbolic mode string like +x, u+w, go-r."""
        result = current

        for part in mode_str.split(','):
            part = part.strip()
            if not part:
                continue

            # Find operation (+, -, =)
            op_idx = -1
            op = None
            for i, c in enumerate(part):
                if c in '+-=':
                    op_idx = i
                    op = c
                    break

            if op is None:
                continue

            who = part[:op_idx] if op_idx > 0 else 'a'
            perms = part[op_idx + 1:]

            # Build mask for who
            mask = 0
            if 'u' in who or 'a' in who:
                mask |= 0o700
            if 'g' in who or 'a' in who:
                mask |= 0o070
            if 'o' in who or 'a' in who:
                mask |= 0o007

            # Build permission bits
            perm_bits = 0
            for p in perms:
                if p == 'r':
                    perm_bits |= 0o444
                elif p == 'w':
                    perm_bits |= 0o222
                elif p == 'x':
                    perm_bits |= 0o111

            perm_bits &= mask

            if op == '+':
                result |= perm_bits
            elif op == '-':
                result &= ~perm_bits
            elif op == '=':
                result = (result & ~mask) | perm_bits

        return result

    def chown(self, owner: str, path: str) -> 'DagShell':
        """Change file owner and group.

        Usage:
            chown OWNER[:GROUP] PATH

        Options:
            OWNER                  New owner (username or uid)
            GROUP                  New group (groupname or gid)
            PATH                   File or directory to modify

        Examples:
            chown alice file.txt           # Change owner to alice
            chown alice:developers dir/    # Change owner and group
            chown :staff file.txt          # Change group only
            chown 1000:1000 file.txt       # Use numeric IDs

        Returns:
            Self for method chaining.
        """
        resolved = self._resolve_path(path)

        # Parse owner:group
        if ':' in owner:
            user_part, group_part = owner.split(':', 1)
        else:
            user_part = owner
            group_part = None

        # Resolve user
        uid = None
        if user_part:
            if user_part.isdigit():
                uid = int(user_part)
            else:
                uid, _ = self.fs.lookup_user(user_part)

        # Resolve group
        gid = None
        if group_part:
            if group_part.isdigit():
                gid = int(group_part)
            else:
                # Look up group in /etc/group
                gid = self._lookup_group(group_part)

        if not self.fs.chown(resolved, uid, gid):
            self._last_result = CommandResult(
                data=None,
                text=f"chown: cannot access '{path}': No such file or directory",
                exit_code=1
            )
        return self

    def _lookup_group(self, groupname: str) -> Optional[int]:
        """Look up group ID from /etc/group."""
        content = self.fs.read('/etc/group')
        if content:
            for line in content.decode('utf-8').strip().split('\\n'):
                parts = line.split(':')
                if len(parts) >= 3 and parts[0] == groupname:
                    return int(parts[2])
        return None

    # Text processing methods

    def grep(self, pattern: str, *paths: str,
             ignore_case: bool = False, invert: bool = False) -> CommandResult:
        """Search for patterns in files or input.

        Usage:
            grep [OPTIONS] PATTERN [FILE...]

        Options:
            -i, --ignore-case      Ignore case distinctions
            -v, --invert           Select non-matching lines
            PATTERN                Regular expression pattern
            FILE                   File(s) to search (default: stdin)

        Examples:
            grep "error" log.txt   # Find lines with "error"
            grep -i "ERROR" *.log  # Case-insensitive search
            ls | grep ".txt"       # Filter ls output
            grep -v "#" config     # Show non-comment lines

        Returns:
            Lines matching the pattern.
        """
        # Prepare regex
        re_flags = re.IGNORECASE if ignore_case else 0
        try:
            regex = re.compile(pattern, re_flags)
        except re.error as e:
            return self._make_result(
                data=[],
                text=f"grep: invalid regex: {e}",
                exit_code=2
            )

        lines = self._read_input_lines(paths)

        # Filter lines
        matches = []
        for line in lines:
            found = regex.search(line) is not None
            if found != invert:
                matches.append(line)

        return self._make_result(data=matches, text='\\n'.join(matches))

    def head(self, n: int = 10, *paths: str) -> CommandResult:
        """Display first lines of a file.

        Usage:
            head [OPTIONS] [FILE...]

        Options:
            -n NUM                 Number of lines to display (default: 10)
            FILE                   File(s) to read (default: stdin)

        Examples:
            head file.txt          # Show first 10 lines
            head -n 5 file.txt     # Show first 5 lines
            cat log | head -20     # Show first 20 lines of piped input

        Returns:
            The first n lines of input.
        """
        lines = self._read_input_lines(paths)

        # Take first n lines
        head_lines = lines[:n]
        return self._make_result(data=head_lines, text='\\n'.join(head_lines))

    def tail(self, n: int = 10, *paths: str) -> CommandResult:
        """Display last lines of a file.

        Usage:
            tail [OPTIONS] [FILE...]

        Options:
            -n NUM                 Number of lines to display (default: 10)
            FILE                   File(s) to read (default: stdin)

        Examples:
            tail file.txt          # Show last 10 lines
            tail -n 20 error.log   # Show last 20 lines
            dmesg | tail -5        # Show last 5 lines of piped input

        Returns:
            The last n lines of input.
        """
        lines = self._read_input_lines(paths)

        # Take last n lines (handle n=0 case explicitly since -0 == 0 in Python)
        tail_lines = lines[-n:] if lines and n > 0 else []
        return self._make_result(data=tail_lines, text='\\n'.join(tail_lines))

    def wc(self, *paths: str, lines: bool = False,
           words: bool = False, chars: bool = False) -> CommandResult:
        """Print line, word, and character counts.

        Usage:
            wc [OPTIONS] [FILE...]

        Options:
            -l, --lines            Print line count
            -w, --words            Print word count
            -c, --chars            Print character count
            FILE                   File(s) to count (default: stdin)

        Examples:
            wc file.txt            # Count lines in file
            wc -l *.py             # Count lines in Python files
            wc -w document.txt     # Count words
            echo "test" | wc -c    # Count characters in piped input

        Returns:
            Count statistics.
        """
        # Get input
        if paths:
            text_data = []
            for path in paths:
                resolved = self._resolve_path(path)
                content = self.fs.read(resolved)
                if content:
                    text_data.append(content.decode('utf-8', errors='replace'))
            text = '\\n'.join(text_data)
        else:
            if self._last_result:
                text = str(self._last_result)
            else:
                text = ''

        # Count - if no flags given, show all three
        show_all = not lines and not words and not chars
        counts = {}
        if lines or show_all:
            # Count lines properly - wc counts non-empty content as at least 1 line
            if text:
                counts['lines'] = text.count('\\n') + (1 if not text.endswith('\\n') else 0)
            else:
                counts['lines'] = 0
        if words or show_all:
            counts['words'] = len(text.split())
        if chars or show_all:
            counts['chars'] = len(text)

        # Format output
        if len(counts) == 1:
            value = list(counts.values())[0]
            return self._make_result(data=value, text=str(value))

        text_parts = []
        for key in ('lines', 'words', 'chars'):
            if key in counts:
                text_parts.append(str(counts[key]))
        return self._make_result(data=counts, text=' '.join(text_parts))

    def sort(self, *paths: str, reverse: bool = False,
             numeric: bool = False, unique: bool = False) -> CommandResult:
        """Sort lines of text files.

        Usage:
            sort [OPTIONS] [FILE...]

        Options:
            -r, --reverse          Sort in reverse order
            -n, --numeric          Sort numerically
            -u, --unique           Remove duplicate lines
            FILE                   File(s) to sort (default: stdin)

        Examples:
            sort names.txt         # Sort alphabetically
            sort -r file.txt       # Sort in reverse
            sort -n numbers.txt    # Sort numerically
            ls | sort -r           # Sort ls output in reverse

        Returns:
            Sorted lines.
        """
        lines = self._read_input_lines(paths)

        # Sort
        if numeric:
            def key_func(x):
                try:
                    return float(x)
                except ValueError:
                    return float('inf')
        else:
            key_func = str.lower

        sorted_lines = sorted(lines, key=key_func, reverse=reverse)

        # Remove duplicates if requested
        if unique:
            seen = set()
            unique_lines = []
            for line in sorted_lines:
                if line not in seen:
                    seen.add(line)
                    unique_lines.append(line)
            sorted_lines = unique_lines

        return self._make_result(data=sorted_lines, text='\\n'.join(sorted_lines))

    def uniq(self, *paths: str, count: bool = False) -> CommandResult:
        """Report or omit repeated lines.

        Usage:
            uniq [OPTIONS] [FILE...]

        Options:
            -c, --count            Prefix lines with occurrence count
            FILE                   File(s) to process (default: stdin)

        Examples:
            uniq file.txt          # Remove consecutive duplicates
            sort file | uniq       # Remove all duplicates (after sort)
            uniq -c data.txt       # Count occurrences

        Returns:
            Unique lines.
        """
        lines = self._read_input_lines(paths)

        # Remove consecutive duplicates
        unique_lines = []
        counts = []
        last_line = None
        current_count = 0

        for line in lines:
            if line != last_line:
                if last_line is not None:
                    unique_lines.append(last_line)
                    counts.append(current_count)
                last_line = line
                current_count = 1
            else:
                current_count += 1

        if last_line is not None:
            unique_lines.append(last_line)
            counts.append(current_count)

        # Format output
        if count:
            output_lines = [f"{c:7} {line}" for c, line in zip(counts, unique_lines)]
            return self._make_result(
                data=list(zip(counts, unique_lines)),
                text='\\n'.join(output_lines)
            )

        return self._make_result(data=unique_lines, text='\\n'.join(unique_lines))

    def cut(self, *paths: str, delimiter: str = '\\t', fields: str = '1') -> CommandResult:
        """Remove sections from each line.

        Usage:
            cut [OPTIONS] [FILE...]

        Options:
            -d, --delimiter        Field delimiter (default: tab)
            -f, --fields           Field numbers to extract (e.g., 1,3 or 1-3)

        Examples:
            cut -d: -f1 /etc/passwd     # Get usernames
            cut -f2,4 data.tsv          # Get columns 2 and 4
            echo "a:b:c" | cut -d: -f2  # Get second field

        Returns:
            Selected fields from each line.
        """
        # Get input lines
        if paths:
            lines = []
            for p in paths:
                resolved = self._resolve_path(p)
                content = self.fs.read(resolved)
                if content:
                    lines.extend(content.decode('utf-8').splitlines())
        elif self._last_result and self._last_result.text:
            lines = self._last_result.text.splitlines()
        else:
            lines = []

        # Parse field specification
        field_indices = self._parse_field_spec(fields)

        # Process each line
        output_lines = []
        for line in lines:
            parts = line.split(delimiter)
            selected = []
            for idx in field_indices:
                if 0 < idx <= len(parts):
                    selected.append(parts[idx - 1])
            output_lines.append(delimiter.join(selected))

        return self._make_result(output_lines, '\\n'.join(output_lines))

    def _parse_field_spec(self, spec: str) -> List[int]:
        """Parse field specification like 1,3 or 1-3."""
        fields = []
        for part in spec.split(','):
            if '-' in part:
                start, end = part.split('-', 1)
                start = int(start) if start else 1
                end = int(end) if end else 100
                fields.extend(range(start, end + 1))
            else:
                fields.append(int(part))
        return fields

    def tr(self, set1: str, set2: str = '', delete: bool = False) -> CommandResult:
        """Translate or delete characters.

        Usage:
            tr SET1 [SET2]

        Options:
            SET1                   Characters to translate from (or delete)
            SET2                   Characters to translate to
            -d, --delete           Delete characters in SET1

        Examples:
            echo "hello" | tr a-z A-Z    # Convert to uppercase
            echo "hello" | tr -d aeiou   # Delete vowels
            tr ' ' '_'                   # Replace spaces with underscores

        Returns:
            Translated text.
        """
        # Get input
        if self._last_result and self._last_result.text:
            text = self._last_result.text
        else:
            text = ''

        if delete:
            # Delete mode - remove all characters in set1
            set1_chars = self._expand_char_range(set1)
            result_text = ''.join(c for c in text if c not in set1_chars)
        else:
            # Translate mode
            # Handle character ranges like a-z
            set1_chars = self._expand_char_range(set1)
            set2_chars = self._expand_char_range(set2)

            # Build translation table
            if len(set2_chars) < len(set1_chars):
                # Extend set2 with its last char
                set2_chars += set2_chars[-1:] * (len(set1_chars) - len(set2_chars))

            trans = str.maketrans(set1_chars, set2_chars[:len(set1_chars)])
            result_text = text.translate(trans)

        return self._make_result(result_text, result_text)

    def _expand_char_range(self, s: str) -> str:
        """Expand character ranges like a-z."""
        result = []
        i = 0
        while i < len(s):
            if i + 2 < len(s) and s[i + 1] == '-':
                # Range like a-z
                start, end = ord(s[i]), ord(s[i + 2])
                result.extend(chr(c) for c in range(start, end + 1))
                i += 3
            else:
                result.append(s[i])
                i += 1
        return ''.join(result)

    def du(self, *paths: str, human_readable: bool = False) -> CommandResult:
        """Estimate file space usage.

        Usage:
            du [OPTIONS] [PATH...]

        Options:
            -h, --human-readable   Print sizes in human-readable format
            PATH                   Paths to analyze (default: current directory)

        Examples:
            du /home               # Show usage of /home
            du -h                  # Human-readable current directory

        Returns:
            Disk usage for each path.
        """
        if not paths:
            paths = [self._cwd]

        results = []
        output_lines = []

        for path in paths:
            resolved = self._resolve_path(path)
            size = self._calculate_size(resolved)

            if human_readable:
                size_str = self._format_size(size)
            else:
                size_str = str(size)

            results.append({'path': path, 'size': size})
            output_lines.append(f"{size_str}\\t{path}")

        return self._make_result(results, '\\n'.join(output_lines))

    def _calculate_size(self, path: str) -> int:
        """Calculate total size of a path (recursive for directories)."""
        stat = self.fs.stat(path)
        if not stat:
            return 0

        if stat['type'] == 'file':
            return stat.get('size', 0)
        elif stat['type'] == 'dir':
            total = 0
            children = self.fs.ls(path)
            if children:
                for child in children:
                    child_path = os.path.join(path, child)
                    total += self._calculate_size(child_path)
            return total
        else:
            return 0

    def _format_size(self, size: int) -> str:
        """Format size in human-readable form."""
        for unit in ['B', 'K', 'M', 'G', 'T']:
            if size < 1024:
                return f"{size}{unit}"
            size //= 1024
        return f"{size}P"

    def diff(self, file1: str, file2: str, unified: bool = False,
             context: int = 3) -> CommandResult:
        """Compare files line by line.

        Usage:
            diff [OPTIONS] FILE1 FILE2

        Options:
            -u, --unified          Output unified diff format
            -c N, --context N      Number of context lines (default: 3)

        Examples:
            diff file1.txt file2.txt       # Compare two files
            diff -u old.py new.py          # Unified diff format

        Returns:
            Differences between files, or empty if identical.
        """
        resolved1 = self._resolve_path(file1)
        resolved2 = self._resolve_path(file2)

        content1 = self.fs.read(resolved1)
        content2 = self.fs.read(resolved2)

        if content1 is None:
            return self._make_result(
                {'error': f"diff: {file1}: No such file"},
                f"diff: {file1}: No such file",
                exit_code=1
            )
        if content2 is None:
            return self._make_result(
                {'error': f"diff: {file2}: No such file"},
                f"diff: {file2}: No such file",
                exit_code=1
            )

        lines1 = content1.decode('utf-8', errors='replace').splitlines(keepends=True)
        lines2 = content2.decode('utf-8', errors='replace').splitlines(keepends=True)

        # Ensure lines end with newline for proper diff format
        if lines1 and not lines1[-1].endswith('\\n'):
            lines1[-1] += '\\n'
        if lines2 and not lines2[-1].endswith('\\n'):
            lines2[-1] += '\\n'

        if unified:
            diff_result = list(difflib.unified_diff(
                lines1, lines2,
                fromfile=file1, tofile=file2,
                n=context
            ))
        else:
            # Simple diff output
            diff_result = []
            matcher = difflib.SequenceMatcher(None, lines1, lines2)
            for tag, i1, i2, j1, j2 in matcher.get_opcodes():
                if tag == 'replace':
                    for i in range(i1, i2):
                        diff_result.append(f"< {lines1[i].rstrip()}\\n")
                    diff_result.append("---\\n")
                    for j in range(j1, j2):
                        diff_result.append(f"> {lines2[j].rstrip()}\\n")
                elif tag == 'delete':
                    for i in range(i1, i2):
                        diff_result.append(f"< {lines1[i].rstrip()}\\n")
                elif tag == 'insert':
                    for j in range(j1, j2):
                        diff_result.append(f"> {lines2[j].rstrip()}\\n")

        output = ''.join(diff_result).rstrip('\\n')
        exit_code = 1 if diff_result else 0  # diff returns 1 if differences found

        return self._make_result(
            {'file1': file1, 'file2': file2, 'differences': len(diff_result) > 0},
            output,
            exit_code=exit_code
        )

    def basename(self, path: str, suffix: str = '') -> CommandResult:
        """Strip directory and suffix from filenames.

        Usage:
            basename PATH [SUFFIX]

        Options:
            PATH                   The path to process
            SUFFIX                 Optional suffix to remove

        Examples:
            basename /usr/bin/sort          # Output: sort
            basename include/stdio.h .h     # Output: stdio
            basename /home/user/            # Output: user

        Returns:
            The base name of the path.
        """
        # Get base name
        result = os.path.basename(path.rstrip('/'))

        # Remove suffix if specified
        if suffix and result.endswith(suffix):
            result = result[:-len(suffix)]

        return self._make_result(result, result)

    def dirname(self, path: str) -> CommandResult:
        """Strip last component from file name.

        Usage:
            dirname PATH

        Options:
            PATH                   The path to process

        Examples:
            dirname /usr/bin/sort           # Output: /usr/bin
            dirname stdio.h                 # Output: .
            dirname /home/user/             # Output: /home

        Returns:
            The directory portion of the path.
        """
        result = os.path.dirname(path.rstrip('/'))
        if not result:
            result = '.'

        return self._make_result(result, result)

    def xargs(self, command: str, *args, max_args: int = 0) -> CommandResult:
        """Build and execute commands from standard input.

        Usage:
            ... | xargs COMMAND [ARGS]

        Options:
            COMMAND                Command to execute
            ARGS                   Additional arguments
            -n N                   Use at most N arguments per command

        Examples:
            find . -name "*.txt" | xargs cat     # Cat all txt files
            echo "a b c" | xargs mkdir           # Create directories a, b, c
            ls | xargs -n 1 echo                 # Echo each file

        Returns:
            Combined output of all command executions.
        """
        # Get input from pipe
        if not self._last_result or not self._last_result.text:
            return self._make_result('', '')

        # Split input into arguments
        input_args = self._last_result.text.strip().split()

        if not input_args:
            return self._make_result('', '')

        # Get the method to call
        method = getattr(self, command, None)
        if not method:
            return self._make_result(
                {'error': f"xargs: {command}: command not found"},
                f"xargs: {command}: command not found",
                exit_code=127
            )

        results = []
        if max_args > 0:
            # Execute in batches
            for i in range(0, len(input_args), max_args):
                batch = input_args[i:i + max_args]
                try:
                    result = method(*list(args) + batch)
                    if isinstance(result, CommandResult):
                        text = result.text if result.text else str(result)
                        if text:
                            results.append(text)
                except Exception as e:
                    results.append(f"xargs: {command}: {str(e)}")
        else:
            # Execute all at once
            try:
                result = method(*list(args) + input_args)
                if isinstance(result, CommandResult):
                    text = result.text if result.text else str(result)
                    if text:
                        results.append(text)
            except Exception as e:
                results.append(f"xargs: {command}: {str(e)}")

        output = '\\n'.join(results)
        return self._make_result(results if results else '', output if output else '')

    # Piping and chaining support

    def pipe(self, func: Callable[['DagShell'], CommandResult]) -> CommandResult:
        """Pipe last result through a function."""
        return func(self)

    def tee(self, path: str) -> CommandResult:
        """Read from stdin and write to file and stdout.

        Usage:
            command | tee FILE

        Options:
            FILE                   Output file

        Examples:
            ls | tee list.txt      # Save ls output and display
            echo "log" | tee -a log.txt  # Append and display

        Returns:
            The input data (passes through).
        """
        if self._last_result:
            # Resolve path relative to current directory
            resolved = self._resolve_path(path)
            # Write directly to filesystem
            content = bytes(self._last_result)
            self.fs.write(resolved, content)
        return self._last_result or CommandResult(data=b'', text='')

    # Advanced features

    def find(self, path: Optional[str] = None, name: Optional[str] = None,
             type: Optional[str] = None, maxdepth: Optional[int] = None) -> CommandResult:
        """Search for files and directories.

        Usage:
            find [PATH] [OPTIONS]

        Options:
            PATH                   Starting directory (default: current)
            -name PATTERN          Find by name pattern (supports wildcards)
            -type TYPE             Find by type (f=file, d=directory)
            -maxdepth N            Maximum search depth

        Examples:
            find                   # List all files/dirs from current
            find /usr -name "*.so" # Find .so files in /usr
            find . -type f         # Find only files
            find -maxdepth 2       # Search only 2 levels deep

        Returns:
            List of matching paths.
        """
        start_path = self._resolve_path(path or self._cwd)
        found = []

        def search(current_path: str, depth: int = 0):
            if maxdepth is not None and depth > maxdepth:
                return

            entries = self.fs.ls(current_path) or []
            for entry in entries:
                full_path = os.path.join(current_path, entry)
                stat = self.fs.stat(full_path)

                if stat:
                    # Check type filter
                    if type:
                        if type == 'f' and stat['type'] != 'file':
                            continue
                        elif type == 'd' and stat['type'] != 'dir':
                            continue

                    # Check name filter
                    if name and not fnmatch.fnmatch(entry, name):
                        if stat['type'] == 'dir':
                            # Still recurse into directories even if name doesn't match
                            search(full_path, depth + 1)
                        continue

                    found.append(full_path)

                    # Recurse into directories
                    if stat['type'] == 'dir':
                        search(full_path, depth + 1)

        # Start search
        if self.fs.exists(start_path):
            stat = self.fs.stat(start_path)

            # For directories, include it if no name filter or name matches
            if stat['type'] == 'dir':
                include_start = True

                # Check type filter
                if type and type == 'f':  # Looking for files only
                    include_start = False

                # Check name filter only if we have one and we're including dirs
                if include_start and name:
                    dir_name = os.path.basename(start_path) if start_path != '/' else '/'
                    if not fnmatch.fnmatch(dir_name, name):
                        include_start = False

                if include_start:
                    found.append(start_path)

                # Always search inside directories
                search(start_path, 1)
            else:
                # It's a file - include if criteria match
                if (not type or type == 'f'):
                    if not name or fnmatch.fnmatch(os.path.basename(start_path), name):
                        found.append(start_path)

        return self._make_result(data=found, text='\\n'.join(found))

    def save(self, filename: str = 'dagshell.json') -> CommandResult:
        """Save virtual filesystem to JSON file.

        Usage:
            save [FILENAME]

        Options:
            FILENAME               Output file (default: dagshell.json)

        Examples:
            save                   # Save to dagshell.json
            save backup.json       # Save to specific file
            save /tmp/fs.json      # Save with absolute path

        Returns:
            Status message.
        """
        try:
            # Get JSON representation
            json_data = self.fs.to_json()

            # Write to real file (not virtual filesystem)
            with open(filename, 'w') as f:
                f.write(json_data)

            result = f"Filesystem saved to {filename}"
            return CommandResult(data=result, text=result, exit_code=0)
        except Exception as e:
            error = f"Failed to save: {e}"
            return CommandResult(data=error, text=error, exit_code=1)

    def load(self, filename: str = 'dagshell.json') -> CommandResult:
        """Load virtual filesystem from JSON file.

        Usage:
            load [FILENAME]

        Options:
            FILENAME               Input file (default: dagshell.json)

        Examples:
            load                   # Load from dagshell.json
            load backup.json       # Load from specific file
            load /tmp/fs.json      # Load from absolute path

        Returns:
            Status message.
        """
        try:
            # Read from real file
            with open(filename, 'r') as f:
                json_data = f.read()

            # Create new filesystem from JSON
            new_fs = dagshell.FileSystem.from_json(json_data)

            # Replace current filesystem
            self.fs = new_fs

            # Reset to root directory
            self._cwd = '/'

            result = f"Filesystem loaded from {filename}"
            return CommandResult(data=result, text=result, exit_code=0)
        except FileNotFoundError:
            error = f"File not found: {filename}"
            return CommandResult(data=error, text=error, exit_code=1)
        except Exception as e:
            error = f"Failed to load: {e}"
            return CommandResult(data=error, text=error, exit_code=1)

    def commit(self, filename: str = 'dagshell.json') -> CommandResult:
        """Commit filesystem to JSON file (alias for save).

        Usage:
            commit [FILENAME]

        Options:
            FILENAME               Output file (default: dagshell.json)

        Examples:
            commit                 # Save to dagshell.json
            commit state.json      # Save to specific file

        Returns:
            Status message.
        """
        return self.save(filename)

    def export(self, target_path: str, preserve_permissions: bool = True) -> CommandResult:
        """Export virtual filesystem to real filesystem.

        Usage:
            export TARGET_PATH [OPTIONS]

        Options:
            TARGET_PATH            Real directory to export to
            --no-preserve-perms    Don't preserve file permissions

        Examples:
            export /tmp/export     # Export to /tmp/export
            export ~/backup        # Export to home directory

        Returns:
            Export status with file count.
        """
        try:
            exported = self.fs.export_to_real(target_path, preserve_permissions)
            result = f"Exported {exported} files/directories to {target_path}"
            return CommandResult(data=exported, text=result, exit_code=0)
        except Exception as e:
            error = f"Export failed: {e}"
            return CommandResult(data=error, text=error, exit_code=1)

    def su(self, username: str = 'root') -> CommandResult:
        """Switch user account.

        Usage:
            su [USERNAME]

        Options:
            USERNAME               User to switch to (default: root)

        Examples:
            su                     # Switch to root
            su alice               # Switch to user alice
            su bob                 # Switch to user bob

        Returns:
            Status message.
        """
        # This is mainly for terminal session use
        # The fluent API doesn't track user context by default
        result = f"Switched to user: {username}"
        return CommandResult(data=username, text=result, exit_code=0)

    def import_file(self, real_path: str, virtual_path: Optional[str] = None,
                    safe_paths: Optional[List[str]] = None, recursive: bool = True) -> CommandResult:
        """Import a file from the real filesystem into the virtual filesystem.

        Usage:
            import_file REAL_PATH [VIRTUAL_PATH]

        Options:
            REAL_PATH              Path to file on real filesystem
            VIRTUAL_PATH           Path in virtual filesystem (default: same as filename)
            --safe-paths          List of allowed directory prefixes
            --recursive           Import directories recursively (default: True)

        Examples:
            import_file /etc/hosts               # Import to /hosts
            import_file ~/code/script.py /scripts/script.py
            import_file ./data.csv /data/input.csv
            import_file ~/project /proj --recursive=True  # Import entire directory tree

        Security:
            By default, imports are restricted to current directory and below.
            Use safe_paths to specify allowed directories.

        Returns:
            Status message or error.
        """
        # Default safe paths if not specified
        if safe_paths is None:
            safe_paths = [
                os.getcwd(),  # Current directory
                os.path.expanduser('~/'),  # Home directory
                '/tmp',  # Temp directory
            ]

        # Resolve real path
        real_path = os.path.abspath(os.path.expanduser(real_path))

        # Security check - ensure path is within safe paths
        is_safe = any(
            real_path.startswith(os.path.abspath(safe))
            for safe in safe_paths
        )

        if not is_safe:
            result = (f"import_file: {real_path}: Permission denied. "
                     f"Path not in safe paths: {safe_paths}")
            return CommandResult(data=None, text=result, exit_code=1)

        # Check if file exists
        if not os.path.exists(real_path):
            result = f"import_file: {real_path}: No such file or directory"
            return CommandResult(data=None, text=result, exit_code=1)

        # Determine virtual path
        if virtual_path is None:
            virtual_path = '/' + os.path.basename(real_path)
        else:
            virtual_path = self._resolve_path(virtual_path)
            # If importing a file and virtual_path is an existing directory,
            # append the original filename (like cp behavior)
            if os.path.isfile(real_path) and self.fs.exists(virtual_path):
                stat_info = self.fs.stat(virtual_path)
                if stat_info and stat_info.get('type') == 'dir':
                    virtual_path = os.path.join(virtual_path, os.path.basename(real_path))

        try:
            if os.path.isfile(real_path):
                # Import single file
                # Ensure parent directory exists (create recursively)
                parent_dir = os.path.dirname(virtual_path)
                if parent_dir and parent_dir != '/':
                    # Create parent directories recursively
                    self._ensure_parent_dirs(parent_dir)

                with open(real_path, 'rb') as f:
                    content = f.read()
                self.fs.write(virtual_path, content)
                result = f"Imported file: {real_path} -> {virtual_path}"

            elif os.path.isdir(real_path):
                # Import directory
                imported = []
                if recursive:
                    # Import recursively using os.walk
                    for root, dirs, files in os.walk(real_path):
                        # Calculate relative path
                        rel_root = os.path.relpath(root, real_path)
                        if rel_root == '.':
                            virt_root = virtual_path
                        else:
                            virt_root = os.path.join(virtual_path, rel_root)

                        # Create directory
                        self.fs.mkdir(virt_root)

                        # Import files
                        for file in files:
                            real_file = os.path.join(root, file)
                            virt_file = os.path.join(virt_root, file)

                            with open(real_file, 'rb') as f:
                                content = f.read()
                            self.fs.write(virt_file, content)
                            imported.append(virt_file)
                else:
                    # Import only top-level files (non-recursive)
                    self.fs.mkdir(virtual_path)
                    for item in os.listdir(real_path):
                        item_path = os.path.join(real_path, item)
                        if os.path.isfile(item_path):
                            virt_file = os.path.join(virtual_path, item)
                            with open(item_path, 'rb') as f:
                                content = f.read()
                            self.fs.write(virt_file, content)
                            imported.append(virt_file)

                result = (f"Imported directory: {real_path} -> {virtual_path}\\n"
                         f"Files imported: {len(imported)}")
            else:
                result = f"import_file: {real_path}: Not a regular file or directory"
                return CommandResult(data=None, text=result, exit_code=1)

            return CommandResult(data={'imported': real_path, 'to': virtual_path},
                               text=result, exit_code=0)

        except Exception as e:
            result = f"import_file: {real_path}: Error: {e}"
            return CommandResult(data=None, text=result, exit_code=1)

    def export_file(self, virtual_path: str, real_path: Optional[str] = None,
                    safe_paths: Optional[List[str]] = None, recursive: bool = True) -> CommandResult:
        """Export a file from the virtual filesystem to the real filesystem.

        Usage:
            export_file VIRTUAL_PATH [REAL_PATH]

        Options:
            VIRTUAL_PATH           Path in virtual filesystem
            REAL_PATH             Path on real filesystem (default: current dir)
            --safe-paths          List of allowed directory prefixes
            --recursive           Export directories recursively (default: True)

        Examples:
            export_file /data/results.csv       # Export to ./results.csv
            export_file /config.json ~/config.json
            export_file /project ./exported_project --recursive=True

        Security:
            By default, exports are restricted to current directory and below.
            Use safe_paths to specify allowed directories.

        Returns:
            Status message or error.
        """
        # Default safe paths if not specified
        if safe_paths is None:
            safe_paths = [
                os.getcwd(),  # Current directory
                os.path.expanduser('~/'),  # Home directory
                '/tmp',  # Temp directory
            ]

        # Resolve virtual path
        virtual_path = self._resolve_path(virtual_path)

        # Check if virtual file exists
        if not self.fs.exists(virtual_path):
            result = f"export_file: {virtual_path}: No such file or directory"
            return CommandResult(data=None, text=result, exit_code=1)

        # Determine real path
        if real_path is None:
            real_path = os.path.join(os.getcwd(), os.path.basename(virtual_path))
        else:
            real_path = os.path.abspath(os.path.expanduser(real_path))

        # Security check
        is_safe = any(
            real_path.startswith(os.path.abspath(safe))
            for safe in safe_paths
        )

        if not is_safe:
            result = (f"export_file: {real_path}: Permission denied. "
                     f"Path not in safe paths: {safe_paths}")
            return CommandResult(data=None, text=result, exit_code=1)

        try:
            # Get stat info to determine file type
            stat_info = self.fs.stat(virtual_path)
            if not stat_info:
                result = f"export_file: {virtual_path}: No such file or directory"
                return CommandResult(data=None, text=result, exit_code=1)

            if stat_info['type'] == 'file':
                # Export single file
                content = self.fs.read(virtual_path)
                os.makedirs(os.path.dirname(real_path), exist_ok=True)
                with open(real_path, 'wb') as f:
                    f.write(content)
                result = f"Exported file: {virtual_path} -> {real_path}"

            elif stat_info['type'] == 'dir':
                # Export directory
                exported = []
                os.makedirs(real_path, exist_ok=True)

                if recursive:
                    # Export recursively
                    def export_recursive_helper(vpath, rpath):
                        os.makedirs(rpath, exist_ok=True)
                        for item in self.fs.ls(vpath) or []:
                            vitem = os.path.join(vpath, item)
                            ritem = os.path.join(rpath, item)

                            item_stat = self.fs.stat(vitem)
                            if item_stat and item_stat['type'] == 'file':
                                content = self.fs.read(vitem)
                                with open(ritem, 'wb') as f:
                                    f.write(content)
                                exported.append(ritem)
                            elif item_stat and item_stat['type'] == 'dir':
                                export_recursive_helper(vitem, ritem)

                    export_recursive_helper(virtual_path, real_path)
                else:
                    # Export only top-level files (non-recursive)
                    for item in self.fs.ls(virtual_path) or []:
                        vitem = os.path.join(virtual_path, item)
                        ritem = os.path.join(real_path, item)

                        item_stat = self.fs.stat(vitem)
                        if item_stat and item_stat['type'] == 'file':
                            content = self.fs.read(vitem)
                            with open(ritem, 'wb') as f:
                                f.write(content)
                            exported.append(ritem)

                result = (f"Exported directory: {virtual_path} -> {real_path}\\n"
                         f"Files exported: {len(exported)}")
            else:
                result = f"export_file: {virtual_path}: Not a regular file or directory"
                return CommandResult(data=None, text=result, exit_code=1)

            return CommandResult(data={'exported': virtual_path, 'to': real_path},
                               text=result, exit_code=0)

        except Exception as e:
            result = f"export_file: {virtual_path}: Error: {e}"
            return CommandResult(data=None, text=result, exit_code=1)


# Create a default global instance
_shell = DagShell()


# Module-level convenience functions that use the global instance
def cd(path: str = None) -> DagShell:
    """Change directory."""
    return _shell.cd(path)

def ls(path: str = None, **kwargs) -> CommandResult:
    """List directory."""
    return _shell.ls(path, **kwargs)

def pwd() -> CommandResult:
    """Get current directory."""
    return _shell.pwd()

def cat(*paths: str) -> CommandResult:
    """Concatenate files."""
    return _shell.cat(*paths)

def grep(pattern: str, *paths: str, **kwargs) -> CommandResult:
    """Search for pattern."""
    return _shell.grep(pattern, *paths, **kwargs)

def head(n: int = 10, *paths: str) -> CommandResult:
    """Show first n lines."""
    return _shell.head(n, *paths)

def tail(n: int = 10, *paths: str) -> CommandResult:
    """Show last n lines."""
    return _shell.tail(n, *paths)

def wc(*paths: str, **kwargs) -> CommandResult:
    """Count lines/words/chars."""
    return _shell.wc(*paths, **kwargs)

def echo(*args, **kwargs) -> CommandResult:
    """Echo arguments."""
    return _shell.echo(*args, **kwargs)

def env(var: str = None) -> CommandResult:
    """Get environment."""
    return _shell.env(var)

def find(path: str = None, **kwargs) -> CommandResult:
    """Find files."""
    return _shell.find(path, **kwargs)

def save(filename: str = 'dagshell.json') -> CommandResult:
    """Save filesystem to JSON file."""
    return _shell.save(filename)

def load(filename: str = 'dagshell.json') -> CommandResult:
    """Load filesystem from JSON file."""
    return _shell.load(filename)

def commit(filename: str = 'dagshell.json') -> CommandResult:
    """Alias for save - commit filesystem to JSON file."""
    return _shell.commit(filename)

def mkdir(path: str, **kwargs) -> DagShell:
    """Create directory."""
    return _shell.mkdir(path, **kwargs)

def touch(path: str) -> DagShell:
    """Create or update file."""
    return _shell.touch(path)

def rm(path: str, **kwargs) -> DagShell:
    """Remove file or directory."""
    return _shell.rm(path, **kwargs)

# Allow direct access to the shell instance for chaining
shell = _shell`,"dagshell/command_parser.py":`#!/usr/bin/env python3
"""
Command parser for the dagshell terminal emulator.

This module provides a clean, composable parser that translates shell command
strings into structured representations that can be executed via the fluent API.

Design Principles:
- Single responsibility: Parse commands, don't execute them
- Composable: Each parsing step is independent
- Elegant: Clean data structures for representing commands
- Testable: Pure functions with predictable outputs
"""

import re
import shlex
from typing import Dict, List, Optional, Tuple, Union
from dataclasses import dataclass
from enum import Enum


class RedirectType(Enum):
    """Types of IO redirection."""
    WRITE = '>'      # Overwrite file
    APPEND = '>>'    # Append to file
    READ = '<'       # Read from file
    HERE_DOC = '<<'  # Here document
    HERE_STR = '<<<' # Here string


@dataclass
class Redirect:
    """Represents an IO redirection."""
    type: RedirectType
    target: str
    fd: int = 1  # File descriptor (1=stdout, 2=stderr, 0=stdin)


@dataclass
class Command:
    """
    Represents a single command with its arguments and redirections.

    This is the fundamental unit of execution. Each command maps to
    a method call on the fluent API.
    """
    name: str
    args: List[str]
    flags: Dict[str, Union[bool, str, int]]  # Parsed flags
    redirects: List[Redirect]
    raw_args: List[str]  # Original arguments before flag parsing

    def __str__(self) -> str:
        parts = [self.name]
        if self.flags:
            for key, value in self.flags.items():
                if value is True:
                    parts.append(f"-{key}")
                else:
                    parts.append(f"--{key}={value}")
        parts.extend(self.args)
        return ' '.join(parts)


@dataclass
class Pipeline:
    """
    Represents a pipeline of commands connected by pipes.

    Commands are executed left to right, with output flowing
    through the pipeline.
    """
    commands: List[Command]
    background: bool = False

    def __str__(self) -> str:
        return ' | '.join(str(cmd) for cmd in self.commands)


@dataclass
class CommandGroup:
    """
    Represents a group of pipelines connected by operators.

    Supports && (and), || (or), ; (sequence), & (background).
    """
    pipelines: List[Tuple[Pipeline, Optional[str]]]  # (pipeline, operator)

    def __str__(self) -> str:
        parts = []
        for pipeline, op in self.pipelines:
            parts.append(str(pipeline))
            if op:
                parts.append(op)
        return ' '.join(parts)


class CommandParser:
    """
    Parser for shell command syntax.

    This parser handles:
    - Basic commands with arguments
    - Flags (short: -a, -abc; long: --flag, --flag=value)
    - Pipes (|)
    - Redirections (>, >>, <)
    - Command sequences (;, &&, ||)
    - Quoting and escaping
    """

    # Common flag mappings for Unix commands
    FLAG_MAPPINGS = {
        'ls': {
            'a': 'all',
            'l': 'long',
            'h': 'human',
            'r': 'reverse',
            't': 'time',
            'S': 'size',
            '1': 'one',
        },
        'grep': {
            'i': 'ignore_case',
            'v': 'invert',
            'E': 'extended',
            'F': 'fixed',
            'n': 'line_number',
            'c': 'count',
            'l': 'files_with_matches',
        },
        'rm': {
            'r': 'recursive',
            'f': 'force',
            'i': 'interactive',
            'v': 'verbose',
        },
        'cp': {
            'r': 'recursive',
            'f': 'force',
            'v': 'verbose',
            'n': 'no_clobber',
        },
        'mkdir': {
            'p': 'parents',
            'v': 'verbose',
        },
        'sort': {
            'r': 'reverse',
            'n': 'numeric',
            'u': 'unique',
        },
        'uniq': {
            'c': 'count',
            'd': 'repeated',
        },
        'wc': {
            'l': 'lines',
            'w': 'words',
            'c': 'chars',
        },
        'echo': {
            'n': 'n',  # no newline
            'e': 'escape',
        },
        'head': {
            'n': 'lines',
        },
        'tail': {
            'n': 'lines',
            'f': 'follow',
        },
        'find': {
            'd': 'type',
            'f': 'type',
        },
        'cut': {
            'd': 'delimiter',
            'f': 'fields',
        },
        'ln': {
            's': 'symbolic',
        },
        'diff': {
            'u': 'unified',
            'c': 'context',
        },
        'du': {
            'h': 'human_readable',
            's': 'summarize',
        },
        'chmod': {},
        'chown': {},
        'tr': {},
    }

    def __init__(self):
        """Initialize the parser."""
        # Regex patterns for parsing
        self.redirect_pattern = re.compile(r'(\\d*)([><]+)\\s*(\\S+)')
        self.operator_pattern = re.compile(r'(&&|\\|\\||;|&)')

    def parse(self, command_line: str) -> CommandGroup:
        """
        Parse a complete command line into a CommandGroup.

        This is the main entry point for parsing shell commands.
        """
        # Handle empty input
        if not command_line or not command_line.strip():
            return CommandGroup(pipelines=[])

        # Split by command operators (&&, ||, ;, &)
        pipelines = []
        parts = self.operator_pattern.split(command_line)

        i = 0
        while i < len(parts):
            part = parts[i].strip()
            if not part:
                i += 1
                continue

            # Check if this is an operator
            if self.operator_pattern.match(part):
                i += 1
                continue

            # Parse this part as a pipeline
            pipeline = self._parse_pipeline(part)

            # Check for following operator
            operator = None
            if i + 1 < len(parts):
                next_part = parts[i + 1].strip()
                if self.operator_pattern.match(next_part):
                    operator = next_part
                    i += 1

            pipelines.append((pipeline, operator))
            i += 1

        return CommandGroup(pipelines=pipelines)

    def _parse_pipeline(self, pipeline_str: str) -> Pipeline:
        """Parse a pipeline of commands connected by pipes."""
        # Check for background execution
        background = False
        if pipeline_str.endswith('&'):
            background = True
            pipeline_str = pipeline_str[:-1].strip()

        # Split by pipes
        pipe_parts = self._split_by_pipe(pipeline_str)

        # Parse each command
        commands = []
        for part in pipe_parts:
            part = part.strip()
            if part:
                cmd = self._parse_command(part)
                if cmd:
                    commands.append(cmd)

        return Pipeline(commands=commands, background=background)

    def _split_by_pipe(self, text: str) -> List[str]:
        """Split text by pipe character, respecting quotes."""
        parts = []
        current = []
        in_single_quote = False
        in_double_quote = False
        escaped = False

        for char in text:
            if escaped:
                current.append(char)
                escaped = False
            elif char == '\\\\':
                escaped = True
                current.append(char)
            elif char == "'" and not in_double_quote:
                in_single_quote = not in_single_quote
                current.append(char)
            elif char == '"' and not in_single_quote:
                in_double_quote = not in_double_quote
                current.append(char)
            elif char == '|' and not in_single_quote and not in_double_quote:
                # Found a pipe separator
                parts.append(''.join(current))
                current = []
            else:
                current.append(char)

        # Add the last part
        if current:
            parts.append(''.join(current))

        return parts

    def _parse_command(self, command_str: str) -> Optional[Command]:
        """Parse a single command with its arguments and redirections."""
        # Extract redirections first
        redirects, clean_str = self._extract_redirections(command_str)

        # Parse the command and arguments
        try:
            tokens = shlex.split(clean_str)
        except ValueError:
            # Handle unclosed quotes gracefully
            tokens = clean_str.split()

        if not tokens:
            return None

        cmd_name = tokens[0]
        raw_args = tokens[1:]

        # Commands that use GNU-style single-dash long flags (e.g. -name, -type)
        # must skip normal flag parsing to avoid shredding them into char flags
        if cmd_name in ('find',):
            flags, args = {}, list(raw_args)
        else:
            # Parse flags and arguments
            flags, args = self._parse_flags(cmd_name, raw_args)

        return Command(
            name=cmd_name,
            args=args,
            flags=flags,
            redirects=redirects,
            raw_args=raw_args
        )

    _REDIRECT_TYPE_MAP = {
        '>': RedirectType.WRITE,
        '>>': RedirectType.APPEND,
        '<': RedirectType.READ,
        '<<': RedirectType.HERE_DOC,
        '<<<': RedirectType.HERE_STR,
    }

    def _extract_redirections(self, command_str: str) -> Tuple[List[Redirect], str]:
        """Extract redirections from a command string."""
        redirects = []

        for match in self.redirect_pattern.finditer(command_str):
            fd_str, op, target = match.groups()
            redirect_type = self._REDIRECT_TYPE_MAP.get(op)
            if redirect_type is None:
                continue

            fd = int(fd_str) if fd_str else (1 if op.startswith('>') else 0)
            redirects.append(Redirect(type=redirect_type, target=target, fd=fd))

        clean_str = self.redirect_pattern.sub('', command_str)
        return redirects, clean_str.strip()

    def _parse_flags(self, cmd_name: str, args: List[str]) -> Tuple[Dict[str, Union[bool, str, int]], List[str]]:
        """
        Parse flags from arguments.

        Returns (flags_dict, remaining_args)
        """
        flags = {}
        remaining = []
        flag_mappings = self.FLAG_MAPPINGS.get(cmd_name, {})

        i = 0
        while i < len(args):
            arg = args[i]

            if arg == '--':
                # End of flags marker
                remaining.extend(args[i + 1:])
                break
            elif arg.startswith('--'):
                # Long flag
                if '=' in arg:
                    key, value = arg[2:].split('=', 1)
                    flags[key] = value
                else:
                    flags[arg[2:]] = True
                i += 1
            elif arg.startswith('-') and len(arg) > 1 and arg[1] != '-':
                # Short flag(s)
                # Special case for head/tail: -N where N is a number
                if cmd_name in ['head', 'tail'] and arg[1:].isdigit():
                    flags['n'] = int(arg[1:])
                    i += 1
                else:
                    for char in arg[1:]:
                        # Map short flag to long name if available
                        long_name = flag_mappings.get(char, char)

                        # Check if this flag takes a value
                        # Flags that take a value argument
                        value_flags = {
                            ('head', 'n'), ('tail', 'n'),
                            ('cut', 'd'), ('cut', 'f'),
                            ('diff', 'c'),
                        }
                        if (cmd_name, char) in value_flags:
                            # This flag takes a value
                            if i + 1 < len(args) and not args[i + 1].startswith('-'):
                                value = args[i + 1]
                                # Try to convert to int for numeric flags
                                if char in ['n', 'c']:
                                    try:
                                        value = int(value)
                                    except ValueError:
                                        pass
                                flags[long_name] = value
                                i += 1
                            else:
                                flags[long_name] = True
                        else:
                            flags[long_name] = True
                    i += 1
            else:
                # Regular argument
                remaining.append(arg)
                i += 1

        return flags, remaining

    def parse_simple(self, command_str: str) -> Command:
        """
        Parse a simple command without pipes or operators.

        Convenience method for testing and simple cases.
        """
        cmd = self._parse_command(command_str)
        return cmd if cmd else Command(name='', args=[], flags={}, redirects=[], raw_args=[])`,"dagshell/terminal.py":`#!/usr/bin/env python3
"""
Terminal emulator for dagshell.

This module provides a complete terminal emulation layer that translates
shell commands into fluent API calls. It maintains session state, handles
command execution, and provides a familiar Unix-like interface.

Design Principles:
- All operations go through the fluent API
- Clean separation between parsing and execution
- Stateful session management
- Composable command execution
"""

import atexit
import builtins
import getpass
import json
import os
import re
try:
    import readline
except ImportError:
    readline = None
import socket
import sys
from dataclasses import dataclass, field
from datetime import datetime
from pathlib import Path
from typing import Dict, List, Optional, Tuple

from .command_parser import (
    CommandParser, Command, Pipeline, CommandGroup,
    RedirectType, Redirect
)
from .dagshell_fluent import DagShell, CommandResult


def _safe_getuser():
    """Get current username, falling back to 'user' in restricted environments."""
    try:
        return getpass.getuser()
    except (ImportError, KeyError, OSError):
        return 'user'


@dataclass
class TerminalConfig:
    """Configuration for terminal session."""
    user: str = field(default_factory=lambda: _safe_getuser())
    hostname: str = field(default_factory=lambda: socket.gethostname().split('.')[0])
    home_dir: str = '/home/user'
    initial_dir: str = '/'
    prompt_format: str = '{user}@{hostname}:{cwd}$ '
    enable_colors: bool = True
    history_size: int = 1000
    safe_host_directory: Optional[str] = None  # Safe directory for host file operations
    snapshots_directory: str = '.dagshell-snapshots'  # Directory for state snapshots
    # Enhanced terminal features
    history_file: str = field(default_factory=lambda: str(Path.home() / '.dagshell_history'))
    history_max_size: int = 10000
    enable_tab_completion: bool = True
    enable_history_expansion: bool = True
    aliases: Dict[str, str] = field(default_factory=dict)
    alias_file: str = field(default_factory=lambda: str(Path.home() / '.dagshell_aliases'))


class HistoryManager:
    """
    Manages command history with persistence and expansion.

    Provides:
    - History persistence to disk
    - History expansion (!!, !n, !-n, !string)
    - Integration with readline library
    """

    def __init__(self, history_file: str, max_size: int = 10000):
        """Initialize history manager with file persistence."""
        self.history_file = history_file
        self.max_size = max_size
        self.history: List[str] = []
        self._initial_history_length = readline.get_current_history_length() if readline else 0
        self._load_history()

    def _load_history(self):
        """Load history from file."""
        if not readline:
            return
        try:
            if os.path.exists(self.history_file):
                readline.clear_history()
                readline.read_history_file(self.history_file)
                history_length = readline.get_current_history_length()
                self.history = [
                    readline.get_history_item(i)
                    for i in range(1, history_length + 1)
                    if readline.get_history_item(i)
                ]
        except (IOError, OSError, AttributeError):
            self.history = []

    def save_history(self):
        """Save history to file."""
        if not readline:
            return
        try:
            readline.write_history_file(self.history_file)
        except (IOError, OSError):
            pass

    def add(self, command: str):
        """Add a command to history."""
        if command and command.strip():
            if not self.history or self.history[-1] != command:
                self.history.append(command)
                if readline:
                    readline.add_history(command)
                if len(self.history) > self.max_size:
                    self.history = self.history[-self.max_size:]

    def get(self, index: int) -> Optional[str]:
        """Get command at specific index (1-based like bash)."""
        if 0 < index <= len(self.history):
            return self.history[index - 1]
        return None

    def get_last(self) -> Optional[str]:
        """Get the last command."""
        return self.history[-1] if self.history else None

    def search(self, pattern: str) -> List[Tuple[int, str]]:
        """Search history for commands containing pattern."""
        results = []
        for i, cmd in enumerate(self.history, 1):
            if pattern in cmd:
                results.append((i, cmd))
        return results

    def expand(self, command: str) -> str:
        """
        Expand history references in command.

        Supports: !! (last), !n (number), !-n (relative), !string (prefix)
        """
        if not command or '!' not in command:
            return command

        # Handle !! (last command)
        if '!!' in command:
            last_cmd = self.get_last()
            if last_cmd:
                command = command.replace('!!', last_cmd)
            else:
                raise ValueError("!!: event not found")

        # Handle !n and !-n
        pattern = r'!(-?\\d+)'
        matches = re.findall(pattern, command)
        for match in matches:
            index = int(match)
            if index < 0:
                index = len(self.history) + index + 1
            expanded = self.get(index)
            if expanded:
                command = command.replace(f'!{match}', expanded)
            else:
                raise ValueError(f"!{match}: event not found")

        # Handle !string (most recent command starting with string)
        pattern = r'!([a-zA-Z]\\w*)'
        matches = re.findall(pattern, command)
        for match in matches:
            for cmd in reversed(self.history):
                if cmd.startswith(match):
                    command = command.replace(f'!{match}', cmd)
                    break
            else:
                raise ValueError(f"!{match}: event not found")

        return command

    def display(self, max_entries: Optional[int] = None) -> str:
        """Format history for display."""
        entries = self.history if max_entries is None else self.history[-max_entries:]
        lines = []
        start_index = len(self.history) - len(entries) + 1
        for i, cmd in enumerate(entries, start_index):
            lines.append(f"{i:5d}  {cmd}")
        return '\\n'.join(lines)


class AliasManager:
    """
    Manages command aliases with persistence.
    """

    def __init__(self, alias_file: str):
        """Initialize alias manager."""
        self.alias_file = alias_file
        self.aliases: Dict[str, str] = {}
        self._load_aliases()

    def _load_aliases(self):
        """Load aliases from file."""
        try:
            if os.path.exists(self.alias_file):
                with open(self.alias_file, 'r') as f:
                    self.aliases = json.load(f)
        except (IOError, json.JSONDecodeError):
            pass

    def save_aliases(self):
        """Save aliases to file."""
        try:
            with open(self.alias_file, 'w') as f:
                json.dump(self.aliases, f, indent=2)
        except IOError:
            pass

    def add(self, name: str, command: str):
        """Add an alias."""
        self.aliases[name] = command
        self.save_aliases()

    def remove(self, name: str) -> bool:
        """Remove an alias. Returns True if removed."""
        if name in self.aliases:
            del self.aliases[name]
            self.save_aliases()
            return True
        return False

    def expand(self, command_line: str) -> str:
        """Expand aliases in command line."""
        parts = command_line.split(None, 1)
        if not parts:
            return command_line
        first_word = parts[0]
        if first_word in self.aliases:
            expanded = self.aliases[first_word]
            if len(parts) > 1:
                return expanded + ' ' + parts[1]
            else:
                return expanded
        return command_line

    def list_aliases(self) -> str:
        """Format aliases for display."""
        if not self.aliases:
            return "No aliases defined"
        lines = []
        for name, command in sorted(self.aliases.items()):
            lines.append(f"alias {name}='{command}'")
        return '\\n'.join(lines)


class TabCompleter:
    """
    Provides tab completion for commands and file paths.
    """

    def __init__(self, shell: DagShell, aliases: Dict[str, str]):
        """Initialize tab completer."""
        self.shell = shell
        self.aliases = aliases
        self._command_cache = self._build_command_cache()

    def _build_command_cache(self) -> List[str]:
        """Build cache of available commands."""
        commands = []
        for attr_name in dir(self.shell):
            if not attr_name.startswith('_'):
                attr = getattr(self.shell, attr_name)
                if callable(attr):
                    commands.append(attr_name)
        commands.extend(['help', 'clear', 'exit', 'quit', 'history', 'alias', 'unalias'])
        commands.extend(self.aliases.keys())
        return sorted(set(commands))

    def complete(self, text: str, state: int) -> Optional[str]:
        """Readline completion function."""
        if not readline:
            return None
        line = readline.get_line_buffer()
        begin = readline.get_begidx()

        if begin == 0:
            matches = self._complete_command(text)
        else:
            matches = self._complete_path(text, line)

        try:
            return matches[state]
        except IndexError:
            return None

    def _complete_command(self, text: str) -> List[str]:
        """Complete command names."""
        if not text:
            return self._command_cache
        return [cmd for cmd in self._command_cache if cmd.startswith(text)]

    def _complete_path(self, text: str, line: str) -> List[str]:
        """Complete file paths in the virtual filesystem."""
        if text.startswith('/'):
            dir_path = os.path.dirname(text) or '/'
            prefix = os.path.basename(text)
        else:
            if '/' in text:
                dir_path = os.path.dirname(text)
                prefix = os.path.basename(text)
            else:
                dir_path = '.'
                prefix = text

        matches = []
        current_dir = self.shell._cwd
        if dir_path == '.':
            search_dir = current_dir
        elif dir_path.startswith('/'):
            search_dir = dir_path
        else:
            search_dir = os.path.join(current_dir, dir_path)

        try:
            result = self.shell.cd(search_dir).ls()
            if result and result.data:
                entries = result.data if isinstance(result.data, list) else result.text.strip().split('\\n')
                for entry_name in entries:
                    if not entry_name or entry_name in ['.', '..']:
                        continue
                    if entry_name.startswith(prefix):
                        if text.startswith('/'):
                            full_path = os.path.join(dir_path, entry_name)
                        elif '/' in text:
                            full_path = os.path.join(dir_path, entry_name)
                        else:
                            full_path = entry_name
                        entry_full_path = os.path.join(search_dir, entry_name)
                        if self.shell.fs.stat(entry_full_path) and self.shell.fs.stat(entry_full_path).get('type') == 'dir':
                            full_path += '/'
                        matches.append(full_path)
            self.shell.cd(current_dir)
        except (AttributeError, KeyError, TypeError):
            pass

        return sorted(matches)


class CommandExecutor:
    """
    Executes parsed commands by translating them to fluent API calls.

    This class bridges the gap between parsed command structures
    and the fluent API, handling the translation of flags and
    arguments into appropriate method calls.
    """

    def __init__(self, shell: DagShell):
        """Initialize with a DagShell instance."""
        self.shell = shell

    def execute(self, command_group: CommandGroup) -> CommandResult:
        """Execute a CommandGroup and return the final result."""
        accumulated_text = []
        last_result = None

        for pipeline, operator in command_group.pipelines:
            result = self._execute_pipeline(pipeline)

            # Handle operators
            if operator == '&&' and result.exit_code != 0:
                accumulated_text.append(result.text or '')
                last_result = result
                break
            elif operator == '||' and result.exit_code == 0:
                accumulated_text.append(result.text or '')
                last_result = result
                break
            # For ';' and '&', always continue

            accumulated_text.append(result.text or '')
            last_result = result

        if last_result is None:
            return CommandResult(data='', text='', exit_code=0)

        combined_text = '\\n'.join(t for t in accumulated_text if t)
        return CommandResult(data=last_result.data, text=combined_text, exit_code=last_result.exit_code)

    def _execute_pipeline(self, pipeline: Pipeline) -> CommandResult:
        """Execute a pipeline of commands."""
        if not pipeline.commands:
            return CommandResult(data='', text='', exit_code=0)

        # Execute first command
        result = self._execute_command(pipeline.commands[0])

        # Chain through remaining commands
        for cmd in pipeline.commands[1:]:
            # Store result in shell's last_result for piping
            self.shell._last_result = result
            result = self._execute_command(cmd)

        return result

    def _expand_variables(self, text: str) -> str:
        """Expand tilde and $VARIABLE references in a string."""
        # Expand ~ at start
        if text.startswith('~'):
            home = self.shell._env.get('HOME', '/home/user')
            text = home + text[1:]
        # Expand \${VAR} first (more specific), then $VAR
        text = re.sub(r'\\$\\{(\\w+)\\}', lambda m: self.shell._env.get(m.group(1), ''), text)
        text = re.sub(r'\\$(\\w+)', lambda m: self.shell._env.get(m.group(1), ''), text)
        return text

    def _execute_command(self, command: Command) -> CommandResult:
        """Execute a single command by calling the appropriate shell method."""

        # Expand variables in command arguments
        command = Command(
            name=command.name,
            args=[self._expand_variables(a) for a in command.args],
            flags=command.flags,
            redirects=command.redirects,
            raw_args=command.raw_args
        )

        # Handle export VAR=value before normal dispatch
        if command.name == 'export' and command.args and '=' in command.args[0]:
            return self._handle_export(command.args)

        # Check for help flags first
        # Note: don't treat -h as help for commands that use -h for other purposes
        commands_with_h_flag = {'du', 'ls', 'df'}  # -h means human-readable
        is_help = '--help' in command.flags or 'help' in command.flags
        if not is_help and 'h' in command.flags and command.name not in commands_with_h_flag:
            is_help = True
        if is_help:
            return self._show_command_help(command.name)

        # Map command name to shell method
        method = self._get_shell_method(command.name)

        if method is None:
            return CommandResult(
                data='',
                text=f"-bash: {command.name}: command not found",
                exit_code=127
            )

        # Prepare arguments based on command type
        args, kwargs = self._prepare_arguments(command)

        # Execute the command
        try:
            try:
                result = method(*args, **kwargs)
            except TypeError as e:
                if 'unexpected keyword argument' in str(e):
                    # Filter out unknown kwargs and retry
                    import inspect
                    sig = inspect.signature(method)
                    valid_kwargs = {k: v for k, v in kwargs.items() if k in sig.parameters}
                    result = method(*args, **valid_kwargs)
                else:
                    raise

            # Handle DagShell returns (for commands like cd, mkdir)
            if isinstance(result, DagShell):
                lr = self.shell._last_result
                if lr and lr.exit_code != 0:
                    result = lr
                else:
                    result = CommandResult(data='', text='', exit_code=0)
            elif not isinstance(result, CommandResult):
                # Wrap non-CommandResult returns
                result = CommandResult(data=result, text=str(result), exit_code=0)

            # Handle redirections
            for redirect in command.redirects:
                result = self._apply_redirection(result, redirect)

            return result

        except Exception as e:
            return CommandResult(
                data='',
                text=f"{command.name}: {str(e)}",
                exit_code=1
            )

    def _show_help(self, command: Optional[str] = None) -> CommandResult:
        """Show help information for commands."""
        if command:
            return self._show_command_help(command)
        return self._show_all_commands_help()

    def _extract_docstring_sections(self, docstring: str) -> dict:
        """Extract structured sections from a docstring."""
        if not docstring:
            return {}

        lines = docstring.strip().split('\\n')
        sections = {
            'description': '',
            'usage': '',
            'options': [],
            'examples': []
        }

        # First line is always the description
        if lines:
            sections['description'] = lines[0].strip()

        current_section = None
        section_content = []

        for line in lines[1:]:
            line = line.strip()
            if line.startswith('Usage:'):
                current_section = 'usage'
                section_content = []
            elif line.startswith('Options:'):
                current_section = 'options'
                section_content = []
            elif line.startswith('Examples:'):
                current_section = 'examples'
                section_content = []
            elif line.startswith('Returns:'):
                current_section = 'returns'
                section_content = []
            elif line and current_section:
                section_content.append(line)
                if current_section == 'usage':
                    sections['usage'] = line
                elif current_section == 'options':
                    sections['options'].append(line)
                elif current_section == 'examples':
                    sections['examples'].append(line)

        return sections

    def _show_command_help(self, command: str) -> CommandResult:
        """Show detailed help for a specific command."""
        # Get the method from shell
        method = getattr(self.shell, command, None)

        if not method or not callable(method):
            # Check aliases
            if command in ['help', '?']:
                help_text = """help - Show this help system

Usage:
    help [COMMAND]

Options:
    COMMAND                Command to get help for

Examples:
    help                   # Show all commands
    help ls                # Show help for ls command
    help grep              # Show help for grep command"""
                return CommandResult(data=help_text, text=help_text, exit_code=0)
            elif command == 'clear':
                help_text = """clear - Clear the terminal screen

Usage:
    clear

Options:
    None

Examples:
    clear                  # Clear the screen"""
                return CommandResult(data=help_text, text=help_text, exit_code=0)
            elif command in ['exit', 'quit']:
                help_text = """exit/quit - Exit the shell

Usage:
    exit
    quit

Options:
    None

Examples:
    exit                   # Exit the shell
    quit                   # Exit the shell"""
                return CommandResult(data=help_text, text=help_text, exit_code=0)
            else:
                error_text = f"help: no help available for '{command}'"
                return CommandResult(data='', text=error_text, exit_code=1)

        # Get and format the docstring
        docstring = method.__doc__
        if docstring:
            sections = self._extract_docstring_sections(docstring)

            # Format the help text
            help_lines = []
            help_lines.append(f"{command} - {sections['description']}")
            help_lines.append("")

            if sections['usage']:
                help_lines.append("Usage:")
                help_lines.append(f"    {sections['usage']}")
                help_lines.append("")

            if sections['options']:
                help_lines.append("Options:")
                for opt in sections['options']:
                    help_lines.append(f"    {opt}")
                help_lines.append("")

            if sections['examples']:
                help_lines.append("Examples:")
                for ex in sections['examples']:
                    help_lines.append(f"    {ex}")
                help_lines.append("")

            help_text = '\\n'.join(help_lines)
        else:
            help_text = f"{command} - No documentation available"

        return CommandResult(data=help_text, text=help_text, exit_code=0)

    def _show_all_commands_help(self) -> CommandResult:
        """Show a list of all available commands with brief descriptions."""
        # Get all command methods from shell
        commands = {}

        # Get methods from DagShell class
        for attr_name in dir(self.shell):
            if not attr_name.startswith('_'):
                attr = getattr(self.shell, attr_name)
                if callable(attr) and hasattr(attr, '__doc__'):
                    doc = attr.__doc__
                    if doc:
                        # Extract first line of docstring
                        first_line = doc.strip().split('\\n')[0]
                        commands[attr_name] = first_line

        # Add special terminal commands
        special_commands = {
            'help': 'Show this help system',
            'clear': 'Clear the terminal screen',
            'exit': 'Exit the shell',
            'quit': 'Exit the shell (alias for exit)'
        }
        commands.update(special_commands)

        # Format the help text
        help_lines = []
        help_lines.append("DagShell Terminal - Available Commands")
        help_lines.append("="*40)
        help_lines.append("")
        help_lines.append("Type 'help COMMAND' for detailed help on a specific command.")
        help_lines.append("")

        # Group commands by category
        categories = {
            'NAVIGATION': ['cd', 'pwd', 'ls'],
            'FILE OPERATIONS': ['cat', 'touch', 'cp', 'mv', 'rm', 'mkdir'],
            'TEXT PROCESSING': ['echo', 'grep', 'head', 'tail', 'sort', 'uniq', 'wc'],
            'SEARCH': ['find'],
            'ENVIRONMENT': ['env', 'setenv', 'whoami', 'su'],
            'PERSISTENCE': ['save', 'load', 'commit', 'export'],
            'PIPING': ['tee', 'pipe'],
            'SYSTEM': ['clear', 'help', 'exit', 'quit']
        }

        for category, cmd_list in categories.items():
            category_commands = [(cmd, commands.get(cmd, '')) for cmd in cmd_list if cmd in commands]
            if category_commands:
                help_lines.append(f"{category}:")
                for cmd, desc in sorted(category_commands):
                    # Format with proper spacing
                    help_lines.append(f"  {cmd:<15} {desc}")
                help_lines.append("")

        # Add any commands not in categories
        uncategorized = []
        categorized_cmds = set()
        for cmd_list in categories.values():
            categorized_cmds.update(cmd_list)

        for cmd, desc in sorted(commands.items()):
            if cmd not in categorized_cmds and cmd not in ['xargs', 'pipe']:
                uncategorized.append((cmd, desc))

        if uncategorized:
            help_lines.append("OTHER:")
            for cmd, desc in uncategorized:
                help_lines.append(f"  {cmd:<15} {desc}")
            help_lines.append("")

        help_lines.append("REDIRECTION AND PIPING:")
        help_lines.append("  command > file     Redirect output to file")
        help_lines.append("  command >> file    Append output to file")
        help_lines.append("  cmd1 | cmd2        Pipe output of cmd1 to cmd2")
        help_lines.append("")
        help_lines.append("SLASH COMMANDS:")
        help_lines.append("  /help              Show slash command help")
        help_lines.append("  /status            Show filesystem statistics")
        help_lines.append("  /import            Import files from host")
        help_lines.append("  /export            Export files to host")
        help_lines.append("  /save              Save state to JSON")
        help_lines.append("  /load              Load state from JSON")
        help_lines.append("")
        help_lines.append("Note: The filesystem is virtual and in-memory.")
        help_lines.append("Use 'save' to persist to JSON, 'export' to write to real filesystem.")

        help_text = '\\n'.join(help_lines)
        return CommandResult(data=help_text, text=help_text, exit_code=0)

    def _get_shell_method(self, command_name: str):
        """Get the shell method for a command name."""
        # Check aliases first for commands that override shell methods
        aliases = {
            'clear': lambda: CommandResult(data='', text='\\033[2J\\033[H', exit_code=0),
            'exit': lambda: CommandResult(data='', text='exit', exit_code=0),
            'quit': lambda: CommandResult(data='', text='exit', exit_code=0),
            'help': lambda *args: self._show_help(args[0] if args else None),
            '?': lambda *args: self._show_help(args[0] if args else None),
            'su': lambda *args: self._su(args[0] if args else 'root'),
            'export': lambda *args: self._handle_export(args),
            'scheme': lambda *args: self._run_scheme_command(args),
        }

        alias = aliases.get(command_name)
        if alias:
            return alias

        # Direct method mapping on shell object
        method = getattr(self.shell, command_name, None)
        if method and callable(method):
            return method

        return None

    def _prepare_arguments(self, command: Command) -> Tuple[List, Dict]:
        """Prepare arguments and keyword arguments for method call."""
        args = []
        kwargs = {}

        # Commands that take a single optional argument
        _SINGLE_ARG_CMDS = {'help', 'cd', 'touch', 'stat', 'readlink', 'id',
                            'env', 'tee', 'dirname', 'tree'}

        # Commands that pass all args with all flags as kwargs
        _ARGS_AND_FLAGS_CMDS = {'echo', 'grep', 'wc', 'sort', 'uniq'}

        # Commands that pass first arg with flags
        _FIRST_ARG_AND_FLAGS_CMDS = {'ls', 'mkdir', 'rm'}

        # Commands that take exactly two positional args
        _TWO_ARG_CMDS = {'cp', 'mv', 'chmod', 'chown', 'tr'}

        if command.name in _SINGLE_ARG_CMDS:
            if command.args:
                args.append(command.args[0])

        elif command.name in _FIRST_ARG_AND_FLAGS_CMDS:
            if command.args:
                args.append(command.args[0])
            kwargs.update(command.flags)

        elif command.name in ['head', 'tail']:
            # head/tail: -n 5, -5, or default 10
            n_value = 10
            if 'n' in command.flags:
                n_value = command.flags['n']
            elif 'lines' in command.flags:
                n_value = command.flags['lines']
            else:
                for flag_key in command.flags:
                    try:
                        n_value = int(flag_key)
                        break
                    except ValueError:
                        pass
            args.append(n_value)
            args.extend(command.args)
            return args, {}

        elif command.name == 'cat':
            args.extend(command.args)

        elif command.name in _ARGS_AND_FLAGS_CMDS:
            args.extend(command.args)
            kwargs.update(command.flags)

        elif command.name == 'find':
            # find uses GNU-style single-dash long flags: -name PATTERN -type TYPE -maxdepth N
            # Parse manually from args since the parser skips flag parsing for find
            path = None
            i = 0
            raw = command.args
            while i < len(raw):
                if raw[i] == '-name' and i + 1 < len(raw):
                    kwargs['name'] = raw[i + 1]
                    i += 2
                elif raw[i] == '-type' and i + 1 < len(raw):
                    kwargs['type'] = raw[i + 1]
                    i += 2
                elif raw[i] == '-maxdepth' and i + 1 < len(raw):
                    kwargs['maxdepth'] = int(raw[i + 1])
                    i += 2
                elif path is None and not raw[i].startswith('-'):
                    path = raw[i]
                    i += 1
                else:
                    i += 1
            if path:
                args.append(path)
            return args, kwargs

        elif command.name in _TWO_ARG_CMDS:
            args.extend(command.args[:2])

        elif command.name == 'ln':
            args.extend(command.args[:2])
            if 's' in command.flags or 'symbolic' in command.flags:
                kwargs['symbolic'] = True

        elif command.name == 'basename':
            if command.args:
                args.append(command.args[0])
                if len(command.args) > 1:
                    args.append(command.args[1])

        elif command.name == 'diff':
            args.extend(command.args[:2])
            if 'u' in command.flags or 'unified' in command.flags:
                kwargs['unified'] = True
            if 'context' in command.flags:
                kwargs['context'] = command.flags['context']
            elif 'c' in command.flags:
                kwargs['context'] = command.flags['c']

        elif command.name == 'cut':
            if 'delimiter' in command.flags:
                kwargs['delimiter'] = command.flags['delimiter']
            elif 'd' in command.flags:
                kwargs['delimiter'] = command.flags['d']
            if 'fields' in command.flags:
                kwargs['fields'] = command.flags['fields']
            elif 'f' in command.flags:
                kwargs['fields'] = command.flags['f']
            args.extend(command.args)

        elif command.name == 'du':
            if 'h' in command.flags or 'human_readable' in command.flags or 'human-readable' in command.flags:
                kwargs['human_readable'] = True
            args.extend(command.args)

        elif command.name == 'xargs':
            if command.args:
                args.append(command.args[0])
                args.extend(command.args[1:])
            if 'n' in command.flags:
                kwargs['max_args'] = command.flags['n']

        elif command.name == 'pwd':
            pass

        else:
            args.extend(command.args)
            kwargs.update(command.flags)

        return args, kwargs

    def _apply_redirection(self, result: CommandResult, redirect: Redirect) -> CommandResult:
        """Apply a redirection to a command result."""
        if redirect.type == RedirectType.WRITE:
            # Overwrite file
            result.out(redirect.target)
            # Clear the text output since it's been redirected
            result.text = ''
        elif redirect.type == RedirectType.APPEND:
            # Append to file
            result.append(redirect.target)
            # Clear the text output since it's been redirected
            result.text = ''
        # TODO: Handle other redirection types (INPUT, etc.)

        return result

    def _handle_export(self, args) -> CommandResult:
        """Handle export: set env var if VAR=value, otherwise fall through."""
        if args:
            arg = args[0]
            if '=' in arg:
                var, _, val = arg.partition('=')
                self.shell.setenv(var.strip(), val.strip())
                return CommandResult(data='', text='', exit_code=0)
        return CommandResult(data='', text='export: missing VAR=value', exit_code=1)

    def _run_scheme_command(self, args) -> CommandResult:
        """Run a Scheme script or expression."""
        from .scheme_interpreter import SchemeREPL

        try:
            repl = SchemeREPL()

            if args and args[0]:
                script_path = args[0]
                # Run script from virtual filesystem
                if script_path.endswith('.scm'):
                    # Resolve relative paths using the shell's resolver
                    resolved_path = self.shell._resolve_path(script_path)
                    # Read script from virtual FS
                    content = self.shell.fs.read(resolved_path)
                    if content is None:
                        return CommandResult(
                            data='',
                            text=f"scheme: {script_path}: No such file",
                            exit_code=1
                        )

                    code = content.decode('utf-8')
                else:
                    # Treat argument as inline code
                    code = script_path

                # Execute the Scheme code
                result = repl.eval_string(code)

                # Format result for display
                if result is not None:
                    if isinstance(result, bool):
                        text = "#t" if result else "#f"
                    elif isinstance(result, list):
                        text = repl._list_to_string(result)
                    else:
                        text = str(result)
                else:
                    text = ""

                return CommandResult(data=result, text=text, exit_code=0)
            else:
                # No script provided, show usage
                help_text = """scheme - Run Scheme code or scripts

Usage:
    scheme SCRIPT.scm          Run a Scheme script file
    scheme "(expression)"      Evaluate a Scheme expression

Examples:
    scheme /project/build.scm
    scheme "(ls)"
    scheme "(mkdir '/test')"

For interactive REPL, run: python scheme"""
                return CommandResult(data=help_text, text=help_text, exit_code=0)

        except Exception as e:
            error = f"Scheme error: {e}"
            return CommandResult(data=error, text=error, exit_code=1)


class CommandHistory:
    """Manages command history for the terminal session."""

    def __init__(self, max_size: int = 1000):
        """Initialize with maximum history size."""
        self.max_size = max_size
        self.history: List[str] = []
        self.position = 0

    def add(self, command: str):
        """Add a command to history."""
        if command and command.strip():
            self.history.append(command)
            if len(self.history) > self.max_size:
                self.history.pop(0)
        self.position = len(self.history)

    def previous(self) -> Optional[str]:
        """Get previous command in history."""
        if self.position > 0:
            self.position -= 1
            return self.history[self.position]
        return None

    def next(self) -> Optional[str]:
        """Get next command in history."""
        if self.position < len(self.history) - 1:
            self.position += 1
            return self.history[self.position]
        elif self.position == len(self.history) - 1:
            self.position += 1
            return ''
        return ''


class TerminalSession:
    """
    Main terminal session manager.

    This class provides the REPL loop and manages the terminal session,
    including prompt display, command execution, and session state.
    Features include readline integration, history expansion, tab completion,
    and command aliases.
    """

    def __init__(self, config: Optional[TerminalConfig] = None,
                 shell: Optional[DagShell] = None,
                 fs: Optional['FileSystem'] = None):
        """Initialize terminal session."""
        self.config = config or TerminalConfig()
        if shell:
            self.shell = shell
        elif fs:
            self.shell = DagShell(fs=fs)
        else:
            self.shell = DagShell()
        self.parser = CommandParser()
        self.executor = CommandExecutor(self.shell)
        self.history = CommandHistory(self.config.history_size)
        self.running = False

        # User context
        self.current_user = self.config.user
        self.uid, self.gid = self.shell.fs.lookup_user(self.current_user)
        self.groups = self.shell.fs.get_user_groups(self.current_user)
        self.strict_permissions = False  # Can be enabled for permission checking

        # Enhanced terminal components
        self.history_manager = HistoryManager(
            self.config.history_file,
            self.config.history_max_size
        )
        self.alias_manager = AliasManager(self.config.alias_file)

        # Setup readline and register cleanup
        self._setup_readline()
        atexit.register(self._cleanup)

        # Initialize shell environment
        self._init_environment()

    def _setup_readline(self):
        """Configure readline for terminal."""
        if not readline:
            return
        if self.config.enable_tab_completion:
            self.tab_completer = TabCompleter(self.shell, self.alias_manager.aliases)
            readline.set_completer(self.tab_completer.complete)
            readline.parse_and_bind('tab: complete')
        readline.parse_and_bind('set editing-mode emacs')
        readline.set_history_length(self.config.history_max_size)

    def _cleanup(self):
        """Clean up on exit."""
        self.history_manager.save_history()
        self.alias_manager.save_aliases()

    def _init_environment(self):
        """Initialize the shell environment."""
        self.shell.setenv('USER', self.config.user)
        self.shell.setenv('HOME', self.config.home_dir)
        self.shell.setenv('HOSTNAME', self.config.hostname)

        # Set initial directory
        if self.config.initial_dir:
            self.shell.cd(self.config.initial_dir)

    def _whoami(self) -> CommandResult:
        """Return current username."""
        return CommandResult(data=self.current_user, text=self.current_user, exit_code=0)

    def _su(self, username: str) -> CommandResult:
        """Switch user."""
        # Check if user exists
        uid, gid = self.shell.fs.lookup_user(username)
        if uid == 1000 and username not in ['user', 'alice', 'bob', 'root']:
            return CommandResult(
                data='',
                text=f"su: user '{username}' does not exist",
                exit_code=1
            )

        # Switch user
        self.current_user = username
        self.uid, self.gid = uid, gid
        self.groups = self.shell.fs.get_user_groups(username)
        self.shell.setenv('USER', username)

        # Change to user's home directory
        home = f"/home/{username}" if username != 'root' else "/root"
        if self.shell.fs.exists(home):
            self.shell.cd(home)
            self.shell.setenv('HOME', home)

        return CommandResult(data='', text='', exit_code=0)

    def _export(self, target_path: str) -> CommandResult:
        """Export virtual filesystem to real filesystem."""
        try:
            exported = self.shell.fs.export_to_real(target_path)
            result = f"Exported {exported} files/directories to {target_path}"
            return CommandResult(data=exported, text=result, exit_code=0)
        except Exception as e:
            error = f"Export failed: {e}"
            return CommandResult(data='', text=error, exit_code=1)

    # Slash command handlers

    def _execute_slash_command(self, command_line: str) -> str:
        """Execute a slash command."""
        parts = command_line[1:].split(None, 2)  # Remove leading / and split
        if not parts:
            return "Error: empty slash command"

        cmd = parts[0].lower()
        args = parts[1:] if len(parts) > 1 else []

        # Dispatch to appropriate handler
        handlers = {
            'import': self._slash_import,
            'export': self._slash_export,
            'save': self._slash_save,
            'load': self._slash_load,
            'snapshot': self._slash_snapshot,
            'snapshots': self._slash_snapshots,
            'status': self._slash_status,
            'dag': self._slash_dag,
            'nodes': self._slash_nodes,
            'info': self._slash_info,
            'help': self._slash_help,
            'aliases': self._slash_aliases,
        }

        handler = handlers.get(cmd)
        if handler:
            try:
                return handler(args)
            except Exception as e:
                return f"Error in /{cmd}: {e}"
        else:
            return f"Unknown slash command: /{cmd}\\nType /help for available commands"

    def _resolve_host_path(self, path: str) -> str:
        """Resolve and validate a host path against safe_host_directory."""
        if self.config.safe_host_directory is None:
            raise ValueError("Host file operations not configured. Set safe_host_directory in config.")

        # Make safe directory absolute
        safe_dir = os.path.abspath(self.config.safe_host_directory)

        # If path is relative, join with safe directory
        if not os.path.isabs(path):
            resolved = os.path.abspath(os.path.join(safe_dir, path))
        else:
            resolved = os.path.abspath(path)

        # Check if resolved path is within safe directory
        if not resolved.startswith(safe_dir):
            raise ValueError(f"Path '{path}' is outside safe directory '{safe_dir}'")

        return resolved

    def _slash_import(self, args: List[str]) -> str:
        """Import file/directory from host filesystem."""
        if len(args) < 2:
            return "Usage: /import <host-path> <dagshell-path>"

        host_path = args[0]
        dagshell_path = args[1]

        try:
            # Resolve host path
            resolved_host = self._resolve_host_path(host_path)

            # Import into virtual filesystem
            imported = self.shell.fs.import_from_real(
                resolved_host,
                dagshell_path,
                preserve_permissions=True,
                uid=self.uid,
                gid=self.gid
            )

            return f"Imported {imported} items from {resolved_host} to {dagshell_path}"
        except Exception as e:
            return f"Import failed: {e}"

    def _slash_export(self, args: List[str]) -> str:
        """Export file/directory to host filesystem."""
        if len(args) < 2:
            return "Usage: /export <dagshell-path> <host-path>"

        dagshell_path = args[0]
        host_path = args[1]

        try:
            # Check if dagshell path exists
            if not self.shell.fs.exists(dagshell_path):
                return f"Error: Path not found in dagshell: {dagshell_path}"

            # Resolve host path
            resolved_host = self._resolve_host_path(host_path)

            # For now, we'll export the entire filesystem to the host path
            # TODO: Implement selective export for specific paths
            exported = self.shell.fs.export_to_real(resolved_host)

            return f"Exported {exported} items to {resolved_host}"
        except Exception as e:
            return f"Export failed: {e}"

    def _slash_save(self, args: List[str]) -> str:
        """Save state to JSON file."""
        filename = args[0] if args else 'dagshell-state.json'

        try:
            # Resolve path
            if self.config.safe_host_directory:
                filepath = self._resolve_host_path(filename)
            else:
                filepath = filename

            # Save state
            with builtins.open(filepath, 'w') as f:
                f.write(self.shell.fs.to_json())

            return f"State saved to {filepath}"
        except Exception as e:
            return f"Save failed: {e}"

    def _slash_load(self, args: List[str]) -> str:
        """Load state from JSON file."""
        if not args:
            return "Usage: /load <filename>"

        filename = args[0]

        try:
            # Resolve path
            if self.config.safe_host_directory:
                filepath = self._resolve_host_path(filename)
            else:
                filepath = filename

            # Load state
            with builtins.open(filepath, 'r') as f:
                json_str = f.read()

            from .dagshell import FileSystem
            self.shell.fs = FileSystem.from_json(json_str)

            return f"State loaded from {filepath}"
        except Exception as e:
            return f"Load failed: {e}"

    def _slash_snapshot(self, args: List[str]) -> str:
        """Create named snapshot of current state."""
        if not args:
            return "Usage: /snapshot <name>"

        name = args[0]

        try:
            # Create snapshots directory if needed
            snapshots_dir = self.config.snapshots_directory
            os.makedirs(snapshots_dir, exist_ok=True)

            # Create snapshot filename with timestamp
            timestamp = datetime.now().strftime('%Y%m%d_%H%M%S')
            filename = f"{name}_{timestamp}.json"
            filepath = os.path.join(snapshots_dir, filename)

            # Save snapshot
            with builtins.open(filepath, 'w') as f:
                f.write(self.shell.fs.to_json())

            return f"Snapshot '{name}' saved to {filepath}"
        except Exception as e:
            return f"Snapshot failed: {e}"

    def _slash_snapshots(self, args: List[str]) -> str:
        """List available snapshots."""
        try:
            snapshots_dir = self.config.snapshots_directory

            if not os.path.exists(snapshots_dir):
                return "No snapshots found"

            # List snapshot files
            snapshots = []
            for filename in os.listdir(snapshots_dir):
                if filename.endswith('.json'):
                    filepath = os.path.join(snapshots_dir, filename)
                    stat = os.stat(filepath)
                    size = stat.st_size
                    mtime = datetime.fromtimestamp(stat.st_mtime).strftime('%Y-%m-%d %H:%M:%S')
                    snapshots.append(f"{filename:50s} {size:>10d} bytes  {mtime}")

            if not snapshots:
                return "No snapshots found"

            return "Available snapshots:\\n" + "\\n".join(snapshots)
        except Exception as e:
            return f"Error listing snapshots: {e}"

    def _slash_status(self, args: List[str]) -> str:
        """Show filesystem status."""
        fs = self.shell.fs

        # Count different types
        file_count = 0
        dir_count = 0
        total_size = 0

        for path in fs.paths:
            if path in fs.deleted:
                continue
            node_hash = fs.paths[path]
            node = fs.nodes[node_hash]

            if node.is_file():
                file_count += 1
                total_size += len(node.content)
            elif node.is_dir():
                dir_count += 1

        # Format size
        if total_size < 1024:
            size_str = f"{total_size} bytes"
        elif total_size < 1024 * 1024:
            size_str = f"{total_size / 1024:.1f} KB"
        else:
            size_str = f"{total_size / (1024 * 1024):.1f} MB"

        return f"""Filesystem Status:
  Total nodes:     {len(fs.nodes)}
  Files:           {file_count}
  Directories:     {dir_count}
  Total paths:     {len(fs.paths)}
  Deleted paths:   {len(fs.deleted)}
  Total size:      {size_str}
  Current dir:     {self.shell._cwd}
  Current user:    {self.current_user} (uid={self.uid}, gid={self.gid})"""

    def _slash_dag(self, args: List[str]) -> str:
        """Visualize DAG structure."""
        # Simple ASCII visualization
        fs = self.shell.fs

        lines = ["DAG Structure (content-addressed nodes):", ""]

        # Show a sample of nodes
        node_sample = list(fs.nodes.items())[:20]
        for node_hash, node in node_sample:
            node_type = "FILE" if node.is_file() else "DIR"
            paths = [p for p, h in fs.paths.items() if h == node_hash and p not in fs.deleted]
            paths_str = ", ".join(paths[:3])
            if len(paths) > 3:
                paths_str += f" ... ({len(paths)} total)"

            lines.append(f"  {node_hash[:12]}... [{node_type}] -> {paths_str}")

        if len(fs.nodes) > 20:
            lines.append(f"\\n  ... and {len(fs.nodes) - 20} more nodes")

        return "\\n".join(lines)

    def _slash_nodes(self, args: List[str]) -> str:
        """List nodes with optional pattern filter."""
        fs = self.shell.fs
        pattern = args[0] if args else None

        lines = ["Content-addressed nodes:", ""]

        for node_hash, node in fs.nodes.items():
            # Filter by pattern if provided
            if pattern and pattern.lower() not in node_hash.lower():
                continue

            node_type = "FILE" if node.is_file() else "DIR "
            size = len(node.content) if node.is_file() else 0
            mode = oct(node.mode & 0o777)

            paths = [p for p, h in fs.paths.items() if h == node_hash and p not in fs.deleted]
            if paths:
                paths_str = ", ".join(paths[:2])
                if len(paths) > 2:
                    paths_str += "..."
            else:
                paths_str = "(unreferenced)"

            lines.append(f"  {node_hash[:12]}  [{node_type}]  {mode:>6s}  {size:>8d}  {paths_str}")

        return "\\n".join(lines)

    def _slash_info(self, args: List[str]) -> str:
        """Show detailed node information."""
        if not args:
            return "Usage: /info <node-hash-prefix>"

        prefix = args[0]
        fs = self.shell.fs

        # Find matching nodes
        matches = [h for h in fs.nodes.keys() if h.startswith(prefix)]

        if not matches:
            return f"No node found matching: {prefix}"

        if len(matches) > 1:
            return f"Ambiguous prefix. Matches: {', '.join([m[:12] for m in matches])}"

        # Show node details
        node_hash = matches[0]
        node = fs.nodes[node_hash]

        lines = [
            f"Node Information:",
            f"  Hash:        {node_hash}",
            f"  Type:        {'FILE' if node.is_file() else 'DIRECTORY'}",
            f"  Mode:        {oct(node.mode)} ({oct(node.mode & 0o777)} permissions)",
            f"  Owner:       uid={node.uid}, gid={node.gid}",
            f"  Modified:    {datetime.fromtimestamp(node.mtime).strftime('%Y-%m-%d %H:%M:%S')}",
        ]

        if node.is_file():
            lines.append(f"  Size:        {len(node.content)} bytes")
            # Show first few bytes as preview
            preview = node.content[:100]
            try:
                preview_str = preview.decode('utf-8', errors='replace')
                lines.append(f"  Preview:     {repr(preview_str)}")
            except (UnicodeDecodeError, AttributeError):
                lines.append(f"  Preview:     (binary data)")

        # Show paths
        paths = [p for p, h in fs.paths.items() if h == node_hash]
        lines.append(f"  Paths ({len(paths)}):")
        for path in paths[:10]:
            deleted = " (deleted)" if path in fs.deleted else ""
            lines.append(f"    {path}{deleted}")
        if len(paths) > 10:
            lines.append(f"    ... and {len(paths) - 10} more")

        return "\\n".join(lines)

    def _slash_help(self, args: List[str]) -> str:
        """Show slash command help."""
        return """Slash Commands:

Host File Operations:
  /import <host-path> <dagshell-path>  Import file/directory from host
  /export <dagshell-path> <host-path>  Export file/directory to host

State Management:
  /save [filename]                     Save state to JSON (default: dagshell-state.json)
  /load <filename>                     Load state from JSON file
  /snapshot <name>                     Create named snapshot with timestamp
  /snapshots                           List available snapshots

DAG/Filesystem Inspection:
  /status                              Show filesystem statistics
  /dag                                 Visualize DAG structure
  /nodes [pattern]                     List nodes (optionally filtered by pattern)
  /info <node-hash>                    Show detailed node information

Meta Commands:
  /help                                Show this help
  /aliases                             List command aliases

Note: Host file operations require safe_host_directory to be configured."""

    def _slash_aliases(self, args: List[str]) -> str:
        """List command aliases."""
        return self.alias_manager.list_aliases()

    def _handle_alias_command(self, command_line: str) -> str:
        """Handle alias creation command."""
        parts = command_line.split(None, 1)
        if len(parts) < 2:
            return "alias: usage: alias name=command"
        alias_def = parts[1]
        if '=' not in alias_def:
            return "alias: usage: alias name=command"
        name, command = alias_def.split('=', 1)
        name = name.strip()
        command = command.strip()
        # Remove quotes if present
        if command.startswith('"') and command.endswith('"'):
            command = command[1:-1]
        elif command.startswith("'") and command.endswith("'"):
            command = command[1:-1]
        self.alias_manager.add(name, command)
        # Update tab completer
        if hasattr(self, 'tab_completer'):
            self.tab_completer.aliases = self.alias_manager.aliases
            self.tab_completer._command_cache = self.tab_completer._build_command_cache()
        return f"alias {name}='{command}'"

    def _handle_unalias_command(self, command_line: str) -> str:
        """Handle alias removal command."""
        parts = command_line.split()
        if len(parts) < 2:
            return "unalias: usage: unalias name"
        name = parts[1]
        if self.alias_manager.remove(name):
            if hasattr(self, 'tab_completer'):
                self.tab_completer.aliases = self.alias_manager.aliases
                self.tab_completer._command_cache = self.tab_completer._build_command_cache()
            return ''
        else:
            return f"unalias: {name}: not found"

    def get_prompt(self) -> str:
        """Generate the command prompt."""
        # Get current directory
        cwd = self.shell._cwd

        # Replace home directory with ~
        home = self.shell._env.get('HOME', '/home/user')
        if cwd.startswith(home):
            display_cwd = '~' + cwd[len(home):]
        else:
            display_cwd = cwd

        # Format prompt
        prompt = self.config.prompt_format.format(
            user=self.current_user,
            hostname=self.config.hostname,
            cwd=display_cwd,
            time=datetime.now().strftime('%H:%M:%S')
        )

        # Add colors if enabled
        if self.config.enable_colors:
            # Green for user@host, blue for path
            prompt = f'\\033[32m{self.config.user}@{self.config.hostname}\\033[0m:\\033[34m{display_cwd}\\033[0m$ '

        return prompt

    def execute_command(self, command_line: str) -> Optional[str]:
        """
        Execute a command line and return the output.

        Returns None for exit commands.
        Includes history expansion and alias support.
        """
        # Handle empty input
        if not command_line or command_line.strip() == '':
            return ''

        # Handle clear command
        if command_line.strip() == 'clear':
            print('\\033[2J\\033[H', end='')
            return ''

        # Handle history command
        if command_line.strip() == 'history':
            return self.history_manager.display()
        elif command_line.strip().startswith('history '):
            parts = command_line.split()
            if len(parts) > 1 and parts[1].isdigit():
                return self.history_manager.display(int(parts[1]))
            return self.history_manager.display()

        # Handle alias commands
        if command_line.strip() == 'alias':
            return self.alias_manager.list_aliases()
        elif command_line.strip().startswith('alias '):
            return self._handle_alias_command(command_line)
        elif command_line.strip().startswith('unalias '):
            return self._handle_unalias_command(command_line)

        # Store original for history
        original_command = command_line

        # Expand history references
        if self.config.enable_history_expansion:
            try:
                expanded = self.history_manager.expand(command_line)
                if expanded != command_line:
                    print(expanded)
                    command_line = expanded
            except ValueError as e:
                return str(e)

        # Expand aliases
        command_line = self.alias_manager.expand(command_line)

        # Check for slash commands first
        if command_line.strip().startswith('/'):
            return self._execute_slash_command(command_line.strip())

        # Parse the command
        command_group = self.parser.parse(command_line)

        # Check for special commands
        if command_group.pipelines and command_group.pipelines[0][0].commands:
            first_cmd = command_group.pipelines[0][0].commands[0]

            # Handle exit
            if first_cmd.name in ['exit', 'quit']:
                return None

            # Handle user management commands specially
            if first_cmd.name == 'whoami':
                return self.current_user
            elif first_cmd.name == 'su':
                username = first_cmd.args[0] if first_cmd.args else 'root'
                result = self._su(username)
                return result.text if result.text else ''
            elif first_cmd.name == 'export':
                if not first_cmd.args:
                    return 'export: missing target path'
                # export VAR=value sets an env var
                if '=' in first_cmd.args[0]:
                    var, _, val = first_cmd.args[0].partition('=')
                    self.shell.setenv(var.strip(), val.strip())
                    return ''
                result = self._export(first_cmd.args[0])
                return result.text

        # Execute the command normally
        result = self.executor.execute(command_group)

        # Add to history after successful execution (but not history expansion commands)
        if result is not None and not original_command.startswith('!'):
            self.history_manager.add(original_command)

        # Return the text output
        return result.text if result.text else str(result)

    def run_interactive(self):
        """Run the interactive REPL loop with readline support."""
        self.running = True

        # Print welcome message
        print("Welcome to DagShell Terminal")
        print("Features: History (\u2191/\u2193), Tab completion, Aliases, History expansion (!!, !n)")
        print("Type 'help' for commands, '/help' for slash commands, 'exit' to quit")
        print()

        while self.running:
            try:
                # Display prompt and get input (readline handles history navigation)
                prompt = self.get_prompt()
                command_line = input(prompt)

                # Execute command (history recording handled in execute_command)
                output = self.execute_command(command_line)

                # Check for exit
                if output is None:
                    break

                # Display output
                if output:
                    print(output)

            except KeyboardInterrupt:
                print("^C")
                continue
            except EOFError:
                print()
                break
            except Exception as e:
                print(f"Error: {e}")

        # Clean up
        self._cleanup()
        print("Goodbye!")

    def run_command(self, command_line: str) -> str:
        """
        Run a single command and return output.

        This method is useful for non-interactive use.
        """
        output = self.execute_command(command_line)
        return output if output is not None else ''

    def run_script(self, script_lines: List[str]) -> List[str]:
        """
        Run a script (list of command lines) and return outputs.
        """
        outputs = []
        for line in script_lines:
            # Skip comments and empty lines
            line = line.strip()
            if not line or line.startswith('#'):
                continue

            output = self.execute_command(line)
            if output is None:  # Exit command
                break
            outputs.append(output)

        return outputs


def main():
    """Main entry point for terminal emulator.

    Supports three modes:
    1. Interactive mode (default): Full shell session
    2. Command mode (-c): Execute a single command string
    3. One-shot mode: Execute command from positional arguments

    Examples:
        dagshell                           # Interactive mode
        dagshell -i                        # Explicit interactive mode
        dagshell -c "ls -la /home"         # Command mode
        dagshell ls -la /home              # One-shot mode
        dagshell --fs project.json ls /    # With filesystem file
        dagshell --fs project.json --save mkdir /new  # Save after write
    """
    import argparse

    parser = argparse.ArgumentParser(
        description='DagShell - Virtual POSIX filesystem with content-addressable DAG structure',
        formatter_class=argparse.RawDescriptionHelpFormatter,
        epilog='''
Examples:
  dagshell                              Start interactive shell
  dagshell ls -la /home                 One-shot command
  dagshell --fs project.json ls /       Use filesystem from file
  dagshell --fs project.json --save mkdir /new
                                        Save changes back to file
  dagshell -c "mkdir /a && touch /a/b"  Execute command string

Environment:
  DAGSHELL_FS    Default filesystem file (overridden by --fs)
'''
    )

    # Filesystem options
    parser.add_argument('--fs', metavar='FILE',
                        help='Load filesystem from JSON file (default: $DAGSHELL_FS or empty)')
    parser.add_argument('--save', '-s', action='store_true',
                        help='Save filesystem back to source file after execution')
    parser.add_argument('-o', '--output', metavar='FILE',
                        help='Save filesystem to FILE after execution')
    parser.add_argument('--json', action='store_true',
                        help='Output filesystem as JSON to stdout (for pipelines)')

    # Execution mode
    parser.add_argument('-c', '--command', metavar='CMD',
                        help='Execute command string and exit')
    parser.add_argument('-i', '--interactive', action='store_true',
                        help='Force interactive mode even with --fs')

    # Session options
    parser.add_argument('-u', '--user', help='Set username', default=_safe_getuser())
    parser.add_argument('-d', '--directory', help='Set initial directory', default='/')
    parser.add_argument('--no-history', action='store_true',
                        help='Disable history expansion')
    parser.add_argument('--no-completion', action='store_true',
                        help='Disable tab completion')

    # Positional arguments for one-shot mode
    parser.add_argument('args', nargs='*', help='Command and arguments for one-shot mode')

    args = parser.parse_args()

    # Determine filesystem source
    fs_file = args.fs or os.environ.get('DAGSHELL_FS')

    # Determine execution mode
    has_oneshot_cmd = len(args.args) > 0
    has_command_str = args.command is not None
    force_interactive = args.interactive

    # Create filesystem
    from .dagshell import FileSystem
    if fs_file and os.path.exists(fs_file):
        try:
            with open(fs_file, 'r') as f:
                fs = FileSystem.from_json(f.read())
        except Exception as e:
            print(f"dagshell: error loading {fs_file}: {e}", file=sys.stderr)
            sys.exit(1)
    else:
        fs = FileSystem()

    # Create shell with loaded filesystem
    from .dagshell_fluent import DagShell
    shell = DagShell(fs=fs)
    shell.cd(args.directory)

    # One-shot or command mode
    if has_oneshot_cmd or has_command_str:
        # Build command string
        if has_command_str:
            cmd_str = args.command
        else:
            # Join positional args, preserving quotes for args with spaces
            cmd_str = ' '.join(args.args)

        # Execute command
        executor = CommandExecutor(shell)
        parser_obj = CommandParser()

        try:
            parsed = parser_obj.parse(cmd_str)
            result = executor.execute(parsed)

            # Output result
            if args.json:
                # Output filesystem as JSON
                print(shell.fs.to_json())
            elif result and result.text:
                print(result.text)

            # Handle save options
            if args.save and fs_file:
                with open(fs_file, 'w') as f:
                    f.write(shell.fs.to_json())
            if args.output:
                with open(args.output, 'w') as f:
                    f.write(shell.fs.to_json())

            sys.exit(result.exit_code if result else 0)

        except Exception as e:
            print(f"dagshell: {e}", file=sys.stderr)
            sys.exit(1)

    # Interactive mode
    else:
        config = TerminalConfig(
            user=args.user,
            initial_dir=args.directory,
            enable_tab_completion=not args.no_completion,
            enable_history_expansion=not args.no_history
        )
        session = TerminalSession(config=config, fs=fs)
        session.run_interactive()

        # Save after interactive session if requested
        if args.save and fs_file:
            with open(fs_file, 'w') as f:
                f.write(session.shell.fs.to_json())
        if args.output:
            with open(args.output, 'w') as f:
                f.write(session.shell.fs.to_json())


if __name__ == '__main__':
    main()`};var n=[{title:"Lesson 1: Where Am I?",intro:["Every time you open a terminal, you land somewhere in the filesystem.","The filesystem is a tree of folders and files, starting at the root: /","","Let's find out where you are."],tasks:[{prompt:"Type `pwd` to see where you are.",validate:i=>i.includes("/home/user")},{prompt:"Type `ls` to see what's here.",validate:i=>i.includes("notes.txt")},{prompt:"Read notes.txt with `cat notes.txt`",validate:i=>i.includes("Welcome")},{prompt:"Go into projects with `cd projects`",validate:(i,t)=>t==="/home/user/projects"},{prompt:"Look around with `ls`",validate:i=>i.includes("README.md")},{prompt:"Go back with `cd ..`",validate:(i,t)=>t==="/home/user"}],philosophy:"The terminal is a conversation. You ask questions (commands), the computer answers (output). `pwd` = where am I? `ls` = what's here? `cd` = go there."},{title:"Lesson 2: Making Things",intro:["Now that you can look around, let's create something.","You'll make a directory, write files, and read them back."],tasks:[{prompt:"Create a journal directory: `mkdir journal`",validate:(i,t,e)=>e},{prompt:"Go into it: `cd journal`",validate:(i,t)=>t.includes("journal")},{prompt:'Write your first entry: `echo "Day 1: learned the terminal" > day1.txt`',validate:(i,t,e)=>e},{prompt:"Read it back: `cat day1.txt`",validate:i=>i.includes("Day 1")},{prompt:'Write another: `echo "Day 2: made my own files" > day2.txt`',validate:(i,t,e)=>e}],philosophy:"Every command is a verb. `mkdir` = make directory. `echo` = say something. `cat` = show me what's inside. The terminal is just English abbreviations."},{title:"Lesson 3: Moving and Copying",intro:["Files can be copied, renamed, and deleted.","But be careful: there is no trash can in the terminal."],tasks:[{prompt:"Copy day1.txt: `cp day1.txt backup.txt`",validate:(i,t,e)=>e},{prompt:"Rename it: `mv backup.txt day1_backup.txt`",validate:(i,t,e)=>e},{prompt:"Delete day2.txt: `rm day2.txt`",validate:(i,t,e)=>e},{prompt:"Go home: `cd ~`",validate:(i,t)=>t==="/home/user"},{prompt:"See everything: `tree projects`",validate:i=>i.includes("src")&&i.includes("hello.py")}],philosophy:"There is no trash can. `rm` is permanent. In a real terminal, this is the most important lesson: think before you delete."},{title:"Lesson 4: Finding and Filtering",intro:["The real power of the terminal is working with text.","Let's look at a server log file and learn to search it."],tasks:[{prompt:"Look at the server log: `cat projects/server.log`",validate:i=>i.includes("INFO")},{prompt:"See just the first 5 lines: `head -5 projects/server.log`",validate:i=>i.length>0},{prompt:"Find the errors: `grep ERROR projects/server.log`",validate:i=>i.includes("ERROR")},{prompt:"Count the log: `wc projects/server.log`",validate:i=>/\d+\s+\d+\s+\d+/.test(i)}],philosophy:"You just searched a log file without opening an editor. `grep` is one of the most powerful commands in Unix. It finds patterns in text. System administrators use it thousands of times a day."},{title:"Lesson 5: Pipes",intro:["Here's the secret that makes the terminal powerful:","you can connect commands together with | (the pipe).","The output of one command becomes the input of the next."],tasks:[{prompt:"Filter warnings: `cat projects/server.log | grep WARN`",validate:i=>i.includes("WARN")},{prompt:"Count the errors: `cat projects/server.log | grep ERROR | wc -l`",validate:i=>/\d+/.test(i)},{prompt:"Sort your journal: `ls ~/journal | sort`",validate:i=>i.length>0},{prompt:"Try combining any commands with `|`. Type `freeplay` when you're done.",validate:()=>!0}],philosophy:"This is the Unix philosophy: build small tools that do one thing well, then connect them with pipes. Each command is a building block. `|` is the glue."}],d=["","Congratulations: you just learned the terminal!","","In 5 lessons you navigated a filesystem, created files, searched","text, and composed tools with pipes. These same commands work on","every Linux server, every Mac, and every cloud machine in the world.","","The virtual filesystem you used is dagshell, a real","Python implementation running in your browser via WebAssembly.","","  github.com/queelius/dagshell    (the full source code)","  Type 'freeplay' to keep exploring, or 'reset' to start over."];var l=class{constructor(t,e){this.lessonIdx=0;this.taskIdx=0;this.freeplay=!1;this.terminal=t,this.bridge=e}async run(){for(this.startLesson(this.lessonIdx);;){let e=(await this.terminal.getInput()).trim();if(!e)continue;if(e==="freeplay"){this.freeplay=!0,this.terminal.writeLine("Freeplay mode. Type `lessons` to resume, `reset` to restart.","lesson");continue}if(e==="lessons"){this.freeplay=!1,this.printCurrentTask();continue}if(e==="reset"){await this.bridge.reset(),this.lessonIdx=0,this.taskIdx=0,this.freeplay=!1,this.terminal.clear(),this.startLesson(0);continue}let s=this.bridge.execute(e);s.text&&this.terminal.writeLine(s.text,s.exitCode!==0?"error":"normal"),this.terminal.setPrompt(this.makePrompt()),!this.freeplay&&this.lessonIdx<n.length&&this.checkValidation(s.text)}}startLesson(t){let e=n[t];this.terminal.writeLine("","normal"),this.terminal.writeLine(`-- ${e.title} --`,"success");for(let s of e.intro)this.terminal.writeLine(s,"lesson");this.terminal.writeLine("","normal"),this.printCurrentTask()}printCurrentTask(){if(this.lessonIdx>=n.length)return;let t=n[this.lessonIdx].tasks[this.taskIdx];this.terminal.writeLine(t.prompt,"lesson")}checkValidation(t){let e=n[this.lessonIdx],s=e.tasks[this.taskIdx],r=this.bridge.getCwd(),h=s.validate(t,r,!1);h||(h=this.checkFsValidation(this.lessonIdx,this.taskIdx)),h&&(this.taskIdx++,this.taskIdx>=e.tasks.length?(this.terminal.writeLine("","normal"),this.terminal.writeLine(`  ${e.philosophy}`,"philosophy"),this.terminal.writeLine("","normal"),this.lessonIdx++,this.taskIdx=0,this.lessonIdx<n.length?this.startLesson(this.lessonIdx):this.showCompletion()):(this.terminal.writeLine("","normal"),this.printCurrentTask()))}checkFsValidation(t,e){let s={"1-0":()=>this.bridge.fsExists("/home/user/journal"),"1-2":()=>this.bridge.fsExists("/home/user/journal/day1.txt"),"1-4":()=>this.bridge.fsExists("/home/user/journal/day2.txt"),"2-0":()=>this.bridge.fsExists(this.bridge.getCwd()+"/backup.txt"),"2-1":()=>this.bridge.fsExists(this.bridge.getCwd()+"/day1_backup.txt")&&!this.bridge.fsExists(this.bridge.getCwd()+"/backup.txt"),"2-2":()=>!this.bridge.fsExists(this.bridge.getCwd()+"/day2.txt")},r=`${t}-${e}`;return s[r]?.()??!1}showCompletion(){for(let t of d)this.terminal.writeLine(t,"success");this.freeplay=!0}makePrompt(){return`user@dagshell:${this.bridge.getCwd().replace("/home/user","~")||"~"}$ `}};async function p(){let i=document.getElementById("terminal-101");if(!i)return;let t=new o(i),e=new a;t.writeLine("Booting virtual filesystem...","lesson");try{await e.init(r=>{t.clear(),t.writeLine(r,"lesson")})}catch(r){t.writeLine("","normal"),t.writeLine("Could not load the terminal engine.","error"),t.writeLine(String(r),"error"),t.writeLine("Check your connection and refresh to try again.","error");return}t.clear(),t.setPrompt("user@dagshell:~$ "),t.enable(),new l(t,e).run()}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",p):p();})();

</script>
<p><em>The terminal above runs <a href="https://github.com/queelius/dagshell">dagshell</a>, a Python implementation of a POSIX filesystem, compiled to WebAssembly via <a href="https://pyodide.org">Pyodide</a>.</em></p>
]]></content:encoded><category>linux</category><category>terminal</category><category>interactive</category><category>tutorial</category></item></channel></rss>